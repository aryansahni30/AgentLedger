{
  "name": "agentledger-root",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean",
    "mcp": "node packages/mcp-server/dist/index.js"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
