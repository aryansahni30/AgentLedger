---
name: verify
description: Manually trigger AgentLedger verification — boundary check plus test commands. Use when the user wants to verify the current session's work.
---

Run this command in the terminal and show the full output:

cd "$CLAUDE_PROJECT_DIR" && agentledger verify
