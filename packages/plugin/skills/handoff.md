---
name: handoff
description: Generate an AgentLedger handoff document for the current run. Use when the user wants to resume context in a new session or hand off work to another agent.
---

Run this command in the terminal and show the full output:

cd "$CLAUDE_PROJECT_DIR" && agentledger handoff
