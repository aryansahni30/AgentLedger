#!/usr/bin/env node
var import_meta_url = require('url').pathToFileURL(__filename).href;
"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf, __hasOwnProp = Object.prototype.hasOwnProperty;
var __esm = (fn, res) => function() {
  return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
};
var __commonJS = (cb, mod) => function() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: !0 });
}, __copyProps = (to, from, except, desc) => {
  if (from && typeof from == "object" || typeof from == "function")
    for (let key of __getOwnPropNames(from))
      !__hasOwnProp.call(to, key) && key !== except && __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: !0 }) : target,
  mod
));

// ../../node_modules/.pnpm/graceful-fs@4.2.11/node_modules/graceful-fs/polyfills.js
var require_polyfills = __commonJS({
  "../../node_modules/.pnpm/graceful-fs@4.2.11/node_modules/graceful-fs/polyfills.js"(exports2, module2) {
    var constants2 = require("constants"), origCwd = process.cwd, cwd = null, platform = process.env.GRACEFUL_FS_PLATFORM || process.platform;
    process.cwd = function() {
      return cwd || (cwd = origCwd.call(process)), cwd;
    };
    try {
      process.cwd();
    } catch {
    }
    typeof process.chdir == "function" && (chdir = process.chdir, process.chdir = function(d) {
      cwd = null, chdir.call(process, d);
    }, Object.setPrototypeOf && Object.setPrototypeOf(process.chdir, chdir));
    var chdir;
    module2.exports = patch;
    function patch(fs8) {
      constants2.hasOwnProperty("O_SYMLINK") && process.version.match(/^v0\.6\.[0-2]|^v0\.5\./) && patchLchmod(fs8), fs8.lutimes || patchLutimes(fs8), fs8.chown = chownFix(fs8.chown), fs8.fchown = chownFix(fs8.fchown), fs8.lchown = chownFix(fs8.lchown), fs8.chmod = chmodFix(fs8.chmod), fs8.fchmod = chmodFix(fs8.fchmod), fs8.lchmod = chmodFix(fs8.lchmod), fs8.chownSync = chownFixSync(fs8.chownSync), fs8.fchownSync = chownFixSync(fs8.fchownSync), fs8.lchownSync = chownFixSync(fs8.lchownSync), fs8.chmodSync = chmodFixSync(fs8.chmodSync), fs8.fchmodSync = chmodFixSync(fs8.fchmodSync), fs8.lchmodSync = chmodFixSync(fs8.lchmodSync), fs8.stat = statFix(fs8.stat), fs8.fstat = statFix(fs8.fstat), fs8.lstat = statFix(fs8.lstat), fs8.statSync = statFixSync(fs8.statSync), fs8.fstatSync = statFixSync(fs8.fstatSync), fs8.lstatSync = statFixSync(fs8.lstatSync), fs8.chmod && !fs8.lchmod && (fs8.lchmod = function(path12, mode, cb) {
        cb && process.nextTick(cb);
      }, fs8.lchmodSync = function() {
      }), fs8.chown && !fs8.lchown && (fs8.lchown = function(path12, uid, gid, cb) {
        cb && process.nextTick(cb);
      }, fs8.lchownSync = function() {
      }), platform === "win32" && (fs8.rename = typeof fs8.rename != "function" ? fs8.rename : (function(fs$rename) {
        function rename3(from, to, cb) {
          var start = Date.now(), backoff3 = 0;
          fs$rename(from, to, function CB(er) {
            if (er && (er.code === "EACCES" || er.code === "EPERM" || er.code === "EBUSY") && Date.now() - start < 6e4) {
              setTimeout(function() {
                fs8.stat(to, function(stater, st) {
                  stater && stater.code === "ENOENT" ? fs$rename(from, to, CB) : cb(er);
                });
              }, backoff3), backoff3 < 100 && (backoff3 += 10);
              return;
            }
            cb && cb(er);
          });
        }
        return Object.setPrototypeOf && Object.setPrototypeOf(rename3, fs$rename), rename3;
      })(fs8.rename)), fs8.read = typeof fs8.read != "function" ? fs8.read : (function(fs$read) {
        function read(fd, buffer, offset, length, position, callback_) {
          var callback;
          if (callback_ && typeof callback_ == "function") {
            var eagCounter = 0;
            callback = function(er, _, __) {
              if (er && er.code === "EAGAIN" && eagCounter < 10)
                return eagCounter++, fs$read.call(fs8, fd, buffer, offset, length, position, callback);
              callback_.apply(this, arguments);
            };
          }
          return fs$read.call(fs8, fd, buffer, offset, length, position, callback);
        }
        return Object.setPrototypeOf && Object.setPrototypeOf(read, fs$read), read;
      })(fs8.read), fs8.readSync = typeof fs8.readSync != "function" ? fs8.readSync : /* @__PURE__ */ (function(fs$readSync) {
        return function(fd, buffer, offset, length, position) {
          for (var eagCounter = 0; ; )
            try {
              return fs$readSync.call(fs8, fd, buffer, offset, length, position);
            } catch (er) {
              if (er.code === "EAGAIN" && eagCounter < 10) {
                eagCounter++;
                continue;
              }
              throw er;
            }
        };
      })(fs8.readSync);
      function patchLchmod(fs9) {
        fs9.lchmod = function(path12, mode, callback) {
          fs9.open(
            path12,
            constants2.O_WRONLY | constants2.O_SYMLINK,
            mode,
            function(err, fd) {
              if (err) {
                callback && callback(err);
                return;
              }
              fs9.fchmod(fd, mode, function(err2) {
                fs9.close(fd, function(err22) {
                  callback && callback(err2 || err22);
                });
              });
            }
          );
        }, fs9.lchmodSync = function(path12, mode) {
          var fd = fs9.openSync(path12, constants2.O_WRONLY | constants2.O_SYMLINK, mode), threw = !0, ret;
          try {
            ret = fs9.fchmodSync(fd, mode), threw = !1;
          } finally {
            if (threw)
              try {
                fs9.closeSync(fd);
              } catch {
              }
            else
              fs9.closeSync(fd);
          }
          return ret;
        };
      }
      function patchLutimes(fs9) {
        constants2.hasOwnProperty("O_SYMLINK") && fs9.futimes ? (fs9.lutimes = function(path12, at, mt, cb) {
          fs9.open(path12, constants2.O_SYMLINK, function(er, fd) {
            if (er) {
              cb && cb(er);
              return;
            }
            fs9.futimes(fd, at, mt, function(er2) {
              fs9.close(fd, function(er22) {
                cb && cb(er2 || er22);
              });
            });
          });
        }, fs9.lutimesSync = function(path12, at, mt) {
          var fd = fs9.openSync(path12, constants2.O_SYMLINK), ret, threw = !0;
          try {
            ret = fs9.futimesSync(fd, at, mt), threw = !1;
          } finally {
            if (threw)
              try {
                fs9.closeSync(fd);
              } catch {
              }
            else
              fs9.closeSync(fd);
          }
          return ret;
        }) : fs9.futimes && (fs9.lutimes = function(_a5, _b, _c, cb) {
          cb && process.nextTick(cb);
        }, fs9.lutimesSync = function() {
        });
      }
      function chmodFix(orig) {
        return orig && function(target, mode, cb) {
          return orig.call(fs8, target, mode, function(er) {
            chownErOk(er) && (er = null), cb && cb.apply(this, arguments);
          });
        };
      }
      function chmodFixSync(orig) {
        return orig && function(target, mode) {
          try {
            return orig.call(fs8, target, mode);
          } catch (er) {
            if (!chownErOk(er)) throw er;
          }
        };
      }
      function chownFix(orig) {
        return orig && function(target, uid, gid, cb) {
          return orig.call(fs8, target, uid, gid, function(er) {
            chownErOk(er) && (er = null), cb && cb.apply(this, arguments);
          });
        };
      }
      function chownFixSync(orig) {
        return orig && function(target, uid, gid) {
          try {
            return orig.call(fs8, target, uid, gid);
          } catch (er) {
            if (!chownErOk(er)) throw er;
          }
        };
      }
      function statFix(orig) {
        return orig && function(target, options, cb) {
          typeof options == "function" && (cb = options, options = null);
          function callback(er, stats) {
            stats && (stats.uid < 0 && (stats.uid += 4294967296), stats.gid < 0 && (stats.gid += 4294967296)), cb && cb.apply(this, arguments);
          }
          return options ? orig.call(fs8, target, options, callback) : orig.call(fs8, target, callback);
        };
      }
      function statFixSync(orig) {
        return orig && function(target, options) {
          var stats = options ? orig.call(fs8, target, options) : orig.call(fs8, target);
          return stats && (stats.uid < 0 && (stats.uid += 4294967296), stats.gid < 0 && (stats.gid += 4294967296)), stats;
        };
      }
      function chownErOk(er) {
        if (!er || er.code === "ENOSYS")
          return !0;
        var nonroot = !process.getuid || process.getuid() !== 0;
        return !!(nonroot && (er.code === "EINVAL" || er.code === "EPERM"));
      }
    }
  }
});

// ../../node_modules/.pnpm/graceful-fs@4.2.11/node_modules/graceful-fs/legacy-streams.js
var require_legacy_streams = __commonJS({
  "../../node_modules/.pnpm/graceful-fs@4.2.11/node_modules/graceful-fs/legacy-streams.js"(exports2, module2) {
    var Stream3 = require("stream").Stream;
    module2.exports = legacy;
    function legacy(fs8) {
      return {
        ReadStream,
        WriteStream
      };
      function ReadStream(path12, options) {
        if (!(this instanceof ReadStream)) return new ReadStream(path12, options);
        Stream3.call(this);
        var self = this;
        this.path = path12, this.fd = null, this.readable = !0, this.paused = !1, this.flags = "r", this.mode = 438, this.bufferSize = 64 * 1024, options = options || {};
        for (var keys = Object.keys(options), index = 0, length = keys.length; index < length; index++) {
          var key = keys[index];
          this[key] = options[key];
        }
        if (this.encoding && this.setEncoding(this.encoding), this.start !== void 0) {
          if (typeof this.start != "number")
            throw TypeError("start must be a Number");
          if (this.end === void 0)
            this.end = 1 / 0;
          else if (typeof this.end != "number")
            throw TypeError("end must be a Number");
          if (this.start > this.end)
            throw new Error("start must be <= end");
          this.pos = this.start;
        }
        if (this.fd !== null) {
          process.nextTick(function() {
            self._read();
          });
          return;
        }
        fs8.open(this.path, this.flags, this.mode, function(err, fd) {
          if (err) {
            self.emit("error", err), self.readable = !1;
            return;
          }
          self.fd = fd, self.emit("open", fd), self._read();
        });
      }
      function WriteStream(path12, options) {
        if (!(this instanceof WriteStream)) return new WriteStream(path12, options);
        Stream3.call(this), this.path = path12, this.fd = null, this.writable = !0, this.flags = "w", this.encoding = "binary", this.mode = 438, this.bytesWritten = 0, options = options || {};
        for (var keys = Object.keys(options), index = 0, length = keys.length; index < length; index++) {
          var key = keys[index];
          this[key] = options[key];
        }
        if (this.start !== void 0) {
          if (typeof this.start != "number")
            throw TypeError("start must be a Number");
          if (this.start < 0)
            throw new Error("start must be >= zero");
          this.pos = this.start;
        }
        this.busy = !1, this._queue = [], this.fd === null && (this._open = fs8.open, this._queue.push([this._open, this.path, this.flags, this.mode, void 0]), this.flush());
      }
    }
  }
});

// ../../node_modules/.pnpm/graceful-fs@4.2.11/node_modules/graceful-fs/clone.js
var require_clone = __commonJS({
  "../../node_modules/.pnpm/graceful-fs@4.2.11/node_modules/graceful-fs/clone.js"(exports2, module2) {
    "use strict";
    module2.exports = clone;
    var getPrototypeOf = Object.getPrototypeOf || function(obj) {
      return obj.__proto__;
    };
    function clone(obj) {
      if (obj === null || typeof obj != "object")
        return obj;
      if (obj instanceof Object)
        var copy = { __proto__: getPrototypeOf(obj) };
      else
        var copy = /* @__PURE__ */ Object.create(null);
      return Object.getOwnPropertyNames(obj).forEach(function(key) {
        Object.defineProperty(copy, key, Object.getOwnPropertyDescriptor(obj, key));
      }), copy;
    }
  }
});

// ../../node_modules/.pnpm/graceful-fs@4.2.11/node_modules/graceful-fs/graceful-fs.js
var require_graceful_fs = __commonJS({
  "../../node_modules/.pnpm/graceful-fs@4.2.11/node_modules/graceful-fs/graceful-fs.js"(exports2, module2) {
    var fs8 = require("fs"), polyfills = require_polyfills(), legacy = require_legacy_streams(), clone = require_clone(), util2 = require("util"), gracefulQueue, previousSymbol;
    typeof Symbol == "function" && typeof Symbol.for == "function" ? (gracefulQueue = Symbol.for("graceful-fs.queue"), previousSymbol = Symbol.for("graceful-fs.previous")) : (gracefulQueue = "___graceful-fs.queue", previousSymbol = "___graceful-fs.previous");
    function noop3() {
    }
    function publishQueue(context, queue2) {
      Object.defineProperty(context, gracefulQueue, {
        get: function() {
          return queue2;
        }
      });
    }
    var debug = noop3;
    util2.debuglog ? debug = util2.debuglog("gfs4") : /\bgfs4\b/i.test(process.env.NODE_DEBUG || "") && (debug = function() {
      var m = util2.format.apply(util2, arguments);
      m = "GFS4: " + m.split(/\n/).join(`
GFS4: `), console.error(m);
    });
    fs8[gracefulQueue] || (queue = global[gracefulQueue] || [], publishQueue(fs8, queue), fs8.close = (function(fs$close) {
      function close(fd, cb) {
        return fs$close.call(fs8, fd, function(err) {
          err || resetQueue(), typeof cb == "function" && cb.apply(this, arguments);
        });
      }
      return Object.defineProperty(close, previousSymbol, {
        value: fs$close
      }), close;
    })(fs8.close), fs8.closeSync = (function(fs$closeSync) {
      function closeSync(fd) {
        fs$closeSync.apply(fs8, arguments), resetQueue();
      }
      return Object.defineProperty(closeSync, previousSymbol, {
        value: fs$closeSync
      }), closeSync;
    })(fs8.closeSync), /\bgfs4\b/i.test(process.env.NODE_DEBUG || "") && process.on("exit", function() {
      debug(fs8[gracefulQueue]), require("assert").equal(fs8[gracefulQueue].length, 0);
    }));
    var queue;
    global[gracefulQueue] || publishQueue(global, fs8[gracefulQueue]);
    module2.exports = patch(clone(fs8));
    process.env.TEST_GRACEFUL_FS_GLOBAL_PATCH && !fs8.__patched && (module2.exports = patch(fs8), fs8.__patched = !0);
    function patch(fs9) {
      polyfills(fs9), fs9.gracefulify = patch, fs9.createReadStream = createReadStream2, fs9.createWriteStream = createWriteStream2;
      var fs$readFile = fs9.readFile;
      fs9.readFile = readFile10;
      function readFile10(path12, options, cb) {
        return typeof options == "function" && (cb = options, options = null), go$readFile(path12, options, cb);
        function go$readFile(path13, options2, cb2, startTime) {
          return fs$readFile(path13, options2, function(err) {
            err && (err.code === "EMFILE" || err.code === "ENFILE") ? enqueue([go$readFile, [path13, options2, cb2], err, startTime || Date.now(), Date.now()]) : typeof cb2 == "function" && cb2.apply(this, arguments);
          });
        }
      }
      var fs$writeFile = fs9.writeFile;
      fs9.writeFile = writeFile8;
      function writeFile8(path12, data, options, cb) {
        return typeof options == "function" && (cb = options, options = null), go$writeFile(path12, data, options, cb);
        function go$writeFile(path13, data2, options2, cb2, startTime) {
          return fs$writeFile(path13, data2, options2, function(err) {
            err && (err.code === "EMFILE" || err.code === "ENFILE") ? enqueue([go$writeFile, [path13, data2, options2, cb2], err, startTime || Date.now(), Date.now()]) : typeof cb2 == "function" && cb2.apply(this, arguments);
          });
        }
      }
      var fs$appendFile = fs9.appendFile;
      fs$appendFile && (fs9.appendFile = appendFile2);
      function appendFile2(path12, data, options, cb) {
        return typeof options == "function" && (cb = options, options = null), go$appendFile(path12, data, options, cb);
        function go$appendFile(path13, data2, options2, cb2, startTime) {
          return fs$appendFile(path13, data2, options2, function(err) {
            err && (err.code === "EMFILE" || err.code === "ENFILE") ? enqueue([go$appendFile, [path13, data2, options2, cb2], err, startTime || Date.now(), Date.now()]) : typeof cb2 == "function" && cb2.apply(this, arguments);
          });
        }
      }
      var fs$copyFile = fs9.copyFile;
      fs$copyFile && (fs9.copyFile = copyFile);
      function copyFile(src, dest, flags, cb) {
        return typeof flags == "function" && (cb = flags, flags = 0), go$copyFile(src, dest, flags, cb);
        function go$copyFile(src2, dest2, flags2, cb2, startTime) {
          return fs$copyFile(src2, dest2, flags2, function(err) {
            err && (err.code === "EMFILE" || err.code === "ENFILE") ? enqueue([go$copyFile, [src2, dest2, flags2, cb2], err, startTime || Date.now(), Date.now()]) : typeof cb2 == "function" && cb2.apply(this, arguments);
          });
        }
      }
      var fs$readdir = fs9.readdir;
      fs9.readdir = readdir6;
      var noReaddirOptionVersions = /^v[0-5]\./;
      function readdir6(path12, options, cb) {
        typeof options == "function" && (cb = options, options = null);
        var go$readdir = noReaddirOptionVersions.test(process.version) ? function(path13, options2, cb2, startTime) {
          return fs$readdir(path13, fs$readdirCallback(
            path13,
            options2,
            cb2,
            startTime
          ));
        } : function(path13, options2, cb2, startTime) {
          return fs$readdir(path13, options2, fs$readdirCallback(
            path13,
            options2,
            cb2,
            startTime
          ));
        };
        return go$readdir(path12, options, cb);
        function fs$readdirCallback(path13, options2, cb2, startTime) {
          return function(err, files) {
            err && (err.code === "EMFILE" || err.code === "ENFILE") ? enqueue([
              go$readdir,
              [path13, options2, cb2],
              err,
              startTime || Date.now(),
              Date.now()
            ]) : (files && files.sort && files.sort(), typeof cb2 == "function" && cb2.call(this, err, files));
          };
        }
      }
      if (process.version.substr(0, 4) === "v0.8") {
        var legStreams = legacy(fs9);
        ReadStream = legStreams.ReadStream, WriteStream = legStreams.WriteStream;
      }
      var fs$ReadStream = fs9.ReadStream;
      fs$ReadStream && (ReadStream.prototype = Object.create(fs$ReadStream.prototype), ReadStream.prototype.open = ReadStream$open);
      var fs$WriteStream = fs9.WriteStream;
      fs$WriteStream && (WriteStream.prototype = Object.create(fs$WriteStream.prototype), WriteStream.prototype.open = WriteStream$open), Object.defineProperty(fs9, "ReadStream", {
        get: function() {
          return ReadStream;
        },
        set: function(val) {
          ReadStream = val;
        },
        enumerable: !0,
        configurable: !0
      }), Object.defineProperty(fs9, "WriteStream", {
        get: function() {
          return WriteStream;
        },
        set: function(val) {
          WriteStream = val;
        },
        enumerable: !0,
        configurable: !0
      });
      var FileReadStream = ReadStream;
      Object.defineProperty(fs9, "FileReadStream", {
        get: function() {
          return FileReadStream;
        },
        set: function(val) {
          FileReadStream = val;
        },
        enumerable: !0,
        configurable: !0
      });
      var FileWriteStream = WriteStream;
      Object.defineProperty(fs9, "FileWriteStream", {
        get: function() {
          return FileWriteStream;
        },
        set: function(val) {
          FileWriteStream = val;
        },
        enumerable: !0,
        configurable: !0
      });
      function ReadStream(path12, options) {
        return this instanceof ReadStream ? (fs$ReadStream.apply(this, arguments), this) : ReadStream.apply(Object.create(ReadStream.prototype), arguments);
      }
      function ReadStream$open() {
        var that = this;
        open3(that.path, that.flags, that.mode, function(err, fd) {
          err ? (that.autoClose && that.destroy(), that.emit("error", err)) : (that.fd = fd, that.emit("open", fd), that.read());
        });
      }
      function WriteStream(path12, options) {
        return this instanceof WriteStream ? (fs$WriteStream.apply(this, arguments), this) : WriteStream.apply(Object.create(WriteStream.prototype), arguments);
      }
      function WriteStream$open() {
        var that = this;
        open3(that.path, that.flags, that.mode, function(err, fd) {
          err ? (that.destroy(), that.emit("error", err)) : (that.fd = fd, that.emit("open", fd));
        });
      }
      function createReadStream2(path12, options) {
        return new fs9.ReadStream(path12, options);
      }
      function createWriteStream2(path12, options) {
        return new fs9.WriteStream(path12, options);
      }
      var fs$open = fs9.open;
      fs9.open = open3;
      function open3(path12, flags, mode, cb) {
        return typeof mode == "function" && (cb = mode, mode = null), go$open(path12, flags, mode, cb);
        function go$open(path13, flags2, mode2, cb2, startTime) {
          return fs$open(path13, flags2, mode2, function(err, fd) {
            err && (err.code === "EMFILE" || err.code === "ENFILE") ? enqueue([go$open, [path13, flags2, mode2, cb2], err, startTime || Date.now(), Date.now()]) : typeof cb2 == "function" && cb2.apply(this, arguments);
          });
        }
      }
      return fs9;
    }
    function enqueue(elem) {
      debug("ENQUEUE", elem[0].name, elem[1]), fs8[gracefulQueue].push(elem), retry();
    }
    var retryTimer;
    function resetQueue() {
      for (var now = Date.now(), i = 0; i < fs8[gracefulQueue].length; ++i)
        fs8[gracefulQueue][i].length > 2 && (fs8[gracefulQueue][i][3] = now, fs8[gracefulQueue][i][4] = now);
      retry();
    }
    function retry() {
      if (clearTimeout(retryTimer), retryTimer = void 0, fs8[gracefulQueue].length !== 0) {
        var elem = fs8[gracefulQueue].shift(), fn = elem[0], args = elem[1], err = elem[2], startTime = elem[3], lastTime = elem[4];
        if (startTime === void 0)
          debug("RETRY", fn.name, args), fn.apply(null, args);
        else if (Date.now() - startTime >= 6e4) {
          debug("TIMEOUT", fn.name, args);
          var cb = args.pop();
          typeof cb == "function" && cb.call(null, err);
        } else {
          var sinceAttempt = Date.now() - lastTime, sinceStart = Math.max(lastTime - startTime, 1), desiredDelay = Math.min(sinceStart * 1.2, 100);
          sinceAttempt >= desiredDelay ? (debug("RETRY", fn.name, args), fn.apply(null, args.concat([startTime]))) : fs8[gracefulQueue].push(elem);
        }
        retryTimer === void 0 && (retryTimer = setTimeout(retry, 0));
      }
    }
  }
});

// ../../node_modules/.pnpm/retry@0.12.0/node_modules/retry/lib/retry_operation.js
var require_retry_operation = __commonJS({
  "../../node_modules/.pnpm/retry@0.12.0/node_modules/retry/lib/retry_operation.js"(exports2, module2) {
    function RetryOperation(timeouts, options) {
      typeof options == "boolean" && (options = { forever: options }), this._originalTimeouts = JSON.parse(JSON.stringify(timeouts)), this._timeouts = timeouts, this._options = options || {}, this._maxRetryTime = options && options.maxRetryTime || 1 / 0, this._fn = null, this._errors = [], this._attempts = 1, this._operationTimeout = null, this._operationTimeoutCb = null, this._timeout = null, this._operationStart = null, this._options.forever && (this._cachedTimeouts = this._timeouts.slice(0));
    }
    module2.exports = RetryOperation;
    RetryOperation.prototype.reset = function() {
      this._attempts = 1, this._timeouts = this._originalTimeouts;
    };
    RetryOperation.prototype.stop = function() {
      this._timeout && clearTimeout(this._timeout), this._timeouts = [], this._cachedTimeouts = null;
    };
    RetryOperation.prototype.retry = function(err) {
      if (this._timeout && clearTimeout(this._timeout), !err)
        return !1;
      var currentTime = (/* @__PURE__ */ new Date()).getTime();
      if (err && currentTime - this._operationStart >= this._maxRetryTime)
        return this._errors.unshift(new Error("RetryOperation timeout occurred")), !1;
      this._errors.push(err);
      var timeout = this._timeouts.shift();
      if (timeout === void 0)
        if (this._cachedTimeouts)
          this._errors.splice(this._errors.length - 1, this._errors.length), this._timeouts = this._cachedTimeouts.slice(0), timeout = this._timeouts.shift();
        else
          return !1;
      var self = this, timer = setTimeout(function() {
        self._attempts++, self._operationTimeoutCb && (self._timeout = setTimeout(function() {
          self._operationTimeoutCb(self._attempts);
        }, self._operationTimeout), self._options.unref && self._timeout.unref()), self._fn(self._attempts);
      }, timeout);
      return this._options.unref && timer.unref(), !0;
    };
    RetryOperation.prototype.attempt = function(fn, timeoutOps) {
      this._fn = fn, timeoutOps && (timeoutOps.timeout && (this._operationTimeout = timeoutOps.timeout), timeoutOps.cb && (this._operationTimeoutCb = timeoutOps.cb));
      var self = this;
      this._operationTimeoutCb && (this._timeout = setTimeout(function() {
        self._operationTimeoutCb();
      }, self._operationTimeout)), this._operationStart = (/* @__PURE__ */ new Date()).getTime(), this._fn(this._attempts);
    };
    RetryOperation.prototype.try = function(fn) {
      console.log("Using RetryOperation.try() is deprecated"), this.attempt(fn);
    };
    RetryOperation.prototype.start = function(fn) {
      console.log("Using RetryOperation.start() is deprecated"), this.attempt(fn);
    };
    RetryOperation.prototype.start = RetryOperation.prototype.try;
    RetryOperation.prototype.errors = function() {
      return this._errors;
    };
    RetryOperation.prototype.attempts = function() {
      return this._attempts;
    };
    RetryOperation.prototype.mainError = function() {
      if (this._errors.length === 0)
        return null;
      for (var counts = {}, mainError = null, mainErrorCount = 0, i = 0; i < this._errors.length; i++) {
        var error = this._errors[i], message = error.message, count = (counts[message] || 0) + 1;
        counts[message] = count, count >= mainErrorCount && (mainError = error, mainErrorCount = count);
      }
      return mainError;
    };
  }
});

// ../../node_modules/.pnpm/retry@0.12.0/node_modules/retry/lib/retry.js
var require_retry = __commonJS({
  "../../node_modules/.pnpm/retry@0.12.0/node_modules/retry/lib/retry.js"(exports2) {
    var RetryOperation = require_retry_operation();
    exports2.operation = function(options) {
      var timeouts = exports2.timeouts(options);
      return new RetryOperation(timeouts, {
        forever: options && options.forever,
        unref: options && options.unref,
        maxRetryTime: options && options.maxRetryTime
      });
    };
    exports2.timeouts = function(options) {
      if (options instanceof Array)
        return [].concat(options);
      var opts = {
        retries: 10,
        factor: 2,
        minTimeout: 1 * 1e3,
        maxTimeout: 1 / 0,
        randomize: !1
      };
      for (var key in options)
        opts[key] = options[key];
      if (opts.minTimeout > opts.maxTimeout)
        throw new Error("minTimeout is greater than maxTimeout");
      for (var timeouts = [], i = 0; i < opts.retries; i++)
        timeouts.push(this.createTimeout(i, opts));
      return options && options.forever && !timeouts.length && timeouts.push(this.createTimeout(i, opts)), timeouts.sort(function(a, b) {
        return a - b;
      }), timeouts;
    };
    exports2.createTimeout = function(attempt, opts) {
      var random = opts.randomize ? Math.random() + 1 : 1, timeout = Math.round(random * opts.minTimeout * Math.pow(opts.factor, attempt));
      return timeout = Math.min(timeout, opts.maxTimeout), timeout;
    };
    exports2.wrap = function(obj, options, methods) {
      if (options instanceof Array && (methods = options, options = null), !methods) {
        methods = [];
        for (var key in obj)
          typeof obj[key] == "function" && methods.push(key);
      }
      for (var i = 0; i < methods.length; i++) {
        var method = methods[i], original = obj[method];
        obj[method] = function(original2) {
          var op = exports2.operation(options), args = Array.prototype.slice.call(arguments, 1), callback = args.pop();
          args.push(function(err) {
            op.retry(err) || (err && (arguments[0] = op.mainError()), callback.apply(this, arguments));
          }), op.attempt(function() {
            original2.apply(obj, args);
          });
        }.bind(obj, original), obj[method].options = options;
      }
    };
  }
});

// ../../node_modules/.pnpm/retry@0.12.0/node_modules/retry/index.js
var require_retry2 = __commonJS({
  "../../node_modules/.pnpm/retry@0.12.0/node_modules/retry/index.js"(exports2, module2) {
    module2.exports = require_retry();
  }
});

// ../../node_modules/.pnpm/signal-exit@3.0.7/node_modules/signal-exit/signals.js
var require_signals = __commonJS({
  "../../node_modules/.pnpm/signal-exit@3.0.7/node_modules/signal-exit/signals.js"(exports2, module2) {
    module2.exports = [
      "SIGABRT",
      "SIGALRM",
      "SIGHUP",
      "SIGINT",
      "SIGTERM"
    ];
    process.platform !== "win32" && module2.exports.push(
      "SIGVTALRM",
      "SIGXCPU",
      "SIGXFSZ",
      "SIGUSR2",
      "SIGTRAP",
      "SIGSYS",
      "SIGQUIT",
      "SIGIOT"
      // should detect profiler and enable/disable accordingly.
      // see #21
      // 'SIGPROF'
    );
    process.platform === "linux" && module2.exports.push(
      "SIGIO",
      "SIGPOLL",
      "SIGPWR",
      "SIGSTKFLT",
      "SIGUNUSED"
    );
  }
});

// ../../node_modules/.pnpm/signal-exit@3.0.7/node_modules/signal-exit/index.js
var require_signal_exit = __commonJS({
  "../../node_modules/.pnpm/signal-exit@3.0.7/node_modules/signal-exit/index.js"(exports2, module2) {
    var process2 = global.process, processOk = function(process3) {
      return process3 && typeof process3 == "object" && typeof process3.removeListener == "function" && typeof process3.emit == "function" && typeof process3.reallyExit == "function" && typeof process3.listeners == "function" && typeof process3.kill == "function" && typeof process3.pid == "number" && typeof process3.on == "function";
    };
    processOk(process2) ? (assert = require("assert"), signals = require_signals(), isWin = /^win/i.test(process2.platform), EE = require("events"), typeof EE != "function" && (EE = EE.EventEmitter), process2.__signal_exit_emitter__ ? emitter = process2.__signal_exit_emitter__ : (emitter = process2.__signal_exit_emitter__ = new EE(), emitter.count = 0, emitter.emitted = {}), emitter.infinite || (emitter.setMaxListeners(1 / 0), emitter.infinite = !0), module2.exports = function(cb, opts) {
      if (!processOk(global.process))
        return function() {
        };
      assert.equal(typeof cb, "function", "a callback must be provided for exit handler"), loaded === !1 && load();
      var ev = "exit";
      opts && opts.alwaysLast && (ev = "afterexit");
      var remove = function() {
        emitter.removeListener(ev, cb), emitter.listeners("exit").length === 0 && emitter.listeners("afterexit").length === 0 && unload();
      };
      return emitter.on(ev, cb), remove;
    }, unload = function() {
      !loaded || !processOk(global.process) || (loaded = !1, signals.forEach(function(sig) {
        try {
          process2.removeListener(sig, sigListeners[sig]);
        } catch {
        }
      }), process2.emit = originalProcessEmit, process2.reallyExit = originalProcessReallyExit, emitter.count -= 1);
    }, module2.exports.unload = unload, emit = function(event, code, signal) {
      emitter.emitted[event] || (emitter.emitted[event] = !0, emitter.emit(event, code, signal));
    }, sigListeners = {}, signals.forEach(function(sig) {
      sigListeners[sig] = function() {
        if (processOk(global.process)) {
          var listeners = process2.listeners(sig);
          listeners.length === emitter.count && (unload(), emit("exit", null, sig), emit("afterexit", null, sig), isWin && sig === "SIGHUP" && (sig = "SIGINT"), process2.kill(process2.pid, sig));
        }
      };
    }), module2.exports.signals = function() {
      return signals;
    }, loaded = !1, load = function() {
      loaded || !processOk(global.process) || (loaded = !0, emitter.count += 1, signals = signals.filter(function(sig) {
        try {
          return process2.on(sig, sigListeners[sig]), !0;
        } catch {
          return !1;
        }
      }), process2.emit = processEmit, process2.reallyExit = processReallyExit);
    }, module2.exports.load = load, originalProcessReallyExit = process2.reallyExit, processReallyExit = function(code) {
      processOk(global.process) && (process2.exitCode = code || /* istanbul ignore next */
      0, emit("exit", process2.exitCode, null), emit("afterexit", process2.exitCode, null), originalProcessReallyExit.call(process2, process2.exitCode));
    }, originalProcessEmit = process2.emit, processEmit = function(ev, arg) {
      if (ev === "exit" && processOk(global.process)) {
        arg !== void 0 && (process2.exitCode = arg);
        var ret = originalProcessEmit.apply(this, arguments);
        return emit("exit", process2.exitCode, null), emit("afterexit", process2.exitCode, null), ret;
      } else
        return originalProcessEmit.apply(this, arguments);
    }) : module2.exports = function() {
      return function() {
      };
    };
    var assert, signals, isWin, EE, emitter, unload, emit, sigListeners, loaded, load, originalProcessReallyExit, processReallyExit, originalProcessEmit, processEmit;
  }
});

// ../../node_modules/.pnpm/proper-lockfile@4.1.2/node_modules/proper-lockfile/lib/mtime-precision.js
var require_mtime_precision = __commonJS({
  "../../node_modules/.pnpm/proper-lockfile@4.1.2/node_modules/proper-lockfile/lib/mtime-precision.js"(exports2, module2) {
    "use strict";
    var cacheSymbol = Symbol();
    function probe(file, fs8, callback) {
      let cachedPrecision = fs8[cacheSymbol];
      if (cachedPrecision)
        return fs8.stat(file, (err, stat2) => {
          if (err)
            return callback(err);
          callback(null, stat2.mtime, cachedPrecision);
        });
      let mtime = new Date(Math.ceil(Date.now() / 1e3) * 1e3 + 5);
      fs8.utimes(file, mtime, mtime, (err) => {
        if (err)
          return callback(err);
        fs8.stat(file, (err2, stat2) => {
          if (err2)
            return callback(err2);
          let precision = stat2.mtime.getTime() % 1e3 === 0 ? "s" : "ms";
          Object.defineProperty(fs8, cacheSymbol, { value: precision }), callback(null, stat2.mtime, precision);
        });
      });
    }
    function getMtime(precision) {
      let now = Date.now();
      return precision === "s" && (now = Math.ceil(now / 1e3) * 1e3), new Date(now);
    }
    module2.exports.probe = probe;
    module2.exports.getMtime = getMtime;
  }
});

// ../../node_modules/.pnpm/proper-lockfile@4.1.2/node_modules/proper-lockfile/lib/lockfile.js
var require_lockfile = __commonJS({
  "../../node_modules/.pnpm/proper-lockfile@4.1.2/node_modules/proper-lockfile/lib/lockfile.js"(exports2, module2) {
    "use strict";
    var path12 = require("path"), fs8 = require_graceful_fs(), retry = require_retry2(), onExit = require_signal_exit(), mtimePrecision = require_mtime_precision(), locks = {};
    function getLockFile(file, options) {
      return options.lockfilePath || `${file}.lock`;
    }
    function resolveCanonicalPath(file, options, callback) {
      if (!options.realpath)
        return callback(null, path12.resolve(file));
      options.fs.realpath(file, callback);
    }
    function acquireLock(file, options, callback) {
      let lockfilePath = getLockFile(file, options);
      options.fs.mkdir(lockfilePath, (err) => {
        if (!err)
          return mtimePrecision.probe(lockfilePath, options.fs, (err2, mtime, mtimePrecision2) => {
            if (err2)
              return options.fs.rmdir(lockfilePath, () => {
              }), callback(err2);
            callback(null, mtime, mtimePrecision2);
          });
        if (err.code !== "EEXIST")
          return callback(err);
        if (options.stale <= 0)
          return callback(Object.assign(new Error("Lock file is already being held"), { code: "ELOCKED", file }));
        options.fs.stat(lockfilePath, (err2, stat2) => {
          if (err2)
            return err2.code === "ENOENT" ? acquireLock(file, { ...options, stale: 0 }, callback) : callback(err2);
          if (!isLockStale(stat2, options))
            return callback(Object.assign(new Error("Lock file is already being held"), { code: "ELOCKED", file }));
          removeLock(file, options, (err3) => {
            if (err3)
              return callback(err3);
            acquireLock(file, { ...options, stale: 0 }, callback);
          });
        });
      });
    }
    function isLockStale(stat2, options) {
      return stat2.mtime.getTime() < Date.now() - options.stale;
    }
    function removeLock(file, options, callback) {
      options.fs.rmdir(getLockFile(file, options), (err) => {
        if (err && err.code !== "ENOENT")
          return callback(err);
        callback();
      });
    }
    function updateLock(file, options) {
      let lock2 = locks[file];
      lock2.updateTimeout || (lock2.updateDelay = lock2.updateDelay || options.update, lock2.updateTimeout = setTimeout(() => {
        lock2.updateTimeout = null, options.fs.stat(lock2.lockfilePath, (err, stat2) => {
          let isOverThreshold = lock2.lastUpdate + options.stale < Date.now();
          if (err)
            return err.code === "ENOENT" || isOverThreshold ? setLockAsCompromised(file, lock2, Object.assign(err, { code: "ECOMPROMISED" })) : (lock2.updateDelay = 1e3, updateLock(file, options));
          if (!(lock2.mtime.getTime() === stat2.mtime.getTime()))
            return setLockAsCompromised(
              file,
              lock2,
              Object.assign(
                new Error("Unable to update lock within the stale threshold"),
                { code: "ECOMPROMISED" }
              )
            );
          let mtime = mtimePrecision.getMtime(lock2.mtimePrecision);
          options.fs.utimes(lock2.lockfilePath, mtime, mtime, (err2) => {
            let isOverThreshold2 = lock2.lastUpdate + options.stale < Date.now();
            if (!lock2.released) {
              if (err2)
                return err2.code === "ENOENT" || isOverThreshold2 ? setLockAsCompromised(file, lock2, Object.assign(err2, { code: "ECOMPROMISED" })) : (lock2.updateDelay = 1e3, updateLock(file, options));
              lock2.mtime = mtime, lock2.lastUpdate = Date.now(), lock2.updateDelay = null, updateLock(file, options);
            }
          });
        });
      }, lock2.updateDelay), lock2.updateTimeout.unref && lock2.updateTimeout.unref());
    }
    function setLockAsCompromised(file, lock2, err) {
      lock2.released = !0, lock2.updateTimeout && clearTimeout(lock2.updateTimeout), locks[file] === lock2 && delete locks[file], lock2.options.onCompromised(err);
    }
    function lock(file, options, callback) {
      options = {
        stale: 1e4,
        update: null,
        realpath: !0,
        retries: 0,
        fs: fs8,
        onCompromised: (err) => {
          throw err;
        },
        ...options
      }, options.retries = options.retries || 0, options.retries = typeof options.retries == "number" ? { retries: options.retries } : options.retries, options.stale = Math.max(options.stale || 0, 2e3), options.update = options.update == null ? options.stale / 2 : options.update || 0, options.update = Math.max(Math.min(options.update, options.stale / 2), 1e3), resolveCanonicalPath(file, options, (err, file2) => {
        if (err)
          return callback(err);
        let operation = retry.operation(options.retries);
        operation.attempt(() => {
          acquireLock(file2, options, (err2, mtime, mtimePrecision2) => {
            if (operation.retry(err2))
              return;
            if (err2)
              return callback(operation.mainError());
            let lock2 = locks[file2] = {
              lockfilePath: getLockFile(file2, options),
              mtime,
              mtimePrecision: mtimePrecision2,
              options,
              lastUpdate: Date.now()
            };
            updateLock(file2, options), callback(null, (releasedCallback) => {
              if (lock2.released)
                return releasedCallback && releasedCallback(Object.assign(new Error("Lock is already released"), { code: "ERELEASED" }));
              unlock(file2, { ...options, realpath: !1 }, releasedCallback);
            });
          });
        });
      });
    }
    function unlock(file, options, callback) {
      options = {
        fs: fs8,
        realpath: !0,
        ...options
      }, resolveCanonicalPath(file, options, (err, file2) => {
        if (err)
          return callback(err);
        let lock2 = locks[file2];
        if (!lock2)
          return callback(Object.assign(new Error("Lock is not acquired/owned by you"), { code: "ENOTACQUIRED" }));
        lock2.updateTimeout && clearTimeout(lock2.updateTimeout), lock2.released = !0, delete locks[file2], removeLock(file2, options, callback);
      });
    }
    function check(file, options, callback) {
      options = {
        stale: 1e4,
        realpath: !0,
        fs: fs8,
        ...options
      }, options.stale = Math.max(options.stale || 0, 2e3), resolveCanonicalPath(file, options, (err, file2) => {
        if (err)
          return callback(err);
        options.fs.stat(getLockFile(file2, options), (err2, stat2) => err2 ? err2.code === "ENOENT" ? callback(null, !1) : callback(err2) : callback(null, !isLockStale(stat2, options)));
      });
    }
    function getLocks() {
      return locks;
    }
    onExit(() => {
      for (let file in locks) {
        let options = locks[file].options;
        try {
          options.fs.rmdirSync(getLockFile(file, options));
        } catch {
        }
      }
    });
    module2.exports.lock = lock;
    module2.exports.unlock = unlock;
    module2.exports.check = check;
    module2.exports.getLocks = getLocks;
  }
});

// ../../node_modules/.pnpm/proper-lockfile@4.1.2/node_modules/proper-lockfile/lib/adapter.js
var require_adapter = __commonJS({
  "../../node_modules/.pnpm/proper-lockfile@4.1.2/node_modules/proper-lockfile/lib/adapter.js"(exports2, module2) {
    "use strict";
    var fs8 = require_graceful_fs();
    function createSyncFs(fs9) {
      let methods = ["mkdir", "realpath", "stat", "rmdir", "utimes"], newFs = { ...fs9 };
      return methods.forEach((method) => {
        newFs[method] = (...args) => {
          let callback = args.pop(), ret;
          try {
            ret = fs9[`${method}Sync`](...args);
          } catch (err) {
            return callback(err);
          }
          callback(null, ret);
        };
      }), newFs;
    }
    function toPromise(method) {
      return (...args) => new Promise((resolve4, reject) => {
        args.push((err, result) => {
          err ? reject(err) : resolve4(result);
        }), method(...args);
      });
    }
    function toSync(method) {
      return (...args) => {
        let err, result;
        if (args.push((_err, _result) => {
          err = _err, result = _result;
        }), method(...args), err)
          throw err;
        return result;
      };
    }
    function toSyncOptions(options) {
      if (options = { ...options }, options.fs = createSyncFs(options.fs || fs8), typeof options.retries == "number" && options.retries > 0 || options.retries && typeof options.retries.retries == "number" && options.retries.retries > 0)
        throw Object.assign(new Error("Cannot use retries with the sync api"), { code: "ESYNC" });
      return options;
    }
    module2.exports = {
      toPromise,
      toSync,
      toSyncOptions
    };
  }
});

// ../../node_modules/.pnpm/proper-lockfile@4.1.2/node_modules/proper-lockfile/index.js
var require_proper_lockfile = __commonJS({
  "../../node_modules/.pnpm/proper-lockfile@4.1.2/node_modules/proper-lockfile/index.js"(exports2, module2) {
    "use strict";
    var lockfile3 = require_lockfile(), { toPromise, toSync, toSyncOptions } = require_adapter();
    async function lock(file, options) {
      let release = await toPromise(lockfile3.lock)(file, options);
      return toPromise(release);
    }
    function lockSync(file, options) {
      let release = toSync(lockfile3.lock)(file, toSyncOptions(options));
      return toSync(release);
    }
    function unlock(file, options) {
      return toPromise(lockfile3.unlock)(file, options);
    }
    function unlockSync(file, options) {
      return toSync(lockfile3.unlock)(file, toSyncOptions(options));
    }
    function check(file, options) {
      return toPromise(lockfile3.check)(file, options);
    }
    function checkSync(file, options) {
      return toSync(lockfile3.check)(file, toSyncOptions(options));
    }
    module2.exports = lock;
    module2.exports.lock = lock;
    module2.exports.unlock = unlock;
    module2.exports.lockSync = lockSync;
    module2.exports.unlockSync = unlockSync;
    module2.exports.check = check;
    module2.exports.checkSync = checkSync;
  }
});

// ../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/helpers/util.js
var util, objectUtil, ZodParsedType, getParsedType, init_util = __esm({
  "../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/helpers/util.js"() {
    (function(util2) {
      util2.assertEqual = (_) => {
      };
      function assertIs(_arg) {
      }
      util2.assertIs = assertIs;
      function assertNever2(_x) {
        throw new Error();
      }
      util2.assertNever = assertNever2, util2.arrayToEnum = (items) => {
        let obj = {};
        for (let item of items)
          obj[item] = item;
        return obj;
      }, util2.getValidEnumValues = (obj) => {
        let validKeys = util2.objectKeys(obj).filter((k) => typeof obj[obj[k]] != "number"), filtered = {};
        for (let k of validKeys)
          filtered[k] = obj[k];
        return util2.objectValues(filtered);
      }, util2.objectValues = (obj) => util2.objectKeys(obj).map(function(e) {
        return obj[e];
      }), util2.objectKeys = typeof Object.keys == "function" ? (obj) => Object.keys(obj) : (object) => {
        let keys = [];
        for (let key in object)
          Object.prototype.hasOwnProperty.call(object, key) && keys.push(key);
        return keys;
      }, util2.find = (arr, checker) => {
        for (let item of arr)
          if (checker(item))
            return item;
      }, util2.isInteger = typeof Number.isInteger == "function" ? (val) => Number.isInteger(val) : (val) => typeof val == "number" && Number.isFinite(val) && Math.floor(val) === val;
      function joinValues(array, separator = " | ") {
        return array.map((val) => typeof val == "string" ? `'${val}'` : val).join(separator);
      }
      util2.joinValues = joinValues, util2.jsonStringifyReplacer = (_, value) => typeof value == "bigint" ? value.toString() : value;
    })(util || (util = {}));
    (function(objectUtil2) {
      objectUtil2.mergeShapes = (first, second) => ({
        ...first,
        ...second
        // second overwrites first
      });
    })(objectUtil || (objectUtil = {}));
    ZodParsedType = util.arrayToEnum([
      "string",
      "nan",
      "number",
      "integer",
      "float",
      "boolean",
      "date",
      "bigint",
      "symbol",
      "function",
      "undefined",
      "null",
      "array",
      "object",
      "unknown",
      "promise",
      "void",
      "never",
      "map",
      "set"
    ]), getParsedType = (data) => {
      switch (typeof data) {
        case "undefined":
          return ZodParsedType.undefined;
        case "string":
          return ZodParsedType.string;
        case "number":
          return Number.isNaN(data) ? ZodParsedType.nan : ZodParsedType.number;
        case "boolean":
          return ZodParsedType.boolean;
        case "function":
          return ZodParsedType.function;
        case "bigint":
          return ZodParsedType.bigint;
        case "symbol":
          return ZodParsedType.symbol;
        case "object":
          return Array.isArray(data) ? ZodParsedType.array : data === null ? ZodParsedType.null : data.then && typeof data.then == "function" && data.catch && typeof data.catch == "function" ? ZodParsedType.promise : typeof Map < "u" && data instanceof Map ? ZodParsedType.map : typeof Set < "u" && data instanceof Set ? ZodParsedType.set : typeof Date < "u" && data instanceof Date ? ZodParsedType.date : ZodParsedType.object;
        default:
          return ZodParsedType.unknown;
      }
    };
  }
});

// ../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/ZodError.js
var ZodIssueCode, quotelessJson, ZodError, init_ZodError = __esm({
  "../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/ZodError.js"() {
    init_util();
    ZodIssueCode = util.arrayToEnum([
      "invalid_type",
      "invalid_literal",
      "custom",
      "invalid_union",
      "invalid_union_discriminator",
      "invalid_enum_value",
      "unrecognized_keys",
      "invalid_arguments",
      "invalid_return_type",
      "invalid_date",
      "invalid_string",
      "too_small",
      "too_big",
      "invalid_intersection_types",
      "not_multiple_of",
      "not_finite"
    ]), quotelessJson = (obj) => JSON.stringify(obj, null, 2).replace(/"([^"]+)":/g, "$1:"), ZodError = class _ZodError extends Error {
      get errors() {
        return this.issues;
      }
      constructor(issues) {
        super(), this.issues = [], this.addIssue = (sub) => {
          this.issues = [...this.issues, sub];
        }, this.addIssues = (subs = []) => {
          this.issues = [...this.issues, ...subs];
        };
        let actualProto = new.target.prototype;
        Object.setPrototypeOf ? Object.setPrototypeOf(this, actualProto) : this.__proto__ = actualProto, this.name = "ZodError", this.issues = issues;
      }
      format(_mapper) {
        let mapper = _mapper || function(issue) {
          return issue.message;
        }, fieldErrors = { _errors: [] }, processError = (error) => {
          for (let issue of error.issues)
            if (issue.code === "invalid_union")
              issue.unionErrors.map(processError);
            else if (issue.code === "invalid_return_type")
              processError(issue.returnTypeError);
            else if (issue.code === "invalid_arguments")
              processError(issue.argumentsError);
            else if (issue.path.length === 0)
              fieldErrors._errors.push(mapper(issue));
            else {
              let curr = fieldErrors, i = 0;
              for (; i < issue.path.length; ) {
                let el = issue.path[i];
                i === issue.path.length - 1 ? (curr[el] = curr[el] || { _errors: [] }, curr[el]._errors.push(mapper(issue))) : curr[el] = curr[el] || { _errors: [] }, curr = curr[el], i++;
              }
            }
        };
        return processError(this), fieldErrors;
      }
      static assert(value) {
        if (!(value instanceof _ZodError))
          throw new Error(`Not a ZodError: ${value}`);
      }
      toString() {
        return this.message;
      }
      get message() {
        return JSON.stringify(this.issues, util.jsonStringifyReplacer, 2);
      }
      get isEmpty() {
        return this.issues.length === 0;
      }
      flatten(mapper = (issue) => issue.message) {
        let fieldErrors = {}, formErrors = [];
        for (let sub of this.issues)
          if (sub.path.length > 0) {
            let firstEl = sub.path[0];
            fieldErrors[firstEl] = fieldErrors[firstEl] || [], fieldErrors[firstEl].push(mapper(sub));
          } else
            formErrors.push(mapper(sub));
        return { formErrors, fieldErrors };
      }
      get formErrors() {
        return this.flatten();
      }
    };
    ZodError.create = (issues) => new ZodError(issues);
  }
});

// ../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/locales/en.js
var errorMap, en_default, init_en = __esm({
  "../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/locales/en.js"() {
    init_ZodError();
    init_util();
    errorMap = (issue, _ctx) => {
      let message;
      switch (issue.code) {
        case ZodIssueCode.invalid_type:
          issue.received === ZodParsedType.undefined ? message = "Required" : message = `Expected ${issue.expected}, received ${issue.received}`;
          break;
        case ZodIssueCode.invalid_literal:
          message = `Invalid literal value, expected ${JSON.stringify(issue.expected, util.jsonStringifyReplacer)}`;
          break;
        case ZodIssueCode.unrecognized_keys:
          message = `Unrecognized key(s) in object: ${util.joinValues(issue.keys, ", ")}`;
          break;
        case ZodIssueCode.invalid_union:
          message = "Invalid input";
          break;
        case ZodIssueCode.invalid_union_discriminator:
          message = `Invalid discriminator value. Expected ${util.joinValues(issue.options)}`;
          break;
        case ZodIssueCode.invalid_enum_value:
          message = `Invalid enum value. Expected ${util.joinValues(issue.options)}, received '${issue.received}'`;
          break;
        case ZodIssueCode.invalid_arguments:
          message = "Invalid function arguments";
          break;
        case ZodIssueCode.invalid_return_type:
          message = "Invalid function return type";
          break;
        case ZodIssueCode.invalid_date:
          message = "Invalid date";
          break;
        case ZodIssueCode.invalid_string:
          typeof issue.validation == "object" ? "includes" in issue.validation ? (message = `Invalid input: must include "${issue.validation.includes}"`, typeof issue.validation.position == "number" && (message = `${message} at one or more positions greater than or equal to ${issue.validation.position}`)) : "startsWith" in issue.validation ? message = `Invalid input: must start with "${issue.validation.startsWith}"` : "endsWith" in issue.validation ? message = `Invalid input: must end with "${issue.validation.endsWith}"` : util.assertNever(issue.validation) : issue.validation !== "regex" ? message = `Invalid ${issue.validation}` : message = "Invalid";
          break;
        case ZodIssueCode.too_small:
          issue.type === "array" ? message = `Array must contain ${issue.exact ? "exactly" : issue.inclusive ? "at least" : "more than"} ${issue.minimum} element(s)` : issue.type === "string" ? message = `String must contain ${issue.exact ? "exactly" : issue.inclusive ? "at least" : "over"} ${issue.minimum} character(s)` : issue.type === "number" ? message = `Number must be ${issue.exact ? "exactly equal to " : issue.inclusive ? "greater than or equal to " : "greater than "}${issue.minimum}` : issue.type === "bigint" ? message = `Number must be ${issue.exact ? "exactly equal to " : issue.inclusive ? "greater than or equal to " : "greater than "}${issue.minimum}` : issue.type === "date" ? message = `Date must be ${issue.exact ? "exactly equal to " : issue.inclusive ? "greater than or equal to " : "greater than "}${new Date(Number(issue.minimum))}` : message = "Invalid input";
          break;
        case ZodIssueCode.too_big:
          issue.type === "array" ? message = `Array must contain ${issue.exact ? "exactly" : issue.inclusive ? "at most" : "less than"} ${issue.maximum} element(s)` : issue.type === "string" ? message = `String must contain ${issue.exact ? "exactly" : issue.inclusive ? "at most" : "under"} ${issue.maximum} character(s)` : issue.type === "number" ? message = `Number must be ${issue.exact ? "exactly" : issue.inclusive ? "less than or equal to" : "less than"} ${issue.maximum}` : issue.type === "bigint" ? message = `BigInt must be ${issue.exact ? "exactly" : issue.inclusive ? "less than or equal to" : "less than"} ${issue.maximum}` : issue.type === "date" ? message = `Date must be ${issue.exact ? "exactly" : issue.inclusive ? "smaller than or equal to" : "smaller than"} ${new Date(Number(issue.maximum))}` : message = "Invalid input";
          break;
        case ZodIssueCode.custom:
          message = "Invalid input";
          break;
        case ZodIssueCode.invalid_intersection_types:
          message = "Intersection results could not be merged";
          break;
        case ZodIssueCode.not_multiple_of:
          message = `Number must be a multiple of ${issue.multipleOf}`;
          break;
        case ZodIssueCode.not_finite:
          message = "Number must be finite";
          break;
        default:
          message = _ctx.defaultError, util.assertNever(issue);
      }
      return { message };
    }, en_default = errorMap;
  }
});

// ../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/errors.js
function setErrorMap(map) {
  overrideErrorMap = map;
}
function getErrorMap() {
  return overrideErrorMap;
}
var overrideErrorMap, init_errors = __esm({
  "../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/errors.js"() {
    init_en();
    overrideErrorMap = en_default;
  }
});

// ../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/helpers/parseUtil.js
function addIssueToContext(ctx, issueData) {
  let overrideMap = getErrorMap(), issue = makeIssue({
    issueData,
    data: ctx.data,
    path: ctx.path,
    errorMaps: [
      ctx.common.contextualErrorMap,
      // contextual error map is first priority
      ctx.schemaErrorMap,
      // then schema-bound map if available
      overrideMap,
      // then global override map
      overrideMap === en_default ? void 0 : en_default
      // then global default map
    ].filter((x) => !!x)
  });
  ctx.common.issues.push(issue);
}
var makeIssue, EMPTY_PATH, ParseStatus, INVALID, DIRTY, OK, isAborted, isDirty, isValid, isAsync, init_parseUtil = __esm({
  "../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/helpers/parseUtil.js"() {
    init_errors();
    init_en();
    makeIssue = (params) => {
      let { data, path: path12, errorMaps, issueData } = params, fullPath = [...path12, ...issueData.path || []], fullIssue = {
        ...issueData,
        path: fullPath
      };
      if (issueData.message !== void 0)
        return {
          ...issueData,
          path: fullPath,
          message: issueData.message
        };
      let errorMessage = "", maps = errorMaps.filter((m) => !!m).slice().reverse();
      for (let map of maps)
        errorMessage = map(fullIssue, { data, defaultError: errorMessage }).message;
      return {
        ...issueData,
        path: fullPath,
        message: errorMessage
      };
    }, EMPTY_PATH = [];
    ParseStatus = class _ParseStatus {
      constructor() {
        this.value = "valid";
      }
      dirty() {
        this.value === "valid" && (this.value = "dirty");
      }
      abort() {
        this.value !== "aborted" && (this.value = "aborted");
      }
      static mergeArray(status, results) {
        let arrayValue = [];
        for (let s of results) {
          if (s.status === "aborted")
            return INVALID;
          s.status === "dirty" && status.dirty(), arrayValue.push(s.value);
        }
        return { status: status.value, value: arrayValue };
      }
      static async mergeObjectAsync(status, pairs) {
        let syncPairs = [];
        for (let pair of pairs) {
          let key = await pair.key, value = await pair.value;
          syncPairs.push({
            key,
            value
          });
        }
        return _ParseStatus.mergeObjectSync(status, syncPairs);
      }
      static mergeObjectSync(status, pairs) {
        let finalObject = {};
        for (let pair of pairs) {
          let { key, value } = pair;
          if (key.status === "aborted" || value.status === "aborted")
            return INVALID;
          key.status === "dirty" && status.dirty(), value.status === "dirty" && status.dirty(), key.value !== "__proto__" && (typeof value.value < "u" || pair.alwaysSet) && (finalObject[key.value] = value.value);
        }
        return { status: status.value, value: finalObject };
      }
    }, INVALID = Object.freeze({
      status: "aborted"
    }), DIRTY = (value) => ({ status: "dirty", value }), OK = (value) => ({ status: "valid", value }), isAborted = (x) => x.status === "aborted", isDirty = (x) => x.status === "dirty", isValid = (x) => x.status === "valid", isAsync = (x) => typeof Promise < "u" && x instanceof Promise;
  }
});

// ../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/helpers/typeAliases.js
var init_typeAliases = __esm({
  "../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/helpers/typeAliases.js"() {
  }
});

// ../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/helpers/errorUtil.js
var errorUtil, init_errorUtil = __esm({
  "../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/helpers/errorUtil.js"() {
    (function(errorUtil2) {
      errorUtil2.errToObj = (message) => typeof message == "string" ? { message } : message || {}, errorUtil2.toString = (message) => typeof message == "string" ? message : message?.message;
    })(errorUtil || (errorUtil = {}));
  }
});

// ../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/types.js
function processCreateParams(params) {
  if (!params)
    return {};
  let { errorMap: errorMap2, invalid_type_error, required_error, description } = params;
  if (errorMap2 && (invalid_type_error || required_error))
    throw new Error(`Can't use "invalid_type_error" or "required_error" in conjunction with custom error map.`);
  return errorMap2 ? { errorMap: errorMap2, description } : { errorMap: (iss, ctx) => {
    let { message } = params;
    return iss.code === "invalid_enum_value" ? { message: message ?? ctx.defaultError } : typeof ctx.data > "u" ? { message: message ?? required_error ?? ctx.defaultError } : iss.code !== "invalid_type" ? { message: ctx.defaultError } : { message: message ?? invalid_type_error ?? ctx.defaultError };
  }, description };
}
function timeRegexSource(args) {
  let secondsRegexSource = "[0-5]\\d";
  args.precision ? secondsRegexSource = `${secondsRegexSource}\\.\\d{${args.precision}}` : args.precision == null && (secondsRegexSource = `${secondsRegexSource}(\\.\\d+)?`);
  let secondsQuantifier = args.precision ? "+" : "?";
  return `([01]\\d|2[0-3]):[0-5]\\d(:${secondsRegexSource})${secondsQuantifier}`;
}
function timeRegex(args) {
  return new RegExp(`^${timeRegexSource(args)}$`);
}
function datetimeRegex(args) {
  let regex = `${dateRegexSource}T${timeRegexSource(args)}`, opts = [];
  return opts.push(args.local ? "Z?" : "Z"), args.offset && opts.push("([+-]\\d{2}:?\\d{2})"), regex = `${regex}(${opts.join("|")})`, new RegExp(`^${regex}$`);
}
function isValidIP(ip, version) {
  return !!((version === "v4" || !version) && ipv4Regex.test(ip) || (version === "v6" || !version) && ipv6Regex.test(ip));
}
function isValidJWT(jwt, alg) {
  if (!jwtRegex.test(jwt))
    return !1;
  try {
    let [header] = jwt.split(".");
    if (!header)
      return !1;
    let base64 = header.replace(/-/g, "+").replace(/_/g, "/").padEnd(header.length + (4 - header.length % 4) % 4, "="), decoded = JSON.parse(atob(base64));
    return !(typeof decoded != "object" || decoded === null || "typ" in decoded && decoded?.typ !== "JWT" || !decoded.alg || alg && decoded.alg !== alg);
  } catch {
    return !1;
  }
}
function isValidCidr(ip, version) {
  return !!((version === "v4" || !version) && ipv4CidrRegex.test(ip) || (version === "v6" || !version) && ipv6CidrRegex.test(ip));
}
function floatSafeRemainder(val, step) {
  let valDecCount = (val.toString().split(".")[1] || "").length, stepDecCount = (step.toString().split(".")[1] || "").length, decCount = valDecCount > stepDecCount ? valDecCount : stepDecCount, valInt = Number.parseInt(val.toFixed(decCount).replace(".", "")), stepInt = Number.parseInt(step.toFixed(decCount).replace(".", ""));
  return valInt % stepInt / 10 ** decCount;
}
function deepPartialify(schema) {
  if (schema instanceof ZodObject) {
    let newShape = {};
    for (let key in schema.shape) {
      let fieldSchema = schema.shape[key];
      newShape[key] = ZodOptional.create(deepPartialify(fieldSchema));
    }
    return new ZodObject({
      ...schema._def,
      shape: () => newShape
    });
  } else return schema instanceof ZodArray ? new ZodArray({
    ...schema._def,
    type: deepPartialify(schema.element)
  }) : schema instanceof ZodOptional ? ZodOptional.create(deepPartialify(schema.unwrap())) : schema instanceof ZodNullable ? ZodNullable.create(deepPartialify(schema.unwrap())) : schema instanceof ZodTuple ? ZodTuple.create(schema.items.map((item) => deepPartialify(item))) : schema;
}
function mergeValues(a, b) {
  let aType = getParsedType(a), bType = getParsedType(b);
  if (a === b)
    return { valid: !0, data: a };
  if (aType === ZodParsedType.object && bType === ZodParsedType.object) {
    let bKeys = util.objectKeys(b), sharedKeys = util.objectKeys(a).filter((key) => bKeys.indexOf(key) !== -1), newObj = { ...a, ...b };
    for (let key of sharedKeys) {
      let sharedValue = mergeValues(a[key], b[key]);
      if (!sharedValue.valid)
        return { valid: !1 };
      newObj[key] = sharedValue.data;
    }
    return { valid: !0, data: newObj };
  } else if (aType === ZodParsedType.array && bType === ZodParsedType.array) {
    if (a.length !== b.length)
      return { valid: !1 };
    let newArray = [];
    for (let index = 0; index < a.length; index++) {
      let itemA = a[index], itemB = b[index], sharedValue = mergeValues(itemA, itemB);
      if (!sharedValue.valid)
        return { valid: !1 };
      newArray.push(sharedValue.data);
    }
    return { valid: !0, data: newArray };
  } else return aType === ZodParsedType.date && bType === ZodParsedType.date && +a == +b ? { valid: !0, data: a } : { valid: !1 };
}
function createZodEnum(values, params) {
  return new ZodEnum({
    values,
    typeName: ZodFirstPartyTypeKind.ZodEnum,
    ...processCreateParams(params)
  });
}
function cleanParams(params, data) {
  let p = typeof params == "function" ? params(data) : typeof params == "string" ? { message: params } : params;
  return typeof p == "string" ? { message: p } : p;
}
function custom(check, _params = {}, fatal) {
  return check ? ZodAny.create().superRefine((data, ctx) => {
    let r = check(data);
    if (r instanceof Promise)
      return r.then((r2) => {
        if (!r2) {
          let params = cleanParams(_params, data), _fatal = params.fatal ?? fatal ?? !0;
          ctx.addIssue({ code: "custom", ...params, fatal: _fatal });
        }
      });
    if (!r) {
      let params = cleanParams(_params, data), _fatal = params.fatal ?? fatal ?? !0;
      ctx.addIssue({ code: "custom", ...params, fatal: _fatal });
    }
  }) : ZodAny.create();
}
var ParseInputLazyPath, handleResult, ZodType, cuidRegex, cuid2Regex, ulidRegex, uuidRegex, nanoidRegex, jwtRegex, durationRegex, emailRegex, _emojiRegex, emojiRegex, ipv4Regex, ipv4CidrRegex, ipv6Regex, ipv6CidrRegex, base64Regex, base64urlRegex, dateRegexSource, dateRegex, ZodString, ZodNumber, ZodBigInt, ZodBoolean, ZodDate, ZodSymbol, ZodUndefined, ZodNull, ZodAny, ZodUnknown, ZodNever, ZodVoid, ZodArray, ZodObject, ZodUnion, getDiscriminator, ZodDiscriminatedUnion, ZodIntersection, ZodTuple, ZodRecord, ZodMap, ZodSet, ZodFunction, ZodLazy, ZodLiteral, ZodEnum, ZodNativeEnum, ZodPromise, ZodEffects, ZodOptional, ZodNullable, ZodDefault, ZodCatch, ZodNaN, BRAND, ZodBranded, ZodPipeline, ZodReadonly, late, ZodFirstPartyTypeKind, instanceOfType, stringType, numberType, nanType, bigIntType, booleanType, dateType, symbolType, undefinedType, nullType, anyType, unknownType, neverType, voidType, arrayType, objectType, strictObjectType, unionType, discriminatedUnionType, intersectionType, tupleType, recordType, mapType, setType, functionType, lazyType, literalType, enumType, nativeEnumType, promiseType, effectsType, optionalType, nullableType, preprocessType, pipelineType, ostring, onumber, oboolean, coerce, NEVER, init_types = __esm({
  "../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/types.js"() {
    init_ZodError();
    init_errors();
    init_errorUtil();
    init_parseUtil();
    init_util();
    ParseInputLazyPath = class {
      constructor(parent, value, path12, key) {
        this._cachedPath = [], this.parent = parent, this.data = value, this._path = path12, this._key = key;
      }
      get path() {
        return this._cachedPath.length || (Array.isArray(this._key) ? this._cachedPath.push(...this._path, ...this._key) : this._cachedPath.push(...this._path, this._key)), this._cachedPath;
      }
    }, handleResult = (ctx, result) => {
      if (isValid(result))
        return { success: !0, data: result.value };
      if (!ctx.common.issues.length)
        throw new Error("Validation failed but no issues detected.");
      return {
        success: !1,
        get error() {
          if (this._error)
            return this._error;
          let error = new ZodError(ctx.common.issues);
          return this._error = error, this._error;
        }
      };
    };
    ZodType = class {
      get description() {
        return this._def.description;
      }
      _getType(input) {
        return getParsedType(input.data);
      }
      _getOrReturnCtx(input, ctx) {
        return ctx || {
          common: input.parent.common,
          data: input.data,
          parsedType: getParsedType(input.data),
          schemaErrorMap: this._def.errorMap,
          path: input.path,
          parent: input.parent
        };
      }
      _processInputParams(input) {
        return {
          status: new ParseStatus(),
          ctx: {
            common: input.parent.common,
            data: input.data,
            parsedType: getParsedType(input.data),
            schemaErrorMap: this._def.errorMap,
            path: input.path,
            parent: input.parent
          }
        };
      }
      _parseSync(input) {
        let result = this._parse(input);
        if (isAsync(result))
          throw new Error("Synchronous parse encountered promise.");
        return result;
      }
      _parseAsync(input) {
        let result = this._parse(input);
        return Promise.resolve(result);
      }
      parse(data, params) {
        let result = this.safeParse(data, params);
        if (result.success)
          return result.data;
        throw result.error;
      }
      safeParse(data, params) {
        let ctx = {
          common: {
            issues: [],
            async: params?.async ?? !1,
            contextualErrorMap: params?.errorMap
          },
          path: params?.path || [],
          schemaErrorMap: this._def.errorMap,
          parent: null,
          data,
          parsedType: getParsedType(data)
        }, result = this._parseSync({ data, path: ctx.path, parent: ctx });
        return handleResult(ctx, result);
      }
      "~validate"(data) {
        let ctx = {
          common: {
            issues: [],
            async: !!this["~standard"].async
          },
          path: [],
          schemaErrorMap: this._def.errorMap,
          parent: null,
          data,
          parsedType: getParsedType(data)
        };
        if (!this["~standard"].async)
          try {
            let result = this._parseSync({ data, path: [], parent: ctx });
            return isValid(result) ? {
              value: result.value
            } : {
              issues: ctx.common.issues
            };
          } catch (err) {
            err?.message?.toLowerCase()?.includes("encountered") && (this["~standard"].async = !0), ctx.common = {
              issues: [],
              async: !0
            };
          }
        return this._parseAsync({ data, path: [], parent: ctx }).then((result) => isValid(result) ? {
          value: result.value
        } : {
          issues: ctx.common.issues
        });
      }
      async parseAsync(data, params) {
        let result = await this.safeParseAsync(data, params);
        if (result.success)
          return result.data;
        throw result.error;
      }
      async safeParseAsync(data, params) {
        let ctx = {
          common: {
            issues: [],
            contextualErrorMap: params?.errorMap,
            async: !0
          },
          path: params?.path || [],
          schemaErrorMap: this._def.errorMap,
          parent: null,
          data,
          parsedType: getParsedType(data)
        }, maybeAsyncResult = this._parse({ data, path: ctx.path, parent: ctx }), result = await (isAsync(maybeAsyncResult) ? maybeAsyncResult : Promise.resolve(maybeAsyncResult));
        return handleResult(ctx, result);
      }
      refine(check, message) {
        let getIssueProperties = (val) => typeof message == "string" || typeof message > "u" ? { message } : typeof message == "function" ? message(val) : message;
        return this._refinement((val, ctx) => {
          let result = check(val), setError = () => ctx.addIssue({
            code: ZodIssueCode.custom,
            ...getIssueProperties(val)
          });
          return typeof Promise < "u" && result instanceof Promise ? result.then((data) => data ? !0 : (setError(), !1)) : result ? !0 : (setError(), !1);
        });
      }
      refinement(check, refinementData) {
        return this._refinement((val, ctx) => check(val) ? !0 : (ctx.addIssue(typeof refinementData == "function" ? refinementData(val, ctx) : refinementData), !1));
      }
      _refinement(refinement) {
        return new ZodEffects({
          schema: this,
          typeName: ZodFirstPartyTypeKind.ZodEffects,
          effect: { type: "refinement", refinement }
        });
      }
      superRefine(refinement) {
        return this._refinement(refinement);
      }
      constructor(def) {
        this.spa = this.safeParseAsync, this._def = def, this.parse = this.parse.bind(this), this.safeParse = this.safeParse.bind(this), this.parseAsync = this.parseAsync.bind(this), this.safeParseAsync = this.safeParseAsync.bind(this), this.spa = this.spa.bind(this), this.refine = this.refine.bind(this), this.refinement = this.refinement.bind(this), this.superRefine = this.superRefine.bind(this), this.optional = this.optional.bind(this), this.nullable = this.nullable.bind(this), this.nullish = this.nullish.bind(this), this.array = this.array.bind(this), this.promise = this.promise.bind(this), this.or = this.or.bind(this), this.and = this.and.bind(this), this.transform = this.transform.bind(this), this.brand = this.brand.bind(this), this.default = this.default.bind(this), this.catch = this.catch.bind(this), this.describe = this.describe.bind(this), this.pipe = this.pipe.bind(this), this.readonly = this.readonly.bind(this), this.isNullable = this.isNullable.bind(this), this.isOptional = this.isOptional.bind(this), this["~standard"] = {
          version: 1,
          vendor: "zod",
          validate: (data) => this["~validate"](data)
        };
      }
      optional() {
        return ZodOptional.create(this, this._def);
      }
      nullable() {
        return ZodNullable.create(this, this._def);
      }
      nullish() {
        return this.nullable().optional();
      }
      array() {
        return ZodArray.create(this);
      }
      promise() {
        return ZodPromise.create(this, this._def);
      }
      or(option) {
        return ZodUnion.create([this, option], this._def);
      }
      and(incoming) {
        return ZodIntersection.create(this, incoming, this._def);
      }
      transform(transform) {
        return new ZodEffects({
          ...processCreateParams(this._def),
          schema: this,
          typeName: ZodFirstPartyTypeKind.ZodEffects,
          effect: { type: "transform", transform }
        });
      }
      default(def) {
        let defaultValueFunc = typeof def == "function" ? def : () => def;
        return new ZodDefault({
          ...processCreateParams(this._def),
          innerType: this,
          defaultValue: defaultValueFunc,
          typeName: ZodFirstPartyTypeKind.ZodDefault
        });
      }
      brand() {
        return new ZodBranded({
          typeName: ZodFirstPartyTypeKind.ZodBranded,
          type: this,
          ...processCreateParams(this._def)
        });
      }
      catch(def) {
        let catchValueFunc = typeof def == "function" ? def : () => def;
        return new ZodCatch({
          ...processCreateParams(this._def),
          innerType: this,
          catchValue: catchValueFunc,
          typeName: ZodFirstPartyTypeKind.ZodCatch
        });
      }
      describe(description) {
        let This = this.constructor;
        return new This({
          ...this._def,
          description
        });
      }
      pipe(target) {
        return ZodPipeline.create(this, target);
      }
      readonly() {
        return ZodReadonly.create(this);
      }
      isOptional() {
        return this.safeParse(void 0).success;
      }
      isNullable() {
        return this.safeParse(null).success;
      }
    }, cuidRegex = /^c[^\s-]{8,}$/i, cuid2Regex = /^[0-9a-z]+$/, ulidRegex = /^[0-9A-HJKMNP-TV-Z]{26}$/i, uuidRegex = /^[0-9a-fA-F]{8}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{12}$/i, nanoidRegex = /^[a-z0-9_-]{21}$/i, jwtRegex = /^[A-Za-z0-9-_]+\.[A-Za-z0-9-_]+\.[A-Za-z0-9-_]*$/, durationRegex = /^[-+]?P(?!$)(?:(?:[-+]?\d+Y)|(?:[-+]?\d+[.,]\d+Y$))?(?:(?:[-+]?\d+M)|(?:[-+]?\d+[.,]\d+M$))?(?:(?:[-+]?\d+W)|(?:[-+]?\d+[.,]\d+W$))?(?:(?:[-+]?\d+D)|(?:[-+]?\d+[.,]\d+D$))?(?:T(?=[\d+-])(?:(?:[-+]?\d+H)|(?:[-+]?\d+[.,]\d+H$))?(?:(?:[-+]?\d+M)|(?:[-+]?\d+[.,]\d+M$))?(?:[-+]?\d+(?:[.,]\d+)?S)?)??$/, emailRegex = /^(?!\.)(?!.*\.\.)([A-Z0-9_'+\-\.]*)[A-Z0-9_+-]@([A-Z0-9][A-Z0-9\-]*\.)+[A-Z]{2,}$/i, _emojiRegex = "^(\\p{Extended_Pictographic}|\\p{Emoji_Component})+$", ipv4Regex = /^(?:(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])$/, ipv4CidrRegex = /^(?:(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\/(3[0-2]|[12]?[0-9])$/, ipv6Regex = /^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:)|fe80:(:[0-9a-fA-F]{0,4}){0,4}%[0-9a-zA-Z]{1,}|::(ffff(:0{1,4}){0,1}:){0,1}((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])|([0-9a-fA-F]{1,4}:){1,4}:((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9]))$/, ipv6CidrRegex = /^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:)|fe80:(:[0-9a-fA-F]{0,4}){0,4}%[0-9a-zA-Z]{1,}|::(ffff(:0{1,4}){0,1}:){0,1}((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])|([0-9a-fA-F]{1,4}:){1,4}:((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9]))\/(12[0-8]|1[01][0-9]|[1-9]?[0-9])$/, base64Regex = /^([0-9a-zA-Z+/]{4})*(([0-9a-zA-Z+/]{2}==)|([0-9a-zA-Z+/]{3}=))?$/, base64urlRegex = /^([0-9a-zA-Z-_]{4})*(([0-9a-zA-Z-_]{2}(==)?)|([0-9a-zA-Z-_]{3}(=)?))?$/, dateRegexSource = "((\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-((0[13578]|1[02])-(0[1-9]|[12]\\d|3[01])|(0[469]|11)-(0[1-9]|[12]\\d|30)|(02)-(0[1-9]|1\\d|2[0-8])))", dateRegex = new RegExp(`^${dateRegexSource}$`);
    ZodString = class _ZodString extends ZodType {
      _parse(input) {
        if (this._def.coerce && (input.data = String(input.data)), this._getType(input) !== ZodParsedType.string) {
          let ctx2 = this._getOrReturnCtx(input);
          return addIssueToContext(ctx2, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.string,
            received: ctx2.parsedType
          }), INVALID;
        }
        let status = new ParseStatus(), ctx;
        for (let check of this._def.checks)
          if (check.kind === "min")
            input.data.length < check.value && (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
              code: ZodIssueCode.too_small,
              minimum: check.value,
              type: "string",
              inclusive: !0,
              exact: !1,
              message: check.message
            }), status.dirty());
          else if (check.kind === "max")
            input.data.length > check.value && (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
              code: ZodIssueCode.too_big,
              maximum: check.value,
              type: "string",
              inclusive: !0,
              exact: !1,
              message: check.message
            }), status.dirty());
          else if (check.kind === "length") {
            let tooBig = input.data.length > check.value, tooSmall = input.data.length < check.value;
            (tooBig || tooSmall) && (ctx = this._getOrReturnCtx(input, ctx), tooBig ? addIssueToContext(ctx, {
              code: ZodIssueCode.too_big,
              maximum: check.value,
              type: "string",
              inclusive: !0,
              exact: !0,
              message: check.message
            }) : tooSmall && addIssueToContext(ctx, {
              code: ZodIssueCode.too_small,
              minimum: check.value,
              type: "string",
              inclusive: !0,
              exact: !0,
              message: check.message
            }), status.dirty());
          } else if (check.kind === "email")
            emailRegex.test(input.data) || (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
              validation: "email",
              code: ZodIssueCode.invalid_string,
              message: check.message
            }), status.dirty());
          else if (check.kind === "emoji")
            emojiRegex || (emojiRegex = new RegExp(_emojiRegex, "u")), emojiRegex.test(input.data) || (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
              validation: "emoji",
              code: ZodIssueCode.invalid_string,
              message: check.message
            }), status.dirty());
          else if (check.kind === "uuid")
            uuidRegex.test(input.data) || (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
              validation: "uuid",
              code: ZodIssueCode.invalid_string,
              message: check.message
            }), status.dirty());
          else if (check.kind === "nanoid")
            nanoidRegex.test(input.data) || (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
              validation: "nanoid",
              code: ZodIssueCode.invalid_string,
              message: check.message
            }), status.dirty());
          else if (check.kind === "cuid")
            cuidRegex.test(input.data) || (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
              validation: "cuid",
              code: ZodIssueCode.invalid_string,
              message: check.message
            }), status.dirty());
          else if (check.kind === "cuid2")
            cuid2Regex.test(input.data) || (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
              validation: "cuid2",
              code: ZodIssueCode.invalid_string,
              message: check.message
            }), status.dirty());
          else if (check.kind === "ulid")
            ulidRegex.test(input.data) || (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
              validation: "ulid",
              code: ZodIssueCode.invalid_string,
              message: check.message
            }), status.dirty());
          else if (check.kind === "url")
            try {
              new URL(input.data);
            } catch {
              ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
                validation: "url",
                code: ZodIssueCode.invalid_string,
                message: check.message
              }), status.dirty();
            }
          else check.kind === "regex" ? (check.regex.lastIndex = 0, check.regex.test(input.data) || (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
            validation: "regex",
            code: ZodIssueCode.invalid_string,
            message: check.message
          }), status.dirty())) : check.kind === "trim" ? input.data = input.data.trim() : check.kind === "includes" ? input.data.includes(check.value, check.position) || (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: { includes: check.value, position: check.position },
            message: check.message
          }), status.dirty()) : check.kind === "toLowerCase" ? input.data = input.data.toLowerCase() : check.kind === "toUpperCase" ? input.data = input.data.toUpperCase() : check.kind === "startsWith" ? input.data.startsWith(check.value) || (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: { startsWith: check.value },
            message: check.message
          }), status.dirty()) : check.kind === "endsWith" ? input.data.endsWith(check.value) || (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: { endsWith: check.value },
            message: check.message
          }), status.dirty()) : check.kind === "datetime" ? datetimeRegex(check).test(input.data) || (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: "datetime",
            message: check.message
          }), status.dirty()) : check.kind === "date" ? dateRegex.test(input.data) || (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: "date",
            message: check.message
          }), status.dirty()) : check.kind === "time" ? timeRegex(check).test(input.data) || (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: "time",
            message: check.message
          }), status.dirty()) : check.kind === "duration" ? durationRegex.test(input.data) || (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
            validation: "duration",
            code: ZodIssueCode.invalid_string,
            message: check.message
          }), status.dirty()) : check.kind === "ip" ? isValidIP(input.data, check.version) || (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
            validation: "ip",
            code: ZodIssueCode.invalid_string,
            message: check.message
          }), status.dirty()) : check.kind === "jwt" ? isValidJWT(input.data, check.alg) || (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
            validation: "jwt",
            code: ZodIssueCode.invalid_string,
            message: check.message
          }), status.dirty()) : check.kind === "cidr" ? isValidCidr(input.data, check.version) || (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
            validation: "cidr",
            code: ZodIssueCode.invalid_string,
            message: check.message
          }), status.dirty()) : check.kind === "base64" ? base64Regex.test(input.data) || (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
            validation: "base64",
            code: ZodIssueCode.invalid_string,
            message: check.message
          }), status.dirty()) : check.kind === "base64url" ? base64urlRegex.test(input.data) || (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
            validation: "base64url",
            code: ZodIssueCode.invalid_string,
            message: check.message
          }), status.dirty()) : util.assertNever(check);
        return { status: status.value, value: input.data };
      }
      _regex(regex, validation, message) {
        return this.refinement((data) => regex.test(data), {
          validation,
          code: ZodIssueCode.invalid_string,
          ...errorUtil.errToObj(message)
        });
      }
      _addCheck(check) {
        return new _ZodString({
          ...this._def,
          checks: [...this._def.checks, check]
        });
      }
      email(message) {
        return this._addCheck({ kind: "email", ...errorUtil.errToObj(message) });
      }
      url(message) {
        return this._addCheck({ kind: "url", ...errorUtil.errToObj(message) });
      }
      emoji(message) {
        return this._addCheck({ kind: "emoji", ...errorUtil.errToObj(message) });
      }
      uuid(message) {
        return this._addCheck({ kind: "uuid", ...errorUtil.errToObj(message) });
      }
      nanoid(message) {
        return this._addCheck({ kind: "nanoid", ...errorUtil.errToObj(message) });
      }
      cuid(message) {
        return this._addCheck({ kind: "cuid", ...errorUtil.errToObj(message) });
      }
      cuid2(message) {
        return this._addCheck({ kind: "cuid2", ...errorUtil.errToObj(message) });
      }
      ulid(message) {
        return this._addCheck({ kind: "ulid", ...errorUtil.errToObj(message) });
      }
      base64(message) {
        return this._addCheck({ kind: "base64", ...errorUtil.errToObj(message) });
      }
      base64url(message) {
        return this._addCheck({
          kind: "base64url",
          ...errorUtil.errToObj(message)
        });
      }
      jwt(options) {
        return this._addCheck({ kind: "jwt", ...errorUtil.errToObj(options) });
      }
      ip(options) {
        return this._addCheck({ kind: "ip", ...errorUtil.errToObj(options) });
      }
      cidr(options) {
        return this._addCheck({ kind: "cidr", ...errorUtil.errToObj(options) });
      }
      datetime(options) {
        return typeof options == "string" ? this._addCheck({
          kind: "datetime",
          precision: null,
          offset: !1,
          local: !1,
          message: options
        }) : this._addCheck({
          kind: "datetime",
          precision: typeof options?.precision > "u" ? null : options?.precision,
          offset: options?.offset ?? !1,
          local: options?.local ?? !1,
          ...errorUtil.errToObj(options?.message)
        });
      }
      date(message) {
        return this._addCheck({ kind: "date", message });
      }
      time(options) {
        return typeof options == "string" ? this._addCheck({
          kind: "time",
          precision: null,
          message: options
        }) : this._addCheck({
          kind: "time",
          precision: typeof options?.precision > "u" ? null : options?.precision,
          ...errorUtil.errToObj(options?.message)
        });
      }
      duration(message) {
        return this._addCheck({ kind: "duration", ...errorUtil.errToObj(message) });
      }
      regex(regex, message) {
        return this._addCheck({
          kind: "regex",
          regex,
          ...errorUtil.errToObj(message)
        });
      }
      includes(value, options) {
        return this._addCheck({
          kind: "includes",
          value,
          position: options?.position,
          ...errorUtil.errToObj(options?.message)
        });
      }
      startsWith(value, message) {
        return this._addCheck({
          kind: "startsWith",
          value,
          ...errorUtil.errToObj(message)
        });
      }
      endsWith(value, message) {
        return this._addCheck({
          kind: "endsWith",
          value,
          ...errorUtil.errToObj(message)
        });
      }
      min(minLength, message) {
        return this._addCheck({
          kind: "min",
          value: minLength,
          ...errorUtil.errToObj(message)
        });
      }
      max(maxLength, message) {
        return this._addCheck({
          kind: "max",
          value: maxLength,
          ...errorUtil.errToObj(message)
        });
      }
      length(len, message) {
        return this._addCheck({
          kind: "length",
          value: len,
          ...errorUtil.errToObj(message)
        });
      }
      /**
       * Equivalent to `.min(1)`
       */
      nonempty(message) {
        return this.min(1, errorUtil.errToObj(message));
      }
      trim() {
        return new _ZodString({
          ...this._def,
          checks: [...this._def.checks, { kind: "trim" }]
        });
      }
      toLowerCase() {
        return new _ZodString({
          ...this._def,
          checks: [...this._def.checks, { kind: "toLowerCase" }]
        });
      }
      toUpperCase() {
        return new _ZodString({
          ...this._def,
          checks: [...this._def.checks, { kind: "toUpperCase" }]
        });
      }
      get isDatetime() {
        return !!this._def.checks.find((ch) => ch.kind === "datetime");
      }
      get isDate() {
        return !!this._def.checks.find((ch) => ch.kind === "date");
      }
      get isTime() {
        return !!this._def.checks.find((ch) => ch.kind === "time");
      }
      get isDuration() {
        return !!this._def.checks.find((ch) => ch.kind === "duration");
      }
      get isEmail() {
        return !!this._def.checks.find((ch) => ch.kind === "email");
      }
      get isURL() {
        return !!this._def.checks.find((ch) => ch.kind === "url");
      }
      get isEmoji() {
        return !!this._def.checks.find((ch) => ch.kind === "emoji");
      }
      get isUUID() {
        return !!this._def.checks.find((ch) => ch.kind === "uuid");
      }
      get isNANOID() {
        return !!this._def.checks.find((ch) => ch.kind === "nanoid");
      }
      get isCUID() {
        return !!this._def.checks.find((ch) => ch.kind === "cuid");
      }
      get isCUID2() {
        return !!this._def.checks.find((ch) => ch.kind === "cuid2");
      }
      get isULID() {
        return !!this._def.checks.find((ch) => ch.kind === "ulid");
      }
      get isIP() {
        return !!this._def.checks.find((ch) => ch.kind === "ip");
      }
      get isCIDR() {
        return !!this._def.checks.find((ch) => ch.kind === "cidr");
      }
      get isBase64() {
        return !!this._def.checks.find((ch) => ch.kind === "base64");
      }
      get isBase64url() {
        return !!this._def.checks.find((ch) => ch.kind === "base64url");
      }
      get minLength() {
        let min = null;
        for (let ch of this._def.checks)
          ch.kind === "min" && (min === null || ch.value > min) && (min = ch.value);
        return min;
      }
      get maxLength() {
        let max = null;
        for (let ch of this._def.checks)
          ch.kind === "max" && (max === null || ch.value < max) && (max = ch.value);
        return max;
      }
    };
    ZodString.create = (params) => new ZodString({
      checks: [],
      typeName: ZodFirstPartyTypeKind.ZodString,
      coerce: params?.coerce ?? !1,
      ...processCreateParams(params)
    });
    ZodNumber = class _ZodNumber extends ZodType {
      constructor() {
        super(...arguments), this.min = this.gte, this.max = this.lte, this.step = this.multipleOf;
      }
      _parse(input) {
        if (this._def.coerce && (input.data = Number(input.data)), this._getType(input) !== ZodParsedType.number) {
          let ctx2 = this._getOrReturnCtx(input);
          return addIssueToContext(ctx2, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.number,
            received: ctx2.parsedType
          }), INVALID;
        }
        let ctx, status = new ParseStatus();
        for (let check of this._def.checks)
          check.kind === "int" ? util.isInteger(input.data) || (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: "integer",
            received: "float",
            message: check.message
          }), status.dirty()) : check.kind === "min" ? (check.inclusive ? input.data < check.value : input.data <= check.value) && (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
            code: ZodIssueCode.too_small,
            minimum: check.value,
            type: "number",
            inclusive: check.inclusive,
            exact: !1,
            message: check.message
          }), status.dirty()) : check.kind === "max" ? (check.inclusive ? input.data > check.value : input.data >= check.value) && (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
            code: ZodIssueCode.too_big,
            maximum: check.value,
            type: "number",
            inclusive: check.inclusive,
            exact: !1,
            message: check.message
          }), status.dirty()) : check.kind === "multipleOf" ? floatSafeRemainder(input.data, check.value) !== 0 && (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
            code: ZodIssueCode.not_multiple_of,
            multipleOf: check.value,
            message: check.message
          }), status.dirty()) : check.kind === "finite" ? Number.isFinite(input.data) || (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
            code: ZodIssueCode.not_finite,
            message: check.message
          }), status.dirty()) : util.assertNever(check);
        return { status: status.value, value: input.data };
      }
      gte(value, message) {
        return this.setLimit("min", value, !0, errorUtil.toString(message));
      }
      gt(value, message) {
        return this.setLimit("min", value, !1, errorUtil.toString(message));
      }
      lte(value, message) {
        return this.setLimit("max", value, !0, errorUtil.toString(message));
      }
      lt(value, message) {
        return this.setLimit("max", value, !1, errorUtil.toString(message));
      }
      setLimit(kind, value, inclusive, message) {
        return new _ZodNumber({
          ...this._def,
          checks: [
            ...this._def.checks,
            {
              kind,
              value,
              inclusive,
              message: errorUtil.toString(message)
            }
          ]
        });
      }
      _addCheck(check) {
        return new _ZodNumber({
          ...this._def,
          checks: [...this._def.checks, check]
        });
      }
      int(message) {
        return this._addCheck({
          kind: "int",
          message: errorUtil.toString(message)
        });
      }
      positive(message) {
        return this._addCheck({
          kind: "min",
          value: 0,
          inclusive: !1,
          message: errorUtil.toString(message)
        });
      }
      negative(message) {
        return this._addCheck({
          kind: "max",
          value: 0,
          inclusive: !1,
          message: errorUtil.toString(message)
        });
      }
      nonpositive(message) {
        return this._addCheck({
          kind: "max",
          value: 0,
          inclusive: !0,
          message: errorUtil.toString(message)
        });
      }
      nonnegative(message) {
        return this._addCheck({
          kind: "min",
          value: 0,
          inclusive: !0,
          message: errorUtil.toString(message)
        });
      }
      multipleOf(value, message) {
        return this._addCheck({
          kind: "multipleOf",
          value,
          message: errorUtil.toString(message)
        });
      }
      finite(message) {
        return this._addCheck({
          kind: "finite",
          message: errorUtil.toString(message)
        });
      }
      safe(message) {
        return this._addCheck({
          kind: "min",
          inclusive: !0,
          value: Number.MIN_SAFE_INTEGER,
          message: errorUtil.toString(message)
        })._addCheck({
          kind: "max",
          inclusive: !0,
          value: Number.MAX_SAFE_INTEGER,
          message: errorUtil.toString(message)
        });
      }
      get minValue() {
        let min = null;
        for (let ch of this._def.checks)
          ch.kind === "min" && (min === null || ch.value > min) && (min = ch.value);
        return min;
      }
      get maxValue() {
        let max = null;
        for (let ch of this._def.checks)
          ch.kind === "max" && (max === null || ch.value < max) && (max = ch.value);
        return max;
      }
      get isInt() {
        return !!this._def.checks.find((ch) => ch.kind === "int" || ch.kind === "multipleOf" && util.isInteger(ch.value));
      }
      get isFinite() {
        let max = null, min = null;
        for (let ch of this._def.checks) {
          if (ch.kind === "finite" || ch.kind === "int" || ch.kind === "multipleOf")
            return !0;
          ch.kind === "min" ? (min === null || ch.value > min) && (min = ch.value) : ch.kind === "max" && (max === null || ch.value < max) && (max = ch.value);
        }
        return Number.isFinite(min) && Number.isFinite(max);
      }
    };
    ZodNumber.create = (params) => new ZodNumber({
      checks: [],
      typeName: ZodFirstPartyTypeKind.ZodNumber,
      coerce: params?.coerce || !1,
      ...processCreateParams(params)
    });
    ZodBigInt = class _ZodBigInt extends ZodType {
      constructor() {
        super(...arguments), this.min = this.gte, this.max = this.lte;
      }
      _parse(input) {
        if (this._def.coerce)
          try {
            input.data = BigInt(input.data);
          } catch {
            return this._getInvalidInput(input);
          }
        if (this._getType(input) !== ZodParsedType.bigint)
          return this._getInvalidInput(input);
        let ctx, status = new ParseStatus();
        for (let check of this._def.checks)
          check.kind === "min" ? (check.inclusive ? input.data < check.value : input.data <= check.value) && (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
            code: ZodIssueCode.too_small,
            type: "bigint",
            minimum: check.value,
            inclusive: check.inclusive,
            message: check.message
          }), status.dirty()) : check.kind === "max" ? (check.inclusive ? input.data > check.value : input.data >= check.value) && (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
            code: ZodIssueCode.too_big,
            type: "bigint",
            maximum: check.value,
            inclusive: check.inclusive,
            message: check.message
          }), status.dirty()) : check.kind === "multipleOf" ? input.data % check.value !== BigInt(0) && (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
            code: ZodIssueCode.not_multiple_of,
            multipleOf: check.value,
            message: check.message
          }), status.dirty()) : util.assertNever(check);
        return { status: status.value, value: input.data };
      }
      _getInvalidInput(input) {
        let ctx = this._getOrReturnCtx(input);
        return addIssueToContext(ctx, {
          code: ZodIssueCode.invalid_type,
          expected: ZodParsedType.bigint,
          received: ctx.parsedType
        }), INVALID;
      }
      gte(value, message) {
        return this.setLimit("min", value, !0, errorUtil.toString(message));
      }
      gt(value, message) {
        return this.setLimit("min", value, !1, errorUtil.toString(message));
      }
      lte(value, message) {
        return this.setLimit("max", value, !0, errorUtil.toString(message));
      }
      lt(value, message) {
        return this.setLimit("max", value, !1, errorUtil.toString(message));
      }
      setLimit(kind, value, inclusive, message) {
        return new _ZodBigInt({
          ...this._def,
          checks: [
            ...this._def.checks,
            {
              kind,
              value,
              inclusive,
              message: errorUtil.toString(message)
            }
          ]
        });
      }
      _addCheck(check) {
        return new _ZodBigInt({
          ...this._def,
          checks: [...this._def.checks, check]
        });
      }
      positive(message) {
        return this._addCheck({
          kind: "min",
          value: BigInt(0),
          inclusive: !1,
          message: errorUtil.toString(message)
        });
      }
      negative(message) {
        return this._addCheck({
          kind: "max",
          value: BigInt(0),
          inclusive: !1,
          message: errorUtil.toString(message)
        });
      }
      nonpositive(message) {
        return this._addCheck({
          kind: "max",
          value: BigInt(0),
          inclusive: !0,
          message: errorUtil.toString(message)
        });
      }
      nonnegative(message) {
        return this._addCheck({
          kind: "min",
          value: BigInt(0),
          inclusive: !0,
          message: errorUtil.toString(message)
        });
      }
      multipleOf(value, message) {
        return this._addCheck({
          kind: "multipleOf",
          value,
          message: errorUtil.toString(message)
        });
      }
      get minValue() {
        let min = null;
        for (let ch of this._def.checks)
          ch.kind === "min" && (min === null || ch.value > min) && (min = ch.value);
        return min;
      }
      get maxValue() {
        let max = null;
        for (let ch of this._def.checks)
          ch.kind === "max" && (max === null || ch.value < max) && (max = ch.value);
        return max;
      }
    };
    ZodBigInt.create = (params) => new ZodBigInt({
      checks: [],
      typeName: ZodFirstPartyTypeKind.ZodBigInt,
      coerce: params?.coerce ?? !1,
      ...processCreateParams(params)
    });
    ZodBoolean = class extends ZodType {
      _parse(input) {
        if (this._def.coerce && (input.data = !!input.data), this._getType(input) !== ZodParsedType.boolean) {
          let ctx = this._getOrReturnCtx(input);
          return addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.boolean,
            received: ctx.parsedType
          }), INVALID;
        }
        return OK(input.data);
      }
    };
    ZodBoolean.create = (params) => new ZodBoolean({
      typeName: ZodFirstPartyTypeKind.ZodBoolean,
      coerce: params?.coerce || !1,
      ...processCreateParams(params)
    });
    ZodDate = class _ZodDate extends ZodType {
      _parse(input) {
        if (this._def.coerce && (input.data = new Date(input.data)), this._getType(input) !== ZodParsedType.date) {
          let ctx2 = this._getOrReturnCtx(input);
          return addIssueToContext(ctx2, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.date,
            received: ctx2.parsedType
          }), INVALID;
        }
        if (Number.isNaN(input.data.getTime())) {
          let ctx2 = this._getOrReturnCtx(input);
          return addIssueToContext(ctx2, {
            code: ZodIssueCode.invalid_date
          }), INVALID;
        }
        let status = new ParseStatus(), ctx;
        for (let check of this._def.checks)
          check.kind === "min" ? input.data.getTime() < check.value && (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
            code: ZodIssueCode.too_small,
            message: check.message,
            inclusive: !0,
            exact: !1,
            minimum: check.value,
            type: "date"
          }), status.dirty()) : check.kind === "max" ? input.data.getTime() > check.value && (ctx = this._getOrReturnCtx(input, ctx), addIssueToContext(ctx, {
            code: ZodIssueCode.too_big,
            message: check.message,
            inclusive: !0,
            exact: !1,
            maximum: check.value,
            type: "date"
          }), status.dirty()) : util.assertNever(check);
        return {
          status: status.value,
          value: new Date(input.data.getTime())
        };
      }
      _addCheck(check) {
        return new _ZodDate({
          ...this._def,
          checks: [...this._def.checks, check]
        });
      }
      min(minDate, message) {
        return this._addCheck({
          kind: "min",
          value: minDate.getTime(),
          message: errorUtil.toString(message)
        });
      }
      max(maxDate, message) {
        return this._addCheck({
          kind: "max",
          value: maxDate.getTime(),
          message: errorUtil.toString(message)
        });
      }
      get minDate() {
        let min = null;
        for (let ch of this._def.checks)
          ch.kind === "min" && (min === null || ch.value > min) && (min = ch.value);
        return min != null ? new Date(min) : null;
      }
      get maxDate() {
        let max = null;
        for (let ch of this._def.checks)
          ch.kind === "max" && (max === null || ch.value < max) && (max = ch.value);
        return max != null ? new Date(max) : null;
      }
    };
    ZodDate.create = (params) => new ZodDate({
      checks: [],
      coerce: params?.coerce || !1,
      typeName: ZodFirstPartyTypeKind.ZodDate,
      ...processCreateParams(params)
    });
    ZodSymbol = class extends ZodType {
      _parse(input) {
        if (this._getType(input) !== ZodParsedType.symbol) {
          let ctx = this._getOrReturnCtx(input);
          return addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.symbol,
            received: ctx.parsedType
          }), INVALID;
        }
        return OK(input.data);
      }
    };
    ZodSymbol.create = (params) => new ZodSymbol({
      typeName: ZodFirstPartyTypeKind.ZodSymbol,
      ...processCreateParams(params)
    });
    ZodUndefined = class extends ZodType {
      _parse(input) {
        if (this._getType(input) !== ZodParsedType.undefined) {
          let ctx = this._getOrReturnCtx(input);
          return addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.undefined,
            received: ctx.parsedType
          }), INVALID;
        }
        return OK(input.data);
      }
    };
    ZodUndefined.create = (params) => new ZodUndefined({
      typeName: ZodFirstPartyTypeKind.ZodUndefined,
      ...processCreateParams(params)
    });
    ZodNull = class extends ZodType {
      _parse(input) {
        if (this._getType(input) !== ZodParsedType.null) {
          let ctx = this._getOrReturnCtx(input);
          return addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.null,
            received: ctx.parsedType
          }), INVALID;
        }
        return OK(input.data);
      }
    };
    ZodNull.create = (params) => new ZodNull({
      typeName: ZodFirstPartyTypeKind.ZodNull,
      ...processCreateParams(params)
    });
    ZodAny = class extends ZodType {
      constructor() {
        super(...arguments), this._any = !0;
      }
      _parse(input) {
        return OK(input.data);
      }
    };
    ZodAny.create = (params) => new ZodAny({
      typeName: ZodFirstPartyTypeKind.ZodAny,
      ...processCreateParams(params)
    });
    ZodUnknown = class extends ZodType {
      constructor() {
        super(...arguments), this._unknown = !0;
      }
      _parse(input) {
        return OK(input.data);
      }
    };
    ZodUnknown.create = (params) => new ZodUnknown({
      typeName: ZodFirstPartyTypeKind.ZodUnknown,
      ...processCreateParams(params)
    });
    ZodNever = class extends ZodType {
      _parse(input) {
        let ctx = this._getOrReturnCtx(input);
        return addIssueToContext(ctx, {
          code: ZodIssueCode.invalid_type,
          expected: ZodParsedType.never,
          received: ctx.parsedType
        }), INVALID;
      }
    };
    ZodNever.create = (params) => new ZodNever({
      typeName: ZodFirstPartyTypeKind.ZodNever,
      ...processCreateParams(params)
    });
    ZodVoid = class extends ZodType {
      _parse(input) {
        if (this._getType(input) !== ZodParsedType.undefined) {
          let ctx = this._getOrReturnCtx(input);
          return addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.void,
            received: ctx.parsedType
          }), INVALID;
        }
        return OK(input.data);
      }
    };
    ZodVoid.create = (params) => new ZodVoid({
      typeName: ZodFirstPartyTypeKind.ZodVoid,
      ...processCreateParams(params)
    });
    ZodArray = class _ZodArray extends ZodType {
      _parse(input) {
        let { ctx, status } = this._processInputParams(input), def = this._def;
        if (ctx.parsedType !== ZodParsedType.array)
          return addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.array,
            received: ctx.parsedType
          }), INVALID;
        if (def.exactLength !== null) {
          let tooBig = ctx.data.length > def.exactLength.value, tooSmall = ctx.data.length < def.exactLength.value;
          (tooBig || tooSmall) && (addIssueToContext(ctx, {
            code: tooBig ? ZodIssueCode.too_big : ZodIssueCode.too_small,
            minimum: tooSmall ? def.exactLength.value : void 0,
            maximum: tooBig ? def.exactLength.value : void 0,
            type: "array",
            inclusive: !0,
            exact: !0,
            message: def.exactLength.message
          }), status.dirty());
        }
        if (def.minLength !== null && ctx.data.length < def.minLength.value && (addIssueToContext(ctx, {
          code: ZodIssueCode.too_small,
          minimum: def.minLength.value,
          type: "array",
          inclusive: !0,
          exact: !1,
          message: def.minLength.message
        }), status.dirty()), def.maxLength !== null && ctx.data.length > def.maxLength.value && (addIssueToContext(ctx, {
          code: ZodIssueCode.too_big,
          maximum: def.maxLength.value,
          type: "array",
          inclusive: !0,
          exact: !1,
          message: def.maxLength.message
        }), status.dirty()), ctx.common.async)
          return Promise.all([...ctx.data].map((item, i) => def.type._parseAsync(new ParseInputLazyPath(ctx, item, ctx.path, i)))).then((result2) => ParseStatus.mergeArray(status, result2));
        let result = [...ctx.data].map((item, i) => def.type._parseSync(new ParseInputLazyPath(ctx, item, ctx.path, i)));
        return ParseStatus.mergeArray(status, result);
      }
      get element() {
        return this._def.type;
      }
      min(minLength, message) {
        return new _ZodArray({
          ...this._def,
          minLength: { value: minLength, message: errorUtil.toString(message) }
        });
      }
      max(maxLength, message) {
        return new _ZodArray({
          ...this._def,
          maxLength: { value: maxLength, message: errorUtil.toString(message) }
        });
      }
      length(len, message) {
        return new _ZodArray({
          ...this._def,
          exactLength: { value: len, message: errorUtil.toString(message) }
        });
      }
      nonempty(message) {
        return this.min(1, message);
      }
    };
    ZodArray.create = (schema, params) => new ZodArray({
      type: schema,
      minLength: null,
      maxLength: null,
      exactLength: null,
      typeName: ZodFirstPartyTypeKind.ZodArray,
      ...processCreateParams(params)
    });
    ZodObject = class _ZodObject extends ZodType {
      constructor() {
        super(...arguments), this._cached = null, this.nonstrict = this.passthrough, this.augment = this.extend;
      }
      _getCached() {
        if (this._cached !== null)
          return this._cached;
        let shape = this._def.shape(), keys = util.objectKeys(shape);
        return this._cached = { shape, keys }, this._cached;
      }
      _parse(input) {
        if (this._getType(input) !== ZodParsedType.object) {
          let ctx2 = this._getOrReturnCtx(input);
          return addIssueToContext(ctx2, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.object,
            received: ctx2.parsedType
          }), INVALID;
        }
        let { status, ctx } = this._processInputParams(input), { shape, keys: shapeKeys } = this._getCached(), extraKeys = [];
        if (!(this._def.catchall instanceof ZodNever && this._def.unknownKeys === "strip"))
          for (let key in ctx.data)
            shapeKeys.includes(key) || extraKeys.push(key);
        let pairs = [];
        for (let key of shapeKeys) {
          let keyValidator = shape[key], value = ctx.data[key];
          pairs.push({
            key: { status: "valid", value: key },
            value: keyValidator._parse(new ParseInputLazyPath(ctx, value, ctx.path, key)),
            alwaysSet: key in ctx.data
          });
        }
        if (this._def.catchall instanceof ZodNever) {
          let unknownKeys = this._def.unknownKeys;
          if (unknownKeys === "passthrough")
            for (let key of extraKeys)
              pairs.push({
                key: { status: "valid", value: key },
                value: { status: "valid", value: ctx.data[key] }
              });
          else if (unknownKeys === "strict")
            extraKeys.length > 0 && (addIssueToContext(ctx, {
              code: ZodIssueCode.unrecognized_keys,
              keys: extraKeys
            }), status.dirty());
          else if (unknownKeys !== "strip")
            throw new Error("Internal ZodObject error: invalid unknownKeys value.");
        } else {
          let catchall = this._def.catchall;
          for (let key of extraKeys) {
            let value = ctx.data[key];
            pairs.push({
              key: { status: "valid", value: key },
              value: catchall._parse(
                new ParseInputLazyPath(ctx, value, ctx.path, key)
                //, ctx.child(key), value, getParsedType(value)
              ),
              alwaysSet: key in ctx.data
            });
          }
        }
        return ctx.common.async ? Promise.resolve().then(async () => {
          let syncPairs = [];
          for (let pair of pairs) {
            let key = await pair.key, value = await pair.value;
            syncPairs.push({
              key,
              value,
              alwaysSet: pair.alwaysSet
            });
          }
          return syncPairs;
        }).then((syncPairs) => ParseStatus.mergeObjectSync(status, syncPairs)) : ParseStatus.mergeObjectSync(status, pairs);
      }
      get shape() {
        return this._def.shape();
      }
      strict(message) {
        return errorUtil.errToObj, new _ZodObject({
          ...this._def,
          unknownKeys: "strict",
          ...message !== void 0 ? {
            errorMap: (issue, ctx) => {
              let defaultError = this._def.errorMap?.(issue, ctx).message ?? ctx.defaultError;
              return issue.code === "unrecognized_keys" ? {
                message: errorUtil.errToObj(message).message ?? defaultError
              } : {
                message: defaultError
              };
            }
          } : {}
        });
      }
      strip() {
        return new _ZodObject({
          ...this._def,
          unknownKeys: "strip"
        });
      }
      passthrough() {
        return new _ZodObject({
          ...this._def,
          unknownKeys: "passthrough"
        });
      }
      // const AugmentFactory =
      //   <Def extends ZodObjectDef>(def: Def) =>
      //   <Augmentation extends ZodRawShape>(
      //     augmentation: Augmentation
      //   ): ZodObject<
      //     extendShape<ReturnType<Def["shape"]>, Augmentation>,
      //     Def["unknownKeys"],
      //     Def["catchall"]
      //   > => {
      //     return new ZodObject({
      //       ...def,
      //       shape: () => ({
      //         ...def.shape(),
      //         ...augmentation,
      //       }),
      //     }) as any;
      //   };
      extend(augmentation) {
        return new _ZodObject({
          ...this._def,
          shape: () => ({
            ...this._def.shape(),
            ...augmentation
          })
        });
      }
      /**
       * Prior to zod@1.0.12 there was a bug in the
       * inferred type of merged objects. Please
       * upgrade if you are experiencing issues.
       */
      merge(merging) {
        return new _ZodObject({
          unknownKeys: merging._def.unknownKeys,
          catchall: merging._def.catchall,
          shape: () => ({
            ...this._def.shape(),
            ...merging._def.shape()
          }),
          typeName: ZodFirstPartyTypeKind.ZodObject
        });
      }
      // merge<
      //   Incoming extends AnyZodObject,
      //   Augmentation extends Incoming["shape"],
      //   NewOutput extends {
      //     [k in keyof Augmentation | keyof Output]: k extends keyof Augmentation
      //       ? Augmentation[k]["_output"]
      //       : k extends keyof Output
      //       ? Output[k]
      //       : never;
      //   },
      //   NewInput extends {
      //     [k in keyof Augmentation | keyof Input]: k extends keyof Augmentation
      //       ? Augmentation[k]["_input"]
      //       : k extends keyof Input
      //       ? Input[k]
      //       : never;
      //   }
      // >(
      //   merging: Incoming
      // ): ZodObject<
      //   extendShape<T, ReturnType<Incoming["_def"]["shape"]>>,
      //   Incoming["_def"]["unknownKeys"],
      //   Incoming["_def"]["catchall"],
      //   NewOutput,
      //   NewInput
      // > {
      //   const merged: any = new ZodObject({
      //     unknownKeys: merging._def.unknownKeys,
      //     catchall: merging._def.catchall,
      //     shape: () =>
      //       objectUtil.mergeShapes(this._def.shape(), merging._def.shape()),
      //     typeName: ZodFirstPartyTypeKind.ZodObject,
      //   }) as any;
      //   return merged;
      // }
      setKey(key, schema) {
        return this.augment({ [key]: schema });
      }
      // merge<Incoming extends AnyZodObject>(
      //   merging: Incoming
      // ): //ZodObject<T & Incoming["_shape"], UnknownKeys, Catchall> = (merging) => {
      // ZodObject<
      //   extendShape<T, ReturnType<Incoming["_def"]["shape"]>>,
      //   Incoming["_def"]["unknownKeys"],
      //   Incoming["_def"]["catchall"]
      // > {
      //   // const mergedShape = objectUtil.mergeShapes(
      //   //   this._def.shape(),
      //   //   merging._def.shape()
      //   // );
      //   const merged: any = new ZodObject({
      //     unknownKeys: merging._def.unknownKeys,
      //     catchall: merging._def.catchall,
      //     shape: () =>
      //       objectUtil.mergeShapes(this._def.shape(), merging._def.shape()),
      //     typeName: ZodFirstPartyTypeKind.ZodObject,
      //   }) as any;
      //   return merged;
      // }
      catchall(index) {
        return new _ZodObject({
          ...this._def,
          catchall: index
        });
      }
      pick(mask) {
        let shape = {};
        for (let key of util.objectKeys(mask))
          mask[key] && this.shape[key] && (shape[key] = this.shape[key]);
        return new _ZodObject({
          ...this._def,
          shape: () => shape
        });
      }
      omit(mask) {
        let shape = {};
        for (let key of util.objectKeys(this.shape))
          mask[key] || (shape[key] = this.shape[key]);
        return new _ZodObject({
          ...this._def,
          shape: () => shape
        });
      }
      /**
       * @deprecated
       */
      deepPartial() {
        return deepPartialify(this);
      }
      partial(mask) {
        let newShape = {};
        for (let key of util.objectKeys(this.shape)) {
          let fieldSchema = this.shape[key];
          mask && !mask[key] ? newShape[key] = fieldSchema : newShape[key] = fieldSchema.optional();
        }
        return new _ZodObject({
          ...this._def,
          shape: () => newShape
        });
      }
      required(mask) {
        let newShape = {};
        for (let key of util.objectKeys(this.shape))
          if (mask && !mask[key])
            newShape[key] = this.shape[key];
          else {
            let newField = this.shape[key];
            for (; newField instanceof ZodOptional; )
              newField = newField._def.innerType;
            newShape[key] = newField;
          }
        return new _ZodObject({
          ...this._def,
          shape: () => newShape
        });
      }
      keyof() {
        return createZodEnum(util.objectKeys(this.shape));
      }
    };
    ZodObject.create = (shape, params) => new ZodObject({
      shape: () => shape,
      unknownKeys: "strip",
      catchall: ZodNever.create(),
      typeName: ZodFirstPartyTypeKind.ZodObject,
      ...processCreateParams(params)
    });
    ZodObject.strictCreate = (shape, params) => new ZodObject({
      shape: () => shape,
      unknownKeys: "strict",
      catchall: ZodNever.create(),
      typeName: ZodFirstPartyTypeKind.ZodObject,
      ...processCreateParams(params)
    });
    ZodObject.lazycreate = (shape, params) => new ZodObject({
      shape,
      unknownKeys: "strip",
      catchall: ZodNever.create(),
      typeName: ZodFirstPartyTypeKind.ZodObject,
      ...processCreateParams(params)
    });
    ZodUnion = class extends ZodType {
      _parse(input) {
        let { ctx } = this._processInputParams(input), options = this._def.options;
        function handleResults(results) {
          for (let result of results)
            if (result.result.status === "valid")
              return result.result;
          for (let result of results)
            if (result.result.status === "dirty")
              return ctx.common.issues.push(...result.ctx.common.issues), result.result;
          let unionErrors = results.map((result) => new ZodError(result.ctx.common.issues));
          return addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_union,
            unionErrors
          }), INVALID;
        }
        if (ctx.common.async)
          return Promise.all(options.map(async (option) => {
            let childCtx = {
              ...ctx,
              common: {
                ...ctx.common,
                issues: []
              },
              parent: null
            };
            return {
              result: await option._parseAsync({
                data: ctx.data,
                path: ctx.path,
                parent: childCtx
              }),
              ctx: childCtx
            };
          })).then(handleResults);
        {
          let dirty, issues = [];
          for (let option of options) {
            let childCtx = {
              ...ctx,
              common: {
                ...ctx.common,
                issues: []
              },
              parent: null
            }, result = option._parseSync({
              data: ctx.data,
              path: ctx.path,
              parent: childCtx
            });
            if (result.status === "valid")
              return result;
            result.status === "dirty" && !dirty && (dirty = { result, ctx: childCtx }), childCtx.common.issues.length && issues.push(childCtx.common.issues);
          }
          if (dirty)
            return ctx.common.issues.push(...dirty.ctx.common.issues), dirty.result;
          let unionErrors = issues.map((issues2) => new ZodError(issues2));
          return addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_union,
            unionErrors
          }), INVALID;
        }
      }
      get options() {
        return this._def.options;
      }
    };
    ZodUnion.create = (types2, params) => new ZodUnion({
      options: types2,
      typeName: ZodFirstPartyTypeKind.ZodUnion,
      ...processCreateParams(params)
    });
    getDiscriminator = (type) => type instanceof ZodLazy ? getDiscriminator(type.schema) : type instanceof ZodEffects ? getDiscriminator(type.innerType()) : type instanceof ZodLiteral ? [type.value] : type instanceof ZodEnum ? type.options : type instanceof ZodNativeEnum ? util.objectValues(type.enum) : type instanceof ZodDefault ? getDiscriminator(type._def.innerType) : type instanceof ZodUndefined ? [void 0] : type instanceof ZodNull ? [null] : type instanceof ZodOptional ? [void 0, ...getDiscriminator(type.unwrap())] : type instanceof ZodNullable ? [null, ...getDiscriminator(type.unwrap())] : type instanceof ZodBranded || type instanceof ZodReadonly ? getDiscriminator(type.unwrap()) : type instanceof ZodCatch ? getDiscriminator(type._def.innerType) : [], ZodDiscriminatedUnion = class _ZodDiscriminatedUnion extends ZodType {
      _parse(input) {
        let { ctx } = this._processInputParams(input);
        if (ctx.parsedType !== ZodParsedType.object)
          return addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.object,
            received: ctx.parsedType
          }), INVALID;
        let discriminator = this.discriminator, discriminatorValue = ctx.data[discriminator], option = this.optionsMap.get(discriminatorValue);
        return option ? ctx.common.async ? option._parseAsync({
          data: ctx.data,
          path: ctx.path,
          parent: ctx
        }) : option._parseSync({
          data: ctx.data,
          path: ctx.path,
          parent: ctx
        }) : (addIssueToContext(ctx, {
          code: ZodIssueCode.invalid_union_discriminator,
          options: Array.from(this.optionsMap.keys()),
          path: [discriminator]
        }), INVALID);
      }
      get discriminator() {
        return this._def.discriminator;
      }
      get options() {
        return this._def.options;
      }
      get optionsMap() {
        return this._def.optionsMap;
      }
      /**
       * The constructor of the discriminated union schema. Its behaviour is very similar to that of the normal z.union() constructor.
       * However, it only allows a union of objects, all of which need to share a discriminator property. This property must
       * have a different value for each object in the union.
       * @param discriminator the name of the discriminator property
       * @param types an array of object schemas
       * @param params
       */
      static create(discriminator, options, params) {
        let optionsMap = /* @__PURE__ */ new Map();
        for (let type of options) {
          let discriminatorValues = getDiscriminator(type.shape[discriminator]);
          if (!discriminatorValues.length)
            throw new Error(`A discriminator value for key \`${discriminator}\` could not be extracted from all schema options`);
          for (let value of discriminatorValues) {
            if (optionsMap.has(value))
              throw new Error(`Discriminator property ${String(discriminator)} has duplicate value ${String(value)}`);
            optionsMap.set(value, type);
          }
        }
        return new _ZodDiscriminatedUnion({
          typeName: ZodFirstPartyTypeKind.ZodDiscriminatedUnion,
          discriminator,
          options,
          optionsMap,
          ...processCreateParams(params)
        });
      }
    };
    ZodIntersection = class extends ZodType {
      _parse(input) {
        let { status, ctx } = this._processInputParams(input), handleParsed = (parsedLeft, parsedRight) => {
          if (isAborted(parsedLeft) || isAborted(parsedRight))
            return INVALID;
          let merged = mergeValues(parsedLeft.value, parsedRight.value);
          return merged.valid ? ((isDirty(parsedLeft) || isDirty(parsedRight)) && status.dirty(), { status: status.value, value: merged.data }) : (addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_intersection_types
          }), INVALID);
        };
        return ctx.common.async ? Promise.all([
          this._def.left._parseAsync({
            data: ctx.data,
            path: ctx.path,
            parent: ctx
          }),
          this._def.right._parseAsync({
            data: ctx.data,
            path: ctx.path,
            parent: ctx
          })
        ]).then(([left, right]) => handleParsed(left, right)) : handleParsed(this._def.left._parseSync({
          data: ctx.data,
          path: ctx.path,
          parent: ctx
        }), this._def.right._parseSync({
          data: ctx.data,
          path: ctx.path,
          parent: ctx
        }));
      }
    };
    ZodIntersection.create = (left, right, params) => new ZodIntersection({
      left,
      right,
      typeName: ZodFirstPartyTypeKind.ZodIntersection,
      ...processCreateParams(params)
    });
    ZodTuple = class _ZodTuple extends ZodType {
      _parse(input) {
        let { status, ctx } = this._processInputParams(input);
        if (ctx.parsedType !== ZodParsedType.array)
          return addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.array,
            received: ctx.parsedType
          }), INVALID;
        if (ctx.data.length < this._def.items.length)
          return addIssueToContext(ctx, {
            code: ZodIssueCode.too_small,
            minimum: this._def.items.length,
            inclusive: !0,
            exact: !1,
            type: "array"
          }), INVALID;
        !this._def.rest && ctx.data.length > this._def.items.length && (addIssueToContext(ctx, {
          code: ZodIssueCode.too_big,
          maximum: this._def.items.length,
          inclusive: !0,
          exact: !1,
          type: "array"
        }), status.dirty());
        let items = [...ctx.data].map((item, itemIndex) => {
          let schema = this._def.items[itemIndex] || this._def.rest;
          return schema ? schema._parse(new ParseInputLazyPath(ctx, item, ctx.path, itemIndex)) : null;
        }).filter((x) => !!x);
        return ctx.common.async ? Promise.all(items).then((results) => ParseStatus.mergeArray(status, results)) : ParseStatus.mergeArray(status, items);
      }
      get items() {
        return this._def.items;
      }
      rest(rest) {
        return new _ZodTuple({
          ...this._def,
          rest
        });
      }
    };
    ZodTuple.create = (schemas, params) => {
      if (!Array.isArray(schemas))
        throw new Error("You must pass an array of schemas to z.tuple([ ... ])");
      return new ZodTuple({
        items: schemas,
        typeName: ZodFirstPartyTypeKind.ZodTuple,
        rest: null,
        ...processCreateParams(params)
      });
    };
    ZodRecord = class _ZodRecord extends ZodType {
      get keySchema() {
        return this._def.keyType;
      }
      get valueSchema() {
        return this._def.valueType;
      }
      _parse(input) {
        let { status, ctx } = this._processInputParams(input);
        if (ctx.parsedType !== ZodParsedType.object)
          return addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.object,
            received: ctx.parsedType
          }), INVALID;
        let pairs = [], keyType = this._def.keyType, valueType = this._def.valueType;
        for (let key in ctx.data)
          pairs.push({
            key: keyType._parse(new ParseInputLazyPath(ctx, key, ctx.path, key)),
            value: valueType._parse(new ParseInputLazyPath(ctx, ctx.data[key], ctx.path, key)),
            alwaysSet: key in ctx.data
          });
        return ctx.common.async ? ParseStatus.mergeObjectAsync(status, pairs) : ParseStatus.mergeObjectSync(status, pairs);
      }
      get element() {
        return this._def.valueType;
      }
      static create(first, second, third) {
        return second instanceof ZodType ? new _ZodRecord({
          keyType: first,
          valueType: second,
          typeName: ZodFirstPartyTypeKind.ZodRecord,
          ...processCreateParams(third)
        }) : new _ZodRecord({
          keyType: ZodString.create(),
          valueType: first,
          typeName: ZodFirstPartyTypeKind.ZodRecord,
          ...processCreateParams(second)
        });
      }
    }, ZodMap = class extends ZodType {
      get keySchema() {
        return this._def.keyType;
      }
      get valueSchema() {
        return this._def.valueType;
      }
      _parse(input) {
        let { status, ctx } = this._processInputParams(input);
        if (ctx.parsedType !== ZodParsedType.map)
          return addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.map,
            received: ctx.parsedType
          }), INVALID;
        let keyType = this._def.keyType, valueType = this._def.valueType, pairs = [...ctx.data.entries()].map(([key, value], index) => ({
          key: keyType._parse(new ParseInputLazyPath(ctx, key, ctx.path, [index, "key"])),
          value: valueType._parse(new ParseInputLazyPath(ctx, value, ctx.path, [index, "value"]))
        }));
        if (ctx.common.async) {
          let finalMap = /* @__PURE__ */ new Map();
          return Promise.resolve().then(async () => {
            for (let pair of pairs) {
              let key = await pair.key, value = await pair.value;
              if (key.status === "aborted" || value.status === "aborted")
                return INVALID;
              (key.status === "dirty" || value.status === "dirty") && status.dirty(), finalMap.set(key.value, value.value);
            }
            return { status: status.value, value: finalMap };
          });
        } else {
          let finalMap = /* @__PURE__ */ new Map();
          for (let pair of pairs) {
            let key = pair.key, value = pair.value;
            if (key.status === "aborted" || value.status === "aborted")
              return INVALID;
            (key.status === "dirty" || value.status === "dirty") && status.dirty(), finalMap.set(key.value, value.value);
          }
          return { status: status.value, value: finalMap };
        }
      }
    };
    ZodMap.create = (keyType, valueType, params) => new ZodMap({
      valueType,
      keyType,
      typeName: ZodFirstPartyTypeKind.ZodMap,
      ...processCreateParams(params)
    });
    ZodSet = class _ZodSet extends ZodType {
      _parse(input) {
        let { status, ctx } = this._processInputParams(input);
        if (ctx.parsedType !== ZodParsedType.set)
          return addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.set,
            received: ctx.parsedType
          }), INVALID;
        let def = this._def;
        def.minSize !== null && ctx.data.size < def.minSize.value && (addIssueToContext(ctx, {
          code: ZodIssueCode.too_small,
          minimum: def.minSize.value,
          type: "set",
          inclusive: !0,
          exact: !1,
          message: def.minSize.message
        }), status.dirty()), def.maxSize !== null && ctx.data.size > def.maxSize.value && (addIssueToContext(ctx, {
          code: ZodIssueCode.too_big,
          maximum: def.maxSize.value,
          type: "set",
          inclusive: !0,
          exact: !1,
          message: def.maxSize.message
        }), status.dirty());
        let valueType = this._def.valueType;
        function finalizeSet(elements2) {
          let parsedSet = /* @__PURE__ */ new Set();
          for (let element of elements2) {
            if (element.status === "aborted")
              return INVALID;
            element.status === "dirty" && status.dirty(), parsedSet.add(element.value);
          }
          return { status: status.value, value: parsedSet };
        }
        let elements = [...ctx.data.values()].map((item, i) => valueType._parse(new ParseInputLazyPath(ctx, item, ctx.path, i)));
        return ctx.common.async ? Promise.all(elements).then((elements2) => finalizeSet(elements2)) : finalizeSet(elements);
      }
      min(minSize, message) {
        return new _ZodSet({
          ...this._def,
          minSize: { value: minSize, message: errorUtil.toString(message) }
        });
      }
      max(maxSize, message) {
        return new _ZodSet({
          ...this._def,
          maxSize: { value: maxSize, message: errorUtil.toString(message) }
        });
      }
      size(size, message) {
        return this.min(size, message).max(size, message);
      }
      nonempty(message) {
        return this.min(1, message);
      }
    };
    ZodSet.create = (valueType, params) => new ZodSet({
      valueType,
      minSize: null,
      maxSize: null,
      typeName: ZodFirstPartyTypeKind.ZodSet,
      ...processCreateParams(params)
    });
    ZodFunction = class _ZodFunction extends ZodType {
      constructor() {
        super(...arguments), this.validate = this.implement;
      }
      _parse(input) {
        let { ctx } = this._processInputParams(input);
        if (ctx.parsedType !== ZodParsedType.function)
          return addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.function,
            received: ctx.parsedType
          }), INVALID;
        function makeArgsIssue(args, error) {
          return makeIssue({
            data: args,
            path: ctx.path,
            errorMaps: [ctx.common.contextualErrorMap, ctx.schemaErrorMap, getErrorMap(), en_default].filter((x) => !!x),
            issueData: {
              code: ZodIssueCode.invalid_arguments,
              argumentsError: error
            }
          });
        }
        function makeReturnsIssue(returns, error) {
          return makeIssue({
            data: returns,
            path: ctx.path,
            errorMaps: [ctx.common.contextualErrorMap, ctx.schemaErrorMap, getErrorMap(), en_default].filter((x) => !!x),
            issueData: {
              code: ZodIssueCode.invalid_return_type,
              returnTypeError: error
            }
          });
        }
        let params = { errorMap: ctx.common.contextualErrorMap }, fn = ctx.data;
        if (this._def.returns instanceof ZodPromise) {
          let me = this;
          return OK(async function(...args) {
            let error = new ZodError([]), parsedArgs = await me._def.args.parseAsync(args, params).catch((e) => {
              throw error.addIssue(makeArgsIssue(args, e)), error;
            }), result = await Reflect.apply(fn, this, parsedArgs);
            return await me._def.returns._def.type.parseAsync(result, params).catch((e) => {
              throw error.addIssue(makeReturnsIssue(result, e)), error;
            });
          });
        } else {
          let me = this;
          return OK(function(...args) {
            let parsedArgs = me._def.args.safeParse(args, params);
            if (!parsedArgs.success)
              throw new ZodError([makeArgsIssue(args, parsedArgs.error)]);
            let result = Reflect.apply(fn, this, parsedArgs.data), parsedReturns = me._def.returns.safeParse(result, params);
            if (!parsedReturns.success)
              throw new ZodError([makeReturnsIssue(result, parsedReturns.error)]);
            return parsedReturns.data;
          });
        }
      }
      parameters() {
        return this._def.args;
      }
      returnType() {
        return this._def.returns;
      }
      args(...items) {
        return new _ZodFunction({
          ...this._def,
          args: ZodTuple.create(items).rest(ZodUnknown.create())
        });
      }
      returns(returnType) {
        return new _ZodFunction({
          ...this._def,
          returns: returnType
        });
      }
      implement(func) {
        return this.parse(func);
      }
      strictImplement(func) {
        return this.parse(func);
      }
      static create(args, returns, params) {
        return new _ZodFunction({
          args: args || ZodTuple.create([]).rest(ZodUnknown.create()),
          returns: returns || ZodUnknown.create(),
          typeName: ZodFirstPartyTypeKind.ZodFunction,
          ...processCreateParams(params)
        });
      }
    }, ZodLazy = class extends ZodType {
      get schema() {
        return this._def.getter();
      }
      _parse(input) {
        let { ctx } = this._processInputParams(input);
        return this._def.getter()._parse({ data: ctx.data, path: ctx.path, parent: ctx });
      }
    };
    ZodLazy.create = (getter, params) => new ZodLazy({
      getter,
      typeName: ZodFirstPartyTypeKind.ZodLazy,
      ...processCreateParams(params)
    });
    ZodLiteral = class extends ZodType {
      _parse(input) {
        if (input.data !== this._def.value) {
          let ctx = this._getOrReturnCtx(input);
          return addIssueToContext(ctx, {
            received: ctx.data,
            code: ZodIssueCode.invalid_literal,
            expected: this._def.value
          }), INVALID;
        }
        return { status: "valid", value: input.data };
      }
      get value() {
        return this._def.value;
      }
    };
    ZodLiteral.create = (value, params) => new ZodLiteral({
      value,
      typeName: ZodFirstPartyTypeKind.ZodLiteral,
      ...processCreateParams(params)
    });
    ZodEnum = class _ZodEnum extends ZodType {
      _parse(input) {
        if (typeof input.data != "string") {
          let ctx = this._getOrReturnCtx(input), expectedValues = this._def.values;
          return addIssueToContext(ctx, {
            expected: util.joinValues(expectedValues),
            received: ctx.parsedType,
            code: ZodIssueCode.invalid_type
          }), INVALID;
        }
        if (this._cache || (this._cache = new Set(this._def.values)), !this._cache.has(input.data)) {
          let ctx = this._getOrReturnCtx(input), expectedValues = this._def.values;
          return addIssueToContext(ctx, {
            received: ctx.data,
            code: ZodIssueCode.invalid_enum_value,
            options: expectedValues
          }), INVALID;
        }
        return OK(input.data);
      }
      get options() {
        return this._def.values;
      }
      get enum() {
        let enumValues = {};
        for (let val of this._def.values)
          enumValues[val] = val;
        return enumValues;
      }
      get Values() {
        let enumValues = {};
        for (let val of this._def.values)
          enumValues[val] = val;
        return enumValues;
      }
      get Enum() {
        let enumValues = {};
        for (let val of this._def.values)
          enumValues[val] = val;
        return enumValues;
      }
      extract(values, newDef = this._def) {
        return _ZodEnum.create(values, {
          ...this._def,
          ...newDef
        });
      }
      exclude(values, newDef = this._def) {
        return _ZodEnum.create(this.options.filter((opt) => !values.includes(opt)), {
          ...this._def,
          ...newDef
        });
      }
    };
    ZodEnum.create = createZodEnum;
    ZodNativeEnum = class extends ZodType {
      _parse(input) {
        let nativeEnumValues = util.getValidEnumValues(this._def.values), ctx = this._getOrReturnCtx(input);
        if (ctx.parsedType !== ZodParsedType.string && ctx.parsedType !== ZodParsedType.number) {
          let expectedValues = util.objectValues(nativeEnumValues);
          return addIssueToContext(ctx, {
            expected: util.joinValues(expectedValues),
            received: ctx.parsedType,
            code: ZodIssueCode.invalid_type
          }), INVALID;
        }
        if (this._cache || (this._cache = new Set(util.getValidEnumValues(this._def.values))), !this._cache.has(input.data)) {
          let expectedValues = util.objectValues(nativeEnumValues);
          return addIssueToContext(ctx, {
            received: ctx.data,
            code: ZodIssueCode.invalid_enum_value,
            options: expectedValues
          }), INVALID;
        }
        return OK(input.data);
      }
      get enum() {
        return this._def.values;
      }
    };
    ZodNativeEnum.create = (values, params) => new ZodNativeEnum({
      values,
      typeName: ZodFirstPartyTypeKind.ZodNativeEnum,
      ...processCreateParams(params)
    });
    ZodPromise = class extends ZodType {
      unwrap() {
        return this._def.type;
      }
      _parse(input) {
        let { ctx } = this._processInputParams(input);
        if (ctx.parsedType !== ZodParsedType.promise && ctx.common.async === !1)
          return addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.promise,
            received: ctx.parsedType
          }), INVALID;
        let promisified = ctx.parsedType === ZodParsedType.promise ? ctx.data : Promise.resolve(ctx.data);
        return OK(promisified.then((data) => this._def.type.parseAsync(data, {
          path: ctx.path,
          errorMap: ctx.common.contextualErrorMap
        })));
      }
    };
    ZodPromise.create = (schema, params) => new ZodPromise({
      type: schema,
      typeName: ZodFirstPartyTypeKind.ZodPromise,
      ...processCreateParams(params)
    });
    ZodEffects = class extends ZodType {
      innerType() {
        return this._def.schema;
      }
      sourceType() {
        return this._def.schema._def.typeName === ZodFirstPartyTypeKind.ZodEffects ? this._def.schema.sourceType() : this._def.schema;
      }
      _parse(input) {
        let { status, ctx } = this._processInputParams(input), effect = this._def.effect || null, checkCtx = {
          addIssue: (arg) => {
            addIssueToContext(ctx, arg), arg.fatal ? status.abort() : status.dirty();
          },
          get path() {
            return ctx.path;
          }
        };
        if (checkCtx.addIssue = checkCtx.addIssue.bind(checkCtx), effect.type === "preprocess") {
          let processed = effect.transform(ctx.data, checkCtx);
          if (ctx.common.async)
            return Promise.resolve(processed).then(async (processed2) => {
              if (status.value === "aborted")
                return INVALID;
              let result = await this._def.schema._parseAsync({
                data: processed2,
                path: ctx.path,
                parent: ctx
              });
              return result.status === "aborted" ? INVALID : result.status === "dirty" ? DIRTY(result.value) : status.value === "dirty" ? DIRTY(result.value) : result;
            });
          {
            if (status.value === "aborted")
              return INVALID;
            let result = this._def.schema._parseSync({
              data: processed,
              path: ctx.path,
              parent: ctx
            });
            return result.status === "aborted" ? INVALID : result.status === "dirty" ? DIRTY(result.value) : status.value === "dirty" ? DIRTY(result.value) : result;
          }
        }
        if (effect.type === "refinement") {
          let executeRefinement = (acc) => {
            let result = effect.refinement(acc, checkCtx);
            if (ctx.common.async)
              return Promise.resolve(result);
            if (result instanceof Promise)
              throw new Error("Async refinement encountered during synchronous parse operation. Use .parseAsync instead.");
            return acc;
          };
          if (ctx.common.async === !1) {
            let inner = this._def.schema._parseSync({
              data: ctx.data,
              path: ctx.path,
              parent: ctx
            });
            return inner.status === "aborted" ? INVALID : (inner.status === "dirty" && status.dirty(), executeRefinement(inner.value), { status: status.value, value: inner.value });
          } else
            return this._def.schema._parseAsync({ data: ctx.data, path: ctx.path, parent: ctx }).then((inner) => inner.status === "aborted" ? INVALID : (inner.status === "dirty" && status.dirty(), executeRefinement(inner.value).then(() => ({ status: status.value, value: inner.value }))));
        }
        if (effect.type === "transform")
          if (ctx.common.async === !1) {
            let base = this._def.schema._parseSync({
              data: ctx.data,
              path: ctx.path,
              parent: ctx
            });
            if (!isValid(base))
              return INVALID;
            let result = effect.transform(base.value, checkCtx);
            if (result instanceof Promise)
              throw new Error("Asynchronous transform encountered during synchronous parse operation. Use .parseAsync instead.");
            return { status: status.value, value: result };
          } else
            return this._def.schema._parseAsync({ data: ctx.data, path: ctx.path, parent: ctx }).then((base) => isValid(base) ? Promise.resolve(effect.transform(base.value, checkCtx)).then((result) => ({
              status: status.value,
              value: result
            })) : INVALID);
        util.assertNever(effect);
      }
    };
    ZodEffects.create = (schema, effect, params) => new ZodEffects({
      schema,
      typeName: ZodFirstPartyTypeKind.ZodEffects,
      effect,
      ...processCreateParams(params)
    });
    ZodEffects.createWithPreprocess = (preprocess, schema, params) => new ZodEffects({
      schema,
      effect: { type: "preprocess", transform: preprocess },
      typeName: ZodFirstPartyTypeKind.ZodEffects,
      ...processCreateParams(params)
    });
    ZodOptional = class extends ZodType {
      _parse(input) {
        return this._getType(input) === ZodParsedType.undefined ? OK(void 0) : this._def.innerType._parse(input);
      }
      unwrap() {
        return this._def.innerType;
      }
    };
    ZodOptional.create = (type, params) => new ZodOptional({
      innerType: type,
      typeName: ZodFirstPartyTypeKind.ZodOptional,
      ...processCreateParams(params)
    });
    ZodNullable = class extends ZodType {
      _parse(input) {
        return this._getType(input) === ZodParsedType.null ? OK(null) : this._def.innerType._parse(input);
      }
      unwrap() {
        return this._def.innerType;
      }
    };
    ZodNullable.create = (type, params) => new ZodNullable({
      innerType: type,
      typeName: ZodFirstPartyTypeKind.ZodNullable,
      ...processCreateParams(params)
    });
    ZodDefault = class extends ZodType {
      _parse(input) {
        let { ctx } = this._processInputParams(input), data = ctx.data;
        return ctx.parsedType === ZodParsedType.undefined && (data = this._def.defaultValue()), this._def.innerType._parse({
          data,
          path: ctx.path,
          parent: ctx
        });
      }
      removeDefault() {
        return this._def.innerType;
      }
    };
    ZodDefault.create = (type, params) => new ZodDefault({
      innerType: type,
      typeName: ZodFirstPartyTypeKind.ZodDefault,
      defaultValue: typeof params.default == "function" ? params.default : () => params.default,
      ...processCreateParams(params)
    });
    ZodCatch = class extends ZodType {
      _parse(input) {
        let { ctx } = this._processInputParams(input), newCtx = {
          ...ctx,
          common: {
            ...ctx.common,
            issues: []
          }
        }, result = this._def.innerType._parse({
          data: newCtx.data,
          path: newCtx.path,
          parent: {
            ...newCtx
          }
        });
        return isAsync(result) ? result.then((result2) => ({
          status: "valid",
          value: result2.status === "valid" ? result2.value : this._def.catchValue({
            get error() {
              return new ZodError(newCtx.common.issues);
            },
            input: newCtx.data
          })
        })) : {
          status: "valid",
          value: result.status === "valid" ? result.value : this._def.catchValue({
            get error() {
              return new ZodError(newCtx.common.issues);
            },
            input: newCtx.data
          })
        };
      }
      removeCatch() {
        return this._def.innerType;
      }
    };
    ZodCatch.create = (type, params) => new ZodCatch({
      innerType: type,
      typeName: ZodFirstPartyTypeKind.ZodCatch,
      catchValue: typeof params.catch == "function" ? params.catch : () => params.catch,
      ...processCreateParams(params)
    });
    ZodNaN = class extends ZodType {
      _parse(input) {
        if (this._getType(input) !== ZodParsedType.nan) {
          let ctx = this._getOrReturnCtx(input);
          return addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.nan,
            received: ctx.parsedType
          }), INVALID;
        }
        return { status: "valid", value: input.data };
      }
    };
    ZodNaN.create = (params) => new ZodNaN({
      typeName: ZodFirstPartyTypeKind.ZodNaN,
      ...processCreateParams(params)
    });
    BRAND = Symbol("zod_brand"), ZodBranded = class extends ZodType {
      _parse(input) {
        let { ctx } = this._processInputParams(input), data = ctx.data;
        return this._def.type._parse({
          data,
          path: ctx.path,
          parent: ctx
        });
      }
      unwrap() {
        return this._def.type;
      }
    }, ZodPipeline = class _ZodPipeline extends ZodType {
      _parse(input) {
        let { status, ctx } = this._processInputParams(input);
        if (ctx.common.async)
          return (async () => {
            let inResult = await this._def.in._parseAsync({
              data: ctx.data,
              path: ctx.path,
              parent: ctx
            });
            return inResult.status === "aborted" ? INVALID : inResult.status === "dirty" ? (status.dirty(), DIRTY(inResult.value)) : this._def.out._parseAsync({
              data: inResult.value,
              path: ctx.path,
              parent: ctx
            });
          })();
        {
          let inResult = this._def.in._parseSync({
            data: ctx.data,
            path: ctx.path,
            parent: ctx
          });
          return inResult.status === "aborted" ? INVALID : inResult.status === "dirty" ? (status.dirty(), {
            status: "dirty",
            value: inResult.value
          }) : this._def.out._parseSync({
            data: inResult.value,
            path: ctx.path,
            parent: ctx
          });
        }
      }
      static create(a, b) {
        return new _ZodPipeline({
          in: a,
          out: b,
          typeName: ZodFirstPartyTypeKind.ZodPipeline
        });
      }
    }, ZodReadonly = class extends ZodType {
      _parse(input) {
        let result = this._def.innerType._parse(input), freeze = (data) => (isValid(data) && (data.value = Object.freeze(data.value)), data);
        return isAsync(result) ? result.then((data) => freeze(data)) : freeze(result);
      }
      unwrap() {
        return this._def.innerType;
      }
    };
    ZodReadonly.create = (type, params) => new ZodReadonly({
      innerType: type,
      typeName: ZodFirstPartyTypeKind.ZodReadonly,
      ...processCreateParams(params)
    });
    late = {
      object: ZodObject.lazycreate
    };
    (function(ZodFirstPartyTypeKind2) {
      ZodFirstPartyTypeKind2.ZodString = "ZodString", ZodFirstPartyTypeKind2.ZodNumber = "ZodNumber", ZodFirstPartyTypeKind2.ZodNaN = "ZodNaN", ZodFirstPartyTypeKind2.ZodBigInt = "ZodBigInt", ZodFirstPartyTypeKind2.ZodBoolean = "ZodBoolean", ZodFirstPartyTypeKind2.ZodDate = "ZodDate", ZodFirstPartyTypeKind2.ZodSymbol = "ZodSymbol", ZodFirstPartyTypeKind2.ZodUndefined = "ZodUndefined", ZodFirstPartyTypeKind2.ZodNull = "ZodNull", ZodFirstPartyTypeKind2.ZodAny = "ZodAny", ZodFirstPartyTypeKind2.ZodUnknown = "ZodUnknown", ZodFirstPartyTypeKind2.ZodNever = "ZodNever", ZodFirstPartyTypeKind2.ZodVoid = "ZodVoid", ZodFirstPartyTypeKind2.ZodArray = "ZodArray", ZodFirstPartyTypeKind2.ZodObject = "ZodObject", ZodFirstPartyTypeKind2.ZodUnion = "ZodUnion", ZodFirstPartyTypeKind2.ZodDiscriminatedUnion = "ZodDiscriminatedUnion", ZodFirstPartyTypeKind2.ZodIntersection = "ZodIntersection", ZodFirstPartyTypeKind2.ZodTuple = "ZodTuple", ZodFirstPartyTypeKind2.ZodRecord = "ZodRecord", ZodFirstPartyTypeKind2.ZodMap = "ZodMap", ZodFirstPartyTypeKind2.ZodSet = "ZodSet", ZodFirstPartyTypeKind2.ZodFunction = "ZodFunction", ZodFirstPartyTypeKind2.ZodLazy = "ZodLazy", ZodFirstPartyTypeKind2.ZodLiteral = "ZodLiteral", ZodFirstPartyTypeKind2.ZodEnum = "ZodEnum", ZodFirstPartyTypeKind2.ZodEffects = "ZodEffects", ZodFirstPartyTypeKind2.ZodNativeEnum = "ZodNativeEnum", ZodFirstPartyTypeKind2.ZodOptional = "ZodOptional", ZodFirstPartyTypeKind2.ZodNullable = "ZodNullable", ZodFirstPartyTypeKind2.ZodDefault = "ZodDefault", ZodFirstPartyTypeKind2.ZodCatch = "ZodCatch", ZodFirstPartyTypeKind2.ZodPromise = "ZodPromise", ZodFirstPartyTypeKind2.ZodBranded = "ZodBranded", ZodFirstPartyTypeKind2.ZodPipeline = "ZodPipeline", ZodFirstPartyTypeKind2.ZodReadonly = "ZodReadonly";
    })(ZodFirstPartyTypeKind || (ZodFirstPartyTypeKind = {}));
    instanceOfType = (cls, params = {
      message: `Input not instance of ${cls.name}`
    }) => custom((data) => data instanceof cls, params), stringType = ZodString.create, numberType = ZodNumber.create, nanType = ZodNaN.create, bigIntType = ZodBigInt.create, booleanType = ZodBoolean.create, dateType = ZodDate.create, symbolType = ZodSymbol.create, undefinedType = ZodUndefined.create, nullType = ZodNull.create, anyType = ZodAny.create, unknownType = ZodUnknown.create, neverType = ZodNever.create, voidType = ZodVoid.create, arrayType = ZodArray.create, objectType = ZodObject.create, strictObjectType = ZodObject.strictCreate, unionType = ZodUnion.create, discriminatedUnionType = ZodDiscriminatedUnion.create, intersectionType = ZodIntersection.create, tupleType = ZodTuple.create, recordType = ZodRecord.create, mapType = ZodMap.create, setType = ZodSet.create, functionType = ZodFunction.create, lazyType = ZodLazy.create, literalType = ZodLiteral.create, enumType = ZodEnum.create, nativeEnumType = ZodNativeEnum.create, promiseType = ZodPromise.create, effectsType = ZodEffects.create, optionalType = ZodOptional.create, nullableType = ZodNullable.create, preprocessType = ZodEffects.createWithPreprocess, pipelineType = ZodPipeline.create, ostring = () => stringType().optional(), onumber = () => numberType().optional(), oboolean = () => booleanType().optional(), coerce = {
      string: ((arg) => ZodString.create({ ...arg, coerce: !0 })),
      number: ((arg) => ZodNumber.create({ ...arg, coerce: !0 })),
      boolean: ((arg) => ZodBoolean.create({
        ...arg,
        coerce: !0
      })),
      bigint: ((arg) => ZodBigInt.create({ ...arg, coerce: !0 })),
      date: ((arg) => ZodDate.create({ ...arg, coerce: !0 }))
    }, NEVER = INVALID;
  }
});

// ../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/external.js
var external_exports = {};
__export(external_exports, {
  BRAND: () => BRAND,
  DIRTY: () => DIRTY,
  EMPTY_PATH: () => EMPTY_PATH,
  INVALID: () => INVALID,
  NEVER: () => NEVER,
  OK: () => OK,
  ParseStatus: () => ParseStatus,
  Schema: () => ZodType,
  ZodAny: () => ZodAny,
  ZodArray: () => ZodArray,
  ZodBigInt: () => ZodBigInt,
  ZodBoolean: () => ZodBoolean,
  ZodBranded: () => ZodBranded,
  ZodCatch: () => ZodCatch,
  ZodDate: () => ZodDate,
  ZodDefault: () => ZodDefault,
  ZodDiscriminatedUnion: () => ZodDiscriminatedUnion,
  ZodEffects: () => ZodEffects,
  ZodEnum: () => ZodEnum,
  ZodError: () => ZodError,
  ZodFirstPartyTypeKind: () => ZodFirstPartyTypeKind,
  ZodFunction: () => ZodFunction,
  ZodIntersection: () => ZodIntersection,
  ZodIssueCode: () => ZodIssueCode,
  ZodLazy: () => ZodLazy,
  ZodLiteral: () => ZodLiteral,
  ZodMap: () => ZodMap,
  ZodNaN: () => ZodNaN,
  ZodNativeEnum: () => ZodNativeEnum,
  ZodNever: () => ZodNever,
  ZodNull: () => ZodNull,
  ZodNullable: () => ZodNullable,
  ZodNumber: () => ZodNumber,
  ZodObject: () => ZodObject,
  ZodOptional: () => ZodOptional,
  ZodParsedType: () => ZodParsedType,
  ZodPipeline: () => ZodPipeline,
  ZodPromise: () => ZodPromise,
  ZodReadonly: () => ZodReadonly,
  ZodRecord: () => ZodRecord,
  ZodSchema: () => ZodType,
  ZodSet: () => ZodSet,
  ZodString: () => ZodString,
  ZodSymbol: () => ZodSymbol,
  ZodTransformer: () => ZodEffects,
  ZodTuple: () => ZodTuple,
  ZodType: () => ZodType,
  ZodUndefined: () => ZodUndefined,
  ZodUnion: () => ZodUnion,
  ZodUnknown: () => ZodUnknown,
  ZodVoid: () => ZodVoid,
  addIssueToContext: () => addIssueToContext,
  any: () => anyType,
  array: () => arrayType,
  bigint: () => bigIntType,
  boolean: () => booleanType,
  coerce: () => coerce,
  custom: () => custom,
  date: () => dateType,
  datetimeRegex: () => datetimeRegex,
  defaultErrorMap: () => en_default,
  discriminatedUnion: () => discriminatedUnionType,
  effect: () => effectsType,
  enum: () => enumType,
  function: () => functionType,
  getErrorMap: () => getErrorMap,
  getParsedType: () => getParsedType,
  instanceof: () => instanceOfType,
  intersection: () => intersectionType,
  isAborted: () => isAborted,
  isAsync: () => isAsync,
  isDirty: () => isDirty,
  isValid: () => isValid,
  late: () => late,
  lazy: () => lazyType,
  literal: () => literalType,
  makeIssue: () => makeIssue,
  map: () => mapType,
  nan: () => nanType,
  nativeEnum: () => nativeEnumType,
  never: () => neverType,
  null: () => nullType,
  nullable: () => nullableType,
  number: () => numberType,
  object: () => objectType,
  objectUtil: () => objectUtil,
  oboolean: () => oboolean,
  onumber: () => onumber,
  optional: () => optionalType,
  ostring: () => ostring,
  pipeline: () => pipelineType,
  preprocess: () => preprocessType,
  promise: () => promiseType,
  quotelessJson: () => quotelessJson,
  record: () => recordType,
  set: () => setType,
  setErrorMap: () => setErrorMap,
  strictObject: () => strictObjectType,
  string: () => stringType,
  symbol: () => symbolType,
  transformer: () => effectsType,
  tuple: () => tupleType,
  undefined: () => undefinedType,
  union: () => unionType,
  unknown: () => unknownType,
  util: () => util,
  void: () => voidType
});
var init_external = __esm({
  "../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/external.js"() {
    init_errors();
    init_parseUtil();
    init_typeAliases();
    init_util();
    init_types();
    init_ZodError();
  }
});

// ../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/index.js
var init_zod = __esm({
  "../../node_modules/.pnpm/zod@3.25.76/node_modules/zod/index.js"() {
    init_external();
    init_external();
  }
});

// ../core/dist/schemas/index.js
var LedgerEventTypeSchema, LedgerEventSchema, TaskStatusSchema, AgentTaskSchema, TaskGraphSchema, IntentContractSchema, WorktreeHandleSchema, WorkerContextSchema, ToolDenialSchema, WorkerResultSchema, VerificationCommandSchema, CommandResultSchema, BoundaryViolationSchema, BoundaryCheckResultSchema, VerificationResultSchema, RunStatusSchema, RunStateSchema, ApprovalTriggerSchema, ApprovalPolicySchema, PendingApprovalSchema, PriorTaskContextSchema, CompletedTaskSummarySchema, PendingTaskSummarySchema, FailedTaskSummarySchema, AwaitingApprovalSummarySchema, HandoffDocumentSchema, PatchRiskSeveritySchema, PatchRiskCategorySchema, PatchRiskSchema, GovernancePolicyRuleSchema, RiskThresholdActionSchema, GovernancePolicySchema, PolicyDecisionSchema, ApprovalRecordSchema, AuditTaskRecordSchema, RiskScoreBreakdownSchema, RiskScoreSchema, LeaderboardEntrySchema, PolicyLeaderboardSchema, GovernanceSummarySchema, AuditReportSchema, FailureReasonSchema, FailureContextSchema, FailedTaskDetailSchema, FileInventorySchema, UnresolvedRiskSchema, ResumptionActionSchema, ResumptionGuidanceSchema, HandoffBriefSchema, RunModeSchema, RunCreatedPayloadSchema, ProjectEntrySchema, ProjectRegistrySchema, AgentLedgerConfigSchema, init_schemas = __esm({
  "../core/dist/schemas/index.js"() {
    "use strict";
    init_zod();
    LedgerEventTypeSchema = external_exports.enum([
      "RUN_CREATED",
      "INTENT_COMPILED",
      "TASK_CREATED",
      "TASK_ASSIGNED",
      "TASK_STARTED",
      "WORKTREE_CREATED",
      "CONTEXT_READ",
      "TOOL_CALLED",
      "TOOL_DENIED",
      "FILE_EDIT_PROPOSED",
      "PATCH_PROPOSED",
      "PATCH_RISK_DETECTED",
      "POLICY_EVALUATED",
      "RISK_THRESHOLD_BREACHED",
      "HUMAN_APPROVAL_REQUESTED",
      "HUMAN_APPROVAL_GRANTED",
      "HUMAN_APPROVAL_REJECTED",
      "VERIFICATION_STARTED",
      "VERIFICATION_PASSED",
      "VERIFICATION_FAILED",
      "BOUNDARY_VIOLATION",
      // Lie Detector — emitted by the plugin's Stop hook on every assistant turn that
      // makes a completion claim. CLAIM_DETECTED records the claim; the CLAIM_VERIFIED /
      // CLAIM_FALSIFIED / CLAIM_UNVERIFIABLE that follows records what checking it found.
      "CLAIM_DETECTED",
      "CLAIM_VERIFIED",
      "CLAIM_FALSIFIED",
      "CLAIM_UNVERIFIABLE",
      "TASK_COMPLETED",
      "TASK_FAILED",
      "RUN_COMPLETED",
      "RUN_FAILED"
    ]), LedgerEventSchema = external_exports.object({
      event_id: external_exports.string(),
      run_id: external_exports.string(),
      task_id: external_exports.string().optional(),
      timestamp: external_exports.string().datetime(),
      actor: external_exports.string(),
      event_type: LedgerEventTypeSchema,
      payload: external_exports.record(external_exports.string(), external_exports.unknown()),
      hash: external_exports.string(),
      previous_hash: external_exports.string()
    }), TaskStatusSchema = external_exports.enum([
      "pending",
      "assigned",
      "running",
      "awaiting_approval",
      "awaiting_verification",
      "completed",
      "failed"
    ]), AgentTaskSchema = external_exports.object({
      taskId: external_exports.string(),
      runId: external_exports.string(),
      title: external_exports.string(),
      description: external_exports.string(),
      owner: external_exports.string(),
      dependencies: external_exports.array(external_exports.string()),
      allowedFiles: external_exports.array(external_exports.string()),
      blockedFiles: external_exports.array(external_exports.string()),
      allowedTools: external_exports.array(external_exports.string()),
      expectedOutputs: external_exports.array(external_exports.string()),
      successCriteria: external_exports.array(external_exports.string()),
      status: TaskStatusSchema,
      /** Optional path to a per-task governance.json, relative to the .agentledger/ directory */
      governancePolicyFile: external_exports.string().optional()
    }), TaskGraphSchema = external_exports.object({
      runId: external_exports.string(),
      tasks: external_exports.array(AgentTaskSchema)
    }), IntentContractSchema = external_exports.object({
      runId: external_exports.string(),
      goal: external_exports.string(),
      constraints: external_exports.array(external_exports.string()),
      successCriteria: external_exports.array(external_exports.string()),
      riskLevel: external_exports.enum(["low", "medium", "high"]),
      budget: external_exports.object({
        maxTokens: external_exports.number().optional(),
        maxUsd: external_exports.number().optional(),
        maxToolCalls: external_exports.number().optional()
      }).optional(),
      approvalRequiredFor: external_exports.array(external_exports.string()).optional()
    }), WorktreeHandleSchema = external_exports.object({
      taskId: external_exports.string(),
      branch: external_exports.string(),
      worktreePath: external_exports.string()
    }), WorkerContextSchema = external_exports.object({
      task: AgentTaskSchema,
      relevantContext: external_exports.record(external_exports.string(), external_exports.unknown()),
      worktreePath: external_exports.string(),
      allowedFiles: external_exports.array(external_exports.string()),
      blockedFiles: external_exports.array(external_exports.string()),
      allowedTools: external_exports.array(external_exports.string()),
      outputSchema: external_exports.record(external_exports.string(), external_exports.unknown())
    }), ToolDenialSchema = external_exports.object({
      toolName: external_exports.string(),
      path: external_exports.string(),
      reason: external_exports.string(),
      violationType: external_exports.enum(["BLOCKED_FILE", "UNOWNED_FILE"])
    }), WorkerResultSchema = external_exports.object({
      taskId: external_exports.string(),
      summary: external_exports.string(),
      filesRead: external_exports.array(external_exports.string()),
      filesModified: external_exports.array(external_exports.string()),
      patchPath: external_exports.string().optional(),
      worktreeBranch: external_exports.string(),
      toolDenials: external_exports.array(ToolDenialSchema).default([]),
      output: external_exports.record(external_exports.string(), external_exports.unknown())
    }), VerificationCommandSchema = external_exports.object({
      name: external_exports.string(),
      command: external_exports.string(),
      required: external_exports.boolean().default(!0)
    }), CommandResultSchema = external_exports.object({
      name: external_exports.string(),
      command: external_exports.string(),
      exitCode: external_exports.number(),
      stdout: external_exports.string(),
      stderr: external_exports.string(),
      durationMs: external_exports.number()
    }), BoundaryViolationSchema = external_exports.object({
      violationType: external_exports.enum([
        "BLOCKED_FILE_MODIFIED",
        "UNOWNED_FILE_MODIFIED",
        "UNEXPECTED_FILE"
      ]),
      file: external_exports.string(),
      message: external_exports.string()
    }), BoundaryCheckResultSchema = external_exports.object({
      passed: external_exports.boolean(),
      violations: external_exports.array(BoundaryViolationSchema)
    }), VerificationResultSchema = external_exports.object({
      taskId: external_exports.string(),
      passed: external_exports.boolean(),
      boundaryCheck: BoundaryCheckResultSchema,
      commandResults: external_exports.array(CommandResultSchema)
    }), RunStatusSchema = external_exports.enum([
      "created",
      "planning",
      "executing",
      "verifying",
      "paused",
      "completed",
      "failed"
    ]), RunStateSchema = external_exports.object({
      runId: external_exports.string(),
      status: RunStatusSchema,
      goal: external_exports.string(),
      operator: external_exports.string().optional(),
      tasks: external_exports.array(AgentTaskSchema),
      filesModified: external_exports.array(external_exports.string()),
      startedAt: external_exports.string().datetime().optional(),
      completedAt: external_exports.string().datetime().optional()
    }), ApprovalTriggerSchema = external_exports.enum([
      "all",
      "high_risk_keywords",
      "new_dependencies",
      "blocked_files_nearby"
    ]), ApprovalPolicySchema = external_exports.object({
      requireApprovalFor: external_exports.array(ApprovalTriggerSchema).default(["high_risk_keywords"]),
      /** "post_patch" = after worker finishes, before verification */
      mode: external_exports.enum(["post_patch"]).default("post_patch"),
      /** Optional timeout in seconds before an unanswered request auto-rejects */
      timeoutSeconds: external_exports.number().int().min(60).optional()
    }), PendingApprovalSchema = external_exports.object({
      runId: external_exports.string(),
      taskId: external_exports.string(),
      requestedAt: external_exports.string().datetime(),
      reasons: external_exports.array(external_exports.string()),
      filesModified: external_exports.array(external_exports.string()),
      summary: external_exports.string()
    }), PriorTaskContextSchema = external_exports.object({
      taskId: external_exports.string(),
      title: external_exports.string(),
      summary: external_exports.string(),
      filesModified: external_exports.array(external_exports.string())
    }), CompletedTaskSummarySchema = external_exports.object({
      taskId: external_exports.string(),
      title: external_exports.string(),
      summary: external_exports.string(),
      filesModified: external_exports.array(external_exports.string())
    }), PendingTaskSummarySchema = external_exports.object({
      taskId: external_exports.string(),
      title: external_exports.string(),
      description: external_exports.string(),
      owner: external_exports.string(),
      blockedBy: external_exports.array(external_exports.string())
    }), FailedTaskSummarySchema = external_exports.object({
      taskId: external_exports.string(),
      title: external_exports.string(),
      failureReason: external_exports.string()
    }), AwaitingApprovalSummarySchema = external_exports.object({
      taskId: external_exports.string(),
      title: external_exports.string(),
      requestedAt: external_exports.string()
    }), HandoffDocumentSchema = external_exports.object({
      runId: external_exports.string(),
      goal: external_exports.string(),
      runStatus: RunStatusSchema,
      completedTasks: external_exports.array(CompletedTaskSummarySchema),
      pendingTasks: external_exports.array(PendingTaskSummarySchema),
      failedTasks: external_exports.array(FailedTaskSummarySchema),
      awaitingApproval: external_exports.array(AwaitingApprovalSummarySchema),
      allFilesModified: external_exports.array(external_exports.string()),
      suggestedNextAction: external_exports.string()
    }), PatchRiskSeveritySchema = external_exports.enum(["critical", "high", "medium"]), PatchRiskCategorySchema = external_exports.enum([
      "secret",
      "schema_mutation",
      "auth_code",
      "dependency_change"
    ]), PatchRiskSchema = external_exports.object({
      pattern: external_exports.string(),
      severity: PatchRiskSeveritySchema,
      category: PatchRiskCategorySchema,
      filePath: external_exports.string(),
      lineNumber: external_exports.number().int().min(1),
      lineContext: external_exports.string()
    }), GovernancePolicyRuleSchema = external_exports.object({
      type: external_exports.enum(["deny_if", "require_approval_if", "warn_if"]),
      categories: external_exports.array(PatchRiskCategorySchema),
      minSeverity: PatchRiskSeveritySchema.optional()
    }), RiskThresholdActionSchema = external_exports.enum(["warn", "pause", "abort"]), GovernancePolicySchema = external_exports.object({
      rules: external_exports.array(GovernancePolicyRuleSchema),
      /** Cumulative run risk score (0-100) that triggers thresholdAction */
      riskThreshold: external_exports.number().int().min(0).max(100).optional(),
      /** What to do when riskThreshold is breached. Default "warn". */
      thresholdAction: RiskThresholdActionSchema.optional()
    }), PolicyDecisionSchema = external_exports.object({
      action: external_exports.enum(["allow", "warn", "deny", "require_approval"]),
      reasons: external_exports.array(external_exports.string()),
      risks: external_exports.array(PatchRiskSchema)
    }), ApprovalRecordSchema = external_exports.object({
      taskId: external_exports.string(),
      requestedAt: external_exports.string(),
      grantedAt: external_exports.string().optional(),
      rejectedAt: external_exports.string().optional(),
      reasons: external_exports.array(external_exports.string())
    }), AuditTaskRecordSchema = external_exports.object({
      taskId: external_exports.string(),
      title: external_exports.string(),
      status: TaskStatusSchema,
      filesModified: external_exports.array(external_exports.string()),
      patchRisks: external_exports.array(PatchRiskSchema),
      toolDenials: external_exports.array(ToolDenialSchema),
      boundaryViolations: external_exports.array(BoundaryViolationSchema),
      policyDecision: PolicyDecisionSchema.optional(),
      approvalRecord: ApprovalRecordSchema.optional()
    }), RiskScoreBreakdownSchema = external_exports.object({
      secret_exposure: external_exports.number().min(0).max(40),
      schema_change: external_exports.number().min(0).max(30),
      auth_change: external_exports.number().min(0).max(20),
      boundary_violation: external_exports.number().min(0).max(10),
      tool_denial: external_exports.number().min(0).max(10)
    }), RiskScoreSchema = external_exports.object({
      total: external_exports.number().int().min(0).max(100),
      breakdown: RiskScoreBreakdownSchema
    }), LeaderboardEntrySchema = external_exports.object({
      runId: external_exports.string(),
      taskId: external_exports.string(),
      title: external_exports.string(),
      riskScore: external_exports.number().int().min(0).max(100),
      denyCount: external_exports.number().int().min(0),
      requireApprovalCount: external_exports.number().int().min(0),
      boundaryViolationCount: external_exports.number().int().min(0),
      toolDenialCount: external_exports.number().int().min(0)
    }), PolicyLeaderboardSchema = external_exports.object({
      generatedAt: external_exports.string().datetime(),
      entries: external_exports.array(LeaderboardEntrySchema)
    }), GovernanceSummarySchema = external_exports.object({
      policyDecisionCounts: external_exports.object({
        allow: external_exports.number().int().min(0),
        warn: external_exports.number().int().min(0),
        require_approval: external_exports.number().int().min(0),
        deny: external_exports.number().int().min(0)
      }),
      thresholdBreached: external_exports.boolean(),
      thresholdBreachAction: RiskThresholdActionSchema.optional()
    }), AuditReportSchema = external_exports.object({
      runId: external_exports.string(),
      goal: external_exports.string(),
      runStatus: RunStatusSchema,
      generatedAt: external_exports.string().datetime(),
      tasks: external_exports.array(AuditTaskRecordSchema),
      allFilesModified: external_exports.array(external_exports.string()),
      riskScore: RiskScoreSchema,
      approvalsSummary: external_exports.object({
        total: external_exports.number().int().min(0),
        granted: external_exports.number().int().min(0),
        rejected: external_exports.number().int().min(0),
        pending: external_exports.number().int().min(0)
      }),
      governanceSummary: GovernanceSummarySchema
    }), FailureReasonSchema = external_exports.enum([
      "boundary_violation",
      "verification_failed",
      "governance_denied",
      "human_approval_rejected",
      "tool_denial",
      "tool_call_limit_exceeded",
      "unknown"
    ]), FailureContextSchema = external_exports.object({
      violatedFile: external_exports.string().optional(),
      violationType: external_exports.string().optional(),
      governanceCategory: external_exports.string().optional(),
      toolName: external_exports.string().optional(),
      exitCode: external_exports.number().optional(),
      detail: external_exports.string().optional()
    }), FailedTaskDetailSchema = external_exports.object({
      taskId: external_exports.string(),
      title: external_exports.string(),
      reason: FailureReasonSchema,
      context: FailureContextSchema.optional(),
      attemptedFiles: external_exports.array(external_exports.string())
    }), FileInventorySchema = external_exports.object({
      mergedFiles: external_exports.array(external_exports.string()),
      worktreeFiles: external_exports.array(external_exports.string()),
      allFiles: external_exports.array(external_exports.string())
    }), UnresolvedRiskSchema = external_exports.object({
      taskId: external_exports.string(),
      category: PatchRiskCategorySchema,
      severity: PatchRiskSeveritySchema,
      filePath: external_exports.string(),
      pattern: external_exports.string()
    }), ResumptionActionSchema = external_exports.enum([
      "approve_pending",
      "retry_failed_task",
      "resume_run",
      "run_completed",
      "investigate_failure"
    ]), ResumptionGuidanceSchema = external_exports.object({
      action: ResumptionActionSchema,
      targetTaskId: external_exports.string().optional(),
      command: external_exports.string(),
      detail: external_exports.string()
    }), HandoffBriefSchema = external_exports.object({
      generatedAt: external_exports.string().datetime(),
      runId: external_exports.string(),
      goal: external_exports.string(),
      operator: external_exports.string().optional(),
      runStatus: RunStatusSchema,
      completedTasks: external_exports.array(CompletedTaskSummarySchema),
      failedTasks: external_exports.array(FailedTaskDetailSchema),
      inProgressTasks: external_exports.array(PendingTaskSummarySchema),
      pendingTasks: external_exports.array(PendingTaskSummarySchema),
      awaitingApproval: external_exports.array(AwaitingApprovalSummarySchema),
      fileInventory: FileInventorySchema,
      unresolvedRisks: external_exports.array(UnresolvedRiskSchema),
      resumptionGuidance: ResumptionGuidanceSchema,
      contextSummary: external_exports.string()
    }), RunModeSchema = external_exports.enum(["orchestrated", "observed"]), RunCreatedPayloadSchema = external_exports.object({
      goal: external_exports.string(),
      riskLevel: external_exports.string().optional(),
      operator: external_exports.string().optional(),
      run_mode: RunModeSchema.default("orchestrated")
    }), ProjectEntrySchema = external_exports.object({
      path: external_exports.string().min(1),
      name: external_exports.string().min(1),
      firstSeen: external_exports.string(),
      lastSeen: external_exports.string()
    }), ProjectRegistrySchema = external_exports.object({
      version: external_exports.literal(1),
      projects: external_exports.array(ProjectEntrySchema)
    }), AgentLedgerConfigSchema = external_exports.object({
      version: external_exports.string(),
      verification: external_exports.object({
        commands: external_exports.record(external_exports.string(), external_exports.string()),
        required: external_exports.array(external_exports.string())
      }),
      budget: external_exports.object({
        maxTokens: external_exports.number().optional(),
        maxUsd: external_exports.number().optional(),
        maxToolCalls: external_exports.number().optional()
      }).optional(),
      approvalPolicy: ApprovalPolicySchema.optional()
    });
  }
});

// ../core/dist/ledger/hashChain.js
function computeHash(previousHash, payload) {
  return (0, import_crypto.createHash)("sha256").update(previousHash + JSON.stringify(payload)).digest("hex");
}
function isValidHash(previousHash, payload, hash) {
  return computeHash(previousHash, payload) === hash;
}
var import_crypto, init_hashChain = __esm({
  "../core/dist/ledger/hashChain.js"() {
    "use strict";
    import_crypto = require("crypto");
  }
});

// ../core/dist/ledger/LedgerWriter.js
var import_promises, import_fs2, import_path2, import_crypto2, GENESIS_HASH, LedgerWriter, init_LedgerWriter = __esm({
  "../core/dist/ledger/LedgerWriter.js"() {
    "use strict";
    import_promises = require("fs/promises"), import_fs2 = require("fs"), import_path2 = require("path"), import_crypto2 = require("crypto");
    init_schemas();
    init_hashChain();
    GENESIS_HASH = "genesis", LedgerWriter = class {
      ledgerPath;
      /** Serial promise queue — ensures concurrent callers never interleave read-then-write */
      _writeQueue = Promise.resolve();
      constructor(ledgerPath3) {
        this.ledgerPath = ledgerPath3;
      }
      async appendEvent(event) {
        return new Promise((resolve4, reject) => {
          this._writeQueue = this._writeQueue.then(async () => {
            try {
              let result = await this._appendEventUnsafe(event);
              resolve4(result);
            } catch (err) {
              reject(err);
            }
          });
        });
      }
      async _appendEventUnsafe(event) {
        await this.ensureLedgerExists();
        let previousHash = await this.getLastEventHash(), hash = computeHash(previousHash, event.payload), fullEvent = LedgerEventSchema.parse({
          ...event,
          hash,
          previous_hash: previousHash
        }), line = JSON.stringify(fullEvent) + `
`;
        return await (0, import_promises.appendFile)(this.ledgerPath, line, { encoding: "utf8" }), fullEvent;
      }
      async getLastEventHash() {
        if (!(0, import_fs2.existsSync)(this.ledgerPath))
          return GENESIS_HASH;
        let lines = (await (0, import_promises.readFile)(this.ledgerPath, "utf8")).split(`
`).filter((l) => l.trim().length > 0);
        if (lines.length === 0)
          return GENESIS_HASH;
        let lastLine = lines[lines.length - 1];
        if (lastLine === void 0)
          return GENESIS_HASH;
        let parsed = JSON.parse(lastLine);
        return typeof parsed == "object" && parsed !== null && "hash" in parsed && typeof parsed.hash == "string" ? parsed.hash : GENESIS_HASH;
      }
      async ensureLedgerExists() {
        let dir = (0, import_path2.dirname)(this.ledgerPath);
        (0, import_fs2.existsSync)(dir) || await (0, import_promises.mkdir)(dir, { recursive: !0 }), (0, import_fs2.existsSync)(this.ledgerPath) || await (0, import_promises.appendFile)(this.ledgerPath, "", { encoding: "utf8" });
      }
      static createEventId() {
        return (0, import_crypto2.randomUUID)();
      }
    };
  }
});

// ../core/dist/ledger/LedgerReader.js
var import_promises2, import_fs3, GENESIS_HASH2, LedgerReader, init_LedgerReader = __esm({
  "../core/dist/ledger/LedgerReader.js"() {
    "use strict";
    import_promises2 = require("fs/promises"), import_fs3 = require("fs");
    init_schemas();
    init_hashChain();
    GENESIS_HASH2 = "genesis", LedgerReader = class {
      ledgerPath;
      constructor(ledgerPath3) {
        this.ledgerPath = ledgerPath3;
      }
      async readAll() {
        return (0, import_fs3.existsSync)(this.ledgerPath) ? (await (0, import_promises2.readFile)(this.ledgerPath, "utf8")).split(`
`).filter((l) => l.trim().length > 0).map((line, index) => {
          try {
            return LedgerEventSchema.parse(JSON.parse(line));
          } catch (err) {
            throw new Error(`Failed to parse ledger line ${index}: ${String(err)}`);
          }
        }) : [];
      }
      async readByRunId(runId) {
        return (await this.readAll()).filter((e) => e.run_id === runId);
      }
      async readByTaskId(taskId) {
        return (await this.readAll()).filter((e) => e.task_id === taskId);
      }
      async verifyChain() {
        let events = await this.readAll();
        if (events.length === 0)
          return { valid: !0 };
        for (let i = 0; i < events.length; i++) {
          let event = events[i];
          if (event === void 0)
            continue;
          let expectedPreviousHash = i === 0 ? GENESIS_HASH2 : events[i - 1]?.hash ?? GENESIS_HASH2;
          if (event.previous_hash !== expectedPreviousHash)
            return {
              valid: !1,
              firstInvalidIndex: i,
              reason: `Event ${i} previous_hash mismatch: expected "${expectedPreviousHash}", got "${event.previous_hash}"`
            };
          if (!isValidHash(event.previous_hash, event.payload, event.hash))
            return {
              valid: !1,
              firstInvalidIndex: i,
              reason: `Event ${i} hash is invalid (payload or hash was tampered)`
            };
        }
        return { valid: !0 };
      }
    };
  }
});

// ../core/dist/ledger/index.js
var init_ledger = __esm({
  "../core/dist/ledger/index.js"() {
    "use strict";
    init_LedgerWriter();
    init_LedgerReader();
    init_hashChain();
  }
});

// ../core/dist/planner/validateTaskGraph.js
function validateTaskGraph(graph) {
  let errors = [], taskIds = /* @__PURE__ */ new Set();
  for (let task of graph.tasks)
    taskIds.has(task.taskId) && errors.push({ type: "DUPLICATE_TASK_ID", taskId: task.taskId }), taskIds.add(task.taskId);
  for (let task of graph.tasks)
    for (let dep of task.dependencies)
      taskIds.has(dep) || errors.push({
        type: "MISSING_DEPENDENCY",
        taskId: task.taskId,
        missingDep: dep
      });
  let cycle = detectCycle(graph.tasks);
  cycle !== null && errors.push({ type: "DEPENDENCY_CYCLE", cycle });
  let overlapErrors = detectOwnershipOverlap(graph.tasks);
  return errors.push(...overlapErrors), errors.length > 0 ? { valid: !1, errors } : { valid: !0 };
}
function detectCycle(tasks) {
  let adjacency = /* @__PURE__ */ new Map();
  for (let task of tasks)
    adjacency.set(task.taskId, [...task.dependencies]);
  let WHITE = 0, GRAY = 1, BLACK = 2, color = /* @__PURE__ */ new Map(), parent = /* @__PURE__ */ new Map();
  for (let task of tasks)
    color.set(task.taskId, WHITE), parent.set(task.taskId, null);
  let foundCycle = null;
  function dfs(nodeId) {
    color.set(nodeId, GRAY);
    for (let neighbor of adjacency.get(nodeId) ?? []) {
      let neighborColor = color.get(neighbor) ?? WHITE;
      if (neighborColor === GRAY) {
        let cycle = [neighbor, nodeId], current = nodeId;
        for (; current !== neighbor; ) {
          let p = parent.get(current);
          if (p == null)
            break;
          cycle.push(p), current = p;
        }
        return foundCycle = cycle.reverse(), !0;
      }
      if (neighborColor === WHITE && (parent.set(neighbor, nodeId), dfs(neighbor)))
        return !0;
    }
    return color.set(nodeId, BLACK), !1;
  }
  for (let task of tasks)
    if ((color.get(task.taskId) ?? WHITE) === WHITE && dfs(task.taskId))
      break;
  return foundCycle;
}
function detectOwnershipOverlap(tasks) {
  let errors = [];
  for (let i = 0; i < tasks.length; i++)
    for (let j = i + 1; j < tasks.length; j++) {
      let taskA = tasks[i], taskB = tasks[j];
      if (taskA === void 0 || taskB === void 0)
        continue;
      let aDepB = taskA.dependencies.includes(taskB.taskId), bDepA = taskB.dependencies.includes(taskA.taskId);
      if (!(aDepB || bDepA))
        for (let fileA of taskA.allowedFiles)
          for (let fileB of taskB.allowedFiles)
            fileA === fileB && errors.push({
              type: "OVERLAPPING_OWNERSHIP",
              taskA: taskA.taskId,
              taskB: taskB.taskId,
              file: fileA
            });
    }
  return errors;
}
function topoSort(tasks) {
  let taskMap = /* @__PURE__ */ new Map();
  for (let task of tasks)
    taskMap.set(task.taskId, task);
  let visited = /* @__PURE__ */ new Set(), result = [];
  function visit(taskId) {
    if (visited.has(taskId))
      return;
    visited.add(taskId);
    let task = taskMap.get(taskId);
    if (task !== void 0) {
      for (let dep of task.dependencies)
        visit(dep);
      result.push(task);
    }
  }
  for (let task of tasks)
    visit(task.taskId);
  return result;
}
var init_validateTaskGraph = __esm({
  "../core/dist/planner/validateTaskGraph.js"() {
    "use strict";
  }
});

// ../core/dist/planner/createPlan.js
function createPlan(intent) {
  let analyzeTaskId = `task-${(0, import_crypto3.randomUUID)().slice(0, 8)}`, implementTaskId = `task-${(0, import_crypto3.randomUUID)().slice(0, 8)}`, analyzeTask = {
    taskId: analyzeTaskId,
    runId: intent.runId,
    title: "Analyze repository",
    description: `Analyze the repository to understand current structure and plan changes for: ${intent.goal}`,
    owner: "worker-analyze",
    dependencies: [],
    allowedFiles: ["**/*.md", "**/*.json", "src/**/*.ts"],
    blockedFiles: ["**/*.env", "**/secrets/**", "**/.git/**"],
    allowedTools: ["read_file", "list_directory", "search_files"],
    expectedOutputs: ["analysis-report"],
    successCriteria: intent.successCriteria.map((c) => `[analyze] ${c}`),
    status: "pending"
  }, implementTask = {
    taskId: implementTaskId,
    runId: intent.runId,
    title: "Implement changes",
    description: `Implement the following goal based on analysis: ${intent.goal}`,
    owner: "worker-implement",
    dependencies: [analyzeTaskId],
    allowedFiles: ["src/**/*.ts", "src/**/*.tsx", "**/*.json"],
    blockedFiles: [
      "**/*.env",
      "**/secrets/**",
      "**/.git/**",
      "**/*.test.ts",
      "**/*.spec.ts"
    ],
    allowedTools: ["read_file", "write_file", "edit_file", "run_command"],
    expectedOutputs: ["modified-files", "patch"],
    successCriteria: intent.successCriteria,
    status: "pending"
  }, graph = TaskGraphSchema.parse({
    runId: intent.runId,
    tasks: [analyzeTask, implementTask]
  }), validationResult = validateTaskGraph(graph);
  if (!validationResult.valid) {
    let messages = validationResult.errors.map((e) => JSON.stringify(e)).join(", ");
    throw new Error(`createPlan produced an invalid task graph: ${messages}`);
  }
  return graph;
}
var import_crypto3, init_createPlan = __esm({
  "../core/dist/planner/createPlan.js"() {
    "use strict";
    import_crypto3 = require("crypto");
    init_schemas();
    init_validateTaskGraph();
  }
});

// ../core/dist/planner/index.js
var init_planner = __esm({
  "../core/dist/planner/index.js"() {
    "use strict";
    init_createPlan();
    init_validateTaskGraph();
  }
});

// ../core/dist/git/createWorktree.js
async function createTaskWorktree(repoRoot, task, worktreeBaseDir) {
  let branch = `${BRANCH_PREFIX}/${task.taskId}`, worktreePath = (0, import_path3.join)(worktreeBaseDir, task.taskId);
  return await (0, import_promises3.mkdir)(worktreeBaseDir, { recursive: !0 }), await execAsync(`git worktree add -b "${branch}" "${worktreePath}"`, {
    cwd: repoRoot
  }), task.allowedFiles.length > 0 && await applySparseCheckout(worktreePath, task.allowedFiles), WorktreeHandleSchema.parse({ taskId: task.taskId, branch, worktreePath });
}
async function cleanupWorktree(repoRoot, handle) {
  try {
    await execAsync(`git worktree remove "${handle.worktreePath}" --force`, {
      cwd: repoRoot
    });
  } catch {
  }
  try {
    await execAsync(`git branch -D "${handle.branch}"`, { cwd: repoRoot });
  } catch {
  }
}
async function applySparseCheckout(worktreePath, patterns) {
  await execAsync("git config core.sparseCheckout true", { cwd: worktreePath });
  let { stdout } = await execAsync("git rev-parse --git-dir", {
    cwd: worktreePath
  }), gitDir = stdout.trim(), resolvedGitDir = (0, import_path3.isAbsolute)(gitDir) ? gitDir : (0, import_path3.join)(worktreePath, gitDir), infoDir = (0, import_path3.join)(resolvedGitDir, "info");
  await (0, import_promises3.mkdir)(infoDir, { recursive: !0 });
  let sparseFile = (0, import_path3.join)(infoDir, "sparse-checkout");
  await (0, import_promises3.writeFile)(sparseFile, patterns.join(`
`) + `
`, "utf8"), await execAsync("git read-tree -mu HEAD", { cwd: worktreePath });
}
var import_child_process, import_util4, import_promises3, import_path3, execAsync, BRANCH_PREFIX, init_createWorktree = __esm({
  "../core/dist/git/createWorktree.js"() {
    "use strict";
    import_child_process = require("child_process"), import_util4 = require("util"), import_promises3 = require("fs/promises"), import_path3 = require("path");
    init_schemas();
    execAsync = (0, import_util4.promisify)(import_child_process.exec), BRANCH_PREFIX = "agentledger";
  }
});

// ../core/dist/git/generatePatch.js
async function generatePatch(worktreePath, outputPath) {
  await execAsync2("git add -A --sparse", { cwd: worktreePath });
  let { stdout: patch } = await execAsync2("git diff --cached HEAD", {
    cwd: worktreePath
  });
  return await (0, import_promises4.mkdir)((0, import_path4.dirname)(outputPath), { recursive: !0 }), await (0, import_promises4.writeFile)(outputPath, patch, "utf8"), patch;
}
async function listModifiedFiles(worktreePath) {
  await execAsync2("git add -A --sparse", { cwd: worktreePath });
  let { stdout } = await execAsync2("git diff --cached --name-only HEAD", { cwd: worktreePath });
  return stdout.split(`
`).map((f) => f.trim()).filter((f) => f.length > 0);
}
var import_child_process2, import_util5, import_promises4, import_path4, execAsync2, init_generatePatch = __esm({
  "../core/dist/git/generatePatch.js"() {
    "use strict";
    import_child_process2 = require("child_process"), import_util5 = require("util"), import_promises4 = require("fs/promises"), import_path4 = require("path"), execAsync2 = (0, import_util5.promisify)(import_child_process2.exec);
  }
});

// ../core/dist/git/getWorktreeDiff.js
async function getWorktreeDiff(worktreePath) {
  await execFileAsync("git", ["add", "-A"], { cwd: worktreePath });
  let { stdout } = await execFileAsync("git", ["diff", "--cached", "HEAD"], { cwd: worktreePath, maxBuffer: 10 * 1024 * 1024 });
  return stdout;
}
var import_node_child_process, import_node_util, execFileAsync, init_getWorktreeDiff = __esm({
  "../core/dist/git/getWorktreeDiff.js"() {
    "use strict";
    import_node_child_process = require("node:child_process"), import_node_util = require("node:util"), execFileAsync = (0, import_node_util.promisify)(import_node_child_process.execFile);
  }
});

// ../core/dist/git/index.js
var init_git = __esm({
  "../core/dist/git/index.js"() {
    "use strict";
    init_createWorktree();
    init_generatePatch();
    init_getWorktreeDiff();
  }
});

// ../core/dist/worker/MockWorker.js
async function runMockWorker(context) {
  let { task, worktreePath } = context, filesRead = [], filesModified = [];
  for (let pattern of task.allowedFiles.slice(0, 2))
    filesRead.push(pattern);
  let outputRelPath = `agentledger-task-${task.taskId}.md`, outputAbsPath = (0, import_path5.join)(worktreePath, outputRelPath);
  await (0, import_promises5.mkdir)((0, import_path5.dirname)(outputAbsPath), { recursive: !0 }), await (0, import_promises5.writeFile)(outputAbsPath, formatTaskOutput(task.title, task.description), "utf8"), filesModified.push(outputRelPath);
  let readmePath = (0, import_path5.join)(worktreePath, "README.md");
  if ((0, import_fs4.existsSync)(readmePath)) {
    let existing = await (0, import_promises5.readFile)(readmePath, "utf8");
    await (0, import_promises5.writeFile)(readmePath, existing + `
<!-- agentledger: ${task.taskId} -->
`, "utf8"), filesRead.push("README.md"), filesModified.push("README.md");
  }
  return WorkerResultSchema.parse({
    taskId: task.taskId,
    summary: `Mock worker completed "${task.title}"`,
    filesRead,
    filesModified,
    worktreeBranch: `agentledger/${task.taskId}`,
    output: {
      completed: !0,
      taskTitle: task.title,
      goal: task.description
    }
  });
}
function formatTaskOutput(title, description) {
  return [
    `# ${title}`,
    "",
    `> ${description}`,
    "",
    "## Result",
    "",
    "Task completed by mock worker.",
    `Generated: ${(/* @__PURE__ */ new Date()).toISOString()}`,
    ""
  ].join(`
`);
}
var import_promises5, import_fs4, import_path5, init_MockWorker = __esm({
  "../core/dist/worker/MockWorker.js"() {
    "use strict";
    import_promises5 = require("fs/promises"), import_fs4 = require("fs"), import_path5 = require("path");
    init_schemas();
  }
});

// ../core/dist/worker/ClaudeCodeWorker.js
function buildConstraintSystemPrompt(task) {
  let allowed = task.allowedFiles.length > 0 ? task.allowedFiles.map((f) => `  - ${f}`).join(`
`) : "  (no explicit file restriction \u2014 use good judgement)", blocked = task.blockedFiles.length > 0 ? task.blockedFiles.map((f) => `  - ${f}`).join(`
`) : "  (none specified)";
  return `
=== AgentLedger Task Constraints ===

You are operating as an isolated worker inside AgentLedger, an orchestration harness
that enforces file boundary checks after you finish. A separate verifier will diff
your changes against the allowed files list and REJECT the patch if you touch files
outside your scope.

TASK ID   : ${task.taskId}
TASK TITLE: ${task.title}

FILES YOU MAY MODIFY:
${allowed}

FILES YOU MUST NOT TOUCH:
${blocked}

RULES:
1. Only read and modify files within your allowed scope.
2. Do not install packages globally \u2014 make only local changes.
3. Run any necessary build or test commands to verify your work.
4. When your work is complete, your FINAL response must be the JSON object
   described in the output schema (summary + filesModified + filesRead).
   Do not add any prose after the JSON.

=== End of AgentLedger Constraints ===
`.trim();
}
function buildTaskUserPrompt(task) {
  return `## Task: ${task.title}

${task.description}

${task.successCriteria.length > 0 ? `### Success criteria
${task.successCriteria.map((c) => `- ${c}`).join(`
`)}` : ""}

Complete this task now. When done, return the structured JSON result.`.trim();
}
function spawnAndCapture(cmd, args, cwd) {
  return new Promise((resolve4) => {
    let child = (0, import_child_process3.spawn)(cmd, args, {
      cwd,
      // Inherit env so Claude Code can find its auth credentials
      env: process.env,
      // stdio: pipe stdout/stderr, inherit stdin (never blocks in -p mode)
      stdio: ["ignore", "pipe", "pipe"]
    }), stdoutChunks = [], stderrChunks = [];
    child.stdout.on("data", (chunk) => stdoutChunks.push(chunk)), child.stderr.on("data", (chunk) => stderrChunks.push(chunk)), child.on("close", (code) => {
      resolve4({
        stdout: Buffer.concat(stdoutChunks).toString("utf8"),
        stderr: Buffer.concat(stderrChunks).toString("utf8"),
        exitCode: code ?? 1
      });
    }), child.on("error", (err) => {
      resolve4({
        stdout: "",
        stderr: err.message,
        exitCode: 1
      });
    });
  });
}
function parseClaudeCodeOutput(rawStdout, taskId) {
  let envelope = null;
  try {
    let firstJson = rawStdout.split(`
`).find((l) => l.trim().startsWith("{"));
    firstJson && (envelope = JSON.parse(firstJson));
  } catch {
  }
  if (envelope?.structured_output) {
    let s = envelope.structured_output;
    return {
      summary: typeof s.summary == "string" ? s.summary : `Task ${taskId} completed`,
      filesModified: Array.isArray(s.filesModified) ? s.filesModified : [],
      filesRead: Array.isArray(s.filesRead) ? s.filesRead : []
    };
  }
  if (envelope?.result)
    try {
      let inner = JSON.parse(envelope.result);
      if (inner.summary)
        return inner;
    } catch {
      return {
        summary: envelope.result.slice(0, 500),
        filesModified: [],
        filesRead: []
      };
    }
  return {
    summary: rawStdout.slice(0, 500) || `Task ${taskId} completed (no structured output)`,
    filesModified: [],
    filesRead: []
  };
}
async function runClaudeCodeWorker(context, opts = {}) {
  let { task, worktreePath } = context, claudeCmd = opts.claudeCodePath ?? "claude", constraintPrompt = buildConstraintSystemPrompt(task), userPrompt = buildTaskUserPrompt(task), defaultDisallowed = ["WebSearch", "WebFetch"], disallowed = opts.disallowedTools ?? defaultDisallowed, args = [
    "--print",
    userPrompt,
    "--append-system-prompt",
    constraintPrompt,
    "--output-format",
    "json",
    "--json-schema",
    JSON.stringify(CLAUDE_CODE_RESULT_SCHEMA),
    "--dangerously-skip-permissions",
    "--no-session-persistence"
  ];
  opts.model && args.push("--model", opts.model), opts.maxBudgetUsd !== void 0 && args.push("--max-budget-usd", String(opts.maxBudgetUsd)), disallowed.length > 0 && args.push("--disallowedTools", disallowed.join(","));
  let { stdout, stderr, exitCode } = await spawnAndCapture(claudeCmd, args, worktreePath);
  if (exitCode !== 0 && !stdout.trim())
    throw new Error(`Claude Code worker exited with code ${exitCode}.
stderr: ${stderr.slice(0, 500)}`);
  let structured = parseClaudeCodeOutput(stdout, task.taskId);
  return WorkerResultSchema.parse({
    taskId: task.taskId,
    summary: structured.summary,
    filesRead: structured.filesRead,
    filesModified: structured.filesModified,
    worktreeBranch: `agentledger/${task.taskId}`,
    toolDenials: [],
    output: {
      taskCompleted: !0,
      workerType: "claude-code",
      exitCode
    }
  });
}
var import_child_process3, CLAUDE_CODE_RESULT_SCHEMA, init_ClaudeCodeWorker = __esm({
  "../core/dist/worker/ClaudeCodeWorker.js"() {
    "use strict";
    import_child_process3 = require("child_process");
    init_schemas();
    CLAUDE_CODE_RESULT_SCHEMA = {
      type: "object",
      description: "Report what you did after completing all work",
      properties: {
        summary: {
          type: "string",
          description: "Concise summary of what was accomplished"
        },
        filesModified: {
          type: "array",
          items: { type: "string" },
          description: "Relative paths (from repo root) of files you created or modified"
        },
        filesRead: {
          type: "array",
          items: { type: "string" },
          description: "Relative paths of files you read"
        }
      },
      required: ["summary", "filesModified", "filesRead"]
    };
  }
});

// ../core/dist/worker/fixtures/boundaryViolatingWorker.js
async function runBoundaryViolatingWorker(context, blockedFilePath) {
  let { task, worktreePath } = context, legitimateRelPath = `agentledger-task-${task.taskId}.md`, legitimateAbsPath = (0, import_path6.join)(worktreePath, legitimateRelPath);
  await (0, import_promises6.mkdir)((0, import_path6.dirname)(legitimateAbsPath), { recursive: !0 }), await (0, import_promises6.writeFile)(legitimateAbsPath, `# ${task.title}

Task output.
`, "utf8");
  let blockedAbsPath = (0, import_path6.join)(worktreePath, blockedFilePath);
  return await (0, import_promises6.mkdir)((0, import_path6.dirname)(blockedAbsPath), { recursive: !0 }), await (0, import_promises6.writeFile)(blockedAbsPath, `EXFILTRATED_SECRET=hunter2
`, "utf8"), WorkerResultSchema.parse({
    taskId: task.taskId,
    summary: `Boundary-violating worker "completed" "${task.title}" (self-report)`,
    filesRead: [],
    filesModified: [legitimateRelPath],
    // deliberately omits the blocked file
    worktreeBranch: `agentledger/${task.taskId}`,
    output: {
      completed: !0,
      selfReportedSuccess: !0
    }
  });
}
var import_promises6, import_path6, init_boundaryViolatingWorker = __esm({
  "../core/dist/worker/fixtures/boundaryViolatingWorker.js"() {
    "use strict";
    import_promises6 = require("fs/promises"), import_path6 = require("path");
    init_schemas();
  }
});

// ../core/dist/worker/fixtures/falseSelfReportWorker.js
async function runFalseSelfReportWorker(context) {
  let { task, worktreePath } = context, outputRelPath = `agentledger-task-${task.taskId}.md`, outputAbsPath = (0, import_path7.join)(worktreePath, outputRelPath);
  return await (0, import_promises7.mkdir)((0, import_path7.dirname)(outputAbsPath), { recursive: !0 }), await (0, import_promises7.writeFile)(outputAbsPath, `# ${task.title}

This output claims success.
`, "utf8"), WorkerResultSchema.parse({
    taskId: task.taskId,
    summary: `False-self-report worker "completed" "${task.title}" (all tests pass! trust me)`,
    filesRead: [],
    filesModified: [outputRelPath],
    worktreeBranch: `agentledger/${task.taskId}`,
    output: {
      completed: !0,
      testsPass: !0,
      // lie — the real command will exit non-zero
      selfReportedSuccess: !0
    }
  });
}
var import_promises7, import_path7, init_falseSelfReportWorker = __esm({
  "../core/dist/worker/fixtures/falseSelfReportWorker.js"() {
    "use strict";
    import_promises7 = require("fs/promises"), import_path7 = require("path");
    init_schemas();
  }
});

// ../core/dist/worker/index.js
var init_worker = __esm({
  "../core/dist/worker/index.js"() {
    "use strict";
    init_MockWorker();
    init_ClaudeCodeWorker();
    init_boundaryViolatingWorker();
    init_falseSelfReportWorker();
  }
});

// ../../node_modules/.pnpm/balanced-match@4.0.4/node_modules/balanced-match/dist/esm/index.js
var balanced, maybeMatch, range, init_esm = __esm({
  "../../node_modules/.pnpm/balanced-match@4.0.4/node_modules/balanced-match/dist/esm/index.js"() {
    balanced = (a, b, str2) => {
      let ma = a instanceof RegExp ? maybeMatch(a, str2) : a, mb = b instanceof RegExp ? maybeMatch(b, str2) : b, r = ma !== null && mb != null && range(ma, mb, str2);
      return r && {
        start: r[0],
        end: r[1],
        pre: str2.slice(0, r[0]),
        body: str2.slice(r[0] + ma.length, r[1]),
        post: str2.slice(r[1] + mb.length)
      };
    }, maybeMatch = (reg, str2) => {
      let m = str2.match(reg);
      return m ? m[0] : null;
    }, range = (a, b, str2) => {
      let begs, beg, left, right, result, ai = str2.indexOf(a), bi = str2.indexOf(b, ai + 1), i = ai;
      if (ai >= 0 && bi > 0) {
        if (a === b)
          return [ai, bi];
        for (begs = [], left = str2.length; i >= 0 && !result; ) {
          if (i === ai)
            begs.push(i), ai = str2.indexOf(a, i + 1);
          else if (begs.length === 1) {
            let r = begs.pop();
            r !== void 0 && (result = [r, bi]);
          } else
            beg = begs.pop(), beg !== void 0 && beg < left && (left = beg, right = bi), bi = str2.indexOf(b, i + 1);
          i = ai < bi && ai >= 0 ? ai : bi;
        }
        begs.length && right !== void 0 && (result = [left, right]);
      }
      return result;
    };
  }
});

// ../../node_modules/.pnpm/brace-expansion@5.0.7/node_modules/brace-expansion/dist/esm/index.js
function numeric(str2) {
  return isNaN(str2) ? str2.charCodeAt(0) : parseInt(str2, 10);
}
function escapeBraces(str2) {
  return str2.replace(slashPattern, escSlash).replace(openPattern, escOpen).replace(closePattern, escClose).replace(commaPattern, escComma).replace(periodPattern, escPeriod);
}
function unescapeBraces(str2) {
  return str2.replace(escSlashPattern, "\\").replace(escOpenPattern, "{").replace(escClosePattern, "}").replace(escCommaPattern, ",").replace(escPeriodPattern, ".");
}
function parseCommaParts(str2) {
  if (!str2)
    return [""];
  let parts = [], m = balanced("{", "}", str2);
  if (!m)
    return str2.split(",");
  let { pre, body, post } = m, p = pre.split(",");
  p[p.length - 1] += "{" + body + "}";
  let postParts = parseCommaParts(post);
  return post.length && (p[p.length - 1] += postParts.shift(), p.push.apply(p, postParts)), parts.push.apply(parts, p), parts;
}
function expand(str2, options = {}) {
  if (!str2)
    return [];
  let { max = EXPANSION_MAX } = options;
  return str2.slice(0, 2) === "{}" && (str2 = "\\{\\}" + str2.slice(2)), expand_(escapeBraces(str2), max, !0).map(unescapeBraces);
}
function embrace(str2) {
  return "{" + str2 + "}";
}
function isPadded(el) {
  return /^-?0\d/.test(el);
}
function lte(i, y) {
  return i <= y;
}
function gte(i, y) {
  return i >= y;
}
function expand_(str2, max, isTop) {
  let expansions = [];
  for (; ; ) {
    let m = balanced("{", "}", str2);
    if (!m)
      return [str2];
    let pre = m.pre;
    if (/\$$/.test(m.pre)) {
      let post2 = m.post.length ? expand_(m.post, max, !1) : [""];
      for (let k = 0; k < post2.length && k < max; k++) {
        let expansion = pre + "{" + m.body + "}" + post2[k];
        expansions.push(expansion);
      }
      return expansions;
    }
    let isNumericSequence = /^-?\d+\.\.-?\d+(?:\.\.-?\d+)?$/.test(m.body), isAlphaSequence = /^[a-zA-Z]\.\.[a-zA-Z](?:\.\.-?\d+)?$/.test(m.body), isSequence = isNumericSequence || isAlphaSequence, isOptions = m.body.indexOf(",") >= 0;
    if (!isSequence && !isOptions) {
      if (m.post.match(/,(?!,).*\}/)) {
        str2 = m.pre + "{" + m.body + escClose + m.post, isTop = !0;
        continue;
      }
      return [str2];
    }
    let post = m.post.length ? expand_(m.post, max, !1) : [""], n;
    if (isSequence)
      n = m.body.split(/\.\./);
    else if (n = parseCommaParts(m.body), n.length === 1 && n[0] !== void 0 && (n = expand_(n[0], max, !1).map(embrace), n.length === 1))
      return post.map((p) => m.pre + n[0] + p);
    let N;
    if (isSequence && n[0] !== void 0 && n[1] !== void 0) {
      let x = numeric(n[0]), y = numeric(n[1]), width = Math.max(n[0].length, n[1].length), incr = n.length === 3 && n[2] !== void 0 ? Math.max(Math.abs(numeric(n[2])), 1) : 1, test = lte;
      y < x && (incr *= -1, test = gte);
      let pad = n.some(isPadded);
      N = [];
      for (let i = x; test(i, y) && N.length < max; i += incr) {
        let c;
        if (isAlphaSequence)
          c = String.fromCharCode(i), c === "\\" && (c = "");
        else if (c = String(i), pad) {
          let need = width - c.length;
          if (need > 0) {
            let z = new Array(need + 1).join("0");
            i < 0 ? c = "-" + z + c.slice(1) : c = z + c;
          }
        }
        N.push(c);
      }
    } else {
      N = [];
      for (let j = 0; j < n.length; j++)
        N.push.apply(N, expand_(n[j], max, !1));
    }
    for (let j = 0; j < N.length; j++)
      for (let k = 0; k < post.length && expansions.length < max; k++) {
        let expansion = pre + N[j] + post[k];
        (!isTop || isSequence || expansion) && expansions.push(expansion);
      }
    return expansions;
  }
}
var escSlash, escOpen, escClose, escComma, escPeriod, escSlashPattern, escOpenPattern, escClosePattern, escCommaPattern, escPeriodPattern, slashPattern, openPattern, closePattern, commaPattern, periodPattern, EXPANSION_MAX, init_esm2 = __esm({
  "../../node_modules/.pnpm/brace-expansion@5.0.7/node_modules/brace-expansion/dist/esm/index.js"() {
    init_esm();
    escSlash = "\0SLASH" + Math.random() + "\0", escOpen = "\0OPEN" + Math.random() + "\0", escClose = "\0CLOSE" + Math.random() + "\0", escComma = "\0COMMA" + Math.random() + "\0", escPeriod = "\0PERIOD" + Math.random() + "\0", escSlashPattern = new RegExp(escSlash, "g"), escOpenPattern = new RegExp(escOpen, "g"), escClosePattern = new RegExp(escClose, "g"), escCommaPattern = new RegExp(escComma, "g"), escPeriodPattern = new RegExp(escPeriod, "g"), slashPattern = /\\\\/g, openPattern = /\\{/g, closePattern = /\\}/g, commaPattern = /\\,/g, periodPattern = /\\\./g, EXPANSION_MAX = 1e5;
  }
});

// ../../node_modules/.pnpm/minimatch@10.2.5/node_modules/minimatch/dist/esm/assert-valid-pattern.js
var assertValidPattern, init_assert_valid_pattern = __esm({
  "../../node_modules/.pnpm/minimatch@10.2.5/node_modules/minimatch/dist/esm/assert-valid-pattern.js"() {
    assertValidPattern = (pattern) => {
      if (typeof pattern != "string")
        throw new TypeError("invalid pattern");
      if (pattern.length > 65536)
        throw new TypeError("pattern is too long");
    };
  }
});

// ../../node_modules/.pnpm/minimatch@10.2.5/node_modules/minimatch/dist/esm/brace-expressions.js
var posixClasses, braceEscape, regexpEscape, rangesToString, parseClass, init_brace_expressions = __esm({
  "../../node_modules/.pnpm/minimatch@10.2.5/node_modules/minimatch/dist/esm/brace-expressions.js"() {
    posixClasses = {
      "[:alnum:]": ["\\p{L}\\p{Nl}\\p{Nd}", !0],
      "[:alpha:]": ["\\p{L}\\p{Nl}", !0],
      "[:ascii:]": ["\\x00-\\x7f", !1],
      "[:blank:]": ["\\p{Zs}\\t", !0],
      "[:cntrl:]": ["\\p{Cc}", !0],
      "[:digit:]": ["\\p{Nd}", !0],
      "[:graph:]": ["\\p{Z}\\p{C}", !0, !0],
      "[:lower:]": ["\\p{Ll}", !0],
      "[:print:]": ["\\p{C}", !0],
      "[:punct:]": ["\\p{P}", !0],
      "[:space:]": ["\\p{Z}\\t\\r\\n\\v\\f", !0],
      "[:upper:]": ["\\p{Lu}", !0],
      "[:word:]": ["\\p{L}\\p{Nl}\\p{Nd}\\p{Pc}", !0],
      "[:xdigit:]": ["A-Fa-f0-9", !1]
    }, braceEscape = (s) => s.replace(/[[\]\\-]/g, "\\$&"), regexpEscape = (s) => s.replace(/[-[\]{}()*+?.,\\^$|#\s]/g, "\\$&"), rangesToString = (ranges) => ranges.join(""), parseClass = (glob2, position) => {
      let pos = position;
      if (glob2.charAt(pos) !== "[")
        throw new Error("not in a brace expression");
      let ranges = [], negs = [], i = pos + 1, sawStart = !1, uflag = !1, escaping = !1, negate = !1, endPos = pos, rangeStart = "";
      WHILE: for (; i < glob2.length; ) {
        let c = glob2.charAt(i);
        if ((c === "!" || c === "^") && i === pos + 1) {
          negate = !0, i++;
          continue;
        }
        if (c === "]" && sawStart && !escaping) {
          endPos = i + 1;
          break;
        }
        if (sawStart = !0, c === "\\" && !escaping) {
          escaping = !0, i++;
          continue;
        }
        if (c === "[" && !escaping) {
          for (let [cls, [unip, u, neg]] of Object.entries(posixClasses))
            if (glob2.startsWith(cls, i)) {
              if (rangeStart)
                return ["$.", !1, glob2.length - pos, !0];
              i += cls.length, neg ? negs.push(unip) : ranges.push(unip), uflag = uflag || u;
              continue WHILE;
            }
        }
        if (escaping = !1, rangeStart) {
          c > rangeStart ? ranges.push(braceEscape(rangeStart) + "-" + braceEscape(c)) : c === rangeStart && ranges.push(braceEscape(c)), rangeStart = "", i++;
          continue;
        }
        if (glob2.startsWith("-]", i + 1)) {
          ranges.push(braceEscape(c + "-")), i += 2;
          continue;
        }
        if (glob2.startsWith("-", i + 1)) {
          rangeStart = c, i += 2;
          continue;
        }
        ranges.push(braceEscape(c)), i++;
      }
      if (endPos < i)
        return ["", !1, 0, !1];
      if (!ranges.length && !negs.length)
        return ["$.", !1, glob2.length - pos, !0];
      if (negs.length === 0 && ranges.length === 1 && /^\\?.$/.test(ranges[0]) && !negate) {
        let r = ranges[0].length === 2 ? ranges[0].slice(-1) : ranges[0];
        return [regexpEscape(r), !1, endPos - pos, !1];
      }
      let sranges = "[" + (negate ? "^" : "") + rangesToString(ranges) + "]", snegs = "[" + (negate ? "" : "^") + rangesToString(negs) + "]";
      return [ranges.length && negs.length ? "(" + sranges + "|" + snegs + ")" : ranges.length ? sranges : snegs, uflag, endPos - pos, !0];
    };
  }
});

// ../../node_modules/.pnpm/minimatch@10.2.5/node_modules/minimatch/dist/esm/unescape.js
var unescape2, init_unescape = __esm({
  "../../node_modules/.pnpm/minimatch@10.2.5/node_modules/minimatch/dist/esm/unescape.js"() {
    unescape2 = (s, { windowsPathsNoEscape = !1, magicalBraces = !0 } = {}) => magicalBraces ? windowsPathsNoEscape ? s.replace(/\[([^/\\])\]/g, "$1") : s.replace(/((?!\\).|^)\[([^/\\])\]/g, "$1$2").replace(/\\([^/])/g, "$1") : windowsPathsNoEscape ? s.replace(/\[([^/\\{}])\]/g, "$1") : s.replace(/((?!\\).|^)\[([^/\\{}])\]/g, "$1$2").replace(/\\([^/{}])/g, "$1");
  }
});

// ../../node_modules/.pnpm/minimatch@10.2.5/node_modules/minimatch/dist/esm/ast.js
var _a, types, isExtglobType, isExtglobAST, adoptionMap, adoptionWithSpaceMap, adoptionAnyMap, usurpMap, startNoTraversal, startNoDot, addPatternStart, justDots, reSpecials, regExpEscape, qmark, star, starNoEmpty, ID, AST, init_ast = __esm({
  "../../node_modules/.pnpm/minimatch@10.2.5/node_modules/minimatch/dist/esm/ast.js"() {
    init_brace_expressions();
    init_unescape();
    types = /* @__PURE__ */ new Set(["!", "?", "+", "*", "@"]), isExtglobType = (c) => types.has(c), isExtglobAST = (c) => isExtglobType(c.type), adoptionMap = /* @__PURE__ */ new Map([
      ["!", ["@"]],
      ["?", ["?", "@"]],
      ["@", ["@"]],
      ["*", ["*", "+", "?", "@"]],
      ["+", ["+", "@"]]
    ]), adoptionWithSpaceMap = /* @__PURE__ */ new Map([
      ["!", ["?"]],
      ["@", ["?"]],
      ["+", ["?", "*"]]
    ]), adoptionAnyMap = /* @__PURE__ */ new Map([
      ["!", ["?", "@"]],
      ["?", ["?", "@"]],
      ["@", ["?", "@"]],
      ["*", ["*", "+", "?", "@"]],
      ["+", ["+", "@", "?", "*"]]
    ]), usurpMap = /* @__PURE__ */ new Map([
      ["!", /* @__PURE__ */ new Map([["!", "@"]])],
      [
        "?",
        /* @__PURE__ */ new Map([
          ["*", "*"],
          ["+", "*"]
        ])
      ],
      [
        "@",
        /* @__PURE__ */ new Map([
          ["!", "!"],
          ["?", "?"],
          ["@", "@"],
          ["*", "*"],
          ["+", "+"]
        ])
      ],
      [
        "+",
        /* @__PURE__ */ new Map([
          ["?", "*"],
          ["*", "*"]
        ])
      ]
    ]), startNoTraversal = "(?!(?:^|/)\\.\\.?(?:$|/))", startNoDot = "(?!\\.)", addPatternStart = /* @__PURE__ */ new Set(["[", "."]), justDots = /* @__PURE__ */ new Set(["..", "."]), reSpecials = new Set("().*{}+?[]^$\\!"), regExpEscape = (s) => s.replace(/[-[\]{}()*+?.,\\^$|#\s]/g, "\\$&"), qmark = "[^/]", star = qmark + "*?", starNoEmpty = qmark + "+?", ID = 0, AST = class {
      type;
      #root;
      #hasMagic;
      #uflag = !1;
      #parts = [];
      #parent;
      #parentIndex;
      #negs;
      #filledNegs = !1;
      #options;
      #toString;
      // set to true if it's an extglob with no children
      // (which really means one child of '')
      #emptyExt = !1;
      id = ++ID;
      get depth() {
        return (this.#parent?.depth ?? -1) + 1;
      }
      [Symbol.for("nodejs.util.inspect.custom")]() {
        return {
          "@@type": "AST",
          id: this.id,
          type: this.type,
          root: this.#root.id,
          parent: this.#parent?.id,
          depth: this.depth,
          partsLength: this.#parts.length,
          parts: this.#parts
        };
      }
      constructor(type, parent, options = {}) {
        this.type = type, type && (this.#hasMagic = !0), this.#parent = parent, this.#root = this.#parent ? this.#parent.#root : this, this.#options = this.#root === this ? options : this.#root.#options, this.#negs = this.#root === this ? [] : this.#root.#negs, type === "!" && !this.#root.#filledNegs && this.#negs.push(this), this.#parentIndex = this.#parent ? this.#parent.#parts.length : 0;
      }
      get hasMagic() {
        if (this.#hasMagic !== void 0)
          return this.#hasMagic;
        for (let p of this.#parts)
          if (typeof p != "string" && (p.type || p.hasMagic))
            return this.#hasMagic = !0;
        return this.#hasMagic;
      }
      // reconstructs the pattern
      toString() {
        return this.#toString !== void 0 ? this.#toString : this.type ? this.#toString = this.type + "(" + this.#parts.map((p) => String(p)).join("|") + ")" : this.#toString = this.#parts.map((p) => String(p)).join("");
      }
      #fillNegs() {
        if (this !== this.#root)
          throw new Error("should only call on root");
        if (this.#filledNegs)
          return this;
        this.toString(), this.#filledNegs = !0;
        let n;
        for (; n = this.#negs.pop(); ) {
          if (n.type !== "!")
            continue;
          let p = n, pp = p.#parent;
          for (; pp; ) {
            for (let i = p.#parentIndex + 1; !pp.type && i < pp.#parts.length; i++)
              for (let part of n.#parts) {
                if (typeof part == "string")
                  throw new Error("string part in extglob AST??");
                part.copyIn(pp.#parts[i]);
              }
            p = pp, pp = p.#parent;
          }
        }
        return this;
      }
      push(...parts) {
        for (let p of parts)
          if (p !== "") {
            if (typeof p != "string" && !(p instanceof _a && p.#parent === this))
              throw new Error("invalid part: " + p);
            this.#parts.push(p);
          }
      }
      toJSON() {
        let ret = this.type === null ? this.#parts.slice().map((p) => typeof p == "string" ? p : p.toJSON()) : [this.type, ...this.#parts.map((p) => p.toJSON())];
        return this.isStart() && !this.type && ret.unshift([]), this.isEnd() && (this === this.#root || this.#root.#filledNegs && this.#parent?.type === "!") && ret.push({}), ret;
      }
      isStart() {
        if (this.#root === this)
          return !0;
        if (!this.#parent?.isStart())
          return !1;
        if (this.#parentIndex === 0)
          return !0;
        let p = this.#parent;
        for (let i = 0; i < this.#parentIndex; i++) {
          let pp = p.#parts[i];
          if (!(pp instanceof _a && pp.type === "!"))
            return !1;
        }
        return !0;
      }
      isEnd() {
        if (this.#root === this || this.#parent?.type === "!")
          return !0;
        if (!this.#parent?.isEnd())
          return !1;
        if (!this.type)
          return this.#parent?.isEnd();
        let pl = this.#parent ? this.#parent.#parts.length : 0;
        return this.#parentIndex === pl - 1;
      }
      copyIn(part) {
        typeof part == "string" ? this.push(part) : this.push(part.clone(this));
      }
      clone(parent) {
        let c = new _a(this.type, parent);
        for (let p of this.#parts)
          c.copyIn(p);
        return c;
      }
      static #parseAST(str2, ast, pos, opt, extDepth) {
        let maxDepth = opt.maxExtglobRecursion ?? 2, escaping = !1, inBrace = !1, braceStart = -1, braceNeg = !1;
        if (ast.type === null) {
          let i2 = pos, acc2 = "";
          for (; i2 < str2.length; ) {
            let c = str2.charAt(i2++);
            if (escaping || c === "\\") {
              escaping = !escaping, acc2 += c;
              continue;
            }
            if (inBrace) {
              i2 === braceStart + 1 ? (c === "^" || c === "!") && (braceNeg = !0) : c === "]" && !(i2 === braceStart + 2 && braceNeg) && (inBrace = !1), acc2 += c;
              continue;
            } else if (c === "[") {
              inBrace = !0, braceStart = i2, braceNeg = !1, acc2 += c;
              continue;
            }
            if (!opt.noext && isExtglobType(c) && str2.charAt(i2) === "(" && extDepth <= maxDepth) {
              ast.push(acc2), acc2 = "";
              let ext2 = new _a(c, ast);
              i2 = _a.#parseAST(str2, ext2, i2, opt, extDepth + 1), ast.push(ext2);
              continue;
            }
            acc2 += c;
          }
          return ast.push(acc2), i2;
        }
        let i = pos + 1, part = new _a(null, ast), parts = [], acc = "";
        for (; i < str2.length; ) {
          let c = str2.charAt(i++);
          if (escaping || c === "\\") {
            escaping = !escaping, acc += c;
            continue;
          }
          if (inBrace) {
            i === braceStart + 1 ? (c === "^" || c === "!") && (braceNeg = !0) : c === "]" && !(i === braceStart + 2 && braceNeg) && (inBrace = !1), acc += c;
            continue;
          } else if (c === "[") {
            inBrace = !0, braceStart = i, braceNeg = !1, acc += c;
            continue;
          }
          if (!opt.noext && isExtglobType(c) && str2.charAt(i) === "(" && /* c8 ignore start - the maxDepth is sufficient here */
          (extDepth <= maxDepth || ast && ast.#canAdoptType(c))) {
            let depthAdd = ast && ast.#canAdoptType(c) ? 0 : 1;
            part.push(acc), acc = "";
            let ext2 = new _a(c, part);
            part.push(ext2), i = _a.#parseAST(str2, ext2, i, opt, extDepth + depthAdd);
            continue;
          }
          if (c === "|") {
            part.push(acc), acc = "", parts.push(part), part = new _a(null, ast);
            continue;
          }
          if (c === ")")
            return acc === "" && ast.#parts.length === 0 && (ast.#emptyExt = !0), part.push(acc), acc = "", ast.push(...parts, part), i;
          acc += c;
        }
        return ast.type = null, ast.#hasMagic = void 0, ast.#parts = [str2.substring(pos - 1)], i;
      }
      #canAdoptWithSpace(child) {
        return this.#canAdopt(child, adoptionWithSpaceMap);
      }
      #canAdopt(child, map = adoptionMap) {
        if (!child || typeof child != "object" || child.type !== null || child.#parts.length !== 1 || this.type === null)
          return !1;
        let gc = child.#parts[0];
        return !gc || typeof gc != "object" || gc.type === null ? !1 : this.#canAdoptType(gc.type, map);
      }
      #canAdoptType(c, map = adoptionAnyMap) {
        return !!map.get(this.type)?.includes(c);
      }
      #adoptWithSpace(child, index) {
        let gc = child.#parts[0], blank = new _a(null, gc, this.options);
        blank.#parts.push(""), gc.push(blank), this.#adopt(child, index);
      }
      #adopt(child, index) {
        let gc = child.#parts[0];
        this.#parts.splice(index, 1, ...gc.#parts);
        for (let p of gc.#parts)
          typeof p == "object" && (p.#parent = this);
        this.#toString = void 0;
      }
      #canUsurpType(c) {
        return !!usurpMap.get(this.type)?.has(c);
      }
      #canUsurp(child) {
        if (!child || typeof child != "object" || child.type !== null || child.#parts.length !== 1 || this.type === null || this.#parts.length !== 1)
          return !1;
        let gc = child.#parts[0];
        return !gc || typeof gc != "object" || gc.type === null ? !1 : this.#canUsurpType(gc.type);
      }
      #usurp(child) {
        let m = usurpMap.get(this.type), gc = child.#parts[0], nt = m?.get(gc.type);
        if (!nt)
          return !1;
        this.#parts = gc.#parts;
        for (let p of this.#parts)
          typeof p == "object" && (p.#parent = this);
        this.type = nt, this.#toString = void 0, this.#emptyExt = !1;
      }
      static fromGlob(pattern, options = {}) {
        let ast = new _a(null, void 0, options);
        return _a.#parseAST(pattern, ast, 0, options, 0), ast;
      }
      // returns the regular expression if there's magic, or the unescaped
      // string if not.
      toMMPattern() {
        if (this !== this.#root)
          return this.#root.toMMPattern();
        let glob2 = this.toString(), [re, body, hasMagic, uflag] = this.toRegExpSource();
        if (!(hasMagic || this.#hasMagic || this.#options.nocase && !this.#options.nocaseMagicOnly && glob2.toUpperCase() !== glob2.toLowerCase()))
          return body;
        let flags = (this.#options.nocase ? "i" : "") + (uflag ? "u" : "");
        return Object.assign(new RegExp(`^${re}$`, flags), {
          _src: re,
          _glob: glob2
        });
      }
      get options() {
        return this.#options;
      }
      // returns the string match, the regexp source, whether there's magic
      // in the regexp (so a regular expression is required) and whether or
      // not the uflag is needed for the regular expression (for posix classes)
      // TODO: instead of injecting the start/end at this point, just return
      // the BODY of the regexp, along with the start/end portions suitable
      // for binding the start/end in either a joined full-path makeRe context
      // (where we bind to (^|/), or a standalone matchPart context (where
      // we bind to ^, and not /).  Otherwise slashes get duped!
      //
      // In part-matching mode, the start is:
      // - if not isStart: nothing
      // - if traversal possible, but not allowed: ^(?!\.\.?$)
      // - if dots allowed or not possible: ^
      // - if dots possible and not allowed: ^(?!\.)
      // end is:
      // - if not isEnd(): nothing
      // - else: $
      //
      // In full-path matching mode, we put the slash at the START of the
      // pattern, so start is:
      // - if first pattern: same as part-matching mode
      // - if not isStart(): nothing
      // - if traversal possible, but not allowed: /(?!\.\.?(?:$|/))
      // - if dots allowed or not possible: /
      // - if dots possible and not allowed: /(?!\.)
      // end is:
      // - if last pattern, same as part-matching mode
      // - else nothing
      //
      // Always put the (?:$|/) on negated tails, though, because that has to be
      // there to bind the end of the negated pattern portion, and it's easier to
      // just stick it in now rather than try to inject it later in the middle of
      // the pattern.
      //
      // We can just always return the same end, and leave it up to the caller
      // to know whether it's going to be used joined or in parts.
      // And, if the start is adjusted slightly, can do the same there:
      // - if not isStart: nothing
      // - if traversal possible, but not allowed: (?:/|^)(?!\.\.?$)
      // - if dots allowed or not possible: (?:/|^)
      // - if dots possible and not allowed: (?:/|^)(?!\.)
      //
      // But it's better to have a simpler binding without a conditional, for
      // performance, so probably better to return both start options.
      //
      // Then the caller just ignores the end if it's not the first pattern,
      // and the start always gets applied.
      //
      // But that's always going to be $ if it's the ending pattern, or nothing,
      // so the caller can just attach $ at the end of the pattern when building.
      //
      // So the todo is:
      // - better detect what kind of start is needed
      // - return both flavors of starting pattern
      // - attach $ at the end of the pattern when creating the actual RegExp
      //
      // Ah, but wait, no, that all only applies to the root when the first pattern
      // is not an extglob. If the first pattern IS an extglob, then we need all
      // that dot prevention biz to live in the extglob portions, because eg
      // +(*|.x*) can match .xy but not .yx.
      //
      // So, return the two flavors if it's #root and the first child is not an
      // AST, otherwise leave it to the child AST to handle it, and there,
      // use the (?:^|/) style of start binding.
      //
      // Even simplified further:
      // - Since the start for a join is eg /(?!\.) and the start for a part
      // is ^(?!\.), we can just prepend (?!\.) to the pattern (either root
      // or start or whatever) and prepend ^ or / at the Regexp construction.
      toRegExpSource(allowDot) {
        let dot = allowDot ?? !!this.#options.dot;
        if (this.#root === this && (this.#flatten(), this.#fillNegs()), !isExtglobAST(this)) {
          let noEmpty = this.isStart() && this.isEnd() && !this.#parts.some((s) => typeof s != "string"), src = this.#parts.map((p) => {
            let [re, _, hasMagic, uflag] = typeof p == "string" ? _a.#parseGlob(p, this.#hasMagic, noEmpty) : p.toRegExpSource(allowDot);
            return this.#hasMagic = this.#hasMagic || hasMagic, this.#uflag = this.#uflag || uflag, re;
          }).join(""), start2 = "";
          if (this.isStart() && typeof this.#parts[0] == "string" && !(this.#parts.length === 1 && justDots.has(this.#parts[0]))) {
            let aps = addPatternStart, needNoTrav = (
              // dots are allowed, and the pattern starts with [ or .
              dot && aps.has(src.charAt(0)) || // the pattern starts with \., and then [ or .
              src.startsWith("\\.") && aps.has(src.charAt(2)) || // the pattern starts with \.\., and then [ or .
              src.startsWith("\\.\\.") && aps.has(src.charAt(4))
            ), needNoDot = !dot && !allowDot && aps.has(src.charAt(0));
            start2 = needNoTrav ? startNoTraversal : needNoDot ? startNoDot : "";
          }
          let end = "";
          return this.isEnd() && this.#root.#filledNegs && this.#parent?.type === "!" && (end = "(?:$|\\/)"), [
            start2 + src + end,
            unescape2(src),
            this.#hasMagic = !!this.#hasMagic,
            this.#uflag
          ];
        }
        let repeated = this.type === "*" || this.type === "+", start = this.type === "!" ? "(?:(?!(?:" : "(?:", body = this.#partsToRegExp(dot);
        if (this.isStart() && this.isEnd() && !body && this.type !== "!") {
          let s = this.toString(), me = this;
          return me.#parts = [s], me.type = null, me.#hasMagic = void 0, [s, unescape2(this.toString()), !1, !1];
        }
        let bodyDotAllowed = !repeated || allowDot || dot || !startNoDot ? "" : this.#partsToRegExp(!0);
        bodyDotAllowed === body && (bodyDotAllowed = ""), bodyDotAllowed && (body = `(?:${body})(?:${bodyDotAllowed})*?`);
        let final = "";
        if (this.type === "!" && this.#emptyExt)
          final = (this.isStart() && !dot ? startNoDot : "") + starNoEmpty;
        else {
          let close = this.type === "!" ? (
            // !() must match something,but !(x) can match ''
            "))" + (this.isStart() && !dot && !allowDot ? startNoDot : "") + star + ")"
          ) : this.type === "@" ? ")" : this.type === "?" ? ")?" : this.type === "+" && bodyDotAllowed ? ")" : this.type === "*" && bodyDotAllowed ? ")?" : `)${this.type}`;
          final = start + body + close;
        }
        return [
          final,
          unescape2(body),
          this.#hasMagic = !!this.#hasMagic,
          this.#uflag
        ];
      }
      #flatten() {
        if (isExtglobAST(this)) {
          let iterations = 0, done = !1;
          do {
            done = !0;
            for (let i = 0; i < this.#parts.length; i++) {
              let c = this.#parts[i];
              typeof c == "object" && (c.#flatten(), this.#canAdopt(c) ? (done = !1, this.#adopt(c, i)) : this.#canAdoptWithSpace(c) ? (done = !1, this.#adoptWithSpace(c, i)) : this.#canUsurp(c) && (done = !1, this.#usurp(c)));
            }
          } while (!done && ++iterations < 10);
        } else
          for (let p of this.#parts)
            typeof p == "object" && p.#flatten();
        this.#toString = void 0;
      }
      #partsToRegExp(dot) {
        return this.#parts.map((p) => {
          if (typeof p == "string")
            throw new Error("string type in extglob ast??");
          let [re, _, _hasMagic, uflag] = p.toRegExpSource(dot);
          return this.#uflag = this.#uflag || uflag, re;
        }).filter((p) => !(this.isStart() && this.isEnd()) || !!p).join("|");
      }
      static #parseGlob(glob2, hasMagic, noEmpty = !1) {
        let escaping = !1, re = "", uflag = !1, inStar = !1;
        for (let i = 0; i < glob2.length; i++) {
          let c = glob2.charAt(i);
          if (escaping) {
            escaping = !1, re += (reSpecials.has(c) ? "\\" : "") + c;
            continue;
          }
          if (c === "*") {
            if (inStar)
              continue;
            inStar = !0, re += noEmpty && /^[*]+$/.test(glob2) ? starNoEmpty : star, hasMagic = !0;
            continue;
          } else
            inStar = !1;
          if (c === "\\") {
            i === glob2.length - 1 ? re += "\\\\" : escaping = !0;
            continue;
          }
          if (c === "[") {
            let [src, needUflag, consumed, magic] = parseClass(glob2, i);
            if (consumed) {
              re += src, uflag = uflag || needUflag, i += consumed - 1, hasMagic = hasMagic || magic;
              continue;
            }
          }
          if (c === "?") {
            re += qmark, hasMagic = !0;
            continue;
          }
          re += regExpEscape(c);
        }
        return [re, unescape2(glob2), !!hasMagic, uflag];
      }
    };
    _a = AST;
  }
});

// ../../node_modules/.pnpm/minimatch@10.2.5/node_modules/minimatch/dist/esm/escape.js
var escape2, init_escape = __esm({
  "../../node_modules/.pnpm/minimatch@10.2.5/node_modules/minimatch/dist/esm/escape.js"() {
    escape2 = (s, { windowsPathsNoEscape = !1, magicalBraces = !1 } = {}) => magicalBraces ? windowsPathsNoEscape ? s.replace(/[?*()[\]{}]/g, "[$&]") : s.replace(/[?*()[\]\\{}]/g, "\\$&") : windowsPathsNoEscape ? s.replace(/[?*()[\]]/g, "[$&]") : s.replace(/[?*()[\]\\]/g, "\\$&");
  }
});

// ../../node_modules/.pnpm/minimatch@10.2.5/node_modules/minimatch/dist/esm/index.js
var minimatch, starDotExtRE, starDotExtTest, starDotExtTestDot, starDotExtTestNocase, starDotExtTestNocaseDot, starDotStarRE, starDotStarTest, starDotStarTestDot, dotStarRE, dotStarTest, starRE, starTest, starTestDot, qmarksRE, qmarksTestNocase, qmarksTestNocaseDot, qmarksTestDot, qmarksTest, qmarksTestNoExt, qmarksTestNoExtDot, defaultPlatform, path2, sep, GLOBSTAR, qmark2, star2, twoStarDot, twoStarNoDot, filter, ext, defaults, braceExpand, makeRe, match, globMagic, regExpEscape2, Minimatch, init_esm3 = __esm({
  "../../node_modules/.pnpm/minimatch@10.2.5/node_modules/minimatch/dist/esm/index.js"() {
    init_esm2();
    init_assert_valid_pattern();
    init_ast();
    init_escape();
    init_unescape();
    init_ast();
    init_escape();
    init_unescape();
    minimatch = (p, pattern, options = {}) => (assertValidPattern(pattern), !options.nocomment && pattern.charAt(0) === "#" ? !1 : new Minimatch(pattern, options).match(p)), starDotExtRE = /^\*+([^+@!?*[(]*)$/, starDotExtTest = (ext2) => (f) => !f.startsWith(".") && f.endsWith(ext2), starDotExtTestDot = (ext2) => (f) => f.endsWith(ext2), starDotExtTestNocase = (ext2) => (ext2 = ext2.toLowerCase(), (f) => !f.startsWith(".") && f.toLowerCase().endsWith(ext2)), starDotExtTestNocaseDot = (ext2) => (ext2 = ext2.toLowerCase(), (f) => f.toLowerCase().endsWith(ext2)), starDotStarRE = /^\*+\.\*+$/, starDotStarTest = (f) => !f.startsWith(".") && f.includes("."), starDotStarTestDot = (f) => f !== "." && f !== ".." && f.includes("."), dotStarRE = /^\.\*+$/, dotStarTest = (f) => f !== "." && f !== ".." && f.startsWith("."), starRE = /^\*+$/, starTest = (f) => f.length !== 0 && !f.startsWith("."), starTestDot = (f) => f.length !== 0 && f !== "." && f !== "..", qmarksRE = /^\?+([^+@!?*[(]*)?$/, qmarksTestNocase = ([$0, ext2 = ""]) => {
      let noext = qmarksTestNoExt([$0]);
      return ext2 ? (ext2 = ext2.toLowerCase(), (f) => noext(f) && f.toLowerCase().endsWith(ext2)) : noext;
    }, qmarksTestNocaseDot = ([$0, ext2 = ""]) => {
      let noext = qmarksTestNoExtDot([$0]);
      return ext2 ? (ext2 = ext2.toLowerCase(), (f) => noext(f) && f.toLowerCase().endsWith(ext2)) : noext;
    }, qmarksTestDot = ([$0, ext2 = ""]) => {
      let noext = qmarksTestNoExtDot([$0]);
      return ext2 ? (f) => noext(f) && f.endsWith(ext2) : noext;
    }, qmarksTest = ([$0, ext2 = ""]) => {
      let noext = qmarksTestNoExt([$0]);
      return ext2 ? (f) => noext(f) && f.endsWith(ext2) : noext;
    }, qmarksTestNoExt = ([$0]) => {
      let len = $0.length;
      return (f) => f.length === len && !f.startsWith(".");
    }, qmarksTestNoExtDot = ([$0]) => {
      let len = $0.length;
      return (f) => f.length === len && f !== "." && f !== "..";
    }, defaultPlatform = typeof process == "object" && process ? typeof process.env == "object" && process.env && process.env.__MINIMATCH_TESTING_PLATFORM__ || process.platform : "posix", path2 = {
      win32: { sep: "\\" },
      posix: { sep: "/" }
    }, sep = defaultPlatform === "win32" ? path2.win32.sep : path2.posix.sep;
    minimatch.sep = sep;
    GLOBSTAR = Symbol("globstar **");
    minimatch.GLOBSTAR = GLOBSTAR;
    qmark2 = "[^/]", star2 = qmark2 + "*?", twoStarDot = "(?:(?!(?:\\/|^)(?:\\.{1,2})($|\\/)).)*?", twoStarNoDot = "(?:(?!(?:\\/|^)\\.).)*?", filter = (pattern, options = {}) => (p) => minimatch(p, pattern, options);
    minimatch.filter = filter;
    ext = (a, b = {}) => Object.assign({}, a, b), defaults = (def) => {
      if (!def || typeof def != "object" || !Object.keys(def).length)
        return minimatch;
      let orig = minimatch;
      return Object.assign((p, pattern, options = {}) => orig(p, pattern, ext(def, options)), {
        Minimatch: class extends orig.Minimatch {
          constructor(pattern, options = {}) {
            super(pattern, ext(def, options));
          }
          static defaults(options) {
            return orig.defaults(ext(def, options)).Minimatch;
          }
        },
        AST: class extends orig.AST {
          /* c8 ignore start */
          constructor(type, parent, options = {}) {
            super(type, parent, ext(def, options));
          }
          /* c8 ignore stop */
          static fromGlob(pattern, options = {}) {
            return orig.AST.fromGlob(pattern, ext(def, options));
          }
        },
        unescape: (s, options = {}) => orig.unescape(s, ext(def, options)),
        escape: (s, options = {}) => orig.escape(s, ext(def, options)),
        filter: (pattern, options = {}) => orig.filter(pattern, ext(def, options)),
        defaults: (options) => orig.defaults(ext(def, options)),
        makeRe: (pattern, options = {}) => orig.makeRe(pattern, ext(def, options)),
        braceExpand: (pattern, options = {}) => orig.braceExpand(pattern, ext(def, options)),
        match: (list, pattern, options = {}) => orig.match(list, pattern, ext(def, options)),
        sep: orig.sep,
        GLOBSTAR
      });
    };
    minimatch.defaults = defaults;
    braceExpand = (pattern, options = {}) => (assertValidPattern(pattern), options.nobrace || !/\{(?:(?!\{).)*\}/.test(pattern) ? [pattern] : expand(pattern, { max: options.braceExpandMax }));
    minimatch.braceExpand = braceExpand;
    makeRe = (pattern, options = {}) => new Minimatch(pattern, options).makeRe();
    minimatch.makeRe = makeRe;
    match = (list, pattern, options = {}) => {
      let mm = new Minimatch(pattern, options);
      return list = list.filter((f) => mm.match(f)), mm.options.nonull && !list.length && list.push(pattern), list;
    };
    minimatch.match = match;
    globMagic = /[?*]|[+@!]\(.*?\)|\[|\]/, regExpEscape2 = (s) => s.replace(/[-[\]{}()*+?.,\\^$|#\s]/g, "\\$&"), Minimatch = class {
      options;
      set;
      pattern;
      windowsPathsNoEscape;
      nonegate;
      negate;
      comment;
      empty;
      preserveMultipleSlashes;
      partial;
      globSet;
      globParts;
      nocase;
      isWindows;
      platform;
      windowsNoMagicRoot;
      maxGlobstarRecursion;
      regexp;
      constructor(pattern, options = {}) {
        assertValidPattern(pattern), options = options || {}, this.options = options, this.maxGlobstarRecursion = options.maxGlobstarRecursion ?? 200, this.pattern = pattern, this.platform = options.platform || defaultPlatform, this.isWindows = this.platform === "win32";
        let awe = "allowWindowsEscape";
        this.windowsPathsNoEscape = !!options.windowsPathsNoEscape || options[awe] === !1, this.windowsPathsNoEscape && (this.pattern = this.pattern.replace(/\\/g, "/")), this.preserveMultipleSlashes = !!options.preserveMultipleSlashes, this.regexp = null, this.negate = !1, this.nonegate = !!options.nonegate, this.comment = !1, this.empty = !1, this.partial = !!options.partial, this.nocase = !!this.options.nocase, this.windowsNoMagicRoot = options.windowsNoMagicRoot !== void 0 ? options.windowsNoMagicRoot : !!(this.isWindows && this.nocase), this.globSet = [], this.globParts = [], this.set = [], this.make();
      }
      hasMagic() {
        if (this.options.magicalBraces && this.set.length > 1)
          return !0;
        for (let pattern of this.set)
          for (let part of pattern)
            if (typeof part != "string")
              return !0;
        return !1;
      }
      debug(..._) {
      }
      make() {
        let pattern = this.pattern, options = this.options;
        if (!options.nocomment && pattern.charAt(0) === "#") {
          this.comment = !0;
          return;
        }
        if (!pattern) {
          this.empty = !0;
          return;
        }
        this.parseNegate(), this.globSet = [...new Set(this.braceExpand())], options.debug && (this.debug = (...args) => console.error(...args)), this.debug(this.pattern, this.globSet);
        let rawGlobParts = this.globSet.map((s) => this.slashSplit(s));
        this.globParts = this.preprocess(rawGlobParts), this.debug(this.pattern, this.globParts);
        let set = this.globParts.map((s, _, __) => {
          if (this.isWindows && this.windowsNoMagicRoot) {
            let isUNC = s[0] === "" && s[1] === "" && (s[2] === "?" || !globMagic.test(s[2])) && !globMagic.test(s[3]), isDrive = /^[a-z]:/i.test(s[0]);
            if (isUNC)
              return [
                ...s.slice(0, 4),
                ...s.slice(4).map((ss) => this.parse(ss))
              ];
            if (isDrive)
              return [s[0], ...s.slice(1).map((ss) => this.parse(ss))];
          }
          return s.map((ss) => this.parse(ss));
        });
        if (this.debug(this.pattern, set), this.set = set.filter((s) => s.indexOf(!1) === -1), this.isWindows)
          for (let i = 0; i < this.set.length; i++) {
            let p = this.set[i];
            p[0] === "" && p[1] === "" && this.globParts[i][2] === "?" && typeof p[3] == "string" && /^[a-z]:$/i.test(p[3]) && (p[2] = "?");
          }
        this.debug(this.pattern, this.set);
      }
      // various transforms to equivalent pattern sets that are
      // faster to process in a filesystem walk.  The goal is to
      // eliminate what we can, and push all ** patterns as far
      // to the right as possible, even if it increases the number
      // of patterns that we have to process.
      preprocess(globParts) {
        if (this.options.noglobstar)
          for (let partset of globParts)
            for (let j = 0; j < partset.length; j++)
              partset[j] === "**" && (partset[j] = "*");
        let { optimizationLevel = 1 } = this.options;
        return optimizationLevel >= 2 ? (globParts = this.firstPhasePreProcess(globParts), globParts = this.secondPhasePreProcess(globParts)) : optimizationLevel >= 1 ? globParts = this.levelOneOptimize(globParts) : globParts = this.adjascentGlobstarOptimize(globParts), globParts;
      }
      // just get rid of adjascent ** portions
      adjascentGlobstarOptimize(globParts) {
        return globParts.map((parts) => {
          let gs = -1;
          for (; (gs = parts.indexOf("**", gs + 1)) !== -1; ) {
            let i = gs;
            for (; parts[i + 1] === "**"; )
              i++;
            i !== gs && parts.splice(gs, i - gs);
          }
          return parts;
        });
      }
      // get rid of adjascent ** and resolve .. portions
      levelOneOptimize(globParts) {
        return globParts.map((parts) => (parts = parts.reduce((set, part) => {
          let prev = set[set.length - 1];
          return part === "**" && prev === "**" ? set : part === ".." && prev && prev !== ".." && prev !== "." && prev !== "**" ? (set.pop(), set) : (set.push(part), set);
        }, []), parts.length === 0 ? [""] : parts));
      }
      levelTwoFileOptimize(parts) {
        Array.isArray(parts) || (parts = this.slashSplit(parts));
        let didSomething = !1;
        do {
          if (didSomething = !1, !this.preserveMultipleSlashes) {
            for (let i = 1; i < parts.length - 1; i++) {
              let p = parts[i];
              i === 1 && p === "" && parts[0] === "" || (p === "." || p === "") && (didSomething = !0, parts.splice(i, 1), i--);
            }
            parts[0] === "." && parts.length === 2 && (parts[1] === "." || parts[1] === "") && (didSomething = !0, parts.pop());
          }
          let dd = 0;
          for (; (dd = parts.indexOf("..", dd + 1)) !== -1; ) {
            let p = parts[dd - 1];
            p && p !== "." && p !== ".." && p !== "**" && !(this.isWindows && /^[a-z]:$/i.test(p)) && (didSomething = !0, parts.splice(dd - 1, 2), dd -= 2);
          }
        } while (didSomething);
        return parts.length === 0 ? [""] : parts;
      }
      // First phase: single-pattern processing
      // <pre> is 1 or more portions
      // <rest> is 1 or more portions
      // <p> is any portion other than ., .., '', or **
      // <e> is . or ''
      //
      // **/.. is *brutal* for filesystem walking performance, because
      // it effectively resets the recursive walk each time it occurs,
      // and ** cannot be reduced out by a .. pattern part like a regexp
      // or most strings (other than .., ., and '') can be.
      //
      // <pre>/**/../<p>/<p>/<rest> -> {<pre>/../<p>/<p>/<rest>,<pre>/**/<p>/<p>/<rest>}
      // <pre>/<e>/<rest> -> <pre>/<rest>
      // <pre>/<p>/../<rest> -> <pre>/<rest>
      // **/**/<rest> -> **/<rest>
      //
      // **/*/<rest> -> */**/<rest> <== not valid because ** doesn't follow
      // this WOULD be allowed if ** did follow symlinks, or * didn't
      firstPhasePreProcess(globParts) {
        let didSomething = !1;
        do {
          didSomething = !1;
          for (let parts of globParts) {
            let gs = -1;
            for (; (gs = parts.indexOf("**", gs + 1)) !== -1; ) {
              let gss = gs;
              for (; parts[gss + 1] === "**"; )
                gss++;
              gss > gs && parts.splice(gs + 1, gss - gs);
              let next = parts[gs + 1], p = parts[gs + 2], p2 = parts[gs + 3];
              if (next !== ".." || !p || p === "." || p === ".." || !p2 || p2 === "." || p2 === "..")
                continue;
              didSomething = !0, parts.splice(gs, 1);
              let other = parts.slice(0);
              other[gs] = "**", globParts.push(other), gs--;
            }
            if (!this.preserveMultipleSlashes) {
              for (let i = 1; i < parts.length - 1; i++) {
                let p = parts[i];
                i === 1 && p === "" && parts[0] === "" || (p === "." || p === "") && (didSomething = !0, parts.splice(i, 1), i--);
              }
              parts[0] === "." && parts.length === 2 && (parts[1] === "." || parts[1] === "") && (didSomething = !0, parts.pop());
            }
            let dd = 0;
            for (; (dd = parts.indexOf("..", dd + 1)) !== -1; ) {
              let p = parts[dd - 1];
              if (p && p !== "." && p !== ".." && p !== "**") {
                didSomething = !0;
                let splin = dd === 1 && parts[dd + 1] === "**" ? ["."] : [];
                parts.splice(dd - 1, 2, ...splin), parts.length === 0 && parts.push(""), dd -= 2;
              }
            }
          }
        } while (didSomething);
        return globParts;
      }
      // second phase: multi-pattern dedupes
      // {<pre>/*/<rest>,<pre>/<p>/<rest>} -> <pre>/*/<rest>
      // {<pre>/<rest>,<pre>/<rest>} -> <pre>/<rest>
      // {<pre>/**/<rest>,<pre>/<rest>} -> <pre>/**/<rest>
      //
      // {<pre>/**/<rest>,<pre>/**/<p>/<rest>} -> <pre>/**/<rest>
      // ^-- not valid because ** doens't follow symlinks
      secondPhasePreProcess(globParts) {
        for (let i = 0; i < globParts.length - 1; i++)
          for (let j = i + 1; j < globParts.length; j++) {
            let matched = this.partsMatch(globParts[i], globParts[j], !this.preserveMultipleSlashes);
            if (matched) {
              globParts[i] = [], globParts[j] = matched;
              break;
            }
          }
        return globParts.filter((gs) => gs.length);
      }
      partsMatch(a, b, emptyGSMatch = !1) {
        let ai = 0, bi = 0, result = [], which = "";
        for (; ai < a.length && bi < b.length; )
          if (a[ai] === b[bi])
            result.push(which === "b" ? b[bi] : a[ai]), ai++, bi++;
          else if (emptyGSMatch && a[ai] === "**" && b[bi] === a[ai + 1])
            result.push(a[ai]), ai++;
          else if (emptyGSMatch && b[bi] === "**" && a[ai] === b[bi + 1])
            result.push(b[bi]), bi++;
          else if (a[ai] === "*" && b[bi] && (this.options.dot || !b[bi].startsWith(".")) && b[bi] !== "**") {
            if (which === "b")
              return !1;
            which = "a", result.push(a[ai]), ai++, bi++;
          } else if (b[bi] === "*" && a[ai] && (this.options.dot || !a[ai].startsWith(".")) && a[ai] !== "**") {
            if (which === "a")
              return !1;
            which = "b", result.push(b[bi]), ai++, bi++;
          } else
            return !1;
        return a.length === b.length && result;
      }
      parseNegate() {
        if (this.nonegate)
          return;
        let pattern = this.pattern, negate = !1, negateOffset = 0;
        for (let i = 0; i < pattern.length && pattern.charAt(i) === "!"; i++)
          negate = !negate, negateOffset++;
        negateOffset && (this.pattern = pattern.slice(negateOffset)), this.negate = negate;
      }
      // set partial to true to test if, for example,
      // "/a/b" matches the start of "/*/b/*/d"
      // Partial means, if you run out of file before you run
      // out of pattern, then that's fine, as long as all
      // the parts match.
      matchOne(file, pattern, partial = !1) {
        let fileStartIndex = 0, patternStartIndex = 0;
        if (this.isWindows) {
          let fileDrive = typeof file[0] == "string" && /^[a-z]:$/i.test(file[0]), fileUNC = !fileDrive && file[0] === "" && file[1] === "" && file[2] === "?" && /^[a-z]:$/i.test(file[3]), patternDrive = typeof pattern[0] == "string" && /^[a-z]:$/i.test(pattern[0]), patternUNC = !patternDrive && pattern[0] === "" && pattern[1] === "" && pattern[2] === "?" && typeof pattern[3] == "string" && /^[a-z]:$/i.test(pattern[3]), fdi = fileUNC ? 3 : fileDrive ? 0 : void 0, pdi = patternUNC ? 3 : patternDrive ? 0 : void 0;
          if (typeof fdi == "number" && typeof pdi == "number") {
            let [fd, pd] = [
              file[fdi],
              pattern[pdi]
            ];
            fd.toLowerCase() === pd.toLowerCase() && (pattern[pdi] = fd, patternStartIndex = pdi, fileStartIndex = fdi);
          }
        }
        let { optimizationLevel = 1 } = this.options;
        return optimizationLevel >= 2 && (file = this.levelTwoFileOptimize(file)), pattern.includes(GLOBSTAR) ? this.#matchGlobstar(file, pattern, partial, fileStartIndex, patternStartIndex) : this.#matchOne(file, pattern, partial, fileStartIndex, patternStartIndex);
      }
      #matchGlobstar(file, pattern, partial, fileIndex, patternIndex) {
        let firstgs = pattern.indexOf(GLOBSTAR, patternIndex), lastgs = pattern.lastIndexOf(GLOBSTAR), [head, body, tail] = partial ? [
          pattern.slice(patternIndex, firstgs),
          pattern.slice(firstgs + 1),
          []
        ] : [
          pattern.slice(patternIndex, firstgs),
          pattern.slice(firstgs + 1, lastgs),
          pattern.slice(lastgs + 1)
        ];
        if (head.length) {
          let fileHead = file.slice(fileIndex, fileIndex + head.length);
          if (!this.#matchOne(fileHead, head, partial, 0, 0))
            return !1;
          fileIndex += head.length, patternIndex += head.length;
        }
        let fileTailMatch = 0;
        if (tail.length) {
          if (tail.length + fileIndex > file.length)
            return !1;
          let tailStart = file.length - tail.length;
          if (this.#matchOne(file, tail, partial, tailStart, 0))
            fileTailMatch = tail.length;
          else {
            if (file[file.length - 1] !== "" || fileIndex + tail.length === file.length || (tailStart--, !this.#matchOne(file, tail, partial, tailStart, 0)))
              return !1;
            fileTailMatch = tail.length + 1;
          }
        }
        if (!body.length) {
          let sawSome = !!fileTailMatch;
          for (let i2 = fileIndex; i2 < file.length - fileTailMatch; i2++) {
            let f = String(file[i2]);
            if (sawSome = !0, f === "." || f === ".." || !this.options.dot && f.startsWith("."))
              return !1;
          }
          return partial || sawSome;
        }
        let bodySegments = [[[], 0]], currentBody = bodySegments[0], nonGsParts = 0, nonGsPartsSums = [0];
        for (let b of body)
          b === GLOBSTAR ? (nonGsPartsSums.push(nonGsParts), currentBody = [[], 0], bodySegments.push(currentBody)) : (currentBody[0].push(b), nonGsParts++);
        let i = bodySegments.length - 1, fileLength = file.length - fileTailMatch;
        for (let b of bodySegments)
          b[1] = fileLength - (nonGsPartsSums[i--] + b[0].length);
        return !!this.#matchGlobStarBodySections(file, bodySegments, fileIndex, 0, partial, 0, !!fileTailMatch);
      }
      // return false for "nope, not matching"
      // return null for "not matching, cannot keep trying"
      #matchGlobStarBodySections(file, bodySegments, fileIndex, bodyIndex, partial, globStarDepth, sawTail) {
        let bs = bodySegments[bodyIndex];
        if (!bs) {
          for (let i = fileIndex; i < file.length; i++) {
            sawTail = !0;
            let f = file[i];
            if (f === "." || f === ".." || !this.options.dot && f.startsWith("."))
              return !1;
          }
          return sawTail;
        }
        let [body, after] = bs;
        for (; fileIndex <= after; ) {
          if (this.#matchOne(file.slice(0, fileIndex + body.length), body, partial, fileIndex, 0) && globStarDepth < this.maxGlobstarRecursion) {
            let sub = this.#matchGlobStarBodySections(file, bodySegments, fileIndex + body.length, bodyIndex + 1, partial, globStarDepth + 1, sawTail);
            if (sub !== !1)
              return sub;
          }
          let f = file[fileIndex];
          if (f === "." || f === ".." || !this.options.dot && f.startsWith("."))
            return !1;
          fileIndex++;
        }
        return partial || null;
      }
      #matchOne(file, pattern, partial, fileIndex, patternIndex) {
        let fi, pi, pl, fl;
        for (fi = fileIndex, pi = patternIndex, fl = file.length, pl = pattern.length; fi < fl && pi < pl; fi++, pi++) {
          this.debug("matchOne loop");
          let p = pattern[pi], f = file[fi];
          if (this.debug(pattern, p, f), p === !1 || p === GLOBSTAR)
            return !1;
          let hit;
          if (typeof p == "string" ? (hit = f === p, this.debug("string match", p, f, hit)) : (hit = p.test(f), this.debug("pattern match", p, f, hit)), !hit)
            return !1;
        }
        if (fi === fl && pi === pl)
          return !0;
        if (fi === fl)
          return partial;
        if (pi === pl)
          return fi === fl - 1 && file[fi] === "";
        throw new Error("wtf?");
      }
      braceExpand() {
        return braceExpand(this.pattern, this.options);
      }
      parse(pattern) {
        assertValidPattern(pattern);
        let options = this.options;
        if (pattern === "**")
          return GLOBSTAR;
        if (pattern === "")
          return "";
        let m, fastTest = null;
        (m = pattern.match(starRE)) ? fastTest = options.dot ? starTestDot : starTest : (m = pattern.match(starDotExtRE)) ? fastTest = (options.nocase ? options.dot ? starDotExtTestNocaseDot : starDotExtTestNocase : options.dot ? starDotExtTestDot : starDotExtTest)(m[1]) : (m = pattern.match(qmarksRE)) ? fastTest = (options.nocase ? options.dot ? qmarksTestNocaseDot : qmarksTestNocase : options.dot ? qmarksTestDot : qmarksTest)(m) : (m = pattern.match(starDotStarRE)) ? fastTest = options.dot ? starDotStarTestDot : starDotStarTest : (m = pattern.match(dotStarRE)) && (fastTest = dotStarTest);
        let re = AST.fromGlob(pattern, this.options).toMMPattern();
        return fastTest && typeof re == "object" && Reflect.defineProperty(re, "test", { value: fastTest }), re;
      }
      makeRe() {
        if (this.regexp || this.regexp === !1)
          return this.regexp;
        let set = this.set;
        if (!set.length)
          return this.regexp = !1, this.regexp;
        let options = this.options, twoStar = options.noglobstar ? star2 : options.dot ? twoStarDot : twoStarNoDot, flags = new Set(options.nocase ? ["i"] : []), re = set.map((pattern) => {
          let pp = pattern.map((p) => {
            if (p instanceof RegExp)
              for (let f of p.flags.split(""))
                flags.add(f);
            return typeof p == "string" ? regExpEscape2(p) : p === GLOBSTAR ? GLOBSTAR : p._src;
          });
          pp.forEach((p, i) => {
            let next = pp[i + 1], prev = pp[i - 1];
            p !== GLOBSTAR || prev === GLOBSTAR || (prev === void 0 ? next !== void 0 && next !== GLOBSTAR ? pp[i + 1] = "(?:\\/|" + twoStar + "\\/)?" + next : pp[i] = twoStar : next === void 0 ? pp[i - 1] = prev + "(?:\\/|\\/" + twoStar + ")?" : next !== GLOBSTAR && (pp[i - 1] = prev + "(?:\\/|\\/" + twoStar + "\\/)" + next, pp[i + 1] = GLOBSTAR));
          });
          let filtered = pp.filter((p) => p !== GLOBSTAR);
          if (this.partial && filtered.length >= 1) {
            let prefixes = [];
            for (let i = 1; i <= filtered.length; i++)
              prefixes.push(filtered.slice(0, i).join("/"));
            return "(?:" + prefixes.join("|") + ")";
          }
          return filtered.join("/");
        }).join("|"), [open3, close] = set.length > 1 ? ["(?:", ")"] : ["", ""];
        re = "^" + open3 + re + close + "$", this.partial && (re = "^(?:\\/|" + open3 + re.slice(1, -1) + close + ")$"), this.negate && (re = "^(?!" + re + ").+$");
        try {
          this.regexp = new RegExp(re, [...flags].join(""));
        } catch {
          this.regexp = !1;
        }
        return this.regexp;
      }
      slashSplit(p) {
        return this.preserveMultipleSlashes ? p.split("/") : this.isWindows && /^\/\/[^/]+/.test(p) ? ["", ...p.split(/\/+/)] : p.split(/\/+/);
      }
      match(f, partial = this.partial) {
        if (this.debug("match", f, this.pattern), this.comment)
          return !1;
        if (this.empty)
          return f === "";
        if (f === "/" && partial)
          return !0;
        let options = this.options;
        this.isWindows && (f = f.split("\\").join("/"));
        let ff = this.slashSplit(f);
        this.debug(this.pattern, "split", ff);
        let set = this.set;
        this.debug(this.pattern, "set", set);
        let filename = ff[ff.length - 1];
        if (!filename)
          for (let i = ff.length - 2; !filename && i >= 0; i--)
            filename = ff[i];
        for (let pattern of set) {
          let file = ff;
          if (options.matchBase && pattern.length === 1 && (file = [filename]), this.matchOne(file, pattern, partial))
            return options.flipNegate ? !0 : !this.negate;
        }
        return options.flipNegate ? !1 : this.negate;
      }
      static defaults(def) {
        return minimatch.defaults(def).Minimatch;
      }
    };
    minimatch.AST = AST;
    minimatch.Minimatch = Minimatch;
    minimatch.escape = escape2;
    minimatch.unescape = unescape2;
  }
});

// ../core/dist/verifier/checkBoundaries.js
function matchesAny(file, patterns) {
  return patterns.some((pattern) => minimatch(file, pattern, { dot: !0 }));
}
async function checkFileBoundaries(worktreePath, task) {
  let modifiedFiles = await listModifiedFiles(worktreePath), violations = [];
  for (let file of modifiedFiles)
    matchesAny(file, task.blockedFiles) ? violations.push({
      violationType: "BLOCKED_FILE_MODIFIED",
      file,
      message: `"${file}" matches a blocked pattern and must not be modified`
    }) : matchesAny(file, task.allowedFiles) || violations.push({
      violationType: "UNOWNED_FILE_MODIFIED",
      file,
      message: `"${file}" is outside the task's allowedFiles scope`
    });
  return BoundaryCheckResultSchema.parse({
    passed: violations.length === 0,
    violations
  });
}
var init_checkBoundaries = __esm({
  "../core/dist/verifier/checkBoundaries.js"() {
    "use strict";
    init_esm3();
    init_generatePatch();
    init_schemas();
  }
});

// ../core/dist/verifier/runCommands.js
async function runCommand(cmd, cwd) {
  let startMs = Date.now(), exitCode = 0, stdout = "", stderr = "";
  try {
    let result = await execAsync3(cmd.command, { cwd });
    stdout = result.stdout, stderr = result.stderr, exitCode = 0;
  } catch (err) {
    let execErr = err;
    exitCode = execErr.code ?? 1, stdout = execErr.stdout ?? "", stderr = execErr.stderr ?? "";
  }
  return CommandResultSchema.parse({
    name: cmd.name,
    command: cmd.command,
    exitCode,
    stdout,
    stderr,
    durationMs: Date.now() - startMs
  });
}
async function runVerificationCommands(worktreePath, commands) {
  let results = [];
  for (let cmd of commands) {
    let result = await runCommand(cmd, worktreePath);
    if (results.push(result), cmd.required && result.exitCode !== 0)
      break;
  }
  return results;
}
var import_child_process4, import_util6, execAsync3, init_runCommands = __esm({
  "../core/dist/verifier/runCommands.js"() {
    "use strict";
    import_child_process4 = require("child_process"), import_util6 = require("util");
    init_schemas();
    execAsync3 = (0, import_util6.promisify)(import_child_process4.exec);
  }
});

// ../core/dist/verifier/verifyTask.js
async function verifyTask(worktreePath, task, commands) {
  let boundaryCheck = await checkFileBoundaries(worktreePath, task), commandResults = boundaryCheck.passed ? await runVerificationCommands(worktreePath, commands) : [], commandsPassed = commandResults.every((r) => !commands.find((c) => c.name === r.name)?.required || r.exitCode === 0), passed = boundaryCheck.passed && (commandResults.length === 0 || commandsPassed);
  return VerificationResultSchema.parse({
    taskId: task.taskId,
    passed,
    boundaryCheck,
    commandResults
  });
}
var init_verifyTask = __esm({
  "../core/dist/verifier/verifyTask.js"() {
    "use strict";
    init_schemas();
    init_checkBoundaries();
    init_runCommands();
  }
});

// ../core/dist/verifier/index.js
var init_verifier = __esm({
  "../core/dist/verifier/index.js"() {
    "use strict";
    init_checkBoundaries();
    init_runCommands();
    init_verifyTask();
  }
});

// ../core/dist/replay/replayLedger.js
function assertRunTransition(current, next, index, eventType) {
  if (!VALID_RUN_TRANSITIONS[current].includes(next))
    throw new RunReplayError(`Invalid run state transition: "${current}" \u2192 "${next}" (event ${index}: ${eventType})`, index, eventType);
}
function assertTaskTransition(taskId, current, next, index, eventType) {
  if (!VALID_TASK_TRANSITIONS[current].includes(next))
    throw new RunReplayError(`Invalid task status transition for "${taskId}": "${current}" \u2192 "${next}" (event ${index}: ${eventType})`, index, eventType);
}
function replayLedger(events, runId) {
  let status = "created", goal = "", operator, startedAt, completedAt, tasks = /* @__PURE__ */ new Map(), filesModified = [], runEvents = events.filter((e) => e.run_id === runId);
  for (let i = 0; i < runEvents.length; i++) {
    let event = runEvents[i], { event_type, payload, timestamp } = event;
    switch (event_type) {
      case "RUN_CREATED": {
        status = "created", goal = typeof payload.goal == "string" ? payload.goal : "", operator = typeof payload.operator == "string" ? payload.operator : void 0, startedAt = timestamp;
        break;
      }
      case "INTENT_COMPILED": {
        assertRunTransition(status, "planning", i, event_type), status = "planning";
        break;
      }
      case "TASK_CREATED": {
        let taskId = event.task_id ?? (typeof payload.taskId == "string" ? payload.taskId : void 0);
        if (taskId) {
          let task = {
            taskId,
            runId,
            title: typeof payload.title == "string" ? payload.title : taskId,
            description: typeof payload.description == "string" ? payload.description : "",
            owner: typeof payload.owner == "string" ? payload.owner : "orchestrator",
            dependencies: Array.isArray(payload.dependencies) ? payload.dependencies : [],
            allowedFiles: Array.isArray(payload.allowedFiles) ? payload.allowedFiles : [],
            blockedFiles: Array.isArray(payload.blockedFiles) ? payload.blockedFiles : [],
            allowedTools: Array.isArray(payload.allowedTools) ? payload.allowedTools : [],
            expectedOutputs: [],
            successCriteria: [],
            status: "pending"
          };
          tasks.set(taskId, task);
        }
        break;
      }
      case "TASK_ASSIGNED": {
        let taskId = event.task_id ?? (typeof payload.taskId == "string" ? payload.taskId : void 0);
        if (taskId) {
          let task = tasks.get(taskId);
          if (task) {
            assertTaskTransition(taskId, task.status, "assigned", i, event_type);
            let newOwner = typeof payload.owner == "string" ? payload.owner : task.owner;
            tasks.set(taskId, { ...task, status: "assigned", owner: newOwner });
          }
        }
        break;
      }
      case "TASK_STARTED": {
        (status === "planning" || status === "created") && (assertRunTransition(status, "executing", i, event_type), status = "executing");
        let taskId = event.task_id ?? (typeof payload.taskId == "string" ? payload.taskId : void 0);
        if (taskId) {
          let task = tasks.get(taskId);
          task && (assertTaskTransition(taskId, task.status, "running", i, event_type), tasks.set(taskId, { ...task, status: "running" }));
        }
        break;
      }
      case "PATCH_PROPOSED": {
        let files = payload.filesModified;
        if (Array.isArray(files))
          for (let f of files)
            typeof f == "string" && !filesModified.includes(f) && filesModified.push(f);
        break;
      }
      case "HUMAN_APPROVAL_REQUESTED": {
        (status === "executing" || status === "verifying") && (status = "paused");
        let taskId = event.task_id ?? (typeof payload.taskId == "string" ? payload.taskId : void 0);
        if (taskId) {
          let task = tasks.get(taskId);
          task && task.status === "running" && (assertTaskTransition(taskId, task.status, "awaiting_approval", i, event_type), tasks.set(taskId, { ...task, status: "awaiting_approval" }));
        }
        break;
      }
      case "HUMAN_APPROVAL_GRANTED": {
        status === "paused" && (status = "executing");
        let taskId = event.task_id ?? (typeof payload.taskId == "string" ? payload.taskId : void 0);
        if (taskId) {
          let task = tasks.get(taskId);
          task && task.status === "awaiting_approval" && (assertTaskTransition(taskId, task.status, "awaiting_verification", i, event_type), tasks.set(taskId, { ...task, status: "awaiting_verification" }));
        }
        break;
      }
      case "HUMAN_APPROVAL_REJECTED": {
        let taskId = event.task_id ?? (typeof payload.taskId == "string" ? payload.taskId : void 0);
        if (taskId) {
          let task = tasks.get(taskId);
          task && task.status === "awaiting_approval" && (assertTaskTransition(taskId, task.status, "failed", i, event_type), tasks.set(taskId, { ...task, status: "failed" }));
        }
        break;
      }
      case "VERIFICATION_STARTED": {
        status !== "verifying" && (status === "paused" || assertRunTransition(status, "verifying", i, event_type), status = "verifying");
        break;
      }
      case "VERIFICATION_PASSED":
      case "TASK_COMPLETED": {
        let taskId = event.task_id ?? (typeof payload.taskId == "string" ? payload.taskId : void 0);
        if (taskId) {
          let task = tasks.get(taskId);
          task && task.status !== "completed" && (task.status !== "awaiting_verification" && task.status !== "running" && assertTaskTransition(taskId, task.status, "completed", i, event_type), tasks.set(taskId, { ...task, status: "completed" }));
        }
        break;
      }
      case "BOUNDARY_VIOLATION":
      case "VERIFICATION_FAILED":
      case "TASK_FAILED": {
        let taskId = event.task_id ?? (typeof payload.taskId == "string" ? payload.taskId : void 0);
        if (taskId) {
          let task = tasks.get(taskId);
          task && task.status !== "failed" && task.status !== "completed" && (assertTaskTransition(taskId, task.status, "failed", i, event_type), tasks.set(taskId, { ...task, status: "failed" }));
        }
        break;
      }
      case "RUN_COMPLETED": {
        assertRunTransition(status, "completed", i, event_type), status = "completed", completedAt = timestamp;
        break;
      }
      case "RUN_FAILED": {
        assertRunTransition(status, "failed", i, event_type), status = "failed", completedAt = timestamp;
        break;
      }
      // Events that don't affect RunState reconstruction
      case "WORKTREE_CREATED":
      case "CONTEXT_READ":
      case "TOOL_CALLED":
      case "TOOL_DENIED":
      case "FILE_EDIT_PROPOSED":
        break;
    }
  }
  return RunStateSchema.parse({
    runId,
    status,
    goal,
    operator,
    tasks: Array.from(tasks.values()),
    filesModified,
    startedAt,
    completedAt
  });
}
var VALID_RUN_TRANSITIONS, VALID_TASK_TRANSITIONS, RunReplayError, init_replayLedger = __esm({
  "../core/dist/replay/replayLedger.js"() {
    "use strict";
    init_schemas();
    VALID_RUN_TRANSITIONS = {
      created: ["planning", "failed"],
      planning: ["executing", "completed", "failed"],
      executing: ["verifying", "paused", "completed", "failed"],
      verifying: ["executing", "paused", "completed", "failed"],
      paused: ["executing", "failed"],
      completed: [],
      failed: []
    }, VALID_TASK_TRANSITIONS = {
      pending: ["assigned", "failed"],
      assigned: ["running", "failed"],
      running: ["awaiting_approval", "awaiting_verification", "completed", "failed"],
      awaiting_approval: ["awaiting_verification", "failed"],
      awaiting_verification: ["completed", "failed"],
      completed: [],
      failed: []
    }, RunReplayError = class extends Error {
      eventIndex;
      eventType;
      constructor(message, eventIndex, eventType) {
        super(message), this.eventIndex = eventIndex, this.eventType = eventType, this.name = "RunReplayError";
      }
    };
  }
});

// ../core/dist/replay/index.js
var init_replay = __esm({
  "../core/dist/replay/index.js"() {
    "use strict";
    init_replayLedger();
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/tslib.mjs
function __classPrivateFieldSet(receiver, state, value, kind, f) {
  if (kind === "m")
    throw new TypeError("Private method is not writable");
  if (kind === "a" && !f)
    throw new TypeError("Private accessor was defined without a setter");
  if (typeof state == "function" ? receiver !== state || !f : !state.has(receiver))
    throw new TypeError("Cannot write private member to an object whose class did not declare it");
  return kind === "a" ? f.call(receiver, value) : f ? f.value = value : state.set(receiver, value), value;
}
function __classPrivateFieldGet(receiver, state, kind, f) {
  if (kind === "a" && !f)
    throw new TypeError("Private accessor was defined without a getter");
  if (typeof state == "function" ? receiver !== state || !f : !state.has(receiver))
    throw new TypeError("Cannot read private member from an object whose class did not declare it");
  return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
}
var init_tslib = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/tslib.mjs"() {
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/uuid.mjs
var uuid4, init_uuid = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/uuid.mjs"() {
    uuid4 = function() {
      let { crypto: crypto3 } = globalThis;
      if (crypto3?.randomUUID)
        return uuid4 = crypto3.randomUUID.bind(crypto3), crypto3.randomUUID();
      let u8 = new Uint8Array(1), randomByte = crypto3 ? () => crypto3.getRandomValues(u8)[0] : () => Math.random() * 255 & 255;
      return "10000000-1000-4000-8000-100000000000".replace(/[018]/g, (c) => (+c ^ randomByte() & 15 >> +c / 4).toString(16));
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/errors.mjs
function isAbortError(err) {
  return typeof err == "object" && err !== null && // Spec-compliant fetch implementations
  ("name" in err && err.name === "AbortError" || // Expo fetch
  "message" in err && String(err.message).includes("FetchRequestCanceledException"));
}
var castToError, init_errors2 = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/errors.mjs"() {
    castToError = (err) => {
      if (err instanceof Error)
        return err;
      if (typeof err == "object" && err !== null) {
        try {
          if (Object.prototype.toString.call(err) === "[object Error]") {
            let error = new Error(err.message, err.cause ? { cause: err.cause } : {});
            return err.stack && (error.stack = err.stack), err.cause && !error.cause && (error.cause = err.cause), err.name && (error.name = err.name), error;
          }
        } catch {
        }
        try {
          return new Error(JSON.stringify(err));
        } catch {
        }
      }
      return new Error(err);
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/core/error.mjs
var AnthropicError, APIError, APIUserAbortError, APIConnectionError, APIConnectionTimeoutError, RetryableError, BadRequestError, AuthenticationError, PermissionDeniedError, NotFoundError, ConflictError, UnprocessableEntityError, RateLimitError, InternalServerError, init_error = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/core/error.mjs"() {
    init_errors2();
    AnthropicError = class extends Error {
    }, APIError = class _APIError extends AnthropicError {
      constructor(status, error, message, headers, type) {
        super(`${_APIError.makeMessage(status, error, message)}`), this.status = status, this.headers = headers, this.requestID = headers?.get("request-id"), this.error = error, this.type = type ?? null;
      }
      static makeMessage(status, error, message) {
        let msg = error?.message ? typeof error.message == "string" ? error.message : JSON.stringify(error.message) : error ? JSON.stringify(error) : message;
        return status && msg ? `${status} ${msg}` : status ? `${status} status code (no body)` : msg || "(no status code or body)";
      }
      static generate(status, errorResponse, message, headers) {
        if (!status || !headers)
          return new APIConnectionError({ message, cause: castToError(errorResponse) });
        let error = errorResponse, type = error?.error?.type;
        return status === 400 ? new BadRequestError(status, error, message, headers, type) : status === 401 ? new AuthenticationError(status, error, message, headers, type) : status === 403 ? new PermissionDeniedError(status, error, message, headers, type) : status === 404 ? new NotFoundError(status, error, message, headers, type) : status === 409 ? new ConflictError(status, error, message, headers, type) : status === 422 ? new UnprocessableEntityError(status, error, message, headers, type) : status === 429 ? new RateLimitError(status, error, message, headers, type) : status >= 500 ? new InternalServerError(status, error, message, headers, type) : new _APIError(status, error, message, headers, type);
      }
    }, APIUserAbortError = class extends APIError {
      constructor({ message } = {}) {
        super(void 0, void 0, message || "Request was aborted.", void 0);
      }
    }, APIConnectionError = class extends APIError {
      constructor({ message, cause }) {
        super(void 0, void 0, message || "Connection error.", void 0), cause && (this.cause = cause);
      }
    }, APIConnectionTimeoutError = class extends APIConnectionError {
      constructor({ message } = {}) {
        super({ message: message ?? "Request timed out." });
      }
    }, RetryableError = class extends AnthropicError {
      constructor(message, { cause } = {}) {
        super(message ?? "Retryable error."), cause !== void 0 && (this.cause = cause);
      }
    }, BadRequestError = class extends APIError {
    }, AuthenticationError = class extends APIError {
    }, PermissionDeniedError = class extends APIError {
    }, NotFoundError = class extends APIError {
    }, ConflictError = class extends APIError {
    }, UnprocessableEntityError = class extends APIError {
    }, RateLimitError = class extends APIError {
    }, InternalServerError = class extends APIError {
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/values.mjs
function maybeObj(x) {
  return typeof x != "object" ? {} : x ?? {};
}
function isEmptyObj(obj) {
  if (!obj)
    return !0;
  for (let _k in obj)
    return !1;
  return !0;
}
function hasOwn(obj, key) {
  return Object.prototype.hasOwnProperty.call(obj, key);
}
var startsWithSchemeRegexp, isAbsoluteURL, isArray, isReadonlyArray, validatePositiveInteger, safeJSON, init_values = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/values.mjs"() {
    init_error();
    startsWithSchemeRegexp = /^[a-z][a-z0-9+.-]*:/i, isAbsoluteURL = (url) => startsWithSchemeRegexp.test(url), isArray = (val) => (isArray = Array.isArray, isArray(val)), isReadonlyArray = isArray;
    validatePositiveInteger = (name, n) => {
      if (typeof n != "number" || !Number.isInteger(n))
        throw new AnthropicError(`${name} must be an integer`);
      if (n < 0)
        throw new AnthropicError(`${name} must be a positive integer`);
      return n;
    }, safeJSON = (text) => {
      try {
        return JSON.parse(text);
      } catch {
        return;
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/sleep.mjs
var sleep, init_sleep = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/sleep.mjs"() {
    sleep = (ms, signal) => new Promise((resolve4) => {
      if (signal?.aborted)
        return resolve4();
      let onAbort = () => {
        clearTimeout(timer), resolve4();
      }, timer = setTimeout(() => {
        signal?.removeEventListener("abort", onAbort), resolve4();
      }, ms);
      signal?.addEventListener("abort", onAbort, { once: !0 });
    });
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/version.mjs
var VERSION, init_version = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/version.mjs"() {
    VERSION = "0.110.0";
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/detect-platform.mjs
function getDetectedPlatform() {
  return typeof Deno < "u" && Deno.build != null ? "deno" : typeof EdgeRuntime < "u" ? "edge" : Object.prototype.toString.call(typeof globalThis.process < "u" ? globalThis.process : 0) === "[object process]" ? "node" : "unknown";
}
function getBrowserInfo() {
  if (typeof navigator > "u" || !navigator)
    return null;
  let browserPatterns = [
    { key: "edge", pattern: /Edge(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/ },
    { key: "ie", pattern: /MSIE(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/ },
    { key: "ie", pattern: /Trident(?:.*rv\:(\d+)\.(\d+)(?:\.(\d+))?)?/ },
    { key: "chrome", pattern: /Chrome(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/ },
    { key: "firefox", pattern: /Firefox(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/ },
    { key: "safari", pattern: /(?:Version\W+(\d+)\.(\d+)(?:\.(\d+))?)?(?:\W+Mobile\S*)?\W+Safari/ }
  ];
  for (let { key, pattern } of browserPatterns) {
    let match2 = pattern.exec(navigator.userAgent);
    if (match2) {
      let major = match2[1] || 0, minor = match2[2] || 0, patch = match2[3] || 0;
      return { browser: key, version: `${major}.${minor}.${patch}` };
    }
  }
  return null;
}
var isRunningInBrowser, getPlatformProperties, normalizeArch, normalizePlatform, _platformHeaders, getPlatformHeaders, init_detect_platform = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/detect-platform.mjs"() {
    init_version();
    isRunningInBrowser = () => (
      // @ts-ignore
      typeof window < "u" && // @ts-ignore
      typeof window.document < "u" && // @ts-ignore
      typeof navigator < "u"
    );
    getPlatformProperties = () => {
      let detectedPlatform = getDetectedPlatform();
      if (detectedPlatform === "deno")
        return {
          "X-Stainless-Lang": "js",
          "X-Stainless-Package-Version": VERSION,
          "X-Stainless-OS": normalizePlatform(Deno.build.os),
          "X-Stainless-Arch": normalizeArch(Deno.build.arch),
          "X-Stainless-Runtime": "deno",
          "X-Stainless-Runtime-Version": typeof Deno.version == "string" ? Deno.version : Deno.version?.deno ?? "unknown"
        };
      if (typeof EdgeRuntime < "u")
        return {
          "X-Stainless-Lang": "js",
          "X-Stainless-Package-Version": VERSION,
          "X-Stainless-OS": "Unknown",
          "X-Stainless-Arch": `other:${EdgeRuntime}`,
          "X-Stainless-Runtime": "edge",
          "X-Stainless-Runtime-Version": globalThis.process.version
        };
      if (detectedPlatform === "node")
        return {
          "X-Stainless-Lang": "js",
          "X-Stainless-Package-Version": VERSION,
          "X-Stainless-OS": normalizePlatform(globalThis.process.platform ?? "unknown"),
          "X-Stainless-Arch": normalizeArch(globalThis.process.arch ?? "unknown"),
          "X-Stainless-Runtime": "node",
          "X-Stainless-Runtime-Version": globalThis.process.version ?? "unknown"
        };
      let browserInfo = getBrowserInfo();
      return browserInfo ? {
        "X-Stainless-Lang": "js",
        "X-Stainless-Package-Version": VERSION,
        "X-Stainless-OS": "Unknown",
        "X-Stainless-Arch": "unknown",
        "X-Stainless-Runtime": `browser:${browserInfo.browser}`,
        "X-Stainless-Runtime-Version": browserInfo.version
      } : {
        "X-Stainless-Lang": "js",
        "X-Stainless-Package-Version": VERSION,
        "X-Stainless-OS": "Unknown",
        "X-Stainless-Arch": "unknown",
        "X-Stainless-Runtime": "unknown",
        "X-Stainless-Runtime-Version": "unknown"
      };
    };
    normalizeArch = (arch) => arch === "x32" ? "x32" : arch === "x86_64" || arch === "x64" ? "x64" : arch === "arm" ? "arm" : arch === "aarch64" || arch === "arm64" ? "arm64" : arch ? `other:${arch}` : "unknown", normalizePlatform = (platform) => (platform = platform.toLowerCase(), platform.includes("ios") ? "iOS" : platform === "android" ? "Android" : platform === "darwin" ? "MacOS" : platform === "win32" ? "Windows" : platform === "freebsd" ? "FreeBSD" : platform === "openbsd" ? "OpenBSD" : platform === "linux" ? "Linux" : platform ? `Other:${platform}` : "Unknown"), getPlatformHeaders = () => _platformHeaders ?? (_platformHeaders = getPlatformProperties());
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/shims.mjs
function getDefaultFetch() {
  if (typeof fetch < "u")
    return fetch;
  throw new Error("`fetch` is not defined as a global; Either pass `fetch` to the client, `new Anthropic({ fetch })` or polyfill the global, `globalThis.fetch = fetch`");
}
function makeReadableStream(...args) {
  let ReadableStream2 = globalThis.ReadableStream;
  if (typeof ReadableStream2 > "u")
    throw new Error("`ReadableStream` is not defined as a global; You will need to polyfill it, `globalThis.ReadableStream = ReadableStream`");
  return new ReadableStream2(...args);
}
function ReadableStreamFrom(iterable) {
  let iter = Symbol.asyncIterator in iterable ? iterable[Symbol.asyncIterator]() : iterable[Symbol.iterator]();
  return makeReadableStream({
    start() {
    },
    async pull(controller) {
      let { done, value } = await iter.next();
      done ? controller.close() : controller.enqueue(value);
    },
    async cancel() {
      await iter.return?.();
    }
  });
}
function ReadableStreamToAsyncIterable(stream) {
  if (stream[Symbol.asyncIterator])
    return stream;
  let reader = stream.getReader();
  return {
    async next() {
      try {
        let result = await reader.read();
        return result?.done && reader.releaseLock(), result;
      } catch (e) {
        throw reader.releaseLock(), e;
      }
    },
    async return() {
      let cancelPromise = reader.cancel();
      return reader.releaseLock(), await cancelPromise, { done: !0, value: void 0 };
    },
    [Symbol.asyncIterator]() {
      return this;
    }
  };
}
async function CancelReadableStream(stream) {
  if (stream === null || typeof stream != "object")
    return;
  if (stream[Symbol.asyncIterator]) {
    await stream[Symbol.asyncIterator]().return?.();
    return;
  }
  let reader = stream.getReader(), cancelPromise = reader.cancel();
  reader.releaseLock(), await cancelPromise;
}
var init_shims = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/shims.mjs"() {
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/request-options.mjs
var FallbackEncoder, init_request_options = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/request-options.mjs"() {
    FallbackEncoder = ({ headers, body }) => ({
      bodyHeaders: {
        "content-type": "application/json"
      },
      body: JSON.stringify(body)
    });
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/qs/formats.mjs
var default_format, default_formatter, formatters, RFC1738, init_formats = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/qs/formats.mjs"() {
    default_format = "RFC3986", default_formatter = (v) => String(v), formatters = {
      RFC1738: (v) => String(v).replace(/%20/g, "+"),
      RFC3986: default_formatter
    }, RFC1738 = "RFC1738";
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/qs/utils.mjs
function is_buffer(obj) {
  return !obj || typeof obj != "object" ? !1 : !!(obj.constructor && obj.constructor.isBuffer && obj.constructor.isBuffer(obj));
}
function maybe_map(val, fn) {
  if (isArray(val)) {
    let mapped = [];
    for (let i = 0; i < val.length; i += 1)
      mapped.push(fn(val[i]));
    return mapped;
  }
  return fn(val);
}
var has, hex_table, limit, encode, init_utils = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/qs/utils.mjs"() {
    init_formats();
    init_values();
    has = (obj, key) => (has = Object.hasOwn ?? Function.prototype.call.bind(Object.prototype.hasOwnProperty), has(obj, key)), hex_table = /* @__PURE__ */ (() => {
      let array = [];
      for (let i = 0; i < 256; ++i)
        array.push("%" + ((i < 16 ? "0" : "") + i.toString(16)).toUpperCase());
      return array;
    })(), limit = 1024, encode = (str2, _defaultEncoder, charset, _kind, format) => {
      if (str2.length === 0)
        return str2;
      let string = str2;
      if (typeof str2 == "symbol" ? string = Symbol.prototype.toString.call(str2) : typeof str2 != "string" && (string = String(str2)), charset === "iso-8859-1")
        return escape(string).replace(/%u[0-9a-f]{4}/gi, function($0) {
          return "%26%23" + parseInt($0.slice(2), 16) + "%3B";
        });
      let out = "";
      for (let j = 0; j < string.length; j += limit) {
        let segment = string.length >= limit ? string.slice(j, j + limit) : string, arr = [];
        for (let i = 0; i < segment.length; ++i) {
          let c = segment.charCodeAt(i);
          if (c === 45 || // -
          c === 46 || // .
          c === 95 || // _
          c === 126 || // ~
          c >= 48 && c <= 57 || // 0-9
          c >= 65 && c <= 90 || // a-z
          c >= 97 && c <= 122 || // A-Z
          format === RFC1738 && (c === 40 || c === 41)) {
            arr[arr.length] = segment.charAt(i);
            continue;
          }
          if (c < 128) {
            arr[arr.length] = hex_table[c];
            continue;
          }
          if (c < 2048) {
            arr[arr.length] = hex_table[192 | c >> 6] + hex_table[128 | c & 63];
            continue;
          }
          if (c < 55296 || c >= 57344) {
            arr[arr.length] = hex_table[224 | c >> 12] + hex_table[128 | c >> 6 & 63] + hex_table[128 | c & 63];
            continue;
          }
          i += 1, c = 65536 + ((c & 1023) << 10 | segment.charCodeAt(i) & 1023), arr[arr.length] = hex_table[240 | c >> 18] + hex_table[128 | c >> 12 & 63] + hex_table[128 | c >> 6 & 63] + hex_table[128 | c & 63];
        }
        out += arr.join("");
      }
      return out;
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/qs/stringify.mjs
function is_non_nullish_primitive(v) {
  return typeof v == "string" || typeof v == "number" || typeof v == "boolean" || typeof v == "symbol" || typeof v == "bigint";
}
function inner_stringify(object, prefix, generateArrayPrefix, commaRoundTrip, allowEmptyArrays, strictNullHandling, skipNulls, encodeDotInKeys, encoder2, filter2, sort, allowDots, serializeDate, format, formatter, encodeValuesOnly, charset, sideChannel) {
  let obj = object, tmp_sc = sideChannel, step = 0, find_flag = !1;
  for (; (tmp_sc = tmp_sc.get(sentinel)) !== void 0 && !find_flag; ) {
    let pos = tmp_sc.get(object);
    if (step += 1, typeof pos < "u") {
      if (pos === step)
        throw new RangeError("Cyclic object value");
      find_flag = !0;
    }
    typeof tmp_sc.get(sentinel) > "u" && (step = 0);
  }
  if (typeof filter2 == "function" ? obj = filter2(prefix, obj) : obj instanceof Date ? obj = serializeDate?.(obj) : generateArrayPrefix === "comma" && isArray(obj) && (obj = maybe_map(obj, function(value) {
    return value instanceof Date ? serializeDate?.(value) : value;
  })), obj === null) {
    if (strictNullHandling)
      return encoder2 && !encodeValuesOnly ? (
        // @ts-expect-error
        encoder2(prefix, defaults2.encoder, charset, "key", format)
      ) : prefix;
    obj = "";
  }
  if (is_non_nullish_primitive(obj) || is_buffer(obj)) {
    if (encoder2) {
      let key_value = encodeValuesOnly ? prefix : encoder2(prefix, defaults2.encoder, charset, "key", format);
      return [
        formatter?.(key_value) + "=" + // @ts-expect-error
        formatter?.(encoder2(obj, defaults2.encoder, charset, "value", format))
      ];
    }
    return [formatter?.(prefix) + "=" + formatter?.(String(obj))];
  }
  let values = [];
  if (typeof obj > "u")
    return values;
  let obj_keys;
  if (generateArrayPrefix === "comma" && isArray(obj))
    encodeValuesOnly && encoder2 && (obj = maybe_map(obj, encoder2)), obj_keys = [{ value: obj.length > 0 ? obj.join(",") || null : void 0 }];
  else if (isArray(filter2))
    obj_keys = filter2;
  else {
    let keys = Object.keys(obj);
    obj_keys = sort ? keys.sort(sort) : keys;
  }
  let encoded_prefix = encodeDotInKeys ? String(prefix).replace(/\./g, "%2E") : String(prefix), adjusted_prefix = commaRoundTrip && isArray(obj) && obj.length === 1 ? encoded_prefix + "[]" : encoded_prefix;
  if (allowEmptyArrays && isArray(obj) && obj.length === 0)
    return adjusted_prefix + "[]";
  for (let j = 0; j < obj_keys.length; ++j) {
    let key = obj_keys[j], value = (
      // @ts-ignore
      typeof key == "object" && typeof key.value < "u" ? key.value : obj[key]
    );
    if (skipNulls && value === null)
      continue;
    let encoded_key = allowDots && encodeDotInKeys ? key.replace(/\./g, "%2E") : key, key_prefix = isArray(obj) ? typeof generateArrayPrefix == "function" ? generateArrayPrefix(adjusted_prefix, encoded_key) : adjusted_prefix : adjusted_prefix + (allowDots ? "." + encoded_key : "[" + encoded_key + "]");
    sideChannel.set(object, step);
    let valueSideChannel = /* @__PURE__ */ new WeakMap();
    valueSideChannel.set(sentinel, sideChannel), push_to_array(values, inner_stringify(
      value,
      key_prefix,
      generateArrayPrefix,
      commaRoundTrip,
      allowEmptyArrays,
      strictNullHandling,
      skipNulls,
      encodeDotInKeys,
      // @ts-ignore
      generateArrayPrefix === "comma" && encodeValuesOnly && isArray(obj) ? null : encoder2,
      filter2,
      sort,
      allowDots,
      serializeDate,
      format,
      formatter,
      encodeValuesOnly,
      charset,
      valueSideChannel
    ));
  }
  return values;
}
function normalize_stringify_options(opts = defaults2) {
  if (typeof opts.allowEmptyArrays < "u" && typeof opts.allowEmptyArrays != "boolean")
    throw new TypeError("`allowEmptyArrays` option can only be `true` or `false`, when provided");
  if (typeof opts.encodeDotInKeys < "u" && typeof opts.encodeDotInKeys != "boolean")
    throw new TypeError("`encodeDotInKeys` option can only be `true` or `false`, when provided");
  if (opts.encoder !== null && typeof opts.encoder < "u" && typeof opts.encoder != "function")
    throw new TypeError("Encoder has to be a function.");
  let charset = opts.charset || defaults2.charset;
  if (typeof opts.charset < "u" && opts.charset !== "utf-8" && opts.charset !== "iso-8859-1")
    throw new TypeError("The charset option must be either utf-8, iso-8859-1, or undefined");
  let format = default_format;
  if (typeof opts.format < "u") {
    if (!has(formatters, opts.format))
      throw new TypeError("Unknown format option provided.");
    format = opts.format;
  }
  let formatter = formatters[format], filter2 = defaults2.filter;
  (typeof opts.filter == "function" || isArray(opts.filter)) && (filter2 = opts.filter);
  let arrayFormat;
  if (opts.arrayFormat && opts.arrayFormat in array_prefix_generators ? arrayFormat = opts.arrayFormat : "indices" in opts ? arrayFormat = opts.indices ? "indices" : "repeat" : arrayFormat = defaults2.arrayFormat, "commaRoundTrip" in opts && typeof opts.commaRoundTrip != "boolean")
    throw new TypeError("`commaRoundTrip` must be a boolean, or absent");
  let allowDots = typeof opts.allowDots > "u" ? opts.encodeDotInKeys ? !0 : defaults2.allowDots : !!opts.allowDots;
  return {
    addQueryPrefix: typeof opts.addQueryPrefix == "boolean" ? opts.addQueryPrefix : defaults2.addQueryPrefix,
    // @ts-ignore
    allowDots,
    allowEmptyArrays: typeof opts.allowEmptyArrays == "boolean" ? !!opts.allowEmptyArrays : defaults2.allowEmptyArrays,
    arrayFormat,
    charset,
    charsetSentinel: typeof opts.charsetSentinel == "boolean" ? opts.charsetSentinel : defaults2.charsetSentinel,
    commaRoundTrip: !!opts.commaRoundTrip,
    delimiter: typeof opts.delimiter > "u" ? defaults2.delimiter : opts.delimiter,
    encode: typeof opts.encode == "boolean" ? opts.encode : defaults2.encode,
    encodeDotInKeys: typeof opts.encodeDotInKeys == "boolean" ? opts.encodeDotInKeys : defaults2.encodeDotInKeys,
    encoder: typeof opts.encoder == "function" ? opts.encoder : defaults2.encoder,
    encodeValuesOnly: typeof opts.encodeValuesOnly == "boolean" ? opts.encodeValuesOnly : defaults2.encodeValuesOnly,
    filter: filter2,
    format,
    formatter,
    serializeDate: typeof opts.serializeDate == "function" ? opts.serializeDate : defaults2.serializeDate,
    skipNulls: typeof opts.skipNulls == "boolean" ? opts.skipNulls : defaults2.skipNulls,
    // @ts-ignore
    sort: typeof opts.sort == "function" ? opts.sort : null,
    strictNullHandling: typeof opts.strictNullHandling == "boolean" ? opts.strictNullHandling : defaults2.strictNullHandling
  };
}
function stringify(object, opts = {}) {
  let obj = object, options = normalize_stringify_options(opts), obj_keys, filter2;
  typeof options.filter == "function" ? (filter2 = options.filter, obj = filter2("", obj)) : isArray(options.filter) && (filter2 = options.filter, obj_keys = filter2);
  let keys = [];
  if (typeof obj != "object" || obj === null)
    return "";
  let generateArrayPrefix = array_prefix_generators[options.arrayFormat], commaRoundTrip = generateArrayPrefix === "comma" && options.commaRoundTrip;
  obj_keys || (obj_keys = Object.keys(obj)), options.sort && obj_keys.sort(options.sort);
  let sideChannel = /* @__PURE__ */ new WeakMap();
  for (let i = 0; i < obj_keys.length; ++i) {
    let key = obj_keys[i];
    options.skipNulls && obj[key] === null || push_to_array(keys, inner_stringify(
      obj[key],
      key,
      // @ts-expect-error
      generateArrayPrefix,
      commaRoundTrip,
      options.allowEmptyArrays,
      options.strictNullHandling,
      options.skipNulls,
      options.encodeDotInKeys,
      options.encode ? options.encoder : null,
      options.filter,
      options.sort,
      options.allowDots,
      options.serializeDate,
      options.format,
      options.formatter,
      options.encodeValuesOnly,
      options.charset,
      sideChannel
    ));
  }
  let joined = keys.join(options.delimiter), prefix = options.addQueryPrefix === !0 ? "?" : "";
  return options.charsetSentinel && (options.charset === "iso-8859-1" ? prefix += "utf8=%26%2310003%3B&" : prefix += "utf8=%E2%9C%93&"), joined.length > 0 ? prefix + joined : "";
}
var array_prefix_generators, push_to_array, toISOString, defaults2, sentinel, init_stringify = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/qs/stringify.mjs"() {
    init_utils();
    init_formats();
    init_values();
    array_prefix_generators = {
      brackets(prefix) {
        return String(prefix) + "[]";
      },
      comma: "comma",
      indices(prefix, key) {
        return String(prefix) + "[" + key + "]";
      },
      repeat(prefix) {
        return String(prefix);
      }
    }, push_to_array = function(arr, value_or_array) {
      Array.prototype.push.apply(arr, isArray(value_or_array) ? value_or_array : [value_or_array]);
    }, defaults2 = {
      addQueryPrefix: !1,
      allowDots: !1,
      allowEmptyArrays: !1,
      arrayFormat: "indices",
      charset: "utf-8",
      charsetSentinel: !1,
      delimiter: "&",
      encode: !0,
      encodeDotInKeys: !1,
      encoder: encode,
      encodeValuesOnly: !1,
      format: default_format,
      formatter: default_formatter,
      /** @deprecated */
      indices: !1,
      serializeDate(date) {
        return (toISOString ?? (toISOString = Function.prototype.call.bind(Date.prototype.toISOString)))(date);
      },
      skipNulls: !1,
      strictNullHandling: !1
    };
    sentinel = {};
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/query.mjs
function stringifyQuery(query) {
  return stringify(query, { arrayFormat: "brackets" });
}
var init_query = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/query.mjs"() {
    init_stringify();
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/credentials/types.mjs
function requireSecureTokenEndpoint(baseURL) {
  if (!baseURL)
    return;
  let u;
  try {
    u = new URL(baseURL);
  } catch (err) {
    throw new WorkloadIdentityError(`Invalid token endpoint base URL "${baseURL}": ${err}`);
  }
  if (u.protocol === "https:")
    return;
  let host = u.hostname.toLowerCase().replace(/^\[|\]$/g, "");
  if (!(u.protocol === "http:" && (host === "localhost" || host === "127.0.0.1" || host === "::1")))
    throw new WorkloadIdentityError(`Refusing to send credential over non-https token endpoint "${baseURL}"`);
}
async function parseTokenResponse(resp, requestId) {
  let text = await readLimitedText(resp), data;
  try {
    data = JSON.parse(text);
  } catch {
    throw new WorkloadIdentityError(`Token endpoint returned non-JSON response (status ${resp.status})`, resp.status, redactSensitive(text), requestId);
  }
  if (!data.access_token)
    throw new WorkloadIdentityError(`Token endpoint response missing access_token: ${JSON.stringify(redactSensitive(data))}`, resp.status, redactSensitive(data), requestId);
  if (data.token_type && data.token_type.toLowerCase() !== "bearer")
    throw new WorkloadIdentityError(`Token endpoint response: unsupported token_type "${data.token_type}" (want Bearer)`, resp.status, redactSensitive(data), requestId);
  return data;
}
function redactSensitive(body) {
  if (body == null)
    return body;
  if (typeof body == "string") {
    let parsed;
    try {
      parsed = JSON.parse(body);
    } catch {
      return body.length <= MAX_ERROR_BODY_CHARS ? body : body.slice(0, MAX_ERROR_BODY_CHARS) + `... <${body.length - MAX_ERROR_BODY_CHARS} more chars>`;
    }
    return JSON.stringify(redactSensitive(parsed));
  }
  if (typeof body == "object" && !Array.isArray(body)) {
    let out = {};
    for (let [k, v] of Object.entries(body))
      SAFE_ERROR_KEYS.has(k) && (out[k] = v);
    return out;
  }
  return null;
}
async function checkCredentialsFileSafety(path12, onWarn = (m) => console.warn(`anthropic-sdk: ${m}`)) {
  if (typeof process > "u" || process.platform === "win32")
    return;
  let fs8 = await import("node:fs"), resolved = path12, st;
  try {
    resolved = await fs8.promises.realpath(path12), st = await fs8.promises.stat(resolved);
  } catch {
    return;
  }
  let mode = st.mode & 511;
  if (mode & 18)
    throw new WorkloadIdentityError(`Credentials file at ${resolved} is group/world-writable (mode 0o${mode.toString(8)}); this allows other local users to plant tokens. Run \`chmod 600 ${resolved}\`.`);
  if (mode & 36)
    throw new WorkloadIdentityError(`Credentials file at ${resolved} is group/world-readable (mode 0o${mode.toString(8)}); run \`chmod 600 ${resolved}\` before retrying.`);
  typeof process.getuid == "function" && st.uid !== process.getuid() && onWarn(`credentials file at ${resolved} is owned by uid ${st.uid} (current process uid ${process.getuid()}); verify this is intentional.`);
}
async function writeCredentialsFileAtomic(targetPath, data) {
  let fs8 = await import("node:fs"), dir = (await import("node:path")).dirname(targetPath);
  await fs8.promises.mkdir(dir, { recursive: !0, mode: 448 });
  let tmpPath = `${targetPath}.${process.pid}.${Math.random().toString(36).slice(2)}.tmp`;
  try {
    let fh = await fs8.promises.open(tmpPath, "w", 384);
    try {
      await fh.writeFile(JSON.stringify(data, null, 2)), await fh.sync();
    } finally {
      await fh.close();
    }
    await fs8.promises.rename(tmpPath, targetPath);
  } catch (err) {
    throw await fs8.promises.unlink(tmpPath).catch(() => {
    }), err;
  }
  try {
    let dirFh = await fs8.promises.open(dir, "r");
    try {
      await dirFh.sync();
    } finally {
      await dirFh.close();
    }
  } catch {
  }
}
async function readLimitedText(resp) {
  if (!resp.body)
    return "";
  let reader = resp.body.getReader(), chunks = [], received = 0;
  for (; ; ) {
    let { done, value } = await reader.read();
    if (done)
      break;
    if (received + value.length > MAX_TOKEN_RESPONSE_BYTES) {
      let remaining = MAX_TOKEN_RESPONSE_BYTES - received;
      remaining > 0 && chunks.push(value.subarray(0, remaining)), await reader.cancel();
      break;
    }
    chunks.push(value), received += value.length;
  }
  let merged;
  if (chunks.length === 1)
    merged = chunks[0];
  else {
    merged = new Uint8Array(chunks.reduce((n, c) => n + c.length, 0));
    let offset = 0;
    for (let c of chunks)
      merged.set(c, offset), offset += c.length;
  }
  return new TextDecoder("utf-8").decode(merged);
}
var GRANT_TYPE_JWT_BEARER, GRANT_TYPE_REFRESH_TOKEN, TOKEN_ENDPOINT, OAUTH_API_BETA_HEADER, FEDERATION_BETA_HEADER, ADVISORY_REFRESH_THRESHOLD_IN_SECONDS, MANDATORY_REFRESH_THRESHOLD_IN_SECONDS, ADVISORY_REFRESH_BACKOFF_IN_SECONDS, MAX_TOKEN_RESPONSE_BYTES, MAX_ERROR_BODY_CHARS, SAFE_ERROR_KEYS, WorkloadIdentityError, init_types2 = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/credentials/types.mjs"() {
    init_error();
    GRANT_TYPE_JWT_BEARER = "urn:ietf:params:oauth:grant-type:jwt-bearer", GRANT_TYPE_REFRESH_TOKEN = "refresh_token", TOKEN_ENDPOINT = "/v1/oauth/token", OAUTH_API_BETA_HEADER = "oauth-2025-04-20", FEDERATION_BETA_HEADER = "oidc-federation-2026-04-01", ADVISORY_REFRESH_THRESHOLD_IN_SECONDS = 120, MANDATORY_REFRESH_THRESHOLD_IN_SECONDS = 30, ADVISORY_REFRESH_BACKOFF_IN_SECONDS = 5, MAX_TOKEN_RESPONSE_BYTES = 1 << 20;
    MAX_ERROR_BODY_CHARS = 2e3, SAFE_ERROR_KEYS = /* @__PURE__ */ new Set(["error", "error_description", "error_uri"]);
    WorkloadIdentityError = class extends AnthropicError {
      constructor(message, statusCode = null, body = null, requestId = null) {
        super(message), this.statusCode = statusCode, this.body = body, this.requestId = requestId;
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/time.mjs
function nowAsSeconds() {
  return Math.floor(Date.now() / 1e3);
}
var init_time = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/time.mjs"() {
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/credentials/token-cache.mjs
var TokenCache, init_token_cache = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/credentials/token-cache.mjs"() {
    init_types2();
    init_time();
    TokenCache = class {
      constructor(provider, onAdvisoryRefreshError) {
        this.cached = null, this.pendingRefresh = null, this.nextForce = !1, this.lastAdvisoryError = 0, this.provider = provider, this.onAdvisoryRefreshError = onAdvisoryRefreshError;
      }
      async getToken() {
        let force = this.nextForce;
        this.nextForce = !1;
        let cached = this.cached;
        if (force || cached == null)
          return (await this.refresh(force)).token;
        if (cached.expiresAt == null)
          return cached.token;
        let remaining = cached.expiresAt - nowAsSeconds();
        return remaining > ADVISORY_REFRESH_THRESHOLD_IN_SECONDS ? cached.token : remaining > MANDATORY_REFRESH_THRESHOLD_IN_SECONDS ? (this.backgroundRefresh(), cached.token) : (await this.refresh()).token;
      }
      /**
       * Clears the cached token and marks the next {@link getToken} as a forced
       * refresh, so the underlying provider bypasses any on-disk freshness check.
       * Called after a 401 — the server has just told us the token is bad even
       * if its `expires_at` still looks fresh.
       */
      invalidate() {
        this.cached = null, this.nextForce = !0;
      }
      /**
       * Mandatory refresh. Joins any in-flight refresh unless forced — a forced
       * refresh must not coalesce into a non-forced one that may re-serve the
       * same stale disk token.
       */
      refresh(force = !1) {
        return this.pendingRefresh && !force ? this.pendingRefresh : this.doRefresh(force);
      }
      /**
       * Advisory background refresh. Shares the same in-flight promise as
       * mandatory refreshes for deduplication, but swallows errors so the
       * stale cached token keeps being served. Backs off for
       * {@link ADVISORY_REFRESH_BACKOFF_IN_SECONDS} after a failure so an
       * outage during the advisory window doesn't hammer the token endpoint.
       */
      backgroundRefresh() {
        this.pendingRefresh || nowAsSeconds() - this.lastAdvisoryError < ADVISORY_REFRESH_BACKOFF_IN_SECONDS || this.doRefresh().catch((err) => {
          this.lastAdvisoryError = nowAsSeconds(), this.onAdvisoryRefreshError?.(err);
        });
      }
      /**
       * Core refresh. Sets {@link pendingRefresh} so concurrent callers
       * (both advisory and mandatory) coalesce into a single provider call.
       */
      doRefresh(force = !1) {
        return this.pendingRefresh = this.provider(force ? { forceRefresh: !0 } : void 0).then((token) => (this.cached = token, this.pendingRefresh = null, token), (err) => {
          throw this.pendingRefresh = null, err;
        }), this.pendingRefresh;
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/env.mjs
var readEnv, init_env = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/env.mjs"() {
    readEnv = (env) => {
      if (typeof globalThis.process < "u")
        return globalThis.process.env?.[env]?.trim() || void 0;
      if (typeof globalThis.Deno < "u")
        return globalThis.Deno.env?.get?.(env)?.trim() || void 0;
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/bytes.mjs
function concatBytes(buffers) {
  let length = 0;
  for (let buffer of buffers)
    length += buffer.length;
  let output = new Uint8Array(length), index = 0;
  for (let buffer of buffers)
    output.set(buffer, index), index += buffer.length;
  return output;
}
function encodeUTF8(str2) {
  let encoder2;
  return (encodeUTF8_ ?? (encoder2 = new globalThis.TextEncoder(), encodeUTF8_ = encoder2.encode.bind(encoder2)))(str2);
}
function decodeUTF8(bytes) {
  let decoder;
  return (decodeUTF8_ ?? (decoder = new globalThis.TextDecoder(), decodeUTF8_ = decoder.decode.bind(decoder)))(bytes);
}
var encodeUTF8_, decodeUTF8_, init_bytes = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/bytes.mjs"() {
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/base64.mjs
var init_base64 = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/base64.mjs"() {
    init_error();
    init_bytes();
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/log.mjs
function noop() {
}
function makeLogFn(fnLevel, logger, logLevel) {
  return !logger || levelNumbers[fnLevel] > levelNumbers[logLevel] ? noop : logger[fnLevel].bind(logger);
}
function filterLogger(logger, logLevel) {
  let cachedLogger = cachedLoggers.get(logger);
  if (cachedLogger && cachedLogger[0] === logLevel)
    return cachedLogger[1];
  let levelLogger = {
    error: makeLogFn("error", logger, logLevel),
    warn: makeLogFn("warn", logger, logLevel),
    info: makeLogFn("info", logger, logLevel),
    debug: makeLogFn("debug", logger, logLevel)
  };
  return cachedLoggers.set(logger, [logLevel, levelLogger]), levelLogger;
}
function loggerFor(client) {
  let logger = client.logger, logLevel = client.logLevel ?? "off";
  return logger ? filterLogger(logger, logLevel) : noopLogger;
}
function defaultLogger() {
  let envLevel = readEnv("ANTHROPIC_LOG");
  return (!cachedDefaultLogger || envLevel !== lastEnvLevel) && (lastEnvLevel = envLevel, cachedDefaultLogger = filterLogger(console, parseLogLevel(envLevel, "process.env['ANTHROPIC_LOG']", filterLogger(console, defaultLogLevel)) ?? defaultLogLevel)), cachedDefaultLogger;
}
var defaultLogLevel, levelNumbers, parseLogLevel, noopLogger, cachedLoggers, lastEnvLevel, cachedDefaultLogger, formatRequestDetails, init_log = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/log.mjs"() {
    init_values();
    init_env();
    defaultLogLevel = "warn", levelNumbers = {
      off: 0,
      error: 200,
      warn: 300,
      info: 400,
      debug: 500
    }, parseLogLevel = (maybeLevel, sourceName, logger) => {
      if (maybeLevel) {
        if (hasOwn(levelNumbers, maybeLevel))
          return maybeLevel;
        logger.warn(`${sourceName} was set to ${JSON.stringify(maybeLevel)}, expected one of ${JSON.stringify(Object.keys(levelNumbers))}`);
      }
    };
    noopLogger = {
      error: noop,
      warn: noop,
      info: noop,
      debug: noop
    }, cachedLoggers = /* @__PURE__ */ new WeakMap();
    formatRequestDetails = (details) => (details.options && (details.options = { ...details.options }, delete details.options.headers), details.headers && (details.headers = Object.fromEntries((details.headers instanceof Headers ? [...details.headers] : Object.entries(details.headers)).map(([name, value]) => [
      name,
      name.toLowerCase() === "authorization" || name.toLowerCase() === "api-key" || name.toLowerCase() === "x-api-key" || name.toLowerCase() === "cookie" || name.toLowerCase() === "set-cookie" ? "***" : value
    ]))), "retryOfRequestLogID" in details && (details.retryOfRequestLogID && (details.retryOf = details.retryOfRequestLogID), delete details.retryOfRequestLogID), details);
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils.mjs
var init_utils2 = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils.mjs"() {
    init_values();
    init_base64();
    init_env();
    init_log();
    init_uuid();
    init_sleep();
    init_query();
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/core/credentials.mjs
function validateProfileName(name) {
  if (!name)
    throw new Error("profile name is empty");
  if (name === "." || name === "..")
    throw new Error(`profile name "${name}" is not allowed`);
  if (name.includes("/") || name.includes("\\"))
    throw new Error(`profile name "${name}" must not contain path separators`);
  if (!PROFILE_NAME_PATTERN.test(name))
    throw new Error(`profile name "${name}" contains disallowed characters (allowed: letters, digits, '_', '.', '-')`);
}
var CREDENTIALS_FILE_VERSION, PROFILE_NAME_PATTERN, loadConfigWithSource, getCredentialsPath, getRootConfigPath, supportsLocalConfigFiles, getActiveProfileName, init_credentials = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/core/credentials.mjs"() {
    init_detect_platform();
    init_utils2();
    CREDENTIALS_FILE_VERSION = "1.0", PROFILE_NAME_PATTERN = /^[A-Za-z0-9_.-]+$/;
    loadConfigWithSource = async (profile) => {
      var _a5, _b;
      let rootConfigPath = await getRootConfigPath();
      if (rootConfigPath === null)
        return null;
      let profileName = profile ?? await getActiveProfileName();
      if (profileName === null)
        return null;
      validateProfileName(profileName);
      let fs8 = await import("node:fs"), configPath2 = (await import("node:path")).join(rootConfigPath, "configs", `${profileName}.json`), configRaw;
      try {
        configRaw = await fs8.promises.readFile(configPath2, "utf-8");
      } catch (err) {
        if (err?.code !== "ENOENT")
          throw new Error(`failed to read config file ${configPath2}: ${err}`);
        configRaw = null;
      }
      if (configRaw === null) {
        let organizationId = readEnv("ANTHROPIC_ORGANIZATION_ID"), identityTokenFile = readEnv("ANTHROPIC_IDENTITY_TOKEN_FILE"), federationRuleId = readEnv("ANTHROPIC_FEDERATION_RULE_ID");
        return federationRuleId && organizationId ? {
          fromFile: !1,
          config: {
            organization_id: organizationId,
            // A defaulted-but-empty CI variable (`ANTHROPIC_WORKSPACE_ID=""`) is
            // treated as unset — readEnv coerces empty to undefined, and the body
            // builder's truthy check skips it — so `"workspace_id": ""` never goes
            // on the wire.
            workspace_id: readEnv("ANTHROPIC_WORKSPACE_ID"),
            base_url: readEnv("ANTHROPIC_BASE_URL"),
            authentication: {
              type: "oidc_federation",
              federation_rule_id: federationRuleId,
              service_account_id: readEnv("ANTHROPIC_SERVICE_ACCOUNT_ID"),
              identity_token: identityTokenFile ? { source: "file", path: identityTokenFile } : void 0,
              scope: readEnv("ANTHROPIC_SCOPE")
            }
          }
        } : null;
      }
      let config;
      try {
        config = JSON.parse(configRaw);
      } catch (err) {
        throw new Error(`failed to parse config file ${configPath2}: ${err}`);
      }
      if (!config.authentication)
        throw new Error(`config file ${configPath2} is missing "authentication"`);
      let authType = config.authentication.type;
      if (authType !== "oidc_federation" && authType !== "user_oauth")
        throw new Error(`authentication.type "${authType}" is not a known authentication type`);
      if (config.organization_id ?? (config.organization_id = readEnv("ANTHROPIC_ORGANIZATION_ID")), config.workspace_id ?? (config.workspace_id = readEnv("ANTHROPIC_WORKSPACE_ID")), config.base_url ?? (config.base_url = readEnv("ANTHROPIC_BASE_URL")), (_a5 = config.authentication).scope ?? (_a5.scope = readEnv("ANTHROPIC_SCOPE")), config.authentication.type === "oidc_federation") {
        if (!config.authentication.identity_token) {
          let identityTokenFile = readEnv("ANTHROPIC_IDENTITY_TOKEN_FILE");
          identityTokenFile && (config.authentication.identity_token = {
            source: "file",
            path: identityTokenFile
          });
        }
        config.authentication.federation_rule_id || (config.authentication.federation_rule_id = readEnv("ANTHROPIC_FEDERATION_RULE_ID") ?? ""), (_b = config.authentication).service_account_id ?? (_b.service_account_id = readEnv("ANTHROPIC_SERVICE_ACCOUNT_ID"));
      }
      return { config, fromFile: !0 };
    }, getCredentialsPath = async (config, profile) => {
      if (config?.authentication.credentials_path)
        return config.authentication.credentials_path;
      let rootConfigPath = await getRootConfigPath();
      if (!rootConfigPath)
        return null;
      let profileName = profile ?? await getActiveProfileName();
      return profileName ? (validateProfileName(profileName), (await import("node:path")).join(rootConfigPath, "credentials", `${profileName}.json`)) : null;
    }, getRootConfigPath = async () => {
      if (!supportsLocalConfigFiles())
        return null;
      let path12 = await import("node:path"), configDir = readEnv("ANTHROPIC_CONFIG_DIR");
      if (configDir)
        return configDir;
      if (getPlatformHeaders()["X-Stainless-OS"] === "Windows") {
        let appData = readEnv("APPDATA");
        if (appData)
          return path12.join(appData, "Anthropic");
        let userProfile = readEnv("USERPROFILE");
        return userProfile ? path12.join(userProfile, "AppData", "Roaming", "Anthropic") : null;
      }
      let xdgConfigHome = readEnv("XDG_CONFIG_HOME");
      if (xdgConfigHome)
        return path12.join(xdgConfigHome, "anthropic");
      let home = readEnv("HOME");
      return home ? path12.join(home, ".config", "anthropic") : null;
    }, supportsLocalConfigFiles = () => {
      let runtime = getPlatformHeaders()["X-Stainless-Runtime"];
      return runtime === "node" || runtime === "deno";
    }, getActiveProfileName = async () => {
      let rootConfigPath = await getRootConfigPath();
      if (!rootConfigPath)
        return null;
      let profileName = readEnv("ANTHROPIC_PROFILE");
      if (profileName)
        return profileName;
      let fs8 = await import("node:fs"), filePath = (await import("node:path")).join(rootConfigPath, "active_config");
      try {
        return (await fs8.promises.readFile(filePath, "utf-8")).trim() || "default";
      } catch (err) {
        if (err?.code !== "ENOENT")
          throw new Error(`failed to read ${filePath}: ${err}`);
        return "default";
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/credentials/identity-token.mjs
function identityTokenFromFile(path12) {
  if (!path12)
    throw new AnthropicError("Identity token file path is empty");
  return async () => {
    let fs8 = await import("node:fs"), content;
    try {
      content = await fs8.promises.readFile(path12, "utf-8");
    } catch (err) {
      throw new AnthropicError(`Failed to read identity token file at ${path12}: ${err}`);
    }
    let token = content.trim();
    if (!token)
      throw new AnthropicError(`Identity token file at ${path12} is empty`);
    return token;
  };
}
function identityTokenFromValue(token) {
  if (!token)
    throw new AnthropicError("Identity token value is empty");
  return () => token;
}
var init_identity_token = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/credentials/identity-token.mjs"() {
    init_error();
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/credentials/oidc-federation.mjs
function oidcFederationProvider(config) {
  return async () => {
    requireSecureTokenEndpoint(config.baseURL);
    let jwt = await config.identityTokenProvider();
    if (jwt.length > 16 * 1024)
      throw new WorkloadIdentityError(`Identity token is ${Math.ceil(jwt.length / 1024)} KiB, exceeds the 16 KiB assertion limit`);
    let body = {
      grant_type: GRANT_TYPE_JWT_BEARER,
      assertion: jwt,
      federation_rule_id: config.federationRuleId,
      organization_id: config.organizationId
    };
    config.serviceAccountId && (body.service_account_id = config.serviceAccountId), config.workspaceId && (body.workspace_id = config.workspaceId);
    let url = `${config.baseURL}${TOKEN_ENDPOINT}`, resp;
    try {
      resp = await config.fetch(url, {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          "anthropic-beta": `${OAUTH_API_BETA_HEADER},${FEDERATION_BETA_HEADER}`,
          "User-Agent": config.userAgent || `anthropic-sdk-typescript/${VERSION} oidcFederationProvider`
        },
        body: JSON.stringify(body)
      });
    } catch (err) {
      throw new WorkloadIdentityError(`Failed to reach token endpoint ${url}: ${err}`);
    }
    let requestId = resp.headers.get("Request-Id");
    if (!resp.ok) {
      let text = await resp.text().catch(() => ""), redacted = redactSensitive(text), hint = "";
      throw resp.status === 401 && (hint = ` Ensure your federation rule matches your identity token. ${config.workspaceId ? "" : "If your federation rule is scoped to multiple workspaces, set the ANTHROPIC_WORKSPACE_ID environment variable, the 'workspace_id' config key, or the `workspaceId` option. "}View your authentication events in the Workload identity page of Claude Console for more details.`), new WorkloadIdentityError(`Token exchange failed with status ${resp.status}${requestId ? ` (request-id ${requestId})` : ""}: ${redacted}${hint}`, resp.status, redacted, requestId);
    }
    let data = await parseTokenResponse(resp, requestId), expiresIn = Number(data.expires_in);
    if (!Number.isFinite(expiresIn))
      throw new WorkloadIdentityError(`Token endpoint response missing required fields: ${JSON.stringify(redactSensitive(data))}`, resp.status, redactSensitive(data), requestId);
    return {
      token: data.access_token,
      expiresAt: nowAsSeconds() + expiresIn
    };
  };
}
var init_oidc_federation = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/credentials/oidc-federation.mjs"() {
    init_types2();
    init_time();
    init_version();
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/credentials/user-oauth.mjs
function userOAuthProvider(config) {
  return async (opts) => {
    let fs8 = await import("node:fs");
    await checkCredentialsFileSafety(config.credentialsPath, config.onSafetyWarning);
    let raw;
    try {
      raw = await fs8.promises.readFile(config.credentialsPath, "utf-8");
    } catch (err) {
      throw new WorkloadIdentityError(`Credentials file not found at ${config.credentialsPath}: ${err}`);
    }
    let creds;
    try {
      creds = JSON.parse(raw);
    } catch (err) {
      throw new WorkloadIdentityError(`Credentials file at ${config.credentialsPath} is not valid JSON: ${err}`);
    }
    let accessToken = creds.access_token;
    if (!accessToken)
      throw new WorkloadIdentityError(`Credentials file at ${config.credentialsPath} must include 'access_token'`);
    let expiresAt = creds.expires_at;
    if (!opts?.forceRefresh && (expiresAt == null || nowAsSeconds() < expiresAt - MANDATORY_REFRESH_THRESHOLD_IN_SECONDS))
      return { token: accessToken, expiresAt: expiresAt ?? null };
    let refreshToken = creds.refresh_token;
    if (!config.clientId || !refreshToken)
      throw new WorkloadIdentityError(`Access token at ${config.credentialsPath} has expired and no refresh is available (client_id ${config.clientId ? "set" : "empty"}, refresh_token ${refreshToken ? "set" : "empty"})`);
    requireSecureTokenEndpoint(config.baseURL);
    let body = {
      grant_type: GRANT_TYPE_REFRESH_TOKEN,
      refresh_token: refreshToken,
      client_id: config.clientId
    }, url = `${config.baseURL}${TOKEN_ENDPOINT}`, resp;
    try {
      resp = await config.fetch(url, {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          "anthropic-beta": OAUTH_API_BETA_HEADER,
          "User-Agent": config.userAgent || `anthropic-sdk-typescript/${VERSION} userOAuthProvider`
        },
        body: JSON.stringify(body)
      });
    } catch (err) {
      throw new WorkloadIdentityError(`User OAuth refresh failed to reach token endpoint: ${err}`);
    }
    let requestId = resp.headers.get("Request-Id");
    if (!resp.ok) {
      let text = await resp.text().catch(() => "");
      throw new WorkloadIdentityError(`User OAuth refresh failed (HTTP ${resp.status}): ${redactSensitive(text)}`, resp.status, redactSensitive(text), requestId);
    }
    let data = await parseTokenResponse(resp, requestId), expiresIn = Number(data.expires_in);
    if (!Number.isFinite(expiresIn))
      throw new WorkloadIdentityError(`User OAuth refresh response missing or invalid expires_in: ${JSON.stringify(redactSensitive(data))}`, resp.status, redactSensitive(data), requestId);
    let newExpiresAt = nowAsSeconds() + expiresIn, newRefreshToken = data.refresh_token || refreshToken;
    return await writeCredentialsFileAtomic(config.credentialsPath, {
      ...creds,
      version: CREDENTIALS_FILE_VERSION,
      type: "oauth_token",
      access_token: data.access_token,
      expires_at: newExpiresAt,
      refresh_token: newRefreshToken
    }), { token: data.access_token, expiresAt: newExpiresAt };
  };
}
var init_user_oauth = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/credentials/user-oauth.mjs"() {
    init_credentials();
    init_types2();
    init_time();
    init_version();
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/credentials/credential-chain.mjs
function resolveCredentialsFromConfig(config, options) {
  let credentialsPath = config.authentication.credentials_path ?? null, effectiveBaseURL = (config.base_url || options.baseURL).replace(/\/+$/, ""), provider = buildProvider(config, credentialsPath, effectiveBaseURL, options), extraHeaders = {};
  return config.workspace_id && config.authentication.type === "user_oauth" && (extraHeaders["anthropic-workspace-id"] = config.workspace_id), { provider, extraHeaders, baseURL: config.base_url || void 0 };
}
async function defaultCredentials(options, profile) {
  let loaded = await loadConfigWithSource(profile);
  if (!loaded)
    return null;
  let { config, fromFile } = loaded, withPath = config.authentication.credentials_path || !fromFile ? config : {
    ...config,
    authentication: {
      ...config.authentication,
      credentials_path: await getCredentialsPath(config, profile) ?? void 0
    }
  };
  return resolveCredentialsFromConfig(withPath, options);
}
function buildProvider(config, credentialsPath, baseURL, options) {
  switch (config.authentication.type) {
    case "oidc_federation": {
      let auth = config.authentication, identityProvider = resolveIdentityTokenProvider(auth);
      if (!identityProvider)
        throw new WorkloadIdentityError("oidc_federation config requires an identity token (set authentication.identity_token, ANTHROPIC_IDENTITY_TOKEN_FILE, or ANTHROPIC_IDENTITY_TOKEN)");
      if (!auth.federation_rule_id)
        throw new WorkloadIdentityError("oidc_federation config requires 'federation_rule_id'. Set it in authentication.federation_rule_id in your profile, or via ANTHROPIC_FEDERATION_RULE_ID (profile takes precedence).");
      if (!config.organization_id)
        throw new WorkloadIdentityError("oidc_federation config requires organization_id (set ANTHROPIC_ORGANIZATION_ID or config.organization_id)");
      let exchange = oidcFederationProvider({
        identityTokenProvider: identityProvider,
        federationRuleId: auth.federation_rule_id,
        organizationId: config.organization_id,
        serviceAccountId: auth.service_account_id,
        workspaceId: config.workspace_id,
        baseURL,
        fetch: options.fetch,
        userAgent: options.userAgent
      });
      return credentialsPath ? cachedExchangeProvider(exchange, credentialsPath, options.onCacheWriteError, options.onSafetyWarning) : exchange;
    }
    case "user_oauth": {
      if (!credentialsPath)
        throw new WorkloadIdentityError("user_oauth config requires authentication.credentials_path (or load via a profile so it defaults to <config_dir>/credentials/<profile>.json)");
      return userOAuthProvider({
        credentialsPath,
        clientId: config.authentication.client_id,
        baseURL,
        fetch: options.fetch,
        userAgent: options.userAgent,
        onSafetyWarning: options.onSafetyWarning
      });
    }
    default: {
      let t = config.authentication.type;
      throw new WorkloadIdentityError(`authentication.type "${t}" is not a known authentication type`);
    }
  }
}
function resolveIdentityTokenProvider(auth) {
  if (auth.identity_token) {
    let source = auth.identity_token.source;
    if (source !== "file")
      throw new WorkloadIdentityError(`identity_token.source "${source}" is not supported by this SDK version (only "file")`);
    if (!auth.identity_token.path)
      throw new WorkloadIdentityError('identity_token.source "file" requires a non-empty path');
    return identityTokenFromFile(auth.identity_token.path);
  }
  let tokenFile = readEnv("ANTHROPIC_IDENTITY_TOKEN_FILE");
  if (tokenFile)
    return identityTokenFromFile(tokenFile);
  let tokenValue = readEnv("ANTHROPIC_IDENTITY_TOKEN");
  return tokenValue ? identityTokenFromValue(tokenValue) : null;
}
function cachedExchangeProvider(exchange, credentialsPath, onCacheWriteError, onSafetyWarning) {
  return async (opts) => {
    let fs8 = await import("node:fs");
    await checkCredentialsFileSafety(credentialsPath, onSafetyWarning);
    let existing;
    try {
      let raw = await fs8.promises.readFile(credentialsPath, "utf-8");
      existing = JSON.parse(raw);
      let token = existing?.access_token;
      if (token && !opts?.forceRefresh) {
        let expiresAt = existing?.expires_at;
        if (expiresAt == null || nowAsSeconds() < expiresAt - MANDATORY_REFRESH_THRESHOLD_IN_SECONDS)
          return { token, expiresAt: expiresAt ?? null };
      }
    } catch (err) {
      err?.code !== "ENOENT" && !(err instanceof SyntaxError) && onCacheWriteError?.(err);
    }
    let result = await exchange(opts);
    try {
      await writeCredentialsFileAtomic(credentialsPath, {
        ...existing ?? {},
        version: CREDENTIALS_FILE_VERSION,
        type: "oauth_token",
        access_token: result.token,
        expires_at: result.expiresAt
      });
    } catch (err) {
      onCacheWriteError?.(err);
    }
    return result;
  };
}
var init_credential_chain = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/credentials/credential-chain.mjs"() {
    init_env();
    init_credentials();
    init_types2();
    init_time();
    init_identity_token();
    init_oidc_federation();
    init_user_oauth();
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/decoders/line.mjs
function findNewlineIndex(buffer, startIndex) {
  for (let i = startIndex ?? 0; i < buffer.length; i++) {
    if (buffer[i] === 10)
      return { preceding: i, index: i + 1, carriage: !1 };
    if (buffer[i] === 13)
      return { preceding: i, index: i + 1, carriage: !0 };
  }
  return null;
}
function findDoubleNewlineIndex(buffer) {
  for (let i = 0; i < buffer.length - 1; i++) {
    if (buffer[i] === 10 && buffer[i + 1] === 10 || buffer[i] === 13 && buffer[i + 1] === 13)
      return i + 2;
    if (buffer[i] === 13 && buffer[i + 1] === 10 && i + 3 < buffer.length && buffer[i + 2] === 13 && buffer[i + 3] === 10)
      return i + 4;
  }
  return -1;
}
var _LineDecoder_buffer, _LineDecoder_carriageReturnIndex, LineDecoder, init_line = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/decoders/line.mjs"() {
    init_tslib();
    init_bytes();
    LineDecoder = class {
      constructor() {
        _LineDecoder_buffer.set(this, void 0), _LineDecoder_carriageReturnIndex.set(this, void 0), __classPrivateFieldSet(this, _LineDecoder_buffer, new Uint8Array(), "f"), __classPrivateFieldSet(this, _LineDecoder_carriageReturnIndex, null, "f");
      }
      decode(chunk) {
        if (chunk == null)
          return [];
        let binaryChunk = chunk instanceof ArrayBuffer ? new Uint8Array(chunk) : typeof chunk == "string" ? encodeUTF8(chunk) : chunk;
        __classPrivateFieldSet(this, _LineDecoder_buffer, concatBytes([__classPrivateFieldGet(this, _LineDecoder_buffer, "f"), binaryChunk]), "f");
        let lines = [], patternIndex;
        for (; (patternIndex = findNewlineIndex(__classPrivateFieldGet(this, _LineDecoder_buffer, "f"), __classPrivateFieldGet(this, _LineDecoder_carriageReturnIndex, "f"))) != null; ) {
          if (patternIndex.carriage && __classPrivateFieldGet(this, _LineDecoder_carriageReturnIndex, "f") == null) {
            __classPrivateFieldSet(this, _LineDecoder_carriageReturnIndex, patternIndex.index, "f");
            continue;
          }
          if (__classPrivateFieldGet(this, _LineDecoder_carriageReturnIndex, "f") != null && (patternIndex.index !== __classPrivateFieldGet(this, _LineDecoder_carriageReturnIndex, "f") + 1 || patternIndex.carriage)) {
            lines.push(decodeUTF8(__classPrivateFieldGet(this, _LineDecoder_buffer, "f").subarray(0, __classPrivateFieldGet(this, _LineDecoder_carriageReturnIndex, "f") - 1))), __classPrivateFieldSet(this, _LineDecoder_buffer, __classPrivateFieldGet(this, _LineDecoder_buffer, "f").subarray(__classPrivateFieldGet(this, _LineDecoder_carriageReturnIndex, "f")), "f"), __classPrivateFieldSet(this, _LineDecoder_carriageReturnIndex, null, "f");
            continue;
          }
          let endIndex = __classPrivateFieldGet(this, _LineDecoder_carriageReturnIndex, "f") !== null ? patternIndex.preceding - 1 : patternIndex.preceding, line = decodeUTF8(__classPrivateFieldGet(this, _LineDecoder_buffer, "f").subarray(0, endIndex));
          lines.push(line), __classPrivateFieldSet(this, _LineDecoder_buffer, __classPrivateFieldGet(this, _LineDecoder_buffer, "f").subarray(patternIndex.index), "f"), __classPrivateFieldSet(this, _LineDecoder_carriageReturnIndex, null, "f");
        }
        return lines;
      }
      flush() {
        return __classPrivateFieldGet(this, _LineDecoder_buffer, "f").length ? this.decode(`
`) : [];
      }
    };
    _LineDecoder_buffer = /* @__PURE__ */ new WeakMap(), _LineDecoder_carriageReturnIndex = /* @__PURE__ */ new WeakMap();
    LineDecoder.NEWLINE_CHARS = /* @__PURE__ */ new Set([`
`, "\r"]);
    LineDecoder.NEWLINE_REGEXP = /\r\n|[\n\r]/g;
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/core/streaming.mjs
async function* _iterSSEMessages(response, controller) {
  if (!response.body)
    throw controller.abort(), typeof globalThis.navigator < "u" && globalThis.navigator.product === "ReactNative" ? new AnthropicError("The default react-native fetch implementation does not support streaming. Please use expo/fetch: https://docs.expo.dev/versions/latest/sdk/expo/#expofetch-api") : new AnthropicError("Attempted to iterate over a response with no body");
  let sseDecoder = new SSEDecoder(), lineDecoder = new LineDecoder(), iter = ReadableStreamToAsyncIterable(response.body);
  for await (let sseChunk of iterSSEChunks(iter))
    for (let line of lineDecoder.decode(sseChunk)) {
      let sse = sseDecoder.decode(line);
      sse && (yield sse);
    }
  for (let line of lineDecoder.flush()) {
    let sse = sseDecoder.decode(line);
    sse && (yield sse);
  }
}
async function* iterSSEChunks(iterator) {
  let data = new Uint8Array();
  for await (let chunk of iterator) {
    if (chunk == null)
      continue;
    let binaryChunk = chunk instanceof ArrayBuffer ? new Uint8Array(chunk) : typeof chunk == "string" ? encodeUTF8(chunk) : chunk, newData = new Uint8Array(data.length + binaryChunk.length);
    newData.set(data), newData.set(binaryChunk, data.length), data = newData;
    let patternIndex;
    for (; (patternIndex = findDoubleNewlineIndex(data)) !== -1; )
      yield data.slice(0, patternIndex), data = data.slice(patternIndex);
  }
  data.length > 0 && (yield data);
}
function partition(str2, delimiter2) {
  let index = str2.indexOf(delimiter2);
  return index !== -1 ? [str2.substring(0, index), delimiter2, str2.substring(index + delimiter2.length)] : [str2, "", ""];
}
var _Stream_client, Stream, SSEDecoder, init_streaming = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/core/streaming.mjs"() {
    init_tslib();
    init_error();
    init_shims();
    init_line();
    init_shims();
    init_errors2();
    init_values();
    init_bytes();
    init_log();
    init_error();
    Stream = class _Stream {
      constructor(iterator, controller, client) {
        this.iterator = iterator, _Stream_client.set(this, void 0), this.controller = controller, __classPrivateFieldSet(this, _Stream_client, client, "f");
      }
      /**
       * Iterate the raw Server-Sent Events from `response` — `{event, data, raw}`
       * objects, before any JSON parsing or event-name filtering.
       *
       * This reads `response.body` directly (not a clone), so the response is
       * consumed. Use this in middleware that fully replaces the stream body; for
       * read-only observation of parsed events, use `ctx.parse()` instead.
       */
      static rawEvents(response, controller = new AbortController()) {
        return _iterSSEMessages(response, controller);
      }
      static fromSSEResponse(response, controller, client) {
        let consumed = !1, logger = client ? loggerFor(client) : console;
        async function* iterator() {
          if (consumed)
            throw new AnthropicError("Cannot iterate over a consumed stream, use `.tee()` to split the stream.");
          consumed = !0;
          let done = !1;
          try {
            for await (let sse of _iterSSEMessages(response, controller)) {
              if (sse.event === "completion")
                try {
                  yield JSON.parse(sse.data);
                } catch (e) {
                  throw logger.error("Could not parse message into JSON:", sse.data), logger.error("From chunk:", sse.raw), e;
                }
              if (sse.event === "message_start" || sse.event === "message_delta" || sse.event === "message_stop" || sse.event === "content_block_start" || sse.event === "content_block_delta" || sse.event === "content_block_stop" || sse.event === "message" || sse.event === "user.message" || sse.event === "user.interrupt" || sse.event === "user.tool_confirmation" || sse.event === "user.custom_tool_result" || sse.event === "user.tool_result" || sse.event === "agent.message" || sse.event === "agent.thinking" || sse.event === "agent.tool_use" || sse.event === "agent.tool_result" || sse.event === "agent.mcp_tool_use" || sse.event === "agent.mcp_tool_result" || sse.event === "agent.custom_tool_use" || sse.event === "agent.thread_context_compacted" || sse.event === "session.status_running" || sse.event === "session.status_idle" || sse.event === "session.status_rescheduled" || sse.event === "session.status_terminated" || sse.event === "session.error" || sse.event === "session.deleted" || sse.event === "session.updated" || sse.event === "span.model_request_start" || sse.event === "span.model_request_end" || sse.event === "span.outcome_evaluation_start" || sse.event === "span.outcome_evaluation_ongoing" || sse.event === "span.outcome_evaluation_end" || sse.event === "user.define_outcome" || sse.event === "agent.thread_message_received" || sse.event === "agent.thread_message_sent" || sse.event === "agent.session_thread_message_received" || sse.event === "agent.session_thread_message_sent" || sse.event === "session.thread_created" || sse.event === "session.thread_status_created" || sse.event === "session.thread_status_running" || sse.event === "session.thread_status_idle" || sse.event === "session.thread_status_rescheduled" || sse.event === "session.thread_status_terminated" || sse.event === "event_start" || sse.event === "event_delta" || sse.event === "system.message")
                try {
                  yield JSON.parse(sse.data);
                } catch (e) {
                  throw logger.error("Could not parse message into JSON:", sse.data), logger.error("From chunk:", sse.raw), e;
                }
              if (sse.event !== "ping" && sse.event === "error") {
                let body = safeJSON(sse.data) ?? sse.data, type = body?.error?.type;
                throw new APIError(void 0, body, void 0, response.headers, type);
              }
            }
            done = !0;
          } catch (e) {
            if (isAbortError(e))
              return;
            throw e;
          } finally {
            done || controller.abort();
          }
        }
        return new _Stream(iterator, controller, client);
      }
      /**
       * Generates a Stream from a newline-separated ReadableStream
       * where each item is a JSON value.
       */
      static fromReadableStream(readableStream, controller, client) {
        let consumed = !1;
        async function* iterLines() {
          let lineDecoder = new LineDecoder(), iter = ReadableStreamToAsyncIterable(readableStream);
          for await (let chunk of iter)
            for (let line of lineDecoder.decode(chunk))
              yield line;
          for (let line of lineDecoder.flush())
            yield line;
        }
        async function* iterator() {
          if (consumed)
            throw new AnthropicError("Cannot iterate over a consumed stream, use `.tee()` to split the stream.");
          consumed = !0;
          let done = !1;
          try {
            for await (let line of iterLines())
              done || line && (yield JSON.parse(line));
            done = !0;
          } catch (e) {
            if (isAbortError(e))
              return;
            throw e;
          } finally {
            done || controller.abort();
          }
        }
        return new _Stream(iterator, controller, client);
      }
      [(_Stream_client = /* @__PURE__ */ new WeakMap(), Symbol.asyncIterator)]() {
        return this.iterator();
      }
      /**
       * Splits the stream into two streams which can be
       * independently read from at different speeds.
       */
      tee() {
        let left = [], right = [], iterator = this.iterator(), teeIterator = (queue) => ({
          next: () => {
            if (queue.length === 0) {
              let result = iterator.next();
              left.push(result), right.push(result);
            }
            return queue.shift();
          }
        });
        return [
          new _Stream(() => teeIterator(left), this.controller, __classPrivateFieldGet(this, _Stream_client, "f")),
          new _Stream(() => teeIterator(right), this.controller, __classPrivateFieldGet(this, _Stream_client, "f"))
        ];
      }
      /**
       * Converts this stream to a newline-separated ReadableStream of
       * JSON stringified values in the stream
       * which can be turned back into a Stream with `Stream.fromReadableStream()`.
       */
      toReadableStream() {
        let self = this, iter;
        return makeReadableStream({
          async start() {
            iter = self[Symbol.asyncIterator]();
          },
          async pull(ctrl) {
            try {
              let { value, done } = await iter.next();
              if (done)
                return ctrl.close();
              let bytes = encodeUTF8(JSON.stringify(value) + `
`);
              ctrl.enqueue(bytes);
            } catch (err) {
              ctrl.error(err);
            }
          },
          async cancel() {
            await iter.return?.();
          }
        });
      }
    };
    SSEDecoder = class {
      constructor() {
        this.event = null, this.data = [], this.chunks = [];
      }
      decode(line) {
        if (line.endsWith("\r") && (line = line.substring(0, line.length - 1)), !line) {
          if (!this.event && !this.data.length)
            return null;
          let sse = {
            event: this.event,
            data: this.data.join(`
`),
            raw: this.chunks
          };
          return this.event = null, this.data = [], this.chunks = [], sse;
        }
        if (this.chunks.push(line), line.startsWith(":"))
          return null;
        let [fieldname, _, value] = partition(line, ":");
        return value.startsWith(" ") && (value = value.substring(1)), fieldname === "event" ? this.event = value : fieldname === "data" && this.data.push(value), null;
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/parse.mjs
async function defaultParseResponse(client, props) {
  let { response, requestLogID, retryOfRequestLogID, startTime } = props, body = await (async () => {
    if (props.options.stream)
      return loggerFor(client).debug("response", response.status, response.url, response.headers, response.body), Stream.fromSSEResponse(response, props.controller);
    if (response.status === 204)
      return null;
    if (props.options.__binaryResponse)
      return response;
    let mediaType = response.headers.get("content-type")?.split(";")[0]?.trim();
    if (mediaType?.includes("application/json") || mediaType?.endsWith("+json")) {
      if (response.headers.get("content-length") === "0")
        return;
      let json = await response.json();
      return addRequestID(json, response);
    }
    return await response.text();
  })();
  return loggerFor(client).debug(`[${requestLogID}] response parsed`, formatRequestDetails({
    retryOfRequestLogID,
    url: response.url,
    status: response.status,
    body,
    durationMs: Date.now() - startTime
  })), body;
}
function addRequestID(value, response) {
  return !value || typeof value != "object" || Array.isArray(value) ? value : Object.defineProperty(value, "_request_id", {
    value: response.headers.get("request-id"),
    enumerable: !1
  });
}
var init_parse = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/parse.mjs"() {
    init_streaming();
    init_log();
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/core/middleware.mjs
function isFetchOriginError(err) {
  return typeof err == "object" && err !== null && fetchOriginErrors.has(err);
}
function isRetryableError(err) {
  let seen = /* @__PURE__ */ new Set();
  for (; typeof err == "object" && err !== null && !seen.has(err); ) {
    if (seen.add(err), isFetchOriginError(err) || isAbortError(err) || err instanceof APIConnectionError || err instanceof RetryableError)
      return !0;
    err = err.cause;
  }
  return !1;
}
function wrapFetchWithMiddleware(fetchFn, middleware, options, client) {
  return async (url, init = {}) => {
    if (middleware.length === 0)
      return fetchFn.call(void 0, url, init);
    let headers = init.headers instanceof Headers ? init.headers : new Headers(init.headers), response = await applyMiddleware(fetchFn, middleware, options, client)({
      ...init,
      headers,
      url: typeof url == "string" ? url : url instanceof URL ? url.href : url.url
    });
    if (response.bodyUsed || response.body?.locked)
      throw new AnthropicError("middleware consumed the response body; use response.clone() to inspect it, or return new Response(body, response) to consume and replace it");
    return response;
  };
}
function createMiddlewareContext(options, client) {
  let cache = /* @__PURE__ */ new WeakMap();
  return {
    options,
    // Resolved per chain, so changes to the client's `logLevel`/`logger`
    // apply to subsequent requests.
    logger: client ? loggerFor(client) : defaultLogger(),
    parse(response) {
      if (options?.stream && response.ok)
        return parseMiddlewareResponse(response, options);
      let parsed = cache.get(response);
      return parsed || (parsed = parseMiddlewareResponse(response, options), cache.set(response, parsed)), parsed;
    }
  };
}
async function parseMiddlewareResponse(response, options) {
  if (response.bodyUsed || response.body?.locked)
    throw new AnthropicError("cannot ctx.parse() a response whose body was already consumed; call ctx.parse() instead of reading the body, or read via response.clone()");
  if (options?.stream && response.ok)
    return Stream.fromSSEResponse(response.clone(), new AbortController());
  if (response.status === 204)
    return null;
  if (options?.__binaryResponse)
    return response;
  let mediaType = response.headers.get("content-type")?.split(";")[0]?.trim();
  return mediaType?.includes("application/json") || mediaType?.endsWith("+json") ? response.headers.get("content-length") === "0" ? void 0 : addRequestID(await response.clone().json(), response) : await response.clone().text();
}
function applyMiddleware(fetchFn, middleware, options, client) {
  let next = async ({ url, ...init }) => {
    try {
      return await fetchFn.call(void 0, url, init);
    } catch (err) {
      let error = castToError(err);
      throw fetchOriginErrors.add(error), error;
    }
  }, ctx = createMiddlewareContext(options, client);
  for (let i = middleware.length - 1; i >= 0; i--) {
    let mw = middleware[i], nextInner = next;
    next = async (request) => mw(request, nextInner, ctx);
  }
  return next;
}
var fetchOriginErrors, init_middleware = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/core/middleware.mjs"() {
    init_errors2();
    init_parse();
    init_log();
    init_error();
    init_streaming();
    fetchOriginErrors = /* @__PURE__ */ new WeakSet();
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/core/api-promise.mjs
var _APIPromise_client, APIPromise, init_api_promise = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/core/api-promise.mjs"() {
    init_tslib();
    init_parse();
    APIPromise = class _APIPromise extends Promise {
      constructor(client, responsePromise, parseResponse2 = defaultParseResponse) {
        super((resolve4) => {
          resolve4(null);
        }), this.responsePromise = responsePromise, this.parseResponse = parseResponse2, _APIPromise_client.set(this, void 0), __classPrivateFieldSet(this, _APIPromise_client, client, "f");
      }
      _thenUnwrap(transform) {
        return new _APIPromise(__classPrivateFieldGet(this, _APIPromise_client, "f"), this.responsePromise, async (client, props) => addRequestID(transform(await this.parseResponse(client, props), props), props.response));
      }
      /**
       * Gets the raw `Response` instance instead of parsing the response
       * data.
       *
       * If you want to parse the response body but still get the `Response`
       * instance, you can use {@link withResponse()}.
       *
       * 👋 Getting the wrong TypeScript type for `Response`?
       * Try setting `"moduleResolution": "NodeNext"` or add `"lib": ["DOM"]`
       * to your `tsconfig.json`.
       */
      asResponse() {
        return this.responsePromise.then((p) => p.response);
      }
      /**
       * Gets the parsed response data, the raw `Response` instance and the ID of the request,
       * returned via the `request-id` header which is useful for debugging requests and resporting
       * issues to Anthropic.
       *
       * If you just want to get the raw `Response` instance without parsing it,
       * you can use {@link asResponse()}.
       *
       * 👋 Getting the wrong TypeScript type for `Response`?
       * Try setting `"moduleResolution": "NodeNext"` or add `"lib": ["DOM"]`
       * to your `tsconfig.json`.
       */
      async withResponse() {
        let [data, response] = await Promise.all([this.parse(), this.asResponse()]);
        return { data, response, request_id: response.headers.get("request-id") };
      }
      parse() {
        return this.parsedPromise || (this.parsedPromise = this.responsePromise.then((data) => this.parseResponse(__classPrivateFieldGet(this, _APIPromise_client, "f"), data))), this.parsedPromise;
      }
      then(onfulfilled, onrejected) {
        return this.parse().then(onfulfilled, onrejected);
      }
      catch(onrejected) {
        return this.parse().catch(onrejected);
      }
      finally(onfinally) {
        return this.parse().finally(onfinally);
      }
    };
    _APIPromise_client = /* @__PURE__ */ new WeakMap();
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/core/pagination.mjs
var _AbstractPage_client, AbstractPage, PagePromise, Page, PageCursor, BidirectionalPageCursor, init_pagination = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/core/pagination.mjs"() {
    init_tslib();
    init_error();
    init_parse();
    init_api_promise();
    init_values();
    AbstractPage = class {
      constructor(client, response, body, options) {
        _AbstractPage_client.set(this, void 0), __classPrivateFieldSet(this, _AbstractPage_client, client, "f"), this.options = options, this.response = response, this.body = body;
      }
      hasNextPage() {
        return this.getPaginatedItems().length ? this.nextPageRequestOptions() != null : !1;
      }
      async getNextPage() {
        let nextOptions = this.nextPageRequestOptions();
        if (!nextOptions)
          throw new AnthropicError("No next page expected; please check `.hasNextPage()` before calling `.getNextPage()`.");
        return await __classPrivateFieldGet(this, _AbstractPage_client, "f").requestAPIList(this.constructor, nextOptions);
      }
      async *iterPages() {
        let page = this;
        for (yield page; page.hasNextPage(); )
          page = await page.getNextPage(), yield page;
      }
      async *[(_AbstractPage_client = /* @__PURE__ */ new WeakMap(), Symbol.asyncIterator)]() {
        for await (let page of this.iterPages())
          for (let item of page.getPaginatedItems())
            yield item;
      }
    }, PagePromise = class extends APIPromise {
      constructor(client, request, Page3) {
        super(client, request, async (client2, props) => new Page3(client2, props.response, await defaultParseResponse(client2, props), props.options));
      }
      /**
       * Allow auto-paginating iteration on an unawaited list call, eg:
       *
       *    for await (const item of client.items.list()) {
       *      console.log(item)
       *    }
       */
      async *[Symbol.asyncIterator]() {
        let page = await this;
        for await (let item of page)
          yield item;
      }
    }, Page = class extends AbstractPage {
      constructor(client, response, body, options) {
        super(client, response, body, options), this.data = body.data || [], this.has_more = body.has_more || !1, this.first_id = body.first_id || null, this.last_id = body.last_id || null;
      }
      getPaginatedItems() {
        return this.data ?? [];
      }
      hasNextPage() {
        return this.has_more === !1 ? !1 : super.hasNextPage();
      }
      nextPageRequestOptions() {
        if (this.options.query?.before_id) {
          let first_id = this.first_id;
          return first_id ? {
            ...this.options,
            query: {
              ...maybeObj(this.options.query),
              before_id: first_id
            }
          } : null;
        }
        let cursor = this.last_id;
        return cursor ? {
          ...this.options,
          query: {
            ...maybeObj(this.options.query),
            after_id: cursor
          }
        } : null;
      }
    }, PageCursor = class extends AbstractPage {
      constructor(client, response, body, options) {
        super(client, response, body, options), this.data = body.data || [], this.next_page = body.next_page || null;
      }
      getPaginatedItems() {
        return this.data ?? [];
      }
      nextPageRequestOptions() {
        let cursor = this.next_page;
        return cursor ? {
          ...this.options,
          query: {
            ...maybeObj(this.options.query),
            page: cursor
          }
        } : null;
      }
    }, BidirectionalPageCursor = class extends AbstractPage {
      constructor(client, response, body, options) {
        super(client, response, body, options), this.data = body.data || [], this.next_page = body.next_page || null, this.prev_page = body.prev_page || null;
      }
      getPaginatedItems() {
        return this.data ?? [];
      }
      nextPageRequestOptions() {
        let cursor = this.next_page;
        return cursor ? {
          ...this.options,
          query: {
            ...maybeObj(this.options.query),
            page: cursor
          }
        } : null;
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/uploads.mjs
function makeFile(fileBits, fileName, options) {
  return checkFileSupport(), new File(fileBits, fileName ?? "unknown_file", options);
}
function getName(value, stripPath) {
  let val = typeof value == "object" && value !== null && ("name" in value && value.name && String(value.name) || "url" in value && value.url && String(value.url) || "filename" in value && value.filename && String(value.filename) || "path" in value && value.path && String(value.path)) || "";
  return stripPath ? val.split(/[\\/]/).pop() || void 0 : val;
}
function supportsFormData(fetchObject) {
  let fetch2 = typeof fetchObject == "function" ? fetchObject : fetchObject.fetch, cached = supportsFormDataMap.get(fetch2);
  if (cached)
    return cached;
  let promise = (async () => {
    try {
      let FetchResponse = "Response" in fetch2 ? fetch2.Response : (await fetch2("data:,")).constructor, data = new FormData();
      return data.toString() !== await new FetchResponse(data).text();
    } catch {
      return !0;
    }
  })();
  return supportsFormDataMap.set(fetch2, promise), promise;
}
var checkFileSupport, isAsyncIterable, multipartFormRequestOptions, supportsFormDataMap, createForm, isNamedBlob, addFormValue, init_uploads = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/uploads.mjs"() {
    init_shims();
    checkFileSupport = () => {
      if (typeof File > "u") {
        let { process: process2 } = globalThis, isOldNode = typeof process2?.versions?.node == "string" && parseInt(process2.versions.node.split(".")) < 20;
        throw new Error("`File` is not defined as a global, which is required for file uploads." + (isOldNode ? " Update to Node 20 LTS or newer, or set `globalThis.File` to `import('node:buffer').File`." : ""));
      }
    };
    isAsyncIterable = (value) => value != null && typeof value == "object" && typeof value[Symbol.asyncIterator] == "function", multipartFormRequestOptions = async (opts, fetch2, stripFilenames = !0) => ({ ...opts, body: await createForm(opts.body, fetch2, stripFilenames) }), supportsFormDataMap = /* @__PURE__ */ new WeakMap();
    createForm = async (body, fetch2, stripFilenames = !0) => {
      if (!await supportsFormData(fetch2))
        throw new TypeError("The provided fetch function does not support file uploads with the current global FormData class.");
      let form = new FormData();
      return await Promise.all(Object.entries(body || {}).map(([key, value]) => addFormValue(form, key, value, stripFilenames))), form;
    }, isNamedBlob = (value) => value instanceof Blob && "name" in value, addFormValue = async (form, key, value, stripFilenames) => {
      if (value !== void 0) {
        if (value == null)
          throw new TypeError(`Received null for "${key}"; to pass null in FormData, you must use the string 'null'`);
        if (typeof value == "string" || typeof value == "number" || typeof value == "boolean")
          form.append(key, String(value));
        else if (value instanceof Response) {
          let options = {}, contentType = value.headers.get("Content-Type");
          contentType && (options = { type: contentType }), form.append(key, makeFile([await value.blob()], getName(value, stripFilenames), options));
        } else if (isAsyncIterable(value))
          form.append(key, makeFile([await new Response(ReadableStreamFrom(value)).blob()], getName(value, stripFilenames)));
        else if (isNamedBlob(value))
          form.append(key, makeFile([value], getName(value, stripFilenames), { type: value.type }));
        else if (Array.isArray(value))
          await Promise.all(value.map((entry) => addFormValue(form, key + "[]", entry, stripFilenames)));
        else if (typeof value == "object")
          await Promise.all(Object.entries(value).map(([name, prop]) => addFormValue(form, `${key}[${name}]`, prop, stripFilenames)));
        else
          throw new TypeError(`Invalid value given to form, expected a string, number, boolean, object, Array, File or Blob but got ${value} instead`);
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/to-file.mjs
async function toFile(value, name, options) {
  if (checkFileSupport(), value = await value, name || (name = getName(value, !0)), isFileLike(value))
    return value instanceof File && name == null && options == null ? value : makeFile([await value.arrayBuffer()], name ?? value.name, {
      type: value.type,
      lastModified: value.lastModified,
      ...options
    });
  if (isResponseLike(value)) {
    let blob = await value.blob();
    return name || (name = new URL(value.url).pathname.split(/[\\/]/).pop()), makeFile(await getBytes(blob), name, options);
  }
  let parts = await getBytes(value);
  if (!options?.type) {
    let type = parts.find((part) => typeof part == "object" && "type" in part && part.type);
    typeof type == "string" && (options = { ...options, type });
  }
  return makeFile(parts, name, options);
}
async function getBytes(value) {
  let parts = [];
  if (typeof value == "string" || ArrayBuffer.isView(value) || // includes Uint8Array, Buffer, etc.
  value instanceof ArrayBuffer)
    parts.push(value);
  else if (isBlobLike(value))
    parts.push(value instanceof Blob ? value : await value.arrayBuffer());
  else if (isAsyncIterable(value))
    for await (let chunk of value)
      parts.push(...await getBytes(chunk));
  else {
    let constructor = value?.constructor?.name;
    throw new Error(`Unexpected data type: ${typeof value}${constructor ? `; constructor: ${constructor}` : ""}${propsForError(value)}`);
  }
  return parts;
}
function propsForError(value) {
  return typeof value != "object" || value === null ? "" : `; props: [${Object.getOwnPropertyNames(value).map((p) => `"${p}"`).join(", ")}]`;
}
var isBlobLike, isFileLike, isResponseLike, init_to_file = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/to-file.mjs"() {
    init_uploads();
    init_uploads();
    isBlobLike = (value) => value != null && typeof value == "object" && typeof value.size == "number" && typeof value.type == "string" && typeof value.text == "function" && typeof value.slice == "function" && typeof value.arrayBuffer == "function", isFileLike = (value) => value != null && typeof value == "object" && typeof value.name == "string" && typeof value.lastModified == "number" && isBlobLike(value), isResponseLike = (value) => value != null && typeof value == "object" && typeof value.url == "string" && typeof value.blob == "function";
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/core/uploads.mjs
var init_uploads2 = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/core/uploads.mjs"() {
    init_to_file();
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/shared.mjs
var init_shared = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/shared.mjs"() {
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/core/resource.mjs
var APIResource, init_resource = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/core/resource.mjs"() {
    APIResource = class {
      constructor(client) {
        this._client = client;
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/headers.mjs
function* iterateHeaders(headers) {
  if (!headers)
    return;
  if (brand_privateNullableHeaders in headers) {
    let { values, nulls } = headers;
    yield* values.entries();
    for (let name of nulls)
      yield [name, null];
    return;
  }
  let shouldClear = !1, iter;
  headers instanceof Headers ? iter = headers.entries() : isReadonlyArray(headers) ? iter = headers : (shouldClear = !0, iter = Object.entries(headers ?? {}));
  for (let row of iter) {
    let name = row[0];
    if (typeof name != "string")
      throw new TypeError("expected header name to be a string");
    let values = isReadonlyArray(row[1]) ? row[1] : [row[1]], didClear = !1;
    for (let value of values)
      value !== void 0 && (shouldClear && !didClear && (didClear = !0, yield [name, clearSentinel]), yield [name, value]);
  }
}
var brand_privateNullableHeaders, clearSentinel, APPEND_HEADERS, appendHeaderValue, buildHeaders, init_headers = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/headers.mjs"() {
    init_values();
    brand_privateNullableHeaders = Symbol.for("brand.privateNullableHeaders");
    clearSentinel = Symbol("clear"), APPEND_HEADERS = /* @__PURE__ */ new Set(["x-stainless-helper"]), appendHeaderValue = (existing, addition) => {
      let tokens = existing ? existing.split(",").map((t) => t.trim()).filter(Boolean) : [];
      for (let tok of addition.split(",").map((t) => t.trim()))
        tok && !tokens.includes(tok) && tokens.push(tok);
      return tokens.join(", ");
    }, buildHeaders = (newHeaders) => {
      let targetHeaders = new Headers(), nullHeaders = /* @__PURE__ */ new Set();
      for (let headers of newHeaders) {
        let seenHeaders = /* @__PURE__ */ new Set();
        for (let [name, value] of iterateHeaders(headers)) {
          let lowerName = name.toLowerCase();
          if (APPEND_HEADERS.has(lowerName)) {
            if (value === clearSentinel)
              continue;
            value === null ? (targetHeaders.delete(name), nullHeaders.add(lowerName)) : (targetHeaders.set(name, appendHeaderValue(targetHeaders.get(name), value)), nullHeaders.delete(lowerName));
            continue;
          }
          (value === clearSentinel || !seenHeaders.has(lowerName)) && (targetHeaders.delete(name), seenHeaders.add(lowerName), value === clearSentinel) || (value === null ? (targetHeaders.delete(name), nullHeaders.add(lowerName)) : (targetHeaders.append(name, value), nullHeaders.delete(lowerName)));
        }
      }
      return { [brand_privateNullableHeaders]: !0, values: targetHeaders, nulls: nullHeaders };
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/path.mjs
function encodeURIPath(str2) {
  return str2.replace(/[^A-Za-z0-9\-._~!$&'()*+,;=:@]+/g, encodeURIComponent);
}
var EMPTY, createPathTagFunction, path3, init_path = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/path.mjs"() {
    init_error();
    EMPTY = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.create(null)), createPathTagFunction = (pathEncoder = encodeURIPath) => function(statics, ...params) {
      if (statics.length === 1)
        return statics[0];
      let postPath = !1, invalidSegments = [], path13 = statics.reduce((previousValue, currentValue, index) => {
        /[?#]/.test(currentValue) && (postPath = !0);
        let value = params[index], encoded = (postPath ? encodeURIComponent : pathEncoder)("" + value);
        return index !== params.length && (value == null || typeof value == "object" && // handle values from other realms
        value.toString === Object.getPrototypeOf(Object.getPrototypeOf(value.hasOwnProperty ?? EMPTY) ?? EMPTY)?.toString) && (encoded = value + "", invalidSegments.push({
          start: previousValue.length + currentValue.length,
          length: encoded.length,
          error: `Value of type ${Object.prototype.toString.call(value).slice(8, -1)} is not a valid path parameter`
        })), previousValue + currentValue + (index === params.length ? "" : encoded);
      }, ""), pathOnly = path13.split(/[?#]/, 1)[0], invalidSegmentPattern = /(?<=^|\/)(?:\.|%2e){1,2}(?=\/|$)/gi, match2;
      for (; (match2 = invalidSegmentPattern.exec(pathOnly)) !== null; )
        invalidSegments.push({
          start: match2.index,
          length: match2[0].length,
          error: `Value "${match2[0]}" can't be safely passed as a path parameter`
        });
      if (invalidSegments.sort((a, b) => a.start - b.start), invalidSegments.length > 0) {
        let lastEnd = 0, underline = invalidSegments.reduce((acc, segment) => {
          let spaces = " ".repeat(segment.start - lastEnd), arrows = "^".repeat(segment.length);
          return lastEnd = segment.start + segment.length, acc + spaces + arrows;
        }, "");
        throw new AnthropicError(`Path parameters result in path with invalid segments:
${invalidSegments.map((e) => e.error).join(`
`)}
${path13}
${underline}`);
      }
      return path13;
    }, path3 = /* @__PURE__ */ createPathTagFunction(encodeURIPath);
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/deployment-runs.mjs
var DeploymentRuns, init_deployment_runs = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/deployment-runs.mjs"() {
    init_resource();
    init_pagination();
    init_headers();
    init_path();
    DeploymentRuns = class extends APIResource {
      /**
       * Get Deployment Run
       *
       * @example
       * ```ts
       * const betaManagedAgentsDeploymentRun =
       *   await client.beta.deploymentRuns.retrieve(
       *     'deployment_run_id',
       *   );
       * ```
       */
      retrieve(deploymentRunID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.get(path3`/v1/deployment_runs/${deploymentRunID}?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * List Deployment Runs
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const betaManagedAgentsDeploymentRun of client.beta.deploymentRuns.list()) {
       *   // ...
       * }
       * ```
       */
      list(params = {}, options) {
        let { betas, ...query } = params ?? {};
        return this._client.getAPIList("/v1/deployment_runs?beta=true", PageCursor, {
          query,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/deployments.mjs
var Deployments, init_deployments = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/deployments.mjs"() {
    init_resource();
    init_pagination();
    init_headers();
    init_path();
    Deployments = class extends APIResource {
      /**
       * Create Deployment
       *
       * @example
       * ```ts
       * const betaManagedAgentsDeployment =
       *   await client.beta.deployments.create({
       *     agent: 'string',
       *     environment_id: 'x',
       *     initial_events: [
       *       {
       *         content: [
       *           {
       *             text: 'Where is my order #1234?',
       *             type: 'text',
       *           },
       *         ],
       *         type: 'user.message',
       *       },
       *     ],
       *     name: 'x',
       *   });
       * ```
       */
      create(params, options) {
        let { betas, ...body } = params;
        return this._client.post("/v1/deployments?beta=true", {
          body,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Get Deployment
       *
       * @example
       * ```ts
       * const betaManagedAgentsDeployment =
       *   await client.beta.deployments.retrieve(
       *     'depl_011CZkZcDH3vPqd7xnEfwTai',
       *   );
       * ```
       */
      retrieve(deploymentID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.get(path3`/v1/deployments/${deploymentID}?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Update Deployment
       *
       * @example
       * ```ts
       * const betaManagedAgentsDeployment =
       *   await client.beta.deployments.update(
       *     'depl_011CZkZcDH3vPqd7xnEfwTai',
       *   );
       * ```
       */
      update(deploymentID, params, options) {
        let { betas, ...body } = params;
        return this._client.post(path3`/v1/deployments/${deploymentID}?beta=true`, {
          body,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * List Deployments
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const betaManagedAgentsDeployment of client.beta.deployments.list()) {
       *   // ...
       * }
       * ```
       */
      list(params = {}, options) {
        let { betas, ...query } = params ?? {};
        return this._client.getAPIList("/v1/deployments?beta=true", PageCursor, {
          query,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Archive Deployment
       *
       * @example
       * ```ts
       * const betaManagedAgentsDeployment =
       *   await client.beta.deployments.archive(
       *     'depl_011CZkZcDH3vPqd7xnEfwTai',
       *   );
       * ```
       */
      archive(deploymentID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.post(path3`/v1/deployments/${deploymentID}/archive?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Pause Deployment
       *
       * @example
       * ```ts
       * const betaManagedAgentsDeployment =
       *   await client.beta.deployments.pause(
       *     'depl_011CZkZcDH3vPqd7xnEfwTai',
       *   );
       * ```
       */
      pause(deploymentID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.post(path3`/v1/deployments/${deploymentID}/pause?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Run Deployment Now
       *
       * @example
       * ```ts
       * const betaManagedAgentsDeploymentRun =
       *   await client.beta.deployments.run(
       *     'depl_011CZkZcDH3vPqd7xnEfwTai',
       *   );
       * ```
       */
      run(deploymentID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.post(path3`/v1/deployments/${deploymentID}/run?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Unpause Deployment
       *
       * @example
       * ```ts
       * const betaManagedAgentsDeployment =
       *   await client.beta.deployments.unpause(
       *     'depl_011CZkZcDH3vPqd7xnEfwTai',
       *   );
       * ```
       */
      unpause(deploymentID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.post(path3`/v1/deployments/${deploymentID}/unpause?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/stainless-helper-header.mjs
function helperHeader(value) {
  return { [STAINLESS_HELPER_HEADER]: value };
}
function wasCreatedByStainlessHelper(value) {
  return typeof value == "object" && value !== null && SDK_HELPER_SYMBOL in value;
}
function collectStainlessHelpers(tools, messages) {
  let helpers = /* @__PURE__ */ new Set();
  if (tools)
    for (let tool of tools)
      wasCreatedByStainlessHelper(tool) && helpers.add(tool[SDK_HELPER_SYMBOL]);
  if (messages)
    for (let message of messages) {
      wasCreatedByStainlessHelper(message) && helpers.add(message[SDK_HELPER_SYMBOL]);
      let content = message.content;
      if (Array.isArray(content))
        for (let block of content)
          wasCreatedByStainlessHelper(block) && helpers.add(block[SDK_HELPER_SYMBOL]);
    }
  return Array.from(helpers);
}
function stainlessHelperHeader(tools, messages) {
  let helpers = collectStainlessHelpers(tools, messages);
  return helpers.length === 0 ? {} : { [STAINLESS_HELPER_HEADER]: helpers.join(", ") };
}
function stainlessHelperHeaderFromFile(file) {
  return wasCreatedByStainlessHelper(file) ? { [STAINLESS_HELPER_HEADER]: file[SDK_HELPER_SYMBOL] } : {};
}
var STAINLESS_HELPER_HEADER, STAINLESS_HELPER_METHOD_HEADER, SDK_HELPER_SYMBOL, init_stainless_helper_header = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/stainless-helper-header.mjs"() {
    STAINLESS_HELPER_HEADER = "x-stainless-helper", STAINLESS_HELPER_METHOD_HEADER = "x-stainless-helper-method";
    SDK_HELPER_SYMBOL = Symbol("anthropic.sdk.stainlessHelper");
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/files.mjs
var Files, init_files = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/files.mjs"() {
    init_resource();
    init_pagination();
    init_headers();
    init_stainless_helper_header();
    init_uploads();
    init_path();
    Files = class extends APIResource {
      /**
       * List Files
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const fileMetadata of client.beta.files.list()) {
       *   // ...
       * }
       * ```
       */
      list(params = {}, options) {
        let { betas, ...query } = params ?? {};
        return this._client.getAPIList("/v1/files?beta=true", Page, {
          query,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "files-api-2025-04-14"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Delete File
       *
       * @example
       * ```ts
       * const deletedFile = await client.beta.files.delete(
       *   'file_id',
       * );
       * ```
       */
      delete(fileID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.delete(path3`/v1/files/${fileID}?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "files-api-2025-04-14"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Download File
       *
       * @example
       * ```ts
       * const response = await client.beta.files.download(
       *   'file_id',
       * );
       *
       * const content = await response.blob();
       * console.log(content);
       * ```
       */
      download(fileID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.get(path3`/v1/files/${fileID}/content?beta=true`, {
          ...options,
          headers: buildHeaders([
            {
              "anthropic-beta": [...betas ?? [], "files-api-2025-04-14"].toString(),
              Accept: "application/binary"
            },
            options?.headers
          ]),
          __binaryResponse: !0
        });
      }
      /**
       * Get File Metadata
       *
       * @example
       * ```ts
       * const fileMetadata =
       *   await client.beta.files.retrieveMetadata('file_id');
       * ```
       */
      retrieveMetadata(fileID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.get(path3`/v1/files/${fileID}?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "files-api-2025-04-14"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Upload File
       *
       * @example
       * ```ts
       * const fileMetadata = await client.beta.files.upload({
       *   file: fs.createReadStream('path/to/file'),
       * });
       * ```
       */
      upload(params, options) {
        let { betas, ...body } = params;
        return this._client.post("/v1/files?beta=true", multipartFormRequestOptions({
          body,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "files-api-2025-04-14"].toString() },
            stainlessHelperHeaderFromFile(body.file),
            options?.headers
          ])
        }, this._client));
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/models.mjs
var Models, init_models = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/models.mjs"() {
    init_resource();
    init_pagination();
    init_headers();
    init_path();
    Models = class extends APIResource {
      /**
       * Get a specific model.
       *
       * The Models API response can be used to determine information about a specific
       * model or resolve a model alias to a model ID.
       *
       * @example
       * ```ts
       * const betaModelInfo = await client.beta.models.retrieve(
       *   'model_id',
       * );
       * ```
       */
      retrieve(modelID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.get(path3`/v1/models/${modelID}?beta=true`, {
          ...options,
          headers: buildHeaders([
            { ...betas?.toString() != null ? { "anthropic-beta": betas?.toString() } : void 0 },
            options?.headers
          ])
        });
      }
      /**
       * List available models.
       *
       * The Models API response can be used to determine which models are available for
       * use in the API. More recently released models are listed first.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const betaModelInfo of client.beta.models.list()) {
       *   // ...
       * }
       * ```
       */
      list(params = {}, options) {
        let { betas, ...query } = params ?? {};
        return this._client.getAPIList("/v1/models?beta=true", Page, {
          query,
          ...options,
          headers: buildHeaders([
            { ...betas?.toString() != null ? { "anthropic-beta": betas?.toString() } : void 0 },
            options?.headers
          ])
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/user-profiles.mjs
var UserProfiles, init_user_profiles = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/user-profiles.mjs"() {
    init_resource();
    init_pagination();
    init_headers();
    init_path();
    UserProfiles = class extends APIResource {
      /**
       * Create User Profile
       *
       * @example
       * ```ts
       * const betaUserProfile =
       *   await client.beta.userProfiles.create();
       * ```
       */
      create(params, options) {
        let { betas, ...body } = params;
        return this._client.post("/v1/user_profiles?beta=true", {
          body,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "user-profiles-2026-03-24"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Get User Profile
       *
       * @example
       * ```ts
       * const betaUserProfile =
       *   await client.beta.userProfiles.retrieve(
       *     'uprof_011CZkZCu8hGbp5mYRQgUmz9',
       *   );
       * ```
       */
      retrieve(userProfileID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.get(path3`/v1/user_profiles/${userProfileID}?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "user-profiles-2026-03-24"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Update User Profile
       *
       * @example
       * ```ts
       * const betaUserProfile =
       *   await client.beta.userProfiles.update(
       *     'uprof_011CZkZCu8hGbp5mYRQgUmz9',
       *   );
       * ```
       */
      update(userProfileID, params, options) {
        let { betas, ...body } = params;
        return this._client.post(path3`/v1/user_profiles/${userProfileID}?beta=true`, {
          body,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "user-profiles-2026-03-24"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * List User Profiles
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const betaUserProfile of client.beta.userProfiles.list()) {
       *   // ...
       * }
       * ```
       */
      list(params = {}, options) {
        let { betas, ...query } = params ?? {};
        return this._client.getAPIList("/v1/user_profiles?beta=true", PageCursor, {
          query,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "user-profiles-2026-03-24"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Create Enrollment URL
       *
       * @example
       * ```ts
       * const betaUserProfileEnrollmentURL =
       *   await client.beta.userProfiles.createEnrollmentURL(
       *     'uprof_011CZkZCu8hGbp5mYRQgUmz9',
       *   );
       * ```
       */
      createEnrollmentURL(userProfileID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.post(path3`/v1/user_profiles/${userProfileID}/enrollment_url?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "user-profiles-2026-03-24"].toString() },
            options?.headers
          ])
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/standardwebhooks@1.0.0/node_modules/standardwebhooks/dist/timing_safe_equal.js
var require_timing_safe_equal = __commonJS({
  "../../node_modules/.pnpm/standardwebhooks@1.0.0/node_modules/standardwebhooks/dist/timing_safe_equal.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: !0 });
    exports2.timingSafeEqual = void 0;
    function assert(expr, msg = "") {
      if (!expr)
        throw new Error(msg);
    }
    function timingSafeEqual(a, b) {
      if (a.byteLength !== b.byteLength)
        return !1;
      a instanceof DataView || (a = new DataView(ArrayBuffer.isView(a) ? a.buffer : a)), b instanceof DataView || (b = new DataView(ArrayBuffer.isView(b) ? b.buffer : b)), assert(a instanceof DataView), assert(b instanceof DataView);
      let length = a.byteLength, out = 0, i = -1;
      for (; ++i < length; )
        out |= a.getUint8(i) ^ b.getUint8(i);
      return out === 0;
    }
    exports2.timingSafeEqual = timingSafeEqual;
  }
});

// ../../node_modules/.pnpm/@stablelib+base64@1.0.1/node_modules/@stablelib/base64/lib/base64.js
var require_base64 = __commonJS({
  "../../node_modules/.pnpm/@stablelib+base64@1.0.1/node_modules/@stablelib/base64/lib/base64.js"(exports2) {
    "use strict";
    var __extends = exports2 && exports2.__extends || /* @__PURE__ */ (function() {
      var extendStatics = function(d, b) {
        return extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
          d2.__proto__ = b2;
        } || function(d2, b2) {
          for (var p in b2) b2.hasOwnProperty(p) && (d2[p] = b2[p]);
        }, extendStatics(d, b);
      };
      return function(d, b) {
        extendStatics(d, b);
        function __() {
          this.constructor = d;
        }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
      };
    })();
    Object.defineProperty(exports2, "__esModule", { value: !0 });
    var INVALID_BYTE = 256, Coder = (
      /** @class */
      (function() {
        function Coder2(_paddingCharacter) {
          _paddingCharacter === void 0 && (_paddingCharacter = "="), this._paddingCharacter = _paddingCharacter;
        }
        return Coder2.prototype.encodedLength = function(length) {
          return this._paddingCharacter ? (length + 2) / 3 * 4 | 0 : (length * 8 + 5) / 6 | 0;
        }, Coder2.prototype.encode = function(data) {
          for (var out = "", i = 0; i < data.length - 2; i += 3) {
            var c = data[i] << 16 | data[i + 1] << 8 | data[i + 2];
            out += this._encodeByte(c >>> 18 & 63), out += this._encodeByte(c >>> 12 & 63), out += this._encodeByte(c >>> 6 & 63), out += this._encodeByte(c >>> 0 & 63);
          }
          var left = data.length - i;
          if (left > 0) {
            var c = data[i] << 16 | (left === 2 ? data[i + 1] << 8 : 0);
            out += this._encodeByte(c >>> 18 & 63), out += this._encodeByte(c >>> 12 & 63), left === 2 ? out += this._encodeByte(c >>> 6 & 63) : out += this._paddingCharacter || "", out += this._paddingCharacter || "";
          }
          return out;
        }, Coder2.prototype.maxDecodedLength = function(length) {
          return this._paddingCharacter ? length / 4 * 3 | 0 : (length * 6 + 7) / 8 | 0;
        }, Coder2.prototype.decodedLength = function(s) {
          return this.maxDecodedLength(s.length - this._getPaddingLength(s));
        }, Coder2.prototype.decode = function(s) {
          if (s.length === 0)
            return new Uint8Array(0);
          for (var paddingLength = this._getPaddingLength(s), length = s.length - paddingLength, out = new Uint8Array(this.maxDecodedLength(length)), op = 0, i = 0, haveBad = 0, v0 = 0, v1 = 0, v2 = 0, v3 = 0; i < length - 4; i += 4)
            v0 = this._decodeChar(s.charCodeAt(i + 0)), v1 = this._decodeChar(s.charCodeAt(i + 1)), v2 = this._decodeChar(s.charCodeAt(i + 2)), v3 = this._decodeChar(s.charCodeAt(i + 3)), out[op++] = v0 << 2 | v1 >>> 4, out[op++] = v1 << 4 | v2 >>> 2, out[op++] = v2 << 6 | v3, haveBad |= v0 & INVALID_BYTE, haveBad |= v1 & INVALID_BYTE, haveBad |= v2 & INVALID_BYTE, haveBad |= v3 & INVALID_BYTE;
          if (i < length - 1 && (v0 = this._decodeChar(s.charCodeAt(i)), v1 = this._decodeChar(s.charCodeAt(i + 1)), out[op++] = v0 << 2 | v1 >>> 4, haveBad |= v0 & INVALID_BYTE, haveBad |= v1 & INVALID_BYTE), i < length - 2 && (v2 = this._decodeChar(s.charCodeAt(i + 2)), out[op++] = v1 << 4 | v2 >>> 2, haveBad |= v2 & INVALID_BYTE), i < length - 3 && (v3 = this._decodeChar(s.charCodeAt(i + 3)), out[op++] = v2 << 6 | v3, haveBad |= v3 & INVALID_BYTE), haveBad !== 0)
            throw new Error("Base64Coder: incorrect characters for decoding");
          return out;
        }, Coder2.prototype._encodeByte = function(b) {
          var result = b;
          return result += 65, result += 25 - b >>> 8 & 6, result += 51 - b >>> 8 & -75, result += 61 - b >>> 8 & -15, result += 62 - b >>> 8 & 3, String.fromCharCode(result);
        }, Coder2.prototype._decodeChar = function(c) {
          var result = INVALID_BYTE;
          return result += (42 - c & c - 44) >>> 8 & -INVALID_BYTE + c - 43 + 62, result += (46 - c & c - 48) >>> 8 & -INVALID_BYTE + c - 47 + 63, result += (47 - c & c - 58) >>> 8 & -INVALID_BYTE + c - 48 + 52, result += (64 - c & c - 91) >>> 8 & -INVALID_BYTE + c - 65 + 0, result += (96 - c & c - 123) >>> 8 & -INVALID_BYTE + c - 97 + 26, result;
        }, Coder2.prototype._getPaddingLength = function(s) {
          var paddingLength = 0;
          if (this._paddingCharacter) {
            for (var i = s.length - 1; i >= 0 && s[i] === this._paddingCharacter; i--)
              paddingLength++;
            if (s.length < 4 || paddingLength > 2)
              throw new Error("Base64Coder: incorrect padding");
          }
          return paddingLength;
        }, Coder2;
      })()
    );
    exports2.Coder = Coder;
    var stdCoder = new Coder();
    function encode3(data) {
      return stdCoder.encode(data);
    }
    exports2.encode = encode3;
    function decode(s) {
      return stdCoder.decode(s);
    }
    exports2.decode = decode;
    var URLSafeCoder = (
      /** @class */
      (function(_super) {
        __extends(URLSafeCoder2, _super);
        function URLSafeCoder2() {
          return _super !== null && _super.apply(this, arguments) || this;
        }
        return URLSafeCoder2.prototype._encodeByte = function(b) {
          var result = b;
          return result += 65, result += 25 - b >>> 8 & 6, result += 51 - b >>> 8 & -75, result += 61 - b >>> 8 & -13, result += 62 - b >>> 8 & 49, String.fromCharCode(result);
        }, URLSafeCoder2.prototype._decodeChar = function(c) {
          var result = INVALID_BYTE;
          return result += (44 - c & c - 46) >>> 8 & -INVALID_BYTE + c - 45 + 62, result += (94 - c & c - 96) >>> 8 & -INVALID_BYTE + c - 95 + 63, result += (47 - c & c - 58) >>> 8 & -INVALID_BYTE + c - 48 + 52, result += (64 - c & c - 91) >>> 8 & -INVALID_BYTE + c - 65 + 0, result += (96 - c & c - 123) >>> 8 & -INVALID_BYTE + c - 97 + 26, result;
        }, URLSafeCoder2;
      })(Coder)
    );
    exports2.URLSafeCoder = URLSafeCoder;
    var urlSafeCoder = new URLSafeCoder();
    function encodeURLSafe(data) {
      return urlSafeCoder.encode(data);
    }
    exports2.encodeURLSafe = encodeURLSafe;
    function decodeURLSafe(s) {
      return urlSafeCoder.decode(s);
    }
    exports2.decodeURLSafe = decodeURLSafe;
    exports2.encodedLength = function(length) {
      return stdCoder.encodedLength(length);
    };
    exports2.maxDecodedLength = function(length) {
      return stdCoder.maxDecodedLength(length);
    };
    exports2.decodedLength = function(s) {
      return stdCoder.decodedLength(s);
    };
  }
});

// ../../node_modules/.pnpm/fast-sha256@1.3.0/node_modules/fast-sha256/sha256.js
var require_sha256 = __commonJS({
  "../../node_modules/.pnpm/fast-sha256@1.3.0/node_modules/fast-sha256/sha256.js"(exports2, module2) {
    (function(root, factory) {
      var exports3 = {};
      factory(exports3);
      var sha256 = exports3.default;
      for (var k in exports3)
        sha256[k] = exports3[k];
      typeof module2 == "object" && typeof module2.exports == "object" ? module2.exports = sha256 : typeof define == "function" && define.amd ? define(function() {
        return sha256;
      }) : root.sha256 = sha256;
    })(exports2, function(exports3) {
      "use strict";
      exports3.__esModule = !0, exports3.digestLength = 32, exports3.blockSize = 64;
      var K = new Uint32Array([
        1116352408,
        1899447441,
        3049323471,
        3921009573,
        961987163,
        1508970993,
        2453635748,
        2870763221,
        3624381080,
        310598401,
        607225278,
        1426881987,
        1925078388,
        2162078206,
        2614888103,
        3248222580,
        3835390401,
        4022224774,
        264347078,
        604807628,
        770255983,
        1249150122,
        1555081692,
        1996064986,
        2554220882,
        2821834349,
        2952996808,
        3210313671,
        3336571891,
        3584528711,
        113926993,
        338241895,
        666307205,
        773529912,
        1294757372,
        1396182291,
        1695183700,
        1986661051,
        2177026350,
        2456956037,
        2730485921,
        2820302411,
        3259730800,
        3345764771,
        3516065817,
        3600352804,
        4094571909,
        275423344,
        430227734,
        506948616,
        659060556,
        883997877,
        958139571,
        1322822218,
        1537002063,
        1747873779,
        1955562222,
        2024104815,
        2227730452,
        2361852424,
        2428436474,
        2756734187,
        3204031479,
        3329325298
      ]);
      function hashBlocks(w, v, p, pos, len) {
        for (var a, b, c, d, e, f, g, h, u, i, j, t1, t2; len >= 64; ) {
          for (a = v[0], b = v[1], c = v[2], d = v[3], e = v[4], f = v[5], g = v[6], h = v[7], i = 0; i < 16; i++)
            j = pos + i * 4, w[i] = (p[j] & 255) << 24 | (p[j + 1] & 255) << 16 | (p[j + 2] & 255) << 8 | p[j + 3] & 255;
          for (i = 16; i < 64; i++)
            u = w[i - 2], t1 = (u >>> 17 | u << 15) ^ (u >>> 19 | u << 13) ^ u >>> 10, u = w[i - 15], t2 = (u >>> 7 | u << 25) ^ (u >>> 18 | u << 14) ^ u >>> 3, w[i] = (t1 + w[i - 7] | 0) + (t2 + w[i - 16] | 0);
          for (i = 0; i < 64; i++)
            t1 = (((e >>> 6 | e << 26) ^ (e >>> 11 | e << 21) ^ (e >>> 25 | e << 7)) + (e & f ^ ~e & g) | 0) + (h + (K[i] + w[i] | 0) | 0) | 0, t2 = ((a >>> 2 | a << 30) ^ (a >>> 13 | a << 19) ^ (a >>> 22 | a << 10)) + (a & b ^ a & c ^ b & c) | 0, h = g, g = f, f = e, e = d + t1 | 0, d = c, c = b, b = a, a = t1 + t2 | 0;
          v[0] += a, v[1] += b, v[2] += c, v[3] += d, v[4] += e, v[5] += f, v[6] += g, v[7] += h, pos += 64, len -= 64;
        }
        return pos;
      }
      var Hash = (
        /** @class */
        (function() {
          function Hash2() {
            this.digestLength = exports3.digestLength, this.blockSize = exports3.blockSize, this.state = new Int32Array(8), this.temp = new Int32Array(64), this.buffer = new Uint8Array(128), this.bufferLength = 0, this.bytesHashed = 0, this.finished = !1, this.reset();
          }
          return Hash2.prototype.reset = function() {
            return this.state[0] = 1779033703, this.state[1] = 3144134277, this.state[2] = 1013904242, this.state[3] = 2773480762, this.state[4] = 1359893119, this.state[5] = 2600822924, this.state[6] = 528734635, this.state[7] = 1541459225, this.bufferLength = 0, this.bytesHashed = 0, this.finished = !1, this;
          }, Hash2.prototype.clean = function() {
            for (var i = 0; i < this.buffer.length; i++)
              this.buffer[i] = 0;
            for (var i = 0; i < this.temp.length; i++)
              this.temp[i] = 0;
            this.reset();
          }, Hash2.prototype.update = function(data, dataLength) {
            if (dataLength === void 0 && (dataLength = data.length), this.finished)
              throw new Error("SHA256: can't update because hash was finished.");
            var dataPos = 0;
            if (this.bytesHashed += dataLength, this.bufferLength > 0) {
              for (; this.bufferLength < 64 && dataLength > 0; )
                this.buffer[this.bufferLength++] = data[dataPos++], dataLength--;
              this.bufferLength === 64 && (hashBlocks(this.temp, this.state, this.buffer, 0, 64), this.bufferLength = 0);
            }
            for (dataLength >= 64 && (dataPos = hashBlocks(this.temp, this.state, data, dataPos, dataLength), dataLength %= 64); dataLength > 0; )
              this.buffer[this.bufferLength++] = data[dataPos++], dataLength--;
            return this;
          }, Hash2.prototype.finish = function(out) {
            if (!this.finished) {
              var bytesHashed = this.bytesHashed, left = this.bufferLength, bitLenHi = bytesHashed / 536870912 | 0, bitLenLo = bytesHashed << 3, padLength = bytesHashed % 64 < 56 ? 64 : 128;
              this.buffer[left] = 128;
              for (var i = left + 1; i < padLength - 8; i++)
                this.buffer[i] = 0;
              this.buffer[padLength - 8] = bitLenHi >>> 24 & 255, this.buffer[padLength - 7] = bitLenHi >>> 16 & 255, this.buffer[padLength - 6] = bitLenHi >>> 8 & 255, this.buffer[padLength - 5] = bitLenHi >>> 0 & 255, this.buffer[padLength - 4] = bitLenLo >>> 24 & 255, this.buffer[padLength - 3] = bitLenLo >>> 16 & 255, this.buffer[padLength - 2] = bitLenLo >>> 8 & 255, this.buffer[padLength - 1] = bitLenLo >>> 0 & 255, hashBlocks(this.temp, this.state, this.buffer, 0, padLength), this.finished = !0;
            }
            for (var i = 0; i < 8; i++)
              out[i * 4 + 0] = this.state[i] >>> 24 & 255, out[i * 4 + 1] = this.state[i] >>> 16 & 255, out[i * 4 + 2] = this.state[i] >>> 8 & 255, out[i * 4 + 3] = this.state[i] >>> 0 & 255;
            return this;
          }, Hash2.prototype.digest = function() {
            var out = new Uint8Array(this.digestLength);
            return this.finish(out), out;
          }, Hash2.prototype._saveState = function(out) {
            for (var i = 0; i < this.state.length; i++)
              out[i] = this.state[i];
          }, Hash2.prototype._restoreState = function(from, bytesHashed) {
            for (var i = 0; i < this.state.length; i++)
              this.state[i] = from[i];
            this.bytesHashed = bytesHashed, this.finished = !1, this.bufferLength = 0;
          }, Hash2;
        })()
      );
      exports3.Hash = Hash;
      var HMAC = (
        /** @class */
        (function() {
          function HMAC2(key) {
            this.inner = new Hash(), this.outer = new Hash(), this.blockSize = this.inner.blockSize, this.digestLength = this.inner.digestLength;
            var pad = new Uint8Array(this.blockSize);
            if (key.length > this.blockSize)
              new Hash().update(key).finish(pad).clean();
            else
              for (var i = 0; i < key.length; i++)
                pad[i] = key[i];
            for (var i = 0; i < pad.length; i++)
              pad[i] ^= 54;
            this.inner.update(pad);
            for (var i = 0; i < pad.length; i++)
              pad[i] ^= 106;
            this.outer.update(pad), this.istate = new Uint32Array(8), this.ostate = new Uint32Array(8), this.inner._saveState(this.istate), this.outer._saveState(this.ostate);
            for (var i = 0; i < pad.length; i++)
              pad[i] = 0;
          }
          return HMAC2.prototype.reset = function() {
            return this.inner._restoreState(this.istate, this.inner.blockSize), this.outer._restoreState(this.ostate, this.outer.blockSize), this;
          }, HMAC2.prototype.clean = function() {
            for (var i = 0; i < this.istate.length; i++)
              this.ostate[i] = this.istate[i] = 0;
            this.inner.clean(), this.outer.clean();
          }, HMAC2.prototype.update = function(data) {
            return this.inner.update(data), this;
          }, HMAC2.prototype.finish = function(out) {
            return this.outer.finished ? this.outer.finish(out) : (this.inner.finish(out), this.outer.update(out, this.digestLength).finish(out)), this;
          }, HMAC2.prototype.digest = function() {
            var out = new Uint8Array(this.digestLength);
            return this.finish(out), out;
          }, HMAC2;
        })()
      );
      exports3.HMAC = HMAC;
      function hash(data) {
        var h = new Hash().update(data), digest = h.digest();
        return h.clean(), digest;
      }
      exports3.hash = hash, exports3.default = hash;
      function hmac(key, data) {
        var h = new HMAC(key).update(data), digest = h.digest();
        return h.clean(), digest;
      }
      exports3.hmac = hmac;
      function fillBuffer(buffer, hmac2, info, counter) {
        var num = counter[0];
        if (num === 0)
          throw new Error("hkdf: cannot expand more");
        hmac2.reset(), num > 1 && hmac2.update(buffer), info && hmac2.update(info), hmac2.update(counter), hmac2.finish(buffer), counter[0]++;
      }
      var hkdfSalt = new Uint8Array(exports3.digestLength);
      function hkdf(key, salt, info, length) {
        salt === void 0 && (salt = hkdfSalt), length === void 0 && (length = 32);
        for (var counter = new Uint8Array([1]), okm = hmac(salt, key), hmac_ = new HMAC(okm), buffer = new Uint8Array(hmac_.digestLength), bufpos = buffer.length, out = new Uint8Array(length), i = 0; i < length; i++)
          bufpos === buffer.length && (fillBuffer(buffer, hmac_, info, counter), bufpos = 0), out[i] = buffer[bufpos++];
        return hmac_.clean(), buffer.fill(0), counter.fill(0), out;
      }
      exports3.hkdf = hkdf;
      function pbkdf2(password, salt, iterations, dkLen) {
        for (var prf = new HMAC(password), len = prf.digestLength, ctr = new Uint8Array(4), t = new Uint8Array(len), u = new Uint8Array(len), dk = new Uint8Array(dkLen), i = 0; i * len < dkLen; i++) {
          var c = i + 1;
          ctr[0] = c >>> 24 & 255, ctr[1] = c >>> 16 & 255, ctr[2] = c >>> 8 & 255, ctr[3] = c >>> 0 & 255, prf.reset(), prf.update(salt), prf.update(ctr), prf.finish(u);
          for (var j = 0; j < len; j++)
            t[j] = u[j];
          for (var j = 2; j <= iterations; j++) {
            prf.reset(), prf.update(u).finish(u);
            for (var k = 0; k < len; k++)
              t[k] ^= u[k];
          }
          for (var j = 0; j < len && i * len + j < dkLen; j++)
            dk[i * len + j] = t[j];
        }
        for (var i = 0; i < len; i++)
          t[i] = u[i] = 0;
        for (var i = 0; i < 4; i++)
          ctr[i] = 0;
        return prf.clean(), dk;
      }
      exports3.pbkdf2 = pbkdf2;
    });
  }
});

// ../../node_modules/.pnpm/standardwebhooks@1.0.0/node_modules/standardwebhooks/dist/index.js
var require_dist = __commonJS({
  "../../node_modules/.pnpm/standardwebhooks@1.0.0/node_modules/standardwebhooks/dist/index.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: !0 });
    exports2.Webhook = exports2.WebhookVerificationError = void 0;
    var timing_safe_equal_1 = require_timing_safe_equal(), base64 = require_base64(), sha256 = require_sha256(), WEBHOOK_TOLERANCE_IN_SECONDS = 300, ExtendableError = class _ExtendableError extends Error {
      constructor(message) {
        super(message), Object.setPrototypeOf(this, _ExtendableError.prototype), this.name = "ExtendableError", this.stack = new Error(message).stack;
      }
    }, WebhookVerificationError = class _WebhookVerificationError extends ExtendableError {
      constructor(message) {
        super(message), Object.setPrototypeOf(this, _WebhookVerificationError.prototype), this.name = "WebhookVerificationError";
      }
    };
    exports2.WebhookVerificationError = WebhookVerificationError;
    var Webhook2 = class _Webhook {
      constructor(secret, options) {
        if (!secret)
          throw new Error("Secret can't be empty.");
        if (options?.format === "raw")
          secret instanceof Uint8Array ? this.key = secret : this.key = Uint8Array.from(secret, (c) => c.charCodeAt(0));
        else {
          if (typeof secret != "string")
            throw new Error("Expected secret to be of type string");
          secret.startsWith(_Webhook.prefix) && (secret = secret.substring(_Webhook.prefix.length)), this.key = base64.decode(secret);
        }
      }
      verify(payload, headers_) {
        let headers = {};
        for (let key of Object.keys(headers_))
          headers[key.toLowerCase()] = headers_[key];
        let msgId = headers["webhook-id"], msgSignature = headers["webhook-signature"], msgTimestamp = headers["webhook-timestamp"];
        if (!msgSignature || !msgId || !msgTimestamp)
          throw new WebhookVerificationError("Missing required headers");
        let timestamp = this.verifyTimestamp(msgTimestamp), expectedSignature = this.sign(msgId, timestamp, payload).split(",")[1], passedSignatures = msgSignature.split(" "), encoder2 = new globalThis.TextEncoder();
        for (let versionedSignature of passedSignatures) {
          let [version, signature] = versionedSignature.split(",");
          if (version === "v1" && (0, timing_safe_equal_1.timingSafeEqual)(encoder2.encode(signature), encoder2.encode(expectedSignature)))
            return JSON.parse(payload.toString());
        }
        throw new WebhookVerificationError("No matching signature found");
      }
      sign(msgId, timestamp, payload) {
        if (typeof payload != "string")
          if (payload.constructor.name === "Buffer")
            payload = payload.toString();
          else
            throw new Error("Expected payload to be of type string or Buffer.");
        let encoder2 = new TextEncoder(), timestampNumber = Math.floor(timestamp.getTime() / 1e3), toSign = encoder2.encode(`${msgId}.${timestampNumber}.${payload}`);
        return `v1,${base64.encode(sha256.hmac(this.key, toSign))}`;
      }
      verifyTimestamp(timestampHeader) {
        let now = Math.floor(Date.now() / 1e3), timestamp = parseInt(timestampHeader, 10);
        if (isNaN(timestamp))
          throw new WebhookVerificationError("Invalid Signature Headers");
        if (now - timestamp > WEBHOOK_TOLERANCE_IN_SECONDS)
          throw new WebhookVerificationError("Message timestamp too old");
        if (timestamp > now + WEBHOOK_TOLERANCE_IN_SECONDS)
          throw new WebhookVerificationError("Message timestamp too new");
        return new Date(timestamp * 1e3);
      }
    };
    exports2.Webhook = Webhook2;
    Webhook2.prefix = "whsec_";
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/webhooks.mjs
var import_standardwebhooks, Webhooks, init_webhooks = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/webhooks.mjs"() {
    init_resource();
    import_standardwebhooks = __toESM(require_dist(), 1), Webhooks = class extends APIResource {
      unwrap(body, { headers, key }) {
        if (headers !== void 0) {
          let keyStr = key === void 0 ? this._client.webhookKey : key;
          if (keyStr === null)
            throw new Error("Webhook key must not be null in order to unwrap");
          new import_standardwebhooks.Webhook(keyStr).verify(body, headers);
        }
        return JSON.parse(body);
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/agents/versions.mjs
var Versions, init_versions = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/agents/versions.mjs"() {
    init_resource();
    init_pagination();
    init_headers();
    init_path();
    Versions = class extends APIResource {
      /**
       * List Agent Versions
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const betaManagedAgentsAgent of client.beta.agents.versions.list(
       *   'agent_011CZkYpogX7uDKUyvBTophP',
       * )) {
       *   // ...
       * }
       * ```
       */
      list(agentID, params = {}, options) {
        let { betas, ...query } = params ?? {};
        return this._client.getAPIList(path3`/v1/agents/${agentID}/versions?beta=true`, PageCursor, {
          query,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/agents/agents.mjs
var Agents, init_agents = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/agents/agents.mjs"() {
    init_resource();
    init_versions();
    init_versions();
    init_pagination();
    init_headers();
    init_path();
    Agents = class extends APIResource {
      constructor() {
        super(...arguments), this.versions = new Versions(this._client);
      }
      /**
       * Create Agent
       *
       * @example
       * ```ts
       * const betaManagedAgentsAgent =
       *   await client.beta.agents.create({
       *     model: 'claude-sonnet-4-6',
       *     name: 'My First Agent',
       *   });
       * ```
       */
      create(params, options) {
        let { betas, ...body } = params;
        return this._client.post("/v1/agents?beta=true", {
          body,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Get Agent
       *
       * @example
       * ```ts
       * const betaManagedAgentsAgent =
       *   await client.beta.agents.retrieve(
       *     'agent_011CZkYpogX7uDKUyvBTophP',
       *   );
       * ```
       */
      retrieve(agentID, params = {}, options) {
        let { betas, ...query } = params ?? {};
        return this._client.get(path3`/v1/agents/${agentID}?beta=true`, {
          query,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Update Agent
       *
       * @example
       * ```ts
       * const betaManagedAgentsAgent =
       *   await client.beta.agents.update(
       *     'agent_011CZkYpogX7uDKUyvBTophP',
       *     { version: 1 },
       *   );
       * ```
       */
      update(agentID, params, options) {
        let { betas, ...body } = params;
        return this._client.post(path3`/v1/agents/${agentID}?beta=true`, {
          body,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * List Agents
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const betaManagedAgentsAgent of client.beta.agents.list()) {
       *   // ...
       * }
       * ```
       */
      list(params = {}, options) {
        let { betas, ...query } = params ?? {};
        return this._client.getAPIList("/v1/agents?beta=true", PageCursor, {
          query,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Archive Agent
       *
       * @example
       * ```ts
       * const betaManagedAgentsAgent =
       *   await client.beta.agents.archive(
       *     'agent_011CZkYpogX7uDKUyvBTophP',
       *   );
       * ```
       */
      archive(agentID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.post(path3`/v1/agents/${agentID}/archive?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
    };
    Agents.Versions = Versions;
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/abort.mjs
function linkAbort(external, controller) {
  if (!external)
    return () => {
    };
  if (external.aborted)
    return controller.abort(), () => {
    };
  let onAbort = () => controller.abort();
  return external.addEventListener("abort", onAbort), () => external.removeEventListener("abort", onAbort);
}
var init_abort = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/abort.mjs"() {
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/backoff.mjs
function isStatus(e, code) {
  return e instanceof APIError && e.status === code;
}
function is4xx(e) {
  return e instanceof APIError && typeof e.status == "number" && e.status >= 400 && e.status < 500;
}
function isFatal4xx(e) {
  return is4xx(e) && !isStatus(e, 408) && !isStatus(e, 409) && !isStatus(e, 429);
}
function backoff(attempt, baseMs, capMs) {
  return Math.min(baseMs * 2 ** attempt, capMs);
}
function jitter(lowMs, highMs) {
  return lowMs + Math.random() * (highMs - lowMs);
}
function applyJitter(ms) {
  return ms * (1 - Math.random() * 0.25);
}
var init_backoff = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/backoff.mjs"() {
    init_error();
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/helper-client.mjs
function copyClientForHelper(client, { authToken, helper }) {
  if (!authToken)
    throw new AnthropicError(`copyClientForHelper: expected a non-empty authToken but received ${JSON.stringify(authToken)}`);
  let internal = client, parentDefaults = internal._options.defaultHeaders, parentAuthExtraHeaders = internal._authState?.extraHeaders, inheritedAuthExtraHeaders = parentAuthExtraHeaders ? Object.fromEntries(Object.entries(parentAuthExtraHeaders).filter(([name]) => {
    let lower = name.toLowerCase();
    return lower !== "authorization" && lower !== "x-api-key";
  })) : void 0, defaultHeaders = buildHeaders([
    inheritedAuthExtraHeaders,
    parentDefaults,
    { [STAINLESS_HELPER_HEADER]: helper }
  ]);
  return client.withOptions({
    apiKey: null,
    authToken,
    baseURL: client.baseURL,
    credentials: void 0,
    defaultHeaders
  });
}
var init_helper_client = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/helper-client.mjs"() {
    init_error();
    init_headers();
    init_stainless_helper_header();
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/environments/poller.mjs
function backoff2(attempt) {
  return backoff(attempt, POLL_BACKOFF_BASE_MS, POLL_BACKOFF_CAP_MS);
}
function defaultWorkerId() {
  let host = globalThis.process?.env?.HOSTNAME;
  return host ? `${host}-${uuid4()}` : uuid4();
}
var _WorkPoller_runnerClient, _WorkPoller_consumed, _WorkPoller_controller, _WorkPoller_detachExternal, _WorkPoller_autoStop, _WorkPoller_drain, _WorkPoller_blockMs, _WorkPoller_reclaimOlderThanMs, _WorkPoller_requestOpts, POLL_BLOCK_MS, POLL_BACKOFF_BASE_MS, POLL_BACKOFF_CAP_MS, WorkPoller, init_poller = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/environments/poller.mjs"() {
    init_tslib();
    init_error();
    init_log();
    init_sleep();
    init_uuid();
    init_abort();
    init_headers();
    init_backoff();
    init_helper_client();
    init_backoff();
    POLL_BLOCK_MS = 999, POLL_BACKOFF_BASE_MS = 1e3, POLL_BACKOFF_CAP_MS = 6e4, WorkPoller = class {
      constructor(opts) {
        _WorkPoller_runnerClient.set(this, void 0), _WorkPoller_consumed.set(this, !1), _WorkPoller_controller.set(this, void 0), _WorkPoller_detachExternal.set(this, void 0), _WorkPoller_autoStop.set(this, void 0), _WorkPoller_drain.set(this, void 0), _WorkPoller_blockMs.set(this, void 0), _WorkPoller_reclaimOlderThanMs.set(this, void 0), _WorkPoller_requestOpts.set(this, void 0), this.client = opts.client, this.environmentId = opts.environmentId, this.environmentKey = opts.environmentKey, this.workerId = opts.workerId ?? defaultWorkerId(), __classPrivateFieldSet(this, _WorkPoller_runnerClient, copyClientForHelper(opts.client, {
          authToken: opts.environmentKey,
          helper: "environments-work-poller"
        }), "f"), __classPrivateFieldSet(this, _WorkPoller_autoStop, opts.autoStop ?? !0, "f"), __classPrivateFieldSet(this, _WorkPoller_drain, opts.drain ?? !1, "f"), __classPrivateFieldSet(this, _WorkPoller_blockMs, opts.blockMs === void 0 ? POLL_BLOCK_MS : opts.blockMs, "f"), __classPrivateFieldSet(this, _WorkPoller_reclaimOlderThanMs, opts.reclaimOlderThanMs ?? null, "f"), __classPrivateFieldSet(this, _WorkPoller_requestOpts, opts.requestOptions, "f"), __classPrivateFieldSet(this, _WorkPoller_controller, new AbortController(), "f"), __classPrivateFieldSet(this, _WorkPoller_detachExternal, linkAbort(opts.signal, __classPrivateFieldGet(this, _WorkPoller_controller, "f")), "f");
      }
      /** Read-only view of this iterator's abort signal. */
      get signal() {
        return __classPrivateFieldGet(this, _WorkPoller_controller, "f").signal;
      }
      /** Abort the iterator. The current `for await` will exit cleanly. */
      abort() {
        __classPrivateFieldGet(this, _WorkPoller_controller, "f").abort();
      }
      async *[(_WorkPoller_runnerClient = /* @__PURE__ */ new WeakMap(), _WorkPoller_consumed = /* @__PURE__ */ new WeakMap(), _WorkPoller_controller = /* @__PURE__ */ new WeakMap(), _WorkPoller_detachExternal = /* @__PURE__ */ new WeakMap(), _WorkPoller_autoStop = /* @__PURE__ */ new WeakMap(), _WorkPoller_drain = /* @__PURE__ */ new WeakMap(), _WorkPoller_blockMs = /* @__PURE__ */ new WeakMap(), _WorkPoller_reclaimOlderThanMs = /* @__PURE__ */ new WeakMap(), _WorkPoller_requestOpts = /* @__PURE__ */ new WeakMap(), Symbol.asyncIterator)]() {
        if (__classPrivateFieldGet(this, _WorkPoller_consumed, "f"))
          throw new AnthropicError("Cannot iterate over a consumed WorkPoller");
        __classPrivateFieldSet(this, _WorkPoller_consumed, !0, "f");
        let log = loggerFor(this.client);
        log.info("poller starting", {
          component: "work-poller",
          environment_id: this.environmentId
        });
        try {
          let attempt = 0;
          for (; !__classPrivateFieldGet(this, _WorkPoller_controller, "f").signal.aborted; ) {
            let work;
            try {
              work = await __classPrivateFieldGet(this, _WorkPoller_runnerClient, "f").beta.environments.work.poll(this.environmentId, {
                "Anthropic-Worker-ID": this.workerId,
                ...__classPrivateFieldGet(this, _WorkPoller_blockMs, "f") !== null ? { block_ms: __classPrivateFieldGet(this, _WorkPoller_blockMs, "f") } : {},
                ...__classPrivateFieldGet(this, _WorkPoller_reclaimOlderThanMs, "f") !== null ? { reclaim_older_than_ms: __classPrivateFieldGet(this, _WorkPoller_reclaimOlderThanMs, "f") } : {}
              }, { headers: buildHeaders([__classPrivateFieldGet(this, _WorkPoller_requestOpts, "f")?.headers]), signal: __classPrivateFieldGet(this, _WorkPoller_controller, "f").signal });
            } catch (e) {
              if (__classPrivateFieldGet(this, _WorkPoller_controller, "f").signal.aborted)
                return;
              if (isFatal4xx(e))
                throw log.error("poll failed permanently, stopping poller", { error: String(e) }), e;
              let wait = applyJitter(backoff2(attempt));
              log.warn("poll failed, backing off", { error: String(e), backoff_ms: wait }), attempt++, await sleep(wait, __classPrivateFieldGet(this, _WorkPoller_controller, "f").signal);
              continue;
            }
            if (attempt = 0, work == null) {
              if (__classPrivateFieldGet(this, _WorkPoller_drain, "f"))
                return;
              await sleep(jitter(1e3, 3e3), __classPrivateFieldGet(this, _WorkPoller_controller, "f").signal);
              continue;
            }
            log.info("claimed work", {
              component: "work-poller",
              environment_id: this.environmentId,
              work_id: work.id,
              work_type: work.data.type
            });
            try {
              await __classPrivateFieldGet(this, _WorkPoller_runnerClient, "f").beta.environments.work.ack(work.id, { environment_id: work.environment_id }, { headers: buildHeaders([__classPrivateFieldGet(this, _WorkPoller_requestOpts, "f")?.headers]), signal: __classPrivateFieldGet(this, _WorkPoller_controller, "f").signal });
            } catch (e) {
              log.error("ack failed", { work_id: work.id, error: String(e) });
              continue;
            }
            try {
              yield work;
            } finally {
              if (__classPrivateFieldGet(this, _WorkPoller_autoStop, "f"))
                try {
                  await __classPrivateFieldGet(this, _WorkPoller_runnerClient, "f").beta.environments.work.stop(work.id, { environment_id: work.environment_id }, { headers: buildHeaders([__classPrivateFieldGet(this, _WorkPoller_requestOpts, "f")?.headers]) });
                } catch (e) {
                  isStatus(e, 409) || log.warn("stop failed", { work_id: work.id, error: String(e) });
                }
            }
          }
        } finally {
          __classPrivateFieldGet(this, _WorkPoller_detachExternal, "f").call(this);
        }
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/async-queue.mjs
var _AsyncQueue_items, _AsyncQueue_waiters, _AsyncQueue_closed, AsyncQueue, init_async_queue = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/async-queue.mjs"() {
    init_tslib();
    AsyncQueue = class {
      constructor() {
        _AsyncQueue_items.set(this, []), _AsyncQueue_waiters.set(this, []), _AsyncQueue_closed.set(this, !1);
      }
      /** Enqueue an item, or hand it directly to a waiting reader. Returns `false` once closed. */
      push(item) {
        if (__classPrivateFieldGet(this, _AsyncQueue_closed, "f"))
          return !1;
        let w = __classPrivateFieldGet(this, _AsyncQueue_waiters, "f").shift();
        return w ? w({ done: !1, value: item }) : __classPrivateFieldGet(this, _AsyncQueue_items, "f").push(item), !0;
      }
      /** Mark the queue done. Idempotent; wakes every pending reader with `done: true`. */
      close() {
        if (!__classPrivateFieldGet(this, _AsyncQueue_closed, "f"))
          for (__classPrivateFieldSet(this, _AsyncQueue_closed, !0, "f"); __classPrivateFieldGet(this, _AsyncQueue_waiters, "f").length > 0; )
            __classPrivateFieldGet(this, _AsyncQueue_waiters, "f").shift()({ done: !0, value: void 0 });
      }
      /**
       * Resolve with the next item, or `done: true` once the queue is closed and
       * drained. When `signal` is supplied, aborting it resolves a pending read
       * with `done: true` (cancellation is pushed down here rather than handled by
       * an outer `Promise.race`).
       */
      next(signal) {
        return __classPrivateFieldGet(this, _AsyncQueue_items, "f").length > 0 ? Promise.resolve({ done: !1, value: __classPrivateFieldGet(this, _AsyncQueue_items, "f").shift() }) : __classPrivateFieldGet(this, _AsyncQueue_closed, "f") || signal?.aborted ? Promise.resolve({ done: !0, value: void 0 }) : new Promise((resolve4) => {
          let waiter = (r) => {
            signal?.removeEventListener("abort", onAbort), resolve4(r);
          }, onAbort = () => {
            let idx = __classPrivateFieldGet(this, _AsyncQueue_waiters, "f").indexOf(waiter);
            idx >= 0 && __classPrivateFieldGet(this, _AsyncQueue_waiters, "f").splice(idx, 1), resolve4({ done: !0, value: void 0 });
          };
          __classPrivateFieldGet(this, _AsyncQueue_waiters, "f").push(waiter), signal?.addEventListener("abort", onAbort, { once: !0 });
        });
      }
      /** Synchronously remove and return the next buffered item, or `undefined` if empty. */
      tryShift() {
        return __classPrivateFieldGet(this, _AsyncQueue_items, "f").shift();
      }
    };
    _AsyncQueue_items = /* @__PURE__ */ new WeakMap(), _AsyncQueue_waiters = /* @__PURE__ */ new WeakMap(), _AsyncQueue_closed = /* @__PURE__ */ new WeakMap();
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/tools/ToolError.mjs
var ToolError, init_ToolError = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/tools/ToolError.mjs"() {
    ToolError = class extends Error {
      constructor(content) {
        let message = typeof content == "string" ? content : content.map((block) => block.type === "text" ? block.text : `[${block.type}]`).join(" ");
        super(message), this.name = "ToolError", this.content = content;
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/tools/BetaRunnableTool.mjs
function toolName(tool) {
  return "name" in tool ? tool.name : tool.mcp_server_name;
}
function toolErrorContent(e) {
  return e instanceof ToolError ? e.content : `Error: ${e instanceof Error ? e.message : String(e)}`;
}
async function runRunnableTool(tool, rawInput, context) {
  try {
    let input = tool.parse ? tool.parse(rawInput) : rawInput;
    return { content: await tool.run(input, context), isError: !1 };
  } catch (e) {
    return { content: toolErrorContent(e), isError: !0 };
  }
}
var init_BetaRunnableTool = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/tools/BetaRunnableTool.mjs"() {
    init_ToolError();
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/tools/SessionToolRunner.mjs
function isEndTurnIdle(ev) {
  return ev.type === "session.status_idle" && ev.stop_reason?.type === "end_turn";
}
function buildResultEvent(ev, isError, content) {
  return ev.type === "agent.custom_tool_use" ? { type: "user.custom_tool_result", custom_tool_use_id: ev.id, is_error: isError, content } : { type: "user.tool_result", tool_use_id: ev.id, is_error: isError, content };
}
function toSessionContent(content) {
  if (typeof content == "string")
    return [{ type: "text", text: content || "(no output)" }];
  let out = content.map((b) => b.type === "text" ? { type: "text", text: b.text || "(no output)" } : b.type === "image" || b.type === "document" ? b : b.type === "search_result" ? {
    type: "search_result",
    source: b.source,
    title: b.title,
    content: b.content.map((c) => ({ type: "text", text: c.text })),
    citations: { enabled: b.citations?.enabled ?? !1 }
  } : { type: "text", text: JSON.stringify(b) });
  return out.length > 0 ? out : [{ type: "text", text: "(no output)" }];
}
var _SessionToolRunner_instances, _SessionToolRunner_consumed, _SessionToolRunner_controller, _SessionToolRunner_detachExternal, _SessionToolRunner_requestOpts, _SessionToolRunner_toolByName, _SessionToolRunner_logger, _SessionToolRunner_seen, _SessionToolRunner_answered, _SessionToolRunner_results, _SessionToolRunner_inFlightCount, _SessionToolRunner_onIdle, _SessionToolRunner_idleTimer, _SessionToolRunner_requestOptions, _SessionToolRunner_streamLoop, _SessionToolRunner_reconcile, _SessionToolRunner_ingestHistory, _SessionToolRunner_handleStreamEvent, _SessionToolRunner_armIdleTimer, _SessionToolRunner_disarmIdleTimer, _SessionToolRunner_execute, _SessionToolRunner_sendResult, _SessionToolRunner_drain, STREAM_BACKOFF_START_MS, STREAM_BACKOFF_CAP_MS, TOOL_TIMEOUT_MS, DRAIN_TIMEOUT_MS, SEND_RETRIES, DEFAULT_MAX_IDLE_MS, SessionToolRunner, init_SessionToolRunner = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/tools/SessionToolRunner.mjs"() {
    init_tslib();
    init_error();
    init_log();
    init_sleep();
    init_backoff();
    init_abort();
    init_async_queue();
    init_headers();
    init_stainless_helper_header();
    init_BetaRunnableTool();
    STREAM_BACKOFF_START_MS = 500, STREAM_BACKOFF_CAP_MS = 1e4, TOOL_TIMEOUT_MS = 12e4, DRAIN_TIMEOUT_MS = 3e4, SEND_RETRIES = 3, DEFAULT_MAX_IDLE_MS = 6e4;
    SessionToolRunner = class {
      constructor(sessionId, opts) {
        _SessionToolRunner_instances.add(this), _SessionToolRunner_consumed.set(this, !1), _SessionToolRunner_controller.set(this, void 0), _SessionToolRunner_detachExternal.set(this, void 0), _SessionToolRunner_requestOpts.set(this, void 0), _SessionToolRunner_toolByName.set(this, void 0), _SessionToolRunner_logger.set(this, void 0), _SessionToolRunner_seen.set(this, /* @__PURE__ */ new Set()), _SessionToolRunner_answered.set(this, /* @__PURE__ */ new Set()), _SessionToolRunner_results.set(this, new AsyncQueue()), _SessionToolRunner_inFlightCount.set(this, 0), _SessionToolRunner_onIdle.set(this, null), _SessionToolRunner_idleTimer.set(this, void 0), this.client = opts.client, this.sessionId = sessionId, this.tools = opts.tools, this.maxIdleMs = opts.maxIdleMs ?? DEFAULT_MAX_IDLE_MS, __classPrivateFieldSet(this, _SessionToolRunner_logger, loggerFor(opts.client), "f"), __classPrivateFieldSet(this, _SessionToolRunner_toolByName, new Map(opts.tools.map((t) => [toolName(t), t])), "f"), __classPrivateFieldSet(this, _SessionToolRunner_controller, new AbortController(), "f"), __classPrivateFieldSet(this, _SessionToolRunner_detachExternal, linkAbort(opts.signal, __classPrivateFieldGet(this, _SessionToolRunner_controller, "f")), "f"), __classPrivateFieldSet(this, _SessionToolRunner_requestOpts, opts.requestOptions, "f");
      }
      /** Read-only view of this runner's abort signal. */
      get signal() {
        return __classPrivateFieldGet(this, _SessionToolRunner_controller, "f").signal;
      }
      /** Abort the runner. Background tasks will wind down and `for await` will exit cleanly. */
      abort() {
        __classPrivateFieldGet(this, _SessionToolRunner_controller, "f").abort();
      }
      async *[(_SessionToolRunner_consumed = /* @__PURE__ */ new WeakMap(), _SessionToolRunner_controller = /* @__PURE__ */ new WeakMap(), _SessionToolRunner_detachExternal = /* @__PURE__ */ new WeakMap(), _SessionToolRunner_requestOpts = /* @__PURE__ */ new WeakMap(), _SessionToolRunner_toolByName = /* @__PURE__ */ new WeakMap(), _SessionToolRunner_logger = /* @__PURE__ */ new WeakMap(), _SessionToolRunner_seen = /* @__PURE__ */ new WeakMap(), _SessionToolRunner_answered = /* @__PURE__ */ new WeakMap(), _SessionToolRunner_results = /* @__PURE__ */ new WeakMap(), _SessionToolRunner_inFlightCount = /* @__PURE__ */ new WeakMap(), _SessionToolRunner_onIdle = /* @__PURE__ */ new WeakMap(), _SessionToolRunner_idleTimer = /* @__PURE__ */ new WeakMap(), _SessionToolRunner_instances = /* @__PURE__ */ new WeakSet(), Symbol.asyncIterator)]() {
        if (__classPrivateFieldGet(this, _SessionToolRunner_consumed, "f"))
          throw new AnthropicError("Cannot iterate over a consumed SessionToolRunner");
        __classPrivateFieldSet(this, _SessionToolRunner_consumed, !0, "f"), __classPrivateFieldGet(this, _SessionToolRunner_logger, "f").info("session tool runner starting", {
          component: "session-tool-runner",
          session_id: this.sessionId
        });
        let streamPromise = __classPrivateFieldGet(this, _SessionToolRunner_instances, "m", _SessionToolRunner_streamLoop).call(this).catch((e) => {
          __classPrivateFieldGet(this, _SessionToolRunner_controller, "f").signal.aborted || __classPrivateFieldGet(this, _SessionToolRunner_logger, "f").error("stream loop failed", { error: String(e) }), __classPrivateFieldGet(this, _SessionToolRunner_controller, "f").abort();
        });
        try {
          for (; ; ) {
            let next = await __classPrivateFieldGet(this, _SessionToolRunner_results, "f").next(__classPrivateFieldGet(this, _SessionToolRunner_controller, "f").signal);
            if (next.done)
              break;
            yield next.value;
          }
          await streamPromise;
          let pending;
          for (; (pending = __classPrivateFieldGet(this, _SessionToolRunner_results, "f").tryShift()) !== void 0; )
            yield pending;
        } finally {
          __classPrivateFieldGet(this, _SessionToolRunner_controller, "f").abort(), __classPrivateFieldGet(this, _SessionToolRunner_instances, "m", _SessionToolRunner_disarmIdleTimer).call(this), await streamPromise;
          try {
            await __classPrivateFieldGet(this, _SessionToolRunner_instances, "m", _SessionToolRunner_drain).call(this);
          } catch (e) {
            __classPrivateFieldGet(this, _SessionToolRunner_logger, "f").warn("drain failed", { error: String(e) });
          }
          __classPrivateFieldGet(this, _SessionToolRunner_results, "f").close();
          for (let t of this.tools)
            try {
              await t.close?.();
            } catch (e) {
              __classPrivateFieldGet(this, _SessionToolRunner_logger, "f").warn("tool.close failed", { tool: toolName(t), error: String(e) });
            }
          __classPrivateFieldGet(this, _SessionToolRunner_detachExternal, "f").call(this);
        }
      }
    };
    _SessionToolRunner_requestOptions = function() {
      return {
        ...__classPrivateFieldGet(this, _SessionToolRunner_requestOpts, "f"),
        headers: buildHeaders([helperHeader("session-tool-runner"), __classPrivateFieldGet(this, _SessionToolRunner_requestOpts, "f")?.headers]),
        signal: __classPrivateFieldGet(this, _SessionToolRunner_controller, "f").signal
      };
    }, _SessionToolRunner_streamLoop = // ===== event stream =====
    async function() {
      let ctrl = __classPrivateFieldGet(this, _SessionToolRunner_controller, "f"), backoff3 = STREAM_BACKOFF_START_MS;
      for (; !ctrl.signal.aborted; ) {
        try {
          let stream = await this.client.beta.sessions.events.stream(this.sessionId, {}, __classPrivateFieldGet(this, _SessionToolRunner_instances, "m", _SessionToolRunner_requestOptions).call(this));
          await __classPrivateFieldGet(this, _SessionToolRunner_instances, "m", _SessionToolRunner_reconcile).call(this);
          for await (let ev of stream)
            if (backoff3 = STREAM_BACKOFF_START_MS, await __classPrivateFieldGet(this, _SessionToolRunner_instances, "m", _SessionToolRunner_handleStreamEvent).call(this, ev))
              return;
        } catch (e) {
          if (ctrl.signal.throwIfAborted(), isFatal4xx(e))
            throw __classPrivateFieldGet(this, _SessionToolRunner_logger, "f").error("permanent stream failure, shutting down", { error: String(e) }), ctrl.abort(), e;
          __classPrivateFieldGet(this, _SessionToolRunner_logger, "f").warn("stream disconnected, reconnecting", {
            error: String(e),
            backoff_ms: backoff3
          });
        }
        ctrl.signal.throwIfAborted(), await sleep(backoff3, ctrl.signal), backoff3 = Math.min(backoff3 * 2, STREAM_BACKOFF_CAP_MS);
      }
    }, _SessionToolRunner_reconcile = /**
     * Read full history before dispatching so a `tool_use` whose result appears
     * later in the same history is not re-executed. Runs after the live stream is
     * already attached (see {@link SessionToolRunner.#streamLoop}).
     */
    async function() {
      let ctrl = __classPrivateFieldGet(this, _SessionToolRunner_controller, "f"), pending = [], lastWasEndTurn = !1;
      try {
        for await (let ev of this.client.beta.sessions.events.list(this.sessionId, { limit: 1e3 }, __classPrivateFieldGet(this, _SessionToolRunner_instances, "m", _SessionToolRunner_requestOptions).call(this)))
          __classPrivateFieldGet(this, _SessionToolRunner_instances, "m", _SessionToolRunner_ingestHistory).call(this, ev, pending), lastWasEndTurn = isEndTurnIdle(ev);
      } catch (e) {
        ctrl.signal.throwIfAborted(), __classPrivateFieldGet(this, _SessionToolRunner_logger, "f").warn("reconcile list failed", { error: String(e) });
        for (let ev of pending)
          __classPrivateFieldGet(this, _SessionToolRunner_seen, "f").delete(ev.id);
        return;
      }
      let unanswered = pending.filter((ev) => !__classPrivateFieldGet(this, _SessionToolRunner_answered, "f").has(ev.id));
      lastWasEndTurn && unanswered.length === 0 ? __classPrivateFieldGet(this, _SessionToolRunner_instances, "m", _SessionToolRunner_armIdleTimer).call(this) : __classPrivateFieldGet(this, _SessionToolRunner_instances, "m", _SessionToolRunner_disarmIdleTimer).call(this);
      for (let ev of unanswered)
        await __classPrivateFieldGet(this, _SessionToolRunner_instances, "m", _SessionToolRunner_execute).call(this, ev);
    }, _SessionToolRunner_ingestHistory = function(ev, pending) {
      ev.type === "agent.tool_use" || ev.type === "agent.custom_tool_use" ? (__classPrivateFieldGet(this, _SessionToolRunner_seen, "f").add(ev.id), __classPrivateFieldGet(this, _SessionToolRunner_answered, "f").has(ev.id) || pending.push(ev)) : ev.type === "user.tool_result" ? __classPrivateFieldGet(this, _SessionToolRunner_answered, "f").add(ev.tool_use_id) : ev.type === "user.custom_tool_result" && __classPrivateFieldGet(this, _SessionToolRunner_answered, "f").add(ev.custom_tool_use_id);
    }, _SessionToolRunner_handleStreamEvent = /** Returns true when the runner should exit. */
    async function(ev) {
      switch (isEndTurnIdle(ev) ? __classPrivateFieldGet(this, _SessionToolRunner_instances, "m", _SessionToolRunner_armIdleTimer).call(this) : __classPrivateFieldGet(this, _SessionToolRunner_instances, "m", _SessionToolRunner_disarmIdleTimer).call(this), ev.type) {
        case "agent.tool_use":
        case "agent.custom_tool_use":
          return __classPrivateFieldGet(this, _SessionToolRunner_seen, "f").has(ev.id) || (__classPrivateFieldGet(this, _SessionToolRunner_seen, "f").add(ev.id), await __classPrivateFieldGet(this, _SessionToolRunner_instances, "m", _SessionToolRunner_execute).call(this, ev)), !1;
        case "user.tool_result":
          return __classPrivateFieldGet(this, _SessionToolRunner_answered, "f").add(ev.tool_use_id), !1;
        case "user.custom_tool_result":
          return __classPrivateFieldGet(this, _SessionToolRunner_answered, "f").add(ev.custom_tool_use_id), !1;
        case "session.status_terminated":
        case "session.deleted":
          return __classPrivateFieldGet(this, _SessionToolRunner_logger, "f").info("session terminated", {
            component: "session-tool-runner",
            session_id: this.sessionId
          }), __classPrivateFieldGet(this, _SessionToolRunner_controller, "f").abort(), !0;
        default:
          return !1;
      }
    }, _SessionToolRunner_armIdleTimer = function() {
      __classPrivateFieldGet(this, _SessionToolRunner_instances, "m", _SessionToolRunner_disarmIdleTimer).call(this), !(this.maxIdleMs <= 0) && __classPrivateFieldSet(this, _SessionToolRunner_idleTimer, setTimeout(() => {
        __classPrivateFieldGet(this, _SessionToolRunner_logger, "f").info("session idle after end_turn; stopping", {
          component: "session-tool-runner",
          session_id: this.sessionId,
          max_idle_ms: this.maxIdleMs
        }), __classPrivateFieldGet(this, _SessionToolRunner_controller, "f").abort();
      }, this.maxIdleMs), "f");
    }, _SessionToolRunner_disarmIdleTimer = function() {
      __classPrivateFieldGet(this, _SessionToolRunner_idleTimer, "f") !== void 0 && (clearTimeout(__classPrivateFieldGet(this, _SessionToolRunner_idleTimer, "f")), __classPrivateFieldSet(this, _SessionToolRunner_idleTimer, void 0, "f"));
    }, _SessionToolRunner_execute = // ===== tool execution =====
    async function(ev) {
      var _a5, _b;
      if (!__classPrivateFieldGet(this, _SessionToolRunner_answered, "f").has(ev.id)) {
        __classPrivateFieldGet(this, _SessionToolRunner_logger, "f").info("executing tool", {
          component: "session-tool-runner",
          session_id: this.sessionId,
          tool: ev.name,
          tool_use_id: ev.id
        }), __classPrivateFieldSet(this, _SessionToolRunner_inFlightCount, (_a5 = __classPrivateFieldGet(this, _SessionToolRunner_inFlightCount, "f"), _a5++, _a5), "f");
        try {
          let tool = __classPrivateFieldGet(this, _SessionToolRunner_toolByName, "f").get(ev.name);
          if (!tool) {
            __classPrivateFieldGet(this, _SessionToolRunner_logger, "f").info("tool not owned by this runner; leaving the tool_use_id pending for its owner", {
              component: "session-tool-runner",
              session_id: this.sessionId,
              tool: ev.name,
              tool_use_id: ev.id
            }), __classPrivateFieldGet(this, _SessionToolRunner_results, "f").push({ event: ev, toolUseId: ev.id, name: ev.name, isError: !1, posted: !1 });
            return;
          }
          let content, isError, toolCtrl = new AbortController(), detachTool = linkAbort(__classPrivateFieldGet(this, _SessionToolRunner_controller, "f").signal, toolCtrl), timer = setTimeout(() => toolCtrl.abort(), TOOL_TIMEOUT_MS);
          try {
            let outcome = await runRunnableTool(tool, ev.input, {
              toolUse: ev,
              toolUseBlock: ev,
              signal: toolCtrl.signal
            });
            content = outcome.content, isError = outcome.isError;
          } finally {
            clearTimeout(timer), detachTool();
          }
          let result = buildResultEvent(ev, isError, toSessionContent(content)), posted = await __classPrivateFieldGet(this, _SessionToolRunner_instances, "m", _SessionToolRunner_sendResult).call(this, result, ev.id);
          __classPrivateFieldGet(this, _SessionToolRunner_results, "f").push({
            event: ev,
            result,
            toolUseId: ev.id,
            name: ev.name,
            isError,
            posted
          });
        } finally {
          __classPrivateFieldSet(this, _SessionToolRunner_inFlightCount, (_b = __classPrivateFieldGet(this, _SessionToolRunner_inFlightCount, "f"), _b--, _b), "f"), __classPrivateFieldGet(this, _SessionToolRunner_inFlightCount, "f") === 0 && __classPrivateFieldGet(this, _SessionToolRunner_onIdle, "f")?.call(this);
        }
      }
    }, _SessionToolRunner_sendResult = async function(result, toolUseId) {
      let ctrl = __classPrivateFieldGet(this, _SessionToolRunner_controller, "f"), lastErr;
      for (let i = 0; i < SEND_RETRIES; i++) {
        ctrl.signal.throwIfAborted();
        try {
          return await this.client.beta.sessions.events.send(this.sessionId, { events: [result] }, __classPrivateFieldGet(this, _SessionToolRunner_instances, "m", _SessionToolRunner_requestOptions).call(this)), __classPrivateFieldGet(this, _SessionToolRunner_answered, "f").add(toolUseId), !0;
        } catch (e) {
          if (lastErr = e, isFatal4xx(e))
            break;
          i < SEND_RETRIES - 1 && await sleep((i + 1) * 1e3, ctrl.signal);
        }
      }
      return __classPrivateFieldGet(this, _SessionToolRunner_logger, "f").error("failed to send tool result", {
        tool_use_id: toolUseId,
        error: String(lastErr)
      }), !1;
    }, _SessionToolRunner_drain = /** Wait (bounded) for in-flight tool executions to finish during teardown. */
    async function() {
      __classPrivateFieldGet(this, _SessionToolRunner_inFlightCount, "f") !== 0 && (await Promise.race([new Promise((r) => __classPrivateFieldSet(this, _SessionToolRunner_onIdle, r, "f")), sleep(DRAIN_TIMEOUT_MS)]), __classPrivateFieldSet(this, _SessionToolRunner_onIdle, null, "f"), __classPrivateFieldGet(this, _SessionToolRunner_inFlightCount, "f") > 0 && __classPrivateFieldGet(this, _SessionToolRunner_logger, "f").warn("drain timeout exceeded"));
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/transform-json-schema.mjs
var init_transform_json_schema = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/transform-json-schema.mjs"() {
    init_utils2();
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/helpers/beta/json-schema.mjs
function betaTool(options) {
  if (options.inputSchema.type !== "object")
    throw new Error(`JSON schema for tool "${options.name}" must be an object, but got ${options.inputSchema.type}`);
  return {
    type: "custom",
    name: options.name,
    input_schema: options.inputSchema,
    description: options.description,
    run: options.run,
    parse: (content) => content,
    ...options.close ? { close: options.close } : {}
  };
}
var init_json_schema = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/helpers/beta/json-schema.mjs"() {
    init_sdk();
    init_transform_json_schema();
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/promise.mjs
function promiseWithResolvers() {
  let resolve4, reject;
  return { promise: new Promise((res, rej) => {
    resolve4 = res, reject = rej;
  }), resolve: resolve4, reject };
}
var init_promise = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/utils/promise.mjs"() {
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/tools/agent-toolset/fs-util.mjs
async function realpathOrSelf(p) {
  try {
    return await fs2.realpath(p);
  } catch {
    return p;
  }
}
async function canonicalize(abs) {
  let tail = [], prefix = abs, hops = 0;
  for (; ; ) {
    let real;
    try {
      real = await fs2.realpath(prefix);
    } catch {
      let isLink = !1;
      try {
        isLink = (await fs2.lstat(prefix)).isSymbolicLink();
      } catch {
      }
      if (isLink) {
        if (++hops > 40)
          throw new ToolError(`path ${JSON.stringify(abs)} has too many levels of symbolic links`);
        prefix = path4.resolve(path4.dirname(prefix), await fs2.readlink(prefix));
        continue;
      }
      let parent = path4.dirname(prefix);
      if (parent === prefix)
        return abs;
      tail.push(path4.basename(prefix)), prefix = parent;
      continue;
    }
    return tail.length ? path4.join(real, ...tail.reverse()) : real;
  }
}
async function confineToRoot(root, p, opts) {
  let allowOutside = opts?.allowOutside ?? !1, realRoot = await realpathOrSelf(path4.resolve(root)), abs = path4.resolve(realRoot, p);
  if (allowOutside)
    return abs;
  let real = await canonicalize(abs);
  if (real !== realRoot && !real.startsWith(realRoot + path4.sep))
    throw new ToolError(`path ${JSON.stringify(p)} escapes workdir`);
  return real;
}
async function atomicWriteFile(targetPath, content) {
  let dir = path4.dirname(targetPath), tempPath = path4.join(dir, `.tmp-${process.pid}-${(0, import_node_crypto.randomUUID)()}`), handle;
  try {
    handle = await fs2.open(tempPath, "wx", FILE_CREATE_MODE), await handle.writeFile(content, "utf-8"), await handle.sync(), await handle.close(), handle = void 0, await fs2.rename(tempPath, targetPath);
  } catch (err) {
    throw handle && await handle.close().catch(() => {
    }), await fs2.unlink(tempPath).catch(() => {
    }), err;
  }
}
function fsErrorMessage(err, file) {
  switch (err?.code) {
    case "ENOENT":
      return `${file}: no such file or directory`;
    case "EACCES":
    case "EPERM":
      return `${file}: permission denied`;
    case "ENOTDIR":
      return `${file}: not a directory`;
    case "EISDIR":
      return `${file}: is a directory`;
    case "ELOOP":
      return `${file}: too many levels of symbolic links`;
    case "ENAMETOOLONG":
      return `${file}: file name too long`;
    case "ENOSPC":
      return `${file}: no space left on device`;
    case "EMFILE":
    case "ENFILE":
      return `${file}: too many open files`;
    default:
      return `${file}: ${err instanceof Error ? err.message : String(err)}`;
  }
}
var fs2, path4, import_node_crypto, DIR_CREATE_MODE, FILE_CREATE_MODE, init_fs_util = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/tools/agent-toolset/fs-util.mjs"() {
    fs2 = __toESM(require("node:fs/promises"), 1), path4 = __toESM(require("node:path"), 1), import_node_crypto = require("node:crypto");
    init_ToolError();
    DIR_CREATE_MODE = 493, FILE_CREATE_MODE = 420;
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/tools/agent-toolset/skills.mjs
async function setupSkills(ctx) {
  let { client, sessionId } = ctx;
  if (!client || !sessionId)
    return async () => {
    };
  let log = loggerFor(client), session = await client.beta.sessions.retrieve(sessionId), skillsRoot = path5.resolve(ctx.workdir, "skills"), created = [];
  for (let skill of session.agent.skills)
    try {
      let versionId = await resolveSkillVersion(client, skill.skill_id, skill.version), version = await client.beta.skills.versions.retrieve(versionId, { skill_id: skill.skill_id }), dirname11 = path5.basename(version.name.trim());
      (dirname11 === "" || dirname11 === "." || dirname11 === "..") && (dirname11 = skill.skill_id);
      let dest = path5.resolve(skillsRoot, dirname11);
      if (dest !== skillsRoot && !dest.startsWith(skillsRoot + path5.sep)) {
        log.warn("skill name escapes the skills dir; skipping", {
          component: "agent-tool-context",
          name: version.name
        });
        continue;
      }
      let resp = await client.beta.skills.versions.download(versionId, { skill_id: skill.skill_id });
      await fs3.rm(dest, { recursive: !0, force: !0 }), await fs3.mkdir(dest, { recursive: !0, mode: DIR_CREATE_MODE }), created.push(dest), await extractSkillArchive(resp, dest), log.info("downloaded skill", {
        component: "agent-tool-context",
        skill_id: skill.skill_id,
        version: versionId,
        dest
      });
    } catch (e) {
      log.warn("failed to download skill", {
        component: "agent-tool-context",
        skill_id: skill.skill_id,
        error: String(e)
      });
    }
  return async () => {
    for (let dest of created)
      await fs3.rm(dest, { recursive: !0, force: !0 }).catch((e) => {
        log.warn("failed to clean up skill", { component: "agent-tool-context", dest, error: String(e) });
      });
  };
}
async function resolveSkillVersion(client, skillId, version) {
  if (/^\d+$/.test(version))
    return version;
  let newest;
  for await (let v of client.beta.skills.versions.list(skillId))
    /^\d+$/.test(v.version) && (newest === void 0 || BigInt(v.version) > BigInt(newest)) && (newest = v.version);
  if (newest === void 0)
    throw new AnthropicError(`skill ${JSON.stringify(skillId)} has no concrete version to resolve ${JSON.stringify(version)} against`);
  return newest;
}
function assertSafeMemberNames(names) {
  for (let raw of names.split(`
`)) {
    let entry = raw.trim();
    if (entry && (path5.isAbsolute(entry) || entry.split(/[\\/]/).includes("..")))
      throw new AnthropicError(`refusing to extract unsafe archive member: ${entry}`);
  }
}
function assertNoSpecialMembers(verboseListing) {
  for (let line of verboseListing.split(`
`)) {
    let type = line.trimStart()[0];
    if (type === "l" || type === "h" || type === "b" || type === "c" || type === "p" || type === "s")
      throw new AnthropicError("refusing to extract archive with symlink/hardlink/device member");
  }
}
async function runArchiveTool(cmd, args) {
  try {
    let { stdout } = await execFileAsync2(cmd, args);
    return stdout;
  } catch (e) {
    throw e != null && typeof e == "object" && e.code === "ENOENT" ? new AnthropicError(`skill extraction requires the \`${cmd}\` command, but it was not found on PATH`) : e;
  }
}
function archiveTopDir(listing) {
  let top, nested = !1;
  for (let raw of listing.split(`
`)) {
    let parts = raw.trim().split("/").filter((p) => p !== "" && p !== ".");
    if (parts.length === 0)
      continue;
    let first = parts[0];
    if (top === void 0)
      top = first;
    else if (first !== top)
      return "";
    parts.length > 1 && (nested = !0);
  }
  return top !== void 0 && nested ? top : "";
}
async function extractSkillArchive(resp, dest) {
  let tmp = path5.join(dest, `.skill-archive-${process.pid}-${Date.now()}`);
  if (!resp.body)
    throw new AnthropicError("skill download response had no body");
  await (0, import_promises8.pipeline)(import_node_stream.Readable.fromWeb(resp.body), fssync.createWriteStream(tmp));
  let stage = path5.join(path5.dirname(dest), `.skill-stage-${process.pid}-${Date.now()}`);
  try {
    let head = await readHead(tmp, 4), isZip = head.length >= 4 && head[0] === 80 && head[1] === 75 && head[2] === 3 && head[3] === 4, archiveCmd = isZip ? "unzip" : "tar", listing = await runArchiveTool(archiveCmd, isZip ? ["-Z1", tmp] : ["-tf", tmp]);
    assertSafeMemberNames(listing), assertNoSpecialMembers(await runArchiveTool(archiveCmd, isZip ? ["-Z", tmp] : ["-tvf", tmp]));
    let top = archiveTopDir(listing);
    await fs3.mkdir(stage, { recursive: !0, mode: DIR_CREATE_MODE }), await runArchiveTool(archiveCmd, isZip ? ["-oq", tmp, "-d", stage] : ["-xf", tmp, "-C", stage]);
    let srcRoot = top ? path5.join(stage, top) : stage;
    for (let entry of await fs3.readdir(srcRoot))
      await fs3.rename(path5.join(srcRoot, entry), path5.join(dest, entry));
  } finally {
    await fs3.rm(tmp, { force: !0 }), await fs3.rm(stage, { recursive: !0, force: !0 });
  }
}
async function readHead(file, n) {
  let handle = await fs3.open(file, "r");
  try {
    let buf = Buffer.alloc(n), { bytesRead } = await handle.read(buf, 0, n, 0);
    return buf.subarray(0, bytesRead);
  } finally {
    await handle.close();
  }
}
var fs3, fssync, path5, import_node_child_process2, import_node_util2, import_node_stream, import_promises8, execFileAsync2, init_skills = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/tools/agent-toolset/skills.mjs"() {
    fs3 = __toESM(require("node:fs/promises"), 1), fssync = __toESM(require("node:fs"), 1), path5 = __toESM(require("node:path"), 1), import_node_child_process2 = require("node:child_process"), import_node_util2 = require("node:util"), import_node_stream = require("node:stream"), import_promises8 = require("node:stream/promises");
    init_error();
    init_log();
    init_fs_util();
    execFileAsync2 = (0, import_node_util2.promisify)(import_node_child_process2.execFile);
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/tools/agent-toolset/node.mjs
var node_exports = {};
__export(node_exports, {
  BashSession: () => BashSession,
  betaAgentToolset20260401: () => betaAgentToolset20260401,
  betaBashTool: () => betaBashTool,
  betaEditTool: () => betaEditTool,
  betaGlobTool: () => betaGlobTool,
  betaGrepTool: () => betaGrepTool,
  betaReadTool: () => betaReadTool,
  betaWriteTool: () => betaWriteTool,
  extractSkillArchive: () => extractSkillArchive,
  resolvePath: () => resolvePath,
  resolveSkillVersion: () => resolveSkillVersion,
  setupSkills: () => setupSkills
});
function resolveMaxBytes(configured) {
  return configured === void 0 ? DEFAULT_MAX_FILE_BYTES : configured;
}
function betaAgentToolset20260401(ctx) {
  return [
    betaBashTool(ctx),
    betaReadTool(ctx),
    betaWriteTool(ctx),
    betaEditTool(ctx),
    betaGlobTool(ctx),
    betaGrepTool(ctx)
  ];
}
function resolvePath(ctx, p) {
  return confineToRoot(ctx.workdir, p, { allowOutside: ctx.unrestrictedPaths ?? !1 });
}
function scrubbedShellEnv() {
  let env = {};
  for (let [key, value] of Object.entries(process.env))
    key.startsWith("ANTHROPIC_") || (env[key] = value);
  return env;
}
function betaBashTool(ctx) {
  let session, tail = Promise.resolve();
  return betaTool({
    name: "bash",
    description: "Run a bash command in a persistent shell. State (cwd, env vars) persists across calls.",
    inputSchema: {
      type: "object",
      properties: {
        command: { type: "string", description: "The command to run" },
        restart: { type: "boolean", description: "Restart the persistent shell before running" },
        timeout_ms: { type: "integer", description: "Per-call timeout in milliseconds" }
      }
    },
    run: async ({ command, restart, timeout_ms }, context) => {
      let prev = tail, gate = promiseWithResolvers();
      tail = gate.promise;
      try {
        await prev;
      } catch {
      }
      try {
        if (restart && (session?.close(), session = void 0), !command) {
          if (restart)
            return "bash session restarted";
          throw new ToolError("bash: command is required");
        }
        session ?? (session = new BashSession(ctx.workdir, ctx.env));
        try {
          let { output, exitCode } = await session.exec(command, {
            timeoutMs: timeout_ms ?? BASH_DEFAULT_TIMEOUT_MS,
            signal: context?.signal
          });
          if (exitCode !== 0)
            throw new ToolError(output || `exit ${exitCode}`);
          return output;
        } catch (e) {
          throw e instanceof ToolError ? e : (session.close(), session = void 0, new ToolError(`bash: ${e instanceof Error ? e.message : String(e)}`));
        }
      } finally {
        gate.resolve();
      }
    },
    close: () => {
      session?.close(), session = void 0;
    }
  });
}
function betaReadTool(ctx) {
  return betaTool({
    name: "read",
    description: "Read a UTF-8 text file relative to the workdir.",
    inputSchema: {
      type: "object",
      properties: {
        file_path: { type: "string" },
        view_range: {
          type: "array",
          items: { type: "integer" },
          description: "[start_line, end_line] 1-indexed inclusive"
        }
      },
      required: ["file_path"]
    },
    run: async ({ file_path, view_range }) => {
      if (!file_path)
        throw new ToolError("read: file_path is required");
      let abs = await resolvePath(ctx, file_path), data;
      try {
        let st = await fs4.stat(abs);
        if (!st.isFile())
          throw new ToolError(`read: ${file_path} is not a regular file`);
        let limit3 = resolveMaxBytes(ctx.maxFileBytes);
        if (limit3 !== null && st.size > limit3)
          throw new ToolError(`read: ${file_path} is ${st.size} bytes, exceeds ${limit3}-byte limit. Use bash (head/tail/sed) to read a slice.`);
        data = await fs4.readFile(abs, "utf8");
      } catch (e) {
        throw e instanceof ToolError ? e : new ToolError(`read: ${fsErrorMessage(e, file_path)}`);
      }
      if (!view_range)
        return data;
      if (view_range.length !== 2)
        throw new ToolError("read: view_range must be [start_line, end_line]");
      let [startLine, endLine] = view_range, lines = data.split(`
`), start = Math.max(0, startLine - 1), end = endLine > 0 ? endLine : lines.length;
      return lines.slice(start, end).join(`
`);
    }
  });
}
function betaWriteTool(ctx) {
  return betaTool({
    name: "write",
    description: "Write a UTF-8 text file relative to the workdir, creating parent directories as needed.",
    inputSchema: {
      type: "object",
      properties: { file_path: { type: "string" }, content: { type: "string" } },
      required: ["file_path", "content"]
    },
    run: async ({ file_path, content }) => {
      if (!file_path)
        throw new ToolError("write: file_path is required");
      let abs = await resolvePath(ctx, file_path);
      try {
        await fs4.mkdir(path6.dirname(abs), { recursive: !0, mode: DIR_CREATE_MODE }), await atomicWriteFile(abs, content ?? "");
      } catch (e) {
        throw new ToolError(`write: ${fsErrorMessage(e, file_path)}`);
      }
      return `wrote ${Buffer.byteLength(content ?? "")} bytes to ${file_path}`;
    }
  });
}
function betaEditTool(ctx) {
  return betaTool({
    name: "edit",
    description: "Replace old_string with new_string in a file. old_string must be unique unless replace_all.",
    inputSchema: {
      type: "object",
      properties: {
        file_path: { type: "string" },
        old_string: { type: "string" },
        new_string: { type: "string" },
        replace_all: { type: "boolean" }
      },
      required: ["file_path", "old_string", "new_string"]
    },
    run: async ({ file_path, old_string, new_string, replace_all }) => {
      if (!file_path)
        throw new ToolError("edit: file_path is required");
      if (!old_string)
        throw new ToolError("edit: old_string is required");
      let abs = await resolvePath(ctx, file_path), data;
      try {
        let st = await fs4.stat(abs);
        if (!st.isFile())
          throw new ToolError(`edit: ${file_path} is not a regular file`);
        let limit3 = resolveMaxBytes(ctx.maxFileBytes);
        if (limit3 !== null && st.size > limit3)
          throw new ToolError(`edit: ${file_path} is ${st.size} bytes, exceeds ${limit3}-byte limit. Use bash (sed/awk) to edit a large file.`);
        data = await fs4.readFile(abs, "utf8");
      } catch (e) {
        throw e instanceof ToolError ? e : new ToolError(`edit: ${fsErrorMessage(e, file_path)}`);
      }
      let count = data.split(old_string).length - 1;
      if (count === 0)
        throw new ToolError(`edit: old_string not found in ${file_path}`);
      let updated;
      if (replace_all)
        updated = data.split(old_string).join(new_string);
      else {
        if (count > 1)
          throw new ToolError(`edit: old_string appears ${count} times in ${file_path} (must be unique)`);
        updated = data.replace(old_string, () => new_string);
      }
      try {
        await atomicWriteFile(abs, updated);
      } catch (e) {
        throw new ToolError(`edit: write: ${fsErrorMessage(e, file_path)}`);
      }
      return `edited ${file_path} (${replace_all ? count : 1} replacement(s))`;
    }
  });
}
function betaGlobTool(ctx) {
  return betaTool({
    name: "glob",
    description: "Match files under the workdir against a glob pattern. Results are mtime-sorted, newest first.",
    inputSchema: {
      type: "object",
      properties: {
        pattern: { type: "string" },
        path: { type: "string", description: "Directory to search in. Defaults to the workdir." }
      },
      required: ["pattern"]
    },
    run: async ({ pattern, path: searchPath }) => {
      if (!pattern)
        throw new ToolError("glob: pattern is required");
      let root = path6.resolve(ctx.workdir), pat = pattern;
      if (path6.isAbsolute(pattern)) {
        if (!ctx.unrestrictedPaths)
          throw new ToolError("glob: absolute pattern not permitted");
        root = path6.parse(pattern).root, pat = path6.relative(root, pattern);
      } else searchPath && (root = await resolvePath(ctx, searchPath));
      if (!ctx.unrestrictedPaths && pat.split(/[\\/]/).includes(".."))
        throw new ToolError('glob: ".." is not permitted in the pattern');
      let realRoot = ctx.unrestrictedPaths ? root : await fs4.realpath(root).catch(() => root), matches = [];
      try {
        for await (let entry of fsGlob(pat, {
          cwd: root,
          withFileTypes: !0,
          exclude: (d) => d.name === ".git" || d.name === "node_modules"
        })) {
          if (!entry.isFile())
            continue;
          let full = path6.join(entry.parentPath, entry.name);
          if (!ctx.unrestrictedPaths) {
            let real;
            try {
              real = await fs4.realpath(full);
            } catch {
              continue;
            }
            if (!isWithin(realRoot, real))
              continue;
          }
          let mtime = 0;
          try {
            mtime = (await fs4.stat(full)).mtimeMs;
          } catch {
          }
          matches.push({ path: full, mtime });
        }
      } catch (e) {
        throw new ToolError(`glob: ${e instanceof Error ? e.message : String(e)}`);
      }
      return matches.length === 0 ? "no matches" : (matches.sort((a, b) => b.mtime - a.mtime), matches.slice(0, GLOB_RESULT_LIMIT).map((m) => m.path).join(`
`));
    }
  });
}
function betaGrepTool(ctx) {
  return betaTool({
    name: "grep",
    description: "Search file contents for a regex. Uses ripgrep if available, otherwise a built-in walker.",
    inputSchema: {
      type: "object",
      properties: { pattern: { type: "string" }, path: { type: "string" } },
      required: ["pattern"]
    },
    run: async ({ pattern, path: p }, context) => {
      if (!pattern)
        throw new ToolError("grep: pattern is required");
      let searchPath = path6.resolve(ctx.workdir);
      p && (searchPath = await resolvePath(ctx, p));
      let rg = await findRg();
      return rg ? runRipgrep(rg, pattern, searchPath, context?.signal) : runWalkGrep(pattern, searchPath, context?.signal);
    }
  });
}
function runRipgrep(rg, pattern, searchPath, signal) {
  return new Promise((resolve4, reject) => {
    let proc = cp.spawn(rg, ["-n", "--no-heading", "-e", pattern, "--", searchPath], {
      ...signal ? { signal } : {}
    }), out = "", errOut = "", truncated = !1;
    proc.stdout.on("data", (d) => {
      truncated || (out += d, out.length > GREP_OUTPUT_LIMIT && (truncated = !0, out = out.slice(0, GREP_OUTPUT_LIMIT), proc.kill("SIGKILL")));
    }), proc.stderr.on("data", (d) => errOut += d), proc.on("close", (code) => {
      if (signal?.aborted)
        return reject(new ToolError("grep: aborted"));
      if (truncated)
        return resolve4(out + `
[output truncated at ${GREP_OUTPUT_LIMIT} bytes]`);
      if (code === 0)
        return resolve4(out);
      if (code === 1)
        return resolve4("no matches");
      reject(new ToolError(`grep: rg failed: ${errOut || `exit ${code}`}`));
    }), proc.on("error", (e) => {
      if (signal?.aborted)
        return reject(new ToolError("grep: aborted"));
      reject(new ToolError(`grep: rg failed: ${e.message}`));
    });
  });
}
async function runWalkGrep(pattern, root, signal) {
  let re;
  try {
    re = new RegExp(pattern);
  } catch (e) {
    throw new ToolError(`grep: invalid regex: ${e instanceof Error ? e.message : String(e)}`);
  }
  let hits = [], budget = GREP_OUTPUT_LIMIT, push = (line) => (budget -= line.length + 1, budget < 0 ? (hits.push(`[output truncated at ${GREP_OUTPUT_LIMIT} bytes]`), !1) : (hits.push(line), !0));
  if ((await fs4.stat(root).catch(() => null))?.isFile() ? await grepFile(root, re, push) : await walk(root, "", (rel) => grepFile(path6.join(root, rel), re, push), signal), signal?.aborted)
    throw new ToolError("grep: aborted");
  return hits.length === 0 ? "no matches" : hits.join(`
`);
}
async function grepFile(file, re, push) {
  let stream = fssync2.createReadStream(file, { encoding: "utf8" }), rl = readline.createInterface({ input: stream, crlfDelay: 1 / 0 }), i = 0;
  try {
    for await (let line of rl)
      if (i++, !(line.length > GREP_MAX_LINE_LENGTH) && re.test(line) && !push(`${file}:${i}:${line}`))
        return !1;
  } catch {
  } finally {
    stream.destroy();
  }
  return !0;
}
function isWithin(root, p) {
  let rel = path6.relative(root, p);
  return rel === "" || !rel.startsWith(".." + path6.sep) && rel !== ".." && !path6.isAbsolute(rel);
}
async function walk(root, rel, fn, signal) {
  let remaining = WALK_MAX_ENTRIES;
  async function inner(rel2, depth) {
    if (depth > WALK_MAX_DEPTH)
      return !0;
    if (signal?.aborted)
      return !1;
    let entries;
    try {
      entries = await fs4.readdir(path6.join(root, rel2), { withFileTypes: !0 });
    } catch {
      return !0;
    }
    for (let e of entries) {
      if (e.name === ".git" || e.name === "node_modules")
        continue;
      if (remaining-- <= 0 || signal?.aborted)
        return !1;
      let childRel = rel2 ? path6.join(rel2, e.name) : e.name;
      if (e.isDirectory()) {
        if (!await inner(childRel, depth + 1))
          return !1;
      } else if (e.isFile() && await fn(childRel) === !1)
        return !1;
    }
    return !0;
  }
  await inner(rel, 0);
}
async function findRg() {
  let dirs = (process.env.PATH ?? "").split(path6.delimiter);
  for (let d of dirs) {
    let candidate = path6.join(d, "rg");
    try {
      return await fs4.access(candidate, fssync2.constants.X_OK), candidate;
    } catch {
    }
  }
  return null;
}
var fs4, fssync2, path6, cp, crypto2, readline, _BashSession_instances, _BashSession_proc, _BashSession_buf, _BashSession_truncated, _BashSession_closed, _BashSession_waiting, _BashSession_append, BASH_OUTPUT_LIMIT, BASH_DEFAULT_TIMEOUT_MS, DEFAULT_MAX_FILE_BYTES, GREP_OUTPUT_LIMIT, GREP_MAX_LINE_LENGTH, GLOB_RESULT_LIMIT, ANSI_RE, fsGlob, BashSession, WALK_MAX_DEPTH, WALK_MAX_ENTRIES, init_node = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/tools/agent-toolset/node.mjs"() {
    init_tslib();
    fs4 = __toESM(require("node:fs/promises"), 1), fssync2 = __toESM(require("node:fs"), 1), path6 = __toESM(require("node:path"), 1), cp = __toESM(require("node:child_process"), 1), crypto2 = __toESM(require("node:crypto"), 1), readline = __toESM(require("node:readline"), 1);
    init_error();
    init_ToolError();
    init_json_schema();
    init_promise();
    init_fs_util();
    init_skills();
    BASH_OUTPUT_LIMIT = 100 * 1024, BASH_DEFAULT_TIMEOUT_MS = 12e4, DEFAULT_MAX_FILE_BYTES = 256 * 1024, GREP_OUTPUT_LIMIT = 100 * 1024, GREP_MAX_LINE_LENGTH = 2e3, GLOB_RESULT_LIMIT = 200, ANSI_RE = /\x1b\[[0-9;?]*[ -/]*[@-~]/g, fsGlob = fs4.glob;
    BashSession = class {
      constructor(dir, env = scrubbedShellEnv()) {
        _BashSession_instances.add(this), _BashSession_proc.set(this, void 0), _BashSession_buf.set(this, ""), _BashSession_truncated.set(this, !1), _BashSession_closed.set(this, !1), _BashSession_waiting.set(this, null), __classPrivateFieldSet(this, _BashSession_proc, cp.spawn("/bin/bash", ["--noprofile", "--norc"], {
          cwd: dir,
          // `env` is the full base environment (the scrubbed process env by
          // default, or the verbatim replacement from `AgentToolContext.env`).
          // PS1/PS2/TERM are shell-control settings BashSession always applies so
          // the pipe-based sentinel exec parsing works — not part of the
          // user-facing environment.
          env: { ...env, PS1: "", PS2: "", TERM: "dumb" },
          stdio: ["pipe", "pipe", "pipe"],
          detached: !0
        }), "f"), __classPrivateFieldGet(this, _BashSession_proc, "f").stdout.setEncoding("utf8"), __classPrivateFieldGet(this, _BashSession_proc, "f").stderr.setEncoding("utf8"), __classPrivateFieldGet(this, _BashSession_proc, "f").stdout.on("data", (d) => __classPrivateFieldGet(this, _BashSession_instances, "m", _BashSession_append).call(this, d)), __classPrivateFieldGet(this, _BashSession_proc, "f").stderr.on("data", (d) => __classPrivateFieldGet(this, _BashSession_instances, "m", _BashSession_append).call(this, d)), __classPrivateFieldGet(this, _BashSession_proc, "f").once("close", () => {
          __classPrivateFieldSet(this, _BashSession_closed, !0, "f");
          let w = __classPrivateFieldGet(this, _BashSession_waiting, "f");
          __classPrivateFieldSet(this, _BashSession_waiting, null, "f"), w?.resolve();
        });
      }
      /** Whether the underlying shell process has exited. */
      get closed() {
        return __classPrivateFieldGet(this, _BashSession_closed, "f");
      }
      async exec(command, opts = {}) {
        if (__classPrivateFieldGet(this, _BashSession_closed, "f"))
          throw new AnthropicError("bash session terminated");
        let timeoutMs = opts.timeoutMs ?? BASH_DEFAULT_TIMEOUT_MS, signal = opts.signal;
        if (signal?.aborted)
          throw new AnthropicError("bash command aborted");
        __classPrivateFieldSet(this, _BashSession_buf, "", "f"), __classPrivateFieldSet(this, _BashSession_truncated, !1, "f");
        let sentinel3 = `__ANT_CMD_${crypto2.randomUUID()}_DONE__`, sentinelSplit = `${sentinel3.slice(0, 8)}''${sentinel3.slice(8)}`, wrapped = `{ ${command}
} </dev/null 2>&1; printf '\\n${sentinelSplit}%d\\n' $?
`;
        if (__classPrivateFieldGet(this, _BashSession_proc, "f").stdin.write(wrapped), __classPrivateFieldGet(this, _BashSession_buf, "f").indexOf(sentinel3) < 0) {
          let { promise: sentinelSeen, resolve: resolve4 } = promiseWithResolvers();
          __classPrivateFieldSet(this, _BashSession_waiting, { sentinel: sentinel3, resolve: resolve4 }, "f");
          let timer, onAbort;
          try {
            await Promise.race([
              sentinelSeen,
              new Promise((_, reject) => {
                timer = setTimeout(() => reject(new AnthropicError(`bash command timed out after ${timeoutMs}ms`)), timeoutMs);
              }),
              new Promise((_, reject) => {
                signal && (onAbort = () => reject(new AnthropicError("bash command aborted")), signal.addEventListener("abort", onAbort, { once: !0 }));
              })
            ]);
          } finally {
            timer && clearTimeout(timer), onAbort && signal && signal.removeEventListener("abort", onAbort), __classPrivateFieldSet(this, _BashSession_waiting, null, "f");
          }
        }
        let idx = __classPrivateFieldGet(this, _BashSession_buf, "f").indexOf(sentinel3);
        if (idx < 0)
          throw new AnthropicError("bash session terminated");
        let m = __classPrivateFieldGet(this, _BashSession_buf, "f").slice(idx + sentinel3.length).match(/^(-?\d+)/), exitCode = m ? parseInt(m[1], 10) : -1, out = __classPrivateFieldGet(this, _BashSession_buf, "f").slice(0, idx).replace(ANSI_RE, "").replace(/\n+$/, "");
        return __classPrivateFieldGet(this, _BashSession_truncated, "f") && (out = `[output truncated]
${out}`), { output: out, exitCode };
      }
      close() {
        if (__classPrivateFieldGet(this, _BashSession_closed, "f"))
          return;
        __classPrivateFieldSet(this, _BashSession_closed, !0, "f");
        let w = __classPrivateFieldGet(this, _BashSession_waiting, "f");
        __classPrivateFieldSet(this, _BashSession_waiting, null, "f"), w?.resolve(), __classPrivateFieldGet(this, _BashSession_proc, "f").stdout.destroy(), __classPrivateFieldGet(this, _BashSession_proc, "f").stderr.destroy(), __classPrivateFieldGet(this, _BashSession_proc, "f").stdin.destroy();
        try {
          process.kill(-__classPrivateFieldGet(this, _BashSession_proc, "f").pid, "SIGKILL");
        } catch {
          __classPrivateFieldGet(this, _BashSession_proc, "f").kill("SIGKILL");
        }
        __classPrivateFieldGet(this, _BashSession_proc, "f").unref();
      }
    };
    _BashSession_proc = /* @__PURE__ */ new WeakMap(), _BashSession_buf = /* @__PURE__ */ new WeakMap(), _BashSession_truncated = /* @__PURE__ */ new WeakMap(), _BashSession_closed = /* @__PURE__ */ new WeakMap(), _BashSession_waiting = /* @__PURE__ */ new WeakMap(), _BashSession_instances = /* @__PURE__ */ new WeakSet(), _BashSession_append = function(d) {
      if (__classPrivateFieldSet(this, _BashSession_buf, __classPrivateFieldGet(this, _BashSession_buf, "f") + d, "f"), __classPrivateFieldGet(this, _BashSession_buf, "f").length > BASH_OUTPUT_LIMIT && (__classPrivateFieldSet(this, _BashSession_buf, __classPrivateFieldGet(this, _BashSession_buf, "f").slice(__classPrivateFieldGet(this, _BashSession_buf, "f").length - BASH_OUTPUT_LIMIT), "f"), __classPrivateFieldSet(this, _BashSession_truncated, !0, "f")), __classPrivateFieldGet(this, _BashSession_waiting, "f") && __classPrivateFieldGet(this, _BashSession_buf, "f").indexOf(__classPrivateFieldGet(this, _BashSession_waiting, "f").sentinel) >= 0) {
        let w = __classPrivateFieldGet(this, _BashSession_waiting, "f");
        __classPrivateFieldSet(this, _BashSession_waiting, null, "f"), w.resolve();
      }
    };
    WALK_MAX_DEPTH = 40, WALK_MAX_ENTRIES = 5e4;
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/environments/worker.mjs
async function forceStop(client, work, log, requestOptions) {
  try {
    await client.beta.environments.work.stop(
      work.id,
      { environment_id: work.environment_id, force: !0 },
      // Caller's headers pass through; the helper-tag header is on the scoped
      // sub-client's default_headers via copyClientForHelper, so no per-call
      // re-stamping needed.
      { ...requestOptions, headers: buildHeaders([requestOptions?.headers]) }
    );
  } catch (e) {
    isStatus(e, 409) || log.error("force-stop on exit failed", { work_id: work.id, error: String(e) });
  }
}
async function heartbeatLoop(client, work, ctrl, logger, requestOptions) {
  let intervalMs = HEARTBEAT_DEFAULT_MS, last = NO_HEARTBEAT_SENTINEL, beat = async () => {
    try {
      let resp = await client.beta.environments.work.heartbeat(work.id, { environment_id: work.environment_id, expected_last_heartbeat: last }, { ...requestOptions, headers: buildHeaders([requestOptions?.headers]), signal: ctrl.signal });
      last = resp.last_heartbeat, resp.ttl_seconds > 0 && (intervalMs = Math.max(1e3, Math.min(resp.ttl_seconds * 1e3 / 2, HEARTBEAT_DEFAULT_MS))), (resp.state === "stopping" || resp.state === "stopped") && (logger.info("heartbeat signals shutdown", { work_id: work.id, state: resp.state }), ctrl.abort()), resp.lease_extended || (logger.warn("lease not extended, shutting down", { work_id: work.id }), ctrl.abort());
    } catch (e) {
      if (ctrl.signal.throwIfAborted(), isFatal4xx(e))
        throw logger.error("permanent heartbeat failure", { work_id: work.id, error: String(e) }), ctrl.abort(), e;
      logger.warn("transient heartbeat failure", { work_id: work.id, error: String(e) });
    }
  };
  for (await beat(); !ctrl.signal.aborted; )
    await sleep(intervalMs, ctrl.signal), ctrl.signal.throwIfAborted(), await beat();
}
var _EnvironmentWorker_instances, _EnvironmentWorker_signal, _EnvironmentWorker_handleItem, HEARTBEAT_DEFAULT_MS, NO_HEARTBEAT_SENTINEL, EnvironmentWorker, init_worker2 = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/environments/worker.mjs"() {
    init_tslib();
    init_error();
    init_log();
    init_env();
    init_sleep();
    init_backoff();
    init_abort();
    init_headers();
    init_SessionToolRunner();
    init_poller();
    init_helper_client();
    HEARTBEAT_DEFAULT_MS = 3e4, NO_HEARTBEAT_SENTINEL = "NO_HEARTBEAT", EnvironmentWorker = class {
      constructor(opts) {
        _EnvironmentWorker_instances.add(this), _EnvironmentWorker_signal.set(this, void 0), this.client = opts.client, this.environmentId = opts.environmentId, this.environmentKey = opts.environmentKey, this.tools = opts.tools, this.workdir = opts.workdir ?? process.cwd(), this.unrestrictedPaths = opts.unrestrictedPaths, this.maxFileBytes = opts.maxFileBytes, this.maxIdleMs = opts.maxIdleMs, this.workerId = opts.workerId, this.requestOptions = opts.requestOptions, __classPrivateFieldSet(this, _EnvironmentWorker_signal, opts.signal, "f");
      }
      /**
       * Poll the environment and service each claimed session until the supplied
       * signal (or the one passed to the constructor) aborts. Throws if
       * `environmentId` / `environmentKey` were not provided to the constructor.
       */
      async run(signal) {
        let { environmentId, environmentKey } = this;
        if (environmentId === void 0 || environmentKey === void 0)
          throw new AnthropicError("EnvironmentWorker.run: environmentId and environmentKey are required to poll for work");
        let externalSignal = signal ?? __classPrivateFieldGet(this, _EnvironmentWorker_signal, "f"), poller = new WorkPoller({
          client: this.client,
          environmentId,
          environmentKey,
          ...this.workerId !== void 0 ? { workerId: this.workerId } : {},
          ...externalSignal ? { signal: externalSignal } : {},
          ...this.requestOptions !== void 0 ? { requestOptions: this.requestOptions } : {},
          // The per-item handler force-stops every work item on exit; let it be the
          // single owner of `work.stop` rather than double-posting from the poller.
          autoStop: !1
        });
        for await (let work of poller)
          await __classPrivateFieldGet(this, _EnvironmentWorker_instances, "m", _EnvironmentWorker_handleItem).call(this, work, environmentKey, poller.signal);
      }
      /**
       * Service a single, already-claimed work item without the poll loop: build the
       * per-session {@link AgentToolContext} (workdir from this worker's options),
       * download the session agent's skills (`setupSkills`), run a
       * {@link SessionToolRunner} for the session while heartbeating the work-item
       * lease in parallel, and force-stop the work item on exit (whether the runner
       * finishes normally, throws, or the heartbeat loop signals shutdown).
       *
       * Use this when something else does the claiming — e.g. a `worker poll
       * --on-work` script that hands an already-claimed item to a fresh process. The
       * work id / environment id / session id each fall back to `ANTHROPIC_WORK_ID` /
       * `ANTHROPIC_ENVIRONMENT_ID` / `ANTHROPIC_SESSION_ID` (the env vars that
       * command sets) when not passed; the environment key resolves from this
       * option, then the worker's own `environmentKey`, then
       * `ANTHROPIC_ENVIRONMENT_KEY`. With no arguments inside that command it just
       * works. Throws a clear error naming the first of the four required values
       * still missing after resolution.
       */
      async handleItem(opts) {
        let workId = opts?.workId ?? readEnv("ANTHROPIC_WORK_ID"), environmentId = opts?.environmentId ?? readEnv("ANTHROPIC_ENVIRONMENT_ID"), sessionId = opts?.sessionId ?? readEnv("ANTHROPIC_SESSION_ID"), environmentKey = opts?.environmentKey ?? this.environmentKey ?? readEnv("ANTHROPIC_ENVIRONMENT_KEY");
        if (!workId)
          throw new AnthropicError("handleItem: workId is required \u2014 pass it or set ANTHROPIC_WORK_ID");
        if (!environmentId)
          throw new AnthropicError("handleItem: environmentId is required \u2014 pass it or set ANTHROPIC_ENVIRONMENT_ID");
        if (!sessionId)
          throw new AnthropicError("handleItem: sessionId is required \u2014 pass it or set ANTHROPIC_SESSION_ID");
        if (!environmentKey)
          throw new AnthropicError("handleItem: environmentKey is required \u2014 pass it, construct the worker with it, or set ANTHROPIC_ENVIRONMENT_KEY");
        let work = {
          id: workId,
          environment_id: environmentId,
          data: { type: "session", id: sessionId }
        };
        await __classPrivateFieldGet(this, _EnvironmentWorker_instances, "m", _EnvironmentWorker_handleItem).call(this, work, environmentKey, opts?.signal ?? __classPrivateFieldGet(this, _EnvironmentWorker_signal, "f"));
      }
    };
    _EnvironmentWorker_signal = /* @__PURE__ */ new WeakMap(), _EnvironmentWorker_instances = /* @__PURE__ */ new WeakSet(), _EnvironmentWorker_handleItem = /**
     * The per-item body shared by {@link EnvironmentWorker.run}'s poll loop and
     * {@link EnvironmentWorker.handleItem}: run a {@link SessionToolRunner} for the
     * work item's session while heartbeating its lease, force-stopping on exit.
     * Non-session work items are ignored.
     */
    async function(work, environmentKey, externalSignal) {
      let log = loggerFor(this.client), sessionClient = copyClientForHelper(this.client, {
        authToken: environmentKey,
        helper: "environments-worker"
      }), sessionId = work.data.id, ctx = {
        workdir: this.workdir,
        client: this.client,
        sessionId,
        ...this.unrestrictedPaths !== void 0 ? { unrestrictedPaths: this.unrestrictedPaths } : {},
        ...this.maxFileBytes !== void 0 ? { maxFileBytes: this.maxFileBytes } : {}
      }, agentToolset = await Promise.resolve().then(() => (init_node(), node_exports)), cleanupSkills = async () => {
      };
      try {
        cleanupSkills = await agentToolset.setupSkills(ctx);
      } catch (e) {
        log.warn("skill setup failed", { session_id: sessionId, work_id: work.id, error: String(e) });
      }
      let tools = typeof this.tools == "function" ? this.tools(ctx) : this.tools ?? agentToolset.betaAgentToolset20260401(ctx), ctrl = new AbortController(), detachExternal = linkAbort(externalSignal, ctrl), heartbeatPromise = heartbeatLoop(sessionClient, work, ctrl, log, this.requestOptions).catch((e) => {
        ctrl.signal.aborted || log.error("heartbeat loop failed", { work_id: work.id, error: String(e) }), ctrl.abort();
      });
      try {
        let runner = new SessionToolRunner(sessionId, {
          client: sessionClient,
          tools,
          ...this.maxIdleMs !== void 0 ? { maxIdleMs: this.maxIdleMs } : {},
          ...this.requestOptions !== void 0 ? { requestOptions: this.requestOptions } : {},
          signal: ctrl.signal
        });
        for await (let _ of runner)
          ;
      } finally {
        ctrl.abort(), detachExternal(), await heartbeatPromise, await cleanupSkills().catch((e) => {
          log.warn("skill cleanup failed", { session_id: sessionId, work_id: work.id, error: String(e) });
        }), await forceStop(sessionClient, work, log, this.requestOptions);
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/environments/work.mjs
var Work, init_work = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/environments/work.mjs"() {
    init_resource();
    init_pagination();
    init_headers();
    init_path();
    init_poller();
    init_worker2();
    init_poller();
    init_worker2();
    Work = class extends APIResource {
      /**
       * Note: these endpoints are called automatically by the pre-built environment
       * worker provided in the SDKs and CLI, for orchestrating sessions with self-hosted
       * sandbox environments. They are included here as a reference; you do not need to
       * invoke them directly.
       *
       * Retrieve detailed information about a specific work item.
       *
       * @example
       * ```ts
       * const betaSelfHostedWork =
       *   await client.beta.environments.work.retrieve('work_id', {
       *     environment_id: 'env_011CZkZ9X2dpNyB7HsEFoRfW',
       *   });
       * ```
       */
      retrieve(workID, params, options) {
        let { environment_id, betas } = params;
        return this._client.get(path3`/v1/environments/${environment_id}/work/${workID}?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Note: these endpoints are called automatically by the pre-built environment
       * worker provided in the SDKs and CLI, for orchestrating sessions with self-hosted
       * sandbox environments. They are included here as a reference; you do not need to
       * invoke them directly.
       *
       * Update work item metadata with merge semantics.
       *
       * @example
       * ```ts
       * const betaSelfHostedWork =
       *   await client.beta.environments.work.update('work_id', {
       *     environment_id: 'env_011CZkZ9X2dpNyB7HsEFoRfW',
       *     metadata: { foo: 'string' },
       *   });
       * ```
       */
      update(workID, params, options) {
        let { environment_id, betas, ...body } = params;
        return this._client.post(path3`/v1/environments/${environment_id}/work/${workID}?beta=true`, {
          body,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Note: these endpoints are called automatically by the pre-built environment
       * worker provided in the SDKs and CLI, for orchestrating sessions with self-hosted
       * sandbox environments. They are included here as a reference; you do not need to
       * invoke them directly.
       *
       * List work items in an environment.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const betaSelfHostedWork of client.beta.environments.work.list(
       *   'env_011CZkZ9X2dpNyB7HsEFoRfW',
       * )) {
       *   // ...
       * }
       * ```
       */
      list(environmentID, params = {}, options) {
        let { betas, ...query } = params ?? {};
        return this._client.getAPIList(path3`/v1/environments/${environmentID}/work?beta=true`, PageCursor, {
          query,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Note: these endpoints are called automatically by the pre-built environment
       * worker provided in the SDKs and CLI, for orchestrating sessions with self-hosted
       * sandbox environments. They are included here as a reference; you do not need to
       * invoke them directly.
       *
       * Acknowledge receipt of a work item, transitioning it from 'queued' to 'starting'
       * and removing it from the queue.
       *
       * @example
       * ```ts
       * const betaSelfHostedWork =
       *   await client.beta.environments.work.ack('work_id', {
       *     environment_id: 'env_011CZkZ9X2dpNyB7HsEFoRfW',
       *   });
       * ```
       */
      ack(workID, params, options) {
        let { environment_id, betas } = params;
        return this._client.post(path3`/v1/environments/${environment_id}/work/${workID}/ack?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Note: these endpoints are called automatically by the pre-built environment
       * worker provided in the SDKs and CLI, for orchestrating sessions with self-hosted
       * sandbox environments. They are included here as a reference; you do not need to
       * invoke them directly.
       *
       * Record a heartbeat for a work item to maintain the lease.
       *
       * @example
       * ```ts
       * const betaSelfHostedWorkHeartbeatResponse =
       *   await client.beta.environments.work.heartbeat('work_id', {
       *     environment_id: 'env_011CZkZ9X2dpNyB7HsEFoRfW',
       *   });
       * ```
       */
      heartbeat(workID, params, options) {
        let { environment_id, desired_ttl_seconds, expected_last_heartbeat, betas } = params;
        return this._client.post(path3`/v1/environments/${environment_id}/work/${workID}/heartbeat?beta=true`, {
          query: { desired_ttl_seconds, expected_last_heartbeat },
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Note: these endpoints are called automatically by the pre-built environment
       * worker provided in the SDKs and CLI, for orchestrating sessions with self-hosted
       * sandbox environments. They are included here as a reference; you do not need to
       * invoke them directly.
       *
       * Long poll for work items in the queue.
       *
       * @example
       * ```ts
       * const betaSelfHostedWork =
       *   await client.beta.environments.work.poll(
       *     'env_011CZkZ9X2dpNyB7HsEFoRfW',
       *   );
       * ```
       */
      poll(environmentID, params = {}, options) {
        let { betas, "Anthropic-Worker-ID": anthropicWorkerID, ...query } = params ?? {};
        return this._client.get(path3`/v1/environments/${environmentID}/work/poll?beta=true`, {
          query,
          ...options,
          headers: buildHeaders([
            {
              "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString(),
              ...anthropicWorkerID != null ? { "Anthropic-Worker-ID": anthropicWorkerID } : void 0
            },
            options?.headers
          ])
        });
      }
      /**
       * Get statistics about the work queue for an environment.
       *
       * @example
       * ```ts
       * const betaSelfHostedWorkQueueStats =
       *   await client.beta.environments.work.stats(
       *     'env_011CZkZ9X2dpNyB7HsEFoRfW',
       *   );
       * ```
       */
      stats(environmentID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.get(path3`/v1/environments/${environmentID}/work/stats?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Note: these endpoints are called automatically by the pre-built environment
       * worker provided in the SDKs and CLI, for orchestrating sessions with self-hosted
       * sandbox environments. They are included here as a reference; you do not need to
       * invoke them directly.
       *
       * Stop a work item, initiating graceful or forced shutdown.
       *
       * @example
       * ```ts
       * const betaSelfHostedWork =
       *   await client.beta.environments.work.stop('work_id', {
       *     environment_id: 'env_011CZkZ9X2dpNyB7HsEFoRfW',
       *   });
       * ```
       */
      stop(workID, params, options) {
        let { environment_id, betas, ...body } = params;
        return this._client.post(path3`/v1/environments/${environment_id}/work/${workID}/stop?beta=true`, {
          body,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Continuously claim work from a self-hosted environment, ack each item,
       * and yield it. Posts `stop` automatically when the consumer's loop body
       * returns or when iteration ends.
       *
       * @example
       * ```ts
       * for await (const work of client.beta.environments.work.poller({
       *   environmentId,
       *   environmentKey,
       * })) {
       *   if (work.data.type !== 'session') continue;
       *   // ...service the work...
       * }
       * ```
       */
      poller(opts) {
        return new WorkPoller({ ...opts, client: this._client });
      }
      /**
       * The self-hosted environment runner: poll for work, and for each claimed
       * session set up the workdir, download the agent's skills, run the tools while
       * heartbeating the lease, and force-stop on exit.
       *
       * @example
       * ```ts
       * // Long-running daemon — poll, serve each session, loop:
       * await client.beta.environments.work
       *   .worker({ environmentId, environmentKey, workdir: '/workspace' })
       *   .run();
       *
       * // Or service one already-claimed work item (e.g. inside a sandbox spawned
       * // by `ant worker poll --on-work`) — handleItem() reads the ANTHROPIC_* env vars:
       * await client.beta.environments.work.worker({ workdir: '/workspace' }).handleItem();
       * ```
       */
      worker(opts) {
        return new EnvironmentWorker({ ...opts, client: this._client });
      }
    };
    Work.WorkPoller = WorkPoller;
    Work.EnvironmentWorker = EnvironmentWorker;
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/environments/environments.mjs
var Environments, init_environments = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/environments/environments.mjs"() {
    init_resource();
    init_work();
    init_work();
    init_pagination();
    init_headers();
    init_path();
    Environments = class extends APIResource {
      constructor() {
        super(...arguments), this.work = new Work(this._client);
      }
      /**
       * Create a new environment with the specified configuration.
       *
       * @example
       * ```ts
       * const betaEnvironment =
       *   await client.beta.environments.create({
       *     name: 'python-data-analysis',
       *   });
       * ```
       */
      create(params, options) {
        let { betas, ...body } = params;
        return this._client.post("/v1/environments?beta=true", {
          body,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Retrieve a specific environment by ID.
       *
       * @example
       * ```ts
       * const betaEnvironment =
       *   await client.beta.environments.retrieve(
       *     'env_011CZkZ9X2dpNyB7HsEFoRfW',
       *   );
       * ```
       */
      retrieve(environmentID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.get(path3`/v1/environments/${environmentID}?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Update an existing environment's configuration.
       *
       * @example
       * ```ts
       * const betaEnvironment =
       *   await client.beta.environments.update(
       *     'env_011CZkZ9X2dpNyB7HsEFoRfW',
       *   );
       * ```
       */
      update(environmentID, params, options) {
        let { betas, ...body } = params;
        return this._client.post(path3`/v1/environments/${environmentID}?beta=true`, {
          body,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * List environments with pagination support.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const betaEnvironment of client.beta.environments.list()) {
       *   // ...
       * }
       * ```
       */
      list(params = {}, options) {
        let { betas, ...query } = params ?? {};
        return this._client.getAPIList("/v1/environments?beta=true", PageCursor, {
          query,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Delete an environment by ID. Returns a confirmation of the deletion.
       *
       * @example
       * ```ts
       * const betaEnvironmentDeleteResponse =
       *   await client.beta.environments.delete(
       *     'env_011CZkZ9X2dpNyB7HsEFoRfW',
       *   );
       * ```
       */
      delete(environmentID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.delete(path3`/v1/environments/${environmentID}?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Archive an environment by ID. Archived environments cannot be used to create new
       * sessions.
       *
       * @example
       * ```ts
       * const betaEnvironment =
       *   await client.beta.environments.archive(
       *     'env_011CZkZ9X2dpNyB7HsEFoRfW',
       *   );
       * ```
       */
      archive(environmentID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.post(path3`/v1/environments/${environmentID}/archive?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
    };
    Environments.Work = Work;
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/memory-stores/memories.mjs
var Memories, init_memories = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/memory-stores/memories.mjs"() {
    init_resource();
    init_pagination();
    init_headers();
    init_path();
    Memories = class extends APIResource {
      /**
       * Create a memory
       *
       * @example
       * ```ts
       * const betaManagedAgentsMemory =
       *   await client.beta.memoryStores.memories.create(
       *     'memory_store_id',
       *     { content: 'content', path: 'xx' },
       *   );
       * ```
       */
      create(memoryStoreID, params, options) {
        let { view, betas, ...body } = params;
        return this._client.post(path3`/v1/memory_stores/${memoryStoreID}/memories?beta=true`, {
          query: { view },
          body,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "agent-memory-2026-07-22"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Retrieve a memory
       *
       * @example
       * ```ts
       * const betaManagedAgentsMemory =
       *   await client.beta.memoryStores.memories.retrieve(
       *     'memory_id',
       *     { memory_store_id: 'memory_store_id' },
       *   );
       * ```
       */
      retrieve(memoryID, params, options) {
        let { memory_store_id, betas, ...query } = params;
        return this._client.get(path3`/v1/memory_stores/${memory_store_id}/memories/${memoryID}?beta=true`, {
          query,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "agent-memory-2026-07-22"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Update a memory
       *
       * @example
       * ```ts
       * const betaManagedAgentsMemory =
       *   await client.beta.memoryStores.memories.update(
       *     'memory_id',
       *     { memory_store_id: 'memory_store_id' },
       *   );
       * ```
       */
      update(memoryID, params, options) {
        let { memory_store_id, view, betas, ...body } = params;
        return this._client.post(path3`/v1/memory_stores/${memory_store_id}/memories/${memoryID}?beta=true`, {
          query: { view },
          body,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "agent-memory-2026-07-22"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * List memories
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const betaManagedAgentsMemoryListItem of client.beta.memoryStores.memories.list(
       *   'memory_store_id',
       * )) {
       *   // ...
       * }
       * ```
       */
      list(memoryStoreID, params = {}, options) {
        let { betas, ...query } = params ?? {};
        return this._client.getAPIList(path3`/v1/memory_stores/${memoryStoreID}/memories?beta=true`, PageCursor, {
          query,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "agent-memory-2026-07-22"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Delete a memory
       *
       * @example
       * ```ts
       * const betaManagedAgentsDeletedMemory =
       *   await client.beta.memoryStores.memories.delete(
       *     'memory_id',
       *     { memory_store_id: 'memory_store_id' },
       *   );
       * ```
       */
      delete(memoryID, params, options) {
        let { memory_store_id, expected_content_sha256, betas } = params;
        return this._client.delete(path3`/v1/memory_stores/${memory_store_id}/memories/${memoryID}?beta=true`, {
          query: { expected_content_sha256 },
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "agent-memory-2026-07-22"].toString() },
            options?.headers
          ])
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/memory-stores/memory-versions.mjs
var MemoryVersions, init_memory_versions = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/memory-stores/memory-versions.mjs"() {
    init_resource();
    init_pagination();
    init_headers();
    init_path();
    MemoryVersions = class extends APIResource {
      /**
       * Retrieve a memory version
       *
       * @example
       * ```ts
       * const betaManagedAgentsMemoryVersion =
       *   await client.beta.memoryStores.memoryVersions.retrieve(
       *     'memory_version_id',
       *     { memory_store_id: 'memory_store_id' },
       *   );
       * ```
       */
      retrieve(memoryVersionID, params, options) {
        let { memory_store_id, betas, ...query } = params;
        return this._client.get(path3`/v1/memory_stores/${memory_store_id}/memory_versions/${memoryVersionID}?beta=true`, {
          query,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "agent-memory-2026-07-22"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * List memory versions
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const betaManagedAgentsMemoryVersion of client.beta.memoryStores.memoryVersions.list(
       *   'memory_store_id',
       * )) {
       *   // ...
       * }
       * ```
       */
      list(memoryStoreID, params = {}, options) {
        let { betas, ...query } = params ?? {};
        return this._client.getAPIList(path3`/v1/memory_stores/${memoryStoreID}/memory_versions?beta=true`, PageCursor, {
          query,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "agent-memory-2026-07-22"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Redact a memory version
       *
       * @example
       * ```ts
       * const betaManagedAgentsMemoryVersion =
       *   await client.beta.memoryStores.memoryVersions.redact(
       *     'memory_version_id',
       *     { memory_store_id: 'memory_store_id' },
       *   );
       * ```
       */
      redact(memoryVersionID, params, options) {
        let { memory_store_id, betas } = params;
        return this._client.post(path3`/v1/memory_stores/${memory_store_id}/memory_versions/${memoryVersionID}/redact?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "agent-memory-2026-07-22"].toString() },
            options?.headers
          ])
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/memory-stores/memory-stores.mjs
var MemoryStores, init_memory_stores = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/memory-stores/memory-stores.mjs"() {
    init_resource();
    init_memories();
    init_memories();
    init_memory_versions();
    init_memory_versions();
    init_pagination();
    init_headers();
    init_path();
    MemoryStores = class extends APIResource {
      constructor() {
        super(...arguments), this.memories = new Memories(this._client), this.memoryVersions = new MemoryVersions(this._client);
      }
      /**
       * Create a memory store
       *
       * @example
       * ```ts
       * const betaManagedAgentsMemoryStore =
       *   await client.beta.memoryStores.create({ name: 'x' });
       * ```
       */
      create(params, options) {
        let { betas, ...body } = params;
        return this._client.post("/v1/memory_stores?beta=true", {
          body,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "agent-memory-2026-07-22"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Retrieve a memory store
       *
       * @example
       * ```ts
       * const betaManagedAgentsMemoryStore =
       *   await client.beta.memoryStores.retrieve(
       *     'memory_store_id',
       *   );
       * ```
       */
      retrieve(memoryStoreID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.get(path3`/v1/memory_stores/${memoryStoreID}?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "agent-memory-2026-07-22"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Update a memory store
       *
       * @example
       * ```ts
       * const betaManagedAgentsMemoryStore =
       *   await client.beta.memoryStores.update('memory_store_id');
       * ```
       */
      update(memoryStoreID, params, options) {
        let { betas, ...body } = params;
        return this._client.post(path3`/v1/memory_stores/${memoryStoreID}?beta=true`, {
          body,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "agent-memory-2026-07-22"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * List memory stores
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const betaManagedAgentsMemoryStore of client.beta.memoryStores.list()) {
       *   // ...
       * }
       * ```
       */
      list(params = {}, options) {
        let { betas, ...query } = params ?? {};
        return this._client.getAPIList("/v1/memory_stores?beta=true", PageCursor, {
          query,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "agent-memory-2026-07-22"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Delete a memory store
       *
       * @example
       * ```ts
       * const betaManagedAgentsDeletedMemoryStore =
       *   await client.beta.memoryStores.delete('memory_store_id');
       * ```
       */
      delete(memoryStoreID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.delete(path3`/v1/memory_stores/${memoryStoreID}?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "agent-memory-2026-07-22"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Archive a memory store
       *
       * @example
       * ```ts
       * const betaManagedAgentsMemoryStore =
       *   await client.beta.memoryStores.archive('memory_store_id');
       * ```
       */
      archive(memoryStoreID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.post(path3`/v1/memory_stores/${memoryStoreID}/archive?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "agent-memory-2026-07-22"].toString() },
            options?.headers
          ])
        });
      }
    };
    MemoryStores.Memories = Memories;
    MemoryStores.MemoryVersions = MemoryVersions;
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/error.mjs
var init_error2 = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/error.mjs"() {
    init_error();
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/decoders/jsonl.mjs
var JSONLDecoder, init_jsonl = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/decoders/jsonl.mjs"() {
    init_error();
    init_shims();
    init_line();
    JSONLDecoder = class _JSONLDecoder {
      constructor(iterator, controller) {
        this.iterator = iterator, this.controller = controller;
      }
      async *decoder() {
        let lineDecoder = new LineDecoder();
        for await (let chunk of this.iterator)
          for (let line of lineDecoder.decode(chunk))
            yield JSON.parse(line);
        for (let line of lineDecoder.flush())
          yield JSON.parse(line);
      }
      [Symbol.asyncIterator]() {
        return this.decoder();
      }
      static fromResponse(response, controller) {
        if (!response.body)
          throw controller.abort(), typeof globalThis.navigator < "u" && globalThis.navigator.product === "ReactNative" ? new AnthropicError("The default react-native fetch implementation does not support streaming. Please use expo/fetch: https://docs.expo.dev/versions/latest/sdk/expo/#expofetch-api") : new AnthropicError("Attempted to iterate over a response with no body");
        return new _JSONLDecoder(ReadableStreamToAsyncIterable(response.body), controller);
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/messages/batches.mjs
var Batches, init_batches = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/messages/batches.mjs"() {
    init_resource();
    init_pagination();
    init_headers();
    init_jsonl();
    init_error2();
    init_path();
    Batches = class extends APIResource {
      /**
       * Send a batch of Message creation requests.
       *
       * The Message Batches API can be used to process multiple Messages API requests at
       * once. Once a Message Batch is created, it begins processing immediately. Batches
       * can take up to 24 hours to complete.
       *
       * Learn more about the Message Batches API in our
       * [user guide](https://platform.claude.com/docs/en/build-with-claude/batch-processing)
       *
       * @example
       * ```ts
       * const betaMessageBatch =
       *   await client.beta.messages.batches.create({
       *     requests: [
       *       {
       *         custom_id: 'my-custom-id-1',
       *         params: {
       *           max_tokens: 1024,
       *           messages: [
       *             { content: 'Hello, world', role: 'user' },
       *           ],
       *           model: 'claude-opus-4-6',
       *         },
       *       },
       *     ],
       *   });
       * ```
       */
      create(params, options) {
        let { betas, user_profile_id, ...body } = params;
        return this._client.post("/v1/messages/batches?beta=true", {
          body,
          ...options,
          headers: buildHeaders([
            {
              "anthropic-beta": [...betas ?? [], "message-batches-2024-09-24"].toString(),
              ...user_profile_id != null ? { "anthropic-user-profile-id": user_profile_id } : void 0
            },
            options?.headers
          ])
        });
      }
      /**
       * This endpoint is idempotent and can be used to poll for Message Batch
       * completion. To access the results of a Message Batch, make a request to the
       * `results_url` field in the response.
       *
       * Learn more about the Message Batches API in our
       * [user guide](https://platform.claude.com/docs/en/build-with-claude/batch-processing)
       *
       * @example
       * ```ts
       * const betaMessageBatch =
       *   await client.beta.messages.batches.retrieve(
       *     'message_batch_id',
       *   );
       * ```
       */
      retrieve(messageBatchID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.get(path3`/v1/messages/batches/${messageBatchID}?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "message-batches-2024-09-24"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * List all Message Batches within a Workspace. Most recently created batches are
       * returned first.
       *
       * Learn more about the Message Batches API in our
       * [user guide](https://platform.claude.com/docs/en/build-with-claude/batch-processing)
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const betaMessageBatch of client.beta.messages.batches.list()) {
       *   // ...
       * }
       * ```
       */
      list(params = {}, options) {
        let { betas, ...query } = params ?? {};
        return this._client.getAPIList("/v1/messages/batches?beta=true", Page, {
          query,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "message-batches-2024-09-24"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Delete a Message Batch.
       *
       * Message Batches can only be deleted once they've finished processing. If you'd
       * like to delete an in-progress batch, you must first cancel it.
       *
       * Learn more about the Message Batches API in our
       * [user guide](https://platform.claude.com/docs/en/build-with-claude/batch-processing)
       *
       * @example
       * ```ts
       * const betaDeletedMessageBatch =
       *   await client.beta.messages.batches.delete(
       *     'message_batch_id',
       *   );
       * ```
       */
      delete(messageBatchID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.delete(path3`/v1/messages/batches/${messageBatchID}?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "message-batches-2024-09-24"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Batches may be canceled any time before processing ends. Once cancellation is
       * initiated, the batch enters a `canceling` state, at which time the system may
       * complete any in-progress, non-interruptible requests before finalizing
       * cancellation.
       *
       * The number of canceled requests is specified in `request_counts`. To determine
       * which requests were canceled, check the individual results within the batch.
       * Note that cancellation may not result in any canceled requests if they were
       * non-interruptible.
       *
       * Learn more about the Message Batches API in our
       * [user guide](https://platform.claude.com/docs/en/build-with-claude/batch-processing)
       *
       * @example
       * ```ts
       * const betaMessageBatch =
       *   await client.beta.messages.batches.cancel(
       *     'message_batch_id',
       *   );
       * ```
       */
      cancel(messageBatchID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.post(path3`/v1/messages/batches/${messageBatchID}/cancel?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "message-batches-2024-09-24"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Streams the results of a Message Batch as a `.jsonl` file.
       *
       * Each line in the file is a JSON object containing the result of a single request
       * in the Message Batch. Results are not guaranteed to be in the same order as
       * requests. Use the `custom_id` field to match results to requests.
       *
       * Learn more about the Message Batches API in our
       * [user guide](https://platform.claude.com/docs/en/build-with-claude/batch-processing)
       *
       * @example
       * ```ts
       * const betaMessageBatchIndividualResponse =
       *   await client.beta.messages.batches.results(
       *     'message_batch_id',
       *   );
       * ```
       */
      async results(messageBatchID, params = {}, options) {
        let batch = await this.retrieve(messageBatchID);
        if (!batch.results_url)
          throw new AnthropicError(`No batch \`results_url\`; Has it finished processing? ${batch.processing_status} - ${batch.id}`);
        let { betas } = params ?? {};
        return this._client.get(batch.results_url, {
          ...options,
          headers: buildHeaders([
            {
              "anthropic-beta": [...betas ?? [], "message-batches-2024-09-24"].toString(),
              Accept: "application/binary"
            },
            options?.headers
          ]),
          stream: !0,
          __binaryResponse: !0
        })._thenUnwrap((_, props) => JSONLDecoder.fromResponse(props.response, props.controller));
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/constants.mjs
var MODEL_NONSTREAMING_TOKENS, init_constants = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/constants.mjs"() {
    MODEL_NONSTREAMING_TOKENS = {
      "claude-opus-4-20250514": 8192,
      "claude-opus-4-0": 8192,
      "claude-4-opus-20250514": 8192,
      "anthropic.claude-opus-4-20250514-v1:0": 8192,
      "claude-opus-4@20250514": 8192,
      "claude-opus-4-1-20250805": 8192,
      "anthropic.claude-opus-4-1-20250805-v1:0": 8192,
      "claude-opus-4-1@20250805": 8192
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/beta-parser.mjs
function getOutputFormat(params) {
  return params?.output_format ?? params?.output_config?.format;
}
function maybeParseBetaMessage(message, params, opts) {
  let outputFormat = getOutputFormat(params);
  return !params || !("parse" in (outputFormat ?? {})) ? {
    ...message,
    content: message.content.map((block) => {
      if (block.type === "text") {
        let parsedBlock = Object.defineProperty({ ...block }, "parsed_output", {
          value: null,
          enumerable: !1
        });
        return Object.defineProperty(parsedBlock, "parsed", {
          get() {
            return opts.logger.warn("The `parsed` property on `text` blocks is deprecated, please use `parsed_output` instead."), null;
          },
          enumerable: !1
        });
      }
      return block;
    }),
    parsed_output: null
  } : parseBetaMessage(message, params, opts);
}
function parseBetaMessage(message, params, opts) {
  let firstParsedOutput = null, content = message.content.map((block) => {
    if (block.type === "text") {
      let parsedOutput = parseBetaOutputFormat(params, block.text);
      firstParsedOutput === null && (firstParsedOutput = parsedOutput);
      let parsedBlock = Object.defineProperty({ ...block }, "parsed_output", {
        value: parsedOutput,
        enumerable: !1
      });
      return Object.defineProperty(parsedBlock, "parsed", {
        get() {
          return opts.logger.warn("The `parsed` property on `text` blocks is deprecated, please use `parsed_output` instead."), parsedOutput;
        },
        enumerable: !1
      });
    }
    return block;
  });
  return {
    ...message,
    content,
    parsed_output: firstParsedOutput
  };
}
function parseBetaOutputFormat(params, content) {
  let outputFormat = getOutputFormat(params);
  if (outputFormat?.type !== "json_schema")
    return null;
  try {
    return "parse" in outputFormat ? outputFormat.parse(content) : JSON.parse(content);
  } catch (error) {
    throw new AnthropicError(`Failed to parse structured output: ${error}`);
  }
}
var init_beta_parser = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/beta-parser.mjs"() {
    init_error();
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/streaming.mjs
var init_streaming2 = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/streaming.mjs"() {
    init_streaming();
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/_vendor/partial-json-parser/parser.mjs
var tokenize, strip, unstrip, generate, partialParse, init_parser = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/_vendor/partial-json-parser/parser.mjs"() {
    tokenize = (input) => {
      let current = 0, tokens = [];
      for (; current < input.length; ) {
        let char = input[current];
        if (char === "\\") {
          current++;
          continue;
        }
        if (char === "{") {
          tokens.push({
            type: "brace",
            value: "{"
          }), current++;
          continue;
        }
        if (char === "}") {
          tokens.push({
            type: "brace",
            value: "}"
          }), current++;
          continue;
        }
        if (char === "[") {
          tokens.push({
            type: "paren",
            value: "["
          }), current++;
          continue;
        }
        if (char === "]") {
          tokens.push({
            type: "paren",
            value: "]"
          }), current++;
          continue;
        }
        if (char === ":") {
          tokens.push({
            type: "separator",
            value: ":"
          }), current++;
          continue;
        }
        if (char === ",") {
          tokens.push({
            type: "delimiter",
            value: ","
          }), current++;
          continue;
        }
        if (char === '"') {
          let value = "", danglingQuote = !1;
          for (char = input[++current]; char !== '"'; ) {
            if (current === input.length) {
              danglingQuote = !0;
              break;
            }
            if (char === "\\") {
              if (current++, current === input.length) {
                danglingQuote = !0;
                break;
              }
              value += char + input[current], char = input[++current];
            } else
              value += char, char = input[++current];
          }
          char = input[++current], danglingQuote || tokens.push({
            type: "string",
            value
          });
          continue;
        }
        if (char && /\s/.test(char)) {
          current++;
          continue;
        }
        let NUMBERS = /[0-9]/;
        if (char && NUMBERS.test(char) || char === "-" || char === ".") {
          let value = "";
          for (char === "-" && (value += char, char = input[++current]); char && (NUMBERS.test(char) || char === "." || // exponent marker, e.g. `1e10` or `1.5E-9`
          char === "e" || char === "E" || // exponent sign, only valid immediately after the exponent marker
          (char === "-" || char === "+") && (value[value.length - 1] === "e" || value[value.length - 1] === "E")); )
            value += char, char = input[++current];
          tokens.push({
            type: "number",
            value
          });
          continue;
        }
        let LETTERS = /[a-z]/i;
        if (char && LETTERS.test(char)) {
          let value = "";
          for (; char && LETTERS.test(char) && current !== input.length; )
            value += char, char = input[++current];
          if (value == "true" || value == "false" || value === "null")
            tokens.push({
              type: "name",
              value
            });
          else {
            current++;
            continue;
          }
          continue;
        }
        current++;
      }
      return tokens;
    }, strip = (tokens) => {
      if (tokens.length === 0)
        return tokens;
      let lastToken = tokens[tokens.length - 1];
      switch (lastToken.type) {
        case "separator":
          return tokens = tokens.slice(0, tokens.length - 1), strip(tokens);
          break;
        case "number":
          let lastCharacterOfLastToken = lastToken.value[lastToken.value.length - 1];
          if (lastCharacterOfLastToken === "." || lastCharacterOfLastToken === "-" || lastCharacterOfLastToken === "+" || lastCharacterOfLastToken === "e" || lastCharacterOfLastToken === "E")
            return tokens = tokens.slice(0, tokens.length - 1), strip(tokens);
        case "string":
          let tokenBeforeTheLastToken = tokens[tokens.length - 2];
          if (tokenBeforeTheLastToken?.type === "delimiter")
            return tokens = tokens.slice(0, tokens.length - 1), strip(tokens);
          if (tokenBeforeTheLastToken?.type === "brace" && tokenBeforeTheLastToken.value === "{")
            return tokens = tokens.slice(0, tokens.length - 1), strip(tokens);
          break;
        case "delimiter":
          return tokens = tokens.slice(0, tokens.length - 1), strip(tokens);
          break;
      }
      return tokens;
    }, unstrip = (tokens) => {
      let tail = [];
      return tokens.map((token) => {
        token.type === "brace" && (token.value === "{" ? tail.push("}") : tail.splice(tail.lastIndexOf("}"), 1)), token.type === "paren" && (token.value === "[" ? tail.push("]") : tail.splice(tail.lastIndexOf("]"), 1));
      }), tail.length > 0 && tail.reverse().map((item) => {
        item === "}" ? tokens.push({
          type: "brace",
          value: "}"
        }) : item === "]" && tokens.push({
          type: "paren",
          value: "]"
        });
      }), tokens;
    }, generate = (tokens) => {
      let output = "";
      return tokens.map((token) => {
        switch (token.type) {
          case "string":
            output += '"' + token.value + '"';
            break;
          default:
            output += token.value;
            break;
        }
      }), output;
    }, partialParse = (input) => JSON.parse(generate(unstrip(strip(tokenize(input)))));
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/message-stream-utils.mjs
function withLazyInput(prev, jsonBuf) {
  let next = {};
  for (let key of Object.keys(prev))
    key !== "input" && (next[key] = prev[key]);
  Object.defineProperty(next, JSON_BUF_PROPERTY, { value: jsonBuf, enumerable: !1, writable: !0 });
  let input, parsed = !1;
  return Object.defineProperty(next, "input", {
    enumerable: !0,
    configurable: !0,
    get() {
      return parsed || (input = jsonBuf ? partialParse(jsonBuf) : {}, parsed = !0), input;
    }
  }), next;
}
var JSON_BUF_PROPERTY, init_message_stream_utils = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/internal/message-stream-utils.mjs"() {
    init_parser();
    JSON_BUF_PROPERTY = "__json_buf";
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/BetaMessageStream.mjs
function tracksToolInput(content) {
  return content.type === "tool_use" || content.type === "server_tool_use" || content.type === "mcp_tool_use";
}
var _BetaMessageStream_instances, _BetaMessageStream_currentMessageSnapshot, _BetaMessageStream_params, _BetaMessageStream_connectedPromise, _BetaMessageStream_resolveConnectedPromise, _BetaMessageStream_rejectConnectedPromise, _BetaMessageStream_endPromise, _BetaMessageStream_resolveEndPromise, _BetaMessageStream_rejectEndPromise, _BetaMessageStream_listeners, _BetaMessageStream_ended, _BetaMessageStream_errored, _BetaMessageStream_aborted, _BetaMessageStream_catchingPromiseCreated, _BetaMessageStream_response, _BetaMessageStream_request_id, _BetaMessageStream_logger, _BetaMessageStream_getFinalMessage, _BetaMessageStream_getFinalText, _BetaMessageStream_handleError, _BetaMessageStream_beginRequest, _BetaMessageStream_addStreamEvent, _BetaMessageStream_endRequest, _BetaMessageStream_accumulateMessage, _BetaMessageStream_toolInputParseError, BetaMessageStream, init_BetaMessageStream = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/BetaMessageStream.mjs"() {
    init_tslib();
    init_stainless_helper_header();
    init_error2();
    init_errors2();
    init_streaming2();
    init_beta_parser();
    init_message_stream_utils();
    BetaMessageStream = class _BetaMessageStream {
      constructor(params, opts) {
        _BetaMessageStream_instances.add(this), this.messages = [], this.receivedMessages = [], _BetaMessageStream_currentMessageSnapshot.set(this, void 0), _BetaMessageStream_params.set(this, null), this.controller = new AbortController(), _BetaMessageStream_connectedPromise.set(this, void 0), _BetaMessageStream_resolveConnectedPromise.set(this, () => {
        }), _BetaMessageStream_rejectConnectedPromise.set(this, () => {
        }), _BetaMessageStream_endPromise.set(this, void 0), _BetaMessageStream_resolveEndPromise.set(this, () => {
        }), _BetaMessageStream_rejectEndPromise.set(this, () => {
        }), _BetaMessageStream_listeners.set(this, {}), _BetaMessageStream_ended.set(this, !1), _BetaMessageStream_errored.set(this, !1), _BetaMessageStream_aborted.set(this, !1), _BetaMessageStream_catchingPromiseCreated.set(this, !1), _BetaMessageStream_response.set(this, void 0), _BetaMessageStream_request_id.set(this, void 0), _BetaMessageStream_logger.set(this, void 0), _BetaMessageStream_handleError.set(this, (error) => {
          if (__classPrivateFieldSet(this, _BetaMessageStream_errored, !0, "f"), isAbortError(error) && (error = new APIUserAbortError()), error instanceof APIUserAbortError)
            return __classPrivateFieldSet(this, _BetaMessageStream_aborted, !0, "f"), this._emit("abort", error);
          if (error instanceof AnthropicError)
            return this._emit("error", error);
          if (error instanceof Error) {
            let anthropicError = new AnthropicError(error.message);
            return anthropicError.cause = error, this._emit("error", anthropicError);
          }
          return this._emit("error", new AnthropicError(String(error)));
        }), __classPrivateFieldSet(this, _BetaMessageStream_connectedPromise, new Promise((resolve4, reject) => {
          __classPrivateFieldSet(this, _BetaMessageStream_resolveConnectedPromise, resolve4, "f"), __classPrivateFieldSet(this, _BetaMessageStream_rejectConnectedPromise, reject, "f");
        }), "f"), __classPrivateFieldSet(this, _BetaMessageStream_endPromise, new Promise((resolve4, reject) => {
          __classPrivateFieldSet(this, _BetaMessageStream_resolveEndPromise, resolve4, "f"), __classPrivateFieldSet(this, _BetaMessageStream_rejectEndPromise, reject, "f");
        }), "f"), __classPrivateFieldGet(this, _BetaMessageStream_connectedPromise, "f").catch(() => {
        }), __classPrivateFieldGet(this, _BetaMessageStream_endPromise, "f").catch(() => {
        }), __classPrivateFieldSet(this, _BetaMessageStream_params, params, "f"), __classPrivateFieldSet(this, _BetaMessageStream_logger, opts?.logger ?? console, "f");
      }
      get response() {
        return __classPrivateFieldGet(this, _BetaMessageStream_response, "f");
      }
      get request_id() {
        return __classPrivateFieldGet(this, _BetaMessageStream_request_id, "f");
      }
      /**
       * Returns the `MessageStream` data, the raw `Response` instance and the ID of the request,
       * returned vie the `request-id` header which is useful for debugging requests and resporting
       * issues to Anthropic.
       *
       * This is the same as the `APIPromise.withResponse()` method.
       *
       * This method will raise an error if you created the stream using `MessageStream.fromReadableStream`
       * as no `Response` is available.
       */
      async withResponse() {
        __classPrivateFieldSet(this, _BetaMessageStream_catchingPromiseCreated, !0, "f");
        let response = await __classPrivateFieldGet(this, _BetaMessageStream_connectedPromise, "f");
        if (!response)
          throw new Error("Could not resolve a `Response` object");
        return {
          data: this,
          response,
          request_id: response.headers.get("request-id")
        };
      }
      /**
       * Intended for use on the frontend, consuming a stream produced with
       * `.toReadableStream()` on the backend.
       *
       * Note that messages sent to the model do not appear in `.on('message')`
       * in this context.
       */
      static fromReadableStream(stream) {
        let runner = new _BetaMessageStream(null);
        return runner._run(() => runner._fromReadableStream(stream)), runner;
      }
      static createMessage(messages, params, options, { logger } = {}) {
        let runner = new _BetaMessageStream(params, { logger });
        for (let message of params.messages)
          runner._addMessageParam(message);
        return __classPrivateFieldSet(runner, _BetaMessageStream_params, { ...params, stream: !0 }, "f"), runner._run(() => runner._createMessage(messages, { ...params, stream: !0 }, { ...options, headers: { ...options?.headers, [STAINLESS_HELPER_METHOD_HEADER]: "stream" } })), runner;
      }
      _run(executor) {
        executor().then(() => {
          this._emitFinal(), this._emit("end");
        }, __classPrivateFieldGet(this, _BetaMessageStream_handleError, "f"));
      }
      _addMessageParam(message) {
        this.messages.push(message);
      }
      _addMessage(message, emit = !0) {
        this.receivedMessages.push(message), emit && this._emit("message", message);
      }
      async _createMessage(messages, params, options) {
        let signal = options?.signal, abortHandler;
        signal && (signal.aborted && this.controller.abort(), abortHandler = this.controller.abort.bind(this.controller), signal.addEventListener("abort", abortHandler));
        try {
          __classPrivateFieldGet(this, _BetaMessageStream_instances, "m", _BetaMessageStream_beginRequest).call(this);
          let { response, data: stream } = await messages.create({ ...params, stream: !0 }, { ...options, signal: this.controller.signal }).withResponse();
          this._connected(response);
          for await (let event of stream)
            __classPrivateFieldGet(this, _BetaMessageStream_instances, "m", _BetaMessageStream_addStreamEvent).call(this, event);
          if (stream.controller.signal?.aborted)
            throw new APIUserAbortError();
          __classPrivateFieldGet(this, _BetaMessageStream_instances, "m", _BetaMessageStream_endRequest).call(this);
        } finally {
          signal && abortHandler && signal.removeEventListener("abort", abortHandler);
        }
      }
      _connected(response) {
        this.ended || (__classPrivateFieldSet(this, _BetaMessageStream_response, response, "f"), __classPrivateFieldSet(this, _BetaMessageStream_request_id, response?.headers.get("request-id"), "f"), __classPrivateFieldGet(this, _BetaMessageStream_resolveConnectedPromise, "f").call(this, response), this._emit("connect"));
      }
      get ended() {
        return __classPrivateFieldGet(this, _BetaMessageStream_ended, "f");
      }
      get errored() {
        return __classPrivateFieldGet(this, _BetaMessageStream_errored, "f");
      }
      get aborted() {
        return __classPrivateFieldGet(this, _BetaMessageStream_aborted, "f");
      }
      abort() {
        this.controller.abort();
      }
      /**
       * Adds the listener function to the end of the listeners array for the event.
       * No checks are made to see if the listener has already been added. Multiple calls passing
       * the same combination of event and listener will result in the listener being added, and
       * called, multiple times.
       * @returns this MessageStream, so that calls can be chained
       */
      on(event, listener) {
        return (__classPrivateFieldGet(this, _BetaMessageStream_listeners, "f")[event] || (__classPrivateFieldGet(this, _BetaMessageStream_listeners, "f")[event] = [])).push({ listener }), this;
      }
      /**
       * Removes the specified listener from the listener array for the event.
       * off() will remove, at most, one instance of a listener from the listener array. If any single
       * listener has been added multiple times to the listener array for the specified event, then
       * off() must be called multiple times to remove each instance.
       * @returns this MessageStream, so that calls can be chained
       */
      off(event, listener) {
        let listeners = __classPrivateFieldGet(this, _BetaMessageStream_listeners, "f")[event];
        if (!listeners)
          return this;
        let index = listeners.findIndex((l) => l.listener === listener);
        return index >= 0 && listeners.splice(index, 1), this;
      }
      /**
       * Adds a one-time listener function for the event. The next time the event is triggered,
       * this listener is removed and then invoked.
       * @returns this MessageStream, so that calls can be chained
       */
      once(event, listener) {
        return (__classPrivateFieldGet(this, _BetaMessageStream_listeners, "f")[event] || (__classPrivateFieldGet(this, _BetaMessageStream_listeners, "f")[event] = [])).push({ listener, once: !0 }), this;
      }
      /**
       * This is similar to `.once()`, but returns a Promise that resolves the next time
       * the event is triggered, instead of calling a listener callback.
       * @returns a Promise that resolves the next time given event is triggered,
       * or rejects if an error is emitted.  (If you request the 'error' event,
       * returns a promise that resolves with the error).
       *
       * Example:
       *
       *   const message = await stream.emitted('message') // rejects if the stream errors
       */
      emitted(event) {
        return new Promise((resolve4, reject) => {
          __classPrivateFieldSet(this, _BetaMessageStream_catchingPromiseCreated, !0, "f"), event !== "error" && this.once("error", reject), this.once(event, resolve4);
        });
      }
      async done() {
        __classPrivateFieldSet(this, _BetaMessageStream_catchingPromiseCreated, !0, "f"), await __classPrivateFieldGet(this, _BetaMessageStream_endPromise, "f");
      }
      get currentMessage() {
        return __classPrivateFieldGet(this, _BetaMessageStream_currentMessageSnapshot, "f");
      }
      /**
       * @returns a promise that resolves with the the final assistant Message response,
       * or rejects if an error occurred or the stream ended prematurely without producing a Message.
       * If structured outputs were used, this will be a ParsedMessage with a `parsed` field.
       */
      async finalMessage() {
        return await this.done(), __classPrivateFieldGet(this, _BetaMessageStream_instances, "m", _BetaMessageStream_getFinalMessage).call(this);
      }
      /**
       * @returns a promise that resolves with the the final assistant Message's text response, concatenated
       * together if there are more than one text blocks.
       * Rejects if an error occurred or the stream ended prematurely without producing a Message.
       */
      async finalText() {
        return await this.done(), __classPrivateFieldGet(this, _BetaMessageStream_instances, "m", _BetaMessageStream_getFinalText).call(this);
      }
      _emit(event, ...args) {
        if (__classPrivateFieldGet(this, _BetaMessageStream_ended, "f"))
          return;
        event === "end" && (__classPrivateFieldSet(this, _BetaMessageStream_ended, !0, "f"), __classPrivateFieldGet(this, _BetaMessageStream_resolveEndPromise, "f").call(this));
        let listeners = __classPrivateFieldGet(this, _BetaMessageStream_listeners, "f")[event];
        if (listeners && (__classPrivateFieldGet(this, _BetaMessageStream_listeners, "f")[event] = listeners.filter((l) => !l.once), listeners.forEach(({ listener }) => listener(...args))), event === "abort") {
          let error = args[0];
          !__classPrivateFieldGet(this, _BetaMessageStream_catchingPromiseCreated, "f") && !listeners?.length && Promise.reject(error), __classPrivateFieldGet(this, _BetaMessageStream_rejectConnectedPromise, "f").call(this, error), __classPrivateFieldGet(this, _BetaMessageStream_rejectEndPromise, "f").call(this, error), this._emit("end");
          return;
        }
        if (event === "error") {
          let error = args[0];
          !__classPrivateFieldGet(this, _BetaMessageStream_catchingPromiseCreated, "f") && !listeners?.length && Promise.reject(error), __classPrivateFieldGet(this, _BetaMessageStream_rejectConnectedPromise, "f").call(this, error), __classPrivateFieldGet(this, _BetaMessageStream_rejectEndPromise, "f").call(this, error), this._emit("end");
        }
      }
      _emitFinal() {
        this.receivedMessages.at(-1) && this._emit("finalMessage", __classPrivateFieldGet(this, _BetaMessageStream_instances, "m", _BetaMessageStream_getFinalMessage).call(this));
      }
      async _fromReadableStream(readableStream, options) {
        let signal = options?.signal, abortHandler;
        signal && (signal.aborted && this.controller.abort(), abortHandler = this.controller.abort.bind(this.controller), signal.addEventListener("abort", abortHandler));
        try {
          __classPrivateFieldGet(this, _BetaMessageStream_instances, "m", _BetaMessageStream_beginRequest).call(this), this._connected(null);
          let stream = Stream.fromReadableStream(readableStream, this.controller);
          for await (let event of stream)
            __classPrivateFieldGet(this, _BetaMessageStream_instances, "m", _BetaMessageStream_addStreamEvent).call(this, event);
          if (stream.controller.signal?.aborted)
            throw new APIUserAbortError();
          __classPrivateFieldGet(this, _BetaMessageStream_instances, "m", _BetaMessageStream_endRequest).call(this);
        } finally {
          signal && abortHandler && signal.removeEventListener("abort", abortHandler);
        }
      }
      [(_BetaMessageStream_currentMessageSnapshot = /* @__PURE__ */ new WeakMap(), _BetaMessageStream_params = /* @__PURE__ */ new WeakMap(), _BetaMessageStream_connectedPromise = /* @__PURE__ */ new WeakMap(), _BetaMessageStream_resolveConnectedPromise = /* @__PURE__ */ new WeakMap(), _BetaMessageStream_rejectConnectedPromise = /* @__PURE__ */ new WeakMap(), _BetaMessageStream_endPromise = /* @__PURE__ */ new WeakMap(), _BetaMessageStream_resolveEndPromise = /* @__PURE__ */ new WeakMap(), _BetaMessageStream_rejectEndPromise = /* @__PURE__ */ new WeakMap(), _BetaMessageStream_listeners = /* @__PURE__ */ new WeakMap(), _BetaMessageStream_ended = /* @__PURE__ */ new WeakMap(), _BetaMessageStream_errored = /* @__PURE__ */ new WeakMap(), _BetaMessageStream_aborted = /* @__PURE__ */ new WeakMap(), _BetaMessageStream_catchingPromiseCreated = /* @__PURE__ */ new WeakMap(), _BetaMessageStream_response = /* @__PURE__ */ new WeakMap(), _BetaMessageStream_request_id = /* @__PURE__ */ new WeakMap(), _BetaMessageStream_logger = /* @__PURE__ */ new WeakMap(), _BetaMessageStream_handleError = /* @__PURE__ */ new WeakMap(), _BetaMessageStream_instances = /* @__PURE__ */ new WeakSet(), _BetaMessageStream_getFinalMessage = function() {
        if (this.receivedMessages.length === 0)
          throw new AnthropicError("stream ended without producing a Message with role=assistant");
        return this.receivedMessages.at(-1);
      }, _BetaMessageStream_getFinalText = function() {
        if (this.receivedMessages.length === 0)
          throw new AnthropicError("stream ended without producing a Message with role=assistant");
        let textBlocks = this.receivedMessages.at(-1).content.filter((block) => block.type === "text").map((block) => block.text);
        if (textBlocks.length === 0)
          throw new AnthropicError("stream ended without producing a content block with type=text");
        return textBlocks.join(" ");
      }, _BetaMessageStream_beginRequest = function() {
        this.ended || __classPrivateFieldSet(this, _BetaMessageStream_currentMessageSnapshot, void 0, "f");
      }, _BetaMessageStream_addStreamEvent = function(event) {
        if (this.ended)
          return;
        let messageSnapshot = __classPrivateFieldGet(this, _BetaMessageStream_instances, "m", _BetaMessageStream_accumulateMessage).call(this, event);
        switch (this._emit("streamEvent", event, messageSnapshot), event.type) {
          case "content_block_delta": {
            let content = messageSnapshot.content.at(-1);
            switch (event.delta.type) {
              case "text_delta": {
                content.type === "text" && this._emit("text", event.delta.text, content.text || "");
                break;
              }
              case "citations_delta": {
                content.type === "text" && this._emit("citation", event.delta.citation, content.citations ?? []);
                break;
              }
              case "input_json_delta": {
                if (tracksToolInput(content) && __classPrivateFieldGet(this, _BetaMessageStream_listeners, "f").inputJson?.length) {
                  let jsonSnapshot;
                  try {
                    jsonSnapshot = content.input;
                  } catch (err) {
                    __classPrivateFieldGet(this, _BetaMessageStream_handleError, "f").call(this, __classPrivateFieldGet(this, _BetaMessageStream_instances, "m", _BetaMessageStream_toolInputParseError).call(this, content, err));
                    break;
                  }
                  this._emit("inputJson", event.delta.partial_json, jsonSnapshot);
                }
                break;
              }
              case "thinking_delta": {
                content.type === "thinking" && this._emit("thinking", event.delta.thinking, content.thinking);
                break;
              }
              case "signature_delta": {
                content.type === "thinking" && this._emit("signature", content.signature);
                break;
              }
              case "compaction_delta": {
                content.type === "compaction" && content.content && this._emit("compaction", content.content);
                break;
              }
              default:
                event.delta;
            }
            break;
          }
          case "message_stop": {
            this._addMessageParam(messageSnapshot), this._addMessage(maybeParseBetaMessage(messageSnapshot, __classPrivateFieldGet(this, _BetaMessageStream_params, "f"), { logger: __classPrivateFieldGet(this, _BetaMessageStream_logger, "f") }), !0);
            break;
          }
          case "content_block_stop": {
            this._emit("contentBlock", messageSnapshot.content.at(-1));
            break;
          }
          case "message_start": {
            __classPrivateFieldSet(this, _BetaMessageStream_currentMessageSnapshot, messageSnapshot, "f");
            break;
          }
          case "content_block_start":
          case "message_delta":
            break;
        }
      }, _BetaMessageStream_endRequest = function() {
        if (this.ended)
          throw new AnthropicError("stream has ended, this shouldn't happen");
        let snapshot = __classPrivateFieldGet(this, _BetaMessageStream_currentMessageSnapshot, "f");
        if (!snapshot)
          throw new AnthropicError("request ended without sending any chunks");
        return __classPrivateFieldSet(this, _BetaMessageStream_currentMessageSnapshot, void 0, "f"), maybeParseBetaMessage(snapshot, __classPrivateFieldGet(this, _BetaMessageStream_params, "f"), { logger: __classPrivateFieldGet(this, _BetaMessageStream_logger, "f") });
      }, _BetaMessageStream_accumulateMessage = function(event) {
        let snapshot = __classPrivateFieldGet(this, _BetaMessageStream_currentMessageSnapshot, "f");
        if (event.type === "message_start") {
          if (snapshot)
            throw new AnthropicError(`Unexpected event order, got ${event.type} before receiving "message_stop"`);
          return event.message;
        }
        if (!snapshot)
          throw new AnthropicError(`Unexpected event order, got ${event.type} before "message_start"`);
        switch (event.type) {
          case "message_stop":
            return snapshot;
          case "message_delta":
            return snapshot.container = event.delta.container, snapshot.stop_reason = event.delta.stop_reason, snapshot.stop_sequence = event.delta.stop_sequence, event.delta.stop_details != null && (snapshot.stop_details = event.delta.stop_details), snapshot.usage.output_tokens = event.usage.output_tokens, snapshot.context_management = event.context_management, event.usage.input_tokens != null && (snapshot.usage.input_tokens = event.usage.input_tokens), event.usage.cache_creation_input_tokens != null && (snapshot.usage.cache_creation_input_tokens = event.usage.cache_creation_input_tokens), event.usage.cache_read_input_tokens != null && (snapshot.usage.cache_read_input_tokens = event.usage.cache_read_input_tokens), event.usage.server_tool_use != null && (snapshot.usage.server_tool_use = event.usage.server_tool_use), event.usage.iterations != null && (snapshot.usage.iterations = event.usage.iterations), snapshot;
          case "content_block_start":
            return snapshot.content.push(event.content_block), event.content_block.type === "fallback" && (snapshot.model = event.content_block.to.model), snapshot;
          case "content_block_delta": {
            let snapshotContent = snapshot.content.at(event.index);
            switch (event.delta.type) {
              case "text_delta": {
                snapshotContent?.type === "text" && (snapshot.content[event.index] = {
                  ...snapshotContent,
                  text: (snapshotContent.text || "") + event.delta.text
                });
                break;
              }
              case "citations_delta": {
                snapshotContent?.type === "text" && (snapshot.content[event.index] = {
                  ...snapshotContent,
                  citations: [...snapshotContent.citations ?? [], event.delta.citation]
                });
                break;
              }
              case "input_json_delta": {
                if (snapshotContent && tracksToolInput(snapshotContent)) {
                  let jsonBuf = (snapshotContent[JSON_BUF_PROPERTY] || "") + event.delta.partial_json;
                  snapshot.content[event.index] = withLazyInput(snapshotContent, jsonBuf);
                }
                break;
              }
              case "thinking_delta": {
                snapshotContent?.type === "thinking" && (snapshot.content[event.index] = {
                  ...snapshotContent,
                  thinking: snapshotContent.thinking + event.delta.thinking
                });
                break;
              }
              case "signature_delta": {
                snapshotContent?.type === "thinking" && (snapshot.content[event.index] = {
                  ...snapshotContent,
                  signature: event.delta.signature
                });
                break;
              }
              case "compaction_delta": {
                snapshotContent?.type === "compaction" && (snapshot.content[event.index] = {
                  ...snapshotContent,
                  content: (snapshotContent.content || "") + event.delta.content,
                  encrypted_content: event.delta.encrypted_content
                });
                break;
              }
              default:
                event.delta;
            }
            return snapshot;
          }
          case "content_block_stop": {
            let snapshotContent = snapshot.content.at(event.index);
            if (snapshotContent && tracksToolInput(snapshotContent) && JSON_BUF_PROPERTY in snapshotContent) {
              let input;
              try {
                input = snapshotContent.input;
              } catch (err) {
                input = {}, __classPrivateFieldGet(this, _BetaMessageStream_handleError, "f").call(this, __classPrivateFieldGet(this, _BetaMessageStream_instances, "m", _BetaMessageStream_toolInputParseError).call(this, snapshotContent, err));
              }
              Object.defineProperty(snapshotContent, "input", {
                value: input,
                enumerable: !0,
                configurable: !0,
                writable: !0
              });
            }
            return snapshot;
          }
        }
      }, _BetaMessageStream_toolInputParseError = function(block, err) {
        let jsonBuf = block[JSON_BUF_PROPERTY];
        return new AnthropicError(`Unable to parse tool parameter JSON from model. Please retry your request or adjust your prompt. Error: ${err}. JSON: ${jsonBuf}`);
      }, Symbol.asyncIterator)]() {
        let pushQueue = [], readQueue = [], done = !1;
        return this.on("streamEvent", (event) => {
          let reader = readQueue.shift();
          reader ? reader.resolve(event) : pushQueue.push(event);
        }), this.on("end", () => {
          done = !0;
          for (let reader of readQueue)
            reader.resolve(void 0);
          readQueue.length = 0;
        }), this.on("abort", (err) => {
          done = !0;
          for (let reader of readQueue)
            reader.reject(err);
          readQueue.length = 0;
        }), this.on("error", (err) => {
          done = !0;
          for (let reader of readQueue)
            reader.reject(err);
          readQueue.length = 0;
        }), {
          next: async () => pushQueue.length ? { value: pushQueue.shift(), done: !1 } : done ? { value: void 0, done: !0 } : new Promise((resolve4, reject) => readQueue.push({ resolve: resolve4, reject })).then((chunk2) => chunk2 ? { value: chunk2, done: !1 } : { value: void 0, done: !0 }),
          return: async () => (this.abort(), { value: void 0, done: !0 })
        };
      }
      toReadableStream() {
        return new Stream(this[Symbol.asyncIterator].bind(this), this.controller).toReadableStream();
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/tools/CompactionControl.mjs
var DEFAULT_SUMMARY_PROMPT, init_CompactionControl = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/tools/CompactionControl.mjs"() {
    DEFAULT_SUMMARY_PROMPT = `You have been working on the task described above but have not yet completed it. Write a continuation summary that will allow you (or another instance of yourself) to resume work efficiently in a future context window where the conversation history will be replaced with this summary. Your summary should be structured, concise, and actionable. Include:
1. Task Overview
The user's core request and success criteria
Any clarifications or constraints they specified
2. Current State
What has been completed so far
Files created, modified, or analyzed (with paths if relevant)
Key outputs or artifacts produced
3. Important Discoveries
Technical constraints or requirements uncovered
Decisions made and their rationale
Errors encountered and how they were resolved
What approaches were tried that didn't work (and why)
4. Next Steps
Specific actions needed to complete the task
Any blockers or open questions to resolve
Priority order if multiple steps remain
5. Context to Preserve
User preferences or style requirements
Domain-specific details that aren't obvious
Any promises made to the user
Be concise but complete\u2014err on the side of including information that would prevent duplicate work or repeated mistakes. Write in a way that enables immediate resumption of the task.
Wrap your summary in <summary></summary> tags.`;
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/tools/BetaToolRunner.mjs
async function generateToolResponse(params, lastMessage = params.messages.at(-1), requestOptions) {
  if (!lastMessage || lastMessage.role !== "assistant" || !lastMessage.content || typeof lastMessage.content == "string")
    return null;
  let toolUseBlocks = lastMessage.content.filter((content) => content.type === "tool_use");
  return toolUseBlocks.length === 0 ? null : {
    role: "user",
    content: await Promise.all(toolUseBlocks.map(async (toolUse) => {
      let tool = params.tools.find((t) => ("name" in t ? t.name : t.mcp_server_name) === toolUse.name);
      if (!tool || !("run" in tool))
        return {
          type: "tool_result",
          tool_use_id: toolUse.id,
          content: `Error: Tool '${toolUse.name}' not found`,
          is_error: !0
        };
      try {
        let input = toolUse.input;
        "parse" in tool && tool.parse && (input = tool.parse(input));
        let result = await tool.run(input, {
          toolUse,
          toolUseBlock: toolUse,
          signal: requestOptions?.signal
        });
        return {
          type: "tool_result",
          tool_use_id: toolUse.id,
          content: result
        };
      } catch (error) {
        return {
          type: "tool_result",
          tool_use_id: toolUse.id,
          content: error instanceof ToolError ? error.content : `Error: ${error instanceof Error ? error.message : String(error)}`,
          is_error: !0
        };
      }
    }))
  };
}
var _BetaToolRunner_instances, _BetaToolRunner_consumed, _BetaToolRunner_mutated, _BetaToolRunner_state, _BetaToolRunner_options, _BetaToolRunner_message, _BetaToolRunner_toolResponse, _BetaToolRunner_completion, _BetaToolRunner_iterationCount, _BetaToolRunner_checkAndCompact, _BetaToolRunner_generateToolResponse, BetaToolRunner, init_BetaToolRunner = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/tools/BetaToolRunner.mjs"() {
    init_tslib();
    init_ToolError();
    init_error();
    init_headers();
    init_promise();
    init_CompactionControl();
    init_stainless_helper_header();
    BetaToolRunner = class {
      constructor(client, params, options) {
        _BetaToolRunner_instances.add(this), this.client = client, _BetaToolRunner_consumed.set(this, !1), _BetaToolRunner_mutated.set(this, !1), _BetaToolRunner_state.set(this, void 0), _BetaToolRunner_options.set(this, void 0), _BetaToolRunner_message.set(this, void 0), _BetaToolRunner_toolResponse.set(this, void 0), _BetaToolRunner_completion.set(this, void 0), _BetaToolRunner_iterationCount.set(this, 0), __classPrivateFieldSet(this, _BetaToolRunner_state, {
          params: {
            // You can't clone the entire params since there are functions as handlers.
            // You also don't really need to clone params.messages, but it probably will prevent a foot gun
            // somewhere.
            ...params,
            messages: structuredClone(params.messages)
          }
        }, "f");
        let collected = collectStainlessHelpers(params.tools, params.messages);
        __classPrivateFieldSet(this, _BetaToolRunner_options, {
          ...options,
          headers: buildHeaders([
            helperHeader("BetaToolRunner"),
            collected.length ? { [STAINLESS_HELPER_HEADER]: collected.join(", ") } : void 0,
            options?.headers
          ])
        }, "f"), __classPrivateFieldSet(this, _BetaToolRunner_completion, promiseWithResolvers(), "f"), params.compactionControl?.enabled && console.warn('Anthropic: The `compactionControl` parameter is deprecated and will be removed in a future version. Use server-side compaction instead by passing `edits: [{ type: "compact_20260112" }]` in the params passed to `toolRunner()`. See https://platform.claude.com/docs/en/build-with-claude/compaction');
      }
      async *[(_BetaToolRunner_consumed = /* @__PURE__ */ new WeakMap(), _BetaToolRunner_mutated = /* @__PURE__ */ new WeakMap(), _BetaToolRunner_state = /* @__PURE__ */ new WeakMap(), _BetaToolRunner_options = /* @__PURE__ */ new WeakMap(), _BetaToolRunner_message = /* @__PURE__ */ new WeakMap(), _BetaToolRunner_toolResponse = /* @__PURE__ */ new WeakMap(), _BetaToolRunner_completion = /* @__PURE__ */ new WeakMap(), _BetaToolRunner_iterationCount = /* @__PURE__ */ new WeakMap(), _BetaToolRunner_instances = /* @__PURE__ */ new WeakSet(), _BetaToolRunner_checkAndCompact = async function() {
        let compactionControl = __classPrivateFieldGet(this, _BetaToolRunner_state, "f").params.compactionControl;
        if (!compactionControl || !compactionControl.enabled)
          return !1;
        let tokensUsed = 0;
        if (__classPrivateFieldGet(this, _BetaToolRunner_message, "f") !== void 0)
          try {
            let message = await __classPrivateFieldGet(this, _BetaToolRunner_message, "f");
            tokensUsed = message.usage.input_tokens + (message.usage.cache_creation_input_tokens ?? 0) + (message.usage.cache_read_input_tokens ?? 0) + message.usage.output_tokens;
          } catch {
            return !1;
          }
        let threshold = compactionControl.contextTokenThreshold ?? 1e5;
        if (tokensUsed < threshold)
          return !1;
        let model = compactionControl.model ?? __classPrivateFieldGet(this, _BetaToolRunner_state, "f").params.model, summaryPrompt = compactionControl.summaryPrompt ?? DEFAULT_SUMMARY_PROMPT, messages = __classPrivateFieldGet(this, _BetaToolRunner_state, "f").params.messages;
        if (messages[messages.length - 1].role === "assistant") {
          let lastMessage = messages[messages.length - 1];
          if (Array.isArray(lastMessage.content)) {
            let nonToolBlocks = lastMessage.content.filter((block) => block.type !== "tool_use");
            nonToolBlocks.length === 0 ? messages.pop() : lastMessage.content = nonToolBlocks;
          }
        }
        let response = await this.client.beta.messages.create({
          model,
          messages: [
            ...messages,
            {
              role: "user",
              content: [
                {
                  type: "text",
                  text: summaryPrompt
                }
              ]
            }
          ],
          max_tokens: __classPrivateFieldGet(this, _BetaToolRunner_state, "f").params.max_tokens
        }, {
          signal: __classPrivateFieldGet(this, _BetaToolRunner_options, "f").signal,
          headers: buildHeaders([__classPrivateFieldGet(this, _BetaToolRunner_options, "f").headers, helperHeader("compaction")])
        });
        if (response.content[0]?.type !== "text")
          throw new AnthropicError("Expected text response for compaction");
        return __classPrivateFieldGet(this, _BetaToolRunner_state, "f").params.messages = [
          {
            role: "user",
            content: response.content
          }
        ], !0;
      }, Symbol.asyncIterator)]() {
        var _a5;
        if (__classPrivateFieldGet(this, _BetaToolRunner_consumed, "f"))
          throw new AnthropicError("Cannot iterate over a consumed stream");
        __classPrivateFieldSet(this, _BetaToolRunner_consumed, !0, "f"), __classPrivateFieldSet(this, _BetaToolRunner_mutated, !0, "f"), __classPrivateFieldSet(this, _BetaToolRunner_toolResponse, void 0, "f");
        try {
          for (; ; ) {
            let stream;
            try {
              if (__classPrivateFieldGet(this, _BetaToolRunner_state, "f").params.max_iterations && __classPrivateFieldGet(this, _BetaToolRunner_iterationCount, "f") >= __classPrivateFieldGet(this, _BetaToolRunner_state, "f").params.max_iterations)
                break;
              __classPrivateFieldSet(this, _BetaToolRunner_mutated, !1, "f"), __classPrivateFieldSet(this, _BetaToolRunner_toolResponse, void 0, "f"), __classPrivateFieldSet(this, _BetaToolRunner_iterationCount, (_a5 = __classPrivateFieldGet(this, _BetaToolRunner_iterationCount, "f"), _a5++, _a5), "f"), __classPrivateFieldSet(this, _BetaToolRunner_message, void 0, "f");
              let { max_iterations, compactionControl, ...params } = __classPrivateFieldGet(this, _BetaToolRunner_state, "f").params;
              if (params.stream ? (stream = this.client.beta.messages.stream({ ...params }, __classPrivateFieldGet(this, _BetaToolRunner_options, "f")), __classPrivateFieldSet(this, _BetaToolRunner_message, stream.finalMessage(), "f"), __classPrivateFieldGet(this, _BetaToolRunner_message, "f").catch(() => {
              }), yield stream) : (__classPrivateFieldSet(this, _BetaToolRunner_message, this.client.beta.messages.create({ ...params, stream: !1 }, __classPrivateFieldGet(this, _BetaToolRunner_options, "f")), "f"), yield __classPrivateFieldGet(this, _BetaToolRunner_message, "f")), !await __classPrivateFieldGet(this, _BetaToolRunner_instances, "m", _BetaToolRunner_checkAndCompact).call(this)) {
                if (!__classPrivateFieldGet(this, _BetaToolRunner_mutated, "f")) {
                  let message = await __classPrivateFieldGet(this, _BetaToolRunner_message, "f");
                  if (__classPrivateFieldGet(this, _BetaToolRunner_state, "f").params.messages.push({ role: message.role, content: message.content }), message.stop_reason === "refusal")
                    break;
                }
                let toolMessage = await __classPrivateFieldGet(this, _BetaToolRunner_instances, "m", _BetaToolRunner_generateToolResponse).call(this, __classPrivateFieldGet(this, _BetaToolRunner_state, "f").params.messages.at(-1));
                if (toolMessage)
                  __classPrivateFieldGet(this, _BetaToolRunner_state, "f").params.messages.push(toolMessage);
                else if (!__classPrivateFieldGet(this, _BetaToolRunner_mutated, "f"))
                  break;
              }
            } finally {
              stream && stream.abort();
            }
          }
          if (!__classPrivateFieldGet(this, _BetaToolRunner_message, "f"))
            throw new AnthropicError("ToolRunner concluded without a message from the server");
          __classPrivateFieldGet(this, _BetaToolRunner_completion, "f").resolve(await __classPrivateFieldGet(this, _BetaToolRunner_message, "f"));
        } catch (error) {
          throw __classPrivateFieldSet(this, _BetaToolRunner_consumed, !1, "f"), __classPrivateFieldGet(this, _BetaToolRunner_completion, "f").promise.catch(() => {
          }), __classPrivateFieldGet(this, _BetaToolRunner_completion, "f").reject(error), __classPrivateFieldSet(this, _BetaToolRunner_completion, promiseWithResolvers(), "f"), error;
        }
      }
      setMessagesParams(paramsOrMutator) {
        typeof paramsOrMutator == "function" ? __classPrivateFieldGet(this, _BetaToolRunner_state, "f").params = paramsOrMutator(__classPrivateFieldGet(this, _BetaToolRunner_state, "f").params) : __classPrivateFieldGet(this, _BetaToolRunner_state, "f").params = paramsOrMutator, __classPrivateFieldSet(this, _BetaToolRunner_mutated, !0, "f"), __classPrivateFieldSet(this, _BetaToolRunner_toolResponse, void 0, "f");
      }
      setRequestOptions(optionsOrMutator) {
        typeof optionsOrMutator == "function" ? __classPrivateFieldSet(this, _BetaToolRunner_options, optionsOrMutator(__classPrivateFieldGet(this, _BetaToolRunner_options, "f")), "f") : __classPrivateFieldSet(this, _BetaToolRunner_options, { ...__classPrivateFieldGet(this, _BetaToolRunner_options, "f"), ...optionsOrMutator }, "f");
      }
      /**
       * Get the tool response for the last message from the assistant.
       * Avoids redundant tool executions by caching results.
       *
       * @returns A promise that resolves to a BetaMessageParam containing tool results, or null if no tools need to be executed
       *
       * @example
       * const toolResponse = await runner.generateToolResponse();
       * if (toolResponse) {
       *   console.log('Tool results:', toolResponse.content);
       * }
       */
      async generateToolResponse(signal = __classPrivateFieldGet(this, _BetaToolRunner_options, "f").signal) {
        let message = await __classPrivateFieldGet(this, _BetaToolRunner_message, "f") ?? this.params.messages.at(-1);
        return message ? __classPrivateFieldGet(this, _BetaToolRunner_instances, "m", _BetaToolRunner_generateToolResponse).call(this, message, signal) : null;
      }
      /**
       * Wait for the async iterator to complete. This works even if the async iterator hasn't yet started, and
       * will wait for an instance to start and go to completion.
       *
       * @returns A promise that resolves to the final BetaMessage when the iterator completes
       *
       * @example
       * // Start consuming the iterator
       * for await (const message of runner) {
       *   console.log('Message:', message.content);
       * }
       *
       * // Meanwhile, wait for completion from another part of the code
       * const finalMessage = await runner.done();
       * console.log('Final response:', finalMessage.content);
       */
      done() {
        return __classPrivateFieldGet(this, _BetaToolRunner_completion, "f").promise;
      }
      /**
       * Returns a promise indicating that the stream is done. Unlike .done(), this will eagerly read the stream:
       * * If the iterator has not been consumed, consume the entire iterator and return the final message from the
       * assistant.
       * * If the iterator has been consumed, waits for it to complete and returns the final message.
       *
       * @returns A promise that resolves to the final BetaMessage from the conversation
       * @throws {AnthropicError} If no messages were processed during the conversation
       *
       * @example
       * const finalMessage = await runner.runUntilDone();
       * console.log('Final response:', finalMessage.content);
       */
      async runUntilDone() {
        if (!__classPrivateFieldGet(this, _BetaToolRunner_consumed, "f"))
          for await (let _ of this)
            ;
        return this.done();
      }
      /**
       * Get the current parameters being used by the ToolRunner.
       *
       * @returns A readonly view of the current ToolRunnerParams
       *
       * @example
       * const currentParams = runner.params;
       * console.log('Current model:', currentParams.model);
       * console.log('Message count:', currentParams.messages.length);
       */
      get params() {
        return __classPrivateFieldGet(this, _BetaToolRunner_state, "f").params;
      }
      /**
       * Add one or more messages to the conversation history.
       *
       * @param messages - One or more BetaMessageParam objects to add to the conversation
       *
       * @example
       * runner.pushMessages(
       *   { role: 'user', content: 'Also, what about the weather in NYC?' }
       * );
       *
       * @example
       * // Adding multiple messages
       * runner.pushMessages(
       *   { role: 'user', content: 'What about NYC?' },
       *   { role: 'user', content: 'And Boston?' }
       * );
       */
      pushMessages(...messages) {
        this.setMessagesParams((params) => ({
          ...params,
          messages: [...params.messages, ...messages]
        }));
      }
      /**
       * Makes the ToolRunner directly awaitable, equivalent to calling .runUntilDone()
       * This allows using `await runner` instead of `await runner.runUntilDone()`
       */
      then(onfulfilled, onrejected) {
        return this.runUntilDone().then(onfulfilled, onrejected);
      }
    };
    _BetaToolRunner_generateToolResponse = async function(lastMessage, signal = __classPrivateFieldGet(this, _BetaToolRunner_options, "f").signal) {
      return __classPrivateFieldGet(this, _BetaToolRunner_toolResponse, "f") !== void 0 ? __classPrivateFieldGet(this, _BetaToolRunner_toolResponse, "f") : (__classPrivateFieldSet(this, _BetaToolRunner_toolResponse, generateToolResponse(__classPrivateFieldGet(this, _BetaToolRunner_state, "f").params, lastMessage, {
        ...__classPrivateFieldGet(this, _BetaToolRunner_options, "f"),
        signal
      }), "f"), __classPrivateFieldGet(this, _BetaToolRunner_toolResponse, "f"));
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/messages/messages.mjs
function transformOutputFormat(params) {
  if (!params.output_format)
    return params;
  if (params.output_config?.format)
    throw new AnthropicError("Both output_format and output_config.format were provided. Please use only output_config.format (output_format is deprecated).");
  let { output_format, ...rest } = params;
  return {
    ...rest,
    output_config: {
      ...params.output_config,
      format: output_format
    }
  };
}
var DEPRECATED_MODELS, MODELS_TO_WARN_WITH_THINKING_ENABLED, Messages, init_messages = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/messages/messages.mjs"() {
    init_error2();
    init_batches();
    init_resource();
    init_constants();
    init_headers();
    init_stainless_helper_header();
    init_beta_parser();
    init_BetaMessageStream();
    init_BetaToolRunner();
    init_ToolError();
    init_batches();
    init_BetaToolRunner();
    init_ToolError();
    DEPRECATED_MODELS = {
      "claude-1.3": "November 6th, 2024",
      "claude-1.3-100k": "November 6th, 2024",
      "claude-instant-1.1": "November 6th, 2024",
      "claude-instant-1.1-100k": "November 6th, 2024",
      "claude-instant-1.2": "November 6th, 2024",
      "claude-3-sonnet-20240229": "July 21st, 2025",
      "claude-3-opus-20240229": "January 5th, 2026",
      "claude-2.1": "July 21st, 2025",
      "claude-2.0": "July 21st, 2025",
      "claude-3-7-sonnet-latest": "February 19th, 2026",
      "claude-3-7-sonnet-20250219": "February 19th, 2026",
      "claude-3-5-haiku-latest": "February 19th, 2026",
      "claude-3-5-haiku-20241022": "February 19th, 2026",
      "claude-opus-4-0": "June 15th, 2026",
      "claude-opus-4-20250514": "June 15th, 2026",
      "claude-sonnet-4-0": "June 15th, 2026",
      "claude-sonnet-4-20250514": "June 15th, 2026",
      "claude-opus-4-1": "August 5th, 2026",
      "claude-opus-4-1-20250805": "August 5th, 2026",
      "claude-mythos-preview": "June 30th, 2026"
    }, MODELS_TO_WARN_WITH_THINKING_ENABLED = ["claude-mythos-preview", "claude-opus-4-6"], Messages = class extends APIResource {
      constructor() {
        super(...arguments), this.batches = new Batches(this._client);
      }
      create(params, options) {
        let modifiedParams = transformOutputFormat(params), { betas, user_profile_id, ...body } = modifiedParams;
        body.model in DEPRECATED_MODELS && console.warn(`The model '${body.model}' is deprecated and will reach end-of-life on ${DEPRECATED_MODELS[body.model]}
Please migrate to a newer model. Visit https://docs.anthropic.com/en/docs/resources/model-deprecations for more information.`), MODELS_TO_WARN_WITH_THINKING_ENABLED.includes(body.model) && body.thinking && body.thinking.type === "enabled" && console.warn(`Using Claude with ${body.model} and 'thinking.type=enabled' is deprecated. Use 'thinking.type=adaptive' instead which results in better model performance in our testing: https://platform.claude.com/docs/en/build-with-claude/adaptive-thinking`);
        let timeout = this._client._options.timeout;
        if (!body.stream && timeout == null) {
          let maxNonstreamingTokens = MODEL_NONSTREAMING_TOKENS[body.model] ?? void 0;
          timeout = this._client.calculateNonstreamingTimeout(body.max_tokens, maxNonstreamingTokens);
        }
        let helperHeader2 = stainlessHelperHeader(body.tools, body.messages);
        return this._client.post("/v1/messages?beta=true", {
          body,
          timeout: timeout ?? 6e5,
          ...options,
          headers: buildHeaders([
            {
              ...betas?.toString() != null ? { "anthropic-beta": betas?.toString() } : void 0,
              ...user_profile_id != null ? { "anthropic-user-profile-id": user_profile_id } : void 0
            },
            helperHeader2,
            options?.headers
          ]),
          stream: modifiedParams.stream ?? !1
        });
      }
      /**
       * Send a structured list of input messages with text and/or image content, along with an expected `output_format` and
       * the response will be automatically parsed and available in the `parsed_output` property of the message.
       *
       * @example
       * ```ts
       * const message = await client.beta.messages.parse({
       *   model: 'claude-3-5-sonnet-20241022',
       *   max_tokens: 1024,
       *   messages: [{ role: 'user', content: 'What is 2+2?' }],
       *   output_format: zodOutputFormat(z.object({ answer: z.number() }), 'math'),
       * });
       *
       * console.log(message.parsed_output?.answer); // 4
       * ```
       */
      parse(params, options) {
        return options = {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...params.betas ?? [], "structured-outputs-2025-12-15"].toString() },
            options?.headers
          ])
        }, this.create(params, options).then((message) => parseBetaMessage(message, params, { logger: this._client.logger ?? console }));
      }
      /**
       * Create a Message stream
       */
      stream(body, options) {
        return BetaMessageStream.createMessage(this, body, options);
      }
      /**
       * Count the number of tokens in a Message.
       *
       * The Token Count API can be used to count the number of tokens in a Message,
       * including tools, images, and documents, without creating it.
       *
       * Learn more about token counting in our
       * [user guide](https://platform.claude.com/docs/en/build-with-claude/token-counting)
       *
       * @example
       * ```ts
       * const betaMessageTokensCount =
       *   await client.beta.messages.countTokens({
       *     messages: [{ content: 'Hello, world', role: 'user' }],
       *     model: 'claude-opus-4-6',
       *   });
       * ```
       */
      countTokens(params, options) {
        let modifiedParams = transformOutputFormat(params), { betas, user_profile_id, ...body } = modifiedParams;
        return this._client.post("/v1/messages/count_tokens?beta=true", {
          body,
          ...options,
          headers: buildHeaders([
            {
              "anthropic-beta": [...betas ?? [], "token-counting-2024-11-01"].toString(),
              ...user_profile_id != null ? { "anthropic-user-profile-id": user_profile_id } : void 0
            },
            options?.headers
          ])
        });
      }
      toolRunner(body, options) {
        return new BetaToolRunner(this._client, body, options);
      }
    };
    Messages.Batches = Batches;
    Messages.BetaToolRunner = BetaToolRunner;
    Messages.ToolError = ToolError;
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/sessions/events.mjs
var Events, init_events = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/sessions/events.mjs"() {
    init_resource();
    init_pagination();
    init_headers();
    init_path();
    init_SessionToolRunner();
    init_SessionToolRunner();
    Events = class extends APIResource {
      /**
       * List Events
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const betaManagedAgentsSessionEvent of client.beta.sessions.events.list(
       *   'sesn_011CZkZAtmR3yMPDzynEDxu7',
       * )) {
       *   // ...
       * }
       * ```
       */
      list(sessionID, params = {}, options) {
        let { betas, ...query } = params ?? {};
        return this._client.getAPIList(path3`/v1/sessions/${sessionID}/events?beta=true`, PageCursor, {
          query,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Send Events
       *
       * @example
       * ```ts
       * const betaManagedAgentsSendSessionEvents =
       *   await client.beta.sessions.events.send(
       *     'sesn_011CZkZAtmR3yMPDzynEDxu7',
       *     {
       *       events: [
       *         {
       *           content: [
       *             {
       *               text: 'Where is my order #1234?',
       *               type: 'text',
       *             },
       *           ],
       *           type: 'user.message',
       *         },
       *       ],
       *     },
       *   );
       * ```
       */
      send(sessionID, params, options) {
        let { betas, ...body } = params;
        return this._client.post(path3`/v1/sessions/${sessionID}/events?beta=true`, {
          body,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Stream Events
       *
       * @example
       * ```ts
       * const betaManagedAgentsStreamSessionEvents =
       *   await client.beta.sessions.events.stream(
       *     'sesn_011CZkZAtmR3yMPDzynEDxu7',
       *   );
       * ```
       */
      stream(sessionID, params = {}, options) {
        let { betas, ...query } = params ?? {};
        return this._client.get(path3`/v1/sessions/${sessionID}/events/stream?beta=true`, {
          query,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ]),
          stream: !0
        });
      }
      /**
       * Attach to a session and dispatch every incoming `agent.tool_use` and
       * `agent.custom_tool_use` event to a local tool registry, sending the matching
       * result back (`user.tool_result` / `user.custom_tool_result`). The
       * sessions-side counterpart to `client.beta.messages.toolRunner`: yields one
       * entry per completed tool call so callers can observe each dispatch (and
       * `break` to abort cleanly).
       *
       * @example
       * ```ts
       * import { betaAgentToolset20260401 } from '@anthropic-ai/sdk/tools/agent-toolset/node';
       *
       * for await (const call of client.beta.sessions.events.toolRunner(work.data.id, {
       *   tools: [...betaAgentToolset20260401({ workdir }), myTool],
       * })) {
       *   console.log(`${call.name} -> ${call.isError ? 'error' : 'ok'}`);
       * }
       * ```
       */
      toolRunner(sessionID, opts) {
        return new SessionToolRunner(sessionID, { ...opts, client: this._client });
      }
    };
    Events.SessionToolRunner = SessionToolRunner;
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/sessions/resources.mjs
var Resources, init_resources = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/sessions/resources.mjs"() {
    init_resource();
    init_pagination();
    init_headers();
    init_path();
    Resources = class extends APIResource {
      /**
       * Get Session Resource
       *
       * @example
       * ```ts
       * const resource =
       *   await client.beta.sessions.resources.retrieve(
       *     'sesrsc_011CZkZBJq5dWxk9fVLNcPht',
       *     { session_id: 'sesn_011CZkZAtmR3yMPDzynEDxu7' },
       *   );
       * ```
       */
      retrieve(resourceID, params, options) {
        let { session_id, betas } = params;
        return this._client.get(path3`/v1/sessions/${session_id}/resources/${resourceID}?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Update Session Resource
       *
       * @example
       * ```ts
       * const resource =
       *   await client.beta.sessions.resources.update(
       *     'sesrsc_011CZkZBJq5dWxk9fVLNcPht',
       *     {
       *       session_id: 'sesn_011CZkZAtmR3yMPDzynEDxu7',
       *       authorization_token: 'ghp_exampletoken',
       *     },
       *   );
       * ```
       */
      update(resourceID, params, options) {
        let { session_id, betas, ...body } = params;
        return this._client.post(path3`/v1/sessions/${session_id}/resources/${resourceID}?beta=true`, {
          body,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * List Session Resources
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const betaManagedAgentsSessionResource of client.beta.sessions.resources.list(
       *   'sesn_011CZkZAtmR3yMPDzynEDxu7',
       * )) {
       *   // ...
       * }
       * ```
       */
      list(sessionID, params = {}, options) {
        let { betas, ...query } = params ?? {};
        return this._client.getAPIList(path3`/v1/sessions/${sessionID}/resources?beta=true`, PageCursor, {
          query,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Delete Session Resource
       *
       * @example
       * ```ts
       * const betaManagedAgentsDeleteSessionResource =
       *   await client.beta.sessions.resources.delete(
       *     'sesrsc_011CZkZBJq5dWxk9fVLNcPht',
       *     { session_id: 'sesn_011CZkZAtmR3yMPDzynEDxu7' },
       *   );
       * ```
       */
      delete(resourceID, params, options) {
        let { session_id, betas } = params;
        return this._client.delete(path3`/v1/sessions/${session_id}/resources/${resourceID}?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Add Session Resource
       *
       * @example
       * ```ts
       * const betaManagedAgentsFileResource =
       *   await client.beta.sessions.resources.add(
       *     'sesn_011CZkZAtmR3yMPDzynEDxu7',
       *     {
       *       file_id: 'file_011CNha8iCJcU1wXNR6q4V8w',
       *       type: 'file',
       *     },
       *   );
       * ```
       */
      add(sessionID, params, options) {
        let { betas, ...body } = params;
        return this._client.post(path3`/v1/sessions/${sessionID}/resources?beta=true`, {
          body,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/sessions/threads/events.mjs
var Events2, init_events2 = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/sessions/threads/events.mjs"() {
    init_resource();
    init_pagination();
    init_headers();
    init_path();
    Events2 = class extends APIResource {
      /**
       * List Session Thread Events
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const betaManagedAgentsSessionEvent of client.beta.sessions.threads.events.list(
       *   'sthr_011CZkZVWa6oIjw0rgXZpnBt',
       *   { session_id: 'sesn_011CZkZAtmR3yMPDzynEDxu7' },
       * )) {
       *   // ...
       * }
       * ```
       */
      list(threadID, params, options) {
        let { session_id, betas, ...query } = params;
        return this._client.getAPIList(path3`/v1/sessions/${session_id}/threads/${threadID}/events?beta=true`, PageCursor, {
          query,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Stream Session Thread Events
       *
       * @example
       * ```ts
       * const betaManagedAgentsStreamSessionThreadEvents =
       *   await client.beta.sessions.threads.events.stream(
       *     'sthr_011CZkZVWa6oIjw0rgXZpnBt',
       *     { session_id: 'sesn_011CZkZAtmR3yMPDzynEDxu7' },
       *   );
       * ```
       */
      stream(threadID, params, options) {
        let { session_id, betas } = params;
        return this._client.get(path3`/v1/sessions/${session_id}/threads/${threadID}/stream?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ]),
          stream: !0
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/sessions/threads/threads.mjs
var Threads, init_threads = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/sessions/threads/threads.mjs"() {
    init_resource();
    init_events2();
    init_events2();
    init_pagination();
    init_headers();
    init_path();
    Threads = class extends APIResource {
      constructor() {
        super(...arguments), this.events = new Events2(this._client);
      }
      /**
       * Get Session Thread
       *
       * @example
       * ```ts
       * const betaManagedAgentsSessionThread =
       *   await client.beta.sessions.threads.retrieve(
       *     'sthr_011CZkZVWa6oIjw0rgXZpnBt',
       *     { session_id: 'sesn_011CZkZAtmR3yMPDzynEDxu7' },
       *   );
       * ```
       */
      retrieve(threadID, params, options) {
        let { session_id, betas } = params;
        return this._client.get(path3`/v1/sessions/${session_id}/threads/${threadID}?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * List Session Threads
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const betaManagedAgentsSessionThread of client.beta.sessions.threads.list(
       *   'sesn_011CZkZAtmR3yMPDzynEDxu7',
       * )) {
       *   // ...
       * }
       * ```
       */
      list(sessionID, params = {}, options) {
        let { betas, ...query } = params ?? {};
        return this._client.getAPIList(path3`/v1/sessions/${sessionID}/threads?beta=true`, PageCursor, {
          query,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Archive Session Thread
       *
       * @example
       * ```ts
       * const betaManagedAgentsSessionThread =
       *   await client.beta.sessions.threads.archive(
       *     'sthr_011CZkZVWa6oIjw0rgXZpnBt',
       *     { session_id: 'sesn_011CZkZAtmR3yMPDzynEDxu7' },
       *   );
       * ```
       */
      archive(threadID, params, options) {
        let { session_id, betas } = params;
        return this._client.post(path3`/v1/sessions/${session_id}/threads/${threadID}/archive?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
    };
    Threads.Events = Events2;
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/sessions/sessions.mjs
var Sessions, init_sessions = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/sessions/sessions.mjs"() {
    init_resource();
    init_events();
    init_events();
    init_resources();
    init_resources();
    init_threads();
    init_threads();
    init_pagination();
    init_headers();
    init_path();
    Sessions = class extends APIResource {
      constructor() {
        super(...arguments), this.events = new Events(this._client), this.resources = new Resources(this._client), this.threads = new Threads(this._client);
      }
      /**
       * Create Session
       *
       * @example
       * ```ts
       * const betaManagedAgentsSession =
       *   await client.beta.sessions.create({
       *     agent: 'agent_011CZkYpogX7uDKUyvBTophP',
       *     environment_id: 'env_011CZkZ9X2dpNyB7HsEFoRfW',
       *   });
       * ```
       */
      create(params, options) {
        let { betas, ...body } = params;
        return this._client.post("/v1/sessions?beta=true", {
          body,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Get Session
       *
       * @example
       * ```ts
       * const betaManagedAgentsSession =
       *   await client.beta.sessions.retrieve(
       *     'sesn_011CZkZAtmR3yMPDzynEDxu7',
       *   );
       * ```
       */
      retrieve(sessionID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.get(path3`/v1/sessions/${sessionID}?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Update Session
       *
       * @example
       * ```ts
       * const betaManagedAgentsSession =
       *   await client.beta.sessions.update(
       *     'sesn_011CZkZAtmR3yMPDzynEDxu7',
       *   );
       * ```
       */
      update(sessionID, params, options) {
        let { betas, ...body } = params;
        return this._client.post(path3`/v1/sessions/${sessionID}?beta=true`, {
          body,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * List Sessions
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const betaManagedAgentsSession of client.beta.sessions.list()) {
       *   // ...
       * }
       * ```
       */
      list(params = {}, options) {
        let { betas, ...query } = params ?? {};
        return this._client.getAPIList("/v1/sessions?beta=true", BidirectionalPageCursor, {
          query,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Delete Session
       *
       * @example
       * ```ts
       * const betaManagedAgentsDeletedSession =
       *   await client.beta.sessions.delete(
       *     'sesn_011CZkZAtmR3yMPDzynEDxu7',
       *   );
       * ```
       */
      delete(sessionID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.delete(path3`/v1/sessions/${sessionID}?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Archive Session
       *
       * @example
       * ```ts
       * const betaManagedAgentsSession =
       *   await client.beta.sessions.archive(
       *     'sesn_011CZkZAtmR3yMPDzynEDxu7',
       *   );
       * ```
       */
      archive(sessionID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.post(path3`/v1/sessions/${sessionID}/archive?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
    };
    Sessions.Events = Events;
    Sessions.Resources = Resources;
    Sessions.Threads = Threads;
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/skills/versions.mjs
var Versions2, init_versions2 = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/skills/versions.mjs"() {
    init_resource();
    init_pagination();
    init_headers();
    init_uploads();
    init_path();
    Versions2 = class extends APIResource {
      /**
       * Create Skill Version
       *
       * @example
       * ```ts
       * const version = await client.beta.skills.versions.create(
       *   'skill_id',
       *   { files: [fs.createReadStream('path/to/file')] },
       * );
       * ```
       */
      create(skillID, params, options) {
        let { betas, ...body } = params;
        return this._client.post(path3`/v1/skills/${skillID}/versions?beta=true`, multipartFormRequestOptions({
          body,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "skills-2025-10-02"].toString() },
            options?.headers
          ])
        }, this._client, !1));
      }
      /**
       * Get Skill Version
       *
       * @example
       * ```ts
       * const version = await client.beta.skills.versions.retrieve(
       *   'version',
       *   { skill_id: 'skill_id' },
       * );
       * ```
       */
      retrieve(version, params, options) {
        let { skill_id, betas } = params;
        return this._client.get(path3`/v1/skills/${skill_id}/versions/${version}?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "skills-2025-10-02"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * List Skill Versions
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const versionListResponse of client.beta.skills.versions.list(
       *   'skill_id',
       * )) {
       *   // ...
       * }
       * ```
       */
      list(skillID, params = {}, options) {
        let { betas, ...query } = params ?? {};
        return this._client.getAPIList(path3`/v1/skills/${skillID}/versions?beta=true`, PageCursor, {
          query,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "skills-2025-10-02"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Delete Skill Version
       *
       * @example
       * ```ts
       * const version = await client.beta.skills.versions.delete(
       *   'version',
       *   { skill_id: 'skill_id' },
       * );
       * ```
       */
      delete(version, params, options) {
        let { skill_id, betas } = params;
        return this._client.delete(path3`/v1/skills/${skill_id}/versions/${version}?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "skills-2025-10-02"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Download a skill version's content as a zip archive.
       *
       * @example
       * ```ts
       * const response = await client.beta.skills.versions.download(
       *   'version',
       *   { skill_id: 'skill_id' },
       * );
       *
       * const content = await response.blob();
       * console.log(content);
       * ```
       */
      download(version, params, options) {
        let { skill_id, betas } = params;
        return this._client.get(path3`/v1/skills/${skill_id}/versions/${version}/content?beta=true`, {
          ...options,
          headers: buildHeaders([
            {
              "anthropic-beta": [...betas ?? [], "skills-2025-10-02"].toString(),
              Accept: "application/binary"
            },
            options?.headers
          ]),
          __binaryResponse: !0
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/skills/skills.mjs
var Skills, init_skills2 = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/skills/skills.mjs"() {
    init_resource();
    init_versions2();
    init_versions2();
    init_pagination();
    init_headers();
    init_uploads();
    init_path();
    Skills = class extends APIResource {
      constructor() {
        super(...arguments), this.versions = new Versions2(this._client);
      }
      /**
       * Create Skill
       *
       * @example
       * ```ts
       * const skill = await client.beta.skills.create({
       *   files: [fs.createReadStream('path/to/file')],
       * });
       * ```
       */
      create(params, options) {
        let { betas, ...body } = params;
        return this._client.post("/v1/skills?beta=true", multipartFormRequestOptions({
          body,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "skills-2025-10-02"].toString() },
            options?.headers
          ])
        }, this._client, !1));
      }
      /**
       * Get Skill
       *
       * @example
       * ```ts
       * const skill = await client.beta.skills.retrieve('skill_id');
       * ```
       */
      retrieve(skillID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.get(path3`/v1/skills/${skillID}?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "skills-2025-10-02"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * List Skills
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const skillListResponse of client.beta.skills.list()) {
       *   // ...
       * }
       * ```
       */
      list(params = {}, options) {
        let { betas, ...query } = params ?? {};
        return this._client.getAPIList("/v1/skills?beta=true", PageCursor, {
          query,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "skills-2025-10-02"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Delete Skill
       *
       * @example
       * ```ts
       * const skill = await client.beta.skills.delete('skill_id');
       * ```
       */
      delete(skillID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.delete(path3`/v1/skills/${skillID}?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "skills-2025-10-02"].toString() },
            options?.headers
          ])
        });
      }
    };
    Skills.Versions = Versions2;
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/vaults/credentials.mjs
var Credentials, init_credentials2 = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/vaults/credentials.mjs"() {
    init_resource();
    init_pagination();
    init_headers();
    init_path();
    Credentials = class extends APIResource {
      /**
       * Create Credential
       *
       * @example
       * ```ts
       * const betaManagedAgentsCredential =
       *   await client.beta.vaults.credentials.create(
       *     'vlt_011CZkZDLs7fYzm1hXNPeRjv',
       *     {
       *       auth: {
       *         token: 'bearer_exampletoken',
       *         mcp_server_url:
       *           'https://example-server.modelcontextprotocol.io/sse',
       *         type: 'static_bearer',
       *       },
       *     },
       *   );
       * ```
       */
      create(vaultID, params, options) {
        let { betas, ...body } = params;
        return this._client.post(path3`/v1/vaults/${vaultID}/credentials?beta=true`, {
          body,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Get Credential
       *
       * @example
       * ```ts
       * const betaManagedAgentsCredential =
       *   await client.beta.vaults.credentials.retrieve(
       *     'vcrd_011CZkZEMt8gZan2iYOQfSkw',
       *     { vault_id: 'vlt_011CZkZDLs7fYzm1hXNPeRjv' },
       *   );
       * ```
       */
      retrieve(credentialID, params, options) {
        let { vault_id, betas } = params;
        return this._client.get(path3`/v1/vaults/${vault_id}/credentials/${credentialID}?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Update Credential
       *
       * @example
       * ```ts
       * const betaManagedAgentsCredential =
       *   await client.beta.vaults.credentials.update(
       *     'vcrd_011CZkZEMt8gZan2iYOQfSkw',
       *     { vault_id: 'vlt_011CZkZDLs7fYzm1hXNPeRjv' },
       *   );
       * ```
       */
      update(credentialID, params, options) {
        let { vault_id, betas, ...body } = params;
        return this._client.post(path3`/v1/vaults/${vault_id}/credentials/${credentialID}?beta=true`, {
          body,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * List Credentials
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const betaManagedAgentsCredential of client.beta.vaults.credentials.list(
       *   'vlt_011CZkZDLs7fYzm1hXNPeRjv',
       * )) {
       *   // ...
       * }
       * ```
       */
      list(vaultID, params = {}, options) {
        let { betas, ...query } = params ?? {};
        return this._client.getAPIList(path3`/v1/vaults/${vaultID}/credentials?beta=true`, PageCursor, {
          query,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Delete Credential
       *
       * @example
       * ```ts
       * const betaManagedAgentsDeletedCredential =
       *   await client.beta.vaults.credentials.delete(
       *     'vcrd_011CZkZEMt8gZan2iYOQfSkw',
       *     { vault_id: 'vlt_011CZkZDLs7fYzm1hXNPeRjv' },
       *   );
       * ```
       */
      delete(credentialID, params, options) {
        let { vault_id, betas } = params;
        return this._client.delete(path3`/v1/vaults/${vault_id}/credentials/${credentialID}?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Archive Credential
       *
       * @example
       * ```ts
       * const betaManagedAgentsCredential =
       *   await client.beta.vaults.credentials.archive(
       *     'vcrd_011CZkZEMt8gZan2iYOQfSkw',
       *     { vault_id: 'vlt_011CZkZDLs7fYzm1hXNPeRjv' },
       *   );
       * ```
       */
      archive(credentialID, params, options) {
        let { vault_id, betas } = params;
        return this._client.post(path3`/v1/vaults/${vault_id}/credentials/${credentialID}/archive?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Validate Credential
       *
       * @example
       * ```ts
       * const betaManagedAgentsCredentialValidation =
       *   await client.beta.vaults.credentials.mcpOAuthValidate(
       *     'vcrd_011CZkZEMt8gZan2iYOQfSkw',
       *     { vault_id: 'vlt_011CZkZDLs7fYzm1hXNPeRjv' },
       *   );
       * ```
       */
      mcpOAuthValidate(credentialID, params, options) {
        let { vault_id, betas } = params;
        return this._client.post(path3`/v1/vaults/${vault_id}/credentials/${credentialID}/mcp_oauth_validate?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/vaults/vaults.mjs
var Vaults, init_vaults = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/vaults/vaults.mjs"() {
    init_resource();
    init_credentials2();
    init_credentials2();
    init_pagination();
    init_headers();
    init_path();
    Vaults = class extends APIResource {
      constructor() {
        super(...arguments), this.credentials = new Credentials(this._client);
      }
      /**
       * Create Vault
       *
       * @example
       * ```ts
       * const betaManagedAgentsVault =
       *   await client.beta.vaults.create({
       *     display_name: 'Example vault',
       *   });
       * ```
       */
      create(params, options) {
        let { betas, ...body } = params;
        return this._client.post("/v1/vaults?beta=true", {
          body,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Get Vault
       *
       * @example
       * ```ts
       * const betaManagedAgentsVault =
       *   await client.beta.vaults.retrieve(
       *     'vlt_011CZkZDLs7fYzm1hXNPeRjv',
       *   );
       * ```
       */
      retrieve(vaultID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.get(path3`/v1/vaults/${vaultID}?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Update Vault
       *
       * @example
       * ```ts
       * const betaManagedAgentsVault =
       *   await client.beta.vaults.update(
       *     'vlt_011CZkZDLs7fYzm1hXNPeRjv',
       *   );
       * ```
       */
      update(vaultID, params, options) {
        let { betas, ...body } = params;
        return this._client.post(path3`/v1/vaults/${vaultID}?beta=true`, {
          body,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * List Vaults
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const betaManagedAgentsVault of client.beta.vaults.list()) {
       *   // ...
       * }
       * ```
       */
      list(params = {}, options) {
        let { betas, ...query } = params ?? {};
        return this._client.getAPIList("/v1/vaults?beta=true", PageCursor, {
          query,
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Delete Vault
       *
       * @example
       * ```ts
       * const betaManagedAgentsDeletedVault =
       *   await client.beta.vaults.delete(
       *     'vlt_011CZkZDLs7fYzm1hXNPeRjv',
       *   );
       * ```
       */
      delete(vaultID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.delete(path3`/v1/vaults/${vaultID}?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
      /**
       * Archive Vault
       *
       * @example
       * ```ts
       * const betaManagedAgentsVault =
       *   await client.beta.vaults.archive(
       *     'vlt_011CZkZDLs7fYzm1hXNPeRjv',
       *   );
       * ```
       */
      archive(vaultID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.post(path3`/v1/vaults/${vaultID}/archive?beta=true`, {
          ...options,
          headers: buildHeaders([
            { "anthropic-beta": [...betas ?? [], "managed-agents-2026-04-01"].toString() },
            options?.headers
          ])
        });
      }
    };
    Vaults.Credentials = Credentials;
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/beta.mjs
var Beta, init_beta = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/beta/beta.mjs"() {
    init_resource();
    init_deployment_runs();
    init_deployment_runs();
    init_deployments();
    init_deployments();
    init_files();
    init_files();
    init_models();
    init_models();
    init_user_profiles();
    init_user_profiles();
    init_webhooks();
    init_webhooks();
    init_agents();
    init_agents();
    init_environments();
    init_environments();
    init_memory_stores();
    init_memory_stores();
    init_messages();
    init_messages();
    init_sessions();
    init_sessions();
    init_skills2();
    init_skills2();
    init_vaults();
    init_vaults();
    Beta = class extends APIResource {
      constructor() {
        super(...arguments), this.models = new Models(this._client), this.messages = new Messages(this._client), this.agents = new Agents(this._client), this.environments = new Environments(this._client), this.sessions = new Sessions(this._client), this.deployments = new Deployments(this._client), this.deploymentRuns = new DeploymentRuns(this._client), this.vaults = new Vaults(this._client), this.memoryStores = new MemoryStores(this._client), this.files = new Files(this._client), this.skills = new Skills(this._client), this.webhooks = new Webhooks(this._client), this.userProfiles = new UserProfiles(this._client);
      }
    };
    Beta.Models = Models;
    Beta.Messages = Messages;
    Beta.Agents = Agents;
    Beta.Environments = Environments;
    Beta.Sessions = Sessions;
    Beta.Deployments = Deployments;
    Beta.DeploymentRuns = DeploymentRuns;
    Beta.Vaults = Vaults;
    Beta.MemoryStores = MemoryStores;
    Beta.Files = Files;
    Beta.Skills = Skills;
    Beta.Webhooks = Webhooks;
    Beta.UserProfiles = UserProfiles;
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/completions.mjs
var Completions, init_completions = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/completions.mjs"() {
    init_resource();
    init_headers();
    Completions = class extends APIResource {
      create(params, options) {
        let { betas, ...body } = params;
        return this._client.post("/v1/complete", {
          body,
          timeout: this._client._options.timeout ?? 6e5,
          ...options,
          headers: buildHeaders([
            { ...betas?.toString() != null ? { "anthropic-beta": betas?.toString() } : void 0 },
            options?.headers
          ]),
          stream: params.stream ?? !1
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/parser.mjs
function getOutputFormat2(params) {
  return params?.output_config?.format;
}
function maybeParseMessage(message, params, opts) {
  let outputFormat = getOutputFormat2(params);
  return !params || !("parse" in (outputFormat ?? {})) ? {
    ...message,
    content: message.content.map((block) => block.type === "text" ? Object.defineProperty({ ...block }, "parsed_output", {
      value: null,
      enumerable: !1
    }) : block),
    parsed_output: null
  } : parseMessage(message, params, opts);
}
function parseMessage(message, params, opts) {
  let firstParsedOutput = null, content = message.content.map((block) => {
    if (block.type === "text") {
      let parsedOutput = parseOutputFormat(params, block.text);
      return firstParsedOutput === null && (firstParsedOutput = parsedOutput), Object.defineProperty({ ...block }, "parsed_output", {
        value: parsedOutput,
        enumerable: !1
      });
    }
    return block;
  });
  return {
    ...message,
    content,
    parsed_output: firstParsedOutput
  };
}
function parseOutputFormat(params, content) {
  let outputFormat = getOutputFormat2(params);
  if (outputFormat?.type !== "json_schema")
    return null;
  try {
    return "parse" in outputFormat ? outputFormat.parse(content) : JSON.parse(content);
  } catch (error) {
    throw new AnthropicError(`Failed to parse structured output: ${error}`);
  }
}
var init_parser2 = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/parser.mjs"() {
    init_error();
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/MessageStream.mjs
function tracksToolInput2(content) {
  return content.type === "tool_use" || content.type === "server_tool_use";
}
var _MessageStream_instances, _MessageStream_currentMessageSnapshot, _MessageStream_params, _MessageStream_connectedPromise, _MessageStream_resolveConnectedPromise, _MessageStream_rejectConnectedPromise, _MessageStream_endPromise, _MessageStream_resolveEndPromise, _MessageStream_rejectEndPromise, _MessageStream_listeners, _MessageStream_ended, _MessageStream_errored, _MessageStream_aborted, _MessageStream_catchingPromiseCreated, _MessageStream_response, _MessageStream_request_id, _MessageStream_logger, _MessageStream_getFinalMessage, _MessageStream_getFinalText, _MessageStream_handleError, _MessageStream_beginRequest, _MessageStream_addStreamEvent, _MessageStream_endRequest, _MessageStream_accumulateMessage, MessageStream, init_MessageStream = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/MessageStream.mjs"() {
    init_tslib();
    init_stainless_helper_header();
    init_errors2();
    init_error2();
    init_streaming2();
    init_parser2();
    init_message_stream_utils();
    MessageStream = class _MessageStream {
      constructor(params, opts) {
        _MessageStream_instances.add(this), this.messages = [], this.receivedMessages = [], _MessageStream_currentMessageSnapshot.set(this, void 0), _MessageStream_params.set(this, null), this.controller = new AbortController(), _MessageStream_connectedPromise.set(this, void 0), _MessageStream_resolveConnectedPromise.set(this, () => {
        }), _MessageStream_rejectConnectedPromise.set(this, () => {
        }), _MessageStream_endPromise.set(this, void 0), _MessageStream_resolveEndPromise.set(this, () => {
        }), _MessageStream_rejectEndPromise.set(this, () => {
        }), _MessageStream_listeners.set(this, {}), _MessageStream_ended.set(this, !1), _MessageStream_errored.set(this, !1), _MessageStream_aborted.set(this, !1), _MessageStream_catchingPromiseCreated.set(this, !1), _MessageStream_response.set(this, void 0), _MessageStream_request_id.set(this, void 0), _MessageStream_logger.set(this, void 0), _MessageStream_handleError.set(this, (error) => {
          if (__classPrivateFieldSet(this, _MessageStream_errored, !0, "f"), isAbortError(error) && (error = new APIUserAbortError()), error instanceof APIUserAbortError)
            return __classPrivateFieldSet(this, _MessageStream_aborted, !0, "f"), this._emit("abort", error);
          if (error instanceof AnthropicError)
            return this._emit("error", error);
          if (error instanceof Error) {
            let anthropicError = new AnthropicError(error.message);
            return anthropicError.cause = error, this._emit("error", anthropicError);
          }
          return this._emit("error", new AnthropicError(String(error)));
        }), __classPrivateFieldSet(this, _MessageStream_connectedPromise, new Promise((resolve4, reject) => {
          __classPrivateFieldSet(this, _MessageStream_resolveConnectedPromise, resolve4, "f"), __classPrivateFieldSet(this, _MessageStream_rejectConnectedPromise, reject, "f");
        }), "f"), __classPrivateFieldSet(this, _MessageStream_endPromise, new Promise((resolve4, reject) => {
          __classPrivateFieldSet(this, _MessageStream_resolveEndPromise, resolve4, "f"), __classPrivateFieldSet(this, _MessageStream_rejectEndPromise, reject, "f");
        }), "f"), __classPrivateFieldGet(this, _MessageStream_connectedPromise, "f").catch(() => {
        }), __classPrivateFieldGet(this, _MessageStream_endPromise, "f").catch(() => {
        }), __classPrivateFieldSet(this, _MessageStream_params, params, "f"), __classPrivateFieldSet(this, _MessageStream_logger, opts?.logger ?? console, "f");
      }
      get response() {
        return __classPrivateFieldGet(this, _MessageStream_response, "f");
      }
      get request_id() {
        return __classPrivateFieldGet(this, _MessageStream_request_id, "f");
      }
      /**
       * Returns the `MessageStream` data, the raw `Response` instance and the ID of the request,
       * returned vie the `request-id` header which is useful for debugging requests and resporting
       * issues to Anthropic.
       *
       * This is the same as the `APIPromise.withResponse()` method.
       *
       * This method will raise an error if you created the stream using `MessageStream.fromReadableStream`
       * as no `Response` is available.
       */
      async withResponse() {
        __classPrivateFieldSet(this, _MessageStream_catchingPromiseCreated, !0, "f");
        let response = await __classPrivateFieldGet(this, _MessageStream_connectedPromise, "f");
        if (!response)
          throw new Error("Could not resolve a `Response` object");
        return {
          data: this,
          response,
          request_id: response.headers.get("request-id")
        };
      }
      /**
       * Intended for use on the frontend, consuming a stream produced with
       * `.toReadableStream()` on the backend.
       *
       * Note that messages sent to the model do not appear in `.on('message')`
       * in this context.
       */
      static fromReadableStream(stream) {
        let runner = new _MessageStream(null);
        return runner._run(() => runner._fromReadableStream(stream)), runner;
      }
      static createMessage(messages, params, options, { logger } = {}) {
        let runner = new _MessageStream(params, { logger });
        for (let message of params.messages)
          runner._addMessageParam(message);
        return __classPrivateFieldSet(runner, _MessageStream_params, { ...params, stream: !0 }, "f"), runner._run(() => runner._createMessage(messages, { ...params, stream: !0 }, { ...options, headers: { ...options?.headers, [STAINLESS_HELPER_METHOD_HEADER]: "stream" } })), runner;
      }
      _run(executor) {
        executor().then(() => {
          this._emitFinal(), this._emit("end");
        }, __classPrivateFieldGet(this, _MessageStream_handleError, "f"));
      }
      _addMessageParam(message) {
        this.messages.push(message);
      }
      _addMessage(message, emit = !0) {
        this.receivedMessages.push(message), emit && this._emit("message", message);
      }
      async _createMessage(messages, params, options) {
        let signal = options?.signal, abortHandler;
        signal && (signal.aborted && this.controller.abort(), abortHandler = this.controller.abort.bind(this.controller), signal.addEventListener("abort", abortHandler));
        try {
          __classPrivateFieldGet(this, _MessageStream_instances, "m", _MessageStream_beginRequest).call(this);
          let { response, data: stream } = await messages.create({ ...params, stream: !0 }, { ...options, signal: this.controller.signal }).withResponse();
          this._connected(response);
          for await (let event of stream)
            __classPrivateFieldGet(this, _MessageStream_instances, "m", _MessageStream_addStreamEvent).call(this, event);
          if (stream.controller.signal?.aborted)
            throw new APIUserAbortError();
          __classPrivateFieldGet(this, _MessageStream_instances, "m", _MessageStream_endRequest).call(this);
        } finally {
          signal && abortHandler && signal.removeEventListener("abort", abortHandler);
        }
      }
      _connected(response) {
        this.ended || (__classPrivateFieldSet(this, _MessageStream_response, response, "f"), __classPrivateFieldSet(this, _MessageStream_request_id, response?.headers.get("request-id"), "f"), __classPrivateFieldGet(this, _MessageStream_resolveConnectedPromise, "f").call(this, response), this._emit("connect"));
      }
      get ended() {
        return __classPrivateFieldGet(this, _MessageStream_ended, "f");
      }
      get errored() {
        return __classPrivateFieldGet(this, _MessageStream_errored, "f");
      }
      get aborted() {
        return __classPrivateFieldGet(this, _MessageStream_aborted, "f");
      }
      abort() {
        this.controller.abort();
      }
      /**
       * Adds the listener function to the end of the listeners array for the event.
       * No checks are made to see if the listener has already been added. Multiple calls passing
       * the same combination of event and listener will result in the listener being added, and
       * called, multiple times.
       * @returns this MessageStream, so that calls can be chained
       */
      on(event, listener) {
        return (__classPrivateFieldGet(this, _MessageStream_listeners, "f")[event] || (__classPrivateFieldGet(this, _MessageStream_listeners, "f")[event] = [])).push({ listener }), this;
      }
      /**
       * Removes the specified listener from the listener array for the event.
       * off() will remove, at most, one instance of a listener from the listener array. If any single
       * listener has been added multiple times to the listener array for the specified event, then
       * off() must be called multiple times to remove each instance.
       * @returns this MessageStream, so that calls can be chained
       */
      off(event, listener) {
        let listeners = __classPrivateFieldGet(this, _MessageStream_listeners, "f")[event];
        if (!listeners)
          return this;
        let index = listeners.findIndex((l) => l.listener === listener);
        return index >= 0 && listeners.splice(index, 1), this;
      }
      /**
       * Adds a one-time listener function for the event. The next time the event is triggered,
       * this listener is removed and then invoked.
       * @returns this MessageStream, so that calls can be chained
       */
      once(event, listener) {
        return (__classPrivateFieldGet(this, _MessageStream_listeners, "f")[event] || (__classPrivateFieldGet(this, _MessageStream_listeners, "f")[event] = [])).push({ listener, once: !0 }), this;
      }
      /**
       * This is similar to `.once()`, but returns a Promise that resolves the next time
       * the event is triggered, instead of calling a listener callback.
       * @returns a Promise that resolves the next time given event is triggered,
       * or rejects if an error is emitted.  (If you request the 'error' event,
       * returns a promise that resolves with the error).
       *
       * Example:
       *
       *   const message = await stream.emitted('message') // rejects if the stream errors
       */
      emitted(event) {
        return new Promise((resolve4, reject) => {
          __classPrivateFieldSet(this, _MessageStream_catchingPromiseCreated, !0, "f"), event !== "error" && this.once("error", reject), this.once(event, resolve4);
        });
      }
      async done() {
        __classPrivateFieldSet(this, _MessageStream_catchingPromiseCreated, !0, "f"), await __classPrivateFieldGet(this, _MessageStream_endPromise, "f");
      }
      get currentMessage() {
        return __classPrivateFieldGet(this, _MessageStream_currentMessageSnapshot, "f");
      }
      /**
       * @returns a promise that resolves with the the final assistant Message response,
       * or rejects if an error occurred or the stream ended prematurely without producing a Message.
       * If structured outputs were used, this will be a ParsedMessage with a `parsed_output` field.
       */
      async finalMessage() {
        return await this.done(), __classPrivateFieldGet(this, _MessageStream_instances, "m", _MessageStream_getFinalMessage).call(this);
      }
      /**
       * @returns a promise that resolves with the the final assistant Message's text response, concatenated
       * together if there are more than one text blocks.
       * Rejects if an error occurred or the stream ended prematurely without producing a Message.
       */
      async finalText() {
        return await this.done(), __classPrivateFieldGet(this, _MessageStream_instances, "m", _MessageStream_getFinalText).call(this);
      }
      _emit(event, ...args) {
        if (__classPrivateFieldGet(this, _MessageStream_ended, "f"))
          return;
        event === "end" && (__classPrivateFieldSet(this, _MessageStream_ended, !0, "f"), __classPrivateFieldGet(this, _MessageStream_resolveEndPromise, "f").call(this));
        let listeners = __classPrivateFieldGet(this, _MessageStream_listeners, "f")[event];
        if (listeners && (__classPrivateFieldGet(this, _MessageStream_listeners, "f")[event] = listeners.filter((l) => !l.once), listeners.forEach(({ listener }) => listener(...args))), event === "abort") {
          let error = args[0];
          !__classPrivateFieldGet(this, _MessageStream_catchingPromiseCreated, "f") && !listeners?.length && Promise.reject(error), __classPrivateFieldGet(this, _MessageStream_rejectConnectedPromise, "f").call(this, error), __classPrivateFieldGet(this, _MessageStream_rejectEndPromise, "f").call(this, error), this._emit("end");
          return;
        }
        if (event === "error") {
          let error = args[0];
          !__classPrivateFieldGet(this, _MessageStream_catchingPromiseCreated, "f") && !listeners?.length && Promise.reject(error), __classPrivateFieldGet(this, _MessageStream_rejectConnectedPromise, "f").call(this, error), __classPrivateFieldGet(this, _MessageStream_rejectEndPromise, "f").call(this, error), this._emit("end");
        }
      }
      _emitFinal() {
        this.receivedMessages.at(-1) && this._emit("finalMessage", __classPrivateFieldGet(this, _MessageStream_instances, "m", _MessageStream_getFinalMessage).call(this));
      }
      async _fromReadableStream(readableStream, options) {
        let signal = options?.signal, abortHandler;
        signal && (signal.aborted && this.controller.abort(), abortHandler = this.controller.abort.bind(this.controller), signal.addEventListener("abort", abortHandler));
        try {
          __classPrivateFieldGet(this, _MessageStream_instances, "m", _MessageStream_beginRequest).call(this), this._connected(null);
          let stream = Stream.fromReadableStream(readableStream, this.controller);
          for await (let event of stream)
            __classPrivateFieldGet(this, _MessageStream_instances, "m", _MessageStream_addStreamEvent).call(this, event);
          if (stream.controller.signal?.aborted)
            throw new APIUserAbortError();
          __classPrivateFieldGet(this, _MessageStream_instances, "m", _MessageStream_endRequest).call(this);
        } finally {
          signal && abortHandler && signal.removeEventListener("abort", abortHandler);
        }
      }
      [(_MessageStream_currentMessageSnapshot = /* @__PURE__ */ new WeakMap(), _MessageStream_params = /* @__PURE__ */ new WeakMap(), _MessageStream_connectedPromise = /* @__PURE__ */ new WeakMap(), _MessageStream_resolveConnectedPromise = /* @__PURE__ */ new WeakMap(), _MessageStream_rejectConnectedPromise = /* @__PURE__ */ new WeakMap(), _MessageStream_endPromise = /* @__PURE__ */ new WeakMap(), _MessageStream_resolveEndPromise = /* @__PURE__ */ new WeakMap(), _MessageStream_rejectEndPromise = /* @__PURE__ */ new WeakMap(), _MessageStream_listeners = /* @__PURE__ */ new WeakMap(), _MessageStream_ended = /* @__PURE__ */ new WeakMap(), _MessageStream_errored = /* @__PURE__ */ new WeakMap(), _MessageStream_aborted = /* @__PURE__ */ new WeakMap(), _MessageStream_catchingPromiseCreated = /* @__PURE__ */ new WeakMap(), _MessageStream_response = /* @__PURE__ */ new WeakMap(), _MessageStream_request_id = /* @__PURE__ */ new WeakMap(), _MessageStream_logger = /* @__PURE__ */ new WeakMap(), _MessageStream_handleError = /* @__PURE__ */ new WeakMap(), _MessageStream_instances = /* @__PURE__ */ new WeakSet(), _MessageStream_getFinalMessage = function() {
        if (this.receivedMessages.length === 0)
          throw new AnthropicError("stream ended without producing a Message with role=assistant");
        return this.receivedMessages.at(-1);
      }, _MessageStream_getFinalText = function() {
        if (this.receivedMessages.length === 0)
          throw new AnthropicError("stream ended without producing a Message with role=assistant");
        let textBlocks = this.receivedMessages.at(-1).content.filter((block) => block.type === "text").map((block) => block.text);
        if (textBlocks.length === 0)
          throw new AnthropicError("stream ended without producing a content block with type=text");
        return textBlocks.join(" ");
      }, _MessageStream_beginRequest = function() {
        this.ended || __classPrivateFieldSet(this, _MessageStream_currentMessageSnapshot, void 0, "f");
      }, _MessageStream_addStreamEvent = function(event) {
        if (this.ended)
          return;
        let messageSnapshot = __classPrivateFieldGet(this, _MessageStream_instances, "m", _MessageStream_accumulateMessage).call(this, event);
        switch (this._emit("streamEvent", event, messageSnapshot), event.type) {
          case "content_block_delta": {
            let content = messageSnapshot.content.at(-1);
            switch (event.delta.type) {
              case "text_delta": {
                content.type === "text" && this._emit("text", event.delta.text, content.text || "");
                break;
              }
              case "citations_delta": {
                content.type === "text" && this._emit("citation", event.delta.citation, content.citations ?? []);
                break;
              }
              case "input_json_delta": {
                tracksToolInput2(content) && __classPrivateFieldGet(this, _MessageStream_listeners, "f").inputJson?.length && this._emit("inputJson", event.delta.partial_json, content.input);
                break;
              }
              case "thinking_delta": {
                content.type === "thinking" && this._emit("thinking", event.delta.thinking, content.thinking);
                break;
              }
              case "signature_delta": {
                content.type === "thinking" && this._emit("signature", content.signature);
                break;
              }
              default:
                event.delta;
            }
            break;
          }
          case "message_stop": {
            this._addMessageParam(messageSnapshot), this._addMessage(maybeParseMessage(messageSnapshot, __classPrivateFieldGet(this, _MessageStream_params, "f"), { logger: __classPrivateFieldGet(this, _MessageStream_logger, "f") }), !0);
            break;
          }
          case "content_block_stop": {
            this._emit("contentBlock", messageSnapshot.content.at(-1));
            break;
          }
          case "message_start": {
            __classPrivateFieldSet(this, _MessageStream_currentMessageSnapshot, messageSnapshot, "f");
            break;
          }
          case "content_block_start":
          case "message_delta":
            break;
        }
      }, _MessageStream_endRequest = function() {
        if (this.ended)
          throw new AnthropicError("stream has ended, this shouldn't happen");
        let snapshot = __classPrivateFieldGet(this, _MessageStream_currentMessageSnapshot, "f");
        if (!snapshot)
          throw new AnthropicError("request ended without sending any chunks");
        return __classPrivateFieldSet(this, _MessageStream_currentMessageSnapshot, void 0, "f"), maybeParseMessage(snapshot, __classPrivateFieldGet(this, _MessageStream_params, "f"), { logger: __classPrivateFieldGet(this, _MessageStream_logger, "f") });
      }, _MessageStream_accumulateMessage = function(event) {
        let snapshot = __classPrivateFieldGet(this, _MessageStream_currentMessageSnapshot, "f");
        if (event.type === "message_start") {
          if (snapshot)
            throw new AnthropicError(`Unexpected event order, got ${event.type} before receiving "message_stop"`);
          return event.message;
        }
        if (!snapshot)
          throw new AnthropicError(`Unexpected event order, got ${event.type} before "message_start"`);
        switch (event.type) {
          case "message_stop":
            return snapshot;
          case "message_delta":
            return snapshot.stop_reason = event.delta.stop_reason, snapshot.stop_sequence = event.delta.stop_sequence, event.delta.stop_details != null && (snapshot.stop_details = event.delta.stop_details), snapshot.usage.output_tokens = event.usage.output_tokens, event.usage.input_tokens != null && (snapshot.usage.input_tokens = event.usage.input_tokens), event.usage.cache_creation_input_tokens != null && (snapshot.usage.cache_creation_input_tokens = event.usage.cache_creation_input_tokens), event.usage.cache_read_input_tokens != null && (snapshot.usage.cache_read_input_tokens = event.usage.cache_read_input_tokens), event.usage.server_tool_use != null && (snapshot.usage.server_tool_use = event.usage.server_tool_use), snapshot;
          case "content_block_start":
            return snapshot.content.push({ ...event.content_block }), snapshot;
          case "content_block_delta": {
            let snapshotContent = snapshot.content.at(event.index);
            switch (event.delta.type) {
              case "text_delta": {
                snapshotContent?.type === "text" && (snapshot.content[event.index] = {
                  ...snapshotContent,
                  text: (snapshotContent.text || "") + event.delta.text
                });
                break;
              }
              case "citations_delta": {
                snapshotContent?.type === "text" && (snapshot.content[event.index] = {
                  ...snapshotContent,
                  citations: [...snapshotContent.citations ?? [], event.delta.citation]
                });
                break;
              }
              case "input_json_delta": {
                if (snapshotContent && tracksToolInput2(snapshotContent)) {
                  let jsonBuf = (snapshotContent[JSON_BUF_PROPERTY] || "") + event.delta.partial_json;
                  snapshot.content[event.index] = withLazyInput(snapshotContent, jsonBuf);
                }
                break;
              }
              case "thinking_delta": {
                snapshotContent?.type === "thinking" && (snapshot.content[event.index] = {
                  ...snapshotContent,
                  thinking: snapshotContent.thinking + event.delta.thinking
                });
                break;
              }
              case "signature_delta": {
                snapshotContent?.type === "thinking" && (snapshot.content[event.index] = {
                  ...snapshotContent,
                  signature: event.delta.signature
                });
                break;
              }
              default:
                event.delta;
            }
            return snapshot;
          }
          case "content_block_stop": {
            let snapshotContent = snapshot.content.at(event.index);
            return snapshotContent && tracksToolInput2(snapshotContent) && JSON_BUF_PROPERTY in snapshotContent && Object.defineProperty(snapshotContent, "input", {
              value: snapshotContent.input,
              enumerable: !0,
              configurable: !0,
              writable: !0
            }), snapshot;
          }
        }
      }, Symbol.asyncIterator)]() {
        let pushQueue = [], readQueue = [], done = !1;
        return this.on("streamEvent", (event) => {
          let reader = readQueue.shift();
          reader ? reader.resolve(event) : pushQueue.push(event);
        }), this.on("end", () => {
          done = !0;
          for (let reader of readQueue)
            reader.resolve(void 0);
          readQueue.length = 0;
        }), this.on("abort", (err) => {
          done = !0;
          for (let reader of readQueue)
            reader.reject(err);
          readQueue.length = 0;
        }), this.on("error", (err) => {
          done = !0;
          for (let reader of readQueue)
            reader.reject(err);
          readQueue.length = 0;
        }), {
          next: async () => pushQueue.length ? { value: pushQueue.shift(), done: !1 } : done ? { value: void 0, done: !0 } : new Promise((resolve4, reject) => readQueue.push({ resolve: resolve4, reject })).then((chunk2) => chunk2 ? { value: chunk2, done: !1 } : { value: void 0, done: !0 }),
          return: async () => (this.abort(), { value: void 0, done: !0 })
        };
      }
      toReadableStream() {
        return new Stream(this[Symbol.asyncIterator].bind(this), this.controller).toReadableStream();
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/messages/batches.mjs
var Batches2, init_batches2 = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/messages/batches.mjs"() {
    init_resource();
    init_pagination();
    init_headers();
    init_jsonl();
    init_error2();
    init_path();
    Batches2 = class extends APIResource {
      /**
       * Send a batch of Message creation requests.
       *
       * The Message Batches API can be used to process multiple Messages API requests at
       * once. Once a Message Batch is created, it begins processing immediately. Batches
       * can take up to 24 hours to complete.
       *
       * Learn more about the Message Batches API in our
       * [user guide](https://platform.claude.com/docs/en/build-with-claude/batch-processing)
       *
       * @example
       * ```ts
       * const messageBatch = await client.messages.batches.create({
       *   requests: [
       *     {
       *       custom_id: 'my-custom-id-1',
       *       params: {
       *         max_tokens: 1024,
       *         messages: [
       *           { content: 'Hello, world', role: 'user' },
       *         ],
       *         model: 'claude-opus-4-6',
       *       },
       *     },
       *   ],
       * });
       * ```
       */
      create(params, options) {
        let { user_profile_id, ...body } = params;
        return this._client.post("/v1/messages/batches", {
          body,
          ...options,
          headers: buildHeaders([
            { ...user_profile_id != null ? { "anthropic-user-profile-id": user_profile_id } : void 0 },
            options?.headers
          ])
        });
      }
      /**
       * This endpoint is idempotent and can be used to poll for Message Batch
       * completion. To access the results of a Message Batch, make a request to the
       * `results_url` field in the response.
       *
       * Learn more about the Message Batches API in our
       * [user guide](https://platform.claude.com/docs/en/build-with-claude/batch-processing)
       *
       * @example
       * ```ts
       * const messageBatch = await client.messages.batches.retrieve(
       *   'message_batch_id',
       * );
       * ```
       */
      retrieve(messageBatchID, options) {
        return this._client.get(path3`/v1/messages/batches/${messageBatchID}`, options);
      }
      /**
       * List all Message Batches within a Workspace. Most recently created batches are
       * returned first.
       *
       * Learn more about the Message Batches API in our
       * [user guide](https://platform.claude.com/docs/en/build-with-claude/batch-processing)
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const messageBatch of client.messages.batches.list()) {
       *   // ...
       * }
       * ```
       */
      list(query = {}, options) {
        return this._client.getAPIList("/v1/messages/batches", Page, { query, ...options });
      }
      /**
       * Delete a Message Batch.
       *
       * Message Batches can only be deleted once they've finished processing. If you'd
       * like to delete an in-progress batch, you must first cancel it.
       *
       * Learn more about the Message Batches API in our
       * [user guide](https://platform.claude.com/docs/en/build-with-claude/batch-processing)
       *
       * @example
       * ```ts
       * const deletedMessageBatch =
       *   await client.messages.batches.delete('message_batch_id');
       * ```
       */
      delete(messageBatchID, options) {
        return this._client.delete(path3`/v1/messages/batches/${messageBatchID}`, options);
      }
      /**
       * Batches may be canceled any time before processing ends. Once cancellation is
       * initiated, the batch enters a `canceling` state, at which time the system may
       * complete any in-progress, non-interruptible requests before finalizing
       * cancellation.
       *
       * The number of canceled requests is specified in `request_counts`. To determine
       * which requests were canceled, check the individual results within the batch.
       * Note that cancellation may not result in any canceled requests if they were
       * non-interruptible.
       *
       * Learn more about the Message Batches API in our
       * [user guide](https://platform.claude.com/docs/en/build-with-claude/batch-processing)
       *
       * @example
       * ```ts
       * const messageBatch = await client.messages.batches.cancel(
       *   'message_batch_id',
       * );
       * ```
       */
      cancel(messageBatchID, options) {
        return this._client.post(path3`/v1/messages/batches/${messageBatchID}/cancel`, options);
      }
      /**
       * Streams the results of a Message Batch as a `.jsonl` file.
       *
       * Each line in the file is a JSON object containing the result of a single request
       * in the Message Batch. Results are not guaranteed to be in the same order as
       * requests. Use the `custom_id` field to match results to requests.
       *
       * Learn more about the Message Batches API in our
       * [user guide](https://platform.claude.com/docs/en/build-with-claude/batch-processing)
       *
       * @example
       * ```ts
       * const messageBatchIndividualResponse =
       *   await client.messages.batches.results('message_batch_id');
       * ```
       */
      async results(messageBatchID, options) {
        let batch = await this.retrieve(messageBatchID);
        if (!batch.results_url)
          throw new AnthropicError(`No batch \`results_url\`; Has it finished processing? ${batch.processing_status} - ${batch.id}`);
        return this._client.get(batch.results_url, {
          ...options,
          headers: buildHeaders([{ Accept: "application/binary" }, options?.headers]),
          stream: !0,
          __binaryResponse: !0
        })._thenUnwrap((_, props) => JSONLDecoder.fromResponse(props.response, props.controller));
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/messages/messages.mjs
var Messages2, DEPRECATED_MODELS2, MODELS_TO_WARN_WITH_THINKING_ENABLED2, init_messages2 = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/messages/messages.mjs"() {
    init_resource();
    init_headers();
    init_stainless_helper_header();
    init_MessageStream();
    init_parser2();
    init_batches2();
    init_batches2();
    init_constants();
    Messages2 = class extends APIResource {
      constructor() {
        super(...arguments), this.batches = new Batches2(this._client);
      }
      create(params, options) {
        let { user_profile_id, ...body } = params;
        body.model in DEPRECATED_MODELS2 && console.warn(`The model '${body.model}' is deprecated and will reach end-of-life on ${DEPRECATED_MODELS2[body.model]}
Please migrate to a newer model. Visit https://docs.anthropic.com/en/docs/resources/model-deprecations for more information.`), MODELS_TO_WARN_WITH_THINKING_ENABLED2.includes(body.model) && body.thinking && body.thinking.type === "enabled" && console.warn(`Using Claude with ${body.model} and 'thinking.type=enabled' is deprecated. Use 'thinking.type=adaptive' instead which results in better model performance in our testing: https://platform.claude.com/docs/en/build-with-claude/adaptive-thinking`);
        let timeout = this._client._options.timeout;
        if (!body.stream && timeout == null) {
          let maxNonstreamingTokens = MODEL_NONSTREAMING_TOKENS[body.model] ?? void 0;
          timeout = this._client.calculateNonstreamingTimeout(body.max_tokens, maxNonstreamingTokens);
        }
        let helperHeader2 = stainlessHelperHeader(body.tools, body.messages);
        return this._client.post("/v1/messages", {
          body,
          timeout: timeout ?? 6e5,
          ...options,
          headers: buildHeaders([
            { ...user_profile_id != null ? { "anthropic-user-profile-id": user_profile_id } : void 0 },
            helperHeader2,
            options?.headers
          ]),
          stream: params.stream ?? !1
        });
      }
      /**
       * Send a structured list of input messages with text and/or image content, along with an expected `output_config.format` and
       * the response will be automatically parsed and available in the `parsed_output` property of the message.
       *
       * @example
       * ```ts
       * const message = await client.messages.parse({
       *   model: 'claude-sonnet-4-5-20250929',
       *   max_tokens: 1024,
       *   messages: [{ role: 'user', content: 'What is 2+2?' }],
       *   output_config: {
       *     format: zodOutputFormat(z.object({ answer: z.number() })),
       *   },
       * });
       *
       * console.log(message.parsed_output?.answer); // 4
       * ```
       */
      parse(params, options) {
        return this.create(params, options).then((message) => parseMessage(message, params, { logger: this._client.logger ?? console }));
      }
      /**
       * Create a Message stream.
       *
       * If `output_config.format` is provided with a parseable format (like `zodOutputFormat()`),
       * the final message will include a `parsed_output` property with the parsed content.
       *
       * @example
       * ```ts
       * const stream = client.messages.stream({
       *   model: 'claude-sonnet-4-5-20250929',
       *   max_tokens: 1024,
       *   messages: [{ role: 'user', content: 'What is 2+2?' }],
       *   output_config: {
       *     format: zodOutputFormat(z.object({ answer: z.number() })),
       *   },
       * });
       *
       * const message = await stream.finalMessage();
       * console.log(message.parsed_output?.answer); // 4
       * ```
       */
      stream(body, options) {
        return MessageStream.createMessage(this, body, options, { logger: this._client.logger ?? console });
      }
      /**
       * Count the number of tokens in a Message.
       *
       * The Token Count API can be used to count the number of tokens in a Message,
       * including tools, images, and documents, without creating it.
       *
       * Learn more about token counting in our
       * [user guide](https://platform.claude.com/docs/en/build-with-claude/token-counting)
       *
       * @example
       * ```ts
       * const messageTokensCount =
       *   await client.messages.countTokens({
       *     messages: [{ content: 'Hello, world', role: 'user' }],
       *     model: 'claude-opus-4-6',
       *   });
       * ```
       */
      countTokens(params, options) {
        let { user_profile_id, ...body } = params;
        return this._client.post("/v1/messages/count_tokens", {
          body,
          ...options,
          headers: buildHeaders([
            { ...user_profile_id != null ? { "anthropic-user-profile-id": user_profile_id } : void 0 },
            options?.headers
          ])
        });
      }
    }, DEPRECATED_MODELS2 = {
      "claude-1.3": "November 6th, 2024",
      "claude-1.3-100k": "November 6th, 2024",
      "claude-instant-1.1": "November 6th, 2024",
      "claude-instant-1.1-100k": "November 6th, 2024",
      "claude-instant-1.2": "November 6th, 2024",
      "claude-3-sonnet-20240229": "July 21st, 2025",
      "claude-3-opus-20240229": "January 5th, 2026",
      "claude-2.1": "July 21st, 2025",
      "claude-2.0": "July 21st, 2025",
      "claude-3-7-sonnet-latest": "February 19th, 2026",
      "claude-3-7-sonnet-20250219": "February 19th, 2026",
      "claude-3-5-haiku-latest": "February 19th, 2026",
      "claude-3-5-haiku-20241022": "February 19th, 2026",
      "claude-opus-4-0": "June 15th, 2026",
      "claude-opus-4-20250514": "June 15th, 2026",
      "claude-sonnet-4-0": "June 15th, 2026",
      "claude-sonnet-4-20250514": "June 15th, 2026",
      "claude-opus-4-1": "August 5th, 2026",
      "claude-opus-4-1-20250805": "August 5th, 2026",
      "claude-mythos-preview": "June 30th, 2026"
    }, MODELS_TO_WARN_WITH_THINKING_ENABLED2 = ["claude-mythos-preview", "claude-opus-4-6"];
    Messages2.Batches = Batches2;
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/models.mjs
var Models2, init_models2 = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/models.mjs"() {
    init_resource();
    init_pagination();
    init_headers();
    init_path();
    Models2 = class extends APIResource {
      /**
       * Get a specific model.
       *
       * The Models API response can be used to determine information about a specific
       * model or resolve a model alias to a model ID.
       */
      retrieve(modelID, params = {}, options) {
        let { betas } = params ?? {};
        return this._client.get(path3`/v1/models/${modelID}`, {
          ...options,
          headers: buildHeaders([
            { ...betas?.toString() != null ? { "anthropic-beta": betas?.toString() } : void 0 },
            options?.headers
          ])
        });
      }
      /**
       * List available models.
       *
       * The Models API response can be used to determine which models are available for
       * use in the API. More recently released models are listed first.
       */
      list(params = {}, options) {
        let { betas, ...query } = params ?? {};
        return this._client.getAPIList("/v1/models", Page, {
          query,
          ...options,
          headers: buildHeaders([
            { ...betas?.toString() != null ? { "anthropic-beta": betas?.toString() } : void 0 },
            options?.headers
          ])
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/index.mjs
var init_resources2 = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/resources/index.mjs"() {
    init_shared();
    init_beta();
    init_completions();
    init_messages2();
    init_models2();
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/client.mjs
var _BaseAnthropic_instances, _a2, _BaseAnthropic_encoder, _BaseAnthropic_baseURLOverridden, HUMAN_PROMPT, AI_PROMPT, BaseAnthropic, Anthropic, init_client = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/client.mjs"() {
    init_tslib();
    init_uuid();
    init_values();
    init_sleep();
    init_errors2();
    init_detect_platform();
    init_shims();
    init_request_options();
    init_query();
    init_version();
    init_error();
    init_types2();
    init_token_cache();
    init_credential_chain();
    init_middleware();
    init_pagination();
    init_uploads2();
    init_resources2();
    init_api_promise();
    init_completions();
    init_models2();
    init_beta();
    init_messages2();
    init_detect_platform();
    init_headers();
    init_env();
    init_log();
    init_values();
    HUMAN_PROMPT = "\\n\\nHuman:", AI_PROMPT = "\\n\\nAssistant:", BaseAnthropic = class {
      /**
       * The active credential provider. Default credential resolution runs once
       * at construction time. If it fails, the error is surfaced on every
       * request and the client must be reconstructed — there is no retry path.
       *
       * Clones returned by {@link withOptions} share the parent's auth state
       * (provider, token cache, pending resolution, and any resolution error)
       * unless the caller passes an explicit `apiKey`, `authToken`,
       * `credentials`, `config`, or `profile` override.
       */
      get credentials() {
        return this._authState.provider;
      }
      /**
       * API Client for interfacing with the Anthropic API.
       *
       * @param {string | null | undefined} [opts.apiKey=process.env['ANTHROPIC_API_KEY'] ?? null]
       * @param {string | null | undefined} [opts.authToken=process.env['ANTHROPIC_AUTH_TOKEN'] ?? null]
       * @param {string | null | undefined} [opts.webhookKey=process.env['ANTHROPIC_WEBHOOK_SIGNING_KEY'] ?? null]
       * @param {string} [opts.baseURL=process.env['ANTHROPIC_BASE_URL'] ?? https://api.anthropic.com] - Override the default base URL for the API.
       * @param {number} [opts.timeout=10 minutes] - The maximum amount of time (in milliseconds) the client will wait for a response before timing out.
       * @param {MergedRequestInit} [opts.fetchOptions] - Additional `RequestInit` options to be passed to `fetch` calls.
       * @param {Fetch} [opts.fetch] - Specify a custom `fetch` function implementation.
       * @param {number} [opts.maxRetries=2] - The maximum number of times the client will retry a request.
       * @param {HeadersLike} opts.defaultHeaders - Default headers to include with every request to the API.
       * @param {Record<string, string | undefined>} opts.defaultQuery - Default query parameters to include with every request to the API.
       * @param {boolean} [opts.dangerouslyAllowBrowser=false] - By default, client-side use of this library is not allowed, as it risks exposing your secret API credentials to attackers.
       */
      constructor({ baseURL = readEnv("ANTHROPIC_BASE_URL"), apiKey, authToken, webhookKey = readEnv("ANTHROPIC_WEBHOOK_SIGNING_KEY") ?? null, ...opts } = {}) {
        if (_BaseAnthropic_instances.add(this), this._requestAuthFlags = /* @__PURE__ */ new WeakMap(), _BaseAnthropic_encoder.set(this, void 0), apiKey === void 0 && (apiKey = opts.profile != null ? null : readEnv("ANTHROPIC_API_KEY") ?? null), authToken === void 0 && (authToken = opts.profile != null ? null : readEnv("ANTHROPIC_AUTH_TOKEN") ?? null), opts.profile != null && (opts.credentials != null || opts.config != null))
          throw new TypeError("Pass at most one of `profile`, `credentials`, or `config`.");
        let options = {
          apiKey,
          authToken,
          webhookKey,
          ...opts,
          baseURL: baseURL || "https://api.anthropic.com"
        };
        if (!options.dangerouslyAllowBrowser && isRunningInBrowser())
          throw new AnthropicError(`It looks like you're running in a browser-like environment.

This is disabled by default, as it risks exposing your secret API credentials to attackers.
If you understand the risks and have appropriate mitigations in place,
you can set the \`dangerouslyAllowBrowser\` option to \`true\`, e.g.,

new Anthropic({ apiKey, dangerouslyAllowBrowser: true });
`);
        this.baseURL = options.baseURL, this._baseURLIsExplicit = opts.__baseURLIsExplicit ?? !!baseURL, this.timeout = options.timeout ?? _a2.DEFAULT_TIMEOUT, this.logger = options.logger ?? console, this.logLevel = defaultLogLevel, this.logLevel = parseLogLevel(options.logLevel, "ClientOptions.logLevel", loggerFor(this)) ?? parseLogLevel(readEnv("ANTHROPIC_LOG"), "process.env['ANTHROPIC_LOG']", loggerFor(this)) ?? defaultLogLevel, this.fetchOptions = options.fetchOptions, this.maxRetries = options.maxRetries ?? 2, this.fetch = options.fetch ?? getDefaultFetch(), __classPrivateFieldSet(this, _BaseAnthropic_encoder, FallbackEncoder, "f"), this.middleware = [...options.middleware ?? []];
        let customHeadersEnv = readEnv("ANTHROPIC_CUSTOM_HEADERS");
        if (customHeadersEnv) {
          let parsed = {};
          for (let line of customHeadersEnv.split(`
`)) {
            let colon = line.indexOf(":");
            colon >= 0 && (parsed[line.substring(0, colon).trim()] = line.substring(colon + 1).trim());
          }
          options.defaultHeaders = { ...parsed, ...options.defaultHeaders };
        }
        let inherited = opts.__auth;
        if (delete options.__auth, delete options.__baseURLIsExplicit, this._options = options, this.apiKey = typeof apiKey == "string" ? apiKey : null, this.authToken = authToken, this.webhookKey = webhookKey, inherited)
          this._authState = inherited, !this._baseURLIsExplicit && inherited.baseURL && (this.baseURL = inherited.baseURL);
        else if (this._authState = { provider: null, tokenCache: null, resolution: null, error: null, extraHeaders: {} }, this.apiKey == null && this.authToken == null) {
          let credentials = options.credentials ?? null;
          if (credentials)
            this._authState.provider = credentials, this._authState.tokenCache = this._makeTokenCache(credentials);
          else if (options.config != null) {
            let result = resolveCredentialsFromConfig(options.config, this._credentialResolverOptions());
            this._authState.provider = result.provider, this._authState.tokenCache = this._makeTokenCache(result.provider), this._authState.extraHeaders = result.extraHeaders, this._applyCredentialBaseURL(result.baseURL);
          } else options.profile != null ? this._authState.resolution = this._resolveDefaultCredentials(options.profile) : this._authState.resolution = this._resolveDefaultCredentials();
        }
      }
      /**
       * Stores a profile/config-supplied base URL on the shared auth state and, if
       * the caller did not pin `baseURL` via constructor option or env, adopts it
       * as this client's outbound API host. Precedence: ctor opt > env > profile >
       * hardcoded default.
       */
      _applyCredentialBaseURL(baseURL) {
        if (!baseURL)
          return;
        let normalized = baseURL.replace(/\/+$/, "");
        this._authState.baseURL = normalized, this._baseURLIsExplicit || (this.baseURL = normalized);
      }
      /**
       * Options bag passed into the credential chain. `baseURL` here is only the
       * fallback host for the token-exchange POST when the config itself omits
       * `base_url`; the chain returns the config's own `base_url` (if any) on
       * {@link CredentialResult.baseURL}, which {@link _applyCredentialBaseURL}
       * then adopts for outbound API requests. The two are deliberately decoupled
       * so this fallback never round-trips into precedence.
       */
      _credentialResolverOptions() {
        return {
          baseURL: this.baseURL,
          fetch: this._credentialsFetch(),
          userAgent: this.getUserAgent(),
          onCacheWriteError: (err) => {
            loggerFor(this).debug("credential cache write failed (best-effort)", err);
          },
          onSafetyWarning: (msg) => {
            loggerFor(this).warn(msg);
          }
        };
      }
      /**
       * A `Fetch` for first-party credential token-exchange requests (OIDC
       * federation jwt-bearer grants, user-OAuth refresh grants) that routes
       * through this client's middleware chain, so middleware observes token
       * traffic like any other request. Only client-level middleware applies:
       * a minted token is shared across requests, so attributing the exchange
       * to any one request's per-request middleware would be arbitrary. For the
       * same reason, `ctx.options` is undefined for these requests.
       */
      _credentialsFetch() {
        return wrapFetchWithMiddleware(this.fetch, this.middleware, void 0, this);
      }
      _makeTokenCache(provider) {
        return new TokenCache(provider, (err) => {
          loggerFor(this).debug("advisory token refresh failed; serving cached token", err);
        });
      }
      /**
       * Create a new client instance re-using the same options given to the current client with optional overriding.
       */
      withOptions(options) {
        let overridesStructuredAuth = "credentials" in options || "config" in options || "profile" in options, overridesAuth = "apiKey" in options || "authToken" in options || overridesStructuredAuth, internal = {
          ...this._options,
          // Only forward baseURL when the caller (or env) explicitly chose it.
          // For a non-explicit parent, this.baseURL may have been mutated to the
          // profile-resolved host; pinning that as the clone's options.baseURL
          // would make _options on the clone misreport caller intent and would
          // leave the clone stuck on the parent's host across an auth override.
          // The clone instead receives the construction-time value via
          // ...this._options above and re-adopts the profile host through the
          // shared _authState.baseURL + __baseURLIsExplicit=false path.
          ...this._baseURLIsExplicit ? { baseURL: this.baseURL } : {},
          maxRetries: this.maxRetries,
          timeout: this.timeout,
          logger: this.logger,
          logLevel: this.logLevel,
          fetch: this.fetch,
          fetchOptions: this.fetchOptions,
          middleware: this.middleware,
          apiKey: this.apiKey,
          authToken: this.authToken,
          webhookKey: this.webhookKey,
          // credentials: this.credentials is a no-op when __auth is shared (the
          // ctor takes the inherited path and ignores options.credentials); when
          // overridesAuth is true via apiKey/authToken only, it lets the clone
          // build a fresh TokenCache around the parent's provider.
          credentials: this.credentials,
          // When the caller passes a structured-credential override, drop inherited
          // structured-credential options so only `...options` supplies them —
          // otherwise an inherited `credentials`/`config`/`profile` would trip the
          // mutual-exclusion check or precedence over the override.
          ...overridesStructuredAuth ? { credentials: void 0, config: void 0, profile: void 0 } : {},
          ...options,
          // Always set __auth so any stale value from ...this._options is
          // overwritten. undefined means "build fresh auth from these options".
          __auth: overridesAuth ? void 0 : this._authState,
          __baseURLIsExplicit: "baseURL" in options ? !0 : this._baseURLIsExplicit
        };
        return new this.constructor(internal);
      }
      /**
       * Lazily resolves credentials from config files or environment variables.
       * Called once from the constructor when no explicit auth is provided, or
       * when an explicit `profile` was passed (in which case a missing/unresolved
       * profile is surfaced as an error instead of falling through to "no auth").
       * The returned promise is stored and awaited on the first request.
       */
      async _resolveDefaultCredentials(profile) {
        try {
          let result = await defaultCredentials(this._credentialResolverOptions(), profile);
          if (result)
            this._authState.provider = result.provider, this._authState.tokenCache = this._makeTokenCache(result.provider), this._authState.extraHeaders = result.extraHeaders, this._applyCredentialBaseURL(result.baseURL);
          else if (profile != null)
            throw new AnthropicError(`Profile "${profile}" could not be resolved (no <config_dir>/configs/${profile}.json found).`);
        } catch (err) {
          this._authState.error = err;
        } finally {
          this._authState.resolution = null;
        }
      }
      defaultQuery() {
        return this._options.defaultQuery;
      }
      validateHeaders({ values, nulls }) {
        if (!(values.get("x-api-key") || values.get("authorization"))) {
          if (this._authState.error)
            throw this._authState.error;
          if (!(this._authState.tokenCache || this._authState.resolution) && !(this.apiKey && values.get("x-api-key")) && !nulls.has("x-api-key") && !(this.authToken && values.get("authorization")) && !nulls.has("authorization"))
            throw new Error('Could not resolve authentication method. Expected one of apiKey, authToken, credentials, config, or profile to be set. Or for one of the "X-Api-Key" or "Authorization" headers to be explicitly omitted');
        }
      }
      _authFlags(opts) {
        let flags = this._requestAuthFlags.get(opts);
        return flags || (flags = { usedTokenCache: !1, didRefreshFor401: !1 }, this._requestAuthFlags.set(opts, flags)), flags;
      }
      async authHeaders(opts) {
        if (this._authState.resolution && await this._authState.resolution, !this._authState.error) {
          if (this._authState.tokenCache && this.apiKey == null) {
            let token = await this._authState.tokenCache.getToken();
            return this._authFlags(opts).usedTokenCache = !0, buildHeaders([{ Authorization: `Bearer ${token}` }]);
          }
          return buildHeaders([await this.apiKeyAuth(opts), await this.bearerAuth(opts)]);
        }
      }
      async apiKeyAuth(opts) {
        if (this.apiKey != null)
          return buildHeaders([{ "X-Api-Key": this.apiKey }]);
      }
      async bearerAuth(opts) {
        if (this.authToken != null)
          return buildHeaders([{ Authorization: `Bearer ${this.authToken}` }]);
      }
      stringifyQuery(query) {
        return stringifyQuery(query);
      }
      getUserAgent() {
        return `${this.constructor.name}/JS ${VERSION}`;
      }
      defaultIdempotencyKey() {
        return `stainless-node-retry-${uuid4()}`;
      }
      makeStatusError(status, error, message, headers) {
        return APIError.generate(status, error, message, headers);
      }
      buildURL(path12, query, defaultBaseURL) {
        let baseURL = !__classPrivateFieldGet(this, _BaseAnthropic_instances, "m", _BaseAnthropic_baseURLOverridden).call(this) && defaultBaseURL || this.baseURL, url = isAbsoluteURL(path12) ? new URL(path12) : new URL(baseURL + (baseURL.endsWith("/") && path12.startsWith("/") ? path12.slice(1) : path12)), defaultQuery = this.defaultQuery(), pathQuery = Object.fromEntries(url.searchParams);
        return (!isEmptyObj(defaultQuery) || !isEmptyObj(pathQuery)) && (query = { ...pathQuery, ...defaultQuery, ...query }), typeof query == "object" && query && !Array.isArray(query) && (url.search = this.stringifyQuery(query)), url.toString();
      }
      _calculateNonstreamingTimeout(maxTokens) {
        if (3600 * maxTokens / 128e3 > 600)
          throw new AnthropicError("Streaming is required for operations that may take longer than 10 minutes. See https://github.com/anthropics/anthropic-sdk-typescript#streaming-responses for more details");
        return 600 * 1e3;
      }
      /**
       * Used as a callback for mutating the given `FinalRequestOptions` object.
       */
      async prepareOptions(options) {
      }
      /**
       * Used as a callback for mutating the given `RequestInit` object.
       *
       * This is useful for cases where you want to add certain headers based off of
       * the request properties, e.g. `method` or `url`.
       *
       * Runs after all middleware (including {@link backendMiddleware}),
       * immediately before each underlying fetch call, so it sees exactly what
       * goes over the wire. Middleware may replay a request by calling `next()`
       * more than once, so this hook can run multiple times per attempt:
       * overrides must be idempotent and overwrite headers from a previous
       * invocation rather than append to them.
       */
      async prepareRequest(request, { url, options }) {
        if (this._authState.tokenCache && this.apiKey == null) {
          let headers = request.headers instanceof Headers ? request.headers : new Headers(request.headers);
          for (let [k, v] of Object.entries(this._authState.extraHeaders))
            headers.has(k) || headers.set(k, v);
          headers.get("anthropic-beta")?.split(",").map((s) => s.trim())?.includes(OAUTH_API_BETA_HEADER) || headers.append("anthropic-beta", OAUTH_API_BETA_HEADER), request.headers = headers;
        }
      }
      /**
       * Internal {@link Middleware} composed innermost in the chain — inside both
       * client-level and per-request middleware, immediately around the underlying
       * `fetch`. Subclasses for third-party backends override this to adapt the
       * canonical Anthropic-shaped request to the backend's wire shape (URL/body
       * rewriting, request signing) and to normalize the wire response back to the
       * canonical shape (e.g. AWS EventStream to SSE).
       *
       * Running inside the user's middleware means user middleware always observes
       * canonical Anthropic-shaped traffic, and the adaptation re-runs (e.g.
       * re-signs) on every `next()` invocation, covering whatever the middleware
       * mutated.
       *
       * Errors thrown here follow the middleware error policy: they propagate to
       * the caller as-is — no retries, no `APIConnectionError` wrapping — unless
       * retryable (see {@link Middleware}); throw a `RetryableError` to opt into
       * the retry path.
       */
      backendMiddleware() {
        return [];
      }
      get(path12, opts) {
        return this.methodRequest("get", path12, opts);
      }
      post(path12, opts) {
        return this.methodRequest("post", path12, opts);
      }
      patch(path12, opts) {
        return this.methodRequest("patch", path12, opts);
      }
      put(path12, opts) {
        return this.methodRequest("put", path12, opts);
      }
      delete(path12, opts) {
        return this.methodRequest("delete", path12, opts);
      }
      methodRequest(method, path12, opts) {
        return this.request(Promise.resolve(opts).then((opts2) => ({ method, path: path12, ...opts2 })));
      }
      request(options, remainingRetries = null) {
        return new APIPromise(this, this.makeRequest(options, remainingRetries, void 0));
      }
      async makeRequest(optionsInput, retriesRemaining, retryOfRequestLogID) {
        let options = await optionsInput, maxRetries = options.maxRetries ?? this.maxRetries;
        retriesRemaining == null && (retriesRemaining = maxRetries, this._requestAuthFlags.delete(options)), await this.prepareOptions(options);
        let { req, url, timeout } = await this.buildRequest(options, {
          retryCount: maxRetries - retriesRemaining
        }), requestLogID = "log_" + (Math.random() * (1 << 24) | 0).toString(16).padStart(6, "0"), retryLogStr = retryOfRequestLogID === void 0 ? "" : `, retryOf: ${retryOfRequestLogID}`, startTime = Date.now();
        if (options.signal?.aborted)
          throw new APIUserAbortError();
        let controller = new AbortController(), response = await this.fetchWithTimeout(url, req, timeout, controller, options, {
          requestLogID,
          retryOfRequestLogID
        }).catch(castToError), headersTime = Date.now();
        if (response instanceof globalThis.Error) {
          let retryMessage = `retrying, ${retriesRemaining} attempts remaining`;
          if (options.signal?.aborted)
            throw new APIUserAbortError();
          let isTimeout = isAbortError(response) || /timed? ?out/i.test(String(response) + ("cause" in response ? String(response.cause) : "")), hasMiddleware = this.middleware.length > 0 || !!options.middleware?.length || this.backendMiddleware().length > 0;
          if (hasMiddleware && !isTimeout && !isRetryableError(response))
            throw loggerFor(this).info(`[${requestLogID}] middleware error (not retryable)`), loggerFor(this).debug(`[${requestLogID}] middleware error (not retryable)`, formatRequestDetails({
              retryOfRequestLogID,
              url,
              durationMs: headersTime - startTime,
              message: response.message
            })), response;
          if (retriesRemaining)
            return loggerFor(this).info(`[${requestLogID}] connection ${isTimeout ? "timed out" : "failed"} - ${retryMessage}`), loggerFor(this).debug(`[${requestLogID}] connection ${isTimeout ? "timed out" : "failed"} (${retryMessage})`, formatRequestDetails({
              retryOfRequestLogID,
              url,
              durationMs: headersTime - startTime,
              message: response.message
            })), this.retryRequest(options, retriesRemaining, retryOfRequestLogID ?? requestLogID);
          throw loggerFor(this).info(`[${requestLogID}] connection ${isTimeout ? "timed out" : "failed"} - error; no more retries left`), loggerFor(this).debug(`[${requestLogID}] connection ${isTimeout ? "timed out" : "failed"} (error; no more retries left)`, formatRequestDetails({
            retryOfRequestLogID,
            url,
            durationMs: headersTime - startTime,
            message: response.message
          })), isTimeout ? new APIConnectionTimeoutError() : hasMiddleware && !isFetchOriginError(response) ? response : new APIConnectionError({ cause: response });
        }
        let specialHeaders = [...response.headers.entries()].filter(([name]) => name === "request-id").map(([name, value]) => ", " + name + ": " + JSON.stringify(value)).join(""), responseInfo = `[${requestLogID}${retryLogStr}${specialHeaders}] ${req.method} ${url} ${response.ok ? "succeeded" : "failed"} with status ${response.status} in ${headersTime - startTime}ms`;
        if (!response.ok) {
          let shouldRetry = await this.shouldRetry(response, options);
          if (retriesRemaining && shouldRetry) {
            let retryMessage2 = `retrying, ${retriesRemaining} attempts remaining`;
            return await CancelReadableStream(response.body), loggerFor(this).info(`${responseInfo} - ${retryMessage2}`), loggerFor(this).debug(`[${requestLogID}] response error (${retryMessage2})`, formatRequestDetails({
              retryOfRequestLogID,
              url: response.url,
              status: response.status,
              headers: response.headers,
              durationMs: headersTime - startTime
            })), this.retryRequest(options, retriesRemaining, retryOfRequestLogID ?? requestLogID, response.headers);
          }
          let retryMessage = shouldRetry ? "error; no more retries left" : "error; not retryable";
          loggerFor(this).info(`${responseInfo} - ${retryMessage}`);
          let errText = await response.text().catch((err2) => castToError(err2).message), errJSON = safeJSON(errText), errMessage = errJSON ? void 0 : errText;
          throw loggerFor(this).debug(`[${requestLogID}] response error (${retryMessage})`, formatRequestDetails({
            retryOfRequestLogID,
            url: response.url,
            status: response.status,
            headers: response.headers,
            message: errMessage,
            durationMs: Date.now() - startTime
          })), this.makeStatusError(response.status, errJSON, errMessage, response.headers);
        }
        return loggerFor(this).info(responseInfo), loggerFor(this).debug(`[${requestLogID}] response start`, formatRequestDetails({
          retryOfRequestLogID,
          url: response.url,
          status: response.status,
          headers: response.headers,
          durationMs: headersTime - startTime
        })), { response, options, controller, requestLogID, retryOfRequestLogID, startTime };
      }
      getAPIList(path12, Page3, opts) {
        return this.requestAPIList(Page3, opts && "then" in opts ? opts.then((opts2) => ({ method: "get", path: path12, ...opts2 })) : { method: "get", path: path12, ...opts });
      }
      requestAPIList(Page3, options) {
        let request = this.makeRequest(options, null, void 0);
        return new PagePromise(this, request, Page3);
      }
      async fetchWithTimeout(url, init, ms, controller, requestOptions, logCtx) {
        let { signal, method, ...options } = init || {}, abort = this._makeAbort(controller);
        signal && signal.addEventListener("abort", abort, { once: !0 });
        let isReadableBody = globalThis.ReadableStream && options.body instanceof globalThis.ReadableStream || typeof options.body == "object" && options.body !== null && Symbol.asyncIterator in options.body, fetchOptions = {
          signal: controller.signal,
          ...isReadableBody ? { duplex: "half" } : {},
          method: "GET",
          ...options
        };
        method && (fetchOptions.method = method.toUpperCase());
        let baseFetch = this.fetch, timedFetch = async (innerUrl, innerInit) => {
          let timeout = setTimeout(abort, ms);
          try {
            return await baseFetch.call(void 0, innerUrl, innerInit);
          } finally {
            clearTimeout(timeout);
          }
        }, innerFetch = requestOptions === void 0 ? timedFetch : (async (innerUrl, innerInit = {}) => {
          let innerUrlStr = typeof innerUrl == "string" ? innerUrl : innerUrl instanceof URL ? innerUrl.href : innerUrl.url;
          return innerInit.headers = innerInit.headers instanceof Headers ? innerInit.headers : new Headers(innerInit.headers), await this.prepareRequest(innerInit, { url: innerUrlStr, options: requestOptions }), logCtx && loggerFor(this).debug(`[${logCtx.requestLogID}] sending request`, formatRequestDetails({
            retryOfRequestLogID: logCtx.retryOfRequestLogID,
            method: innerInit.method,
            url: innerUrlStr,
            options: requestOptions,
            headers: innerInit.headers
          })), timedFetch(innerUrl, innerInit);
        }), requestMiddleware = requestOptions?.middleware, backendMiddleware = this.backendMiddleware(), allMiddleware = requestMiddleware?.length || backendMiddleware.length ? [...this.middleware, ...requestMiddleware ?? [], ...backendMiddleware] : this.middleware;
        return await wrapFetchWithMiddleware(innerFetch, allMiddleware, requestOptions, this)(url, fetchOptions);
      }
      async shouldRetry(response, options) {
        let flags = this._authFlags(options);
        if (response.status === 401 && this._authState.tokenCache && flags.usedTokenCache && !flags.didRefreshFor401)
          return flags.didRefreshFor401 = !0, this._authState.tokenCache.invalidate(), !0;
        let shouldRetryHeader = response.headers.get("x-should-retry");
        return shouldRetryHeader === "true" ? !0 : shouldRetryHeader === "false" ? !1 : response.status === 408 || response.status === 409 || response.status === 429 || response.status >= 500;
      }
      async retryRequest(options, retriesRemaining, requestLogID, responseHeaders) {
        let timeoutMillis, retryAfterMillisHeader = responseHeaders?.get("retry-after-ms");
        if (retryAfterMillisHeader) {
          let timeoutMs = parseFloat(retryAfterMillisHeader);
          Number.isNaN(timeoutMs) || (timeoutMillis = timeoutMs);
        }
        let retryAfterHeader = responseHeaders?.get("retry-after");
        if (retryAfterHeader && !timeoutMillis) {
          let timeoutSeconds = parseFloat(retryAfterHeader);
          Number.isNaN(timeoutSeconds) ? timeoutMillis = Date.parse(retryAfterHeader) - Date.now() : timeoutMillis = timeoutSeconds * 1e3;
        }
        if (timeoutMillis === void 0) {
          let maxRetries = options.maxRetries ?? this.maxRetries;
          timeoutMillis = this.calculateDefaultRetryTimeoutMillis(retriesRemaining, maxRetries);
        }
        return await sleep(timeoutMillis), this.makeRequest(options, retriesRemaining - 1, requestLogID);
      }
      calculateDefaultRetryTimeoutMillis(retriesRemaining, maxRetries) {
        let numRetries = maxRetries - retriesRemaining, sleepSeconds = Math.min(0.5 * Math.pow(2, numRetries), 8), jitter2 = 1 - Math.random() * 0.25;
        return sleepSeconds * jitter2 * 1e3;
      }
      calculateNonstreamingTimeout(maxTokens, maxNonstreamingTokens) {
        if (36e5 * maxTokens / 128e3 > 6e5 || maxNonstreamingTokens != null && maxTokens > maxNonstreamingTokens)
          throw new AnthropicError("Streaming is required for operations that may take longer than 10 minutes. See https://github.com/anthropics/anthropic-sdk-typescript#long-requests for more details");
        return 6e5;
      }
      async buildRequest(inputOptions, { retryCount = 0 } = {}) {
        let options = { ...inputOptions }, { method, path: path12, query, defaultBaseURL } = options;
        this._authState.resolution && await this._authState.resolution, !this._baseURLIsExplicit && this._authState.baseURL && this.baseURL !== this._authState.baseURL && (this.baseURL = this._authState.baseURL);
        let url = this.buildURL(path12, query, defaultBaseURL);
        "timeout" in options && validatePositiveInteger("timeout", options.timeout), options.timeout = options.timeout ?? this.timeout;
        let { bodyHeaders, body } = this.buildBody({ options }), reqHeaders = await this.buildHeaders({ options: inputOptions, method, bodyHeaders, retryCount });
        return { req: {
          method,
          headers: reqHeaders,
          ...options.signal && { signal: options.signal },
          ...globalThis.ReadableStream && body instanceof globalThis.ReadableStream && { duplex: "half" },
          ...body && { body },
          ...this.fetchOptions ?? {},
          ...options.fetchOptions ?? {}
        }, url, timeout: options.timeout };
      }
      async buildHeaders({ options, method, bodyHeaders, retryCount }) {
        let idempotencyHeaders = {};
        this.idempotencyHeader && method !== "get" && (options.idempotencyKey || (options.idempotencyKey = this.defaultIdempotencyKey()), idempotencyHeaders[this.idempotencyHeader] = options.idempotencyKey);
        let headers = buildHeaders([
          idempotencyHeaders,
          {
            Accept: "application/json",
            "User-Agent": this.getUserAgent(),
            "X-Stainless-Retry-Count": String(retryCount),
            ...options.timeout ? { "X-Stainless-Timeout": String(Math.trunc(options.timeout / 1e3)) } : {},
            ...getPlatformHeaders(),
            ...this._options.dangerouslyAllowBrowser ? { "anthropic-dangerous-direct-browser-access": "true" } : void 0,
            "anthropic-version": "2023-06-01"
          },
          await this.authHeaders(options),
          this._options.defaultHeaders,
          bodyHeaders,
          options.headers
        ]);
        return this.validateHeaders(headers), headers.values;
      }
      _makeAbort(controller) {
        return () => controller.abort();
      }
      buildBody({ options: { body, headers: rawHeaders } }) {
        if (!body)
          return { bodyHeaders: void 0, body: void 0 };
        let headers = buildHeaders([rawHeaders]);
        return (
          // Pass raw type verbatim
          ArrayBuffer.isView(body) || body instanceof ArrayBuffer || body instanceof DataView || typeof body == "string" && // Preserve legacy string encoding behavior for now
          headers.values.has("content-type") || // `Blob` is superset of `File`
          globalThis.Blob && body instanceof globalThis.Blob || // `FormData` -> `multipart/form-data`
          body instanceof FormData || // `URLSearchParams` -> `application/x-www-form-urlencoded`
          body instanceof URLSearchParams || // Send chunked stream (each chunk has own `length`)
          globalThis.ReadableStream && body instanceof globalThis.ReadableStream ? { bodyHeaders: void 0, body } : typeof body == "object" && (Symbol.asyncIterator in body || Symbol.iterator in body && "next" in body && typeof body.next == "function") ? { bodyHeaders: void 0, body: ReadableStreamFrom(body) } : typeof body == "object" && headers.values.get("content-type") === "application/x-www-form-urlencoded" ? {
            bodyHeaders: { "content-type": "application/x-www-form-urlencoded" },
            body: this.stringifyQuery(body)
          } : __classPrivateFieldGet(this, _BaseAnthropic_encoder, "f").call(this, { body, headers })
        );
      }
    };
    _a2 = BaseAnthropic, _BaseAnthropic_encoder = /* @__PURE__ */ new WeakMap(), _BaseAnthropic_instances = /* @__PURE__ */ new WeakSet(), _BaseAnthropic_baseURLOverridden = function() {
      return this.baseURL !== "https://api.anthropic.com";
    };
    BaseAnthropic.Anthropic = _a2;
    BaseAnthropic.HUMAN_PROMPT = HUMAN_PROMPT;
    BaseAnthropic.AI_PROMPT = AI_PROMPT;
    BaseAnthropic.DEFAULT_TIMEOUT = 6e5;
    BaseAnthropic.AnthropicError = AnthropicError;
    BaseAnthropic.APIError = APIError;
    BaseAnthropic.APIConnectionError = APIConnectionError;
    BaseAnthropic.APIConnectionTimeoutError = APIConnectionTimeoutError;
    BaseAnthropic.APIUserAbortError = APIUserAbortError;
    BaseAnthropic.NotFoundError = NotFoundError;
    BaseAnthropic.ConflictError = ConflictError;
    BaseAnthropic.RateLimitError = RateLimitError;
    BaseAnthropic.BadRequestError = BadRequestError;
    BaseAnthropic.AuthenticationError = AuthenticationError;
    BaseAnthropic.InternalServerError = InternalServerError;
    BaseAnthropic.PermissionDeniedError = PermissionDeniedError;
    BaseAnthropic.UnprocessableEntityError = UnprocessableEntityError;
    BaseAnthropic.toFile = toFile;
    Anthropic = class extends BaseAnthropic {
      constructor() {
        super(...arguments), this.completions = new Completions(this), this.messages = new Messages2(this), this.models = new Models2(this), this.beta = new Beta(this);
      }
    };
    Anthropic.Completions = Completions;
    Anthropic.Messages = Messages2;
    Anthropic.Models = Models2;
    Anthropic.Beta = Beta;
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/middleware.mjs
var encoder, init_middleware2 = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/lib/middleware.mjs"() {
    init_error();
    init_streaming();
    init_errors2();
    init_headers();
    init_stainless_helper_header();
    init_values();
    init_request_options();
    encoder = new TextEncoder();
  }
});

// ../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/index.mjs
var init_sdk = __esm({
  "../../node_modules/.pnpm/@anthropic-ai+sdk@0.110.0_zod@3.25.76/node_modules/@anthropic-ai/sdk/index.mjs"() {
    init_client();
    init_uploads2();
    init_api_promise();
    init_middleware2();
    init_client();
    init_pagination();
    init_error();
  }
});

// ../core/dist/llm/client.js
function getAnthropicClient() {
  if (_client !== null)
    return _client;
  let apiKey = process.env.ANTHROPIC_API_KEY;
  if (!apiKey)
    throw new Error(`ANTHROPIC_API_KEY environment variable is not set.
Export it before running: export ANTHROPIC_API_KEY=sk-ant-...`);
  return _client = new Anthropic({ apiKey }), _client;
}
function _resetClient() {
  _client = null;
}
var _client, init_client2 = __esm({
  "../core/dist/llm/client.js"() {
    "use strict";
    init_sdk();
    _client = null;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/tslib.mjs
function __classPrivateFieldSet2(receiver, state, value, kind, f) {
  if (kind === "m")
    throw new TypeError("Private method is not writable");
  if (kind === "a" && !f)
    throw new TypeError("Private accessor was defined without a setter");
  if (typeof state == "function" ? receiver !== state || !f : !state.has(receiver))
    throw new TypeError("Cannot write private member to an object whose class did not declare it");
  return kind === "a" ? f.call(receiver, value) : f ? f.value = value : state.set(receiver, value), value;
}
function __classPrivateFieldGet2(receiver, state, kind, f) {
  if (kind === "a" && !f)
    throw new TypeError("Private accessor was defined without a getter");
  if (typeof state == "function" ? receiver !== state || !f : !state.has(receiver))
    throw new TypeError("Cannot read private member from an object whose class did not declare it");
  return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
}
var init_tslib2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/tslib.mjs"() {
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/utils/uuid.mjs
var uuid42, init_uuid2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/utils/uuid.mjs"() {
    uuid42 = function() {
      let { crypto: crypto3 } = globalThis;
      if (crypto3?.randomUUID)
        return uuid42 = crypto3.randomUUID.bind(crypto3), crypto3.randomUUID();
      let u8 = new Uint8Array(1), randomByte = crypto3 ? () => crypto3.getRandomValues(u8)[0] : () => Math.random() * 255 & 255;
      return "10000000-1000-4000-8000-100000000000".replace(/[018]/g, (c) => (+c ^ randomByte() & 15 >> +c / 4).toString(16));
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/errors.mjs
function isAbortError2(err) {
  return typeof err == "object" && err !== null && // Spec-compliant fetch implementations
  ("name" in err && err.name === "AbortError" || // Expo fetch
  "message" in err && String(err.message).includes("FetchRequestCanceledException"));
}
var castToError2, init_errors3 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/errors.mjs"() {
    castToError2 = (err) => {
      if (err instanceof Error)
        return err;
      if (typeof err == "object" && err !== null) {
        try {
          if (Object.prototype.toString.call(err) === "[object Error]") {
            let error = new Error(err.message, err.cause ? { cause: err.cause } : {});
            return err.stack && (error.stack = err.stack), err.cause && !error.cause && (error.cause = err.cause), err.name && (error.name = err.name), error;
          }
        } catch {
        }
        try {
          return new Error(JSON.stringify(err));
        } catch {
        }
      }
      return new Error(err);
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/core/error.mjs
var OpenAIError, APIError2, APIUserAbortError2, APIConnectionError2, APIConnectionTimeoutError2, BadRequestError2, AuthenticationError2, PermissionDeniedError2, NotFoundError2, ConflictError2, UnprocessableEntityError2, RateLimitError2, InternalServerError2, LengthFinishReasonError, ContentFilterFinishReasonError, InvalidWebhookSignatureError, OAuthError, SubjectTokenProviderError, init_error3 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/core/error.mjs"() {
    init_errors3();
    OpenAIError = class extends Error {
    }, APIError2 = class _APIError extends OpenAIError {
      constructor(status, error, message, headers) {
        super(`${_APIError.makeMessage(status, error, message)}`), this.status = status, this.headers = headers, this.requestID = headers?.get("x-request-id"), this.error = error;
        let data = error;
        this.code = data?.code, this.param = data?.param, this.type = data?.type;
      }
      static makeMessage(status, error, message) {
        let msg = error?.message ? typeof error.message == "string" ? error.message : JSON.stringify(error.message) : error ? JSON.stringify(error) : message;
        return status && msg ? `${status} ${msg}` : status ? `${status} status code (no body)` : msg || "(no status code or body)";
      }
      static generate(status, errorResponse, message, headers) {
        if (!status || !headers)
          return new APIConnectionError2({ message, cause: castToError2(errorResponse) });
        let error = errorResponse?.error;
        return status === 400 ? new BadRequestError2(status, error, message, headers) : status === 401 ? new AuthenticationError2(status, error, message, headers) : status === 403 ? new PermissionDeniedError2(status, error, message, headers) : status === 404 ? new NotFoundError2(status, error, message, headers) : status === 409 ? new ConflictError2(status, error, message, headers) : status === 422 ? new UnprocessableEntityError2(status, error, message, headers) : status === 429 ? new RateLimitError2(status, error, message, headers) : status >= 500 ? new InternalServerError2(status, error, message, headers) : new _APIError(status, error, message, headers);
      }
    }, APIUserAbortError2 = class extends APIError2 {
      constructor({ message } = {}) {
        super(void 0, void 0, message || "Request was aborted.", void 0);
      }
    }, APIConnectionError2 = class extends APIError2 {
      constructor({ message, cause }) {
        super(void 0, void 0, message || "Connection error.", void 0), cause && (this.cause = cause);
      }
    }, APIConnectionTimeoutError2 = class extends APIConnectionError2 {
      constructor({ message } = {}) {
        super({ message: message ?? "Request timed out." });
      }
    }, BadRequestError2 = class extends APIError2 {
    }, AuthenticationError2 = class extends APIError2 {
    }, PermissionDeniedError2 = class extends APIError2 {
    }, NotFoundError2 = class extends APIError2 {
    }, ConflictError2 = class extends APIError2 {
    }, UnprocessableEntityError2 = class extends APIError2 {
    }, RateLimitError2 = class extends APIError2 {
    }, InternalServerError2 = class extends APIError2 {
    }, LengthFinishReasonError = class extends OpenAIError {
      constructor() {
        super("Could not parse response content as the length limit was reached");
      }
    }, ContentFilterFinishReasonError = class extends OpenAIError {
      constructor() {
        super("Could not parse response content as the request was rejected by the content filter");
      }
    }, InvalidWebhookSignatureError = class extends Error {
      constructor(message) {
        super(message);
      }
    }, OAuthError = class extends APIError2 {
      constructor(status, error, headers) {
        let finalMessage = "OAuth2 authentication error", error_code;
        if (error && typeof error == "object") {
          let errorData = error;
          error_code = errorData.error;
          let description = errorData.error_description;
          description && typeof description == "string" ? finalMessage = description : error_code && (finalMessage = error_code);
        }
        super(status, error, finalMessage, headers), this.error_code = error_code;
      }
    }, SubjectTokenProviderError = class extends OpenAIError {
      constructor(message, provider, cause) {
        super(message), this.provider = provider, this.cause = cause;
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/utils/values.mjs
function maybeObj2(x) {
  return typeof x != "object" ? {} : x ?? {};
}
function isEmptyObj2(obj) {
  if (!obj)
    return !0;
  for (let _k in obj)
    return !1;
  return !0;
}
function hasOwn2(obj, key) {
  return Object.prototype.hasOwnProperty.call(obj, key);
}
function isObj(obj) {
  return obj != null && typeof obj == "object" && !Array.isArray(obj);
}
var startsWithSchemeRegexp2, isAbsoluteURL2, isArray2, isReadonlyArray2, validatePositiveInteger2, safeJSON2, init_values2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/utils/values.mjs"() {
    init_error3();
    startsWithSchemeRegexp2 = /^[a-z][a-z0-9+.-]*:/i, isAbsoluteURL2 = (url) => startsWithSchemeRegexp2.test(url), isArray2 = (val) => (isArray2 = Array.isArray, isArray2(val)), isReadonlyArray2 = isArray2;
    validatePositiveInteger2 = (name, n) => {
      if (typeof n != "number" || !Number.isInteger(n))
        throw new OpenAIError(`${name} must be an integer`);
      if (n < 0)
        throw new OpenAIError(`${name} must be a positive integer`);
      return n;
    }, safeJSON2 = (text) => {
      try {
        return JSON.parse(text);
      } catch {
        return;
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/utils/sleep.mjs
var sleep2, init_sleep2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/utils/sleep.mjs"() {
    sleep2 = (ms) => new Promise((resolve4) => setTimeout(resolve4, ms));
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/version.mjs
var VERSION2, init_version2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/version.mjs"() {
    VERSION2 = "6.45.0";
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/detect-platform.mjs
function getDetectedPlatform2() {
  return typeof Deno < "u" && Deno.build != null ? "deno" : typeof EdgeRuntime < "u" ? "edge" : Object.prototype.toString.call(typeof globalThis.process < "u" ? globalThis.process : 0) === "[object process]" ? "node" : "unknown";
}
function getBrowserInfo2() {
  if (typeof navigator > "u" || !navigator)
    return null;
  let browserPatterns = [
    { key: "edge", pattern: /Edge(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/ },
    { key: "ie", pattern: /MSIE(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/ },
    { key: "ie", pattern: /Trident(?:.*rv\:(\d+)\.(\d+)(?:\.(\d+))?)?/ },
    { key: "chrome", pattern: /Chrome(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/ },
    { key: "firefox", pattern: /Firefox(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/ },
    { key: "safari", pattern: /(?:Version\W+(\d+)\.(\d+)(?:\.(\d+))?)?(?:\W+Mobile\S*)?\W+Safari/ }
  ];
  for (let { key, pattern } of browserPatterns) {
    let match2 = pattern.exec(navigator.userAgent);
    if (match2) {
      let major = match2[1] || 0, minor = match2[2] || 0, patch = match2[3] || 0;
      return { browser: key, version: `${major}.${minor}.${patch}` };
    }
  }
  return null;
}
var isRunningInBrowser2, getPlatformProperties2, normalizeArch2, normalizePlatform2, _platformHeaders2, getPlatformHeaders2, init_detect_platform2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/detect-platform.mjs"() {
    init_version2();
    isRunningInBrowser2 = () => (
      // @ts-ignore
      typeof window < "u" && // @ts-ignore
      typeof window.document < "u" && // @ts-ignore
      typeof navigator < "u"
    );
    getPlatformProperties2 = () => {
      let detectedPlatform = getDetectedPlatform2();
      if (detectedPlatform === "deno")
        return {
          "X-Stainless-Lang": "js",
          "X-Stainless-Package-Version": VERSION2,
          "X-Stainless-OS": normalizePlatform2(Deno.build.os),
          "X-Stainless-Arch": normalizeArch2(Deno.build.arch),
          "X-Stainless-Runtime": "deno",
          "X-Stainless-Runtime-Version": typeof Deno.version == "string" ? Deno.version : Deno.version?.deno ?? "unknown"
        };
      if (typeof EdgeRuntime < "u")
        return {
          "X-Stainless-Lang": "js",
          "X-Stainless-Package-Version": VERSION2,
          "X-Stainless-OS": "Unknown",
          "X-Stainless-Arch": `other:${EdgeRuntime}`,
          "X-Stainless-Runtime": "edge",
          "X-Stainless-Runtime-Version": globalThis.process.version
        };
      if (detectedPlatform === "node")
        return {
          "X-Stainless-Lang": "js",
          "X-Stainless-Package-Version": VERSION2,
          "X-Stainless-OS": normalizePlatform2(globalThis.process.platform ?? "unknown"),
          "X-Stainless-Arch": normalizeArch2(globalThis.process.arch ?? "unknown"),
          "X-Stainless-Runtime": "node",
          "X-Stainless-Runtime-Version": globalThis.process.version ?? "unknown"
        };
      let browserInfo = getBrowserInfo2();
      return browserInfo ? {
        "X-Stainless-Lang": "js",
        "X-Stainless-Package-Version": VERSION2,
        "X-Stainless-OS": "Unknown",
        "X-Stainless-Arch": "unknown",
        "X-Stainless-Runtime": `browser:${browserInfo.browser}`,
        "X-Stainless-Runtime-Version": browserInfo.version
      } : {
        "X-Stainless-Lang": "js",
        "X-Stainless-Package-Version": VERSION2,
        "X-Stainless-OS": "Unknown",
        "X-Stainless-Arch": "unknown",
        "X-Stainless-Runtime": "unknown",
        "X-Stainless-Runtime-Version": "unknown"
      };
    };
    normalizeArch2 = (arch) => arch === "x32" ? "x32" : arch === "x86_64" || arch === "x64" ? "x64" : arch === "arm" ? "arm" : arch === "aarch64" || arch === "arm64" ? "arm64" : arch ? `other:${arch}` : "unknown", normalizePlatform2 = (platform) => (platform = platform.toLowerCase(), platform.includes("ios") ? "iOS" : platform === "android" ? "Android" : platform === "darwin" ? "MacOS" : platform === "win32" ? "Windows" : platform === "freebsd" ? "FreeBSD" : platform === "openbsd" ? "OpenBSD" : platform === "linux" ? "Linux" : platform ? `Other:${platform}` : "Unknown"), getPlatformHeaders2 = () => _platformHeaders2 ?? (_platformHeaders2 = getPlatformProperties2());
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/shims.mjs
function getDefaultFetch2() {
  if (typeof fetch < "u")
    return fetch;
  throw new Error("`fetch` is not defined as a global; Either pass `fetch` to the client, `new OpenAI({ fetch })` or polyfill the global, `globalThis.fetch = fetch`");
}
function makeReadableStream2(...args) {
  let ReadableStream2 = globalThis.ReadableStream;
  if (typeof ReadableStream2 > "u")
    throw new Error("`ReadableStream` is not defined as a global; You will need to polyfill it, `globalThis.ReadableStream = ReadableStream`");
  return new ReadableStream2(...args);
}
function ReadableStreamFrom2(iterable) {
  let iter = Symbol.asyncIterator in iterable ? iterable[Symbol.asyncIterator]() : iterable[Symbol.iterator]();
  return makeReadableStream2({
    start() {
    },
    async pull(controller) {
      let { done, value } = await iter.next();
      done ? controller.close() : controller.enqueue(value);
    },
    async cancel() {
      await iter.return?.();
    }
  });
}
function ReadableStreamToAsyncIterable2(stream) {
  if (stream[Symbol.asyncIterator])
    return stream;
  let reader = stream.getReader();
  return {
    async next() {
      try {
        let result = await reader.read();
        return result?.done && reader.releaseLock(), result;
      } catch (e) {
        throw reader.releaseLock(), e;
      }
    },
    async return() {
      let cancelPromise = reader.cancel();
      return reader.releaseLock(), await cancelPromise, { done: !0, value: void 0 };
    },
    [Symbol.asyncIterator]() {
      return this;
    }
  };
}
async function CancelReadableStream2(stream) {
  if (stream === null || typeof stream != "object")
    return;
  if (stream[Symbol.asyncIterator]) {
    await stream[Symbol.asyncIterator]().return?.();
    return;
  }
  let reader = stream.getReader(), cancelPromise = reader.cancel();
  reader.releaseLock(), await cancelPromise;
}
var init_shims2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/shims.mjs"() {
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/request-options.mjs
var FallbackEncoder2, init_request_options2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/request-options.mjs"() {
    FallbackEncoder2 = ({ headers, body }) => ({
      bodyHeaders: {
        "content-type": "application/json"
      },
      body: JSON.stringify(body)
    });
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/qs/formats.mjs
var default_format2, default_formatter2, formatters2, RFC17382, init_formats2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/qs/formats.mjs"() {
    default_format2 = "RFC3986", default_formatter2 = (v) => String(v), formatters2 = {
      RFC1738: (v) => String(v).replace(/%20/g, "+"),
      RFC3986: default_formatter2
    }, RFC17382 = "RFC1738";
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/qs/utils.mjs
function is_buffer2(obj) {
  return !obj || typeof obj != "object" ? !1 : !!(obj.constructor && obj.constructor.isBuffer && obj.constructor.isBuffer(obj));
}
function maybe_map2(val, fn) {
  if (isArray2(val)) {
    let mapped = [];
    for (let i = 0; i < val.length; i += 1)
      mapped.push(fn(val[i]));
    return mapped;
  }
  return fn(val);
}
var has2, hex_table2, limit2, encode2, init_utils3 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/qs/utils.mjs"() {
    init_formats2();
    init_values2();
    has2 = (obj, key) => (has2 = Object.hasOwn ?? Function.prototype.call.bind(Object.prototype.hasOwnProperty), has2(obj, key)), hex_table2 = /* @__PURE__ */ (() => {
      let array = [];
      for (let i = 0; i < 256; ++i)
        array.push("%" + ((i < 16 ? "0" : "") + i.toString(16)).toUpperCase());
      return array;
    })(), limit2 = 1024, encode2 = (str2, _defaultEncoder, charset, _kind, format) => {
      if (str2.length === 0)
        return str2;
      let string = str2;
      if (typeof str2 == "symbol" ? string = Symbol.prototype.toString.call(str2) : typeof str2 != "string" && (string = String(str2)), charset === "iso-8859-1")
        return escape(string).replace(/%u[0-9a-f]{4}/gi, function($0) {
          return "%26%23" + parseInt($0.slice(2), 16) + "%3B";
        });
      let out = "";
      for (let j = 0; j < string.length; j += limit2) {
        let segment = string.length >= limit2 ? string.slice(j, j + limit2) : string, arr = [];
        for (let i = 0; i < segment.length; ++i) {
          let c = segment.charCodeAt(i);
          if (c === 45 || // -
          c === 46 || // .
          c === 95 || // _
          c === 126 || // ~
          c >= 48 && c <= 57 || // 0-9
          c >= 65 && c <= 90 || // a-z
          c >= 97 && c <= 122 || // A-Z
          format === RFC17382 && (c === 40 || c === 41)) {
            arr[arr.length] = segment.charAt(i);
            continue;
          }
          if (c < 128) {
            arr[arr.length] = hex_table2[c];
            continue;
          }
          if (c < 2048) {
            arr[arr.length] = hex_table2[192 | c >> 6] + hex_table2[128 | c & 63];
            continue;
          }
          if (c < 55296 || c >= 57344) {
            arr[arr.length] = hex_table2[224 | c >> 12] + hex_table2[128 | c >> 6 & 63] + hex_table2[128 | c & 63];
            continue;
          }
          i += 1, c = 65536 + ((c & 1023) << 10 | segment.charCodeAt(i) & 1023), arr[arr.length] = hex_table2[240 | c >> 18] + hex_table2[128 | c >> 12 & 63] + hex_table2[128 | c >> 6 & 63] + hex_table2[128 | c & 63];
        }
        out += arr.join("");
      }
      return out;
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/qs/stringify.mjs
function is_non_nullish_primitive2(v) {
  return typeof v == "string" || typeof v == "number" || typeof v == "boolean" || typeof v == "symbol" || typeof v == "bigint";
}
function inner_stringify2(object, prefix, generateArrayPrefix, commaRoundTrip, allowEmptyArrays, strictNullHandling, skipNulls, encodeDotInKeys, encoder2, filter2, sort, allowDots, serializeDate, format, formatter, encodeValuesOnly, charset, sideChannel) {
  let obj = object, tmp_sc = sideChannel, step = 0, find_flag = !1;
  for (; (tmp_sc = tmp_sc.get(sentinel2)) !== void 0 && !find_flag; ) {
    let pos = tmp_sc.get(object);
    if (step += 1, typeof pos < "u") {
      if (pos === step)
        throw new RangeError("Cyclic object value");
      find_flag = !0;
    }
    typeof tmp_sc.get(sentinel2) > "u" && (step = 0);
  }
  if (typeof filter2 == "function" ? obj = filter2(prefix, obj) : obj instanceof Date ? obj = serializeDate?.(obj) : generateArrayPrefix === "comma" && isArray2(obj) && (obj = maybe_map2(obj, function(value) {
    return value instanceof Date ? serializeDate?.(value) : value;
  })), obj === null) {
    if (strictNullHandling)
      return encoder2 && !encodeValuesOnly ? (
        // @ts-expect-error
        encoder2(prefix, defaults3.encoder, charset, "key", format)
      ) : prefix;
    obj = "";
  }
  if (is_non_nullish_primitive2(obj) || is_buffer2(obj)) {
    if (encoder2) {
      let key_value = encodeValuesOnly ? prefix : encoder2(prefix, defaults3.encoder, charset, "key", format);
      return [
        formatter?.(key_value) + "=" + // @ts-expect-error
        formatter?.(encoder2(obj, defaults3.encoder, charset, "value", format))
      ];
    }
    return [formatter?.(prefix) + "=" + formatter?.(String(obj))];
  }
  let values = [];
  if (typeof obj > "u")
    return values;
  let obj_keys;
  if (generateArrayPrefix === "comma" && isArray2(obj))
    encodeValuesOnly && encoder2 && (obj = maybe_map2(obj, encoder2)), obj_keys = [{ value: obj.length > 0 ? obj.join(",") || null : void 0 }];
  else if (isArray2(filter2))
    obj_keys = filter2;
  else {
    let keys = Object.keys(obj);
    obj_keys = sort ? keys.sort(sort) : keys;
  }
  let encoded_prefix = encodeDotInKeys ? String(prefix).replace(/\./g, "%2E") : String(prefix), adjusted_prefix = commaRoundTrip && isArray2(obj) && obj.length === 1 ? encoded_prefix + "[]" : encoded_prefix;
  if (allowEmptyArrays && isArray2(obj) && obj.length === 0)
    return adjusted_prefix + "[]";
  for (let j = 0; j < obj_keys.length; ++j) {
    let key = obj_keys[j], value = (
      // @ts-ignore
      typeof key == "object" && typeof key.value < "u" ? key.value : obj[key]
    );
    if (skipNulls && value === null)
      continue;
    let encoded_key = allowDots && encodeDotInKeys ? key.replace(/\./g, "%2E") : key, key_prefix = isArray2(obj) ? typeof generateArrayPrefix == "function" ? generateArrayPrefix(adjusted_prefix, encoded_key) : adjusted_prefix : adjusted_prefix + (allowDots ? "." + encoded_key : "[" + encoded_key + "]");
    sideChannel.set(object, step);
    let valueSideChannel = /* @__PURE__ */ new WeakMap();
    valueSideChannel.set(sentinel2, sideChannel), push_to_array2(values, inner_stringify2(
      value,
      key_prefix,
      generateArrayPrefix,
      commaRoundTrip,
      allowEmptyArrays,
      strictNullHandling,
      skipNulls,
      encodeDotInKeys,
      // @ts-ignore
      generateArrayPrefix === "comma" && encodeValuesOnly && isArray2(obj) ? null : encoder2,
      filter2,
      sort,
      allowDots,
      serializeDate,
      format,
      formatter,
      encodeValuesOnly,
      charset,
      valueSideChannel
    ));
  }
  return values;
}
function normalize_stringify_options2(opts = defaults3) {
  if (typeof opts.allowEmptyArrays < "u" && typeof opts.allowEmptyArrays != "boolean")
    throw new TypeError("`allowEmptyArrays` option can only be `true` or `false`, when provided");
  if (typeof opts.encodeDotInKeys < "u" && typeof opts.encodeDotInKeys != "boolean")
    throw new TypeError("`encodeDotInKeys` option can only be `true` or `false`, when provided");
  if (opts.encoder !== null && typeof opts.encoder < "u" && typeof opts.encoder != "function")
    throw new TypeError("Encoder has to be a function.");
  let charset = opts.charset || defaults3.charset;
  if (typeof opts.charset < "u" && opts.charset !== "utf-8" && opts.charset !== "iso-8859-1")
    throw new TypeError("The charset option must be either utf-8, iso-8859-1, or undefined");
  let format = default_format2;
  if (typeof opts.format < "u") {
    if (!has2(formatters2, opts.format))
      throw new TypeError("Unknown format option provided.");
    format = opts.format;
  }
  let formatter = formatters2[format], filter2 = defaults3.filter;
  (typeof opts.filter == "function" || isArray2(opts.filter)) && (filter2 = opts.filter);
  let arrayFormat;
  if (opts.arrayFormat && opts.arrayFormat in array_prefix_generators2 ? arrayFormat = opts.arrayFormat : "indices" in opts ? arrayFormat = opts.indices ? "indices" : "repeat" : arrayFormat = defaults3.arrayFormat, "commaRoundTrip" in opts && typeof opts.commaRoundTrip != "boolean")
    throw new TypeError("`commaRoundTrip` must be a boolean, or absent");
  let allowDots = typeof opts.allowDots > "u" ? opts.encodeDotInKeys ? !0 : defaults3.allowDots : !!opts.allowDots;
  return {
    addQueryPrefix: typeof opts.addQueryPrefix == "boolean" ? opts.addQueryPrefix : defaults3.addQueryPrefix,
    // @ts-ignore
    allowDots,
    allowEmptyArrays: typeof opts.allowEmptyArrays == "boolean" ? !!opts.allowEmptyArrays : defaults3.allowEmptyArrays,
    arrayFormat,
    charset,
    charsetSentinel: typeof opts.charsetSentinel == "boolean" ? opts.charsetSentinel : defaults3.charsetSentinel,
    commaRoundTrip: !!opts.commaRoundTrip,
    delimiter: typeof opts.delimiter > "u" ? defaults3.delimiter : opts.delimiter,
    encode: typeof opts.encode == "boolean" ? opts.encode : defaults3.encode,
    encodeDotInKeys: typeof opts.encodeDotInKeys == "boolean" ? opts.encodeDotInKeys : defaults3.encodeDotInKeys,
    encoder: typeof opts.encoder == "function" ? opts.encoder : defaults3.encoder,
    encodeValuesOnly: typeof opts.encodeValuesOnly == "boolean" ? opts.encodeValuesOnly : defaults3.encodeValuesOnly,
    filter: filter2,
    format,
    formatter,
    serializeDate: typeof opts.serializeDate == "function" ? opts.serializeDate : defaults3.serializeDate,
    skipNulls: typeof opts.skipNulls == "boolean" ? opts.skipNulls : defaults3.skipNulls,
    // @ts-ignore
    sort: typeof opts.sort == "function" ? opts.sort : null,
    strictNullHandling: typeof opts.strictNullHandling == "boolean" ? opts.strictNullHandling : defaults3.strictNullHandling
  };
}
function stringify2(object, opts = {}) {
  let obj = object, options = normalize_stringify_options2(opts), obj_keys, filter2;
  typeof options.filter == "function" ? (filter2 = options.filter, obj = filter2("", obj)) : isArray2(options.filter) && (filter2 = options.filter, obj_keys = filter2);
  let keys = [];
  if (typeof obj != "object" || obj === null)
    return "";
  let generateArrayPrefix = array_prefix_generators2[options.arrayFormat], commaRoundTrip = generateArrayPrefix === "comma" && options.commaRoundTrip;
  obj_keys || (obj_keys = Object.keys(obj)), options.sort && obj_keys.sort(options.sort);
  let sideChannel = /* @__PURE__ */ new WeakMap();
  for (let i = 0; i < obj_keys.length; ++i) {
    let key = obj_keys[i];
    options.skipNulls && obj[key] === null || push_to_array2(keys, inner_stringify2(
      obj[key],
      key,
      // @ts-expect-error
      generateArrayPrefix,
      commaRoundTrip,
      options.allowEmptyArrays,
      options.strictNullHandling,
      options.skipNulls,
      options.encodeDotInKeys,
      options.encode ? options.encoder : null,
      options.filter,
      options.sort,
      options.allowDots,
      options.serializeDate,
      options.format,
      options.formatter,
      options.encodeValuesOnly,
      options.charset,
      sideChannel
    ));
  }
  let joined = keys.join(options.delimiter), prefix = options.addQueryPrefix === !0 ? "?" : "";
  return options.charsetSentinel && (options.charset === "iso-8859-1" ? prefix += "utf8=%26%2310003%3B&" : prefix += "utf8=%E2%9C%93&"), joined.length > 0 ? prefix + joined : "";
}
var array_prefix_generators2, push_to_array2, toISOString2, defaults3, sentinel2, init_stringify2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/qs/stringify.mjs"() {
    init_utils3();
    init_formats2();
    init_values2();
    array_prefix_generators2 = {
      brackets(prefix) {
        return String(prefix) + "[]";
      },
      comma: "comma",
      indices(prefix, key) {
        return String(prefix) + "[" + key + "]";
      },
      repeat(prefix) {
        return String(prefix);
      }
    }, push_to_array2 = function(arr, value_or_array) {
      Array.prototype.push.apply(arr, isArray2(value_or_array) ? value_or_array : [value_or_array]);
    }, defaults3 = {
      addQueryPrefix: !1,
      allowDots: !1,
      allowEmptyArrays: !1,
      arrayFormat: "indices",
      charset: "utf-8",
      charsetSentinel: !1,
      delimiter: "&",
      encode: !0,
      encodeDotInKeys: !1,
      encoder: encode2,
      encodeValuesOnly: !1,
      format: default_format2,
      formatter: default_formatter2,
      /** @deprecated */
      indices: !1,
      serializeDate(date) {
        return (toISOString2 ?? (toISOString2 = Function.prototype.call.bind(Date.prototype.toISOString)))(date);
      },
      skipNulls: !1,
      strictNullHandling: !1
    };
    sentinel2 = {};
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/utils/query.mjs
function stringifyQuery2(query) {
  return stringify2(query, { arrayFormat: "brackets" });
}
var init_query2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/utils/query.mjs"() {
    init_stringify2();
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/utils/bytes.mjs
function concatBytes2(buffers) {
  let length = 0;
  for (let buffer of buffers)
    length += buffer.length;
  let output = new Uint8Array(length), index = 0;
  for (let buffer of buffers)
    output.set(buffer, index), index += buffer.length;
  return output;
}
function encodeUTF82(str2) {
  let encoder2;
  return (encodeUTF8_2 ?? (encoder2 = new globalThis.TextEncoder(), encodeUTF8_2 = encoder2.encode.bind(encoder2)))(str2);
}
function decodeUTF82(bytes) {
  let decoder;
  return (decodeUTF8_2 ?? (decoder = new globalThis.TextDecoder(), decodeUTF8_2 = decoder.decode.bind(decoder)))(bytes);
}
var encodeUTF8_2, decodeUTF8_2, init_bytes2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/utils/bytes.mjs"() {
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/decoders/line.mjs
function findNewlineIndex2(buffer, startIndex) {
  for (let i = startIndex ?? 0; i < buffer.length; i++) {
    if (buffer[i] === 10)
      return { preceding: i, index: i + 1, carriage: !1 };
    if (buffer[i] === 13)
      return { preceding: i, index: i + 1, carriage: !0 };
  }
  return null;
}
function findDoubleNewlineIndex2(buffer) {
  for (let i = 0; i < buffer.length - 1; i++) {
    if (buffer[i] === 10 && buffer[i + 1] === 10 || buffer[i] === 13 && buffer[i + 1] === 13)
      return i + 2;
    if (buffer[i] === 13 && buffer[i + 1] === 10 && i + 3 < buffer.length && buffer[i + 2] === 13 && buffer[i + 3] === 10)
      return i + 4;
  }
  return -1;
}
var _LineDecoder_buffer2, _LineDecoder_carriageReturnIndex2, LineDecoder2, init_line2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/decoders/line.mjs"() {
    init_tslib2();
    init_bytes2();
    LineDecoder2 = class {
      constructor() {
        _LineDecoder_buffer2.set(this, void 0), _LineDecoder_carriageReturnIndex2.set(this, void 0), __classPrivateFieldSet2(this, _LineDecoder_buffer2, new Uint8Array(), "f"), __classPrivateFieldSet2(this, _LineDecoder_carriageReturnIndex2, null, "f");
      }
      decode(chunk) {
        if (chunk == null)
          return [];
        let binaryChunk = chunk instanceof ArrayBuffer ? new Uint8Array(chunk) : typeof chunk == "string" ? encodeUTF82(chunk) : chunk;
        __classPrivateFieldSet2(this, _LineDecoder_buffer2, concatBytes2([__classPrivateFieldGet2(this, _LineDecoder_buffer2, "f"), binaryChunk]), "f");
        let lines = [], patternIndex;
        for (; (patternIndex = findNewlineIndex2(__classPrivateFieldGet2(this, _LineDecoder_buffer2, "f"), __classPrivateFieldGet2(this, _LineDecoder_carriageReturnIndex2, "f"))) != null; ) {
          if (patternIndex.carriage && __classPrivateFieldGet2(this, _LineDecoder_carriageReturnIndex2, "f") == null) {
            __classPrivateFieldSet2(this, _LineDecoder_carriageReturnIndex2, patternIndex.index, "f");
            continue;
          }
          if (__classPrivateFieldGet2(this, _LineDecoder_carriageReturnIndex2, "f") != null && (patternIndex.index !== __classPrivateFieldGet2(this, _LineDecoder_carriageReturnIndex2, "f") + 1 || patternIndex.carriage)) {
            lines.push(decodeUTF82(__classPrivateFieldGet2(this, _LineDecoder_buffer2, "f").subarray(0, __classPrivateFieldGet2(this, _LineDecoder_carriageReturnIndex2, "f") - 1))), __classPrivateFieldSet2(this, _LineDecoder_buffer2, __classPrivateFieldGet2(this, _LineDecoder_buffer2, "f").subarray(__classPrivateFieldGet2(this, _LineDecoder_carriageReturnIndex2, "f")), "f"), __classPrivateFieldSet2(this, _LineDecoder_carriageReturnIndex2, null, "f");
            continue;
          }
          let endIndex = __classPrivateFieldGet2(this, _LineDecoder_carriageReturnIndex2, "f") !== null ? patternIndex.preceding - 1 : patternIndex.preceding, line = decodeUTF82(__classPrivateFieldGet2(this, _LineDecoder_buffer2, "f").subarray(0, endIndex));
          lines.push(line), __classPrivateFieldSet2(this, _LineDecoder_buffer2, __classPrivateFieldGet2(this, _LineDecoder_buffer2, "f").subarray(patternIndex.index), "f"), __classPrivateFieldSet2(this, _LineDecoder_carriageReturnIndex2, null, "f");
        }
        return lines;
      }
      flush() {
        return __classPrivateFieldGet2(this, _LineDecoder_buffer2, "f").length ? this.decode(`
`) : [];
      }
    };
    _LineDecoder_buffer2 = /* @__PURE__ */ new WeakMap(), _LineDecoder_carriageReturnIndex2 = /* @__PURE__ */ new WeakMap();
    LineDecoder2.NEWLINE_CHARS = /* @__PURE__ */ new Set([`
`, "\r"]);
    LineDecoder2.NEWLINE_REGEXP = /\r\n|[\n\r]/g;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/utils/log.mjs
function noop2() {
}
function makeLogFn2(fnLevel, logger, logLevel) {
  return !logger || levelNumbers2[fnLevel] > levelNumbers2[logLevel] ? noop2 : logger[fnLevel].bind(logger);
}
function loggerFor2(client) {
  let logger = client.logger, logLevel = client.logLevel ?? "off";
  if (!logger)
    return noopLogger2;
  let cachedLogger = cachedLoggers2.get(logger);
  if (cachedLogger && cachedLogger[0] === logLevel)
    return cachedLogger[1];
  let levelLogger = {
    error: makeLogFn2("error", logger, logLevel),
    warn: makeLogFn2("warn", logger, logLevel),
    info: makeLogFn2("info", logger, logLevel),
    debug: makeLogFn2("debug", logger, logLevel)
  };
  return cachedLoggers2.set(logger, [logLevel, levelLogger]), levelLogger;
}
var levelNumbers2, parseLogLevel2, noopLogger2, cachedLoggers2, formatRequestDetails2, init_log2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/utils/log.mjs"() {
    init_values2();
    levelNumbers2 = {
      off: 0,
      error: 200,
      warn: 300,
      info: 400,
      debug: 500
    }, parseLogLevel2 = (maybeLevel, sourceName, client) => {
      if (maybeLevel) {
        if (hasOwn2(levelNumbers2, maybeLevel))
          return maybeLevel;
        loggerFor2(client).warn(`${sourceName} was set to ${JSON.stringify(maybeLevel)}, expected one of ${JSON.stringify(Object.keys(levelNumbers2))}`);
      }
    };
    noopLogger2 = {
      error: noop2,
      warn: noop2,
      info: noop2,
      debug: noop2
    }, cachedLoggers2 = /* @__PURE__ */ new WeakMap();
    formatRequestDetails2 = (details) => (details.options && (details.options = { ...details.options }, delete details.options.headers), details.headers && (details.headers = Object.fromEntries((details.headers instanceof Headers ? [...details.headers] : Object.entries(details.headers)).map(([name, value]) => [
      name,
      name.toLowerCase() === "authorization" || name.toLowerCase() === "api-key" || name.toLowerCase() === "x-api-key" || name.toLowerCase() === "x-amz-security-token" || name.toLowerCase() === "cookie" || name.toLowerCase() === "set-cookie" ? "***" : value
    ]))), "retryOfRequestLogID" in details && (details.retryOfRequestLogID && (details.retryOf = details.retryOfRequestLogID), delete details.retryOfRequestLogID), details);
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/core/streaming.mjs
async function* _iterSSEMessages2(response, controller) {
  if (!response.body)
    throw controller.abort(), typeof globalThis.navigator < "u" && globalThis.navigator.product === "ReactNative" ? new OpenAIError("The default react-native fetch implementation does not support streaming. Please use expo/fetch: https://docs.expo.dev/versions/latest/sdk/expo/#expofetch-api") : new OpenAIError("Attempted to iterate over a response with no body");
  let sseDecoder = new SSEDecoder2(), lineDecoder = new LineDecoder2(), iter = ReadableStreamToAsyncIterable2(response.body);
  for await (let sseChunk of iterSSEChunks2(iter))
    for (let line of lineDecoder.decode(sseChunk)) {
      let sse = sseDecoder.decode(line);
      sse && (yield sse);
    }
  for (let line of lineDecoder.flush()) {
    let sse = sseDecoder.decode(line);
    sse && (yield sse);
  }
}
async function* iterSSEChunks2(iterator) {
  let data = new Uint8Array();
  for await (let chunk of iterator) {
    if (chunk == null)
      continue;
    let binaryChunk = chunk instanceof ArrayBuffer ? new Uint8Array(chunk) : typeof chunk == "string" ? encodeUTF82(chunk) : chunk, newData = new Uint8Array(data.length + binaryChunk.length);
    newData.set(data), newData.set(binaryChunk, data.length), data = newData;
    let patternIndex;
    for (; (patternIndex = findDoubleNewlineIndex2(data)) !== -1; )
      yield data.slice(0, patternIndex), data = data.slice(patternIndex);
  }
  data.length > 0 && (yield data);
}
function partition2(str2, delimiter2) {
  let index = str2.indexOf(delimiter2);
  return index !== -1 ? [str2.substring(0, index), delimiter2, str2.substring(index + delimiter2.length)] : [str2, "", ""];
}
var _Stream_client2, Stream2, SSEDecoder2, init_streaming3 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/core/streaming.mjs"() {
    init_tslib2();
    init_error3();
    init_shims2();
    init_line2();
    init_shims2();
    init_errors3();
    init_bytes2();
    init_log2();
    init_error3();
    Stream2 = class _Stream {
      constructor(iterator, controller, client) {
        this.iterator = iterator, _Stream_client2.set(this, void 0), this.controller = controller, __classPrivateFieldSet2(this, _Stream_client2, client, "f");
      }
      static fromSSEResponse(response, controller, client, synthesizeEventData) {
        let consumed = !1, logger = client ? loggerFor2(client) : console;
        async function* iterator() {
          if (consumed)
            throw new OpenAIError("Cannot iterate over a consumed stream, use `.tee()` to split the stream.");
          consumed = !0;
          let done = !1;
          try {
            for await (let sse of _iterSSEMessages2(response, controller))
              if (!done) {
                if (sse.data.startsWith("[DONE]")) {
                  done = !0;
                  continue;
                }
                if (sse.event === null || !sse.event.startsWith("thread.")) {
                  let data;
                  try {
                    data = JSON.parse(sse.data);
                  } catch (e) {
                    throw logger.error("Could not parse message into JSON:", sse.data), logger.error("From chunk:", sse.raw), e;
                  }
                  if (data && data.error)
                    throw new APIError2(void 0, data.error, void 0, response.headers);
                  yield synthesizeEventData ? { event: sse.event, data } : data;
                } else {
                  let data;
                  try {
                    data = JSON.parse(sse.data);
                  } catch (e) {
                    throw console.error("Could not parse message into JSON:", sse.data), console.error("From chunk:", sse.raw), e;
                  }
                  if (sse.event == "error")
                    throw new APIError2(void 0, data.error, data.message, void 0);
                  yield { event: sse.event, data };
                }
              }
            done = !0;
          } catch (e) {
            if (isAbortError2(e))
              return;
            throw e;
          } finally {
            done || controller.abort();
          }
        }
        return new _Stream(iterator, controller, client);
      }
      /**
       * Generates a Stream from a newline-separated ReadableStream
       * where each item is a JSON value.
       */
      static fromReadableStream(readableStream, controller, client) {
        let consumed = !1;
        async function* iterLines() {
          let lineDecoder = new LineDecoder2(), iter = ReadableStreamToAsyncIterable2(readableStream);
          for await (let chunk of iter)
            for (let line of lineDecoder.decode(chunk))
              yield line;
          for (let line of lineDecoder.flush())
            yield line;
        }
        async function* iterator() {
          if (consumed)
            throw new OpenAIError("Cannot iterate over a consumed stream, use `.tee()` to split the stream.");
          consumed = !0;
          let done = !1;
          try {
            for await (let line of iterLines())
              done || line && (yield JSON.parse(line));
            done = !0;
          } catch (e) {
            if (isAbortError2(e))
              return;
            throw e;
          } finally {
            done || controller.abort();
          }
        }
        return new _Stream(iterator, controller, client);
      }
      [(_Stream_client2 = /* @__PURE__ */ new WeakMap(), Symbol.asyncIterator)]() {
        return this.iterator();
      }
      /**
       * Splits the stream into two streams which can be
       * independently read from at different speeds.
       */
      tee() {
        let left = [], right = [], iterator = this.iterator(), teeIterator = (queue) => ({
          next: () => {
            if (queue.length === 0) {
              let result = iterator.next();
              left.push(result), right.push(result);
            }
            return queue.shift();
          }
        });
        return [
          new _Stream(() => teeIterator(left), this.controller, __classPrivateFieldGet2(this, _Stream_client2, "f")),
          new _Stream(() => teeIterator(right), this.controller, __classPrivateFieldGet2(this, _Stream_client2, "f"))
        ];
      }
      /**
       * Converts this stream to a newline-separated ReadableStream of
       * JSON stringified values in the stream
       * which can be turned back into a Stream with `Stream.fromReadableStream()`.
       */
      toReadableStream() {
        let self = this, iter;
        return makeReadableStream2({
          async start() {
            iter = self[Symbol.asyncIterator]();
          },
          async pull(ctrl) {
            try {
              let { value, done } = await iter.next();
              if (done)
                return ctrl.close();
              let bytes = encodeUTF82(JSON.stringify(value) + `
`);
              ctrl.enqueue(bytes);
            } catch (err) {
              ctrl.error(err);
            }
          },
          async cancel() {
            await iter.return?.();
          }
        });
      }
    };
    SSEDecoder2 = class {
      constructor() {
        this.event = null, this.data = [], this.chunks = [];
      }
      decode(line) {
        if (line.endsWith("\r") && (line = line.substring(0, line.length - 1)), !line) {
          if (!this.event && !this.data.length)
            return null;
          let sse = {
            event: this.event,
            data: this.data.join(`
`),
            raw: this.chunks
          };
          return this.event = null, this.data = [], this.chunks = [], sse;
        }
        if (this.chunks.push(line), line.startsWith(":"))
          return null;
        let [fieldname, _, value] = partition2(line, ":");
        return value.startsWith(" ") && (value = value.substring(1)), fieldname === "event" ? this.event = value : fieldname === "data" && this.data.push(value), null;
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/parse.mjs
async function defaultParseResponse2(client, props) {
  let { response, requestLogID, retryOfRequestLogID, startTime } = props, body = await (async () => {
    if (props.options.stream)
      return loggerFor2(client).debug("response", response.status, response.url, response.headers, response.body), props.options.__streamClass ? props.options.__streamClass.fromSSEResponse(response, props.controller, client, props.options.__synthesizeEventData) : Stream2.fromSSEResponse(response, props.controller, client, props.options.__synthesizeEventData);
    if (response.status === 204)
      return null;
    if (props.options.__binaryResponse)
      return response;
    let mediaType = response.headers.get("content-type")?.split(";")[0]?.trim();
    if (mediaType?.includes("application/json") || mediaType?.endsWith("+json")) {
      if (response.headers.get("content-length") === "0")
        return;
      let json = await response.json();
      return addRequestID2(json, response);
    }
    return await response.text();
  })();
  return loggerFor2(client).debug(`[${requestLogID}] response parsed`, formatRequestDetails2({
    retryOfRequestLogID,
    url: response.url,
    status: response.status,
    body,
    durationMs: Date.now() - startTime
  })), body;
}
function addRequestID2(value, response) {
  return !value || typeof value != "object" || Array.isArray(value) ? value : Object.defineProperty(value, "_request_id", {
    value: response.headers.get("x-request-id"),
    enumerable: !1
  });
}
var init_parse2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/parse.mjs"() {
    init_streaming3();
    init_log2();
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/core/api-promise.mjs
var _APIPromise_client2, APIPromise2, init_api_promise2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/core/api-promise.mjs"() {
    init_tslib2();
    init_parse2();
    APIPromise2 = class _APIPromise extends Promise {
      constructor(client, responsePromise, parseResponse2 = defaultParseResponse2) {
        super((resolve4) => {
          resolve4(null);
        }), this.responsePromise = responsePromise, this.parseResponse = parseResponse2, _APIPromise_client2.set(this, void 0), __classPrivateFieldSet2(this, _APIPromise_client2, client, "f");
      }
      _thenUnwrap(transform) {
        return new _APIPromise(__classPrivateFieldGet2(this, _APIPromise_client2, "f"), this.responsePromise, async (client, props) => addRequestID2(transform(await this.parseResponse(client, props), props), props.response));
      }
      /**
       * Gets the raw `Response` instance instead of parsing the response
       * data.
       *
       * If you want to parse the response body but still get the `Response`
       * instance, you can use {@link withResponse()}.
       *
       * 👋 Getting the wrong TypeScript type for `Response`?
       * Try setting `"moduleResolution": "NodeNext"` or add `"lib": ["DOM"]`
       * to your `tsconfig.json`.
       */
      asResponse() {
        return this.responsePromise.then((p) => p.response);
      }
      /**
       * Gets the parsed response data, the raw `Response` instance and the ID of the request,
       * returned via the X-Request-ID header which is useful for debugging requests and reporting
       * issues to OpenAI.
       *
       * If you just want to get the raw `Response` instance without parsing it,
       * you can use {@link asResponse()}.
       *
       * 👋 Getting the wrong TypeScript type for `Response`?
       * Try setting `"moduleResolution": "NodeNext"` or add `"lib": ["DOM"]`
       * to your `tsconfig.json`.
       */
      async withResponse() {
        let [data, response] = await Promise.all([this.parse(), this.asResponse()]);
        return { data, response, request_id: response.headers.get("x-request-id") };
      }
      parse() {
        return this.parsedPromise || (this.parsedPromise = this.responsePromise.then((data) => this.parseResponse(__classPrivateFieldGet2(this, _APIPromise_client2, "f"), data))), this.parsedPromise;
      }
      then(onfulfilled, onrejected) {
        return this.parse().then(onfulfilled, onrejected);
      }
      catch(onrejected) {
        return this.parse().catch(onrejected);
      }
      finally(onfinally) {
        return this.parse().finally(onfinally);
      }
    };
    _APIPromise_client2 = /* @__PURE__ */ new WeakMap();
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/core/pagination.mjs
var _AbstractPage_client2, AbstractPage2, PagePromise2, Page2, CursorPage, ConversationCursorPage, NextCursorPage, init_pagination2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/core/pagination.mjs"() {
    init_tslib2();
    init_error3();
    init_parse2();
    init_api_promise2();
    init_values2();
    AbstractPage2 = class {
      constructor(client, response, body, options) {
        _AbstractPage_client2.set(this, void 0), __classPrivateFieldSet2(this, _AbstractPage_client2, client, "f"), this.options = options, this.response = response, this.body = body;
      }
      hasNextPage() {
        return this.getPaginatedItems().length ? this.nextPageRequestOptions() != null : !1;
      }
      async getNextPage() {
        let nextOptions = this.nextPageRequestOptions();
        if (!nextOptions)
          throw new OpenAIError("No next page expected; please check `.hasNextPage()` before calling `.getNextPage()`.");
        return await __classPrivateFieldGet2(this, _AbstractPage_client2, "f").requestAPIList(this.constructor, nextOptions);
      }
      async *iterPages() {
        let page = this;
        for (yield page; page.hasNextPage(); )
          page = await page.getNextPage(), yield page;
      }
      async *[(_AbstractPage_client2 = /* @__PURE__ */ new WeakMap(), Symbol.asyncIterator)]() {
        for await (let page of this.iterPages())
          for (let item of page.getPaginatedItems())
            yield item;
      }
    }, PagePromise2 = class extends APIPromise2 {
      constructor(client, request, Page3) {
        super(client, request, async (client2, props) => new Page3(client2, props.response, await defaultParseResponse2(client2, props), props.options));
      }
      /**
       * Allow auto-paginating iteration on an unawaited list call, eg:
       *
       *    for await (const item of client.items.list()) {
       *      console.log(item)
       *    }
       */
      async *[Symbol.asyncIterator]() {
        let page = await this;
        for await (let item of page)
          yield item;
      }
    }, Page2 = class extends AbstractPage2 {
      constructor(client, response, body, options) {
        super(client, response, body, options), this.data = body.data || [], this.object = body.object;
      }
      getPaginatedItems() {
        return this.data ?? [];
      }
      nextPageRequestOptions() {
        return null;
      }
    }, CursorPage = class extends AbstractPage2 {
      constructor(client, response, body, options) {
        super(client, response, body, options), this.data = body.data || [], this.has_more = body.has_more || !1;
      }
      getPaginatedItems() {
        return this.data ?? [];
      }
      hasNextPage() {
        return this.has_more === !1 ? !1 : super.hasNextPage();
      }
      nextPageRequestOptions() {
        let data = this.getPaginatedItems(), id = data[data.length - 1]?.id;
        return id ? {
          ...this.options,
          query: {
            ...maybeObj2(this.options.query),
            after: id
          }
        } : null;
      }
    }, ConversationCursorPage = class extends AbstractPage2 {
      constructor(client, response, body, options) {
        super(client, response, body, options), this.data = body.data || [], this.has_more = body.has_more || !1, this.last_id = body.last_id || "";
      }
      getPaginatedItems() {
        return this.data ?? [];
      }
      hasNextPage() {
        return this.has_more === !1 ? !1 : super.hasNextPage();
      }
      nextPageRequestOptions() {
        let cursor = this.last_id;
        return cursor ? {
          ...this.options,
          query: {
            ...maybeObj2(this.options.query),
            after: cursor
          }
        } : null;
      }
    }, NextCursorPage = class extends AbstractPage2 {
      constructor(client, response, body, options) {
        super(client, response, body, options), this.data = body.data || [], this.has_more = body.has_more || !1, this.next = body.next || null;
      }
      getPaginatedItems() {
        return this.data ?? [];
      }
      hasNextPage() {
        return this.has_more === !1 ? !1 : super.hasNextPage();
      }
      nextPageRequestOptions() {
        let cursor = this.next;
        return cursor ? {
          ...this.options,
          query: {
            ...maybeObj2(this.options.query),
            after: cursor
          }
        } : null;
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/auth/workload-identity-auth.mjs
var SUBJECT_TOKEN_TYPES, TOKEN_EXCHANGE_GRANT_TYPE, WorkloadIdentityAuth, init_workload_identity_auth = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/auth/workload-identity-auth.mjs"() {
    init_shims2();
    init_error3();
    SUBJECT_TOKEN_TYPES = {
      jwt: "urn:ietf:params:oauth:token-type:jwt",
      id: "urn:ietf:params:oauth:token-type:id_token"
    }, TOKEN_EXCHANGE_GRANT_TYPE = "urn:ietf:params:oauth:grant-type:token-exchange", WorkloadIdentityAuth = class {
      constructor(config, fetch2) {
        this.cachedToken = null, this.refreshPromise = null, this.tokenExchangeUrl = "https://auth.openai.com/oauth/token", this.config = config, this.fetch = fetch2 ?? getDefaultFetch2();
      }
      async getToken() {
        if (!this.cachedToken || this.isTokenExpired(this.cachedToken)) {
          if (this.refreshPromise)
            return await this.refreshPromise;
          this.refreshPromise = this.refreshToken();
          try {
            return await this.refreshPromise;
          } finally {
            this.refreshPromise = null;
          }
        }
        return this.needsRefresh(this.cachedToken) && !this.refreshPromise && (this.refreshPromise = this.refreshToken().finally(() => {
          this.refreshPromise = null;
        })), this.cachedToken.token;
      }
      async refreshToken() {
        let subjectToken = await this.config.provider.getToken(), body = {
          grant_type: TOKEN_EXCHANGE_GRANT_TYPE,
          subject_token: subjectToken,
          subject_token_type: SUBJECT_TOKEN_TYPES[this.config.provider.tokenType],
          identity_provider_id: this.config.identityProviderId,
          service_account_id: this.config.serviceAccountId
        };
        this.config.clientId && (body.client_id = this.config.clientId);
        let response = await this.fetch(this.tokenExchangeUrl, {
          method: "POST",
          headers: {
            "Content-Type": "application/json"
          },
          body: JSON.stringify(body)
        });
        if (!response.ok) {
          let errorText = await response.text(), body2;
          try {
            body2 = JSON.parse(errorText);
          } catch {
          }
          throw response.status === 400 || response.status === 401 || response.status === 403 ? new OAuthError(response.status, body2, response.headers) : APIError2.generate(response.status, body2, `Token exchange failed with status ${response.status}`, response.headers);
        }
        let tokenResponse = await response.json();
        if (typeof tokenResponse != "object" || tokenResponse === null || !("access_token" in tokenResponse) || typeof tokenResponse.access_token != "string" || tokenResponse.access_token.trim().length === 0)
          throw new OpenAIError("Token exchange response missing 'access_token' field");
        let accessToken = tokenResponse.access_token, expiresIn = tokenResponse.expires_in ?? 3600, expiresAt = Date.now() + expiresIn * 1e3;
        return this.cachedToken = {
          token: accessToken,
          expiresAt
        }, accessToken;
      }
      isTokenExpired(cachedToken) {
        return Date.now() >= cachedToken.expiresAt;
      }
      needsRefresh(cachedToken) {
        let bufferMs = (this.config.refreshBufferSeconds ?? 1200) * 1e3;
        return Date.now() >= cachedToken.expiresAt - bufferMs;
      }
      invalidateToken() {
        this.cachedToken = null, this.refreshPromise = null;
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/uploads.mjs
function makeFile2(fileBits, fileName, options) {
  return checkFileSupport2(), new File(fileBits, fileName ?? "unknown_file", options);
}
function getName2(value) {
  return (typeof value == "object" && value !== null && ("name" in value && value.name && String(value.name) || "url" in value && value.url && String(value.url) || "filename" in value && value.filename && String(value.filename) || "path" in value && value.path && String(value.path)) || "").split(/[\\/]/).pop() || void 0;
}
function supportsFormData2(fetchObject) {
  let fetch2 = typeof fetchObject == "function" ? fetchObject : fetchObject.fetch, cached = supportsFormDataMap2.get(fetch2);
  if (cached)
    return cached;
  let promise = (async () => {
    try {
      let FetchResponse = "Response" in fetch2 ? fetch2.Response : (await fetch2("data:,")).constructor, data = new FormData();
      return data.toString() !== await new FetchResponse(data).text();
    } catch {
      return !0;
    }
  })();
  return supportsFormDataMap2.set(fetch2, promise), promise;
}
var checkFileSupport2, isAsyncIterable2, maybeMultipartFormRequestOptions, multipartFormRequestOptions2, supportsFormDataMap2, createForm2, isNamedBlob2, isUploadable, hasUploadableValue, addFormValue2, init_uploads3 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/uploads.mjs"() {
    init_shims2();
    checkFileSupport2 = () => {
      if (typeof File > "u") {
        let { process: process2 } = globalThis, isOldNode = typeof process2?.versions?.node == "string" && parseInt(process2.versions.node.split(".")) < 20;
        throw new Error("`File` is not defined as a global, which is required for file uploads." + (isOldNode ? " Update to Node 20 LTS or newer, or set `globalThis.File` to `import('node:buffer').File`." : ""));
      }
    };
    isAsyncIterable2 = (value) => value != null && typeof value == "object" && typeof value[Symbol.asyncIterator] == "function", maybeMultipartFormRequestOptions = async (opts, fetch2) => hasUploadableValue(opts.body) ? { ...opts, body: await createForm2(opts.body, fetch2) } : opts, multipartFormRequestOptions2 = async (opts, fetch2) => ({ ...opts, body: await createForm2(opts.body, fetch2) }), supportsFormDataMap2 = /* @__PURE__ */ new WeakMap();
    createForm2 = async (body, fetch2) => {
      if (!await supportsFormData2(fetch2))
        throw new TypeError("The provided fetch function does not support file uploads with the current global FormData class.");
      let form = new FormData();
      return await Promise.all(Object.entries(body || {}).map(([key, value]) => addFormValue2(form, key, value))), form;
    }, isNamedBlob2 = (value) => value instanceof Blob && "name" in value, isUploadable = (value) => typeof value == "object" && value !== null && (value instanceof Response || isAsyncIterable2(value) || isNamedBlob2(value)), hasUploadableValue = (value) => {
      if (isUploadable(value))
        return !0;
      if (Array.isArray(value))
        return value.some(hasUploadableValue);
      if (value && typeof value == "object") {
        for (let k in value)
          if (hasUploadableValue(value[k]))
            return !0;
      }
      return !1;
    }, addFormValue2 = async (form, key, value) => {
      if (value !== void 0) {
        if (value == null)
          throw new TypeError(`Received null for "${key}"; to pass null in FormData, you must use the string 'null'`);
        if (typeof value == "string" || typeof value == "number" || typeof value == "boolean")
          form.append(key, String(value));
        else if (value instanceof Response)
          form.append(key, makeFile2([await value.blob()], getName2(value)));
        else if (isAsyncIterable2(value))
          form.append(key, makeFile2([await new Response(ReadableStreamFrom2(value)).blob()], getName2(value)));
        else if (isNamedBlob2(value))
          form.append(key, value, getName2(value));
        else if (Array.isArray(value))
          await Promise.all(value.map((entry) => addFormValue2(form, key + "[]", entry)));
        else if (typeof value == "object")
          await Promise.all(Object.entries(value).map(([name, prop]) => addFormValue2(form, `${key}[${name}]`, prop)));
        else
          throw new TypeError(`Invalid value given to form, expected a string, number, boolean, object, Array, File or Blob but got ${value} instead`);
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/to-file.mjs
async function toFile2(value, name, options) {
  if (checkFileSupport2(), value = await value, isFileLike2(value))
    return value instanceof File ? value : makeFile2([await value.arrayBuffer()], value.name);
  if (isResponseLike2(value)) {
    let blob = await value.blob();
    return name || (name = new URL(value.url).pathname.split(/[\\/]/).pop()), makeFile2(await getBytes2(blob), name, options);
  }
  let parts = await getBytes2(value);
  if (name || (name = getName2(value)), !options?.type) {
    let type = parts.find((part) => typeof part == "object" && "type" in part && part.type);
    typeof type == "string" && (options = { ...options, type });
  }
  return makeFile2(parts, name, options);
}
async function getBytes2(value) {
  let parts = [];
  if (typeof value == "string" || ArrayBuffer.isView(value) || // includes Uint8Array, Buffer, etc.
  value instanceof ArrayBuffer)
    parts.push(value);
  else if (isBlobLike2(value))
    parts.push(value instanceof Blob ? value : await value.arrayBuffer());
  else if (isAsyncIterable2(value))
    for await (let chunk of value)
      parts.push(...await getBytes2(chunk));
  else {
    let constructor = value?.constructor?.name;
    throw new Error(`Unexpected data type: ${typeof value}${constructor ? `; constructor: ${constructor}` : ""}${propsForError2(value)}`);
  }
  return parts;
}
function propsForError2(value) {
  return typeof value != "object" || value === null ? "" : `; props: [${Object.getOwnPropertyNames(value).map((p) => `"${p}"`).join(", ")}]`;
}
var isBlobLike2, isFileLike2, isResponseLike2, init_to_file2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/to-file.mjs"() {
    init_uploads3();
    init_uploads3();
    isBlobLike2 = (value) => value != null && typeof value == "object" && typeof value.size == "number" && typeof value.type == "string" && typeof value.text == "function" && typeof value.slice == "function" && typeof value.arrayBuffer == "function", isFileLike2 = (value) => value != null && typeof value == "object" && typeof value.name == "string" && typeof value.lastModified == "number" && isBlobLike2(value), isResponseLike2 = (value) => value != null && typeof value == "object" && typeof value.url == "string" && typeof value.blob == "function";
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/core/uploads.mjs
var init_uploads4 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/core/uploads.mjs"() {
    init_to_file2();
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/core/resource.mjs
var APIResource2, init_resource2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/core/resource.mjs"() {
    APIResource2 = class {
      constructor(client) {
        this._client = client;
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/utils/path.mjs
function encodeURIPath2(str2) {
  return str2.replace(/[^A-Za-z0-9\-._~!$&'()*+,;=:@]+/g, encodeURIComponent);
}
var EMPTY2, createPathTagFunction2, path7, init_path2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/utils/path.mjs"() {
    init_error3();
    EMPTY2 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.create(null)), createPathTagFunction2 = (pathEncoder = encodeURIPath2) => function(statics, ...params) {
      if (statics.length === 1)
        return statics[0];
      let postPath = !1, invalidSegments = [], path13 = statics.reduce((previousValue, currentValue, index) => {
        /[?#]/.test(currentValue) && (postPath = !0);
        let value = params[index], encoded = (postPath ? encodeURIComponent : pathEncoder)("" + value);
        return index !== params.length && (value == null || typeof value == "object" && // handle values from other realms
        value.toString === Object.getPrototypeOf(Object.getPrototypeOf(value.hasOwnProperty ?? EMPTY2) ?? EMPTY2)?.toString) && (encoded = value + "", invalidSegments.push({
          start: previousValue.length + currentValue.length,
          length: encoded.length,
          error: `Value of type ${Object.prototype.toString.call(value).slice(8, -1)} is not a valid path parameter`
        })), previousValue + currentValue + (index === params.length ? "" : encoded);
      }, ""), pathOnly = path13.split(/[?#]/, 1)[0], invalidSegmentPattern = /(?<=^|\/)(?:\.|%2e){1,2}(?=\/|$)/gi, match2;
      for (; (match2 = invalidSegmentPattern.exec(pathOnly)) !== null; )
        invalidSegments.push({
          start: match2.index,
          length: match2[0].length,
          error: `Value "${match2[0]}" can't be safely passed as a path parameter`
        });
      if (invalidSegments.sort((a, b) => a.start - b.start), invalidSegments.length > 0) {
        let lastEnd = 0, underline = invalidSegments.reduce((acc, segment) => {
          let spaces = " ".repeat(segment.start - lastEnd), arrows = "^".repeat(segment.length);
          return lastEnd = segment.start + segment.length, acc + spaces + arrows;
        }, "");
        throw new OpenAIError(`Path parameters result in path with invalid segments:
${invalidSegments.map((e) => e.error).join(`
`)}
${path13}
${underline}`);
      }
      return path13;
    }, path7 = /* @__PURE__ */ createPathTagFunction2(encodeURIPath2);
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/chat/completions/messages.mjs
var Messages3, init_messages3 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/chat/completions/messages.mjs"() {
    init_resource2();
    init_pagination2();
    init_path2();
    Messages3 = class extends APIResource2 {
      /**
       * Get the messages in a stored chat completion. Only Chat Completions that have
       * been created with the `store` parameter set to `true` will be returned.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const chatCompletionStoreMessage of client.chat.completions.messages.list(
       *   'completion_id',
       * )) {
       *   // ...
       * }
       * ```
       */
      list(completionID, query = {}, options) {
        return this._client.getAPIList(path7`/chat/completions/${completionID}/messages`, CursorPage, { query, ...options, __security: { bearerAuth: !0 } });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/error.mjs
var init_error4 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/error.mjs"() {
    init_error3();
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/lib/parser.mjs
function isChatCompletionFunctionTool(tool) {
  return tool !== void 0 && "function" in tool && tool.function !== void 0;
}
function isAutoParsableResponseFormat(response_format) {
  return response_format?.$brand === "auto-parseable-response-format";
}
function isAutoParsableTool(tool) {
  return tool?.$brand === "auto-parseable-tool";
}
function maybeParseChatCompletion(completion, params) {
  return !params || !hasAutoParseableInput(params) ? {
    ...completion,
    choices: completion.choices.map((choice) => (assertToolCallsAreChatCompletionFunctionToolCalls(choice.message.tool_calls), {
      ...choice,
      message: {
        ...choice.message,
        parsed: null,
        ...choice.message.tool_calls ? {
          tool_calls: choice.message.tool_calls
        } : void 0
      }
    }))
  } : parseChatCompletion(completion, params);
}
function parseChatCompletion(completion, params) {
  let choices = completion.choices.map((choice) => {
    if (choice.finish_reason === "length")
      throw new LengthFinishReasonError();
    if (choice.finish_reason === "content_filter")
      throw new ContentFilterFinishReasonError();
    return assertToolCallsAreChatCompletionFunctionToolCalls(choice.message.tool_calls), {
      ...choice,
      message: {
        ...choice.message,
        ...choice.message.tool_calls ? {
          tool_calls: choice.message.tool_calls?.map((toolCall) => parseToolCall(params, toolCall)) ?? void 0
        } : void 0,
        parsed: choice.message.content && !choice.message.refusal ? parseResponseFormat(params, choice.message.content) : null
      }
    };
  });
  return { ...completion, choices };
}
function parseResponseFormat(params, content) {
  return params.response_format?.type !== "json_schema" ? null : params.response_format?.type === "json_schema" ? "$parseRaw" in params.response_format ? params.response_format.$parseRaw(content) : JSON.parse(content) : null;
}
function parseToolCall(params, toolCall) {
  let inputTool = params.tools?.find((inputTool2) => isChatCompletionFunctionTool(inputTool2) && inputTool2.function?.name === toolCall.function.name);
  return {
    ...toolCall,
    function: {
      ...toolCall.function,
      parsed_arguments: isAutoParsableTool(inputTool) ? inputTool.$parseRaw(toolCall.function.arguments) : inputTool?.function.strict ? JSON.parse(toolCall.function.arguments) : null
    }
  };
}
function shouldParseToolCall(params, toolCall) {
  if (!params || !("tools" in params) || !params.tools)
    return !1;
  let inputTool = params.tools?.find((inputTool2) => isChatCompletionFunctionTool(inputTool2) && inputTool2.function?.name === toolCall.function.name);
  return isChatCompletionFunctionTool(inputTool) && (isAutoParsableTool(inputTool) || inputTool?.function.strict || !1);
}
function hasAutoParseableInput(params) {
  return isAutoParsableResponseFormat(params.response_format) ? !0 : params.tools?.some((t) => isAutoParsableTool(t) || t.type === "function" && t.function.strict === !0) ?? !1;
}
function assertToolCallsAreChatCompletionFunctionToolCalls(toolCalls) {
  for (let toolCall of toolCalls || [])
    if (toolCall.type !== "function")
      throw new OpenAIError(`Currently only \`function\` tool calls are supported; Received \`${toolCall.type}\``);
}
function validateInputTools(tools) {
  for (let tool of tools ?? []) {
    if (tool.type !== "function")
      throw new OpenAIError(`Currently only \`function\` tool types support auto-parsing; Received \`${tool.type}\``);
    if (tool.function.strict !== !0)
      throw new OpenAIError(`The \`${tool.function.name}\` tool is not marked with \`strict: true\`. Only strict function tools can be auto-parsed`);
  }
}
var init_parser3 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/lib/parser.mjs"() {
    init_error4();
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/lib/chatCompletionUtils.mjs
var isAssistantMessage, isToolMessage, init_chatCompletionUtils = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/lib/chatCompletionUtils.mjs"() {
    isAssistantMessage = (message) => message?.role === "assistant", isToolMessage = (message) => message?.role === "tool";
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/lib/EventStream.mjs
var _EventStream_instances, _EventStream_connectedPromise, _EventStream_resolveConnectedPromise, _EventStream_rejectConnectedPromise, _EventStream_endPromise, _EventStream_resolveEndPromise, _EventStream_rejectEndPromise, _EventStream_listeners, _EventStream_abortListeners, _EventStream_ended, _EventStream_errored, _EventStream_aborted, _EventStream_catchingPromiseCreated, _EventStream_removeAbortListeners, _EventStream_handleError, EventStream, init_EventStream = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/lib/EventStream.mjs"() {
    init_tslib2();
    init_error4();
    EventStream = class {
      constructor() {
        _EventStream_instances.add(this), this.controller = new AbortController(), _EventStream_connectedPromise.set(this, void 0), _EventStream_resolveConnectedPromise.set(this, () => {
        }), _EventStream_rejectConnectedPromise.set(this, () => {
        }), _EventStream_endPromise.set(this, void 0), _EventStream_resolveEndPromise.set(this, () => {
        }), _EventStream_rejectEndPromise.set(this, () => {
        }), _EventStream_listeners.set(this, {}), _EventStream_abortListeners.set(this, []), _EventStream_ended.set(this, !1), _EventStream_errored.set(this, !1), _EventStream_aborted.set(this, !1), _EventStream_catchingPromiseCreated.set(this, !1), __classPrivateFieldSet2(this, _EventStream_connectedPromise, new Promise((resolve4, reject) => {
          __classPrivateFieldSet2(this, _EventStream_resolveConnectedPromise, resolve4, "f"), __classPrivateFieldSet2(this, _EventStream_rejectConnectedPromise, reject, "f");
        }), "f"), __classPrivateFieldSet2(this, _EventStream_endPromise, new Promise((resolve4, reject) => {
          __classPrivateFieldSet2(this, _EventStream_resolveEndPromise, resolve4, "f"), __classPrivateFieldSet2(this, _EventStream_rejectEndPromise, reject, "f");
        }), "f"), __classPrivateFieldGet2(this, _EventStream_connectedPromise, "f").catch(() => {
        }), __classPrivateFieldGet2(this, _EventStream_endPromise, "f").catch(() => {
        });
      }
      _run(executor) {
        setTimeout(() => {
          executor().then(() => {
            this._emitFinal(), this._emit("end");
          }, __classPrivateFieldGet2(this, _EventStream_instances, "m", _EventStream_handleError).bind(this));
        }, 0);
      }
      _connected() {
        this.ended || (__classPrivateFieldGet2(this, _EventStream_resolveConnectedPromise, "f").call(this), this._emit("connect"));
      }
      get ended() {
        return __classPrivateFieldGet2(this, _EventStream_ended, "f");
      }
      get errored() {
        return __classPrivateFieldGet2(this, _EventStream_errored, "f");
      }
      get aborted() {
        return __classPrivateFieldGet2(this, _EventStream_aborted, "f");
      }
      abort() {
        this.controller.abort();
      }
      _listenForAbort(signal) {
        if (!signal || this.ended)
          return;
        if (signal.aborted) {
          this.controller.abort();
          return;
        }
        let listener = () => this.controller.abort();
        signal.addEventListener("abort", listener, { once: !0 }), __classPrivateFieldGet2(this, _EventStream_abortListeners, "f").push({ signal, listener });
      }
      /**
       * Adds the listener function to the end of the listeners array for the event.
       * No checks are made to see if the listener has already been added. Multiple calls passing
       * the same combination of event and listener will result in the listener being added, and
       * called, multiple times.
       * @returns this ChatCompletionStream, so that calls can be chained
       */
      on(event, listener) {
        return (__classPrivateFieldGet2(this, _EventStream_listeners, "f")[event] || (__classPrivateFieldGet2(this, _EventStream_listeners, "f")[event] = [])).push({ listener }), this;
      }
      /**
       * Removes the specified listener from the listener array for the event.
       * off() will remove, at most, one instance of a listener from the listener array. If any single
       * listener has been added multiple times to the listener array for the specified event, then
       * off() must be called multiple times to remove each instance.
       * @returns this ChatCompletionStream, so that calls can be chained
       */
      off(event, listener) {
        let listeners = __classPrivateFieldGet2(this, _EventStream_listeners, "f")[event];
        if (!listeners)
          return this;
        let index = listeners.findIndex((l) => l.listener === listener);
        return index >= 0 && listeners.splice(index, 1), this;
      }
      /**
       * Adds a one-time listener function for the event. The next time the event is triggered,
       * this listener is removed and then invoked.
       * @returns this ChatCompletionStream, so that calls can be chained
       */
      once(event, listener) {
        return (__classPrivateFieldGet2(this, _EventStream_listeners, "f")[event] || (__classPrivateFieldGet2(this, _EventStream_listeners, "f")[event] = [])).push({ listener, once: !0 }), this;
      }
      /**
       * This is similar to `.once()`, but returns a Promise that resolves the next time
       * the event is triggered, instead of calling a listener callback.
       * @returns a Promise that resolves the next time given event is triggered,
       * or rejects if an error is emitted.  (If you request the 'error' event,
       * returns a promise that resolves with the error).
       *
       * Example:
       *
       *   const message = await stream.emitted('message') // rejects if the stream errors
       */
      emitted(event) {
        return new Promise((resolve4, reject) => {
          __classPrivateFieldSet2(this, _EventStream_catchingPromiseCreated, !0, "f"), event !== "error" && this.once("error", reject), this.once(event, resolve4);
        });
      }
      async done() {
        __classPrivateFieldSet2(this, _EventStream_catchingPromiseCreated, !0, "f"), await __classPrivateFieldGet2(this, _EventStream_endPromise, "f");
      }
      _emit(event, ...args) {
        if (__classPrivateFieldGet2(this, _EventStream_ended, "f"))
          return;
        event === "end" && (__classPrivateFieldGet2(this, _EventStream_instances, "m", _EventStream_removeAbortListeners).call(this), __classPrivateFieldSet2(this, _EventStream_ended, !0, "f"), __classPrivateFieldGet2(this, _EventStream_resolveEndPromise, "f").call(this));
        let listeners = __classPrivateFieldGet2(this, _EventStream_listeners, "f")[event];
        if (listeners && (__classPrivateFieldGet2(this, _EventStream_listeners, "f")[event] = listeners.filter((l) => !l.once), listeners.forEach(({ listener }) => listener(...args))), event === "abort") {
          let error = args[0];
          !__classPrivateFieldGet2(this, _EventStream_catchingPromiseCreated, "f") && !listeners?.length && Promise.reject(error), __classPrivateFieldGet2(this, _EventStream_rejectConnectedPromise, "f").call(this, error), __classPrivateFieldGet2(this, _EventStream_rejectEndPromise, "f").call(this, error), this._emit("end");
          return;
        }
        if (event === "error") {
          let error = args[0];
          !__classPrivateFieldGet2(this, _EventStream_catchingPromiseCreated, "f") && !listeners?.length && Promise.reject(error), __classPrivateFieldGet2(this, _EventStream_rejectConnectedPromise, "f").call(this, error), __classPrivateFieldGet2(this, _EventStream_rejectEndPromise, "f").call(this, error), this._emit("end");
        }
      }
      _emitFinal() {
      }
    };
    _EventStream_connectedPromise = /* @__PURE__ */ new WeakMap(), _EventStream_resolveConnectedPromise = /* @__PURE__ */ new WeakMap(), _EventStream_rejectConnectedPromise = /* @__PURE__ */ new WeakMap(), _EventStream_endPromise = /* @__PURE__ */ new WeakMap(), _EventStream_resolveEndPromise = /* @__PURE__ */ new WeakMap(), _EventStream_rejectEndPromise = /* @__PURE__ */ new WeakMap(), _EventStream_listeners = /* @__PURE__ */ new WeakMap(), _EventStream_abortListeners = /* @__PURE__ */ new WeakMap(), _EventStream_ended = /* @__PURE__ */ new WeakMap(), _EventStream_errored = /* @__PURE__ */ new WeakMap(), _EventStream_aborted = /* @__PURE__ */ new WeakMap(), _EventStream_catchingPromiseCreated = /* @__PURE__ */ new WeakMap(), _EventStream_instances = /* @__PURE__ */ new WeakSet(), _EventStream_removeAbortListeners = function() {
      for (let { signal, listener } of __classPrivateFieldGet2(this, _EventStream_abortListeners, "f").splice(0))
        signal.removeEventListener("abort", listener);
    }, _EventStream_handleError = function(error) {
      if (__classPrivateFieldSet2(this, _EventStream_errored, !0, "f"), error instanceof Error && error.name === "AbortError" && (error = new APIUserAbortError2()), error instanceof APIUserAbortError2)
        return __classPrivateFieldSet2(this, _EventStream_aborted, !0, "f"), this._emit("abort", error);
      if (error instanceof OpenAIError)
        return this._emit("error", error);
      if (error instanceof Error) {
        let openAIError = new OpenAIError(error.message);
        return openAIError.cause = error, this._emit("error", openAIError);
      }
      return this._emit("error", new OpenAIError(String(error)));
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/lib/RunnableFunction.mjs
function isRunnableFunctionWithParse(fn) {
  return typeof fn.parse == "function";
}
var init_RunnableFunction = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/lib/RunnableFunction.mjs"() {
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/lib/AbstractChatCompletionRunner.mjs
var _AbstractChatCompletionRunner_instances, _AbstractChatCompletionRunner_getFinalContent, _AbstractChatCompletionRunner_getFinalMessage, _AbstractChatCompletionRunner_getFinalFunctionToolCall, _AbstractChatCompletionRunner_getFinalFunctionToolCallResult, _AbstractChatCompletionRunner_calculateTotalUsage, _AbstractChatCompletionRunner_validateParams, _AbstractChatCompletionRunner_stringifyFunctionCallResult, DEFAULT_MAX_CHAT_COMPLETIONS, AbstractChatCompletionRunner, init_AbstractChatCompletionRunner = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/lib/AbstractChatCompletionRunner.mjs"() {
    init_tslib2();
    init_error4();
    init_parser3();
    init_chatCompletionUtils();
    init_EventStream();
    init_RunnableFunction();
    DEFAULT_MAX_CHAT_COMPLETIONS = 10, AbstractChatCompletionRunner = class extends EventStream {
      constructor() {
        super(...arguments), _AbstractChatCompletionRunner_instances.add(this), this._chatCompletions = [], this.messages = [];
      }
      _addChatCompletion(chatCompletion) {
        this._chatCompletions.push(chatCompletion), this._emit("chatCompletion", chatCompletion);
        let message = chatCompletion.choices[0]?.message;
        return message && this._addMessage(message), chatCompletion;
      }
      _addMessage(message, emit = !0) {
        if ("content" in message || (message.content = null), this.messages.push(message), emit) {
          if (this._emit("message", message), isToolMessage(message) && message.content)
            this._emit("functionToolCallResult", message.content);
          else if (isAssistantMessage(message) && message.tool_calls)
            for (let tool_call of message.tool_calls)
              tool_call.type === "function" && this._emit("functionToolCall", tool_call.function);
        }
      }
      /**
       * @returns a promise that resolves with the final ChatCompletion, or rejects
       * if an error occurred or the stream ended prematurely without producing a ChatCompletion.
       */
      async finalChatCompletion() {
        await this.done();
        let completion = this._chatCompletions[this._chatCompletions.length - 1];
        if (!completion)
          throw new OpenAIError("stream ended without producing a ChatCompletion");
        return completion;
      }
      /**
       * @returns a promise that resolves with the content of the final ChatCompletionMessage, or rejects
       * if an error occurred or the stream ended prematurely without producing a ChatCompletionMessage.
       */
      async finalContent() {
        return await this.done(), __classPrivateFieldGet2(this, _AbstractChatCompletionRunner_instances, "m", _AbstractChatCompletionRunner_getFinalContent).call(this);
      }
      /**
       * @returns a promise that resolves with the final assistant ChatCompletionMessage response,
       * or rejects if an error occurred or the stream ended prematurely without producing a ChatCompletionMessage.
       */
      async finalMessage() {
        return await this.done(), __classPrivateFieldGet2(this, _AbstractChatCompletionRunner_instances, "m", _AbstractChatCompletionRunner_getFinalMessage).call(this);
      }
      /**
       * @returns a promise that resolves with the content of the final FunctionCall, or rejects
       * if an error occurred or the stream ended prematurely without producing a ChatCompletionMessage.
       */
      async finalFunctionToolCall() {
        return await this.done(), __classPrivateFieldGet2(this, _AbstractChatCompletionRunner_instances, "m", _AbstractChatCompletionRunner_getFinalFunctionToolCall).call(this);
      }
      async finalFunctionToolCallResult() {
        return await this.done(), __classPrivateFieldGet2(this, _AbstractChatCompletionRunner_instances, "m", _AbstractChatCompletionRunner_getFinalFunctionToolCallResult).call(this);
      }
      async totalUsage() {
        return await this.done(), __classPrivateFieldGet2(this, _AbstractChatCompletionRunner_instances, "m", _AbstractChatCompletionRunner_calculateTotalUsage).call(this);
      }
      allChatCompletions() {
        return [...this._chatCompletions];
      }
      _emitFinal() {
        let completion = this._chatCompletions[this._chatCompletions.length - 1];
        completion && this._emit("finalChatCompletion", completion);
        let finalMessage = __classPrivateFieldGet2(this, _AbstractChatCompletionRunner_instances, "m", _AbstractChatCompletionRunner_getFinalMessage).call(this);
        finalMessage && this._emit("finalMessage", finalMessage);
        let finalContent = __classPrivateFieldGet2(this, _AbstractChatCompletionRunner_instances, "m", _AbstractChatCompletionRunner_getFinalContent).call(this);
        finalContent && this._emit("finalContent", finalContent);
        let finalFunctionCall = __classPrivateFieldGet2(this, _AbstractChatCompletionRunner_instances, "m", _AbstractChatCompletionRunner_getFinalFunctionToolCall).call(this);
        finalFunctionCall && this._emit("finalFunctionToolCall", finalFunctionCall);
        let finalFunctionCallResult = __classPrivateFieldGet2(this, _AbstractChatCompletionRunner_instances, "m", _AbstractChatCompletionRunner_getFinalFunctionToolCallResult).call(this);
        finalFunctionCallResult != null && this._emit("finalFunctionToolCallResult", finalFunctionCallResult), this._chatCompletions.some((c) => c.usage) && this._emit("totalUsage", __classPrivateFieldGet2(this, _AbstractChatCompletionRunner_instances, "m", _AbstractChatCompletionRunner_calculateTotalUsage).call(this));
      }
      async _createChatCompletion(client, params, options) {
        this._listenForAbort(options?.signal), __classPrivateFieldGet2(this, _AbstractChatCompletionRunner_instances, "m", _AbstractChatCompletionRunner_validateParams).call(this, params);
        let chatCompletion = await client.chat.completions.create({ ...params, stream: !1 }, { ...options, signal: this.controller.signal });
        return this._connected(), this._addChatCompletion(parseChatCompletion(chatCompletion, params));
      }
      async _runChatCompletion(client, params, options) {
        for (let message of params.messages)
          this._addMessage(message, !1);
        return await this._createChatCompletion(client, params, options);
      }
      async _runTools(client, params, runner, options) {
        let role = "tool", { tool_choice = "auto", stream, ...restParams } = params, singleFunctionToCall = typeof tool_choice != "string" && tool_choice.type === "function" && tool_choice?.function?.name, { maxChatCompletions = DEFAULT_MAX_CHAT_COMPLETIONS, afterCompletion } = options || {}, inputTools = params.tools.map((tool) => {
          if (isAutoParsableTool(tool)) {
            if (!tool.$callback)
              throw new OpenAIError("Tool given to `.runTools()` that does not have an associated function");
            return {
              type: "function",
              function: {
                function: tool.$callback,
                name: tool.function.name,
                description: tool.function.description || "",
                parameters: tool.function.parameters,
                parse: tool.$parseRaw,
                strict: !0
              }
            };
          }
          return tool;
        }), functionsByName = {};
        for (let f of inputTools)
          f.type === "function" && (functionsByName[f.function.name || f.function.function.name] = f.function);
        let tools = "tools" in params ? inputTools.map((t) => t.type === "function" ? {
          type: "function",
          function: {
            name: t.function.name || t.function.function.name,
            parameters: t.function.parameters,
            description: t.function.description,
            strict: t.function.strict
          }
        } : t) : void 0;
        for (let message of params.messages)
          this._addMessage(message, !1);
        let runToolCall = async (toolCall) => {
          if (toolCall.type !== "function")
            return { message: void 0, functionCalled: !1 };
          let tool_call_id = toolCall.id, { name, arguments: args } = toolCall.function, fn = functionsByName[name];
          if (!fn) {
            let content2 = `Invalid tool_call: ${JSON.stringify(name)}. Available options are: ${Object.keys(functionsByName).map((name2) => JSON.stringify(name2)).join(", ")}. Please try again`;
            return { message: { role, tool_call_id, content: content2 }, functionCalled: !1 };
          }
          if (singleFunctionToCall && singleFunctionToCall !== name) {
            let content2 = `Invalid tool_call: ${JSON.stringify(name)}. ${JSON.stringify(singleFunctionToCall)} requested. Please try again`;
            return { message: { role, tool_call_id, content: content2 }, functionCalled: !1 };
          }
          let rawContent;
          if (isRunnableFunctionWithParse(fn)) {
            let parsed;
            try {
              parsed = await fn.parse(args);
            } catch (error) {
              let content2 = error instanceof Error ? error.message : String(error);
              return { message: { role, tool_call_id, content: content2 }, functionCalled: !1 };
            }
            rawContent = await fn.function(parsed, runner);
          } else
            rawContent = await fn.function(args, runner);
          let content = __classPrivateFieldGet2(this, _AbstractChatCompletionRunner_instances, "m", _AbstractChatCompletionRunner_stringifyFunctionCallResult).call(this, rawContent);
          return { message: { role, tool_call_id, content }, functionCalled: !0 };
        };
        for (let i = 0; i < maxChatCompletions; ++i) {
          let chatCompletion = await this._createChatCompletion(client, {
            ...restParams,
            tool_choice,
            tools,
            messages: [...this.messages]
          }, options), message = chatCompletion.choices[0]?.message;
          if (!message)
            throw new OpenAIError("missing message in ChatCompletion response");
          if (!message.tool_calls?.length) {
            await afterCompletion?.(chatCompletion, runner);
            return;
          }
          if (singleFunctionToCall || params.parallel_tool_calls === !1)
            for (let toolCall of message.tool_calls) {
              let result = await runToolCall(toolCall);
              if (result.message && this._addMessage(result.message), singleFunctionToCall && result.functionCalled) {
                await afterCompletion?.(chatCompletion, runner);
                return;
              }
            }
          else {
            let results = await Promise.allSettled(message.tool_calls.map(runToolCall));
            for (let result of results)
              if (result.status === "rejected")
                throw result.reason;
            for (let result of results)
              result.status === "fulfilled" && result.value.message && this._addMessage(result.value.message);
          }
          await afterCompletion?.(chatCompletion, runner);
        }
      }
    };
    _AbstractChatCompletionRunner_instances = /* @__PURE__ */ new WeakSet(), _AbstractChatCompletionRunner_getFinalContent = function() {
      return __classPrivateFieldGet2(this, _AbstractChatCompletionRunner_instances, "m", _AbstractChatCompletionRunner_getFinalMessage).call(this).content ?? null;
    }, _AbstractChatCompletionRunner_getFinalMessage = function() {
      let i = this.messages.length;
      for (; i-- > 0; ) {
        let message = this.messages[i];
        if (isAssistantMessage(message))
          return {
            ...message,
            content: message.content ?? null,
            refusal: message.refusal ?? null
          };
      }
      throw new OpenAIError("stream ended without producing a ChatCompletionMessage with role=assistant");
    }, _AbstractChatCompletionRunner_getFinalFunctionToolCall = function() {
      for (let i = this.messages.length - 1; i >= 0; i--) {
        let message = this.messages[i];
        if (isAssistantMessage(message) && message?.tool_calls?.length)
          for (let j = message.tool_calls.length - 1; j >= 0; j--) {
            let toolCall = message.tool_calls[j];
            if (toolCall?.type === "function")
              return toolCall.function;
          }
      }
    }, _AbstractChatCompletionRunner_getFinalFunctionToolCallResult = function() {
      for (let i = this.messages.length - 1; i >= 0; i--) {
        let message = this.messages[i];
        if (isToolMessage(message) && message.content != null && typeof message.content == "string" && this.messages.some((x) => x.role === "assistant" && x.tool_calls?.some((y) => y.type === "function" && y.id === message.tool_call_id)))
          return message.content;
      }
    }, _AbstractChatCompletionRunner_calculateTotalUsage = function() {
      let total = {
        completion_tokens: 0,
        prompt_tokens: 0,
        total_tokens: 0
      };
      for (let { usage } of this._chatCompletions)
        usage && (total.completion_tokens += usage.completion_tokens, total.prompt_tokens += usage.prompt_tokens, total.total_tokens += usage.total_tokens);
      return total;
    }, _AbstractChatCompletionRunner_validateParams = function(params) {
      if (params.n != null && params.n > 1)
        throw new OpenAIError("ChatCompletion convenience helpers only support n=1 at this time. To use n>1, please use chat.completions.create() directly.");
    }, _AbstractChatCompletionRunner_stringifyFunctionCallResult = function(rawContent) {
      return typeof rawContent == "string" ? rawContent : rawContent === void 0 ? "undefined" : JSON.stringify(rawContent);
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/lib/ChatCompletionRunner.mjs
var ChatCompletionRunner, init_ChatCompletionRunner = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/lib/ChatCompletionRunner.mjs"() {
    init_AbstractChatCompletionRunner();
    init_chatCompletionUtils();
    ChatCompletionRunner = class _ChatCompletionRunner extends AbstractChatCompletionRunner {
      static runTools(client, params, options) {
        let runner = new _ChatCompletionRunner(), opts = {
          ...options,
          headers: { ...options?.headers, "X-Stainless-Helper-Method": "runTools" }
        };
        return runner._run(() => runner._runTools(client, params, runner, opts)), runner;
      }
      _addMessage(message, emit = !0) {
        super._addMessage(message, emit), isAssistantMessage(message) && message.content && this._emit("content", message.content);
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/_vendor/partial-json-parser/parser.mjs
function parseJSON(jsonString, allowPartial = Allow.ALL) {
  if (typeof jsonString != "string")
    throw new TypeError(`expecting str, got ${typeof jsonString}`);
  if (!jsonString.trim())
    throw new Error(`${jsonString} is empty`);
  return _parseJSON(jsonString.trim(), allowPartial);
}
var Allow, PartialJSON, MalformedJSON, _parseJSON, partialParse2, init_parser4 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/_vendor/partial-json-parser/parser.mjs"() {
    Allow = {
      STR: 1,
      NUM: 2,
      ARR: 4,
      OBJ: 8,
      NULL: 16,
      BOOL: 32,
      NAN: 64,
      INFINITY: 128,
      MINUS_INFINITY: 256,
      INF: 384,
      SPECIAL: 496,
      ATOM: 499,
      COLLECTION: 12,
      ALL: 511
    }, PartialJSON = class extends Error {
    }, MalformedJSON = class extends Error {
    };
    _parseJSON = (jsonString, allow) => {
      let length = jsonString.length, index = 0, markPartialJSON = (msg) => {
        throw new PartialJSON(`${msg} at position ${index}`);
      }, throwMalformedError = (msg) => {
        throw new MalformedJSON(`${msg} at position ${index}`);
      }, parseAny = () => (skipBlank(), index >= length && markPartialJSON("Unexpected end of input"), jsonString[index] === '"' ? parseStr() : jsonString[index] === "{" ? parseObj() : jsonString[index] === "[" ? parseArr() : jsonString.substring(index, index + 4) === "null" || Allow.NULL & allow && length - index < 4 && "null".startsWith(jsonString.substring(index)) ? (index += 4, null) : jsonString.substring(index, index + 4) === "true" || Allow.BOOL & allow && length - index < 4 && "true".startsWith(jsonString.substring(index)) ? (index += 4, !0) : jsonString.substring(index, index + 5) === "false" || Allow.BOOL & allow && length - index < 5 && "false".startsWith(jsonString.substring(index)) ? (index += 5, !1) : jsonString.substring(index, index + 8) === "Infinity" || Allow.INFINITY & allow && length - index < 8 && "Infinity".startsWith(jsonString.substring(index)) ? (index += 8, 1 / 0) : jsonString.substring(index, index + 9) === "-Infinity" || Allow.MINUS_INFINITY & allow && 1 < length - index && length - index < 9 && "-Infinity".startsWith(jsonString.substring(index)) ? (index += 9, -1 / 0) : jsonString.substring(index, index + 3) === "NaN" || Allow.NAN & allow && length - index < 3 && "NaN".startsWith(jsonString.substring(index)) ? (index += 3, NaN) : parseNum()), parseStr = () => {
        let start = index, escape3 = !1;
        for (index++; index < length && (jsonString[index] !== '"' || escape3 && jsonString[index - 1] === "\\"); )
          escape3 = jsonString[index] === "\\" ? !escape3 : !1, index++;
        if (jsonString.charAt(index) == '"')
          try {
            return JSON.parse(jsonString.substring(start, ++index - Number(escape3)));
          } catch (e) {
            throwMalformedError(String(e));
          }
        else if (Allow.STR & allow)
          try {
            return JSON.parse(jsonString.substring(start, index - Number(escape3)) + '"');
          } catch {
            return JSON.parse(jsonString.substring(start, jsonString.lastIndexOf("\\")) + '"');
          }
        markPartialJSON("Unterminated string literal");
      }, parseObj = () => {
        index++, skipBlank();
        let obj = {};
        try {
          for (; jsonString[index] !== "}"; ) {
            if (skipBlank(), index >= length && Allow.OBJ & allow)
              return obj;
            let key = parseStr();
            skipBlank(), index++;
            try {
              let value = parseAny();
              Object.defineProperty(obj, key, { value, writable: !0, enumerable: !0, configurable: !0 });
            } catch (e) {
              if (Allow.OBJ & allow)
                return obj;
              throw e;
            }
            skipBlank(), jsonString[index] === "," && index++;
          }
        } catch {
          if (Allow.OBJ & allow)
            return obj;
          markPartialJSON("Expected '}' at end of object");
        }
        return index++, obj;
      }, parseArr = () => {
        index++;
        let arr = [];
        try {
          for (; jsonString[index] !== "]"; )
            arr.push(parseAny()), skipBlank(), jsonString[index] === "," && index++;
        } catch {
          if (Allow.ARR & allow)
            return arr;
          markPartialJSON("Expected ']' at end of array");
        }
        return index++, arr;
      }, parseNum = () => {
        if (index === 0) {
          jsonString === "-" && Allow.NUM & allow && markPartialJSON("Not sure what '-' is");
          try {
            return JSON.parse(jsonString);
          } catch (e) {
            if (Allow.NUM & allow)
              try {
                return jsonString[jsonString.length - 1] === "." ? JSON.parse(jsonString.substring(0, jsonString.lastIndexOf("."))) : JSON.parse(jsonString.substring(0, jsonString.lastIndexOf("e")));
              } catch {
              }
            throwMalformedError(String(e));
          }
        }
        let start = index;
        for (jsonString[index] === "-" && index++; jsonString[index] && !",]}".includes(jsonString[index]); )
          index++;
        index == length && !(Allow.NUM & allow) && markPartialJSON("Unterminated number literal");
        try {
          return JSON.parse(jsonString.substring(start, index));
        } catch {
          jsonString.substring(start, index) === "-" && Allow.NUM & allow && markPartialJSON("Not sure what '-' is");
          try {
            return JSON.parse(jsonString.substring(start, jsonString.lastIndexOf("e")));
          } catch (e2) {
            throwMalformedError(String(e2));
          }
        }
      }, skipBlank = () => {
        for (; index < length && ` 
\r	`.includes(jsonString[index]); )
          index++;
      };
      return parseAny();
    }, partialParse2 = (input) => parseJSON(input, Allow.ALL ^ Allow.NUM);
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/streaming.mjs
var init_streaming4 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/streaming.mjs"() {
    init_streaming3();
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/lib/ChatCompletionStream.mjs
function finalizeChatCompletion(snapshot, params) {
  let { id, choices, created, model, system_fingerprint, ...rest } = snapshot, completion = {
    ...rest,
    id,
    choices: choices.map(({ message, finish_reason, index, logprobs, ...choiceRest }) => {
      if (!finish_reason)
        throw new OpenAIError(`missing finish_reason for choice ${index}`);
      let { content = null, function_call, tool_calls, ...messageRest } = message, role = message.role;
      if (!role)
        throw new OpenAIError(`missing role for choice ${index}`);
      if (function_call) {
        let { arguments: args, name } = function_call;
        if (args == null)
          throw new OpenAIError(`missing function_call.arguments for choice ${index}`);
        if (!name)
          throw new OpenAIError(`missing function_call.name for choice ${index}`);
        return {
          ...choiceRest,
          message: {
            content,
            function_call: { arguments: args, name },
            role,
            refusal: message.refusal ?? null
          },
          finish_reason,
          index,
          logprobs
        };
      }
      return tool_calls ? {
        ...choiceRest,
        index,
        finish_reason,
        logprobs,
        message: {
          ...messageRest,
          role,
          content,
          refusal: message.refusal ?? null,
          tool_calls: tool_calls.map((tool_call, i) => {
            let { function: fn, type, id: id2, ...toolRest } = tool_call, { arguments: args, name, ...fnRest } = fn || {};
            if (id2 == null)
              throw new OpenAIError(`missing choices[${index}].tool_calls[${i}].id
${str(snapshot)}`);
            if (type == null)
              throw new OpenAIError(`missing choices[${index}].tool_calls[${i}].type
${str(snapshot)}`);
            if (name == null)
              throw new OpenAIError(`missing choices[${index}].tool_calls[${i}].function.name
${str(snapshot)}`);
            if (args == null)
              throw new OpenAIError(`missing choices[${index}].tool_calls[${i}].function.arguments
${str(snapshot)}`);
            return { ...toolRest, id: id2, type, function: { ...fnRest, name, arguments: args } };
          })
        }
      } : {
        ...choiceRest,
        message: { ...messageRest, content, role, refusal: message.refusal ?? null },
        finish_reason,
        index,
        logprobs
      };
    }),
    created,
    model,
    object: "chat.completion",
    ...system_fingerprint ? { system_fingerprint } : {}
  };
  return maybeParseChatCompletion(completion, params);
}
function str(x) {
  return JSON.stringify(x);
}
var _ChatCompletionStream_instances, _ChatCompletionStream_params, _ChatCompletionStream_choiceEventStates, _ChatCompletionStream_currentChatCompletionSnapshot, _ChatCompletionStream_beginRequest, _ChatCompletionStream_getChoiceEventState, _ChatCompletionStream_addChunk, _ChatCompletionStream_emitToolCallDoneEvent, _ChatCompletionStream_emitContentDoneEvents, _ChatCompletionStream_endRequest, _ChatCompletionStream_getAutoParseableResponseFormat, _ChatCompletionStream_accumulateChatCompletion, ChatCompletionStream, init_ChatCompletionStream = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/lib/ChatCompletionStream.mjs"() {
    init_tslib2();
    init_parser4();
    init_error4();
    init_parser3();
    init_streaming4();
    init_AbstractChatCompletionRunner();
    ChatCompletionStream = class _ChatCompletionStream extends AbstractChatCompletionRunner {
      constructor(params) {
        super(), _ChatCompletionStream_instances.add(this), _ChatCompletionStream_params.set(this, void 0), _ChatCompletionStream_choiceEventStates.set(this, void 0), _ChatCompletionStream_currentChatCompletionSnapshot.set(this, void 0), __classPrivateFieldSet2(this, _ChatCompletionStream_params, params, "f"), __classPrivateFieldSet2(this, _ChatCompletionStream_choiceEventStates, [], "f");
      }
      get currentChatCompletionSnapshot() {
        return __classPrivateFieldGet2(this, _ChatCompletionStream_currentChatCompletionSnapshot, "f");
      }
      /**
       * Intended for use on the frontend, consuming a stream produced with
       * `.toReadableStream()` on the backend.
       *
       * Note that messages sent to the model do not appear in `.on('message')`
       * in this context.
       */
      static fromReadableStream(stream) {
        let runner = new _ChatCompletionStream(null);
        return runner._run(() => runner._fromReadableStream(stream)), runner;
      }
      static createChatCompletion(client, params, options) {
        let runner = new _ChatCompletionStream(params);
        return runner._run(() => runner._runChatCompletion(client, { ...params, stream: !0 }, { ...options, headers: { ...options?.headers, "X-Stainless-Helper-Method": "stream" } })), runner;
      }
      async _createChatCompletion(client, params, options) {
        super._createChatCompletion, this._listenForAbort(options?.signal), __classPrivateFieldGet2(this, _ChatCompletionStream_instances, "m", _ChatCompletionStream_beginRequest).call(this);
        let stream = await client.chat.completions.create({ ...params, stream: !0 }, { ...options, signal: this.controller.signal });
        this._connected();
        for await (let chunk of stream)
          __classPrivateFieldGet2(this, _ChatCompletionStream_instances, "m", _ChatCompletionStream_addChunk).call(this, chunk);
        if (stream.controller.signal?.aborted)
          throw new APIUserAbortError2();
        return this._addChatCompletion(__classPrivateFieldGet2(this, _ChatCompletionStream_instances, "m", _ChatCompletionStream_endRequest).call(this));
      }
      async _fromReadableStream(readableStream, options) {
        this._listenForAbort(options?.signal), __classPrivateFieldGet2(this, _ChatCompletionStream_instances, "m", _ChatCompletionStream_beginRequest).call(this), this._connected();
        let stream = Stream2.fromReadableStream(readableStream, this.controller), chatId;
        for await (let chunk of stream)
          chatId && chatId !== chunk.id && this._addChatCompletion(__classPrivateFieldGet2(this, _ChatCompletionStream_instances, "m", _ChatCompletionStream_endRequest).call(this)), __classPrivateFieldGet2(this, _ChatCompletionStream_instances, "m", _ChatCompletionStream_addChunk).call(this, chunk), chatId = chunk.id;
        if (stream.controller.signal?.aborted)
          throw new APIUserAbortError2();
        return this._addChatCompletion(__classPrivateFieldGet2(this, _ChatCompletionStream_instances, "m", _ChatCompletionStream_endRequest).call(this));
      }
      [(_ChatCompletionStream_params = /* @__PURE__ */ new WeakMap(), _ChatCompletionStream_choiceEventStates = /* @__PURE__ */ new WeakMap(), _ChatCompletionStream_currentChatCompletionSnapshot = /* @__PURE__ */ new WeakMap(), _ChatCompletionStream_instances = /* @__PURE__ */ new WeakSet(), _ChatCompletionStream_beginRequest = function() {
        this.ended || __classPrivateFieldSet2(this, _ChatCompletionStream_currentChatCompletionSnapshot, void 0, "f");
      }, _ChatCompletionStream_getChoiceEventState = function(choice) {
        let state = __classPrivateFieldGet2(this, _ChatCompletionStream_choiceEventStates, "f")[choice.index];
        return state || (state = {
          content_done: !1,
          refusal_done: !1,
          logprobs_content_done: !1,
          logprobs_refusal_done: !1,
          done_tool_calls: /* @__PURE__ */ new Set(),
          current_tool_call_index: null
        }, __classPrivateFieldGet2(this, _ChatCompletionStream_choiceEventStates, "f")[choice.index] = state, state);
      }, _ChatCompletionStream_addChunk = function(chunk) {
        if (this.ended)
          return;
        let completion = __classPrivateFieldGet2(this, _ChatCompletionStream_instances, "m", _ChatCompletionStream_accumulateChatCompletion).call(this, chunk);
        this._emit("chunk", chunk, completion);
        for (let choice of chunk.choices) {
          let choiceSnapshot = completion.choices[choice.index];
          choice.delta.content != null && choiceSnapshot.message?.role === "assistant" && choiceSnapshot.message?.content && (this._emit("content", choice.delta.content, choiceSnapshot.message.content), this._emit("content.delta", {
            delta: choice.delta.content,
            snapshot: choiceSnapshot.message.content,
            parsed: choiceSnapshot.message.parsed
          })), choice.delta.refusal != null && choiceSnapshot.message?.role === "assistant" && choiceSnapshot.message?.refusal && this._emit("refusal.delta", {
            delta: choice.delta.refusal,
            snapshot: choiceSnapshot.message.refusal
          }), choice.logprobs?.content != null && choiceSnapshot.message?.role === "assistant" && this._emit("logprobs.content.delta", {
            content: choice.logprobs?.content,
            snapshot: choiceSnapshot.logprobs?.content ?? []
          }), choice.logprobs?.refusal != null && choiceSnapshot.message?.role === "assistant" && this._emit("logprobs.refusal.delta", {
            refusal: choice.logprobs?.refusal,
            snapshot: choiceSnapshot.logprobs?.refusal ?? []
          });
          let state = __classPrivateFieldGet2(this, _ChatCompletionStream_instances, "m", _ChatCompletionStream_getChoiceEventState).call(this, choiceSnapshot);
          choiceSnapshot.finish_reason && (__classPrivateFieldGet2(this, _ChatCompletionStream_instances, "m", _ChatCompletionStream_emitContentDoneEvents).call(this, choiceSnapshot), state.current_tool_call_index != null && __classPrivateFieldGet2(this, _ChatCompletionStream_instances, "m", _ChatCompletionStream_emitToolCallDoneEvent).call(this, choiceSnapshot, state.current_tool_call_index));
          for (let toolCall of choice.delta.tool_calls ?? [])
            state.current_tool_call_index !== toolCall.index && (__classPrivateFieldGet2(this, _ChatCompletionStream_instances, "m", _ChatCompletionStream_emitContentDoneEvents).call(this, choiceSnapshot), state.current_tool_call_index != null && __classPrivateFieldGet2(this, _ChatCompletionStream_instances, "m", _ChatCompletionStream_emitToolCallDoneEvent).call(this, choiceSnapshot, state.current_tool_call_index)), state.current_tool_call_index = toolCall.index;
          for (let toolCallDelta of choice.delta.tool_calls ?? []) {
            let toolCallSnapshot = choiceSnapshot.message.tool_calls?.[toolCallDelta.index];
            toolCallSnapshot?.type && (toolCallSnapshot?.type === "function" ? this._emit("tool_calls.function.arguments.delta", {
              name: toolCallSnapshot.function?.name,
              index: toolCallDelta.index,
              arguments: toolCallSnapshot.function.arguments,
              parsed_arguments: toolCallSnapshot.function.parsed_arguments,
              arguments_delta: toolCallDelta.function?.arguments ?? ""
            }) : (toolCallSnapshot?.type, void 0));
          }
        }
      }, _ChatCompletionStream_emitToolCallDoneEvent = function(choiceSnapshot, toolCallIndex) {
        if (__classPrivateFieldGet2(this, _ChatCompletionStream_instances, "m", _ChatCompletionStream_getChoiceEventState).call(this, choiceSnapshot).done_tool_calls.has(toolCallIndex))
          return;
        let toolCallSnapshot = choiceSnapshot.message.tool_calls?.[toolCallIndex];
        if (!toolCallSnapshot)
          throw new Error("no tool call snapshot");
        if (!toolCallSnapshot.type)
          throw new Error("tool call snapshot missing `type`");
        if (toolCallSnapshot.type === "function") {
          let inputTool = __classPrivateFieldGet2(this, _ChatCompletionStream_params, "f")?.tools?.find((tool) => isChatCompletionFunctionTool(tool) && tool.function.name === toolCallSnapshot.function.name);
          this._emit("tool_calls.function.arguments.done", {
            name: toolCallSnapshot.function.name,
            index: toolCallIndex,
            arguments: toolCallSnapshot.function.arguments,
            parsed_arguments: isAutoParsableTool(inputTool) ? inputTool.$parseRaw(toolCallSnapshot.function.arguments) : inputTool?.function.strict ? JSON.parse(toolCallSnapshot.function.arguments) : null
          });
        } else
          toolCallSnapshot.type;
      }, _ChatCompletionStream_emitContentDoneEvents = function(choiceSnapshot) {
        let state = __classPrivateFieldGet2(this, _ChatCompletionStream_instances, "m", _ChatCompletionStream_getChoiceEventState).call(this, choiceSnapshot);
        if (choiceSnapshot.message.content && !state.content_done) {
          state.content_done = !0;
          let responseFormat = __classPrivateFieldGet2(this, _ChatCompletionStream_instances, "m", _ChatCompletionStream_getAutoParseableResponseFormat).call(this);
          this._emit("content.done", {
            content: choiceSnapshot.message.content,
            parsed: responseFormat ? responseFormat.$parseRaw(choiceSnapshot.message.content) : null
          });
        }
        choiceSnapshot.message.refusal && !state.refusal_done && (state.refusal_done = !0, this._emit("refusal.done", { refusal: choiceSnapshot.message.refusal })), choiceSnapshot.logprobs?.content && !state.logprobs_content_done && (state.logprobs_content_done = !0, this._emit("logprobs.content.done", { content: choiceSnapshot.logprobs.content })), choiceSnapshot.logprobs?.refusal && !state.logprobs_refusal_done && (state.logprobs_refusal_done = !0, this._emit("logprobs.refusal.done", { refusal: choiceSnapshot.logprobs.refusal }));
      }, _ChatCompletionStream_endRequest = function() {
        if (this.ended)
          throw new OpenAIError("stream has ended, this shouldn't happen");
        let snapshot = __classPrivateFieldGet2(this, _ChatCompletionStream_currentChatCompletionSnapshot, "f");
        if (!snapshot)
          throw new OpenAIError("request ended without sending any chunks");
        return __classPrivateFieldSet2(this, _ChatCompletionStream_currentChatCompletionSnapshot, void 0, "f"), __classPrivateFieldSet2(this, _ChatCompletionStream_choiceEventStates, [], "f"), finalizeChatCompletion(snapshot, __classPrivateFieldGet2(this, _ChatCompletionStream_params, "f"));
      }, _ChatCompletionStream_getAutoParseableResponseFormat = function() {
        let responseFormat = __classPrivateFieldGet2(this, _ChatCompletionStream_params, "f")?.response_format;
        return isAutoParsableResponseFormat(responseFormat) ? responseFormat : null;
      }, _ChatCompletionStream_accumulateChatCompletion = function(chunk) {
        var _a5, _b, _c, _d;
        let snapshot = __classPrivateFieldGet2(this, _ChatCompletionStream_currentChatCompletionSnapshot, "f"), { choices, ...rest } = chunk;
        snapshot ? Object.assign(snapshot, rest) : snapshot = __classPrivateFieldSet2(this, _ChatCompletionStream_currentChatCompletionSnapshot, {
          ...rest,
          choices: []
        }, "f");
        for (let { delta, finish_reason, index, logprobs = null, ...other } of chunk.choices) {
          let choice = snapshot.choices[index];
          if (choice || (choice = snapshot.choices[index] = { finish_reason, index, message: {}, logprobs, ...other }), logprobs)
            if (!choice.logprobs)
              choice.logprobs = Object.assign({}, logprobs);
            else {
              let { content: content2, refusal: refusal2, ...rest3 } = logprobs;
              Object.assign(choice.logprobs, rest3), content2 && ((_a5 = choice.logprobs).content ?? (_a5.content = []), choice.logprobs.content.push(...content2)), refusal2 && ((_b = choice.logprobs).refusal ?? (_b.refusal = []), choice.logprobs.refusal.push(...refusal2));
            }
          if (finish_reason && (choice.finish_reason = finish_reason, __classPrivateFieldGet2(this, _ChatCompletionStream_params, "f") && hasAutoParseableInput(__classPrivateFieldGet2(this, _ChatCompletionStream_params, "f")))) {
            if (finish_reason === "length")
              throw new LengthFinishReasonError();
            if (finish_reason === "content_filter")
              throw new ContentFilterFinishReasonError();
          }
          if (Object.assign(choice, other), !delta)
            continue;
          let { content, refusal, function_call, role, tool_calls, ...rest2 } = delta;
          if (Object.assign(choice.message, rest2), refusal && (choice.message.refusal = (choice.message.refusal || "") + refusal), role && (choice.message.role = role), function_call && (choice.message.function_call ? (function_call.name && (choice.message.function_call.name = function_call.name), function_call.arguments && ((_c = choice.message.function_call).arguments ?? (_c.arguments = ""), choice.message.function_call.arguments += function_call.arguments)) : choice.message.function_call = function_call), content && (choice.message.content = (choice.message.content || "") + content, !choice.message.refusal && __classPrivateFieldGet2(this, _ChatCompletionStream_instances, "m", _ChatCompletionStream_getAutoParseableResponseFormat).call(this) && (choice.message.parsed = choice.message.content.trim() ? partialParse2(choice.message.content) : null)), tool_calls) {
            choice.message.tool_calls || (choice.message.tool_calls = []);
            for (let { index: index2, id, type, function: fn, ...rest3 } of tool_calls) {
              let tool_call = (_d = choice.message.tool_calls)[index2] ?? (_d[index2] = {});
              Object.assign(tool_call, rest3), id && (tool_call.id = id), type && (tool_call.type = type), fn && (tool_call.function ?? (tool_call.function = { name: fn.name ?? "", arguments: "" })), fn?.name && (tool_call.function.name = fn.name), fn?.arguments && (tool_call.function.arguments += fn.arguments, shouldParseToolCall(__classPrivateFieldGet2(this, _ChatCompletionStream_params, "f"), tool_call) && (tool_call.function.parsed_arguments = partialParse2(tool_call.function.arguments)));
            }
          }
        }
        return snapshot;
      }, Symbol.asyncIterator)]() {
        let pushQueue = [], readQueue = [], done = !1;
        return this.on("chunk", (chunk) => {
          let reader = readQueue.shift();
          reader ? reader.resolve(chunk) : pushQueue.push(chunk);
        }), this.on("end", () => {
          done = !0;
          for (let reader of readQueue)
            reader.resolve(void 0);
          readQueue.length = 0;
        }), this.on("abort", (err) => {
          done = !0;
          for (let reader of readQueue)
            reader.reject(err);
          readQueue.length = 0;
        }), this.on("error", (err) => {
          done = !0;
          for (let reader of readQueue)
            reader.reject(err);
          readQueue.length = 0;
        }), {
          next: async () => pushQueue.length ? { value: pushQueue.shift(), done: !1 } : done ? { value: void 0, done: !0 } : new Promise((resolve4, reject) => readQueue.push({ resolve: resolve4, reject })).then((chunk2) => chunk2 ? { value: chunk2, done: !1 } : { value: void 0, done: !0 }),
          return: async () => (this.abort(), { value: void 0, done: !0 })
        };
      }
      toReadableStream() {
        return new Stream2(this[Symbol.asyncIterator].bind(this), this.controller).toReadableStream();
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/lib/ChatCompletionStreamingRunner.mjs
var ChatCompletionStreamingRunner, init_ChatCompletionStreamingRunner = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/lib/ChatCompletionStreamingRunner.mjs"() {
    init_ChatCompletionStream();
    ChatCompletionStreamingRunner = class _ChatCompletionStreamingRunner extends ChatCompletionStream {
      static fromReadableStream(stream) {
        let runner = new _ChatCompletionStreamingRunner(null);
        return runner._run(() => runner._fromReadableStream(stream)), runner;
      }
      static runTools(client, params, options) {
        let runner = new _ChatCompletionStreamingRunner(
          // @ts-expect-error TODO these types are incompatible
          params
        ), opts = {
          ...options,
          headers: { ...options?.headers, "X-Stainless-Helper-Method": "runTools" }
        };
        return runner._run(() => runner._runTools(client, params, runner, opts)), runner;
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/chat/completions/completions.mjs
var Completions2, init_completions2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/chat/completions/completions.mjs"() {
    init_resource2();
    init_messages3();
    init_messages3();
    init_pagination2();
    init_path2();
    init_ChatCompletionRunner();
    init_ChatCompletionStreamingRunner();
    init_ChatCompletionStream();
    init_parser3();
    init_ChatCompletionStreamingRunner();
    init_RunnableFunction();
    init_ChatCompletionStream();
    init_ChatCompletionRunner();
    Completions2 = class extends APIResource2 {
      constructor() {
        super(...arguments), this.messages = new Messages3(this._client);
      }
      create(body, options) {
        return this._client.post("/chat/completions", {
          body,
          ...options,
          stream: body.stream ?? !1,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Get a stored chat completion. Only Chat Completions that have been created with
       * the `store` parameter set to `true` will be returned.
       *
       * @example
       * ```ts
       * const chatCompletion =
       *   await client.chat.completions.retrieve('completion_id');
       * ```
       */
      retrieve(completionID, options) {
        return this._client.get(path7`/chat/completions/${completionID}`, {
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Modify a stored chat completion. Only Chat Completions that have been created
       * with the `store` parameter set to `true` can be modified. Currently, the only
       * supported modification is to update the `metadata` field.
       *
       * @example
       * ```ts
       * const chatCompletion = await client.chat.completions.update(
       *   'completion_id',
       *   { metadata: { foo: 'string' } },
       * );
       * ```
       */
      update(completionID, body, options) {
        return this._client.post(path7`/chat/completions/${completionID}`, {
          body,
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * List stored Chat Completions. Only Chat Completions that have been stored with
       * the `store` parameter set to `true` will be returned.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const chatCompletion of client.chat.completions.list()) {
       *   // ...
       * }
       * ```
       */
      list(query = {}, options) {
        return this._client.getAPIList("/chat/completions", CursorPage, {
          query,
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Delete a stored chat completion. Only Chat Completions that have been created
       * with the `store` parameter set to `true` can be deleted.
       *
       * @example
       * ```ts
       * const chatCompletionDeleted =
       *   await client.chat.completions.delete('completion_id');
       * ```
       */
      delete(completionID, options) {
        return this._client.delete(path7`/chat/completions/${completionID}`, {
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      parse(body, options) {
        return validateInputTools(body.tools), this._client.chat.completions.create(body, {
          ...options,
          headers: {
            ...options?.headers,
            "X-Stainless-Helper-Method": "chat.completions.parse"
          }
        })._thenUnwrap((completion) => parseChatCompletion(completion, body));
      }
      runTools(body, options) {
        return body.stream ? ChatCompletionStreamingRunner.runTools(this._client, body, options) : ChatCompletionRunner.runTools(this._client, body, options);
      }
      /**
       * Creates a chat completion stream
       */
      stream(body, options) {
        return ChatCompletionStream.createChatCompletion(this._client, body, options);
      }
    };
    Completions2.Messages = Messages3;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/chat/chat.mjs
var Chat, init_chat = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/chat/chat.mjs"() {
    init_resource2();
    init_completions2();
    init_completions2();
    Chat = class extends APIResource2 {
      constructor() {
        super(...arguments), this.completions = new Completions2(this._client);
      }
    };
    Chat.Completions = Completions2;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/chat/completions/index.mjs
var init_completions3 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/chat/completions/index.mjs"() {
    init_completions2();
    init_completions2();
    init_messages3();
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/chat/index.mjs
var init_chat2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/chat/index.mjs"() {
    init_chat();
    init_completions3();
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/shared.mjs
var init_shared2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/shared.mjs"() {
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/admin-api-keys.mjs
var AdminAPIKeys, init_admin_api_keys = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/admin-api-keys.mjs"() {
    init_resource2();
    init_pagination2();
    init_path2();
    AdminAPIKeys = class extends APIResource2 {
      /**
       * Create an organization admin API key
       *
       * @example
       * ```ts
       * const adminAPIKey =
       *   await client.admin.organization.adminAPIKeys.create({
       *     name: 'New Admin Key',
       *   });
       * ```
       */
      create(body, options) {
        return this._client.post("/organization/admin_api_keys", {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Retrieve a single organization API key
       *
       * @example
       * ```ts
       * const adminAPIKey =
       *   await client.admin.organization.adminAPIKeys.retrieve(
       *     'key_id',
       *   );
       * ```
       */
      retrieve(keyID, options) {
        return this._client.get(path7`/organization/admin_api_keys/${keyID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * List organization API keys
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const adminAPIKey of client.admin.organization.adminAPIKeys.list()) {
       *   // ...
       * }
       * ```
       */
      list(query = {}, options) {
        return this._client.getAPIList("/organization/admin_api_keys", CursorPage, {
          query,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Delete an organization admin API key
       *
       * @example
       * ```ts
       * const adminAPIKey =
       *   await client.admin.organization.adminAPIKeys.delete(
       *     'key_id',
       *   );
       * ```
       */
      delete(keyID, options) {
        return this._client.delete(path7`/organization/admin_api_keys/${keyID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/audit-logs.mjs
var AuditLogs, init_audit_logs = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/audit-logs.mjs"() {
    init_resource2();
    init_pagination2();
    AuditLogs = class extends APIResource2 {
      /**
       * List user actions and configuration changes within this organization.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const auditLogListResponse of client.admin.organization.auditLogs.list()) {
       *   // ...
       * }
       * ```
       */
      list(query = {}, options) {
        return this._client.getAPIList("/organization/audit_logs", ConversationCursorPage, {
          query,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/certificates.mjs
var Certificates, init_certificates = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/certificates.mjs"() {
    init_resource2();
    init_pagination2();
    init_path2();
    Certificates = class extends APIResource2 {
      /**
       * Upload a certificate to the organization. This does **not** automatically
       * activate the certificate.
       *
       * Organizations can upload up to 50 certificates.
       *
       * @example
       * ```ts
       * const certificate =
       *   await client.admin.organization.certificates.create({
       *     certificate: 'certificate',
       *   });
       * ```
       */
      create(body, options) {
        return this._client.post("/organization/certificates", {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Get a certificate that has been uploaded to the organization.
       *
       * You can get a certificate regardless of whether it is active or not.
       *
       * @example
       * ```ts
       * const certificate =
       *   await client.admin.organization.certificates.retrieve(
       *     'certificate_id',
       *   );
       * ```
       */
      retrieve(certificateID, query = {}, options) {
        return this._client.get(path7`/organization/certificates/${certificateID}`, {
          query,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Modify a certificate. Note that only the name can be modified.
       *
       * @example
       * ```ts
       * const certificate =
       *   await client.admin.organization.certificates.update(
       *     'certificate_id',
       *   );
       * ```
       */
      update(certificateID, body, options) {
        return this._client.post(path7`/organization/certificates/${certificateID}`, {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * List uploaded certificates for this organization.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const certificateListResponse of client.admin.organization.certificates.list()) {
       *   // ...
       * }
       * ```
       */
      list(query = {}, options) {
        return this._client.getAPIList("/organization/certificates", ConversationCursorPage, { query, ...options, __security: { adminAPIKeyAuth: !0 } });
      }
      /**
       * Delete a certificate from the organization.
       *
       * The certificate must be inactive for the organization and all projects.
       *
       * @example
       * ```ts
       * const certificate =
       *   await client.admin.organization.certificates.delete(
       *     'certificate_id',
       *   );
       * ```
       */
      delete(certificateID, options) {
        return this._client.delete(path7`/organization/certificates/${certificateID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Activate certificates at the organization level.
       *
       * You can atomically and idempotently activate up to 10 certificates at a time.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const certificateActivateResponse of client.admin.organization.certificates.activate(
       *   { certificate_ids: ['cert_abc'] },
       * )) {
       *   // ...
       * }
       * ```
       */
      activate(body, options) {
        return this._client.getAPIList("/organization/certificates/activate", Page2, {
          body,
          method: "post",
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Deactivate certificates at the organization level.
       *
       * You can atomically and idempotently deactivate up to 10 certificates at a time.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const certificateDeactivateResponse of client.admin.organization.certificates.deactivate(
       *   { certificate_ids: ['cert_abc'] },
       * )) {
       *   // ...
       * }
       * ```
       */
      deactivate(body, options) {
        return this._client.getAPIList("/organization/certificates/deactivate", Page2, { body, method: "post", ...options, __security: { adminAPIKeyAuth: !0 } });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/data-retention.mjs
var DataRetention, init_data_retention = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/data-retention.mjs"() {
    init_resource2();
    DataRetention = class extends APIResource2 {
      /**
       * Retrieves organization data retention controls.
       *
       * @example
       * ```ts
       * const organizationDataRetention =
       *   await client.admin.organization.dataRetention.retrieve();
       * ```
       */
      retrieve(options) {
        return this._client.get("/organization/data_retention", {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Updates organization data retention controls.
       *
       * @example
       * ```ts
       * const organizationDataRetention =
       *   await client.admin.organization.dataRetention.update({
       *     retention_type: 'zero_data_retention',
       *   });
       * ```
       */
      update(body, options) {
        return this._client.post("/organization/data_retention", {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/invites.mjs
var Invites, init_invites = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/invites.mjs"() {
    init_resource2();
    init_pagination2();
    init_path2();
    Invites = class extends APIResource2 {
      /**
       * Create an invite for a user to the organization. The invite must be accepted by
       * the user before they have access to the organization.
       *
       * @example
       * ```ts
       * const invite =
       *   await client.admin.organization.invites.create({
       *     email: 'email',
       *     role: 'reader',
       *   });
       * ```
       */
      create(body, options) {
        return this._client.post("/organization/invites", {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Retrieves an invite.
       *
       * @example
       * ```ts
       * const invite =
       *   await client.admin.organization.invites.retrieve(
       *     'invite_id',
       *   );
       * ```
       */
      retrieve(inviteID, options) {
        return this._client.get(path7`/organization/invites/${inviteID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Returns a list of invites in the organization.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const invite of client.admin.organization.invites.list()) {
       *   // ...
       * }
       * ```
       */
      list(query = {}, options) {
        return this._client.getAPIList("/organization/invites", ConversationCursorPage, {
          query,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Delete an invite. If the invite has already been accepted, it cannot be deleted.
       *
       * @example
       * ```ts
       * const invite =
       *   await client.admin.organization.invites.delete(
       *     'invite_id',
       *   );
       * ```
       */
      delete(inviteID, options) {
        return this._client.delete(path7`/organization/invites/${inviteID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/roles.mjs
var Roles, init_roles = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/roles.mjs"() {
    init_resource2();
    init_pagination2();
    init_path2();
    Roles = class extends APIResource2 {
      /**
       * Creates a custom role for the organization.
       *
       * @example
       * ```ts
       * const role = await client.admin.organization.roles.create({
       *   permissions: ['string'],
       *   role_name: 'role_name',
       * });
       * ```
       */
      create(body, options) {
        return this._client.post("/organization/roles", {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Retrieves an organization role.
       *
       * @example
       * ```ts
       * const role = await client.admin.organization.roles.retrieve(
       *   'role_id',
       * );
       * ```
       */
      retrieve(roleID, options) {
        return this._client.get(path7`/organization/roles/${roleID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Updates an existing organization role.
       *
       * @example
       * ```ts
       * const role = await client.admin.organization.roles.update(
       *   'role_id',
       * );
       * ```
       */
      update(roleID, body, options) {
        return this._client.post(path7`/organization/roles/${roleID}`, {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Lists the roles configured for the organization.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const role of client.admin.organization.roles.list()) {
       *   // ...
       * }
       * ```
       */
      list(query = {}, options) {
        return this._client.getAPIList("/organization/roles", NextCursorPage, {
          query,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Deletes a custom role from the organization.
       *
       * @example
       * ```ts
       * const role = await client.admin.organization.roles.delete(
       *   'role_id',
       * );
       * ```
       */
      delete(roleID, options) {
        return this._client.delete(path7`/organization/roles/${roleID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/spend-alerts.mjs
var SpendAlerts, init_spend_alerts = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/spend-alerts.mjs"() {
    init_resource2();
    init_pagination2();
    init_path2();
    SpendAlerts = class extends APIResource2 {
      /**
       * Creates an organization spend alert.
       *
       * @example
       * ```ts
       * const organizationSpendAlert =
       *   await client.admin.organization.spendAlerts.create({
       *     currency: 'USD',
       *     interval: 'month',
       *     notification_channel: {
       *       recipients: ['string'],
       *       type: 'email',
       *     },
       *     threshold_amount: 0,
       *   });
       * ```
       */
      create(body, options) {
        return this._client.post("/organization/spend_alerts", {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Retrieves an organization spend alert.
       *
       * @example
       * ```ts
       * const organizationSpendAlert =
       *   await client.admin.organization.spendAlerts.retrieve(
       *     'alert_id',
       *   );
       * ```
       */
      retrieve(alertID, options) {
        return this._client.get(path7`/organization/spend_alerts/${alertID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Updates an organization spend alert.
       *
       * @example
       * ```ts
       * const organizationSpendAlert =
       *   await client.admin.organization.spendAlerts.update(
       *     'alert_id',
       *     {
       *       currency: 'USD',
       *       interval: 'month',
       *       notification_channel: {
       *         recipients: ['string'],
       *         type: 'email',
       *       },
       *       threshold_amount: 0,
       *     },
       *   );
       * ```
       */
      update(alertID, body, options) {
        return this._client.post(path7`/organization/spend_alerts/${alertID}`, {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Lists organization spend alerts.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const organizationSpendAlert of client.admin.organization.spendAlerts.list()) {
       *   // ...
       * }
       * ```
       */
      list(query = {}, options) {
        return this._client.getAPIList("/organization/spend_alerts", ConversationCursorPage, { query, ...options, __security: { adminAPIKeyAuth: !0 } });
      }
      /**
       * Deletes an organization spend alert.
       *
       * @example
       * ```ts
       * const organizationSpendAlertDeleted =
       *   await client.admin.organization.spendAlerts.delete(
       *     'alert_id',
       *   );
       * ```
       */
      delete(alertID, options) {
        return this._client.delete(path7`/organization/spend_alerts/${alertID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/usage.mjs
var Usage, init_usage = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/usage.mjs"() {
    init_resource2();
    Usage = class extends APIResource2 {
      /**
       * Get audio speeches usage details for the organization.
       *
       * @example
       * ```ts
       * const response =
       *   await client.admin.organization.usage.audioSpeeches({
       *     start_time: 0,
       *   });
       * ```
       */
      audioSpeeches(query, options) {
        return this._client.get("/organization/usage/audio_speeches", {
          query,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Get audio transcriptions usage details for the organization.
       *
       * @example
       * ```ts
       * const response =
       *   await client.admin.organization.usage.audioTranscriptions(
       *     { start_time: 0 },
       *   );
       * ```
       */
      audioTranscriptions(query, options) {
        return this._client.get("/organization/usage/audio_transcriptions", {
          query,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Get code interpreter sessions usage details for the organization.
       *
       * @example
       * ```ts
       * const response =
       *   await client.admin.organization.usage.codeInterpreterSessions(
       *     { start_time: 0 },
       *   );
       * ```
       */
      codeInterpreterSessions(query, options) {
        return this._client.get("/organization/usage/code_interpreter_sessions", {
          query,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Get completions usage details for the organization.
       *
       * @example
       * ```ts
       * const response =
       *   await client.admin.organization.usage.completions({
       *     start_time: 0,
       *   });
       * ```
       */
      completions(query, options) {
        return this._client.get("/organization/usage/completions", {
          query,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Get costs details for the organization.
       *
       * @example
       * ```ts
       * const response =
       *   await client.admin.organization.usage.costs({
       *     start_time: 0,
       *   });
       * ```
       */
      costs(query, options) {
        return this._client.get("/organization/costs", {
          query,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Get embeddings usage details for the organization.
       *
       * @example
       * ```ts
       * const response =
       *   await client.admin.organization.usage.embeddings({
       *     start_time: 0,
       *   });
       * ```
       */
      embeddings(query, options) {
        return this._client.get("/organization/usage/embeddings", {
          query,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Get file search calls usage details for the organization.
       *
       * @example
       * ```ts
       * const response =
       *   await client.admin.organization.usage.fileSearchCalls({
       *     start_time: 0,
       *   });
       * ```
       */
      fileSearchCalls(query, options) {
        return this._client.get("/organization/usage/file_search_calls", {
          query,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Get images usage details for the organization.
       *
       * @example
       * ```ts
       * const response =
       *   await client.admin.organization.usage.images({
       *     start_time: 0,
       *   });
       * ```
       */
      images(query, options) {
        return this._client.get("/organization/usage/images", {
          query,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Get moderations usage details for the organization.
       *
       * @example
       * ```ts
       * const response =
       *   await client.admin.organization.usage.moderations({
       *     start_time: 0,
       *   });
       * ```
       */
      moderations(query, options) {
        return this._client.get("/organization/usage/moderations", {
          query,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Get vector stores usage details for the organization.
       *
       * @example
       * ```ts
       * const response =
       *   await client.admin.organization.usage.vectorStores({
       *     start_time: 0,
       *   });
       * ```
       */
      vectorStores(query, options) {
        return this._client.get("/organization/usage/vector_stores", {
          query,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Get web search calls usage details for the organization.
       *
       * @example
       * ```ts
       * const response =
       *   await client.admin.organization.usage.webSearchCalls({
       *     start_time: 0,
       *   });
       * ```
       */
      webSearchCalls(query, options) {
        return this._client.get("/organization/usage/web_search_calls", {
          query,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/groups/roles.mjs
var Roles2, init_roles2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/groups/roles.mjs"() {
    init_resource2();
    init_pagination2();
    init_path2();
    Roles2 = class extends APIResource2 {
      /**
       * Assigns an organization role to a group within the organization.
       *
       * @example
       * ```ts
       * const role =
       *   await client.admin.organization.groups.roles.create(
       *     'group_id',
       *     { role_id: 'role_id' },
       *   );
       * ```
       */
      create(groupID, body, options) {
        return this._client.post(path7`/organization/groups/${groupID}/roles`, {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Retrieves an organization role assigned to a group.
       *
       * @example
       * ```ts
       * const role =
       *   await client.admin.organization.groups.roles.retrieve(
       *     'role_id',
       *     { group_id: 'group_id' },
       *   );
       * ```
       */
      retrieve(roleID, params, options) {
        let { group_id } = params;
        return this._client.get(path7`/organization/groups/${group_id}/roles/${roleID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Lists the organization roles assigned to a group within the organization.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const roleListResponse of client.admin.organization.groups.roles.list(
       *   'group_id',
       * )) {
       *   // ...
       * }
       * ```
       */
      list(groupID, query = {}, options) {
        return this._client.getAPIList(path7`/organization/groups/${groupID}/roles`, NextCursorPage, { query, ...options, __security: { adminAPIKeyAuth: !0 } });
      }
      /**
       * Unassigns an organization role from a group within the organization.
       *
       * @example
       * ```ts
       * const role =
       *   await client.admin.organization.groups.roles.delete(
       *     'role_id',
       *     { group_id: 'group_id' },
       *   );
       * ```
       */
      delete(roleID, params, options) {
        let { group_id } = params;
        return this._client.delete(path7`/organization/groups/${group_id}/roles/${roleID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/groups/users.mjs
var Users, init_users = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/groups/users.mjs"() {
    init_resource2();
    init_pagination2();
    init_path2();
    Users = class extends APIResource2 {
      /**
       * Adds a user to a group.
       *
       * @example
       * ```ts
       * const user =
       *   await client.admin.organization.groups.users.create(
       *     'group_id',
       *     { user_id: 'user_id' },
       *   );
       * ```
       */
      create(groupID, body, options) {
        return this._client.post(path7`/organization/groups/${groupID}/users`, {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Retrieves a user in a group.
       *
       * @example
       * ```ts
       * const user =
       *   await client.admin.organization.groups.users.retrieve(
       *     'user_id',
       *     { group_id: 'group_id' },
       *   );
       * ```
       */
      retrieve(userID, params, options) {
        let { group_id } = params;
        return this._client.get(path7`/organization/groups/${group_id}/users/${userID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Lists the users assigned to a group.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const organizationGroupUser of client.admin.organization.groups.users.list(
       *   'group_id',
       * )) {
       *   // ...
       * }
       * ```
       */
      list(groupID, query = {}, options) {
        return this._client.getAPIList(path7`/organization/groups/${groupID}/users`, NextCursorPage, { query, ...options, __security: { adminAPIKeyAuth: !0 } });
      }
      /**
       * Removes a user from a group.
       *
       * @example
       * ```ts
       * const user =
       *   await client.admin.organization.groups.users.delete(
       *     'user_id',
       *     { group_id: 'group_id' },
       *   );
       * ```
       */
      delete(userID, params, options) {
        let { group_id } = params;
        return this._client.delete(path7`/organization/groups/${group_id}/users/${userID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/groups/groups.mjs
var Groups, init_groups = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/groups/groups.mjs"() {
    init_resource2();
    init_roles2();
    init_roles2();
    init_users();
    init_users();
    init_pagination2();
    init_path2();
    Groups = class extends APIResource2 {
      constructor() {
        super(...arguments), this.users = new Users(this._client), this.roles = new Roles2(this._client);
      }
      /**
       * Creates a new group in the organization.
       *
       * @example
       * ```ts
       * const group = await client.admin.organization.groups.create(
       *   { name: 'x' },
       * );
       * ```
       */
      create(body, options) {
        return this._client.post("/organization/groups", {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Retrieves a group.
       *
       * @example
       * ```ts
       * const group =
       *   await client.admin.organization.groups.retrieve(
       *     'group_id',
       *   );
       * ```
       */
      retrieve(groupID, options) {
        return this._client.get(path7`/organization/groups/${groupID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Updates a group's information.
       *
       * @example
       * ```ts
       * const group = await client.admin.organization.groups.update(
       *   'group_id',
       *   { name: 'x' },
       * );
       * ```
       */
      update(groupID, body, options) {
        return this._client.post(path7`/organization/groups/${groupID}`, {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Lists all groups in the organization.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const group of client.admin.organization.groups.list()) {
       *   // ...
       * }
       * ```
       */
      list(query = {}, options) {
        return this._client.getAPIList("/organization/groups", NextCursorPage, {
          query,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Deletes a group from the organization.
       *
       * @example
       * ```ts
       * const group = await client.admin.organization.groups.delete(
       *   'group_id',
       * );
       * ```
       */
      delete(groupID, options) {
        return this._client.delete(path7`/organization/groups/${groupID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
    };
    Groups.Users = Users;
    Groups.Roles = Roles2;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/api-keys.mjs
var APIKeys, init_api_keys = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/api-keys.mjs"() {
    init_resource2();
    init_pagination2();
    init_path2();
    APIKeys = class extends APIResource2 {
      /**
       * Retrieves an API key in the project.
       *
       * @example
       * ```ts
       * const projectAPIKey =
       *   await client.admin.organization.projects.apiKeys.retrieve(
       *     'api_key_id',
       *     { project_id: 'project_id' },
       *   );
       * ```
       */
      retrieve(apiKeyID, params, options) {
        let { project_id } = params;
        return this._client.get(path7`/organization/projects/${project_id}/api_keys/${apiKeyID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Returns a list of API keys in the project.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const projectAPIKey of client.admin.organization.projects.apiKeys.list(
       *   'project_id',
       * )) {
       *   // ...
       * }
       * ```
       */
      list(projectID, query = {}, options) {
        return this._client.getAPIList(path7`/organization/projects/${projectID}/api_keys`, ConversationCursorPage, { query, ...options, __security: { adminAPIKeyAuth: !0 } });
      }
      /**
       * Deletes an API key from the project.
       *
       * Returns confirmation of the key deletion, or an error if the key belonged to a
       * service account.
       *
       * @example
       * ```ts
       * const apiKey =
       *   await client.admin.organization.projects.apiKeys.delete(
       *     'api_key_id',
       *     { project_id: 'project_id' },
       *   );
       * ```
       */
      delete(apiKeyID, params, options) {
        let { project_id } = params;
        return this._client.delete(path7`/organization/projects/${project_id}/api_keys/${apiKeyID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/certificates.mjs
var Certificates2, init_certificates2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/certificates.mjs"() {
    init_resource2();
    init_pagination2();
    init_path2();
    Certificates2 = class extends APIResource2 {
      /**
       * List certificates for this project.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const certificateListResponse of client.admin.organization.projects.certificates.list(
       *   'project_id',
       * )) {
       *   // ...
       * }
       * ```
       */
      list(projectID, query = {}, options) {
        return this._client.getAPIList(path7`/organization/projects/${projectID}/certificates`, ConversationCursorPage, { query, ...options, __security: { adminAPIKeyAuth: !0 } });
      }
      /**
       * Activate certificates at the project level.
       *
       * You can atomically and idempotently activate up to 10 certificates at a time.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const certificateActivateResponse of client.admin.organization.projects.certificates.activate(
       *   'project_id',
       *   { certificate_ids: ['cert_abc'] },
       * )) {
       *   // ...
       * }
       * ```
       */
      activate(projectID, body, options) {
        return this._client.getAPIList(path7`/organization/projects/${projectID}/certificates/activate`, Page2, { body, method: "post", ...options, __security: { adminAPIKeyAuth: !0 } });
      }
      /**
       * Deactivate certificates at the project level. You can atomically and
       * idempotently deactivate up to 10 certificates at a time.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const certificateDeactivateResponse of client.admin.organization.projects.certificates.deactivate(
       *   'project_id',
       *   { certificate_ids: ['cert_abc'] },
       * )) {
       *   // ...
       * }
       * ```
       */
      deactivate(projectID, body, options) {
        return this._client.getAPIList(path7`/organization/projects/${projectID}/certificates/deactivate`, Page2, { body, method: "post", ...options, __security: { adminAPIKeyAuth: !0 } });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/data-retention.mjs
var DataRetention2, init_data_retention2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/data-retention.mjs"() {
    init_resource2();
    init_path2();
    DataRetention2 = class extends APIResource2 {
      /**
       * Retrieves project data retention controls.
       *
       * @example
       * ```ts
       * const projectDataRetention =
       *   await client.admin.organization.projects.dataRetention.retrieve(
       *     'project_id',
       *   );
       * ```
       */
      retrieve(projectID, options) {
        return this._client.get(path7`/organization/projects/${projectID}/data_retention`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Updates project data retention controls.
       *
       * @example
       * ```ts
       * const projectDataRetention =
       *   await client.admin.organization.projects.dataRetention.update(
       *     'project_id',
       *     { retention_type: 'organization_default' },
       *   );
       * ```
       */
      update(projectID, body, options) {
        return this._client.post(path7`/organization/projects/${projectID}/data_retention`, {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/hosted-tool-permissions.mjs
var HostedToolPermissions, init_hosted_tool_permissions = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/hosted-tool-permissions.mjs"() {
    init_resource2();
    init_path2();
    HostedToolPermissions = class extends APIResource2 {
      /**
       * Returns hosted tool permissions for a project.
       *
       * @example
       * ```ts
       * const projectHostedToolPermissions =
       *   await client.admin.organization.projects.hostedToolPermissions.retrieve(
       *     'project_id',
       *   );
       * ```
       */
      retrieve(projectID, options) {
        return this._client.get(path7`/organization/projects/${projectID}/hosted_tool_permissions`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Updates hosted tool permissions for a project.
       *
       * @example
       * ```ts
       * const projectHostedToolPermissions =
       *   await client.admin.organization.projects.hostedToolPermissions.update(
       *     'project_id',
       *   );
       * ```
       */
      update(projectID, body, options) {
        return this._client.post(path7`/organization/projects/${projectID}/hosted_tool_permissions`, {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/model-permissions.mjs
var ModelPermissions, init_model_permissions = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/model-permissions.mjs"() {
    init_resource2();
    init_path2();
    ModelPermissions = class extends APIResource2 {
      /**
       * Returns model permissions for a project.
       *
       * @example
       * ```ts
       * const projectModelPermissions =
       *   await client.admin.organization.projects.modelPermissions.retrieve(
       *     'project_id',
       *   );
       * ```
       */
      retrieve(projectID, options) {
        return this._client.get(path7`/organization/projects/${projectID}/model_permissions`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Updates model permissions for a project.
       *
       * @example
       * ```ts
       * const projectModelPermissions =
       *   await client.admin.organization.projects.modelPermissions.update(
       *     'project_id',
       *     { mode: 'allow_list', model_ids: ['string'] },
       *   );
       * ```
       */
      update(projectID, body, options) {
        return this._client.post(path7`/organization/projects/${projectID}/model_permissions`, {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Deletes model permissions for a project.
       *
       * @example
       * ```ts
       * const projectModelPermissionsDeleted =
       *   await client.admin.organization.projects.modelPermissions.delete(
       *     'project_id',
       *   );
       * ```
       */
      delete(projectID, options) {
        return this._client.delete(path7`/organization/projects/${projectID}/model_permissions`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/rate-limits.mjs
var RateLimits, init_rate_limits = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/rate-limits.mjs"() {
    init_resource2();
    init_pagination2();
    init_path2();
    RateLimits = class extends APIResource2 {
      /**
       * Returns the rate limits per model for a project.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const projectRateLimit of client.admin.organization.projects.rateLimits.listRateLimits(
       *   'project_id',
       * )) {
       *   // ...
       * }
       * ```
       */
      listRateLimits(projectID, query = {}, options) {
        return this._client.getAPIList(path7`/organization/projects/${projectID}/rate_limits`, ConversationCursorPage, { query, ...options, __security: { adminAPIKeyAuth: !0 } });
      }
      /**
       * Updates a project rate limit.
       *
       * @example
       * ```ts
       * const projectRateLimit =
       *   await client.admin.organization.projects.rateLimits.updateRateLimit(
       *     'rate_limit_id',
       *     { project_id: 'project_id' },
       *   );
       * ```
       */
      updateRateLimit(rateLimitID, params, options) {
        let { project_id, ...body } = params;
        return this._client.post(path7`/organization/projects/${project_id}/rate_limits/${rateLimitID}`, {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/roles.mjs
var Roles3, init_roles3 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/roles.mjs"() {
    init_resource2();
    init_pagination2();
    init_path2();
    Roles3 = class extends APIResource2 {
      /**
       * Creates a custom role for a project.
       *
       * @example
       * ```ts
       * const role =
       *   await client.admin.organization.projects.roles.create(
       *     'project_id',
       *     { permissions: ['string'], role_name: 'role_name' },
       *   );
       * ```
       */
      create(projectID, body, options) {
        return this._client.post(path7`/projects/${projectID}/roles`, {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Retrieves a project role.
       *
       * @example
       * ```ts
       * const role =
       *   await client.admin.organization.projects.roles.retrieve(
       *     'role_id',
       *     { project_id: 'project_id' },
       *   );
       * ```
       */
      retrieve(roleID, params, options) {
        let { project_id } = params;
        return this._client.get(path7`/projects/${project_id}/roles/${roleID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Updates an existing project role.
       *
       * @example
       * ```ts
       * const role =
       *   await client.admin.organization.projects.roles.update(
       *     'role_id',
       *     { project_id: 'project_id' },
       *   );
       * ```
       */
      update(roleID, params, options) {
        let { project_id, ...body } = params;
        return this._client.post(path7`/projects/${project_id}/roles/${roleID}`, {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Lists the roles configured for a project.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const role of client.admin.organization.projects.roles.list(
       *   'project_id',
       * )) {
       *   // ...
       * }
       * ```
       */
      list(projectID, query = {}, options) {
        return this._client.getAPIList(path7`/projects/${projectID}/roles`, NextCursorPage, {
          query,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Deletes a custom role from a project.
       *
       * @example
       * ```ts
       * const role =
       *   await client.admin.organization.projects.roles.delete(
       *     'role_id',
       *     { project_id: 'project_id' },
       *   );
       * ```
       */
      delete(roleID, params, options) {
        let { project_id } = params;
        return this._client.delete(path7`/projects/${project_id}/roles/${roleID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/service-accounts.mjs
var ServiceAccounts, init_service_accounts = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/service-accounts.mjs"() {
    init_resource2();
    init_pagination2();
    init_path2();
    ServiceAccounts = class extends APIResource2 {
      /**
       * Creates a new service account in the project. This also returns an unredacted
       * API key for the service account.
       *
       * @example
       * ```ts
       * const serviceAccount =
       *   await client.admin.organization.projects.serviceAccounts.create(
       *     'project_id',
       *     { name: 'name' },
       *   );
       * ```
       */
      create(projectID, body, options) {
        return this._client.post(path7`/organization/projects/${projectID}/service_accounts`, {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Retrieves a service account in the project.
       *
       * @example
       * ```ts
       * const projectServiceAccount =
       *   await client.admin.organization.projects.serviceAccounts.retrieve(
       *     'service_account_id',
       *     { project_id: 'project_id' },
       *   );
       * ```
       */
      retrieve(serviceAccountID, params, options) {
        let { project_id } = params;
        return this._client.get(path7`/organization/projects/${project_id}/service_accounts/${serviceAccountID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Updates a service account in the project.
       *
       * @example
       * ```ts
       * const projectServiceAccount =
       *   await client.admin.organization.projects.serviceAccounts.update(
       *     'service_account_id',
       *     { project_id: 'project_id' },
       *   );
       * ```
       */
      update(serviceAccountID, params, options) {
        let { project_id, ...body } = params;
        return this._client.post(path7`/organization/projects/${project_id}/service_accounts/${serviceAccountID}`, { body, ...options, __security: { adminAPIKeyAuth: !0 } });
      }
      /**
       * Returns a list of service accounts in the project.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const projectServiceAccount of client.admin.organization.projects.serviceAccounts.list(
       *   'project_id',
       * )) {
       *   // ...
       * }
       * ```
       */
      list(projectID, query = {}, options) {
        return this._client.getAPIList(path7`/organization/projects/${projectID}/service_accounts`, ConversationCursorPage, { query, ...options, __security: { adminAPIKeyAuth: !0 } });
      }
      /**
       * Deletes a service account from the project.
       *
       * Returns confirmation of service account deletion, or an error if the project is
       * archived (archived projects have no service accounts).
       *
       * @example
       * ```ts
       * const serviceAccount =
       *   await client.admin.organization.projects.serviceAccounts.delete(
       *     'service_account_id',
       *     { project_id: 'project_id' },
       *   );
       * ```
       */
      delete(serviceAccountID, params, options) {
        let { project_id } = params;
        return this._client.delete(path7`/organization/projects/${project_id}/service_accounts/${serviceAccountID}`, { ...options, __security: { adminAPIKeyAuth: !0 } });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/spend-alerts.mjs
var SpendAlerts2, init_spend_alerts2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/spend-alerts.mjs"() {
    init_resource2();
    init_pagination2();
    init_path2();
    SpendAlerts2 = class extends APIResource2 {
      /**
       * Creates a project spend alert.
       *
       * @example
       * ```ts
       * const projectSpendAlert =
       *   await client.admin.organization.projects.spendAlerts.create(
       *     'project_id',
       *     {
       *       currency: 'USD',
       *       interval: 'month',
       *       notification_channel: {
       *         recipients: ['string'],
       *         type: 'email',
       *       },
       *       threshold_amount: 0,
       *     },
       *   );
       * ```
       */
      create(projectID, body, options) {
        return this._client.post(path7`/organization/projects/${projectID}/spend_alerts`, {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Retrieves a project spend alert.
       *
       * @example
       * ```ts
       * const projectSpendAlert =
       *   await client.admin.organization.projects.spendAlerts.retrieve(
       *     'alert_id',
       *     { project_id: 'project_id' },
       *   );
       * ```
       */
      retrieve(alertID, params, options) {
        let { project_id } = params;
        return this._client.get(path7`/organization/projects/${project_id}/spend_alerts/${alertID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Updates a project spend alert.
       *
       * @example
       * ```ts
       * const projectSpendAlert =
       *   await client.admin.organization.projects.spendAlerts.update(
       *     'alert_id',
       *     {
       *       project_id: 'project_id',
       *       currency: 'USD',
       *       interval: 'month',
       *       notification_channel: {
       *         recipients: ['string'],
       *         type: 'email',
       *       },
       *       threshold_amount: 0,
       *     },
       *   );
       * ```
       */
      update(alertID, params, options) {
        let { project_id, ...body } = params;
        return this._client.post(path7`/organization/projects/${project_id}/spend_alerts/${alertID}`, {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Lists project spend alerts.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const projectSpendAlert of client.admin.organization.projects.spendAlerts.list(
       *   'project_id',
       * )) {
       *   // ...
       * }
       * ```
       */
      list(projectID, query = {}, options) {
        return this._client.getAPIList(path7`/organization/projects/${projectID}/spend_alerts`, ConversationCursorPage, { query, ...options, __security: { adminAPIKeyAuth: !0 } });
      }
      /**
       * Deletes a project spend alert.
       *
       * @example
       * ```ts
       * const projectSpendAlertDeleted =
       *   await client.admin.organization.projects.spendAlerts.delete(
       *     'alert_id',
       *     { project_id: 'project_id' },
       *   );
       * ```
       */
      delete(alertID, params, options) {
        let { project_id } = params;
        return this._client.delete(path7`/organization/projects/${project_id}/spend_alerts/${alertID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/groups/roles.mjs
var Roles4, init_roles4 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/groups/roles.mjs"() {
    init_resource2();
    init_pagination2();
    init_path2();
    Roles4 = class extends APIResource2 {
      /**
       * Assigns a project role to a group within a project.
       *
       * @example
       * ```ts
       * const role =
       *   await client.admin.organization.projects.groups.roles.create(
       *     'group_id',
       *     { project_id: 'project_id', role_id: 'role_id' },
       *   );
       * ```
       */
      create(groupID, params, options) {
        let { project_id, ...body } = params;
        return this._client.post(path7`/projects/${project_id}/groups/${groupID}/roles`, {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Retrieves a project role assigned to a group.
       *
       * @example
       * ```ts
       * const role =
       *   await client.admin.organization.projects.groups.roles.retrieve(
       *     'role_id',
       *     { project_id: 'project_id', group_id: 'group_id' },
       *   );
       * ```
       */
      retrieve(roleID, params, options) {
        let { project_id, group_id } = params;
        return this._client.get(path7`/projects/${project_id}/groups/${group_id}/roles/${roleID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Lists the project roles assigned to a group within a project.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const roleListResponse of client.admin.organization.projects.groups.roles.list(
       *   'group_id',
       *   { project_id: 'project_id' },
       * )) {
       *   // ...
       * }
       * ```
       */
      list(groupID, params, options) {
        let { project_id, ...query } = params;
        return this._client.getAPIList(path7`/projects/${project_id}/groups/${groupID}/roles`, NextCursorPage, { query, ...options, __security: { adminAPIKeyAuth: !0 } });
      }
      /**
       * Unassigns a project role from a group within a project.
       *
       * @example
       * ```ts
       * const role =
       *   await client.admin.organization.projects.groups.roles.delete(
       *     'role_id',
       *     { project_id: 'project_id', group_id: 'group_id' },
       *   );
       * ```
       */
      delete(roleID, params, options) {
        let { project_id, group_id } = params;
        return this._client.delete(path7`/projects/${project_id}/groups/${group_id}/roles/${roleID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/groups/groups.mjs
var Groups2, init_groups2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/groups/groups.mjs"() {
    init_resource2();
    init_roles4();
    init_roles4();
    init_pagination2();
    init_path2();
    Groups2 = class extends APIResource2 {
      constructor() {
        super(...arguments), this.roles = new Roles4(this._client);
      }
      /**
       * Grants a group access to a project.
       *
       * @example
       * ```ts
       * const projectGroup =
       *   await client.admin.organization.projects.groups.create(
       *     'project_id',
       *     { group_id: 'group_id', role: 'role' },
       *   );
       * ```
       */
      create(projectID, body, options) {
        return this._client.post(path7`/organization/projects/${projectID}/groups`, {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Retrieves a project's group.
       *
       * @example
       * ```ts
       * const projectGroup =
       *   await client.admin.organization.projects.groups.retrieve(
       *     'group_id',
       *     { project_id: 'project_id' },
       *   );
       * ```
       */
      retrieve(groupID, params, options) {
        let { project_id, ...query } = params;
        return this._client.get(path7`/organization/projects/${project_id}/groups/${groupID}`, {
          query,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Lists the groups that have access to a project.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const projectGroup of client.admin.organization.projects.groups.list(
       *   'project_id',
       * )) {
       *   // ...
       * }
       * ```
       */
      list(projectID, query = {}, options) {
        return this._client.getAPIList(path7`/organization/projects/${projectID}/groups`, NextCursorPage, { query, ...options, __security: { adminAPIKeyAuth: !0 } });
      }
      /**
       * Revokes a group's access to a project.
       *
       * @example
       * ```ts
       * const group =
       *   await client.admin.organization.projects.groups.delete(
       *     'group_id',
       *     { project_id: 'project_id' },
       *   );
       * ```
       */
      delete(groupID, params, options) {
        let { project_id } = params;
        return this._client.delete(path7`/organization/projects/${project_id}/groups/${groupID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
    };
    Groups2.Roles = Roles4;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/users/roles.mjs
var Roles5, init_roles5 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/users/roles.mjs"() {
    init_resource2();
    init_pagination2();
    init_path2();
    Roles5 = class extends APIResource2 {
      /**
       * Assigns a project role to a user within a project.
       *
       * @example
       * ```ts
       * const role =
       *   await client.admin.organization.projects.users.roles.create(
       *     'user_id',
       *     { project_id: 'project_id', role_id: 'role_id' },
       *   );
       * ```
       */
      create(userID, params, options) {
        let { project_id, ...body } = params;
        return this._client.post(path7`/projects/${project_id}/users/${userID}/roles`, {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Retrieves a project role assigned to a user.
       *
       * @example
       * ```ts
       * const role =
       *   await client.admin.organization.projects.users.roles.retrieve(
       *     'role_id',
       *     { project_id: 'project_id', user_id: 'user_id' },
       *   );
       * ```
       */
      retrieve(roleID, params, options) {
        let { project_id, user_id } = params;
        return this._client.get(path7`/projects/${project_id}/users/${user_id}/roles/${roleID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Lists the project roles assigned to a user within a project.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const roleListResponse of client.admin.organization.projects.users.roles.list(
       *   'user_id',
       *   { project_id: 'project_id' },
       * )) {
       *   // ...
       * }
       * ```
       */
      list(userID, params, options) {
        let { project_id, ...query } = params;
        return this._client.getAPIList(path7`/projects/${project_id}/users/${userID}/roles`, NextCursorPage, { query, ...options, __security: { adminAPIKeyAuth: !0 } });
      }
      /**
       * Unassigns a project role from a user within a project.
       *
       * @example
       * ```ts
       * const role =
       *   await client.admin.organization.projects.users.roles.delete(
       *     'role_id',
       *     { project_id: 'project_id', user_id: 'user_id' },
       *   );
       * ```
       */
      delete(roleID, params, options) {
        let { project_id, user_id } = params;
        return this._client.delete(path7`/projects/${project_id}/users/${user_id}/roles/${roleID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/users/users.mjs
var Users2, init_users2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/users/users.mjs"() {
    init_resource2();
    init_roles5();
    init_roles5();
    init_pagination2();
    init_path2();
    Users2 = class extends APIResource2 {
      constructor() {
        super(...arguments), this.roles = new Roles5(this._client);
      }
      /**
       * Adds a user to the project. Users must already be members of the organization to
       * be added to a project.
       *
       * @example
       * ```ts
       * const projectUser =
       *   await client.admin.organization.projects.users.create(
       *     'project_id',
       *     { role: 'role' },
       *   );
       * ```
       */
      create(projectID, body, options) {
        return this._client.post(path7`/organization/projects/${projectID}/users`, {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Retrieves a user in the project.
       *
       * @example
       * ```ts
       * const projectUser =
       *   await client.admin.organization.projects.users.retrieve(
       *     'user_id',
       *     { project_id: 'project_id' },
       *   );
       * ```
       */
      retrieve(userID, params, options) {
        let { project_id } = params;
        return this._client.get(path7`/organization/projects/${project_id}/users/${userID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Modifies a user's role in the project.
       *
       * @example
       * ```ts
       * const projectUser =
       *   await client.admin.organization.projects.users.update(
       *     'user_id',
       *     { project_id: 'project_id' },
       *   );
       * ```
       */
      update(userID, params, options) {
        let { project_id, ...body } = params;
        return this._client.post(path7`/organization/projects/${project_id}/users/${userID}`, {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Returns a list of users in the project.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const projectUser of client.admin.organization.projects.users.list(
       *   'project_id',
       * )) {
       *   // ...
       * }
       * ```
       */
      list(projectID, query = {}, options) {
        return this._client.getAPIList(path7`/organization/projects/${projectID}/users`, ConversationCursorPage, { query, ...options, __security: { adminAPIKeyAuth: !0 } });
      }
      /**
       * Deletes a user from the project.
       *
       * Returns confirmation of project user deletion, or an error if the project is
       * archived (archived projects have no users).
       *
       * @example
       * ```ts
       * const user =
       *   await client.admin.organization.projects.users.delete(
       *     'user_id',
       *     { project_id: 'project_id' },
       *   );
       * ```
       */
      delete(userID, params, options) {
        let { project_id } = params;
        return this._client.delete(path7`/organization/projects/${project_id}/users/${userID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
    };
    Users2.Roles = Roles5;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/projects.mjs
var Projects, init_projects = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/projects/projects.mjs"() {
    init_resource2();
    init_api_keys();
    init_api_keys();
    init_certificates2();
    init_certificates2();
    init_data_retention2();
    init_data_retention2();
    init_hosted_tool_permissions();
    init_hosted_tool_permissions();
    init_model_permissions();
    init_model_permissions();
    init_rate_limits();
    init_rate_limits();
    init_roles3();
    init_roles3();
    init_service_accounts();
    init_service_accounts();
    init_spend_alerts2();
    init_spend_alerts2();
    init_groups2();
    init_groups2();
    init_users2();
    init_users2();
    init_pagination2();
    init_path2();
    Projects = class extends APIResource2 {
      constructor() {
        super(...arguments), this.users = new Users2(this._client), this.serviceAccounts = new ServiceAccounts(this._client), this.apiKeys = new APIKeys(this._client), this.rateLimits = new RateLimits(this._client), this.modelPermissions = new ModelPermissions(this._client), this.hostedToolPermissions = new HostedToolPermissions(this._client), this.groups = new Groups2(this._client), this.roles = new Roles3(this._client), this.dataRetention = new DataRetention2(this._client), this.spendAlerts = new SpendAlerts2(this._client), this.certificates = new Certificates2(this._client);
      }
      /**
       * Create a new project in the organization. Projects can be created and archived,
       * but cannot be deleted.
       *
       * @example
       * ```ts
       * const project =
       *   await client.admin.organization.projects.create({
       *     name: 'name',
       *   });
       * ```
       */
      create(body, options) {
        return this._client.post("/organization/projects", {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Retrieves a project.
       *
       * @example
       * ```ts
       * const project =
       *   await client.admin.organization.projects.retrieve(
       *     'project_id',
       *   );
       * ```
       */
      retrieve(projectID, options) {
        return this._client.get(path7`/organization/projects/${projectID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Modifies a project in the organization.
       *
       * @example
       * ```ts
       * const project =
       *   await client.admin.organization.projects.update(
       *     'project_id',
       *   );
       * ```
       */
      update(projectID, body, options) {
        return this._client.post(path7`/organization/projects/${projectID}`, {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Returns a list of projects.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const project of client.admin.organization.projects.list()) {
       *   // ...
       * }
       * ```
       */
      list(query = {}, options) {
        return this._client.getAPIList("/organization/projects", ConversationCursorPage, {
          query,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Archives a project in the organization. Archived projects cannot be used or
       * updated.
       *
       * @example
       * ```ts
       * const project =
       *   await client.admin.organization.projects.archive(
       *     'project_id',
       *   );
       * ```
       */
      archive(projectID, options) {
        return this._client.post(path7`/organization/projects/${projectID}/archive`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
    };
    Projects.Users = Users2;
    Projects.ServiceAccounts = ServiceAccounts;
    Projects.APIKeys = APIKeys;
    Projects.RateLimits = RateLimits;
    Projects.ModelPermissions = ModelPermissions;
    Projects.HostedToolPermissions = HostedToolPermissions;
    Projects.Groups = Groups2;
    Projects.Roles = Roles3;
    Projects.DataRetention = DataRetention2;
    Projects.SpendAlerts = SpendAlerts2;
    Projects.Certificates = Certificates2;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/users/roles.mjs
var Roles6, init_roles6 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/users/roles.mjs"() {
    init_resource2();
    init_pagination2();
    init_path2();
    Roles6 = class extends APIResource2 {
      /**
       * Assigns an organization role to a user within the organization.
       *
       * @example
       * ```ts
       * const role =
       *   await client.admin.organization.users.roles.create(
       *     'user_id',
       *     { role_id: 'role_id' },
       *   );
       * ```
       */
      create(userID, body, options) {
        return this._client.post(path7`/organization/users/${userID}/roles`, {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Retrieves an organization role assigned to a user.
       *
       * @example
       * ```ts
       * const role =
       *   await client.admin.organization.users.roles.retrieve(
       *     'role_id',
       *     { user_id: 'user_id' },
       *   );
       * ```
       */
      retrieve(roleID, params, options) {
        let { user_id } = params;
        return this._client.get(path7`/organization/users/${user_id}/roles/${roleID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Lists the organization roles assigned to a user within the organization.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const roleListResponse of client.admin.organization.users.roles.list(
       *   'user_id',
       * )) {
       *   // ...
       * }
       * ```
       */
      list(userID, query = {}, options) {
        return this._client.getAPIList(path7`/organization/users/${userID}/roles`, NextCursorPage, { query, ...options, __security: { adminAPIKeyAuth: !0 } });
      }
      /**
       * Unassigns an organization role from a user within the organization.
       *
       * @example
       * ```ts
       * const role =
       *   await client.admin.organization.users.roles.delete(
       *     'role_id',
       *     { user_id: 'user_id' },
       *   );
       * ```
       */
      delete(roleID, params, options) {
        let { user_id } = params;
        return this._client.delete(path7`/organization/users/${user_id}/roles/${roleID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/users/users.mjs
var Users3, init_users3 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/users/users.mjs"() {
    init_resource2();
    init_roles6();
    init_roles6();
    init_pagination2();
    init_path2();
    Users3 = class extends APIResource2 {
      constructor() {
        super(...arguments), this.roles = new Roles6(this._client);
      }
      /**
       * Retrieves a user by their identifier.
       *
       * @example
       * ```ts
       * const organizationUser =
       *   await client.admin.organization.users.retrieve('user_id');
       * ```
       */
      retrieve(userID, options) {
        return this._client.get(path7`/organization/users/${userID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Modifies a user's role in the organization.
       *
       * @example
       * ```ts
       * const organizationUser =
       *   await client.admin.organization.users.update('user_id');
       * ```
       */
      update(userID, body, options) {
        return this._client.post(path7`/organization/users/${userID}`, {
          body,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Lists all of the users in the organization.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const organizationUser of client.admin.organization.users.list()) {
       *   // ...
       * }
       * ```
       */
      list(query = {}, options) {
        return this._client.getAPIList("/organization/users", ConversationCursorPage, {
          query,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * Deletes a user from the organization.
       *
       * @example
       * ```ts
       * const user = await client.admin.organization.users.delete(
       *   'user_id',
       * );
       * ```
       */
      delete(userID, options) {
        return this._client.delete(path7`/organization/users/${userID}`, {
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
    };
    Users3.Roles = Roles6;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/organization.mjs
var Organization, init_organization = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/organization/organization.mjs"() {
    init_resource2();
    init_admin_api_keys();
    init_admin_api_keys();
    init_audit_logs();
    init_audit_logs();
    init_certificates();
    init_certificates();
    init_data_retention();
    init_data_retention();
    init_invites();
    init_invites();
    init_roles();
    init_roles();
    init_spend_alerts();
    init_spend_alerts();
    init_usage();
    init_usage();
    init_groups();
    init_groups();
    init_projects();
    init_projects();
    init_users3();
    init_users3();
    Organization = class extends APIResource2 {
      constructor() {
        super(...arguments), this.auditLogs = new AuditLogs(this._client), this.adminAPIKeys = new AdminAPIKeys(this._client), this.usage = new Usage(this._client), this.invites = new Invites(this._client), this.users = new Users3(this._client), this.groups = new Groups(this._client), this.roles = new Roles(this._client), this.dataRetention = new DataRetention(this._client), this.spendAlerts = new SpendAlerts(this._client), this.certificates = new Certificates(this._client), this.projects = new Projects(this._client);
      }
    };
    Organization.AuditLogs = AuditLogs;
    Organization.AdminAPIKeys = AdminAPIKeys;
    Organization.Usage = Usage;
    Organization.Invites = Invites;
    Organization.Users = Users3;
    Organization.Groups = Groups;
    Organization.Roles = Roles;
    Organization.DataRetention = DataRetention;
    Organization.SpendAlerts = SpendAlerts;
    Organization.Certificates = Certificates;
    Organization.Projects = Projects;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/admin.mjs
var Admin, init_admin = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/admin/admin.mjs"() {
    init_resource2();
    init_organization();
    init_organization();
    Admin = class extends APIResource2 {
      constructor() {
        super(...arguments), this.organization = new Organization(this._client);
      }
    };
    Admin.Organization = Organization;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/headers.mjs
function* iterateHeaders2(headers) {
  if (!headers)
    return;
  if (brand_privateNullableHeaders2 in headers) {
    let { values, nulls } = headers;
    yield* values.entries();
    for (let name of nulls)
      yield [name, null];
    return;
  }
  let shouldClear = !1, iter;
  headers instanceof Headers ? iter = headers.entries() : isReadonlyArray2(headers) ? iter = headers : (shouldClear = !0, iter = Object.entries(headers ?? {}));
  for (let row of iter) {
    let name = row[0];
    if (typeof name != "string")
      throw new TypeError("expected header name to be a string");
    let values = isReadonlyArray2(row[1]) ? row[1] : [row[1]], didClear = !1;
    for (let value of values)
      value !== void 0 && (shouldClear && !didClear && (didClear = !0, yield [name, null]), yield [name, value]);
  }
}
var brand_privateNullableHeaders2, buildHeaders2, init_headers2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/headers.mjs"() {
    init_values2();
    brand_privateNullableHeaders2 = /* @__PURE__ */ Symbol("brand.privateNullableHeaders");
    buildHeaders2 = (newHeaders) => {
      let targetHeaders = new Headers(), nullHeaders = /* @__PURE__ */ new Set();
      for (let headers of newHeaders) {
        let seenHeaders = /* @__PURE__ */ new Set();
        for (let [name, value] of iterateHeaders2(headers)) {
          let lowerName = name.toLowerCase();
          seenHeaders.has(lowerName) || (targetHeaders.delete(name), seenHeaders.add(lowerName)), value === null ? (targetHeaders.delete(name), nullHeaders.add(lowerName)) : (targetHeaders.append(name, value), nullHeaders.delete(lowerName));
        }
      }
      return { [brand_privateNullableHeaders2]: !0, values: targetHeaders, nulls: nullHeaders };
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/audio/speech.mjs
var Speech, init_speech = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/audio/speech.mjs"() {
    init_resource2();
    init_headers2();
    Speech = class extends APIResource2 {
      /**
       * Generates audio from the input text.
       *
       * Returns the audio file content, or a stream of audio events.
       *
       * @example
       * ```ts
       * const speech = await client.audio.speech.create({
       *   input: 'input',
       *   model: 'tts-1',
       *   voice: 'alloy',
       * });
       *
       * const content = await speech.blob();
       * console.log(content);
       * ```
       */
      create(body, options) {
        return this._client.post("/audio/speech", {
          body,
          ...options,
          headers: buildHeaders2([{ Accept: "application/octet-stream" }, options?.headers]),
          __security: { bearerAuth: !0 },
          __binaryResponse: !0
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/audio/transcriptions.mjs
var Transcriptions, init_transcriptions = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/audio/transcriptions.mjs"() {
    init_resource2();
    init_uploads3();
    Transcriptions = class extends APIResource2 {
      create(body, options) {
        return this._client.post("/audio/transcriptions", multipartFormRequestOptions2({
          body,
          ...options,
          stream: body.stream ?? !1,
          __metadata: { model: body.model },
          __security: { bearerAuth: !0 }
        }, this._client));
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/audio/translations.mjs
var Translations, init_translations = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/audio/translations.mjs"() {
    init_resource2();
    init_uploads3();
    Translations = class extends APIResource2 {
      create(body, options) {
        return this._client.post("/audio/translations", multipartFormRequestOptions2({ body, ...options, __metadata: { model: body.model }, __security: { bearerAuth: !0 } }, this._client));
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/audio/audio.mjs
var Audio, init_audio = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/audio/audio.mjs"() {
    init_resource2();
    init_speech();
    init_speech();
    init_transcriptions();
    init_transcriptions();
    init_translations();
    init_translations();
    Audio = class extends APIResource2 {
      constructor() {
        super(...arguments), this.transcriptions = new Transcriptions(this._client), this.translations = new Translations(this._client), this.speech = new Speech(this._client);
      }
    };
    Audio.Transcriptions = Transcriptions;
    Audio.Translations = Translations;
    Audio.Speech = Speech;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/batches.mjs
var Batches3, init_batches3 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/batches.mjs"() {
    init_resource2();
    init_pagination2();
    init_path2();
    Batches3 = class extends APIResource2 {
      /**
       * Creates and executes a batch from an uploaded file of requests
       */
      create(body, options) {
        return this._client.post("/batches", { body, ...options, __security: { bearerAuth: !0 } });
      }
      /**
       * Retrieves a batch.
       */
      retrieve(batchID, options) {
        return this._client.get(path7`/batches/${batchID}`, { ...options, __security: { bearerAuth: !0 } });
      }
      /**
       * List your organization's batches.
       */
      list(query = {}, options) {
        return this._client.getAPIList("/batches", CursorPage, {
          query,
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Cancels an in-progress batch. The batch will be in status `cancelling` for up to
       * 10 minutes, before changing to `cancelled`, where it will have partial results
       * (if any) available in the output file.
       */
      cancel(batchID, options) {
        return this._client.post(path7`/batches/${batchID}/cancel`, {
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/beta/assistants.mjs
var Assistants, init_assistants = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/beta/assistants.mjs"() {
    init_resource2();
    init_pagination2();
    init_headers2();
    init_path2();
    Assistants = class extends APIResource2 {
      /**
       * Create an assistant with a model and instructions.
       *
       * @deprecated
       */
      create(body, options) {
        return this._client.post("/assistants", {
          body,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Retrieves an assistant.
       *
       * @deprecated
       */
      retrieve(assistantID, options) {
        return this._client.get(path7`/assistants/${assistantID}`, {
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Modifies an assistant.
       *
       * @deprecated
       */
      update(assistantID, body, options) {
        return this._client.post(path7`/assistants/${assistantID}`, {
          body,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Returns a list of assistants.
       *
       * @deprecated
       */
      list(query = {}, options) {
        return this._client.getAPIList("/assistants", CursorPage, {
          query,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Delete an assistant.
       *
       * @deprecated
       */
      delete(assistantID, options) {
        return this._client.delete(path7`/assistants/${assistantID}`, {
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/beta/realtime/sessions.mjs
var Sessions2, init_sessions2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/beta/realtime/sessions.mjs"() {
    init_resource2();
    init_headers2();
    Sessions2 = class extends APIResource2 {
      /**
       * Create an ephemeral API token for use in client-side applications with the
       * Realtime API. Can be configured with the same session parameters as the
       * `session.update` client event.
       *
       * It responds with a session object, plus a `client_secret` key which contains a
       * usable ephemeral API token that can be used to authenticate browser clients for
       * the Realtime API.
       *
       * @example
       * ```ts
       * const session =
       *   await client.beta.realtime.sessions.create();
       * ```
       */
      create(body, options) {
        return this._client.post("/realtime/sessions", {
          body,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/beta/realtime/transcription-sessions.mjs
var TranscriptionSessions, init_transcription_sessions = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/beta/realtime/transcription-sessions.mjs"() {
    init_resource2();
    init_headers2();
    TranscriptionSessions = class extends APIResource2 {
      /**
       * Create an ephemeral API token for use in client-side applications with the
       * Realtime API specifically for realtime transcriptions. Can be configured with
       * the same session parameters as the `transcription_session.update` client event.
       *
       * It responds with a session object, plus a `client_secret` key which contains a
       * usable ephemeral API token that can be used to authenticate browser clients for
       * the Realtime API.
       *
       * @example
       * ```ts
       * const transcriptionSession =
       *   await client.beta.realtime.transcriptionSessions.create();
       * ```
       */
      create(body, options) {
        return this._client.post("/realtime/transcription_sessions", {
          body,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/beta/realtime/realtime.mjs
var Realtime, init_realtime = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/beta/realtime/realtime.mjs"() {
    init_resource2();
    init_sessions2();
    init_sessions2();
    init_transcription_sessions();
    init_transcription_sessions();
    Realtime = class extends APIResource2 {
      constructor() {
        super(...arguments), this.sessions = new Sessions2(this._client), this.transcriptionSessions = new TranscriptionSessions(this._client);
      }
    };
    Realtime.Sessions = Sessions2;
    Realtime.TranscriptionSessions = TranscriptionSessions;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/beta/chatkit/sessions.mjs
var Sessions3, init_sessions3 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/beta/chatkit/sessions.mjs"() {
    init_resource2();
    init_headers2();
    init_path2();
    Sessions3 = class extends APIResource2 {
      /**
       * Create a ChatKit session.
       *
       * @example
       * ```ts
       * const chatSession =
       *   await client.beta.chatkit.sessions.create({
       *     user: 'x',
       *     workflow: { id: 'id' },
       *   });
       * ```
       */
      create(body, options) {
        return this._client.post("/chatkit/sessions", {
          body,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "chatkit_beta=v1" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Cancel an active ChatKit session and return its most recent metadata.
       *
       * Cancelling prevents new requests from using the issued client secret.
       *
       * @example
       * ```ts
       * const chatSession =
       *   await client.beta.chatkit.sessions.cancel('cksess_123');
       * ```
       */
      cancel(sessionID, options) {
        return this._client.post(path7`/chatkit/sessions/${sessionID}/cancel`, {
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "chatkit_beta=v1" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/beta/chatkit/threads.mjs
var Threads2, init_threads2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/beta/chatkit/threads.mjs"() {
    init_resource2();
    init_pagination2();
    init_headers2();
    init_path2();
    Threads2 = class extends APIResource2 {
      /**
       * Retrieve a ChatKit thread by its identifier.
       *
       * @example
       * ```ts
       * const chatkitThread =
       *   await client.beta.chatkit.threads.retrieve('cthr_123');
       * ```
       */
      retrieve(threadID, options) {
        return this._client.get(path7`/chatkit/threads/${threadID}`, {
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "chatkit_beta=v1" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * List ChatKit threads with optional pagination and user filters.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const chatkitThread of client.beta.chatkit.threads.list()) {
       *   // ...
       * }
       * ```
       */
      list(query = {}, options) {
        return this._client.getAPIList("/chatkit/threads", ConversationCursorPage, {
          query,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "chatkit_beta=v1" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Delete a ChatKit thread along with its items and stored attachments.
       *
       * @example
       * ```ts
       * const thread = await client.beta.chatkit.threads.delete(
       *   'cthr_123',
       * );
       * ```
       */
      delete(threadID, options) {
        return this._client.delete(path7`/chatkit/threads/${threadID}`, {
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "chatkit_beta=v1" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * List items that belong to a ChatKit thread.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const thread of client.beta.chatkit.threads.listItems(
       *   'cthr_123',
       * )) {
       *   // ...
       * }
       * ```
       */
      listItems(threadID, query = {}, options) {
        return this._client.getAPIList(path7`/chatkit/threads/${threadID}/items`, ConversationCursorPage, {
          query,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "chatkit_beta=v1" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/beta/chatkit/chatkit.mjs
var ChatKit, init_chatkit = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/beta/chatkit/chatkit.mjs"() {
    init_resource2();
    init_sessions3();
    init_sessions3();
    init_threads2();
    init_threads2();
    ChatKit = class extends APIResource2 {
      constructor() {
        super(...arguments), this.sessions = new Sessions3(this._client), this.threads = new Threads2(this._client);
      }
    };
    ChatKit.Sessions = Sessions3;
    ChatKit.Threads = Threads2;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/beta/threads/messages.mjs
var Messages4, init_messages4 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/beta/threads/messages.mjs"() {
    init_resource2();
    init_pagination2();
    init_headers2();
    init_path2();
    Messages4 = class extends APIResource2 {
      /**
       * Create a message.
       *
       * @deprecated The Assistants API is deprecated in favor of the Responses API
       */
      create(threadID, body, options) {
        return this._client.post(path7`/threads/${threadID}/messages`, {
          body,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Retrieve a message.
       *
       * @deprecated The Assistants API is deprecated in favor of the Responses API
       */
      retrieve(messageID, params, options) {
        let { thread_id } = params;
        return this._client.get(path7`/threads/${thread_id}/messages/${messageID}`, {
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Modifies a message.
       *
       * @deprecated The Assistants API is deprecated in favor of the Responses API
       */
      update(messageID, params, options) {
        let { thread_id, ...body } = params;
        return this._client.post(path7`/threads/${thread_id}/messages/${messageID}`, {
          body,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Returns a list of messages for a given thread.
       *
       * @deprecated The Assistants API is deprecated in favor of the Responses API
       */
      list(threadID, query = {}, options) {
        return this._client.getAPIList(path7`/threads/${threadID}/messages`, CursorPage, {
          query,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Deletes a message.
       *
       * @deprecated The Assistants API is deprecated in favor of the Responses API
       */
      delete(messageID, params, options) {
        let { thread_id } = params;
        return this._client.delete(path7`/threads/${thread_id}/messages/${messageID}`, {
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/beta/threads/runs/steps.mjs
var Steps, init_steps = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/beta/threads/runs/steps.mjs"() {
    init_resource2();
    init_pagination2();
    init_headers2();
    init_path2();
    Steps = class extends APIResource2 {
      /**
       * Retrieves a run step.
       *
       * @deprecated The Assistants API is deprecated in favor of the Responses API
       */
      retrieve(stepID, params, options) {
        let { thread_id, run_id, ...query } = params;
        return this._client.get(path7`/threads/${thread_id}/runs/${run_id}/steps/${stepID}`, {
          query,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Returns a list of run steps belonging to a run.
       *
       * @deprecated The Assistants API is deprecated in favor of the Responses API
       */
      list(runID, params, options) {
        let { thread_id, ...query } = params;
        return this._client.getAPIList(path7`/threads/${thread_id}/runs/${runID}/steps`, CursorPage, {
          query,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/utils/base64.mjs
var toFloat32Array, init_base642 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/utils/base64.mjs"() {
    init_error3();
    init_bytes2();
    toFloat32Array = (base64Str) => {
      if (typeof Buffer < "u") {
        let buf = Buffer.from(base64Str, "base64");
        return Array.from(new Float32Array(buf.buffer, buf.byteOffset, buf.length / Float32Array.BYTES_PER_ELEMENT));
      } else {
        let binaryStr = atob(base64Str), len = binaryStr.length, bytes = new Uint8Array(len);
        for (let i = 0; i < len; i++)
          bytes[i] = binaryStr.charCodeAt(i);
        return Array.from(new Float32Array(bytes.buffer));
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/utils/env.mjs
var readEnv2, init_env2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/utils/env.mjs"() {
    readEnv2 = (env) => {
      if (typeof globalThis.process < "u")
        return globalThis.process.env?.[env]?.trim() || void 0;
      if (typeof globalThis.Deno < "u")
        return globalThis.Deno.env?.get?.(env)?.trim() || void 0;
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/utils.mjs
var init_utils4 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/utils.mjs"() {
    init_values2();
    init_base642();
    init_env2();
    init_log2();
    init_uuid2();
    init_sleep2();
    init_query2();
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/lib/AssistantStream.mjs
var _AssistantStream_instances, _a3, _AssistantStream_events, _AssistantStream_runStepSnapshots, _AssistantStream_messageSnapshots, _AssistantStream_messageSnapshot, _AssistantStream_finalRun, _AssistantStream_currentContentIndex, _AssistantStream_currentContent, _AssistantStream_currentToolCallIndex, _AssistantStream_currentToolCall, _AssistantStream_currentEvent, _AssistantStream_currentRunSnapshot, _AssistantStream_currentRunStepSnapshot, _AssistantStream_addEvent, _AssistantStream_endRequest, _AssistantStream_handleMessage, _AssistantStream_handleRunStep, _AssistantStream_handleEvent, _AssistantStream_accumulateRunStep, _AssistantStream_accumulateMessage, _AssistantStream_accumulateContent, _AssistantStream_handleRun, AssistantStream, init_AssistantStream = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/lib/AssistantStream.mjs"() {
    init_tslib2();
    init_streaming4();
    init_error4();
    init_EventStream();
    init_utils4();
    AssistantStream = class extends EventStream {
      constructor() {
        super(...arguments), _AssistantStream_instances.add(this), _AssistantStream_events.set(this, []), _AssistantStream_runStepSnapshots.set(this, {}), _AssistantStream_messageSnapshots.set(this, {}), _AssistantStream_messageSnapshot.set(this, void 0), _AssistantStream_finalRun.set(this, void 0), _AssistantStream_currentContentIndex.set(this, void 0), _AssistantStream_currentContent.set(this, void 0), _AssistantStream_currentToolCallIndex.set(this, void 0), _AssistantStream_currentToolCall.set(this, void 0), _AssistantStream_currentEvent.set(this, void 0), _AssistantStream_currentRunSnapshot.set(this, void 0), _AssistantStream_currentRunStepSnapshot.set(this, void 0);
      }
      [(_AssistantStream_events = /* @__PURE__ */ new WeakMap(), _AssistantStream_runStepSnapshots = /* @__PURE__ */ new WeakMap(), _AssistantStream_messageSnapshots = /* @__PURE__ */ new WeakMap(), _AssistantStream_messageSnapshot = /* @__PURE__ */ new WeakMap(), _AssistantStream_finalRun = /* @__PURE__ */ new WeakMap(), _AssistantStream_currentContentIndex = /* @__PURE__ */ new WeakMap(), _AssistantStream_currentContent = /* @__PURE__ */ new WeakMap(), _AssistantStream_currentToolCallIndex = /* @__PURE__ */ new WeakMap(), _AssistantStream_currentToolCall = /* @__PURE__ */ new WeakMap(), _AssistantStream_currentEvent = /* @__PURE__ */ new WeakMap(), _AssistantStream_currentRunSnapshot = /* @__PURE__ */ new WeakMap(), _AssistantStream_currentRunStepSnapshot = /* @__PURE__ */ new WeakMap(), _AssistantStream_instances = /* @__PURE__ */ new WeakSet(), Symbol.asyncIterator)]() {
        let pushQueue = [], readQueue = [], done = !1;
        return this.on("event", (event) => {
          let reader = readQueue.shift();
          reader ? reader.resolve(event) : pushQueue.push(event);
        }), this.on("end", () => {
          done = !0;
          for (let reader of readQueue)
            reader.resolve(void 0);
          readQueue.length = 0;
        }), this.on("abort", (err) => {
          done = !0;
          for (let reader of readQueue)
            reader.reject(err);
          readQueue.length = 0;
        }), this.on("error", (err) => {
          done = !0;
          for (let reader of readQueue)
            reader.reject(err);
          readQueue.length = 0;
        }), {
          next: async () => pushQueue.length ? { value: pushQueue.shift(), done: !1 } : done ? { value: void 0, done: !0 } : new Promise((resolve4, reject) => readQueue.push({ resolve: resolve4, reject })).then((chunk2) => chunk2 ? { value: chunk2, done: !1 } : { value: void 0, done: !0 }),
          return: async () => (this.abort(), { value: void 0, done: !0 })
        };
      }
      static fromReadableStream(stream) {
        let runner = new _a3();
        return runner._run(() => runner._fromReadableStream(stream)), runner;
      }
      async _fromReadableStream(readableStream, options) {
        this._listenForAbort(options?.signal), this._connected();
        let stream = Stream2.fromReadableStream(readableStream, this.controller);
        for await (let event of stream)
          __classPrivateFieldGet2(this, _AssistantStream_instances, "m", _AssistantStream_addEvent).call(this, event);
        if (stream.controller.signal?.aborted)
          throw new APIUserAbortError2();
        return this._addRun(__classPrivateFieldGet2(this, _AssistantStream_instances, "m", _AssistantStream_endRequest).call(this));
      }
      toReadableStream() {
        return new Stream2(this[Symbol.asyncIterator].bind(this), this.controller).toReadableStream();
      }
      static createToolAssistantStream(runId, runs, params, options) {
        let runner = new _a3();
        return runner._run(() => runner._runToolAssistantStream(runId, runs, params, {
          ...options,
          headers: { ...options?.headers, "X-Stainless-Helper-Method": "stream" }
        })), runner;
      }
      async _createToolAssistantStream(run, runId, params, options) {
        this._listenForAbort(options?.signal);
        let body = { ...params, stream: !0 }, stream = await run.submitToolOutputs(runId, body, {
          ...options,
          signal: this.controller.signal
        });
        this._connected();
        for await (let event of stream)
          __classPrivateFieldGet2(this, _AssistantStream_instances, "m", _AssistantStream_addEvent).call(this, event);
        if (stream.controller.signal?.aborted)
          throw new APIUserAbortError2();
        return this._addRun(__classPrivateFieldGet2(this, _AssistantStream_instances, "m", _AssistantStream_endRequest).call(this));
      }
      static createThreadAssistantStream(params, thread, options) {
        let runner = new _a3();
        return runner._run(() => runner._threadAssistantStream(params, thread, {
          ...options,
          headers: { ...options?.headers, "X-Stainless-Helper-Method": "stream" }
        })), runner;
      }
      static createAssistantStream(threadId, runs, params, options) {
        let runner = new _a3();
        return runner._run(() => runner._runAssistantStream(threadId, runs, params, {
          ...options,
          headers: { ...options?.headers, "X-Stainless-Helper-Method": "stream" }
        })), runner;
      }
      currentEvent() {
        return __classPrivateFieldGet2(this, _AssistantStream_currentEvent, "f");
      }
      currentRun() {
        return __classPrivateFieldGet2(this, _AssistantStream_currentRunSnapshot, "f");
      }
      currentMessageSnapshot() {
        return __classPrivateFieldGet2(this, _AssistantStream_messageSnapshot, "f");
      }
      currentRunStepSnapshot() {
        return __classPrivateFieldGet2(this, _AssistantStream_currentRunStepSnapshot, "f");
      }
      async finalRunSteps() {
        return await this.done(), Object.values(__classPrivateFieldGet2(this, _AssistantStream_runStepSnapshots, "f"));
      }
      async finalMessages() {
        return await this.done(), Object.values(__classPrivateFieldGet2(this, _AssistantStream_messageSnapshots, "f"));
      }
      async finalRun() {
        if (await this.done(), !__classPrivateFieldGet2(this, _AssistantStream_finalRun, "f"))
          throw Error("Final run was not received.");
        return __classPrivateFieldGet2(this, _AssistantStream_finalRun, "f");
      }
      async _createThreadAssistantStream(thread, params, options) {
        this._listenForAbort(options?.signal);
        let body = { ...params, stream: !0 }, stream = await thread.createAndRun(body, { ...options, signal: this.controller.signal });
        this._connected();
        for await (let event of stream)
          __classPrivateFieldGet2(this, _AssistantStream_instances, "m", _AssistantStream_addEvent).call(this, event);
        if (stream.controller.signal?.aborted)
          throw new APIUserAbortError2();
        return this._addRun(__classPrivateFieldGet2(this, _AssistantStream_instances, "m", _AssistantStream_endRequest).call(this));
      }
      async _createAssistantStream(run, threadId, params, options) {
        this._listenForAbort(options?.signal);
        let body = { ...params, stream: !0 }, stream = await run.create(threadId, body, { ...options, signal: this.controller.signal });
        this._connected();
        for await (let event of stream)
          __classPrivateFieldGet2(this, _AssistantStream_instances, "m", _AssistantStream_addEvent).call(this, event);
        if (stream.controller.signal?.aborted)
          throw new APIUserAbortError2();
        return this._addRun(__classPrivateFieldGet2(this, _AssistantStream_instances, "m", _AssistantStream_endRequest).call(this));
      }
      static accumulateDelta(acc, delta) {
        for (let [key, deltaValue] of Object.entries(delta)) {
          if (!acc.hasOwnProperty(key)) {
            acc[key] = deltaValue;
            continue;
          }
          let accValue = acc[key];
          if (accValue == null) {
            acc[key] = deltaValue;
            continue;
          }
          if (key === "index" || key === "type") {
            acc[key] = deltaValue;
            continue;
          }
          if (typeof accValue == "string" && typeof deltaValue == "string")
            accValue += deltaValue;
          else if (typeof accValue == "number" && typeof deltaValue == "number")
            accValue += deltaValue;
          else if (isObj(accValue) && isObj(deltaValue))
            accValue = this.accumulateDelta(accValue, deltaValue);
          else if (Array.isArray(accValue) && Array.isArray(deltaValue)) {
            if (accValue.every((x) => typeof x == "string" || typeof x == "number")) {
              accValue.push(...deltaValue);
              continue;
            }
            for (let deltaEntry of deltaValue) {
              if (!isObj(deltaEntry))
                throw new Error(`Expected array delta entry to be an object but got: ${deltaEntry}`);
              let index = deltaEntry.index;
              if (index == null)
                throw console.error(deltaEntry), new Error("Expected array delta entry to have an `index` property");
              if (typeof index != "number")
                throw new Error(`Expected array delta entry \`index\` property to be a number but got ${index}`);
              let accEntry = accValue[index];
              accEntry == null ? accValue.push(deltaEntry) : accValue[index] = this.accumulateDelta(accEntry, deltaEntry);
            }
            continue;
          } else
            throw Error(`Unhandled record type: ${key}, deltaValue: ${deltaValue}, accValue: ${accValue}`);
          acc[key] = accValue;
        }
        return acc;
      }
      _addRun(run) {
        return run;
      }
      async _threadAssistantStream(params, thread, options) {
        return await this._createThreadAssistantStream(thread, params, options);
      }
      async _runAssistantStream(threadId, runs, params, options) {
        return await this._createAssistantStream(runs, threadId, params, options);
      }
      async _runToolAssistantStream(runId, runs, params, options) {
        return await this._createToolAssistantStream(runs, runId, params, options);
      }
    };
    _a3 = AssistantStream, _AssistantStream_addEvent = function(event) {
      if (!this.ended)
        switch (__classPrivateFieldSet2(this, _AssistantStream_currentEvent, event, "f"), __classPrivateFieldGet2(this, _AssistantStream_instances, "m", _AssistantStream_handleEvent).call(this, event), event.event) {
          case "thread.created":
            break;
          case "thread.run.created":
          case "thread.run.queued":
          case "thread.run.in_progress":
          case "thread.run.requires_action":
          case "thread.run.completed":
          case "thread.run.incomplete":
          case "thread.run.failed":
          case "thread.run.cancelling":
          case "thread.run.cancelled":
          case "thread.run.expired":
            __classPrivateFieldGet2(this, _AssistantStream_instances, "m", _AssistantStream_handleRun).call(this, event);
            break;
          case "thread.run.step.created":
          case "thread.run.step.in_progress":
          case "thread.run.step.delta":
          case "thread.run.step.completed":
          case "thread.run.step.failed":
          case "thread.run.step.cancelled":
          case "thread.run.step.expired":
            __classPrivateFieldGet2(this, _AssistantStream_instances, "m", _AssistantStream_handleRunStep).call(this, event);
            break;
          case "thread.message.created":
          case "thread.message.in_progress":
          case "thread.message.delta":
          case "thread.message.completed":
          case "thread.message.incomplete":
            __classPrivateFieldGet2(this, _AssistantStream_instances, "m", _AssistantStream_handleMessage).call(this, event);
            break;
          case "error":
            throw new Error("Encountered an error event in event processing - errors should be processed earlier");
          default:
        }
    }, _AssistantStream_endRequest = function() {
      if (this.ended)
        throw new OpenAIError("stream has ended, this shouldn't happen");
      if (!__classPrivateFieldGet2(this, _AssistantStream_finalRun, "f"))
        throw Error("Final run has not been received");
      return __classPrivateFieldGet2(this, _AssistantStream_finalRun, "f");
    }, _AssistantStream_handleMessage = function(event) {
      let [accumulatedMessage, newContent] = __classPrivateFieldGet2(this, _AssistantStream_instances, "m", _AssistantStream_accumulateMessage).call(this, event, __classPrivateFieldGet2(this, _AssistantStream_messageSnapshot, "f"));
      __classPrivateFieldSet2(this, _AssistantStream_messageSnapshot, accumulatedMessage, "f"), __classPrivateFieldGet2(this, _AssistantStream_messageSnapshots, "f")[accumulatedMessage.id] = accumulatedMessage;
      for (let content of newContent) {
        let snapshotContent = accumulatedMessage.content[content.index];
        snapshotContent?.type == "text" && this._emit("textCreated", snapshotContent.text);
      }
      switch (event.event) {
        case "thread.message.created":
          this._emit("messageCreated", event.data);
          break;
        case "thread.message.in_progress":
          break;
        case "thread.message.delta":
          if (this._emit("messageDelta", event.data.delta, accumulatedMessage), event.data.delta.content)
            for (let content of event.data.delta.content) {
              if (content.type == "text" && content.text) {
                let textDelta = content.text, snapshot = accumulatedMessage.content[content.index];
                if (snapshot && snapshot.type == "text")
                  this._emit("textDelta", textDelta, snapshot.text);
                else
                  throw Error("The snapshot associated with this text delta is not text or missing");
              }
              if (content.index != __classPrivateFieldGet2(this, _AssistantStream_currentContentIndex, "f")) {
                if (__classPrivateFieldGet2(this, _AssistantStream_currentContent, "f"))
                  switch (__classPrivateFieldGet2(this, _AssistantStream_currentContent, "f").type) {
                    case "text":
                      this._emit("textDone", __classPrivateFieldGet2(this, _AssistantStream_currentContent, "f").text, __classPrivateFieldGet2(this, _AssistantStream_messageSnapshot, "f"));
                      break;
                    case "image_file":
                      this._emit("imageFileDone", __classPrivateFieldGet2(this, _AssistantStream_currentContent, "f").image_file, __classPrivateFieldGet2(this, _AssistantStream_messageSnapshot, "f"));
                      break;
                  }
                __classPrivateFieldSet2(this, _AssistantStream_currentContentIndex, content.index, "f");
              }
              __classPrivateFieldSet2(this, _AssistantStream_currentContent, accumulatedMessage.content[content.index], "f");
            }
          break;
        case "thread.message.completed":
        case "thread.message.incomplete":
          if (__classPrivateFieldGet2(this, _AssistantStream_currentContentIndex, "f") !== void 0) {
            let currentContent = event.data.content[__classPrivateFieldGet2(this, _AssistantStream_currentContentIndex, "f")];
            if (currentContent)
              switch (currentContent.type) {
                case "image_file":
                  this._emit("imageFileDone", currentContent.image_file, __classPrivateFieldGet2(this, _AssistantStream_messageSnapshot, "f"));
                  break;
                case "text":
                  this._emit("textDone", currentContent.text, __classPrivateFieldGet2(this, _AssistantStream_messageSnapshot, "f"));
                  break;
              }
          }
          __classPrivateFieldGet2(this, _AssistantStream_messageSnapshot, "f") && this._emit("messageDone", event.data), __classPrivateFieldSet2(this, _AssistantStream_messageSnapshot, void 0, "f");
      }
    }, _AssistantStream_handleRunStep = function(event) {
      let accumulatedRunStep = __classPrivateFieldGet2(this, _AssistantStream_instances, "m", _AssistantStream_accumulateRunStep).call(this, event);
      switch (__classPrivateFieldSet2(this, _AssistantStream_currentRunStepSnapshot, accumulatedRunStep, "f"), event.event) {
        case "thread.run.step.created":
          this._emit("runStepCreated", event.data);
          break;
        case "thread.run.step.delta":
          let delta = event.data.delta;
          if (delta.step_details && delta.step_details.type == "tool_calls" && delta.step_details.tool_calls && accumulatedRunStep.step_details.type == "tool_calls")
            for (let toolCall of delta.step_details.tool_calls)
              toolCall.index == __classPrivateFieldGet2(this, _AssistantStream_currentToolCallIndex, "f") ? this._emit("toolCallDelta", toolCall, accumulatedRunStep.step_details.tool_calls[toolCall.index]) : (__classPrivateFieldGet2(this, _AssistantStream_currentToolCall, "f") && this._emit("toolCallDone", __classPrivateFieldGet2(this, _AssistantStream_currentToolCall, "f")), __classPrivateFieldSet2(this, _AssistantStream_currentToolCallIndex, toolCall.index, "f"), __classPrivateFieldSet2(this, _AssistantStream_currentToolCall, accumulatedRunStep.step_details.tool_calls[toolCall.index], "f"), __classPrivateFieldGet2(this, _AssistantStream_currentToolCall, "f") && this._emit("toolCallCreated", __classPrivateFieldGet2(this, _AssistantStream_currentToolCall, "f")));
          this._emit("runStepDelta", event.data.delta, accumulatedRunStep);
          break;
        case "thread.run.step.completed":
        case "thread.run.step.failed":
        case "thread.run.step.cancelled":
        case "thread.run.step.expired":
          __classPrivateFieldSet2(this, _AssistantStream_currentRunStepSnapshot, void 0, "f"), event.data.step_details.type == "tool_calls" && __classPrivateFieldGet2(this, _AssistantStream_currentToolCall, "f") && (this._emit("toolCallDone", __classPrivateFieldGet2(this, _AssistantStream_currentToolCall, "f")), __classPrivateFieldSet2(this, _AssistantStream_currentToolCall, void 0, "f")), this._emit("runStepDone", event.data, accumulatedRunStep);
          break;
        case "thread.run.step.in_progress":
          break;
      }
    }, _AssistantStream_handleEvent = function(event) {
      __classPrivateFieldGet2(this, _AssistantStream_events, "f").push(event), this._emit("event", event);
    }, _AssistantStream_accumulateRunStep = function(event) {
      switch (event.event) {
        case "thread.run.step.created":
          return __classPrivateFieldGet2(this, _AssistantStream_runStepSnapshots, "f")[event.data.id] = event.data, event.data;
        case "thread.run.step.delta":
          let snapshot = __classPrivateFieldGet2(this, _AssistantStream_runStepSnapshots, "f")[event.data.id];
          if (!snapshot)
            throw Error("Received a RunStepDelta before creation of a snapshot");
          let data = event.data;
          if (data.delta) {
            let accumulated = _a3.accumulateDelta(snapshot, data.delta);
            __classPrivateFieldGet2(this, _AssistantStream_runStepSnapshots, "f")[event.data.id] = accumulated;
          }
          return __classPrivateFieldGet2(this, _AssistantStream_runStepSnapshots, "f")[event.data.id];
        case "thread.run.step.completed":
        case "thread.run.step.failed":
        case "thread.run.step.cancelled":
        case "thread.run.step.expired":
        case "thread.run.step.in_progress":
          __classPrivateFieldGet2(this, _AssistantStream_runStepSnapshots, "f")[event.data.id] = event.data;
          break;
      }
      if (__classPrivateFieldGet2(this, _AssistantStream_runStepSnapshots, "f")[event.data.id])
        return __classPrivateFieldGet2(this, _AssistantStream_runStepSnapshots, "f")[event.data.id];
      throw new Error("No snapshot available");
    }, _AssistantStream_accumulateMessage = function(event, snapshot) {
      let newContent = [];
      switch (event.event) {
        case "thread.message.created":
          return [event.data, newContent];
        case "thread.message.delta":
          if (!snapshot)
            throw Error("Received a delta with no existing snapshot (there should be one from message creation)");
          let data = event.data;
          if (data.delta.content)
            for (let contentElement of data.delta.content)
              if (contentElement.index in snapshot.content) {
                let currentContent = snapshot.content[contentElement.index];
                snapshot.content[contentElement.index] = __classPrivateFieldGet2(this, _AssistantStream_instances, "m", _AssistantStream_accumulateContent).call(this, contentElement, currentContent);
              } else
                snapshot.content[contentElement.index] = contentElement, newContent.push(contentElement);
          return [snapshot, newContent];
        case "thread.message.in_progress":
        case "thread.message.completed":
        case "thread.message.incomplete":
          if (snapshot)
            return [snapshot, newContent];
          throw Error("Received thread message event with no existing snapshot");
      }
      throw Error("Tried to accumulate a non-message event");
    }, _AssistantStream_accumulateContent = function(contentElement, currentContent) {
      return _a3.accumulateDelta(currentContent, contentElement);
    }, _AssistantStream_handleRun = function(event) {
      switch (__classPrivateFieldSet2(this, _AssistantStream_currentRunSnapshot, event.data, "f"), event.event) {
        case "thread.run.created":
          break;
        case "thread.run.queued":
          break;
        case "thread.run.in_progress":
          break;
        case "thread.run.requires_action":
        case "thread.run.cancelled":
        case "thread.run.failed":
        case "thread.run.completed":
        case "thread.run.expired":
        case "thread.run.incomplete":
          __classPrivateFieldSet2(this, _AssistantStream_finalRun, event.data, "f"), __classPrivateFieldGet2(this, _AssistantStream_currentToolCall, "f") && (this._emit("toolCallDone", __classPrivateFieldGet2(this, _AssistantStream_currentToolCall, "f")), __classPrivateFieldSet2(this, _AssistantStream_currentToolCall, void 0, "f"));
          break;
        case "thread.run.cancelling":
          break;
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/beta/threads/runs/runs.mjs
var Runs, init_runs = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/beta/threads/runs/runs.mjs"() {
    init_resource2();
    init_steps();
    init_steps();
    init_pagination2();
    init_headers2();
    init_AssistantStream();
    init_sleep2();
    init_path2();
    Runs = class extends APIResource2 {
      constructor() {
        super(...arguments), this.steps = new Steps(this._client);
      }
      create(threadID, params, options) {
        let { include, ...body } = params;
        return this._client.post(path7`/threads/${threadID}/runs`, {
          query: { include },
          body,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          stream: params.stream ?? !1,
          __synthesizeEventData: !0,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Retrieves a run.
       *
       * @deprecated The Assistants API is deprecated in favor of the Responses API
       */
      retrieve(runID, params, options) {
        let { thread_id } = params;
        return this._client.get(path7`/threads/${thread_id}/runs/${runID}`, {
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Modifies a run.
       *
       * @deprecated The Assistants API is deprecated in favor of the Responses API
       */
      update(runID, params, options) {
        let { thread_id, ...body } = params;
        return this._client.post(path7`/threads/${thread_id}/runs/${runID}`, {
          body,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Returns a list of runs belonging to a thread.
       *
       * @deprecated The Assistants API is deprecated in favor of the Responses API
       */
      list(threadID, query = {}, options) {
        return this._client.getAPIList(path7`/threads/${threadID}/runs`, CursorPage, {
          query,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Cancels a run that is `in_progress`.
       *
       * @deprecated The Assistants API is deprecated in favor of the Responses API
       */
      cancel(runID, params, options) {
        let { thread_id } = params;
        return this._client.post(path7`/threads/${thread_id}/runs/${runID}/cancel`, {
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * A helper to create a run an poll for a terminal state. More information on Run
       * lifecycles can be found here:
       * https://platform.openai.com/docs/assistants/how-it-works/runs-and-run-steps
       */
      async createAndPoll(threadId, body, options) {
        let run = await this.create(threadId, body, options);
        return await this.poll(run.id, { thread_id: threadId }, options);
      }
      /**
       * Create a Run stream
       *
       * @deprecated use `stream` instead
       */
      createAndStream(threadId, body, options) {
        return AssistantStream.createAssistantStream(threadId, this._client.beta.threads.runs, body, options);
      }
      /**
       * A helper to poll a run status until it reaches a terminal state. More
       * information on Run lifecycles can be found here:
       * https://platform.openai.com/docs/assistants/how-it-works/runs-and-run-steps
       */
      async poll(runId, params, options) {
        let headers = buildHeaders2([
          options?.headers,
          {
            "X-Stainless-Poll-Helper": "true",
            "X-Stainless-Custom-Poll-Interval": options?.pollIntervalMs?.toString() ?? void 0
          }
        ]);
        for (; ; ) {
          let { data: run, response } = await this.retrieve(runId, params, {
            ...options,
            headers: { ...options?.headers, ...headers }
          }).withResponse();
          switch (run.status) {
            //If we are in any sort of intermediate state we poll
            case "queued":
            case "in_progress":
            case "cancelling":
              let sleepInterval = 5e3;
              if (options?.pollIntervalMs)
                sleepInterval = options.pollIntervalMs;
              else {
                let headerInterval = response.headers.get("openai-poll-after-ms");
                if (headerInterval) {
                  let headerIntervalMs = parseInt(headerInterval);
                  isNaN(headerIntervalMs) || (sleepInterval = headerIntervalMs);
                }
              }
              await sleep2(sleepInterval);
              break;
            //We return the run in any terminal state.
            case "requires_action":
            case "incomplete":
            case "cancelled":
            case "completed":
            case "failed":
            case "expired":
              return run;
          }
        }
      }
      /**
       * Create a Run stream
       */
      stream(threadId, body, options) {
        return AssistantStream.createAssistantStream(threadId, this._client.beta.threads.runs, body, options);
      }
      submitToolOutputs(runID, params, options) {
        let { thread_id, ...body } = params;
        return this._client.post(path7`/threads/${thread_id}/runs/${runID}/submit_tool_outputs`, {
          body,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          stream: params.stream ?? !1,
          __synthesizeEventData: !0,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * A helper to submit a tool output to a run and poll for a terminal run state.
       * More information on Run lifecycles can be found here:
       * https://platform.openai.com/docs/assistants/how-it-works/runs-and-run-steps
       */
      async submitToolOutputsAndPoll(runId, params, options) {
        let run = await this.submitToolOutputs(runId, params, options);
        return await this.poll(run.id, params, options);
      }
      /**
       * Submit the tool outputs from a previous run and stream the run to a terminal
       * state. More information on Run lifecycles can be found here:
       * https://platform.openai.com/docs/assistants/how-it-works/runs-and-run-steps
       */
      submitToolOutputsStream(runId, params, options) {
        return AssistantStream.createToolAssistantStream(runId, this._client.beta.threads.runs, params, options);
      }
    };
    Runs.Steps = Steps;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/beta/threads/threads.mjs
var Threads3, init_threads3 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/beta/threads/threads.mjs"() {
    init_resource2();
    init_messages4();
    init_messages4();
    init_runs();
    init_runs();
    init_headers2();
    init_AssistantStream();
    init_path2();
    Threads3 = class extends APIResource2 {
      constructor() {
        super(...arguments), this.runs = new Runs(this._client), this.messages = new Messages4(this._client);
      }
      /**
       * Create a thread.
       *
       * @deprecated The Assistants API is deprecated in favor of the Responses API
       */
      create(body = {}, options) {
        return this._client.post("/threads", {
          body,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Retrieves a thread.
       *
       * @deprecated The Assistants API is deprecated in favor of the Responses API
       */
      retrieve(threadID, options) {
        return this._client.get(path7`/threads/${threadID}`, {
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Modifies a thread.
       *
       * @deprecated The Assistants API is deprecated in favor of the Responses API
       */
      update(threadID, body, options) {
        return this._client.post(path7`/threads/${threadID}`, {
          body,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Delete a thread.
       *
       * @deprecated The Assistants API is deprecated in favor of the Responses API
       */
      delete(threadID, options) {
        return this._client.delete(path7`/threads/${threadID}`, {
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      createAndRun(body, options) {
        return this._client.post("/threads/runs", {
          body,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          stream: body.stream ?? !1,
          __synthesizeEventData: !0,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * A helper to create a thread, start a run and then poll for a terminal state.
       * More information on Run lifecycles can be found here:
       * https://platform.openai.com/docs/assistants/how-it-works/runs-and-run-steps
       */
      async createAndRunPoll(body, options) {
        let run = await this.createAndRun(body, options);
        return await this.runs.poll(run.id, { thread_id: run.thread_id }, options);
      }
      /**
       * Create a thread and stream the run back
       */
      createAndRunStream(body, options) {
        return AssistantStream.createThreadAssistantStream(body, this._client.beta.threads, options);
      }
    };
    Threads3.Runs = Runs;
    Threads3.Messages = Messages4;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/beta/beta.mjs
var Beta2, init_beta2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/beta/beta.mjs"() {
    init_resource2();
    init_assistants();
    init_assistants();
    init_realtime();
    init_realtime();
    init_chatkit();
    init_chatkit();
    init_threads3();
    init_threads3();
    Beta2 = class extends APIResource2 {
      constructor() {
        super(...arguments), this.realtime = new Realtime(this._client), this.chatkit = new ChatKit(this._client), this.assistants = new Assistants(this._client), this.threads = new Threads3(this._client);
      }
    };
    Beta2.Realtime = Realtime;
    Beta2.ChatKit = ChatKit;
    Beta2.Assistants = Assistants;
    Beta2.Threads = Threads3;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/completions.mjs
var Completions3, init_completions4 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/completions.mjs"() {
    init_resource2();
    Completions3 = class extends APIResource2 {
      create(body, options) {
        return this._client.post("/completions", {
          body,
          ...options,
          stream: body.stream ?? !1,
          __security: { bearerAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/containers/files/content.mjs
var Content, init_content = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/containers/files/content.mjs"() {
    init_resource2();
    init_headers2();
    init_path2();
    Content = class extends APIResource2 {
      /**
       * Retrieve Container File Content
       */
      retrieve(fileID, params, options) {
        let { container_id } = params;
        return this._client.get(path7`/containers/${container_id}/files/${fileID}/content`, {
          ...options,
          headers: buildHeaders2([{ Accept: "application/binary" }, options?.headers]),
          __security: { bearerAuth: !0 },
          __binaryResponse: !0
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/containers/files/files.mjs
var Files2, init_files2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/containers/files/files.mjs"() {
    init_resource2();
    init_content();
    init_content();
    init_pagination2();
    init_headers2();
    init_uploads3();
    init_path2();
    Files2 = class extends APIResource2 {
      constructor() {
        super(...arguments), this.content = new Content(this._client);
      }
      /**
       * Create a Container File
       *
       * You can send either a multipart/form-data request with the raw file content, or
       * a JSON request with a file ID.
       */
      create(containerID, body, options) {
        return this._client.post(path7`/containers/${containerID}/files`, maybeMultipartFormRequestOptions({ body, ...options, __security: { bearerAuth: !0 } }, this._client));
      }
      /**
       * Retrieve Container File
       */
      retrieve(fileID, params, options) {
        let { container_id } = params;
        return this._client.get(path7`/containers/${container_id}/files/${fileID}`, {
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * List Container files
       */
      list(containerID, query = {}, options) {
        return this._client.getAPIList(path7`/containers/${containerID}/files`, CursorPage, {
          query,
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Delete Container File
       */
      delete(fileID, params, options) {
        let { container_id } = params;
        return this._client.delete(path7`/containers/${container_id}/files/${fileID}`, {
          ...options,
          headers: buildHeaders2([{ Accept: "*/*" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
    };
    Files2.Content = Content;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/containers/containers.mjs
var Containers, init_containers = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/containers/containers.mjs"() {
    init_resource2();
    init_files2();
    init_files2();
    init_pagination2();
    init_headers2();
    init_path2();
    Containers = class extends APIResource2 {
      constructor() {
        super(...arguments), this.files = new Files2(this._client);
      }
      /**
       * Create Container
       */
      create(body, options) {
        return this._client.post("/containers", { body, ...options, __security: { bearerAuth: !0 } });
      }
      /**
       * Retrieve Container
       */
      retrieve(containerID, options) {
        return this._client.get(path7`/containers/${containerID}`, {
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * List Containers
       */
      list(query = {}, options) {
        return this._client.getAPIList("/containers", CursorPage, {
          query,
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Delete Container
       */
      delete(containerID, options) {
        return this._client.delete(path7`/containers/${containerID}`, {
          ...options,
          headers: buildHeaders2([{ Accept: "*/*" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
    };
    Containers.Files = Files2;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/conversations/items.mjs
var Items, init_items = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/conversations/items.mjs"() {
    init_resource2();
    init_pagination2();
    init_path2();
    Items = class extends APIResource2 {
      /**
       * Create items in a conversation with the given ID.
       */
      create(conversationID, params, options) {
        let { include, ...body } = params;
        return this._client.post(path7`/conversations/${conversationID}/items`, {
          query: { include },
          body,
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Get a single item from a conversation with the given IDs.
       */
      retrieve(itemID, params, options) {
        let { conversation_id, ...query } = params;
        return this._client.get(path7`/conversations/${conversation_id}/items/${itemID}`, {
          query,
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * List all items for a conversation with the given ID.
       */
      list(conversationID, query = {}, options) {
        return this._client.getAPIList(path7`/conversations/${conversationID}/items`, ConversationCursorPage, { query, ...options, __security: { bearerAuth: !0 } });
      }
      /**
       * Delete an item from a conversation with the given IDs.
       */
      delete(itemID, params, options) {
        let { conversation_id } = params;
        return this._client.delete(path7`/conversations/${conversation_id}/items/${itemID}`, {
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/conversations/conversations.mjs
var Conversations, init_conversations = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/conversations/conversations.mjs"() {
    init_resource2();
    init_items();
    init_items();
    init_path2();
    Conversations = class extends APIResource2 {
      constructor() {
        super(...arguments), this.items = new Items(this._client);
      }
      /**
       * Create a conversation.
       */
      create(body = {}, options) {
        return this._client.post("/conversations", { body, ...options, __security: { bearerAuth: !0 } });
      }
      /**
       * Get a conversation
       */
      retrieve(conversationID, options) {
        return this._client.get(path7`/conversations/${conversationID}`, {
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Update a conversation
       */
      update(conversationID, body, options) {
        return this._client.post(path7`/conversations/${conversationID}`, {
          body,
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Delete a conversation. Items in the conversation will not be deleted.
       */
      delete(conversationID, options) {
        return this._client.delete(path7`/conversations/${conversationID}`, {
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
    };
    Conversations.Items = Items;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/embeddings.mjs
var Embeddings, init_embeddings = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/embeddings.mjs"() {
    init_resource2();
    init_utils4();
    Embeddings = class extends APIResource2 {
      /**
       * Creates an embedding vector representing the input text.
       *
       * @example
       * ```ts
       * const createEmbeddingResponse =
       *   await client.embeddings.create({
       *     input: 'The quick brown fox jumped over the lazy dog',
       *     model: 'text-embedding-3-small',
       *   });
       * ```
       */
      create(body, options) {
        let hasUserProvidedEncodingFormat = !!body.encoding_format, encoding_format = hasUserProvidedEncodingFormat ? body.encoding_format : "base64";
        hasUserProvidedEncodingFormat && loggerFor2(this._client).debug("embeddings/user defined encoding_format:", body.encoding_format);
        let response = this._client.post("/embeddings", {
          body: {
            ...body,
            encoding_format
          },
          ...options,
          __security: { bearerAuth: !0 }
        });
        return hasUserProvidedEncodingFormat ? response : (loggerFor2(this._client).debug("embeddings/decoding base64 embeddings from base64"), response._thenUnwrap((response2) => (response2 && response2.data && response2.data.forEach((embeddingBase64Obj) => {
          let embeddingBase64Str = embeddingBase64Obj.embedding;
          embeddingBase64Obj.embedding = toFloat32Array(embeddingBase64Str);
        }), response2)));
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/evals/runs/output-items.mjs
var OutputItems, init_output_items = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/evals/runs/output-items.mjs"() {
    init_resource2();
    init_pagination2();
    init_path2();
    OutputItems = class extends APIResource2 {
      /**
       * Get an evaluation run output item by ID.
       */
      retrieve(outputItemID, params, options) {
        let { eval_id, run_id } = params;
        return this._client.get(path7`/evals/${eval_id}/runs/${run_id}/output_items/${outputItemID}`, {
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Get a list of output items for an evaluation run.
       */
      list(runID, params, options) {
        let { eval_id, ...query } = params;
        return this._client.getAPIList(path7`/evals/${eval_id}/runs/${runID}/output_items`, CursorPage, { query, ...options, __security: { bearerAuth: !0 } });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/evals/runs/runs.mjs
var Runs2, init_runs2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/evals/runs/runs.mjs"() {
    init_resource2();
    init_output_items();
    init_output_items();
    init_pagination2();
    init_path2();
    Runs2 = class extends APIResource2 {
      constructor() {
        super(...arguments), this.outputItems = new OutputItems(this._client);
      }
      /**
       * Kicks off a new run for a given evaluation, specifying the data source, and what
       * model configuration to use to test. The datasource will be validated against the
       * schema specified in the config of the evaluation.
       */
      create(evalID, body, options) {
        return this._client.post(path7`/evals/${evalID}/runs`, {
          body,
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Get an evaluation run by ID.
       */
      retrieve(runID, params, options) {
        let { eval_id } = params;
        return this._client.get(path7`/evals/${eval_id}/runs/${runID}`, {
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Get a list of runs for an evaluation.
       */
      list(evalID, query = {}, options) {
        return this._client.getAPIList(path7`/evals/${evalID}/runs`, CursorPage, {
          query,
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Delete an eval run.
       */
      delete(runID, params, options) {
        let { eval_id } = params;
        return this._client.delete(path7`/evals/${eval_id}/runs/${runID}`, {
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Cancel an ongoing evaluation run.
       */
      cancel(runID, params, options) {
        let { eval_id } = params;
        return this._client.post(path7`/evals/${eval_id}/runs/${runID}`, {
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
    };
    Runs2.OutputItems = OutputItems;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/evals/evals.mjs
var Evals, init_evals = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/evals/evals.mjs"() {
    init_resource2();
    init_runs2();
    init_runs2();
    init_pagination2();
    init_path2();
    Evals = class extends APIResource2 {
      constructor() {
        super(...arguments), this.runs = new Runs2(this._client);
      }
      /**
       * Create the structure of an evaluation that can be used to test a model's
       * performance. An evaluation is a set of testing criteria and the config for a
       * data source, which dictates the schema of the data used in the evaluation. After
       * creating an evaluation, you can run it on different models and model parameters.
       * We support several types of graders and datasources. For more information, see
       * the [Evals guide](https://platform.openai.com/docs/guides/evals).
       */
      create(body, options) {
        return this._client.post("/evals", { body, ...options, __security: { bearerAuth: !0 } });
      }
      /**
       * Get an evaluation by ID.
       */
      retrieve(evalID, options) {
        return this._client.get(path7`/evals/${evalID}`, { ...options, __security: { bearerAuth: !0 } });
      }
      /**
       * Update certain properties of an evaluation.
       */
      update(evalID, body, options) {
        return this._client.post(path7`/evals/${evalID}`, { body, ...options, __security: { bearerAuth: !0 } });
      }
      /**
       * List evaluations for a project.
       */
      list(query = {}, options) {
        return this._client.getAPIList("/evals", CursorPage, {
          query,
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Delete an evaluation.
       */
      delete(evalID, options) {
        return this._client.delete(path7`/evals/${evalID}`, { ...options, __security: { bearerAuth: !0 } });
      }
    };
    Evals.Runs = Runs2;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/files.mjs
var Files3, init_files3 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/files.mjs"() {
    init_resource2();
    init_pagination2();
    init_headers2();
    init_sleep2();
    init_error4();
    init_uploads3();
    init_path2();
    Files3 = class extends APIResource2 {
      /**
       * Upload a file that can be used across various endpoints. Individual files can be
       * up to 512 MB, and each project can store up to 2.5 TB of files in total. There
       * is no organization-wide storage limit. Uploads to this endpoint are rate-limited
       * to 1,000 requests per minute per authenticated user.
       *
       * - The Assistants API supports files up to 2 million tokens and of specific file
       *   types. See the
       *   [Assistants Tools guide](https://platform.openai.com/docs/assistants/tools)
       *   for details.
       * - The Fine-tuning API only supports `.jsonl` files. The input also has certain
       *   required formats for fine-tuning
       *   [chat](https://platform.openai.com/docs/api-reference/fine-tuning/chat-input)
       *   or
       *   [completions](https://platform.openai.com/docs/api-reference/fine-tuning/completions-input)
       *   models.
       * - The Batch API only supports `.jsonl` files up to 200 MB in size. The input
       *   also has a specific required
       *   [format](https://platform.openai.com/docs/api-reference/batch/request-input).
       * - For Retrieval or `file_search` ingestion, upload files here first. If you need
       *   to attach multiple uploaded files to the same vector store, use
       *   [`/vector_stores/{vector_store_id}/file_batches`](https://platform.openai.com/docs/api-reference/vector-stores-file-batches/createBatch)
       *   instead of attaching them one by one. Vector store attachment has separate
       *   limits from file upload, including 2,000 attached files per minute per
       *   organization.
       *
       * Please [contact us](https://help.openai.com/) if you need to increase these
       * storage limits.
       */
      create(body, options) {
        return this._client.post("/files", multipartFormRequestOptions2({ body, ...options, __security: { bearerAuth: !0 } }, this._client));
      }
      /**
       * Returns information about a specific file.
       */
      retrieve(fileID, options) {
        return this._client.get(path7`/files/${fileID}`, { ...options, __security: { bearerAuth: !0 } });
      }
      /**
       * Returns a list of files.
       */
      list(query = {}, options) {
        return this._client.getAPIList("/files", CursorPage, {
          query,
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Delete a file and remove it from all vector stores.
       */
      delete(fileID, options) {
        return this._client.delete(path7`/files/${fileID}`, { ...options, __security: { bearerAuth: !0 } });
      }
      /**
       * Returns the contents of the specified file.
       */
      content(fileID, options) {
        return this._client.get(path7`/files/${fileID}/content`, {
          ...options,
          headers: buildHeaders2([{ Accept: "application/binary" }, options?.headers]),
          __security: { bearerAuth: !0 },
          __binaryResponse: !0
        });
      }
      /**
       * Waits for the given file to be processed, default timeout is 30 mins.
       */
      async waitForProcessing(id, { pollInterval = 5e3, maxWait = 1800 * 1e3 } = {}) {
        let TERMINAL_STATES = /* @__PURE__ */ new Set(["processed", "error", "deleted"]), start = Date.now(), file = await this.retrieve(id);
        for (; !file.status || !TERMINAL_STATES.has(file.status); )
          if (await sleep2(pollInterval), file = await this.retrieve(id), Date.now() - start > maxWait)
            throw new APIConnectionTimeoutError2({
              message: `Giving up on waiting for file ${id} to finish processing after ${maxWait} milliseconds.`
            });
        return file;
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/fine-tuning/methods.mjs
var Methods, init_methods = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/fine-tuning/methods.mjs"() {
    init_resource2();
    Methods = class extends APIResource2 {
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/fine-tuning/alpha/graders.mjs
var Graders, init_graders = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/fine-tuning/alpha/graders.mjs"() {
    init_resource2();
    Graders = class extends APIResource2 {
      /**
       * Run a grader.
       *
       * @example
       * ```ts
       * const response = await client.fineTuning.alpha.graders.run({
       *   grader: {
       *     input: 'input',
       *     name: 'name',
       *     operation: 'eq',
       *     reference: 'reference',
       *     type: 'string_check',
       *   },
       *   model_sample: 'model_sample',
       * });
       * ```
       */
      run(body, options) {
        return this._client.post("/fine_tuning/alpha/graders/run", {
          body,
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Validate a grader.
       *
       * @example
       * ```ts
       * const response =
       *   await client.fineTuning.alpha.graders.validate({
       *     grader: {
       *       input: 'input',
       *       name: 'name',
       *       operation: 'eq',
       *       reference: 'reference',
       *       type: 'string_check',
       *     },
       *   });
       * ```
       */
      validate(body, options) {
        return this._client.post("/fine_tuning/alpha/graders/validate", {
          body,
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/fine-tuning/alpha/alpha.mjs
var Alpha, init_alpha = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/fine-tuning/alpha/alpha.mjs"() {
    init_resource2();
    init_graders();
    init_graders();
    Alpha = class extends APIResource2 {
      constructor() {
        super(...arguments), this.graders = new Graders(this._client);
      }
    };
    Alpha.Graders = Graders;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/fine-tuning/checkpoints/permissions.mjs
var Permissions, init_permissions = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/fine-tuning/checkpoints/permissions.mjs"() {
    init_resource2();
    init_pagination2();
    init_path2();
    Permissions = class extends APIResource2 {
      /**
       * **NOTE:** Calling this endpoint requires an [admin API key](../admin-api-keys).
       *
       * This enables organization owners to share fine-tuned models with other projects
       * in their organization.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const permissionCreateResponse of client.fineTuning.checkpoints.permissions.create(
       *   'ft:gpt-4o-mini-2024-07-18:org:weather:B7R9VjQd',
       *   { project_ids: ['string'] },
       * )) {
       *   // ...
       * }
       * ```
       */
      create(fineTunedModelCheckpoint, body, options) {
        return this._client.getAPIList(path7`/fine_tuning/checkpoints/${fineTunedModelCheckpoint}/permissions`, Page2, { body, method: "post", ...options, __security: { adminAPIKeyAuth: !0 } });
      }
      /**
       * **NOTE:** This endpoint requires an [admin API key](../admin-api-keys).
       *
       * Organization owners can use this endpoint to view all permissions for a
       * fine-tuned model checkpoint.
       *
       * @deprecated Retrieve is deprecated. Please swap to the paginated list method instead.
       */
      retrieve(fineTunedModelCheckpoint, query = {}, options) {
        return this._client.get(path7`/fine_tuning/checkpoints/${fineTunedModelCheckpoint}/permissions`, {
          query,
          ...options,
          __security: { adminAPIKeyAuth: !0 }
        });
      }
      /**
       * **NOTE:** This endpoint requires an [admin API key](../admin-api-keys).
       *
       * Organization owners can use this endpoint to view all permissions for a
       * fine-tuned model checkpoint.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const permissionListResponse of client.fineTuning.checkpoints.permissions.list(
       *   'ft-AF1WoRqd3aJAHsqc9NY7iL8F',
       * )) {
       *   // ...
       * }
       * ```
       */
      list(fineTunedModelCheckpoint, query = {}, options) {
        return this._client.getAPIList(path7`/fine_tuning/checkpoints/${fineTunedModelCheckpoint}/permissions`, ConversationCursorPage, { query, ...options, __security: { adminAPIKeyAuth: !0 } });
      }
      /**
       * **NOTE:** This endpoint requires an [admin API key](../admin-api-keys).
       *
       * Organization owners can use this endpoint to delete a permission for a
       * fine-tuned model checkpoint.
       *
       * @example
       * ```ts
       * const permission =
       *   await client.fineTuning.checkpoints.permissions.delete(
       *     'cp_zc4Q7MP6XxulcVzj4MZdwsAB',
       *     {
       *       fine_tuned_model_checkpoint:
       *         'ft:gpt-4o-mini-2024-07-18:org:weather:B7R9VjQd',
       *     },
       *   );
       * ```
       */
      delete(permissionID, params, options) {
        let { fine_tuned_model_checkpoint } = params;
        return this._client.delete(path7`/fine_tuning/checkpoints/${fine_tuned_model_checkpoint}/permissions/${permissionID}`, { ...options, __security: { adminAPIKeyAuth: !0 } });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/fine-tuning/checkpoints/checkpoints.mjs
var Checkpoints, init_checkpoints = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/fine-tuning/checkpoints/checkpoints.mjs"() {
    init_resource2();
    init_permissions();
    init_permissions();
    Checkpoints = class extends APIResource2 {
      constructor() {
        super(...arguments), this.permissions = new Permissions(this._client);
      }
    };
    Checkpoints.Permissions = Permissions;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/fine-tuning/jobs/checkpoints.mjs
var Checkpoints2, init_checkpoints2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/fine-tuning/jobs/checkpoints.mjs"() {
    init_resource2();
    init_pagination2();
    init_path2();
    Checkpoints2 = class extends APIResource2 {
      /**
       * List checkpoints for a fine-tuning job.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const fineTuningJobCheckpoint of client.fineTuning.jobs.checkpoints.list(
       *   'ft-AF1WoRqd3aJAHsqc9NY7iL8F',
       * )) {
       *   // ...
       * }
       * ```
       */
      list(fineTuningJobID, query = {}, options) {
        return this._client.getAPIList(path7`/fine_tuning/jobs/${fineTuningJobID}/checkpoints`, CursorPage, { query, ...options, __security: { bearerAuth: !0 } });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/fine-tuning/jobs/jobs.mjs
var Jobs, init_jobs = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/fine-tuning/jobs/jobs.mjs"() {
    init_resource2();
    init_checkpoints2();
    init_checkpoints2();
    init_pagination2();
    init_path2();
    Jobs = class extends APIResource2 {
      constructor() {
        super(...arguments), this.checkpoints = new Checkpoints2(this._client);
      }
      /**
       * Creates a fine-tuning job which begins the process of creating a new model from
       * a given dataset.
       *
       * Response includes details of the enqueued job including job status and the name
       * of the fine-tuned models once complete.
       *
       * [Learn more about fine-tuning](https://platform.openai.com/docs/guides/model-optimization)
       *
       * @example
       * ```ts
       * const fineTuningJob = await client.fineTuning.jobs.create({
       *   model: 'gpt-4o-mini',
       *   training_file: 'file-abc123',
       * });
       * ```
       */
      create(body, options) {
        return this._client.post("/fine_tuning/jobs", { body, ...options, __security: { bearerAuth: !0 } });
      }
      /**
       * Get info about a fine-tuning job.
       *
       * [Learn more about fine-tuning](https://platform.openai.com/docs/guides/model-optimization)
       *
       * @example
       * ```ts
       * const fineTuningJob = await client.fineTuning.jobs.retrieve(
       *   'ft-AF1WoRqd3aJAHsqc9NY7iL8F',
       * );
       * ```
       */
      retrieve(fineTuningJobID, options) {
        return this._client.get(path7`/fine_tuning/jobs/${fineTuningJobID}`, {
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * List your organization's fine-tuning jobs
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const fineTuningJob of client.fineTuning.jobs.list()) {
       *   // ...
       * }
       * ```
       */
      list(query = {}, options) {
        return this._client.getAPIList("/fine_tuning/jobs", CursorPage, {
          query,
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Immediately cancel a fine-tune job.
       *
       * @example
       * ```ts
       * const fineTuningJob = await client.fineTuning.jobs.cancel(
       *   'ft-AF1WoRqd3aJAHsqc9NY7iL8F',
       * );
       * ```
       */
      cancel(fineTuningJobID, options) {
        return this._client.post(path7`/fine_tuning/jobs/${fineTuningJobID}/cancel`, {
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Get status updates for a fine-tuning job.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const fineTuningJobEvent of client.fineTuning.jobs.listEvents(
       *   'ft-AF1WoRqd3aJAHsqc9NY7iL8F',
       * )) {
       *   // ...
       * }
       * ```
       */
      listEvents(fineTuningJobID, query = {}, options) {
        return this._client.getAPIList(path7`/fine_tuning/jobs/${fineTuningJobID}/events`, CursorPage, { query, ...options, __security: { bearerAuth: !0 } });
      }
      /**
       * Pause a fine-tune job.
       *
       * @example
       * ```ts
       * const fineTuningJob = await client.fineTuning.jobs.pause(
       *   'ft-AF1WoRqd3aJAHsqc9NY7iL8F',
       * );
       * ```
       */
      pause(fineTuningJobID, options) {
        return this._client.post(path7`/fine_tuning/jobs/${fineTuningJobID}/pause`, {
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Resume a fine-tune job.
       *
       * @example
       * ```ts
       * const fineTuningJob = await client.fineTuning.jobs.resume(
       *   'ft-AF1WoRqd3aJAHsqc9NY7iL8F',
       * );
       * ```
       */
      resume(fineTuningJobID, options) {
        return this._client.post(path7`/fine_tuning/jobs/${fineTuningJobID}/resume`, {
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
    };
    Jobs.Checkpoints = Checkpoints2;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/fine-tuning/fine-tuning.mjs
var FineTuning, init_fine_tuning = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/fine-tuning/fine-tuning.mjs"() {
    init_resource2();
    init_methods();
    init_methods();
    init_alpha();
    init_alpha();
    init_checkpoints();
    init_checkpoints();
    init_jobs();
    init_jobs();
    FineTuning = class extends APIResource2 {
      constructor() {
        super(...arguments), this.methods = new Methods(this._client), this.jobs = new Jobs(this._client), this.checkpoints = new Checkpoints(this._client), this.alpha = new Alpha(this._client);
      }
    };
    FineTuning.Methods = Methods;
    FineTuning.Jobs = Jobs;
    FineTuning.Checkpoints = Checkpoints;
    FineTuning.Alpha = Alpha;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/graders/grader-models.mjs
var GraderModels, init_grader_models = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/graders/grader-models.mjs"() {
    init_resource2();
    GraderModels = class extends APIResource2 {
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/graders/graders.mjs
var Graders2, init_graders2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/graders/graders.mjs"() {
    init_resource2();
    init_grader_models();
    init_grader_models();
    Graders2 = class extends APIResource2 {
      constructor() {
        super(...arguments), this.graderModels = new GraderModels(this._client);
      }
    };
    Graders2.GraderModels = GraderModels;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/images.mjs
var Images, init_images = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/images.mjs"() {
    init_resource2();
    init_uploads3();
    Images = class extends APIResource2 {
      /**
       * Creates a variation of a given image. This endpoint only supports `dall-e-2`.
       *
       * @example
       * ```ts
       * const imagesResponse = await client.images.createVariation({
       *   image: fs.createReadStream('otter.png'),
       * });
       * ```
       */
      createVariation(body, options) {
        return this._client.post("/images/variations", multipartFormRequestOptions2({ body, ...options, __security: { bearerAuth: !0 } }, this._client));
      }
      edit(body, options) {
        return this._client.post("/images/edits", multipartFormRequestOptions2({ body, ...options, stream: body.stream ?? !1, __security: { bearerAuth: !0 } }, this._client));
      }
      generate(body, options) {
        return this._client.post("/images/generations", {
          body,
          ...options,
          stream: body.stream ?? !1,
          __security: { bearerAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/models.mjs
var Models3, init_models3 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/models.mjs"() {
    init_resource2();
    init_pagination2();
    init_path2();
    Models3 = class extends APIResource2 {
      /**
       * Retrieves a model instance, providing basic information about the model such as
       * the owner and permissioning.
       */
      retrieve(model, options) {
        return this._client.get(path7`/models/${model}`, { ...options, __security: { bearerAuth: !0 } });
      }
      /**
       * Lists the currently available models, and provides basic information about each
       * one such as the owner and availability.
       */
      list(options) {
        return this._client.getAPIList("/models", Page2, { ...options, __security: { bearerAuth: !0 } });
      }
      /**
       * Delete a fine-tuned model. You must have the Owner role in your organization to
       * delete a model.
       */
      delete(model, options) {
        return this._client.delete(path7`/models/${model}`, { ...options, __security: { bearerAuth: !0 } });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/moderations.mjs
var Moderations, init_moderations = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/moderations.mjs"() {
    init_resource2();
    Moderations = class extends APIResource2 {
      /**
       * Classifies if text and/or image inputs are potentially harmful. Learn more in
       * the [moderation guide](https://platform.openai.com/docs/guides/moderation).
       */
      create(body, options) {
        return this._client.post("/moderations", { body, ...options, __security: { bearerAuth: !0 } });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/realtime/calls.mjs
var Calls, init_calls = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/realtime/calls.mjs"() {
    init_resource2();
    init_headers2();
    init_path2();
    Calls = class extends APIResource2 {
      /**
       * Accept an incoming SIP call and configure the realtime session that will handle
       * it.
       *
       * @example
       * ```ts
       * await client.realtime.calls.accept('call_id', {
       *   type: 'realtime',
       * });
       * ```
       */
      accept(callID, body, options) {
        return this._client.post(path7`/realtime/calls/${callID}/accept`, {
          body,
          ...options,
          headers: buildHeaders2([{ Accept: "*/*" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * End an active Realtime API call, whether it was initiated over SIP or WebRTC.
       *
       * @example
       * ```ts
       * await client.realtime.calls.hangup('call_id');
       * ```
       */
      hangup(callID, options) {
        return this._client.post(path7`/realtime/calls/${callID}/hangup`, {
          ...options,
          headers: buildHeaders2([{ Accept: "*/*" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Transfer an active SIP call to a new destination using the SIP REFER verb.
       *
       * @example
       * ```ts
       * await client.realtime.calls.refer('call_id', {
       *   target_uri: 'tel:+14155550123',
       * });
       * ```
       */
      refer(callID, body, options) {
        return this._client.post(path7`/realtime/calls/${callID}/refer`, {
          body,
          ...options,
          headers: buildHeaders2([{ Accept: "*/*" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Decline an incoming SIP call by returning a SIP status code to the caller.
       *
       * @example
       * ```ts
       * await client.realtime.calls.reject('call_id');
       * ```
       */
      reject(callID, body = {}, options) {
        return this._client.post(path7`/realtime/calls/${callID}/reject`, {
          body,
          ...options,
          headers: buildHeaders2([{ Accept: "*/*" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/realtime/client-secrets.mjs
var ClientSecrets, init_client_secrets = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/realtime/client-secrets.mjs"() {
    init_resource2();
    ClientSecrets = class extends APIResource2 {
      /**
       * Create a Realtime client secret with an associated session configuration.
       *
       * Client secrets are short-lived tokens that can be passed to a client app, such
       * as a web frontend or mobile client, which grants access to the Realtime API
       * without leaking your main API key. You can configure a custom TTL for each
       * client secret.
       *
       * You can also attach session configuration options to the client secret, which
       * will be applied to any sessions created using that client secret, but these can
       * also be overridden by the client connection.
       *
       * [Learn more about authentication with client secrets over WebRTC](https://platform.openai.com/docs/guides/realtime-webrtc).
       *
       * Returns the created client secret and the effective session object. The client
       * secret is a string that looks like `ek_1234`.
       *
       * @example
       * ```ts
       * const clientSecret =
       *   await client.realtime.clientSecrets.create();
       * ```
       */
      create(body, options) {
        return this._client.post("/realtime/client_secrets", {
          body,
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/realtime/realtime.mjs
var Realtime2, init_realtime2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/realtime/realtime.mjs"() {
    init_resource2();
    init_calls();
    init_calls();
    init_client_secrets();
    init_client_secrets();
    Realtime2 = class extends APIResource2 {
      constructor() {
        super(...arguments), this.clientSecrets = new ClientSecrets(this._client), this.calls = new Calls(this._client);
      }
    };
    Realtime2.ClientSecrets = ClientSecrets;
    Realtime2.Calls = Calls;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/lib/ResponsesParser.mjs
function maybeParseResponse(response, params) {
  if (!params || !hasAutoParseableInput2(params)) {
    let parsed = {
      ...response,
      output_parsed: null,
      output: response.output.map((item) => item.type === "function_call" ? {
        ...item,
        parsed_arguments: null
      } : item.type === "message" ? {
        ...item,
        content: item.content.map((content) => ({
          ...content,
          parsed: null
        }))
      } : item)
    };
    return needsOutputText(response, parsed) && addOutputText(parsed), parsed;
  }
  return parseResponse(response, params);
}
function parseResponse(response, params) {
  let shouldParse = !response.status || response.status === "completed", output = response.output.map((item) => {
    if (item.type === "function_call")
      return {
        ...item,
        parsed_arguments: shouldParse ? parseToolCall2(params, item) : null
      };
    if (item.type === "message") {
      let content = item.content.map((content2) => content2.type === "output_text" ? {
        ...content2,
        parsed: shouldParse ? parseTextFormat(params, content2.text) : null
      } : content2);
      return {
        ...item,
        content
      };
    }
    return item;
  }), parsed = Object.assign({}, response, { output });
  return needsOutputText(response, parsed) && addOutputText(parsed), Object.defineProperty(parsed, "output_parsed", {
    enumerable: !0,
    get() {
      for (let output2 of parsed.output)
        if (output2.type === "message") {
          for (let content of output2.content)
            if (content.type === "output_text" && content.parsed !== null)
              return content.parsed;
        }
      return null;
    }
  }), parsed;
}
function parseTextFormat(params, content) {
  return params.text?.format?.type !== "json_schema" ? null : "$parseRaw" in params.text?.format ? (params.text?.format).$parseRaw(content) : JSON.parse(content);
}
function hasAutoParseableInput2(params) {
  return !!isAutoParsableResponseFormat(params.text?.format);
}
function isAutoParsableTool2(tool) {
  return tool?.$brand === "auto-parseable-tool";
}
function getInputToolByName(input_tools, name) {
  return input_tools.find((tool) => tool.type === "function" && tool.name === name);
}
function parseToolCall2(params, toolCall) {
  let inputTool = getInputToolByName(params.tools ?? [], toolCall.name);
  return {
    ...toolCall,
    ...toolCall,
    parsed_arguments: isAutoParsableTool2(inputTool) ? inputTool.$parseRaw(toolCall.arguments) : inputTool?.strict ? JSON.parse(toolCall.arguments) : null
  };
}
function needsOutputText(response, target) {
  return !Object.getOwnPropertyDescriptor(response, "output_text") || target.output_text == null;
}
function addOutputText(rsp) {
  let texts = [];
  for (let output of rsp.output)
    if (output.type === "message")
      for (let content of output.content)
        content.type === "output_text" && texts.push(content.text);
  rsp.output_text = texts.join("");
}
var init_ResponsesParser = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/lib/ResponsesParser.mjs"() {
    init_error4();
    init_parser3();
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/lib/responses/ResponseAccumulator.mjs
function accumulateResponse(event, snapshot) {
  if (!snapshot) {
    if (event.type !== "response.created")
      throw new OpenAIError(`When snapshot hasn't been set yet, expected 'response.created' event, got ${event.type}`);
    return cloneResponse(event.response);
  }
  switch (event.type) {
    case "response.output_item.added": {
      snapshot.output.push(structuredClone(event.item)), event.item.type === "message" && addOutputText(snapshot);
      break;
    }
    case "response.output_item.done": {
      getOutput(snapshot, event.output_index), snapshot.output[event.output_index] = structuredClone(event.item), event.item.type === "message" && addOutputText(snapshot);
      break;
    }
    case "response.content_part.added": {
      let output = getOutput(snapshot, event.output_index), type = output.type, part = event.part;
      type === "message" && part.type !== "reasoning_text" ? (output.content.push(structuredClone(part)), part.type === "output_text" && addOutputText(snapshot)) : type === "reasoning" && part.type === "reasoning_text" && (output.content || (output.content = []), output.content.push(structuredClone(part)));
      break;
    }
    case "response.content_part.done": {
      let output = getOutput(snapshot, event.output_index), part = event.part;
      if (output.type === "message" && part.type !== "reasoning_text")
        getContent(output.content, event.content_index), output.content[event.content_index] = structuredClone(part), part.type === "output_text" && addOutputText(snapshot);
      else if (output.type === "reasoning" && part.type === "reasoning_text") {
        let content = output.content;
        if (!content)
          throw new OpenAIError(`missing content at index ${event.content_index}`);
        getContent(content, event.content_index), content[event.content_index] = structuredClone(part);
      }
      break;
    }
    case "response.output_text.delta": {
      let output = getOutput(snapshot, event.output_index);
      if (output.type === "message") {
        let content = getContent(output.content, event.content_index);
        if (content.type !== "output_text")
          throw new OpenAIError(`expected content to be 'output_text', got ${content.type}`);
        content.text += event.delta, snapshot.output_text += event.delta;
      }
      break;
    }
    case "response.output_text.done": {
      let output = getOutput(snapshot, event.output_index);
      if (output.type === "message") {
        let content = getContent(output.content, event.content_index);
        if (content.type !== "output_text")
          throw new OpenAIError(`expected content to be 'output_text', got ${content.type}`);
        content.text = event.text, addOutputText(snapshot);
      }
      break;
    }
    case "response.output_text.annotation.added": {
      let output = getOutput(snapshot, event.output_index);
      if (output.type === "message") {
        let content = getContent(output.content, event.content_index);
        if (content.type !== "output_text")
          throw new OpenAIError(`expected content to be 'output_text', got ${content.type}`);
        content.annotations[event.annotation_index] = structuredClone(event.annotation);
      }
      break;
    }
    case "response.refusal.delta": {
      let output = getOutput(snapshot, event.output_index);
      if (output.type === "message") {
        let content = getContent(output.content, event.content_index);
        if (content.type !== "refusal")
          throw new OpenAIError(`expected content to be 'refusal', got ${content.type}`);
        content.refusal += event.delta;
      }
      break;
    }
    case "response.refusal.done": {
      let output = getOutput(snapshot, event.output_index);
      if (output.type === "message") {
        let content = getContent(output.content, event.content_index);
        if (content.type !== "refusal")
          throw new OpenAIError(`expected content to be 'refusal', got ${content.type}`);
        content.refusal = event.refusal;
      }
      break;
    }
    case "response.function_call_arguments.delta": {
      let output = getOutput(snapshot, event.output_index);
      output.type === "function_call" && (output.arguments += event.delta);
      break;
    }
    case "response.function_call_arguments.done": {
      let output = getOutput(snapshot, event.output_index);
      output.type === "function_call" && (output.arguments = event.arguments);
      break;
    }
    case "response.reasoning_text.delta": {
      let output = getOutput(snapshot, event.output_index);
      if (output.type === "reasoning") {
        if (!output.content)
          throw new OpenAIError(`missing content at index ${event.content_index}`);
        let content = getContent(output.content, event.content_index);
        if (content.type !== "reasoning_text")
          throw new OpenAIError(`expected content to be 'reasoning_text', got ${content.type}`);
        content.text += event.delta;
      }
      break;
    }
    case "response.reasoning_text.done": {
      let output = getOutput(snapshot, event.output_index);
      if (output.type === "reasoning") {
        if (!output.content)
          throw new OpenAIError(`missing content at index ${event.content_index}`);
        let content = getContent(output.content, event.content_index);
        if (content.type !== "reasoning_text")
          throw new OpenAIError(`expected content to be 'reasoning_text', got ${content.type}`);
        content.text = event.text;
      }
      break;
    }
    case "response.reasoning_summary_part.added": {
      let output = getOutput(snapshot, event.output_index);
      output.type === "reasoning" && output.summary.push(structuredClone(event.part));
      break;
    }
    case "response.reasoning_summary_part.done": {
      let output = getOutput(snapshot, event.output_index);
      output.type === "reasoning" && (getContent(output.summary, event.summary_index), output.summary[event.summary_index] = structuredClone(event.part));
      break;
    }
    case "response.reasoning_summary_text.delta": {
      let output = getOutput(snapshot, event.output_index);
      if (output.type === "reasoning") {
        let part = getContent(output.summary, event.summary_index);
        part.text += event.delta;
      }
      break;
    }
    case "response.reasoning_summary_text.done": {
      let output = getOutput(snapshot, event.output_index);
      if (output.type === "reasoning") {
        let part = getContent(output.summary, event.summary_index);
        part.text = event.text;
      }
      break;
    }
    case "response.custom_tool_call_input.delta": {
      let output = getOutput(snapshot, event.output_index);
      output.type === "custom_tool_call" && (output.input += event.delta);
      break;
    }
    case "response.custom_tool_call_input.done": {
      let output = getOutput(snapshot, event.output_index);
      output.type === "custom_tool_call" && (output.input = event.input);
      break;
    }
    case "response.mcp_call_arguments.delta": {
      let output = getOutput(snapshot, event.output_index);
      output.type === "mcp_call" && (output.arguments += event.delta);
      break;
    }
    case "response.mcp_call_arguments.done": {
      let output = getOutput(snapshot, event.output_index);
      output.type === "mcp_call" && (output.arguments = event.arguments);
      break;
    }
    case "response.code_interpreter_call_code.delta": {
      let output = getOutput(snapshot, event.output_index);
      output.type === "code_interpreter_call" && (output.code = (output.code ?? "") + event.delta);
      break;
    }
    case "response.code_interpreter_call_code.done": {
      let output = getOutput(snapshot, event.output_index);
      output.type === "code_interpreter_call" && (output.code = event.code);
      break;
    }
    case "response.code_interpreter_call.in_progress": {
      let output = getOutput(snapshot, event.output_index);
      output.type === "code_interpreter_call" && (output.status = "in_progress");
      break;
    }
    case "response.code_interpreter_call.interpreting": {
      let output = getOutput(snapshot, event.output_index);
      output.type === "code_interpreter_call" && (output.status = "interpreting");
      break;
    }
    case "response.code_interpreter_call.completed": {
      let output = getOutput(snapshot, event.output_index);
      output.type === "code_interpreter_call" && (output.status = "completed");
      break;
    }
    case "response.file_search_call.in_progress": {
      let output = getOutput(snapshot, event.output_index);
      output.type === "file_search_call" && (output.status = "in_progress");
      break;
    }
    case "response.file_search_call.searching": {
      let output = getOutput(snapshot, event.output_index);
      output.type === "file_search_call" && (output.status = "searching");
      break;
    }
    case "response.file_search_call.completed": {
      let output = getOutput(snapshot, event.output_index);
      output.type === "file_search_call" && (output.status = "completed");
      break;
    }
    case "response.web_search_call.in_progress": {
      let output = getOutput(snapshot, event.output_index);
      output.type === "web_search_call" && (output.status = "in_progress");
      break;
    }
    case "response.web_search_call.searching": {
      let output = getOutput(snapshot, event.output_index);
      output.type === "web_search_call" && (output.status = "searching");
      break;
    }
    case "response.web_search_call.completed": {
      let output = getOutput(snapshot, event.output_index);
      output.type === "web_search_call" && (output.status = "completed");
      break;
    }
    case "response.image_generation_call.in_progress": {
      let output = getOutput(snapshot, event.output_index);
      output.type === "image_generation_call" && (output.status = "in_progress");
      break;
    }
    case "response.image_generation_call.generating": {
      let output = getOutput(snapshot, event.output_index);
      output.type === "image_generation_call" && (output.status = "generating");
      break;
    }
    case "response.image_generation_call.completed": {
      let output = getOutput(snapshot, event.output_index);
      output.type === "image_generation_call" && (output.status = "completed");
      break;
    }
    case "response.mcp_call.in_progress": {
      let output = getOutput(snapshot, event.output_index);
      output.type === "mcp_call" && (output.status = "in_progress");
      break;
    }
    case "response.mcp_call.completed": {
      let output = getOutput(snapshot, event.output_index);
      output.type === "mcp_call" && (output.status = "completed");
      break;
    }
    case "response.mcp_call.failed": {
      let output = getOutput(snapshot, event.output_index);
      output.type === "mcp_call" && (output.status = "failed");
      break;
    }
    case "response.created":
    case "response.queued":
    case "response.in_progress":
    case "response.completed":
    case "response.failed":
    case "response.incomplete": {
      snapshot = cloneResponse(event.response);
      break;
    }
    case "response.audio.delta":
    case "response.audio.done":
    case "response.audio.transcript.delta":
    case "response.audio.transcript.done":
    case "response.image_generation_call.partial_image":
    case "response.mcp_list_tools.in_progress":
    case "response.mcp_list_tools.completed":
    case "response.mcp_list_tools.failed":
    case "error":
      break;
    default:
      assertNever(event);
  }
  return snapshot;
}
function cloneResponse(response) {
  let snapshot = structuredClone(response);
  return (!Object.getOwnPropertyDescriptor(snapshot, "output_text") || snapshot.output_text == null) && addOutputText(snapshot), snapshot;
}
function getOutput(snapshot, outputIndex) {
  let output = snapshot.output[outputIndex];
  if (!output)
    throw new OpenAIError(`missing output at index ${outputIndex}`);
  return output;
}
function getContent(content, contentIndex) {
  let part = content[contentIndex];
  if (!part)
    throw new OpenAIError(`missing content at index ${contentIndex}`);
  return part;
}
function assertNever(value) {
  throw new OpenAIError(`Unhandled response stream event: ${JSON.stringify(value)}`);
}
var init_ResponseAccumulator = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/lib/responses/ResponseAccumulator.mjs"() {
    init_error4();
    init_ResponsesParser();
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/lib/responses/ResponseStream.mjs
function finalizeResponse(snapshot, params) {
  return maybeParseResponse(snapshot, params);
}
var _ResponseStream_instances, _ResponseStream_params, _ResponseStream_currentResponseSnapshot, _ResponseStream_finalResponse, _ResponseStream_beginRequest, _ResponseStream_addEvent, _ResponseStream_endRequest, ResponseStream, init_ResponseStream = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/lib/responses/ResponseStream.mjs"() {
    init_tslib2();
    init_error4();
    init_EventStream();
    init_ResponseAccumulator();
    init_ResponsesParser();
    ResponseStream = class _ResponseStream extends EventStream {
      constructor(params) {
        super(), _ResponseStream_instances.add(this), _ResponseStream_params.set(this, void 0), _ResponseStream_currentResponseSnapshot.set(this, void 0), _ResponseStream_finalResponse.set(this, void 0), __classPrivateFieldSet2(this, _ResponseStream_params, params, "f");
      }
      static createResponse(client, params, options) {
        let runner = new _ResponseStream(params);
        return runner._run(() => runner._createOrRetrieveResponse(client, params, {
          ...options,
          headers: { ...options?.headers, "X-Stainless-Helper-Method": "stream" }
        })), runner;
      }
      async _createOrRetrieveResponse(client, params, options) {
        this._listenForAbort(options?.signal), __classPrivateFieldGet2(this, _ResponseStream_instances, "m", _ResponseStream_beginRequest).call(this);
        let stream, starting_after = null;
        "response_id" in params ? (stream = await client.responses.retrieve(params.response_id, { stream: !0 }, { ...options, signal: this.controller.signal, stream: !0 }), starting_after = params.starting_after ?? null) : stream = await client.responses.create({ ...params, stream: !0 }, { ...options, signal: this.controller.signal }), this._connected();
        for await (let event of stream)
          __classPrivateFieldGet2(this, _ResponseStream_instances, "m", _ResponseStream_addEvent).call(this, event, starting_after);
        if (stream.controller.signal?.aborted)
          throw new APIUserAbortError2();
        return __classPrivateFieldGet2(this, _ResponseStream_instances, "m", _ResponseStream_endRequest).call(this);
      }
      [(_ResponseStream_params = /* @__PURE__ */ new WeakMap(), _ResponseStream_currentResponseSnapshot = /* @__PURE__ */ new WeakMap(), _ResponseStream_finalResponse = /* @__PURE__ */ new WeakMap(), _ResponseStream_instances = /* @__PURE__ */ new WeakSet(), _ResponseStream_beginRequest = function() {
        this.ended || __classPrivateFieldSet2(this, _ResponseStream_currentResponseSnapshot, void 0, "f");
      }, _ResponseStream_addEvent = function(event, starting_after) {
        if (this.ended)
          return;
        let maybeEmit = (name, event2) => {
          (starting_after == null || event2.sequence_number > starting_after) && this._emit(name, event2);
        }, response = accumulateResponse(event, __classPrivateFieldGet2(this, _ResponseStream_currentResponseSnapshot, "f"));
        switch (__classPrivateFieldSet2(this, _ResponseStream_currentResponseSnapshot, response, "f"), maybeEmit("event", event), event.type) {
          case "response.output_text.delta": {
            let output = response.output[event.output_index];
            if (!output)
              throw new OpenAIError(`missing output at index ${event.output_index}`);
            if (output.type === "message") {
              let content = output.content[event.content_index];
              if (!content)
                throw new OpenAIError(`missing content at index ${event.content_index}`);
              if (content.type !== "output_text")
                throw new OpenAIError(`expected content to be 'output_text', got ${content.type}`);
              maybeEmit("response.output_text.delta", {
                ...event,
                snapshot: content.text
              });
            }
            break;
          }
          case "response.function_call_arguments.delta": {
            let output = response.output[event.output_index];
            if (!output)
              throw new OpenAIError(`missing output at index ${event.output_index}`);
            output.type === "function_call" && maybeEmit("response.function_call_arguments.delta", {
              ...event,
              snapshot: output.arguments
            });
            break;
          }
          default:
            maybeEmit(event.type, event);
            break;
        }
      }, _ResponseStream_endRequest = function() {
        if (this.ended)
          throw new OpenAIError("stream has ended, this shouldn't happen");
        let snapshot = __classPrivateFieldGet2(this, _ResponseStream_currentResponseSnapshot, "f");
        if (!snapshot)
          throw new OpenAIError("request ended without sending any events");
        __classPrivateFieldSet2(this, _ResponseStream_currentResponseSnapshot, void 0, "f");
        let parsedResponse = finalizeResponse(snapshot, __classPrivateFieldGet2(this, _ResponseStream_params, "f"));
        return __classPrivateFieldSet2(this, _ResponseStream_finalResponse, parsedResponse, "f"), parsedResponse;
      }, Symbol.asyncIterator)]() {
        let pushQueue = [], readQueue = [], done = !1;
        return this.on("event", (event) => {
          let reader = readQueue.shift();
          reader ? reader.resolve(event) : pushQueue.push(event);
        }), this.on("end", () => {
          done = !0;
          for (let reader of readQueue)
            reader.resolve(void 0);
          readQueue.length = 0;
        }), this.on("abort", (err) => {
          done = !0;
          for (let reader of readQueue)
            reader.reject(err);
          readQueue.length = 0;
        }), this.on("error", (err) => {
          done = !0;
          for (let reader of readQueue)
            reader.reject(err);
          readQueue.length = 0;
        }), {
          next: async () => pushQueue.length ? { value: pushQueue.shift(), done: !1 } : done ? { value: void 0, done: !0 } : new Promise((resolve4, reject) => readQueue.push({ resolve: resolve4, reject })).then((event2) => event2 ? { value: event2, done: !1 } : { value: void 0, done: !0 }),
          return: async () => (this.abort(), { value: void 0, done: !0 })
        };
      }
      /**
       * @returns a promise that resolves with the final Response, or rejects
       * if an error occurred or the stream ended prematurely without producing a REsponse.
       */
      async finalResponse() {
        await this.done();
        let response = __classPrivateFieldGet2(this, _ResponseStream_finalResponse, "f");
        if (!response)
          throw new OpenAIError("stream ended without producing a ChatCompletion");
        return response;
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/responses/input-items.mjs
var InputItems, init_input_items = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/responses/input-items.mjs"() {
    init_resource2();
    init_pagination2();
    init_path2();
    InputItems = class extends APIResource2 {
      /**
       * Returns a list of input items for a given response.
       *
       * @example
       * ```ts
       * // Automatically fetches more pages as needed.
       * for await (const responseItem of client.responses.inputItems.list(
       *   'response_id',
       * )) {
       *   // ...
       * }
       * ```
       */
      list(responseID, query = {}, options) {
        return this._client.getAPIList(path7`/responses/${responseID}/input_items`, CursorPage, { query, ...options, __security: { bearerAuth: !0 } });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/responses/input-tokens.mjs
var InputTokens, init_input_tokens = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/responses/input-tokens.mjs"() {
    init_resource2();
    InputTokens = class extends APIResource2 {
      /**
       * Returns input token counts of the request.
       *
       * Returns an object with `object` set to `response.input_tokens` and an
       * `input_tokens` count.
       *
       * @example
       * ```ts
       * const response = await client.responses.inputTokens.count();
       * ```
       */
      count(body = {}, options) {
        return this._client.post("/responses/input_tokens", {
          body,
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/responses/responses.mjs
var Responses, init_responses = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/responses/responses.mjs"() {
    init_ResponsesParser();
    init_ResponseStream();
    init_resource2();
    init_input_items();
    init_input_items();
    init_input_tokens();
    init_input_tokens();
    init_headers2();
    init_path2();
    Responses = class extends APIResource2 {
      constructor() {
        super(...arguments), this.inputItems = new InputItems(this._client), this.inputTokens = new InputTokens(this._client);
      }
      create(body, options) {
        return this._client.post("/responses", {
          body,
          ...options,
          stream: body.stream ?? !1,
          __security: { bearerAuth: !0 }
        })._thenUnwrap((rsp) => ("object" in rsp && rsp.object === "response" && addOutputText(rsp), rsp));
      }
      retrieve(responseID, query = {}, options) {
        return this._client.get(path7`/responses/${responseID}`, {
          query,
          ...options,
          stream: query?.stream ?? !1,
          __security: { bearerAuth: !0 }
        })._thenUnwrap((rsp) => ("object" in rsp && rsp.object === "response" && addOutputText(rsp), rsp));
      }
      /**
       * Deletes a model response with the given ID.
       *
       * @example
       * ```ts
       * await client.responses.delete(
       *   'resp_677efb5139a88190b512bc3fef8e535d',
       * );
       * ```
       */
      delete(responseID, options) {
        return this._client.delete(path7`/responses/${responseID}`, {
          ...options,
          headers: buildHeaders2([{ Accept: "*/*" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      parse(body, options) {
        return this._client.responses.create(body, options)._thenUnwrap((response) => parseResponse(response, body));
      }
      /**
       * Creates a model response stream
       */
      stream(body, options) {
        return ResponseStream.createResponse(this._client, body, options);
      }
      /**
       * Cancels a model response with the given ID. Only responses created with the
       * `background` parameter set to `true` can be cancelled.
       * [Learn more](https://platform.openai.com/docs/guides/background).
       *
       * @example
       * ```ts
       * const response = await client.responses.cancel(
       *   'resp_677efb5139a88190b512bc3fef8e535d',
       * );
       * ```
       */
      cancel(responseID, options) {
        return this._client.post(path7`/responses/${responseID}/cancel`, {
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Compact a conversation. Returns a compacted response object.
       *
       * Learn when and how to compact long-running conversations in the
       * [conversation state guide](https://platform.openai.com/docs/guides/conversation-state#managing-the-context-window).
       * For ZDR-compatible compaction details, see
       * [Compaction (advanced)](https://platform.openai.com/docs/guides/conversation-state#compaction-advanced).
       *
       * @example
       * ```ts
       * const compactedResponse = await client.responses.compact({
       *   model: 'gpt-5.4',
       * });
       * ```
       */
      compact(body, options) {
        return this._client.post("/responses/compact", { body, ...options, __security: { bearerAuth: !0 } });
      }
    };
    Responses.InputItems = InputItems;
    Responses.InputTokens = InputTokens;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/skills/content.mjs
var Content2, init_content2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/skills/content.mjs"() {
    init_resource2();
    init_headers2();
    init_path2();
    Content2 = class extends APIResource2 {
      /**
       * Download a skill zip bundle by its ID.
       */
      retrieve(skillID, options) {
        return this._client.get(path7`/skills/${skillID}/content`, {
          ...options,
          headers: buildHeaders2([{ Accept: "application/binary" }, options?.headers]),
          __security: { bearerAuth: !0 },
          __binaryResponse: !0
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/skills/versions/content.mjs
var Content3, init_content3 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/skills/versions/content.mjs"() {
    init_resource2();
    init_headers2();
    init_path2();
    Content3 = class extends APIResource2 {
      /**
       * Download a skill version zip bundle.
       */
      retrieve(version, params, options) {
        let { skill_id } = params;
        return this._client.get(path7`/skills/${skill_id}/versions/${version}/content`, {
          ...options,
          headers: buildHeaders2([{ Accept: "application/binary" }, options?.headers]),
          __security: { bearerAuth: !0 },
          __binaryResponse: !0
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/skills/versions/versions.mjs
var Versions3, init_versions3 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/skills/versions/versions.mjs"() {
    init_resource2();
    init_content3();
    init_content3();
    init_pagination2();
    init_uploads3();
    init_path2();
    Versions3 = class extends APIResource2 {
      constructor() {
        super(...arguments), this.content = new Content3(this._client);
      }
      /**
       * Create a new immutable skill version.
       */
      create(skillID, body = {}, options) {
        return this._client.post(path7`/skills/${skillID}/versions`, maybeMultipartFormRequestOptions({ body, ...options, __security: { bearerAuth: !0 } }, this._client));
      }
      /**
       * Get a specific skill version.
       */
      retrieve(version, params, options) {
        let { skill_id } = params;
        return this._client.get(path7`/skills/${skill_id}/versions/${version}`, {
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * List skill versions for a skill.
       */
      list(skillID, query = {}, options) {
        return this._client.getAPIList(path7`/skills/${skillID}/versions`, CursorPage, {
          query,
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Delete a skill version.
       */
      delete(version, params, options) {
        let { skill_id } = params;
        return this._client.delete(path7`/skills/${skill_id}/versions/${version}`, {
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
    };
    Versions3.Content = Content3;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/skills/skills.mjs
var Skills2, init_skills3 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/skills/skills.mjs"() {
    init_resource2();
    init_content2();
    init_content2();
    init_versions3();
    init_versions3();
    init_pagination2();
    init_uploads3();
    init_path2();
    Skills2 = class extends APIResource2 {
      constructor() {
        super(...arguments), this.content = new Content2(this._client), this.versions = new Versions3(this._client);
      }
      /**
       * Create a new skill.
       */
      create(body = {}, options) {
        return this._client.post("/skills", maybeMultipartFormRequestOptions({ body, ...options, __security: { bearerAuth: !0 } }, this._client));
      }
      /**
       * Get a skill by its ID.
       */
      retrieve(skillID, options) {
        return this._client.get(path7`/skills/${skillID}`, { ...options, __security: { bearerAuth: !0 } });
      }
      /**
       * Update the default version pointer for a skill.
       */
      update(skillID, body, options) {
        return this._client.post(path7`/skills/${skillID}`, {
          body,
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * List all skills for the current project.
       */
      list(query = {}, options) {
        return this._client.getAPIList("/skills", CursorPage, {
          query,
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Delete a skill by its ID.
       */
      delete(skillID, options) {
        return this._client.delete(path7`/skills/${skillID}`, { ...options, __security: { bearerAuth: !0 } });
      }
    };
    Skills2.Content = Content2;
    Skills2.Versions = Versions3;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/uploads/parts.mjs
var Parts, init_parts = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/uploads/parts.mjs"() {
    init_resource2();
    init_uploads3();
    init_path2();
    Parts = class extends APIResource2 {
      /**
       * Adds a
       * [Part](https://platform.openai.com/docs/api-reference/uploads/part-object) to an
       * [Upload](https://platform.openai.com/docs/api-reference/uploads/object) object.
       * A Part represents a chunk of bytes from the file you are trying to upload.
       *
       * Each Part can be at most 64 MB, and you can add Parts until you hit the Upload
       * maximum of 8 GB.
       *
       * It is possible to add multiple Parts in parallel. You can decide the intended
       * order of the Parts when you
       * [complete the Upload](https://platform.openai.com/docs/api-reference/uploads/complete).
       */
      create(uploadID, body, options) {
        return this._client.post(path7`/uploads/${uploadID}/parts`, multipartFormRequestOptions2({ body, ...options, __security: { bearerAuth: !0 } }, this._client));
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/uploads/uploads.mjs
var Uploads, init_uploads5 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/uploads/uploads.mjs"() {
    init_resource2();
    init_parts();
    init_parts();
    init_path2();
    Uploads = class extends APIResource2 {
      constructor() {
        super(...arguments), this.parts = new Parts(this._client);
      }
      /**
       * Creates an intermediate
       * [Upload](https://platform.openai.com/docs/api-reference/uploads/object) object
       * that you can add
       * [Parts](https://platform.openai.com/docs/api-reference/uploads/part-object) to.
       * Currently, an Upload can accept at most 8 GB in total and expires after an hour
       * after you create it.
       *
       * Once you complete the Upload, we will create a
       * [File](https://platform.openai.com/docs/api-reference/files/object) object that
       * contains all the parts you uploaded. This File is usable in the rest of our
       * platform as a regular File object.
       *
       * For certain `purpose` values, the correct `mime_type` must be specified. Please
       * refer to documentation for the
       * [supported MIME types for your use case](https://platform.openai.com/docs/assistants/tools/file-search#supported-files).
       *
       * For guidance on the proper filename extensions for each purpose, please follow
       * the documentation on
       * [creating a File](https://platform.openai.com/docs/api-reference/files/create).
       *
       * Returns the Upload object with status `pending`.
       */
      create(body, options) {
        return this._client.post("/uploads", { body, ...options, __security: { bearerAuth: !0 } });
      }
      /**
       * Cancels the Upload. No Parts may be added after an Upload is cancelled.
       *
       * Returns the Upload object with status `cancelled`.
       */
      cancel(uploadID, options) {
        return this._client.post(path7`/uploads/${uploadID}/cancel`, {
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Completes the
       * [Upload](https://platform.openai.com/docs/api-reference/uploads/object).
       *
       * Within the returned Upload object, there is a nested
       * [File](https://platform.openai.com/docs/api-reference/files/object) object that
       * is ready to use in the rest of the platform.
       *
       * You can specify the order of the Parts by passing in an ordered list of the Part
       * IDs.
       *
       * The number of bytes uploaded upon completion must match the number of bytes
       * initially specified when creating the Upload object. No Parts may be added after
       * an Upload is completed. Returns the Upload object with status `completed`,
       * including an additional `file` property containing the created usable File
       * object.
       */
      complete(uploadID, body, options) {
        return this._client.post(path7`/uploads/${uploadID}/complete`, {
          body,
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
    };
    Uploads.Parts = Parts;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/lib/Util.mjs
var allSettledWithThrow, init_Util = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/lib/Util.mjs"() {
    allSettledWithThrow = async (promises) => {
      let results = await Promise.allSettled(promises), rejected = results.filter((result) => result.status === "rejected");
      if (rejected.length) {
        for (let result of rejected)
          console.error(result.reason);
        throw new Error(`${rejected.length} promise(s) failed - see the above errors`);
      }
      let values = [];
      for (let result of results)
        result.status === "fulfilled" && values.push(result.value);
      return values;
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/vector-stores/file-batches.mjs
var FileBatches, init_file_batches = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/vector-stores/file-batches.mjs"() {
    init_resource2();
    init_pagination2();
    init_headers2();
    init_sleep2();
    init_Util();
    init_path2();
    FileBatches = class extends APIResource2 {
      /**
       * Create a vector store file batch.
       */
      create(vectorStoreID, body, options) {
        return this._client.post(path7`/vector_stores/${vectorStoreID}/file_batches`, {
          body,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Retrieves a vector store file batch.
       */
      retrieve(batchID, params, options) {
        let { vector_store_id } = params;
        return this._client.get(path7`/vector_stores/${vector_store_id}/file_batches/${batchID}`, {
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Cancel a vector store file batch. This attempts to cancel the processing of
       * files in this batch as soon as possible.
       */
      cancel(batchID, params, options) {
        let { vector_store_id } = params;
        return this._client.post(path7`/vector_stores/${vector_store_id}/file_batches/${batchID}/cancel`, {
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Create a vector store batch and poll until all files have been processed.
       */
      async createAndPoll(vectorStoreId, body, options) {
        let batch = await this.create(vectorStoreId, body);
        return await this.poll(vectorStoreId, batch.id, options);
      }
      /**
       * Returns a list of vector store files in a batch.
       */
      listFiles(batchID, params, options) {
        let { vector_store_id, ...query } = params;
        return this._client.getAPIList(path7`/vector_stores/${vector_store_id}/file_batches/${batchID}/files`, CursorPage, {
          query,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Wait for the given file batch to be processed.
       *
       * Note: this will return even if one of the files failed to process, you need to
       * check batch.file_counts.failed_count to handle this case.
       */
      async poll(vectorStoreID, batchID, options) {
        let headers = buildHeaders2([
          options?.headers,
          {
            "X-Stainless-Poll-Helper": "true",
            "X-Stainless-Custom-Poll-Interval": options?.pollIntervalMs?.toString() ?? void 0
          }
        ]);
        for (; ; ) {
          let { data: batch, response } = await this.retrieve(batchID, { vector_store_id: vectorStoreID }, {
            ...options,
            headers
          }).withResponse();
          switch (batch.status) {
            case "in_progress":
              let sleepInterval = 5e3;
              if (options?.pollIntervalMs)
                sleepInterval = options.pollIntervalMs;
              else {
                let headerInterval = response.headers.get("openai-poll-after-ms");
                if (headerInterval) {
                  let headerIntervalMs = parseInt(headerInterval);
                  isNaN(headerIntervalMs) || (sleepInterval = headerIntervalMs);
                }
              }
              await sleep2(sleepInterval);
              break;
            case "failed":
            case "cancelled":
            case "completed":
              return batch;
          }
        }
      }
      /**
       * Uploads the given files concurrently and then creates a vector store file batch.
       *
       * The concurrency limit is configurable using the `maxConcurrency` parameter.
       */
      async uploadAndPoll(vectorStoreId, { files, fileIds = [] }, options) {
        if (files == null || files.length == 0)
          throw new Error("No `files` provided to process. If you've already uploaded files you should use `.createAndPoll()` instead");
        let configuredConcurrency = options?.maxConcurrency ?? 5, concurrencyLimit = Math.min(configuredConcurrency, files.length), client = this._client, fileIterator = files.values(), allFileIds = [...fileIds];
        async function processFiles(iterator) {
          for (let item of iterator) {
            let fileObj = await client.files.create({ file: item, purpose: "assistants" }, options);
            allFileIds.push(fileObj.id);
          }
        }
        let workers = Array(concurrencyLimit).fill(fileIterator).map(processFiles);
        return await allSettledWithThrow(workers), await this.createAndPoll(vectorStoreId, {
          file_ids: allFileIds
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/vector-stores/files.mjs
var Files4, init_files4 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/vector-stores/files.mjs"() {
    init_resource2();
    init_pagination2();
    init_headers2();
    init_utils4();
    init_path2();
    Files4 = class extends APIResource2 {
      /**
       * Create a vector store file by attaching a
       * [File](https://platform.openai.com/docs/api-reference/files) to a
       * [vector store](https://platform.openai.com/docs/api-reference/vector-stores/object).
       */
      create(vectorStoreID, body, options) {
        return this._client.post(path7`/vector_stores/${vectorStoreID}/files`, {
          body,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Retrieves a vector store file.
       */
      retrieve(fileID, params, options) {
        let { vector_store_id } = params;
        return this._client.get(path7`/vector_stores/${vector_store_id}/files/${fileID}`, {
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Update attributes on a vector store file.
       */
      update(fileID, params, options) {
        let { vector_store_id, ...body } = params;
        return this._client.post(path7`/vector_stores/${vector_store_id}/files/${fileID}`, {
          body,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Returns a list of vector store files.
       */
      list(vectorStoreID, query = {}, options) {
        return this._client.getAPIList(path7`/vector_stores/${vectorStoreID}/files`, CursorPage, {
          query,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Delete a vector store file. This will remove the file from the vector store but
       * the file itself will not be deleted. To delete the file, use the
       * [delete file](https://platform.openai.com/docs/api-reference/files/delete)
       * endpoint.
       */
      delete(fileID, params, options) {
        let { vector_store_id } = params;
        return this._client.delete(path7`/vector_stores/${vector_store_id}/files/${fileID}`, {
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Attach a file to the given vector store and wait for it to be processed.
       */
      async createAndPoll(vectorStoreId, body, options) {
        let file = await this.create(vectorStoreId, body, options);
        return await this.poll(vectorStoreId, file.id, options);
      }
      /**
       * Wait for the vector store file to finish processing.
       *
       * Note: this will return even if the file failed to process, you need to check
       * file.last_error and file.status to handle these cases
       */
      async poll(vectorStoreID, fileID, options) {
        let headers = buildHeaders2([
          options?.headers,
          {
            "X-Stainless-Poll-Helper": "true",
            "X-Stainless-Custom-Poll-Interval": options?.pollIntervalMs?.toString() ?? void 0
          }
        ]);
        for (; ; ) {
          let fileResponse = await this.retrieve(fileID, {
            vector_store_id: vectorStoreID
          }, { ...options, headers }).withResponse(), file = fileResponse.data;
          switch (file.status) {
            case "in_progress":
              let sleepInterval = 5e3;
              if (options?.pollIntervalMs)
                sleepInterval = options.pollIntervalMs;
              else {
                let headerInterval = fileResponse.response.headers.get("openai-poll-after-ms");
                if (headerInterval) {
                  let headerIntervalMs = parseInt(headerInterval);
                  isNaN(headerIntervalMs) || (sleepInterval = headerIntervalMs);
                }
              }
              await sleep2(sleepInterval);
              break;
            case "failed":
            case "completed":
              return file;
          }
        }
      }
      /**
       * Upload a file to the `files` API and then attach it to the given vector store.
       *
       * Note the file will be asynchronously processed (you can use the alternative
       * polling helper method to wait for processing to complete).
       */
      async upload(vectorStoreId, file, options) {
        let fileInfo = await this._client.files.create({ file, purpose: "assistants" }, options);
        return this.create(vectorStoreId, { file_id: fileInfo.id }, options);
      }
      /**
       * Add a file to a vector store and poll until processing is complete.
       */
      async uploadAndPoll(vectorStoreId, file, options) {
        let fileInfo = await this.upload(vectorStoreId, file, options);
        return await this.poll(vectorStoreId, fileInfo.id, options);
      }
      /**
       * Retrieve the parsed contents of a vector store file.
       */
      content(fileID, params, options) {
        let { vector_store_id } = params;
        return this._client.getAPIList(path7`/vector_stores/${vector_store_id}/files/${fileID}/content`, Page2, {
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/vector-stores/vector-stores.mjs
var VectorStores, init_vector_stores = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/vector-stores/vector-stores.mjs"() {
    init_resource2();
    init_file_batches();
    init_file_batches();
    init_files4();
    init_files4();
    init_pagination2();
    init_headers2();
    init_path2();
    VectorStores = class extends APIResource2 {
      constructor() {
        super(...arguments), this.files = new Files4(this._client), this.fileBatches = new FileBatches(this._client);
      }
      /**
       * Create a vector store.
       */
      create(body, options) {
        return this._client.post("/vector_stores", {
          body,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Retrieves a vector store.
       */
      retrieve(vectorStoreID, options) {
        return this._client.get(path7`/vector_stores/${vectorStoreID}`, {
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Modifies a vector store.
       */
      update(vectorStoreID, body, options) {
        return this._client.post(path7`/vector_stores/${vectorStoreID}`, {
          body,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Returns a list of vector stores.
       */
      list(query = {}, options) {
        return this._client.getAPIList("/vector_stores", CursorPage, {
          query,
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Delete a vector store.
       */
      delete(vectorStoreID, options) {
        return this._client.delete(path7`/vector_stores/${vectorStoreID}`, {
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Search a vector store for relevant chunks based on a query and file attributes
       * filter.
       */
      search(vectorStoreID, body, options) {
        return this._client.getAPIList(path7`/vector_stores/${vectorStoreID}/search`, Page2, {
          body,
          method: "post",
          ...options,
          headers: buildHeaders2([{ "OpenAI-Beta": "assistants=v2" }, options?.headers]),
          __security: { bearerAuth: !0 }
        });
      }
    };
    VectorStores.Files = Files4;
    VectorStores.FileBatches = FileBatches;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/videos.mjs
var Videos, init_videos = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/videos.mjs"() {
    init_resource2();
    init_pagination2();
    init_headers2();
    init_uploads3();
    init_path2();
    Videos = class extends APIResource2 {
      /**
       * Create a new video generation job from a prompt and optional reference assets.
       */
      create(body, options) {
        return this._client.post("/videos", multipartFormRequestOptions2({ body, ...options, __security: { bearerAuth: !0 } }, this._client));
      }
      /**
       * Fetch the latest metadata for a generated video.
       */
      retrieve(videoID, options) {
        return this._client.get(path7`/videos/${videoID}`, { ...options, __security: { bearerAuth: !0 } });
      }
      /**
       * List recently generated videos for the current project.
       */
      list(query = {}, options) {
        return this._client.getAPIList("/videos", ConversationCursorPage, {
          query,
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Permanently delete a completed or failed video and its stored assets.
       */
      delete(videoID, options) {
        return this._client.delete(path7`/videos/${videoID}`, { ...options, __security: { bearerAuth: !0 } });
      }
      /**
       * Create a character from an uploaded video.
       */
      createCharacter(body, options) {
        return this._client.post("/videos/characters", multipartFormRequestOptions2({ body, ...options, __security: { bearerAuth: !0 } }, this._client));
      }
      /**
       * Download the generated video bytes or a derived preview asset.
       *
       * Streams the rendered video content for the specified video job.
       */
      downloadContent(videoID, query = {}, options) {
        return this._client.get(path7`/videos/${videoID}/content`, {
          query,
          ...options,
          headers: buildHeaders2([{ Accept: "application/binary" }, options?.headers]),
          __security: { bearerAuth: !0 },
          __binaryResponse: !0
        });
      }
      /**
       * Create a new video generation job by editing a source video or existing
       * generated video.
       */
      edit(body, options) {
        return this._client.post("/videos/edits", multipartFormRequestOptions2({ body, ...options, __security: { bearerAuth: !0 } }, this._client));
      }
      /**
       * Create an extension of a completed video.
       */
      extend(body, options) {
        return this._client.post("/videos/extensions", multipartFormRequestOptions2({ body, ...options, __security: { bearerAuth: !0 } }, this._client));
      }
      /**
       * Fetch a character.
       */
      getCharacter(characterID, options) {
        return this._client.get(path7`/videos/characters/${characterID}`, {
          ...options,
          __security: { bearerAuth: !0 }
        });
      }
      /**
       * Create a remix of a completed video using a refreshed prompt.
       */
      remix(videoID, body, options) {
        return this._client.post(path7`/videos/${videoID}/remix`, maybeMultipartFormRequestOptions({ body, ...options, __security: { bearerAuth: !0 } }, this._client));
      }
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/webhooks/webhooks.mjs
var _Webhooks_instances, _Webhooks_validateSecret, _Webhooks_getRequiredHeader, Webhooks2, init_webhooks2 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/webhooks/webhooks.mjs"() {
    init_tslib2();
    init_error4();
    init_resource2();
    init_headers2();
    Webhooks2 = class extends APIResource2 {
      constructor() {
        super(...arguments), _Webhooks_instances.add(this);
      }
      /**
       * Validates that the given payload was sent by OpenAI and parses the payload.
       */
      async unwrap(payload, headers, secret = this._client.webhookSecret, tolerance = 300) {
        return await this.verifySignature(payload, headers, secret, tolerance), JSON.parse(payload);
      }
      /**
       * Validates whether or not the webhook payload was sent by OpenAI.
       *
       * An error will be raised if the webhook payload was not sent by OpenAI.
       *
       * @param payload - The webhook payload
       * @param headers - The webhook headers
       * @param secret - The webhook secret (optional, will use client secret if not provided)
       * @param tolerance - Maximum age of the webhook in seconds (default: 300 = 5 minutes)
       */
      async verifySignature(payload, headers, secret = this._client.webhookSecret, tolerance = 300) {
        if (typeof crypto > "u" || typeof crypto.subtle.importKey != "function" || typeof crypto.subtle.verify != "function")
          throw new Error("Webhook signature verification is only supported when the `crypto` global is defined");
        __classPrivateFieldGet2(this, _Webhooks_instances, "m", _Webhooks_validateSecret).call(this, secret);
        let headersObj = buildHeaders2([headers]).values, signatureHeader = __classPrivateFieldGet2(this, _Webhooks_instances, "m", _Webhooks_getRequiredHeader).call(this, headersObj, "webhook-signature"), timestamp = __classPrivateFieldGet2(this, _Webhooks_instances, "m", _Webhooks_getRequiredHeader).call(this, headersObj, "webhook-timestamp"), webhookId = __classPrivateFieldGet2(this, _Webhooks_instances, "m", _Webhooks_getRequiredHeader).call(this, headersObj, "webhook-id"), timestampSeconds = parseInt(timestamp, 10);
        if (isNaN(timestampSeconds))
          throw new InvalidWebhookSignatureError("Invalid webhook timestamp format");
        let nowSeconds = Math.floor(Date.now() / 1e3);
        if (nowSeconds - timestampSeconds > tolerance)
          throw new InvalidWebhookSignatureError("Webhook timestamp is too old");
        if (timestampSeconds > nowSeconds + tolerance)
          throw new InvalidWebhookSignatureError("Webhook timestamp is too new");
        let signatures = signatureHeader.split(" ").map((part) => part.startsWith("v1,") ? part.substring(3) : part), decodedSecret = secret.startsWith("whsec_") ? Buffer.from(secret.replace("whsec_", ""), "base64") : Buffer.from(secret, "utf-8"), signedPayload = webhookId ? `${webhookId}.${timestamp}.${payload}` : `${timestamp}.${payload}`, key = await crypto.subtle.importKey("raw", decodedSecret, { name: "HMAC", hash: "SHA-256" }, !1, ["verify"]);
        for (let signature of signatures)
          try {
            let signatureBytes = Buffer.from(signature, "base64");
            if (await crypto.subtle.verify("HMAC", key, signatureBytes, new TextEncoder().encode(signedPayload)))
              return;
          } catch {
            continue;
          }
        throw new InvalidWebhookSignatureError("The given webhook signature does not match the expected signature");
      }
    };
    _Webhooks_instances = /* @__PURE__ */ new WeakSet(), _Webhooks_validateSecret = function(secret) {
      if (typeof secret != "string" || secret.length === 0)
        throw new Error("The webhook secret must either be set using the env var, OPENAI_WEBHOOK_SECRET, on the client class, OpenAI({ webhookSecret: '123' }), or passed to this function");
    }, _Webhooks_getRequiredHeader = function(headers, name) {
      if (!headers)
        throw new Error("Headers are required");
      let value = headers.get(name);
      if (value == null)
        throw new Error(`Missing required header: ${name}`);
      return value;
    };
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/webhooks/index.mjs
var init_webhooks3 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/webhooks/index.mjs"() {
    init_webhooks2();
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/webhooks.mjs
var init_webhooks4 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/webhooks.mjs"() {
    init_webhooks3();
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/index.mjs
var init_resources3 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/resources/index.mjs"() {
    init_chat2();
    init_shared2();
    init_admin();
    init_audio();
    init_batches3();
    init_beta2();
    init_completions4();
    init_containers();
    init_conversations();
    init_embeddings();
    init_evals();
    init_files3();
    init_fine_tuning();
    init_graders2();
    init_images();
    init_models3();
    init_moderations();
    init_realtime2();
    init_responses();
    init_skills3();
    init_uploads5();
    init_vector_stores();
    init_videos();
    init_webhooks4();
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/provider.mjs
function configureProvider(provider) {
  let definition = providerDefinitions.get(provider);
  if (!definition)
    throw new Error("Invalid provider. Providers must be created with createProvider().");
  return definition.configure();
}
var providerDefinitionsKey, providerGlobal, existingProviderDefinitions, providerDefinitions, init_provider = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/internal/provider.mjs"() {
    providerDefinitionsKey = Symbol.for("openai.node.providerDefinitions.v1"), providerGlobal = globalThis, existingProviderDefinitions = providerGlobal[providerDefinitionsKey], providerDefinitions = existingProviderDefinitions ?? /* @__PURE__ */ new WeakMap();
    existingProviderDefinitions || Object.defineProperty(providerGlobal, providerDefinitionsKey, { value: providerDefinitions });
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/client.mjs
function getConnectionErrorMessage(error) {
  if (isUndiciDispatcherVersionMismatchError(error))
    return "Connection error. This may be caused by passing an undici dispatcher, such as ProxyAgent, that is incompatible with the fetch implementation. If you are using undici's ProxyAgent, pass the fetch implementation from the same undici package: import { fetch, ProxyAgent } from 'undici'; new OpenAI({ fetch, fetchOptions: { dispatcher: new ProxyAgent(...) } });";
}
function isUndiciDispatcherVersionMismatchError(error) {
  let current = error;
  for (let i = 0; i < 8 && current && typeof current == "object"; i++) {
    let err = current;
    if (err.code === "UND_ERR_INVALID_ARG" && typeof err.message == "string" && err.message.includes("invalid onRequestStart method"))
      return !0;
    current = err.cause;
  }
  return !1;
}
var _OpenAI_instances, _a4, _OpenAI_encoder, _OpenAI_baseURLOverridden, WORKLOAD_IDENTITY_API_KEY_PLACEHOLDER, OpenAI, init_client3 = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/client.mjs"() {
    init_tslib2();
    init_uuid2();
    init_values2();
    init_sleep2();
    init_errors3();
    init_detect_platform2();
    init_shims2();
    init_request_options2();
    init_query2();
    init_version2();
    init_error3();
    init_pagination2();
    init_workload_identity_auth();
    init_error3();
    init_uploads4();
    init_resources3();
    init_api_promise2();
    init_batches3();
    init_completions4();
    init_embeddings();
    init_files3();
    init_images();
    init_models3();
    init_moderations();
    init_videos();
    init_admin();
    init_audio();
    init_beta2();
    init_chat();
    init_containers();
    init_conversations();
    init_evals();
    init_fine_tuning();
    init_graders2();
    init_realtime2();
    init_responses();
    init_skills3();
    init_uploads5();
    init_vector_stores();
    init_webhooks2();
    init_detect_platform2();
    init_headers2();
    init_provider();
    init_env2();
    init_log2();
    init_values2();
    WORKLOAD_IDENTITY_API_KEY_PLACEHOLDER = "workload-identity-auth", OpenAI = class {
      /**
       * API Client for interfacing with the OpenAI API.
       *
       * @param {string | null | undefined} [opts.apiKey=process.env['OPENAI_API_KEY'] ?? null]
       * @param {string | null | undefined} [opts.adminAPIKey=process.env['OPENAI_ADMIN_KEY'] ?? null]
       * @param {string | null | undefined} [opts.organization=process.env['OPENAI_ORG_ID'] ?? null]
       * @param {string | null | undefined} [opts.project=process.env['OPENAI_PROJECT_ID'] ?? null]
       * @param {string | null | undefined} [opts.webhookSecret=process.env['OPENAI_WEBHOOK_SECRET'] ?? null]
       * @param {string} [opts.baseURL=process.env['OPENAI_BASE_URL'] ?? https://api.openai.com/v1] - Override the default base URL for the API.
       * @param {Provider} [opts.provider] - Configure a third-party API provider. Mutually exclusive with top-level authentication and base URL options.
       * @param {number} [opts.timeout=10 minutes] - The maximum amount of time (in milliseconds) the client will wait for a response before timing out.
       * @param {MergedRequestInit} [opts.fetchOptions] - Additional `RequestInit` options to be passed to `fetch` calls.
       * @param {Fetch} [opts.fetch] - Specify a custom `fetch` function implementation.
       * @param {number} [opts.maxRetries=2] - The maximum number of times the client will retry a request.
       * @param {HeadersLike} opts.defaultHeaders - Default headers to include with every request to the API.
       * @param {Record<string, string | undefined>} opts.defaultQuery - Default query parameters to include with every request to the API.
       * @param {boolean} [opts.dangerouslyAllowBrowser=false] - By default, client-side use of this library is not allowed, as it risks exposing your secret API credentials to attackers.
       */
      constructor(clientOptions = {}) {
        _OpenAI_instances.add(this), _OpenAI_encoder.set(this, void 0), this.completions = new Completions3(this), this.chat = new Chat(this), this.embeddings = new Embeddings(this), this.files = new Files3(this), this.images = new Images(this), this.audio = new Audio(this), this.moderations = new Moderations(this), this.models = new Models3(this), this.fineTuning = new FineTuning(this), this.graders = new Graders2(this), this.vectorStores = new VectorStores(this), this.webhooks = new Webhooks2(this), this.beta = new Beta2(this), this.batches = new Batches3(this), this.uploads = new Uploads(this), this.admin = new Admin(this), this.responses = new Responses(this), this.realtime = new Realtime2(this), this.conversations = new Conversations(this), this.evals = new Evals(this), this.containers = new Containers(this), this.skills = new Skills2(this), this.videos = new Videos(this);
        let provider = clientOptions.provider;
        if (provider) {
          let conflictingOptions = ["apiKey", "adminAPIKey", "workloadIdentity", "baseURL"].filter((key) => clientOptions[key] != null);
          if (conflictingOptions.length)
            throw new OpenAIError(`The \`provider\` option cannot be used with ${conflictingOptions.map((key) => `\`${key}\``).join(", ")}. Configure authentication and the base URL through the provider instead.`);
        }
        let { baseURL = provider ? null : readEnv2("OPENAI_BASE_URL"), apiKey = provider ? null : readEnv2("OPENAI_API_KEY") ?? null, adminAPIKey = provider ? null : readEnv2("OPENAI_ADMIN_KEY") ?? null, organization = provider ? null : readEnv2("OPENAI_ORG_ID") ?? null, project = provider ? null : readEnv2("OPENAI_PROJECT_ID") ?? null, webhookSecret = readEnv2("OPENAI_WEBHOOK_SECRET") ?? null, workloadIdentity, ...opts } = clientOptions, providerRuntime = provider ? configureProvider(provider) : void 0, options = {
          apiKey,
          adminAPIKey,
          organization,
          project,
          webhookSecret,
          workloadIdentity,
          provider,
          ...opts,
          baseURL: providerRuntime?.baseURL ?? (baseURL || "https://api.openai.com/v1")
        };
        if (apiKey && workloadIdentity)
          throw new OpenAIError("The `apiKey` and `workloadIdentity` options are mutually exclusive");
        if (!providerRuntime && !apiKey && !adminAPIKey && !workloadIdentity)
          throw new OpenAIError("Missing credentials. Please pass an `apiKey`, `workloadIdentity`, `adminAPIKey`, or set the `OPENAI_API_KEY` or `OPENAI_ADMIN_KEY` environment variable.");
        if (!options.dangerouslyAllowBrowser && isRunningInBrowser2())
          throw new OpenAIError(`It looks like you're running in a browser-like environment.

This is disabled by default, as it risks exposing your secret API credentials to attackers.
If you understand the risks and have appropriate mitigations in place,
you can set the \`dangerouslyAllowBrowser\` option to \`true\`, e.g.,

new OpenAI({ apiKey, dangerouslyAllowBrowser: true });

https://help.openai.com/en/articles/5112595-best-practices-for-api-key-safety
`);
        this.baseURL = options.baseURL, this.timeout = options.timeout ?? _a4.DEFAULT_TIMEOUT, this.logger = options.logger ?? console;
        let defaultLogLevel2 = "warn";
        this.logLevel = defaultLogLevel2, this.logLevel = parseLogLevel2(options.logLevel, "ClientOptions.logLevel", this) ?? parseLogLevel2(readEnv2("OPENAI_LOG"), "process.env['OPENAI_LOG']", this) ?? defaultLogLevel2, this.fetchOptions = options.fetchOptions, this.maxRetries = options.maxRetries ?? 2, this.fetch = options.fetch ?? getDefaultFetch2(), __classPrivateFieldSet2(this, _OpenAI_encoder, FallbackEncoder2, "f");
        let customHeadersEnv = provider ? void 0 : readEnv2("OPENAI_CUSTOM_HEADERS");
        if (customHeadersEnv) {
          let parsed = {};
          for (let line of customHeadersEnv.split(`
`)) {
            let colon = line.indexOf(":");
            colon >= 0 && (parsed[line.substring(0, colon).trim()] = line.substring(colon + 1).trim());
          }
          options.defaultHeaders = buildHeaders2([parsed, options.defaultHeaders]);
        }
        this._options = options, this._provider = providerRuntime, workloadIdentity && (this._workloadIdentityAuth = new WorkloadIdentityAuth(workloadIdentity, this.fetch)), this.apiKey = typeof apiKey == "string" ? apiKey : null, this.adminAPIKey = adminAPIKey, this.organization = organization, this.project = project, this.webhookSecret = webhookSecret;
      }
      /**
       * Create a new client instance re-using the same options given to the current client with optional overriding.
       */
      withOptions(options) {
        let inheritedProvider = this._options.provider, provider = options.provider ?? inheritedProvider, inheritedOptions = {
          ...this._options,
          baseURL: this.baseURL,
          maxRetries: this.maxRetries,
          timeout: this.timeout,
          logger: this.logger,
          logLevel: this.logLevel,
          fetch: this.fetch,
          fetchOptions: this.fetchOptions,
          apiKey: this._options.apiKey,
          adminAPIKey: this.adminAPIKey,
          workloadIdentity: this._options.workloadIdentity,
          organization: this.organization,
          project: this.project,
          webhookSecret: this.webhookSecret
        };
        return provider && (delete inheritedOptions.apiKey, delete inheritedOptions.adminAPIKey, delete inheritedOptions.workloadIdentity, delete inheritedOptions.baseURL, provider !== inheritedProvider && (delete inheritedOptions.organization, delete inheritedOptions.project, delete inheritedOptions.defaultHeaders)), new this.constructor({
          ...inheritedOptions,
          ...options,
          provider
        });
      }
      defaultQuery() {
        return this._options.defaultQuery;
      }
      validateHeaders({ values, nulls }, schemes = {
        bearerAuth: !0,
        adminAPIKeyAuth: !0
      }) {
        if (!(values.get("authorization") || values.get("api-key")) && !(nulls.has("authorization") || nulls.has("api-key")) && !(this._workloadIdentityAuth && schemes.bearerAuth))
          throw new Error('Could not resolve authentication method. Expected either apiKey or adminAPIKey to be set. Or for one of the "Authorization" or "api-key" headers to be explicitly omitted');
      }
      async authHeaders(opts, schemes = {
        bearerAuth: !0,
        adminAPIKeyAuth: !0
      }) {
        return buildHeaders2([
          schemes.bearerAuth ? await this.bearerAuth(opts) : null,
          schemes.adminAPIKeyAuth ? await this.adminAPIKeyAuth(opts) : null
        ]);
      }
      async bearerAuth(opts) {
        if (this._workloadIdentityAuth)
          return buildHeaders2([{ Authorization: `Bearer ${await this._workloadIdentityAuth.getToken()}` }]);
        if (this.apiKey != null)
          return buildHeaders2([{ Authorization: `Bearer ${this.apiKey}` }]);
      }
      async adminAPIKeyAuth(opts) {
        if (this.adminAPIKey != null)
          return buildHeaders2([{ Authorization: `Bearer ${this.adminAPIKey}` }]);
      }
      stringifyQuery(query) {
        return stringifyQuery2(query);
      }
      getUserAgent() {
        return `${this.constructor.name}/JS ${VERSION2}`;
      }
      defaultIdempotencyKey() {
        return `stainless-node-retry-${uuid42()}`;
      }
      makeStatusError(status, error, message, headers) {
        return APIError2.generate(status, error, message, headers);
      }
      async _callApiKey() {
        if (this._provider)
          return !1;
        let apiKey = this._options.apiKey;
        if (typeof apiKey != "function")
          return !1;
        let token;
        try {
          token = await apiKey();
        } catch (err) {
          throw err instanceof OpenAIError ? err : new OpenAIError(
            `Failed to get token from 'apiKey' function: ${err.message}`,
            // @ts-ignore
            { cause: err }
          );
        }
        if (typeof token != "string" || !token)
          throw new OpenAIError(`Expected 'apiKey' function argument to return a string but it returned ${token}`);
        return this.apiKey = token, !0;
      }
      buildURL(path12, query, defaultBaseURL) {
        let baseURL = !__classPrivateFieldGet2(this, _OpenAI_instances, "m", _OpenAI_baseURLOverridden).call(this) && defaultBaseURL || this.baseURL, url = isAbsoluteURL2(path12) ? new URL(path12) : new URL(baseURL + (baseURL.endsWith("/") && path12.startsWith("/") ? path12.slice(1) : path12)), defaultQuery = this.defaultQuery(), pathQuery = Object.fromEntries(url.searchParams);
        return (!isEmptyObj2(defaultQuery) || !isEmptyObj2(pathQuery)) && (query = { ...pathQuery, ...defaultQuery, ...query }), typeof query == "object" && query && !Array.isArray(query) && (url.search = this.stringifyQuery(query)), url.toString();
      }
      /**
       * Used as a callback for mutating the given `FinalRequestOptions` object.
       */
      async prepareOptions(options) {
        if (this._provider)
          return;
        (options.__security ?? { bearerAuth: !0 }).bearerAuth && await this._callApiKey();
      }
      /**
       * Used as a callback for mutating the given `RequestInit` object.
       *
       * This is useful for cases where you want to add certain headers based off of
       * the request properties, e.g. `method` or `url`.
       */
      async prepareRequest(request, { url, options }) {
      }
      get(path12, opts) {
        return this.methodRequest("get", path12, opts);
      }
      post(path12, opts) {
        return this.methodRequest("post", path12, opts);
      }
      patch(path12, opts) {
        return this.methodRequest("patch", path12, opts);
      }
      put(path12, opts) {
        return this.methodRequest("put", path12, opts);
      }
      delete(path12, opts) {
        return this.methodRequest("delete", path12, opts);
      }
      methodRequest(method, path12, opts) {
        return this.request(Promise.resolve(opts).then((opts2) => ({ method, path: path12, ...opts2 })));
      }
      request(options, remainingRetries = null) {
        return new APIPromise2(this, this.makeRequest(options, remainingRetries, void 0));
      }
      async makeRequest(optionsInput, retriesRemaining, retryOfRequestLogID) {
        let options = await optionsInput, maxRetries = options.maxRetries ?? this.maxRetries;
        retriesRemaining == null && (retriesRemaining = maxRetries), await this.prepareOptions(options);
        let { req, url, timeout } = await this.buildRequest(options, {
          retryCount: maxRetries - retriesRemaining
        });
        await this.prepareRequest(req, { url, options }), await this._provider?.prepareRequest?.(req, { url, options });
        let requestLogID = "log_" + (Math.random() * (1 << 24) | 0).toString(16).padStart(6, "0"), retryLogStr = retryOfRequestLogID === void 0 ? "" : `, retryOf: ${retryOfRequestLogID}`, startTime = Date.now();
        if (loggerFor2(this).debug(`[${requestLogID}] sending request`, formatRequestDetails2({
          retryOfRequestLogID,
          method: options.method,
          url,
          options,
          headers: req.headers
        })), options.signal?.aborted)
          throw new APIUserAbortError2();
        let security = options.__security ?? { bearerAuth: !0 }, controller = new AbortController(), response = await this.fetchWithAuth(url, req, timeout, controller, security).catch(castToError2), headersTime = Date.now();
        if (response instanceof globalThis.Error) {
          let retryMessage = `retrying, ${retriesRemaining} attempts remaining`;
          if (options.signal?.aborted)
            throw new APIUserAbortError2();
          let isTimeout = isAbortError2(response) || /timed? ?out/i.test(String(response) + ("cause" in response ? String(response.cause) : ""));
          if (retriesRemaining)
            return loggerFor2(this).info(`[${requestLogID}] connection ${isTimeout ? "timed out" : "failed"} - ${retryMessage}`), loggerFor2(this).debug(`[${requestLogID}] connection ${isTimeout ? "timed out" : "failed"} (${retryMessage})`, formatRequestDetails2({
              retryOfRequestLogID,
              url,
              durationMs: headersTime - startTime,
              message: response.message
            })), this.retryRequest(options, retriesRemaining, retryOfRequestLogID ?? requestLogID);
          throw loggerFor2(this).info(`[${requestLogID}] connection ${isTimeout ? "timed out" : "failed"} - error; no more retries left`), loggerFor2(this).debug(`[${requestLogID}] connection ${isTimeout ? "timed out" : "failed"} (error; no more retries left)`, formatRequestDetails2({
            retryOfRequestLogID,
            url,
            durationMs: headersTime - startTime,
            message: response.message
          })), response instanceof OAuthError || response instanceof SubjectTokenProviderError ? response : isTimeout ? new APIConnectionTimeoutError2() : new APIConnectionError2({
            message: getConnectionErrorMessage(response),
            cause: response
          });
        }
        let specialHeaders = [...response.headers.entries()].filter(([name]) => name === "x-request-id").map(([name, value]) => ", " + name + ": " + JSON.stringify(value)).join(""), responseInfo = `[${requestLogID}${retryLogStr}${specialHeaders}] ${req.method} ${url} ${response.ok ? "succeeded" : "failed"} with status ${response.status} in ${headersTime - startTime}ms`;
        if (!response.ok) {
          if (response.status === 401 && this._workloadIdentityAuth && security.bearerAuth && !options.__metadata?.hasStreamingBody && !options.__metadata?.workloadIdentityTokenRefreshed)
            return await CancelReadableStream2(response.body), this._workloadIdentityAuth.invalidateToken(), this.makeRequest({
              ...options,
              __metadata: {
                ...options.__metadata,
                workloadIdentityTokenRefreshed: !0
              }
            }, retriesRemaining, retryOfRequestLogID ?? requestLogID);
          let shouldRetry = await this.shouldRetry(response);
          if (retriesRemaining && shouldRetry) {
            let retryMessage2 = `retrying, ${retriesRemaining} attempts remaining`;
            return await CancelReadableStream2(response.body), loggerFor2(this).info(`${responseInfo} - ${retryMessage2}`), loggerFor2(this).debug(`[${requestLogID}] response error (${retryMessage2})`, formatRequestDetails2({
              retryOfRequestLogID,
              url: response.url,
              status: response.status,
              headers: response.headers,
              durationMs: headersTime - startTime
            })), this.retryRequest(options, retriesRemaining, retryOfRequestLogID ?? requestLogID, response.headers);
          }
          let retryMessage = shouldRetry ? "error; no more retries left" : "error; not retryable";
          loggerFor2(this).info(`${responseInfo} - ${retryMessage}`);
          let errText = await response.text().catch((err2) => castToError2(err2).message), errJSON = safeJSON2(errText), errMessage = errJSON ? void 0 : errText;
          throw loggerFor2(this).debug(`[${requestLogID}] response error (${retryMessage})`, formatRequestDetails2({
            retryOfRequestLogID,
            url: response.url,
            status: response.status,
            headers: response.headers,
            message: errMessage,
            durationMs: Date.now() - startTime
          })), this.makeStatusError(response.status, errJSON, errMessage, response.headers);
        }
        return loggerFor2(this).info(responseInfo), loggerFor2(this).debug(`[${requestLogID}] response start`, formatRequestDetails2({
          retryOfRequestLogID,
          url: response.url,
          status: response.status,
          headers: response.headers,
          durationMs: headersTime - startTime
        })), { response, options, controller, requestLogID, retryOfRequestLogID, startTime };
      }
      getAPIList(path12, Page3, opts) {
        return this.requestAPIList(Page3, opts && "then" in opts ? opts.then((opts2) => ({ method: "get", path: path12, ...opts2 })) : { method: "get", path: path12, ...opts });
      }
      requestAPIList(Page3, options) {
        let request = this.makeRequest(options, null, void 0);
        return new PagePromise2(this, request, Page3);
      }
      async fetchWithAuth(url, init, timeout, controller, schemes = {
        bearerAuth: !0,
        adminAPIKeyAuth: !0
      }) {
        if (this._workloadIdentityAuth && schemes.bearerAuth) {
          let headers = init.headers, authHeader = headers.get("Authorization");
          if (!authHeader || authHeader === `Bearer ${WORKLOAD_IDENTITY_API_KEY_PLACEHOLDER}`) {
            let token = await this._workloadIdentityAuth.getToken();
            headers.set("Authorization", `Bearer ${token}`);
          }
        }
        return await this.fetchWithTimeout(url, init, timeout, controller);
      }
      async fetchWithTimeout(url, init, ms, controller) {
        let { signal, method, ...options } = init || {}, abort = this._makeAbort(controller);
        signal && signal.addEventListener("abort", abort, { once: !0 });
        let timeout = setTimeout(abort, ms), isReadableBody = globalThis.ReadableStream && options.body instanceof globalThis.ReadableStream || typeof options.body == "object" && options.body !== null && Symbol.asyncIterator in options.body, fetchOptions = {
          signal: controller.signal,
          ...isReadableBody ? { duplex: "half" } : {},
          method: "GET",
          ...options
        };
        method && (fetchOptions.method = method.toUpperCase());
        try {
          return await this.fetch.call(void 0, url, fetchOptions);
        } finally {
          clearTimeout(timeout);
        }
      }
      async shouldRetry(response) {
        let shouldRetryHeader = response.headers.get("x-should-retry");
        return shouldRetryHeader === "true" ? !0 : shouldRetryHeader === "false" ? !1 : response.status === 408 || response.status === 409 || response.status === 429 || response.status >= 500;
      }
      async retryRequest(options, retriesRemaining, requestLogID, responseHeaders) {
        let timeoutMillis, retryAfterMillisHeader = responseHeaders?.get("retry-after-ms");
        if (retryAfterMillisHeader) {
          let timeoutMs = parseFloat(retryAfterMillisHeader);
          Number.isNaN(timeoutMs) || (timeoutMillis = timeoutMs);
        }
        let retryAfterHeader = responseHeaders?.get("retry-after");
        if (retryAfterHeader && !timeoutMillis) {
          let timeoutSeconds = parseFloat(retryAfterHeader);
          Number.isNaN(timeoutSeconds) ? timeoutMillis = Date.parse(retryAfterHeader) - Date.now() : timeoutMillis = timeoutSeconds * 1e3;
        }
        if (timeoutMillis === void 0) {
          let maxRetries = options.maxRetries ?? this.maxRetries;
          timeoutMillis = this.calculateDefaultRetryTimeoutMillis(retriesRemaining, maxRetries);
        }
        return await sleep2(timeoutMillis), this.makeRequest(options, retriesRemaining - 1, requestLogID);
      }
      calculateDefaultRetryTimeoutMillis(retriesRemaining, maxRetries) {
        let numRetries = maxRetries - retriesRemaining, sleepSeconds = Math.min(0.5 * Math.pow(2, numRetries), 8), jitter2 = 1 - Math.random() * 0.25;
        return sleepSeconds * jitter2 * 1e3;
      }
      async buildRequest(inputOptions, { retryCount = 0 } = {}) {
        let options = { ...inputOptions }, { method, path: path12, query, defaultBaseURL } = options, url = this.buildURL(path12, query, defaultBaseURL);
        "timeout" in options && validatePositiveInteger2("timeout", options.timeout), options.timeout = options.timeout ?? this.timeout;
        let { bodyHeaders, body, isStreamingBody } = this.buildBody({ options });
        isStreamingBody && (inputOptions.__metadata = {
          ...inputOptions.__metadata,
          hasStreamingBody: !0
        });
        let reqHeaders = await this.buildHeaders({ options: inputOptions, method, bodyHeaders, retryCount });
        return { req: {
          method,
          headers: reqHeaders,
          ...options.signal && { signal: options.signal },
          ...globalThis.ReadableStream && body instanceof globalThis.ReadableStream && { duplex: "half" },
          ...body && { body },
          ...this.fetchOptions ?? {},
          ...options.fetchOptions ?? {}
        }, url, timeout: options.timeout };
      }
      async buildHeaders({ options, method, bodyHeaders, retryCount }) {
        let idempotencyHeaders = {};
        this.idempotencyHeader && method !== "get" && (options.idempotencyKey || (options.idempotencyKey = this.defaultIdempotencyKey()), idempotencyHeaders[this.idempotencyHeader] = options.idempotencyKey);
        let headers = buildHeaders2([
          idempotencyHeaders,
          {
            Accept: "application/json",
            "User-Agent": this.getUserAgent(),
            "X-Stainless-Retry-Count": String(retryCount),
            ...options.timeout ? { "X-Stainless-Timeout": String(Math.trunc(options.timeout / 1e3)) } : {},
            ...getPlatformHeaders2(),
            "OpenAI-Organization": this.organization,
            "OpenAI-Project": this.project
          },
          this._provider ? void 0 : await this.authHeaders(options, options.__security ?? { bearerAuth: !0 }),
          this._options.defaultHeaders,
          bodyHeaders,
          options.headers
        ]);
        return this._provider || this.validateHeaders(headers, options.__security ?? { bearerAuth: !0 }), headers.values;
      }
      _makeAbort(controller) {
        return () => controller.abort();
      }
      buildBody({ options }) {
        let { body, headers: rawHeaders } = options;
        if (!body)
          return body === void 0 && "body" in options ? { ...__classPrivateFieldGet2(this, _OpenAI_encoder, "f").call(this, { body, headers: buildHeaders2([rawHeaders]) }), isStreamingBody: !1 } : { bodyHeaders: void 0, body: void 0, isStreamingBody: !1 };
        let headers = buildHeaders2([rawHeaders]), isReadableStream = typeof globalThis.ReadableStream < "u" && body instanceof globalThis.ReadableStream, isRetryableBody = !isReadableStream && (typeof body == "string" || body instanceof ArrayBuffer || ArrayBuffer.isView(body) || typeof globalThis.Blob < "u" && body instanceof globalThis.Blob || body instanceof URLSearchParams || body instanceof FormData);
        return (
          // Pass raw type verbatim
          ArrayBuffer.isView(body) || body instanceof ArrayBuffer || body instanceof DataView || typeof body == "string" && // Preserve legacy string encoding behavior for now
          headers.values.has("content-type") || // `Blob` is superset of `File`
          globalThis.Blob && body instanceof globalThis.Blob || // `FormData` -> `multipart/form-data`
          body instanceof FormData || // `URLSearchParams` -> `application/x-www-form-urlencoded`
          body instanceof URLSearchParams || // Send chunked stream (each chunk has own `length`)
          isReadableStream ? { bodyHeaders: void 0, body, isStreamingBody: !isRetryableBody } : typeof body == "object" && (Symbol.asyncIterator in body || Symbol.iterator in body && "next" in body && typeof body.next == "function") ? {
            bodyHeaders: void 0,
            body: ReadableStreamFrom2(body),
            isStreamingBody: !0
          } : typeof body == "object" && headers.values.get("content-type") === "application/x-www-form-urlencoded" ? {
            bodyHeaders: { "content-type": "application/x-www-form-urlencoded" },
            body: this.stringifyQuery(body),
            isStreamingBody: !1
          } : { ...__classPrivateFieldGet2(this, _OpenAI_encoder, "f").call(this, { body, headers }), isStreamingBody: !1 }
        );
      }
    };
    _a4 = OpenAI, _OpenAI_encoder = /* @__PURE__ */ new WeakMap(), _OpenAI_instances = /* @__PURE__ */ new WeakSet(), _OpenAI_baseURLOverridden = function() {
      return this._provider !== void 0 || this.baseURL !== "https://api.openai.com/v1";
    };
    OpenAI.OpenAI = _a4;
    OpenAI.DEFAULT_TIMEOUT = 6e5;
    OpenAI.OpenAIError = OpenAIError;
    OpenAI.APIError = APIError2;
    OpenAI.APIConnectionError = APIConnectionError2;
    OpenAI.APIConnectionTimeoutError = APIConnectionTimeoutError2;
    OpenAI.APIUserAbortError = APIUserAbortError2;
    OpenAI.NotFoundError = NotFoundError2;
    OpenAI.ConflictError = ConflictError2;
    OpenAI.RateLimitError = RateLimitError2;
    OpenAI.BadRequestError = BadRequestError2;
    OpenAI.AuthenticationError = AuthenticationError2;
    OpenAI.InternalServerError = InternalServerError2;
    OpenAI.PermissionDeniedError = PermissionDeniedError2;
    OpenAI.UnprocessableEntityError = UnprocessableEntityError2;
    OpenAI.InvalidWebhookSignatureError = InvalidWebhookSignatureError;
    OpenAI.toFile = toFile2;
    OpenAI.Completions = Completions3;
    OpenAI.Chat = Chat;
    OpenAI.Embeddings = Embeddings;
    OpenAI.Files = Files3;
    OpenAI.Images = Images;
    OpenAI.Audio = Audio;
    OpenAI.Moderations = Moderations;
    OpenAI.Models = Models3;
    OpenAI.FineTuning = FineTuning;
    OpenAI.Graders = Graders2;
    OpenAI.VectorStores = VectorStores;
    OpenAI.Webhooks = Webhooks2;
    OpenAI.Beta = Beta2;
    OpenAI.Batches = Batches3;
    OpenAI.Uploads = Uploads;
    OpenAI.Admin = Admin;
    OpenAI.Responses = Responses;
    OpenAI.Realtime = Realtime2;
    OpenAI.Conversations = Conversations;
    OpenAI.Evals = Evals;
    OpenAI.Containers = Containers;
    OpenAI.Skills = Skills2;
    OpenAI.Videos = Videos;
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/azure.mjs
var init_azure = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/azure.mjs"() {
    init_headers2();
    init_error4();
    init_utils4();
    init_client3();
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/bedrock.mjs
var init_bedrock = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/bedrock.mjs"() {
    init_error4();
    init_client3();
    init_headers2();
    init_utils4();
    init_ResponsesParser();
    init_resources3();
  }
});

// ../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/index.mjs
var init_openai = __esm({
  "../../node_modules/.pnpm/openai@6.45.0_ws@8.21.0_zod@3.25.76/node_modules/openai/index.mjs"() {
    init_client3();
    init_uploads4();
    init_api_promise2();
    init_client3();
    init_pagination2();
    init_error3();
    init_azure();
    init_bedrock();
  }
});

// ../core/dist/llm/togetherClient.js
function getTogetherClient() {
  if (_client2 !== null)
    return _client2;
  let apiKey = process.env.TOGETHER_API_KEY;
  if (!apiKey)
    throw new Error(`TOGETHER_API_KEY environment variable is not set.
Export it before running: export TOGETHER_API_KEY=<your-key>`);
  return _client2 = new OpenAI({ apiKey, baseURL: TOGETHER_BASE_URL }), _client2;
}
function _resetTogetherClient() {
  _client2 = null;
}
var TOGETHER_BASE_URL, _client2, init_togetherClient = __esm({
  "../core/dist/llm/togetherClient.js"() {
    "use strict";
    init_openai();
    TOGETHER_BASE_URL = "https://api.together.xyz/v1", _client2 = null;
  }
});

// ../core/dist/llm/retryWithSchema.js
function extractJSON(text) {
  let labelledFence = /```json\s*([\s\S]*?)```/i.exec(text);
  if (labelledFence?.[1])
    return labelledFence[1].trim();
  let unlabelledFence = /```\s*([\s\S]*?)```/.exec(text);
  if (unlabelledFence?.[1])
    return unlabelledFence[1].trim();
  let jsonBlock = /(\{[\s\S]*\}|\[[\s\S]*\])/.exec(text);
  return jsonBlock?.[1] ? jsonBlock[1].trim() : text.trim();
}
function validateWithSchema(raw, schema) {
  let extracted = extractJSON(raw), parsed = JSON.parse(extracted);
  return schema.parse(parsed);
}
async function retryWithSchema(generate2, schema, maxAttempts = 3) {
  let lastRaw = "", lastError;
  for (let attempt = 1; attempt <= maxAttempts; attempt++) {
    lastRaw = await generate2(attempt, lastError);
    try {
      return validateWithSchema(lastRaw, schema);
    } catch (err) {
      err instanceof ZodError ? lastError = err.issues.map((i) => `${i.path.join(".")}: ${i.message}`).join("; ") : err instanceof SyntaxError ? lastError = `JSON parse error: ${err.message}` : lastError = err instanceof Error ? err.message : String(err);
    }
  }
  throw new SchemaValidationError(`Schema validation failed after ${maxAttempts} attempts. Last error: ${lastError ?? "unknown"}`, maxAttempts, lastRaw, lastError ?? "unknown");
}
var SchemaValidationError, init_retryWithSchema = __esm({
  "../core/dist/llm/retryWithSchema.js"() {
    "use strict";
    init_zod();
    SchemaValidationError = class extends Error {
      attempts;
      lastRaw;
      lastError;
      constructor(message, attempts, lastRaw, lastError) {
        super(message), this.attempts = attempts, this.lastRaw = lastRaw, this.lastError = lastError, this.name = "SchemaValidationError";
      }
    };
  }
});

// ../core/dist/llm/prompts/planner.js
function buildPlannerSystemPrompt() {
  return `You are an expert software engineering project planner. Your job is to decompose a development goal into a TaskGraph \u2014 a structured list of tasks with file ownership boundaries.

You MUST respond with a single JSON object matching this exact schema (no markdown, no explanation \u2014 JSON ONLY):
{
  "runId": "<copy from intent>",
  "tasks": [
    {
      "taskId": "task-001",
      "runId": "<copy from intent>",
      "title": "<short descriptive title>",
      "description": "<what this task does and how>",
      "owner": "worker-<name>",
      "dependencies": [],
      "allowedFiles": ["<glob patterns this task MAY write>"],
      "blockedFiles": ["**/.env", "**/secrets/**", "**/.git/**"],
      "allowedTools": ["read_file", "write_file", "list_directory"],
      "expectedOutputs": ["<artifact names>"],
      "successCriteria": ["<verifiable completion condition>"],
      "status": "pending"
    }
  ]
}

Strict rules:
1. Keep it SIMPLE: 1\u20132 tasks for most goals. More tasks = more risk.
2. ALWAYS block sensitive files in every task: ["**/.env", "**/.env.*", "**/secrets/**", "**/.git/**", "**/node_modules/**"].
3. Never give two independent tasks the same allowedFiles pattern.
4. status must always be "pending".
5. taskId values must be unique (task-001, task-002, \u2026).
6. dependencies is an array of taskId strings (not indices).
7. Respond with JSON only \u2014 no text before or after.`;
}
function buildPlannerUserMessage(intent, repoContext) {
  return `Goal: ${intent.goal}

Constraints: ${intent.constraints.length > 0 ? intent.constraints.join(", ") : "none"}
Success criteria: ${intent.successCriteria.join(", ")}
Risk level: ${intent.riskLevel}

Repository context:
${repoContext}

runId to use: ${intent.runId}

Produce the TaskGraph JSON now.`;
}
function buildPlannerRetryMessage(intent, repoContext, lastError) {
  return buildPlannerUserMessage(intent, repoContext) + `

Your previous response failed schema validation with this error:
${lastError}

Fix the issue and return valid JSON only.`;
}
var init_planner2 = __esm({
  "../core/dist/llm/prompts/planner.js"() {
    "use strict";
  }
});

// ../core/dist/llm/plannerLLM.js
async function planWithLLM(intent, repoContext, model = DEFAULT_PLANNER_MODEL) {
  let client = getAnthropicClient(), systemPrompt = buildPlannerSystemPrompt();
  return retryWithSchema(async (attempt, lastError) => {
    let userContent = attempt === 1 ? buildPlannerUserMessage(intent, repoContext) : buildPlannerRetryMessage(intent, repoContext, lastError ?? "unknown"), block = (await client.messages.create({
      model,
      max_tokens: 4096,
      system: systemPrompt,
      messages: [{ role: "user", content: userContent }]
    })).content[0];
    if (!block || block.type !== "text")
      throw new Error("Planner returned non-text response");
    return block.text;
  }, TaskGraphSchema, 3);
}
var DEFAULT_PLANNER_MODEL, init_plannerLLM = __esm({
  "../core/dist/llm/plannerLLM.js"() {
    "use strict";
    init_client2();
    init_retryWithSchema();
    init_planner2();
    init_schemas();
    DEFAULT_PLANNER_MODEL = "claude-haiku-4-5-20251001";
  }
});

// ../core/dist/llm/prompts/priorContextBuilder.js
function formatPriorContextForPrompt(priorContext) {
  if (priorContext.length === 0)
    return "";
  let lines = [
    "Prior completed tasks (upstream work your task may depend on):",
    ""
  ];
  for (let ctx of priorContext)
    lines.push(`  Task: ${ctx.title} (ID: ${ctx.taskId})`), lines.push(`  Summary: ${ctx.summary}`), ctx.filesModified.length > 0 && lines.push(`  Files modified: ${ctx.filesModified.join(", ")}`), lines.push("");
  return lines.join(`
`);
}
var init_priorContextBuilder = __esm({
  "../core/dist/llm/prompts/priorContextBuilder.js"() {
    "use strict";
  }
});

// ../core/dist/llm/prompts/worker.js
function buildWorkerSystemPrompt(task, priorContext) {
  let priorContextBlock = priorContext && priorContext.length > 0 ? `
${formatPriorContextForPrompt(priorContext)}
` : "";
  return `You are an expert software engineer executing a single, scoped task inside an isolated git worktree.

Your task:
  Title: ${task.title}
  Description: ${task.description}
  Success criteria: ${task.successCriteria.join(", ")}
${priorContextBlock}
Tools available to you:
  - list_directory(path): List files in a directory
  - read_file(path): Read a file's content
  - write_file(path, content): Create or overwrite a file
  - task_complete(summary, filesModified): Mark the task done and report results

RULES:
1. You MUST call task_complete when done. Without it, the run will time out.
2. filesModified in task_complete should list only files you actually wrote.
3. All paths are relative to the worktree root.

Work step-by-step: explore the repo, understand the code, make ALL necessary changes (including config files), call task_complete.`;
}
function buildWorkerUserMessage(task) {
  return `Execute this task now:

"${task.description}"

Start by exploring the repository structure, then make your changes. Call task_complete when finished.`;
}
var init_worker3 = __esm({
  "../core/dist/llm/prompts/worker.js"() {
    "use strict";
    init_priorContextBuilder();
  }
});

// ../core/dist/llm/writeBoundaryGuard.js
function matchesAny2(file, patterns) {
  return patterns.some((pattern) => minimatch(file, pattern, { dot: !0 }));
}
function checkWritePermission(relativePath, allowedFiles, blockedFiles) {
  return matchesAny2(relativePath, blockedFiles) ? {
    denied: !0,
    reason: `BLOCKED: '${relativePath}' matches a blocked pattern \u2014 write denied`,
    violationType: "BLOCKED_FILE"
  } : matchesAny2(relativePath, allowedFiles) ? { denied: !1 } : {
    denied: !0,
    reason: `UNOWNED: '${relativePath}' is outside this task's allowedFiles scope \u2014 write denied`,
    violationType: "UNOWNED_FILE"
  };
}
var init_writeBoundaryGuard = __esm({
  "../core/dist/llm/writeBoundaryGuard.js"() {
    "use strict";
    init_esm3();
  }
});

// ../core/dist/llm/workerLLM.js
function safeRelativePath(worktreePath, rawPath) {
  let rel = (0, import_path92.isAbsolute)(rawPath) ? (0, import_path92.relative)(worktreePath, rawPath) : rawPath;
  if (!(0, import_path92.join)(worktreePath, rel).startsWith(worktreePath))
    throw new Error(`Path '${rawPath}' is outside the worktree`);
  return rel;
}
async function execListDirectory(worktreePath, input) {
  let rel = safeRelativePath(worktreePath, input.path), abs = (0, import_path92.join)(worktreePath, rel);
  if (!(0, import_fs5.existsSync)(abs))
    return `Directory not found: ${input.path}`;
  let lines = (await (0, import_promises9.readdir)(abs, { withFileTypes: !0 })).map((e) => `${e.isDirectory() ? "\u{1F4C1}" : "\u{1F4C4}"} ${e.name}`);
  return lines.length > 0 ? lines.join(`
`) : "(empty directory)";
}
async function execReadFile(worktreePath, input) {
  let rel = safeRelativePath(worktreePath, input.path), abs = (0, import_path92.join)(worktreePath, rel);
  return (0, import_fs5.existsSync)(abs) ? (0, import_promises9.readFile)(abs, "utf8") : `File not found: ${input.path}`;
}
async function execWriteFile(worktreePath, input, filesWritten, allowedFiles, blockedFiles, toolDenials, ledgerOpts) {
  let rel = safeRelativePath(worktreePath, input.path), permission = checkWritePermission(rel, allowedFiles, blockedFiles);
  if (permission.denied) {
    let denial = {
      toolName: "write_file",
      path: rel,
      reason: permission.reason,
      violationType: permission.violationType
    };
    return toolDenials.push(denial), ledgerOpts && await ledgerOpts.writer.appendEvent({
      event_id: LedgerWriter.createEventId(),
      run_id: ledgerOpts.runId,
      task_id: ledgerOpts.taskId,
      timestamp: (/* @__PURE__ */ new Date()).toISOString(),
      actor: "worker",
      event_type: "TOOL_DENIED",
      payload: {
        toolName: "write_file",
        path: rel,
        reason: permission.reason,
        violationType: permission.violationType
      }
    }), permission.reason;
  }
  let abs = (0, import_path92.join)(worktreePath, rel);
  return await (0, import_promises9.mkdir)((0, import_path92.dirname)(abs), { recursive: !0 }), await (0, import_promises9.writeFile)(abs, input.content, "utf8"), filesWritten.add(rel), `Written: ${rel}`;
}
async function runWorkerLLM(context, model = DEFAULT_WORKER_MODEL, ledgerOpts, priorContext) {
  let { task, worktreePath } = context, client = getAnthropicClient(), systemPrompt = buildWorkerSystemPrompt(task, priorContext), filesWritten = /* @__PURE__ */ new Set(), filesRead = [], toolDenials = [], messages = [
    { role: "user", content: buildWorkerUserMessage(task) }
  ], toolCallCount = 0, taskCompleteResult = null;
  for (; toolCallCount < MAX_TOOL_CALLS; ) {
    let response = await client.messages.create({
      model,
      max_tokens: 8192,
      system: systemPrompt,
      tools: WORKER_TOOLS,
      messages
    });
    if (messages.push({ role: "assistant", content: response.content }), response.stop_reason === "end_turn" || response.stop_reason !== "tool_use")
      break;
    let toolResults = [];
    for (let block of response.content) {
      if (block.type !== "tool_use")
        continue;
      toolCallCount++;
      let resultContent;
      try {
        if (block.name === "list_directory")
          resultContent = await execListDirectory(worktreePath, block.input);
        else if (block.name === "read_file") {
          let input = block.input;
          resultContent = await execReadFile(worktreePath, input), filesRead.push(block.input.path);
        } else block.name === "write_file" ? resultContent = await execWriteFile(worktreePath, block.input, filesWritten, task.allowedFiles, task.blockedFiles, toolDenials, ledgerOpts) : block.name === "task_complete" ? (taskCompleteResult = block.input, resultContent = "Task marked complete.") : resultContent = `Unknown tool: ${block.name}`;
      } catch (err) {
        resultContent = `Error: ${err instanceof Error ? err.message : String(err)}`;
      }
      if (toolResults.push({
        type: "tool_result",
        tool_use_id: block.id,
        content: resultContent
      }), taskCompleteResult !== null)
        break;
    }
    if (messages.push({ role: "user", content: toolResults }), taskCompleteResult !== null)
      break;
  }
  let summary = taskCompleteResult?.summary ?? `Worker reached max tool calls (${MAX_TOOL_CALLS}) without calling task_complete`, reportedModified = taskCompleteResult?.filesModified ?? [...filesWritten];
  return WorkerResultSchema.parse({
    taskId: task.taskId,
    summary,
    filesRead,
    filesModified: reportedModified,
    worktreeBranch: `agentledger/${task.taskId}`,
    toolDenials,
    output: {
      taskCompleted: taskCompleteResult !== null,
      toolCallCount,
      selfReportedFilesModified: reportedModified,
      actualFilesWritten: [...filesWritten],
      toolDenialCount: toolDenials.length
    }
  });
}
var import_promises9, import_path92, import_fs5, DEFAULT_WORKER_MODEL, MAX_TOOL_CALLS, WORKER_TOOLS, init_workerLLM = __esm({
  "../core/dist/llm/workerLLM.js"() {
    "use strict";
    import_promises9 = require("fs/promises"), import_path92 = require("path"), import_fs5 = require("fs");
    init_client2();
    init_worker3();
    init_schemas();
    init_LedgerWriter();
    init_writeBoundaryGuard();
    DEFAULT_WORKER_MODEL = "claude-sonnet-4-6", MAX_TOOL_CALLS = 40, WORKER_TOOLS = [
      {
        name: "list_directory",
        description: "List files and directories at a given path (relative to worktree root)",
        input_schema: {
          type: "object",
          properties: {
            path: {
              type: "string",
              description: "Directory path relative to worktree root (use '.' for root)"
            }
          },
          required: ["path"]
        }
      },
      {
        name: "read_file",
        description: "Read the content of a file (relative path from worktree root)",
        input_schema: {
          type: "object",
          properties: {
            path: {
              type: "string",
              description: "File path relative to worktree root"
            }
          },
          required: ["path"]
        }
      },
      {
        name: "write_file",
        description: "Create or overwrite a file at the given path (relative to worktree root).",
        input_schema: {
          type: "object",
          properties: {
            path: {
              type: "string",
              description: "File path relative to worktree root"
            },
            content: {
              type: "string",
              description: "Full content to write to the file"
            }
          },
          required: ["path", "content"]
        }
      },
      {
        name: "task_complete",
        description: "Mark the task as complete. Call this when all changes are made. REQUIRED \u2014 the run will stall without it.",
        input_schema: {
          type: "object",
          properties: {
            summary: {
              type: "string",
              description: "Summary of what was done"
            },
            filesModified: {
              type: "array",
              items: { type: "string" },
              description: "Relative paths of files you wrote (not read)"
            }
          },
          required: ["summary", "filesModified"]
        }
      }
    ];
  }
});

// ../core/dist/llm/plannerTogetherLLM.js
async function planWithTogether(intent, repoContext, model = DEFAULT_TOGETHER_PLANNER_MODEL) {
  let client = getTogetherClient(), systemPrompt = buildPlannerSystemPrompt();
  return retryWithSchema(async (attempt, lastError) => {
    let userContent = attempt === 1 ? buildPlannerUserMessage(intent, repoContext) : buildPlannerRetryMessage(intent, repoContext, lastError ?? "unknown"), content = (await client.chat.completions.create({
      model,
      max_tokens: 4096,
      messages: [
        { role: "system", content: systemPrompt },
        { role: "user", content: userContent }
      ]
    })).choices[0]?.message?.content;
    if (!content)
      throw new Error("Planner returned empty response");
    return content;
  }, TaskGraphSchema, 3);
}
var DEFAULT_TOGETHER_PLANNER_MODEL, init_plannerTogetherLLM = __esm({
  "../core/dist/llm/plannerTogetherLLM.js"() {
    "use strict";
    init_togetherClient();
    init_retryWithSchema();
    init_planner2();
    init_schemas();
    DEFAULT_TOGETHER_PLANNER_MODEL = "meta-llama/Llama-3.3-70B-Instruct-Turbo";
  }
});

// ../core/dist/llm/workerTogetherLLM.js
function safeRelativePath2(worktreePath, rawPath) {
  let rel = (0, import_path93.isAbsolute)(rawPath) ? (0, import_path93.relative)(worktreePath, rawPath) : rawPath;
  if (!(0, import_path93.join)(worktreePath, rel).startsWith(worktreePath))
    throw new Error(`Path '${rawPath}' is outside the worktree`);
  return rel;
}
async function execListDirectory2(worktreePath, input) {
  let rel = safeRelativePath2(worktreePath, input.path), abs = (0, import_path93.join)(worktreePath, rel);
  if (!(0, import_fs6.existsSync)(abs))
    return `Directory not found: ${input.path}`;
  let lines = (await (0, import_promises10.readdir)(abs, { withFileTypes: !0 })).map((e) => `${e.isDirectory() ? "\u{1F4C1}" : "\u{1F4C4}"} ${e.name}`);
  return lines.length > 0 ? lines.join(`
`) : "(empty directory)";
}
async function execReadFile2(worktreePath, input) {
  let rel = safeRelativePath2(worktreePath, input.path), abs = (0, import_path93.join)(worktreePath, rel);
  return (0, import_fs6.existsSync)(abs) ? (0, import_promises10.readFile)(abs, "utf8") : `File not found: ${input.path}`;
}
async function execWriteFile2(worktreePath, input, filesWritten, allowedFiles, blockedFiles, toolDenials, ledgerOpts) {
  let rel = safeRelativePath2(worktreePath, input.path), permission = checkWritePermission(rel, allowedFiles, blockedFiles);
  if (permission.denied) {
    let denial = {
      toolName: "write_file",
      path: rel,
      reason: permission.reason,
      violationType: permission.violationType
    };
    return toolDenials.push(denial), ledgerOpts && await ledgerOpts.writer.appendEvent({
      event_id: LedgerWriter.createEventId(),
      run_id: ledgerOpts.runId,
      task_id: ledgerOpts.taskId,
      timestamp: (/* @__PURE__ */ new Date()).toISOString(),
      actor: "worker",
      event_type: "TOOL_DENIED",
      payload: {
        toolName: "write_file",
        path: rel,
        reason: permission.reason,
        violationType: permission.violationType
      }
    }), permission.reason;
  }
  let abs = (0, import_path93.join)(worktreePath, rel);
  return await (0, import_promises10.mkdir)((0, import_path93.dirname)(abs), { recursive: !0 }), await (0, import_promises10.writeFile)(abs, input.content, "utf8"), filesWritten.add(rel), `Written: ${rel}`;
}
async function runWorkerTogether(context, model = DEFAULT_TOGETHER_WORKER_MODEL, ledgerOpts, priorContext) {
  let { task, worktreePath } = context, client = getTogetherClient(), systemPrompt = buildWorkerSystemPrompt(task, priorContext), filesWritten = /* @__PURE__ */ new Set(), filesRead = [], toolDenials = [], messages = [
    { role: "system", content: systemPrompt },
    { role: "user", content: buildWorkerUserMessage(task) }
  ], toolCallCount = 0, taskCompleteResult = null;
  for (; toolCallCount < MAX_TOOL_CALLS2; ) {
    let choice = (await client.chat.completions.create({
      model,
      max_tokens: 8192,
      tools: WORKER_TOOLS2,
      tool_choice: "auto",
      messages
    })).choices[0];
    if (!choice)
      break;
    let assistantMessage = choice.message;
    if (messages.push(assistantMessage), choice.finish_reason === "stop" || choice.finish_reason === "length" || choice.finish_reason !== "tool_calls" || !assistantMessage.tool_calls?.length)
      break;
    for (let toolCall of assistantMessage.tool_calls) {
      if (toolCall.type !== "function")
        continue;
      toolCallCount++;
      let fnName = toolCall.function.name, fnArgs = toolCall.function.arguments, resultContent, parsedInput;
      try {
        parsedInput = JSON.parse(fnArgs);
      } catch {
        parsedInput = {}, resultContent = `Error: could not parse tool arguments: ${fnArgs}`, messages.push({
          role: "tool",
          tool_call_id: toolCall.id,
          content: resultContent
        });
        continue;
      }
      try {
        if (fnName === "list_directory")
          resultContent = await execListDirectory2(worktreePath, parsedInput);
        else if (fnName === "read_file") {
          let input = parsedInput;
          resultContent = await execReadFile2(worktreePath, input), filesRead.push(input.path);
        } else fnName === "write_file" ? resultContent = await execWriteFile2(worktreePath, parsedInput, filesWritten, task.allowedFiles, task.blockedFiles, toolDenials, ledgerOpts) : fnName === "task_complete" ? (taskCompleteResult = parsedInput, resultContent = "Task marked complete.") : resultContent = `Unknown tool: ${fnName}`;
      } catch (err) {
        resultContent = `Error: ${err instanceof Error ? err.message : String(err)}`;
      }
      messages.push({
        role: "tool",
        tool_call_id: toolCall.id,
        content: resultContent
      });
    }
    if (taskCompleteResult !== null)
      break;
  }
  let summary = taskCompleteResult?.summary ?? `Worker reached max tool calls (${MAX_TOOL_CALLS2}) without calling task_complete`, reportedModified = taskCompleteResult?.filesModified ?? [...filesWritten];
  return WorkerResultSchema.parse({
    taskId: task.taskId,
    summary,
    filesRead,
    filesModified: reportedModified,
    worktreeBranch: `agentledger/${task.taskId}`,
    toolDenials,
    output: {
      taskCompleted: taskCompleteResult !== null,
      toolCallCount,
      selfReportedFilesModified: reportedModified,
      actualFilesWritten: [...filesWritten],
      toolDenialCount: toolDenials.length
    }
  });
}
var import_promises10, import_path93, import_fs6, DEFAULT_TOGETHER_WORKER_MODEL, MAX_TOOL_CALLS2, WORKER_TOOLS2, init_workerTogetherLLM = __esm({
  "../core/dist/llm/workerTogetherLLM.js"() {
    "use strict";
    import_promises10 = require("fs/promises"), import_path93 = require("path"), import_fs6 = require("fs");
    init_togetherClient();
    init_worker3();
    init_schemas();
    init_LedgerWriter();
    init_writeBoundaryGuard();
    DEFAULT_TOGETHER_WORKER_MODEL = "meta-llama/Llama-3.3-70B-Instruct-Turbo", MAX_TOOL_CALLS2 = 40, WORKER_TOOLS2 = [
      {
        type: "function",
        function: {
          name: "list_directory",
          description: "List files and directories at a given path (relative to worktree root)",
          parameters: {
            type: "object",
            properties: {
              path: {
                type: "string",
                description: "Directory path relative to worktree root (use '.' for root)"
              }
            },
            required: ["path"]
          }
        }
      },
      {
        type: "function",
        function: {
          name: "read_file",
          description: "Read the content of a file (relative path from worktree root)",
          parameters: {
            type: "object",
            properties: {
              path: {
                type: "string",
                description: "File path relative to worktree root"
              }
            },
            required: ["path"]
          }
        }
      },
      {
        type: "function",
        function: {
          name: "write_file",
          description: "Create or overwrite a file at the given path (relative to worktree root).",
          parameters: {
            type: "object",
            properties: {
              path: {
                type: "string",
                description: "File path relative to worktree root"
              },
              content: {
                type: "string",
                description: "Full content to write to the file"
              }
            },
            required: ["path", "content"]
          }
        }
      },
      {
        type: "function",
        function: {
          name: "task_complete",
          description: "Mark the task as complete. Call this when all changes are made. REQUIRED \u2014 the run will stall without it.",
          parameters: {
            type: "object",
            properties: {
              summary: {
                type: "string",
                description: "Summary of what was done"
              },
              filesModified: {
                type: "array",
                items: { type: "string" },
                description: "Relative paths of files you wrote (not read)"
              }
            },
            required: ["summary", "filesModified"]
          }
        }
      }
    ];
  }
});

// ../core/dist/llm/repoContext.js
async function gatherRepoContext(repoRoot) {
  let parts = [], topLevel = (await (0, import_promises11.readdir)(repoRoot, { withFileTypes: !0 }).catch(() => [])).filter((e) => !IGNORED_DIRS.has(e.name)).map((e) => `${e.isDirectory() ? "\u{1F4C1}" : "\u{1F4C4}"} ${e.name}`).join(`
`);
  parts.push(`Top-level files:
${topLevel}`);
  let readmePath = (0, import_path94.join)(repoRoot, "README.md");
  if ((0, import_fs7.existsSync)(readmePath)) {
    let readme = await (0, import_promises11.readFile)(readmePath, "utf8"), truncated = readme.length > 2e3 ? readme.slice(0, 2e3) + `
...(truncated)` : readme;
    parts.push(`README.md:
${truncated}`);
  }
  let srcPath = (0, import_path94.join)(repoRoot, "src");
  if ((0, import_fs7.existsSync)(srcPath)) {
    let srcFiles = await collectFiles(srcPath, repoRoot, MAX_FILES_LISTED);
    parts.push(`src/ files:
${srcFiles.join(`
`)}`);
  }
  return parts.join(`

---

`);
}
async function collectFiles(dir, repoRoot, limit3, results = []) {
  if (results.length >= limit3)
    return results;
  let entries = await (0, import_promises11.readdir)(dir, { withFileTypes: !0 }).catch(() => []);
  for (let entry of entries) {
    if (results.length >= limit3)
      break;
    if (IGNORED_DIRS.has(entry.name))
      continue;
    let full = (0, import_path94.join)(dir, entry.name);
    if (entry.isDirectory())
      await collectFiles(full, repoRoot, limit3, results);
    else {
      let rel = full.slice(repoRoot.length + 1);
      results.push(rel);
    }
  }
  return results;
}
var import_promises11, import_path94, import_fs7, IGNORED_DIRS, MAX_FILES_LISTED, init_repoContext = __esm({
  "../core/dist/llm/repoContext.js"() {
    "use strict";
    import_promises11 = require("fs/promises"), import_path94 = require("path"), import_fs7 = require("fs"), IGNORED_DIRS = /* @__PURE__ */ new Set([".git", "node_modules", "dist", ".agentledger", "coverage"]), MAX_FILES_LISTED = 60;
  }
});

// ../core/dist/llm/index.js
var init_llm = __esm({
  "../core/dist/llm/index.js"() {
    "use strict";
    init_client2();
    init_togetherClient();
    init_retryWithSchema();
    init_plannerLLM();
    init_workerLLM();
    init_plannerTogetherLLM();
    init_workerTogetherLLM();
    init_repoContext();
    init_planner2();
    init_worker3();
    init_priorContextBuilder();
  }
});

// ../core/dist/approvals/approvalPolicy.js
function shouldRequireApproval(task, workerResult, policy) {
  let reasons = [];
  for (let trigger of policy.requireApprovalFor)
    switch (trigger) {
      case "all": {
        reasons.push("approval required for all tasks (policy: all)");
        break;
      }
      case "high_risk_keywords": {
        let textToScan = [
          workerResult.summary,
          task.title,
          task.description,
          ...workerResult.filesModified
        ].join(" ").toLowerCase(), matched = HIGH_RISK_KEYWORDS.filter((kw) => textToScan.includes(kw));
        matched.length > 0 && reasons.push(`high-risk keyword(s) detected: ${matched.slice(0, 3).join(", ")}`);
        break;
      }
      case "new_dependencies": {
        workerResult.filesModified.some((f) => DEPENDENCY_FILE_PATTERNS.some((pattern) => f.endsWith(pattern) || f === pattern)) && reasons.push(`dependency file modified: ${workerResult.filesModified.find((f) => DEPENDENCY_FILE_PATTERNS.some((p) => f.endsWith(p) || f === p))}`);
        break;
      }
      case "blocked_files_nearby": {
        let modifiedDirs = new Set(workerResult.filesModified.map((f) => f.split("/").slice(0, -1).join("/"))), blockedDirs = new Set(task.blockedFiles.map((f) => f.split("/").slice(0, -1).join("/"))), overlap = [...modifiedDirs].filter((d) => blockedDirs.has(d) && d !== "");
        overlap.length > 0 && reasons.push(`modified files share directory with blocked files: ${overlap[0]}`);
        break;
      }
    }
  return { required: reasons.length > 0, reasons };
}
var HIGH_RISK_KEYWORDS, DEPENDENCY_FILE_PATTERNS, init_approvalPolicy = __esm({
  "../core/dist/approvals/approvalPolicy.js"() {
    "use strict";
    HIGH_RISK_KEYWORDS = [
      "auth",
      "authentication",
      "authorization",
      "password",
      "secret",
      "token",
      "credential",
      "payment",
      "billing",
      "invoice",
      "migration",
      "schema",
      "prod",
      "production",
      "deploy",
      "infrastructure",
      "firewall",
      "permission",
      "role",
      "admin",
      "sudo",
      "root",
      "encryption",
      "decrypt",
      "private_key",
      "certificate"
    ], DEPENDENCY_FILE_PATTERNS = [
      "package.json",
      "package-lock.json",
      "pnpm-lock.yaml",
      "yarn.lock",
      "requirements.txt",
      "Pipfile",
      "Cargo.toml",
      "go.mod",
      "go.sum",
      "Gemfile",
      "composer.json"
    ];
  }
});

// ../core/dist/approvals/approvalState.js
function getPendingApprovals(events) {
  let requested = /* @__PURE__ */ new Map(), resolved = /* @__PURE__ */ new Set();
  for (let event of events) {
    let taskId = event.task_id;
    taskId && (event.event_type === "HUMAN_APPROVAL_REQUESTED" ? requested.set(taskId, event) : (event.event_type === "HUMAN_APPROVAL_GRANTED" || event.event_type === "HUMAN_APPROVAL_REJECTED") && resolved.add(taskId));
  }
  let pending = [];
  for (let [taskId, requestEvent] of requested) {
    if (resolved.has(taskId))
      continue;
    let payload = requestEvent.payload;
    pending.push({
      runId: requestEvent.run_id,
      taskId,
      requestedAt: requestEvent.timestamp,
      reasons: Array.isArray(payload.reasons) ? payload.reasons : [],
      filesModified: Array.isArray(payload.filesModified) ? payload.filesModified : [],
      summary: typeof payload.summary == "string" ? payload.summary : ""
    });
  }
  return pending;
}
function isApproved(events, taskId) {
  let granted = !1, rejected = !1;
  for (let event of events)
    event.task_id === taskId && (event.event_type === "HUMAN_APPROVAL_GRANTED" && (granted = !0), event.event_type === "HUMAN_APPROVAL_REJECTED" && (rejected = !0));
  return granted && !rejected;
}
function isRejected(events, taskId) {
  return events.some((e) => e.task_id === taskId && e.event_type === "HUMAN_APPROVAL_REJECTED");
}
function isAwaitingApproval(events, taskId) {
  return getPendingApprovals(events).some((p) => p.taskId === taskId);
}
var init_approvalState = __esm({
  "../core/dist/approvals/approvalState.js"() {
    "use strict";
  }
});

// ../core/dist/approvals/index.js
var init_approvals = __esm({
  "../core/dist/approvals/index.js"() {
    "use strict";
    init_approvalPolicy();
    init_approvalState();
  }
});

// ../core/dist/handoff/generateHandoff.js
function generateHandoff(events, runId) {
  let state = replayLedger(events, runId), patchByTask = /* @__PURE__ */ new Map(), approvalRequestedAt = /* @__PURE__ */ new Map();
  for (let event of events)
    if (event.run_id === runId) {
      if (event.event_type === "PATCH_PROPOSED") {
        let taskId = resolveTaskId(event);
        taskId && patchByTask.set(taskId, {
          summary: typeof event.payload.summary == "string" ? event.payload.summary : "",
          filesModified: Array.isArray(event.payload.filesModified) ? event.payload.filesModified : []
        });
      }
      if (event.event_type === "HUMAN_APPROVAL_REQUESTED") {
        let taskId = resolveTaskId(event);
        taskId && !approvalRequestedAt.has(taskId) && approvalRequestedAt.set(taskId, event.timestamp);
      }
    }
  let completedTasks = [], pendingTasks = [], failedTasks = [], awaitingApproval = [];
  for (let task of state.tasks)
    switch (task.status) {
      case "completed": {
        let patch = patchByTask.get(task.taskId);
        completedTasks.push({
          taskId: task.taskId,
          title: task.title,
          summary: patch?.summary ?? "",
          filesModified: patch?.filesModified ?? []
        });
        break;
      }
      case "pending":
      case "assigned":
      case "running": {
        let blockedBy = task.dependencies.filter((depId) => {
          let dep = state.tasks.find((t) => t.taskId === depId);
          return !dep || dep.status !== "completed";
        });
        pendingTasks.push({
          taskId: task.taskId,
          title: task.title,
          description: task.description,
          owner: task.owner,
          blockedBy
        });
        break;
      }
      case "failed": {
        let failureEvent = events.find((e) => e.run_id === runId && (e.task_id === task.taskId || resolveTaskId(e) === task.taskId) && FAILURE_EVENTS.has(e.event_type)), failureReason = failureEvent ? typeof failureEvent.payload.reason == "string" ? failureEvent.payload.reason : failureEvent.event_type : "unknown";
        failedTasks.push({
          taskId: task.taskId,
          title: task.title,
          failureReason
        });
        break;
      }
      case "awaiting_approval": {
        awaitingApproval.push({
          taskId: task.taskId,
          title: task.title,
          requestedAt: approvalRequestedAt.get(task.taskId) ?? task.taskId
        });
        break;
      }
      // awaiting_verification treated as in-flight pending
      case "awaiting_verification": {
        pendingTasks.push({
          taskId: task.taskId,
          title: task.title,
          description: task.description,
          owner: task.owner,
          blockedBy: []
        });
        break;
      }
    }
  return {
    runId,
    goal: state.goal,
    runStatus: state.status,
    completedTasks,
    pendingTasks,
    failedTasks,
    awaitingApproval,
    allFilesModified: state.filesModified,
    suggestedNextAction: buildSuggestedAction(runId, state.status, pendingTasks, failedTasks, awaitingApproval)
  };
}
function resolveTaskId(event) {
  return event.task_id ?? (typeof event.payload.taskId == "string" ? event.payload.taskId : void 0);
}
function buildSuggestedAction(runId, runStatus, pendingTasks, failedTasks, awaitingApproval) {
  if (awaitingApproval.length > 0)
    return `Run \`agentledger approvals approve ${runId}\` then \`agentledger resume ${runId}\` to continue.`;
  if (runStatus === "completed")
    return "Run completed successfully. No action needed.";
  if (failedTasks.length > 0 && pendingTasks.length === 0)
    return `${failedTasks.length} task(s) failed. Review with \`agentledger replay -r ${runId}\` and retry.`;
  if (pendingTasks.length > 0) {
    let next = pendingTasks[0];
    return next.blockedBy.length > 0 ? `Task "${next.title}" blocked by: ${next.blockedBy.join(", ")}. Resolve blockers first.` : `Run \`agentledger resume ${runId}\` to continue with task "${next.title}".`;
  }
  return `Run \`agentledger resume ${runId}\` to continue.`;
}
var FAILURE_EVENTS, init_generateHandoff = __esm({
  "../core/dist/handoff/generateHandoff.js"() {
    "use strict";
    init_replayLedger();
    FAILURE_EVENTS = /* @__PURE__ */ new Set(["TASK_FAILED", "VERIFICATION_FAILED", "BOUNDARY_VIOLATION"]);
  }
});

// ../core/dist/handoff/generateHandoffBrief.js
function resolveTaskId2(event) {
  return event.task_id ?? (typeof event.payload.taskId == "string" ? event.payload.taskId : void 0);
}
function classifyFailure(taskId, runEvents) {
  let taskEvents = runEvents.filter((e) => resolveTaskId2(e) === taskId).sort((a, b) => a.timestamp.localeCompare(b.timestamp));
  for (let event of taskEvents)
    switch (event.event_type) {
      case "BOUNDARY_VIOLATION":
        return {
          reason: "boundary_violation",
          context: {
            violatedFile: typeof event.payload.file == "string" ? event.payload.file : void 0,
            violationType: typeof event.payload.violationType == "string" ? event.payload.violationType : void 0,
            detail: typeof event.payload.message == "string" ? event.payload.message : void 0
          }
        };
      case "VERIFICATION_FAILED":
        return {
          reason: "verification_failed",
          context: {
            exitCode: typeof event.payload.exitCode == "number" ? event.payload.exitCode : void 0,
            detail: typeof event.payload.reason == "string" ? event.payload.reason : void 0
          }
        };
      case "HUMAN_APPROVAL_REJECTED":
        return { reason: "human_approval_rejected" };
      case "POLICY_EVALUATED": {
        if (event.payload.action === "deny")
          return {
            reason: "governance_denied",
            context: { detail: Array.isArray(event.payload.reasons) ? event.payload.reasons.join("; ") : void 0 }
          };
        break;
      }
      case "TOOL_DENIED":
        return {
          reason: "tool_denial",
          context: {
            toolName: typeof event.payload.toolName == "string" ? event.payload.toolName : void 0,
            violationType: typeof event.payload.violationType == "string" ? event.payload.violationType : void 0
          }
        };
      case "PATCH_PROPOSED": {
        let summary = event.payload.summary;
        if (typeof summary == "string" && summary.toLowerCase().includes("max tool calls"))
          return {
            reason: "tool_call_limit_exceeded",
            context: { detail: summary }
          };
        break;
      }
    }
  return { reason: "unknown" };
}
function buildFileInventory(runEvents, completedTaskIds) {
  let mergedFiles = [], worktreeFiles = [];
  for (let event of runEvents) {
    if (event.event_type !== "PATCH_PROPOSED")
      continue;
    let taskId = resolveTaskId2(event), files = Array.isArray(event.payload.filesModified) ? event.payload.filesModified : [], target = taskId !== void 0 && completedTaskIds.has(taskId) ? mergedFiles : worktreeFiles;
    for (let f of files)
      target.includes(f) || target.push(f);
  }
  let allFiles = [.../* @__PURE__ */ new Set([...mergedFiles, ...worktreeFiles])];
  return { mergedFiles, worktreeFiles, allFiles };
}
function extractUnresolvedRisks(runEvents, completedTaskIds) {
  let risks = [];
  for (let event of runEvents) {
    if (event.event_type !== "PATCH_RISK_DETECTED")
      continue;
    let taskId = resolveTaskId2(event);
    if (taskId !== void 0 && completedTaskIds.has(taskId))
      continue;
    let rawRisks = Array.isArray(event.payload.risks) ? event.payload.risks : [];
    for (let r of rawRisks) {
      if (typeof r != "object" || r === null)
        continue;
      let rec = r;
      risks.push({
        taskId: taskId ?? "unknown",
        category: rec.category,
        severity: rec.severity,
        filePath: typeof rec.filePath == "string" ? rec.filePath : "",
        pattern: typeof rec.pattern == "string" ? rec.pattern : ""
      });
    }
  }
  return risks.sort((a, b) => (SEVERITY_ORDER[b.severity] ?? 0) - (SEVERITY_ORDER[a.severity] ?? 0)).slice(0, 5);
}
function buildResumptionGuidance(runId, runStatus, awaitingApproval, failedTasks, pendingTasks, inProgressTasks) {
  if (runStatus === "completed")
    return {
      action: "run_completed",
      command: "agentledger ledger view",
      detail: "Run completed. No action needed."
    };
  if (awaitingApproval.length > 0)
    return {
      action: "approve_pending",
      targetTaskId: awaitingApproval[0].taskId,
      command: `agentledger approvals approve ${runId}`,
      detail: `${awaitingApproval.length} task(s) awaiting approval. Approve to unblock.`
    };
  if (failedTasks.length > 0) {
    let first = failedTasks[0];
    return inProgressTasks.length === 0 && pendingTasks.length === 0 ? {
      action: "investigate_failure",
      targetTaskId: first.taskId,
      command: `agentledger audit -r ${runId}`,
      detail: `${failedTasks.length} task(s) failed (first: ${first.reason}). Review audit before retrying.`
    } : {
      action: "retry_failed_task",
      targetTaskId: first.taskId,
      command: `agentledger resume ${runId}`,
      detail: `Task "${first.taskId}" failed (${first.reason}). Fix issue and retry.`
    };
  }
  let readyNow = pendingTasks.filter((t) => t.blockedBy.length === 0);
  return readyNow.length > 0 || inProgressTasks.length > 0 ? {
    action: "resume_run",
    targetTaskId: readyNow[0]?.taskId ?? inProgressTasks[0]?.taskId,
    command: `agentledger resume ${runId}`,
    detail: `${readyNow.length + inProgressTasks.length} task(s) ready. Resume to continue.`
  } : {
    action: "investigate_failure",
    command: `agentledger audit -r ${runId}`,
    detail: "Run in unknown state. Review audit report."
  };
}
function buildContextSummary(runId, goal, runStatus, operator, completedTasks, failedTasks, inProgressTasks, pendingTasks, awaitingApproval, unresolvedRisks, guidance) {
  let lines = [];
  if (lines.push(`RUN ${runId} [${runStatus.toUpperCase()}]`), lines.push(`GOAL: ${goal}`), operator && lines.push(`STARTED BY: ${operator}`), completedTasks.length > 0) {
    lines.push(""), lines.push("COMPLETED:");
    for (let t of completedTasks) {
      let files = t.filesModified.length > 0 ? ` \u2192 ${t.filesModified.join(", ")}` : "";
      lines.push(`  \u2713 ${t.taskId}: ${t.title}${files}`);
    }
  }
  if (failedTasks.length > 0) {
    lines.push(""), lines.push("FAILED:");
    for (let t of failedTasks) {
      let ctxPart = t.context?.violatedFile ? ` (${t.context.violatedFile})` : t.context?.detail ? ` (${t.context.detail.slice(0, 60)})` : "";
      lines.push(`  \u2717 ${t.taskId}: ${t.title} [${t.reason}${ctxPart}]`);
    }
  }
  if (awaitingApproval.length > 0) {
    lines.push(""), lines.push("AWAITING APPROVAL:");
    for (let t of awaitingApproval)
      lines.push(`  \u23F3 ${t.taskId}: ${t.title}`);
  }
  if (inProgressTasks.length > 0) {
    lines.push(""), lines.push("IN PROGRESS:");
    for (let t of inProgressTasks)
      lines.push(`  \u21BA ${t.taskId}: ${t.title}`);
  }
  if (pendingTasks.length > 0) {
    lines.push(""), lines.push("PENDING:");
    for (let t of pendingTasks) {
      let state = t.blockedBy.length > 0 ? `[blocked: ${t.blockedBy.join(", ")}]` : "[ready]";
      lines.push(`  \u2192 ${t.taskId}: ${t.title} ${state}`);
    }
  }
  if (unresolvedRisks.length > 0) {
    lines.push(""), lines.push("UNRESOLVED RISKS:");
    for (let r of unresolvedRisks)
      lines.push(`  \u26A0 ${r.severity}/${r.category} in ${r.filePath} [${r.pattern}]`);
  }
  return lines.push(""), lines.push(`NEXT: ${guidance.detail}`), lines.push(`CMD: ${guidance.command}`), lines.join(`
`).slice(0, MAX_SUMMARY_CHARS);
}
function generateHandoffBrief(events, runId) {
  let state = replayLedger(events, runId), runEvents = events.filter((e) => e.run_id === runId), approvalRequestedAt = /* @__PURE__ */ new Map();
  for (let event of runEvents)
    if (event.event_type === "HUMAN_APPROVAL_REQUESTED") {
      let taskId = resolveTaskId2(event);
      taskId && !approvalRequestedAt.has(taskId) && approvalRequestedAt.set(taskId, event.timestamp);
    }
  let patchFilesByTask = /* @__PURE__ */ new Map();
  for (let event of runEvents)
    if (event.event_type === "PATCH_PROPOSED") {
      let taskId = resolveTaskId2(event);
      taskId && patchFilesByTask.set(taskId, Array.isArray(event.payload.filesModified) ? event.payload.filesModified : []);
    }
  let completedTasks = [], failedTasks = [], inProgressTasks = [], pendingTasks = [], awaitingApproval = [], completedTaskIds = /* @__PURE__ */ new Set();
  for (let task of state.tasks)
    switch (task.status) {
      case "completed": {
        completedTaskIds.add(task.taskId), completedTasks.push({
          taskId: task.taskId,
          title: task.title,
          summary: "",
          filesModified: patchFilesByTask.get(task.taskId) ?? []
        });
        break;
      }
      case "failed": {
        let { reason, context } = classifyFailure(task.taskId, runEvents);
        failedTasks.push({
          taskId: task.taskId,
          title: task.title,
          reason,
          context,
          attemptedFiles: patchFilesByTask.get(task.taskId) ?? []
        });
        break;
      }
      case "awaiting_approval": {
        awaitingApproval.push({
          taskId: task.taskId,
          title: task.title,
          requestedAt: approvalRequestedAt.get(task.taskId) ?? task.taskId
        });
        break;
      }
      case "running":
      case "awaiting_verification": {
        inProgressTasks.push({
          taskId: task.taskId,
          title: task.title,
          description: task.description,
          owner: task.owner,
          blockedBy: []
        });
        break;
      }
      case "pending":
      case "assigned": {
        let blockedBy = task.dependencies.filter((depId) => {
          let dep = state.tasks.find((t) => t.taskId === depId);
          return !dep || dep.status !== "completed";
        });
        pendingTasks.push({
          taskId: task.taskId,
          title: task.title,
          description: task.description,
          owner: task.owner,
          blockedBy
        });
        break;
      }
    }
  let fileInventory = buildFileInventory(runEvents, completedTaskIds), unresolvedRisks = extractUnresolvedRisks(runEvents, completedTaskIds), resumptionGuidance = buildResumptionGuidance(runId, state.status, awaitingApproval, failedTasks, pendingTasks, inProgressTasks), contextSummary = buildContextSummary(runId, state.goal, state.status, state.operator, completedTasks, failedTasks, inProgressTasks, pendingTasks, awaitingApproval, unresolvedRisks, resumptionGuidance);
  return HandoffBriefSchema.parse({
    generatedAt: (/* @__PURE__ */ new Date()).toISOString(),
    runId,
    goal: state.goal,
    operator: state.operator,
    runStatus: state.status,
    completedTasks,
    failedTasks,
    inProgressTasks,
    pendingTasks,
    awaitingApproval,
    fileInventory,
    unresolvedRisks,
    resumptionGuidance,
    contextSummary
  });
}
var SEVERITY_ORDER, MAX_SUMMARY_CHARS, init_generateHandoffBrief = __esm({
  "../core/dist/handoff/generateHandoffBrief.js"() {
    "use strict";
    init_schemas();
    init_replayLedger();
    SEVERITY_ORDER = { critical: 3, high: 2, medium: 1 };
    MAX_SUMMARY_CHARS = 2e3;
  }
});

// ../core/dist/handoff/index.js
var init_handoff = __esm({
  "../core/dist/handoff/index.js"() {
    "use strict";
    init_generateHandoff();
    init_generateHandoffBrief();
  }
});

// ../core/dist/context/priorTaskContext.js
function buildPriorTaskContext(events, task) {
  if (task.dependencies.length === 0)
    return [];
  let depSet = new Set(task.dependencies), titleByTaskId = /* @__PURE__ */ new Map();
  for (let event of events) {
    if (event.run_id !== task.runId || event.event_type !== "TASK_CREATED")
      continue;
    let taskId = resolveTaskId3(event);
    taskId && depSet.has(taskId) && titleByTaskId.set(taskId, typeof event.payload.title == "string" ? event.payload.title : taskId);
  }
  let latestPatch = /* @__PURE__ */ new Map();
  for (let event of events) {
    if (event.run_id !== task.runId || event.event_type !== "PATCH_PROPOSED")
      continue;
    let taskId = resolveTaskId3(event);
    taskId && depSet.has(taskId) && latestPatch.set(taskId, event);
  }
  let result = [];
  for (let depId of task.dependencies) {
    let patch = latestPatch.get(depId);
    patch && result.push({
      taskId: depId,
      title: titleByTaskId.get(depId) ?? depId,
      summary: typeof patch.payload.summary == "string" ? patch.payload.summary : "",
      filesModified: Array.isArray(patch.payload.filesModified) ? patch.payload.filesModified : []
    });
  }
  return result;
}
function resolveTaskId3(event) {
  return event.task_id ?? (typeof event.payload.taskId == "string" ? event.payload.taskId : void 0);
}
var init_priorTaskContext = __esm({
  "../core/dist/context/priorTaskContext.js"() {
    "use strict";
  }
});

// ../core/dist/context/index.js
var init_context = __esm({
  "../core/dist/context/index.js"() {
    "use strict";
    init_priorTaskContext();
  }
});

// ../core/dist/reassignment/reassignTask.js
function validateReassignment(task, newOwner) {
  if (!newOwner.trim())
    throw new Error("newOwner must be a non-empty string.");
  if (task.status !== "pending")
    throw new Error(`Cannot reassign task "${task.taskId}": status is "${task.status}" but must be "pending". Only pending tasks can be reassigned.`);
}
async function reassignTask(task, newOwner, writer) {
  validateReassignment(task, newOwner), await writer.appendEvent({
    event_id: LedgerWriter.createEventId(),
    run_id: task.runId,
    task_id: task.taskId,
    timestamp: (/* @__PURE__ */ new Date()).toISOString(),
    actor: "orchestrator",
    event_type: "TASK_ASSIGNED",
    payload: {
      taskId: task.taskId,
      owner: newOwner
    }
  });
}
var init_reassignTask = __esm({
  "../core/dist/reassignment/reassignTask.js"() {
    "use strict";
    init_LedgerWriter();
  }
});

// ../core/dist/reassignment/index.js
var init_reassignment = __esm({
  "../core/dist/reassignment/index.js"() {
    "use strict";
    init_reassignTask();
  }
});

// ../core/dist/governance/patchScanner.js
function parseAddedLines(diff) {
  let lines = [], currentFile = "", currentLineNumber = 0;
  for (let raw of diff.split(`
`)) {
    if (raw.startsWith("+++ b/")) {
      currentFile = raw.slice(6).trim(), currentLineNumber = 0;
      continue;
    }
    let hunk = /^@@ -\d+(?:,\d+)? \+(\d+)(?:,\d+)? @@/.exec(raw);
    if (hunk) {
      currentLineNumber = parseInt(hunk[1], 10) - 1;
      continue;
    }
    if (raw.startsWith(" ")) {
      currentLineNumber++;
      continue;
    }
    raw.startsWith("-") || raw.startsWith("+") && currentFile && (currentLineNumber++, lines.push({ filePath: currentFile, lineNumber: currentLineNumber, content: raw.slice(1) }));
  }
  return lines;
}
function scanPatch(diff) {
  if (!diff.trim())
    return [];
  let addedLines = parseAddedLines(diff), risks = [];
  for (let { filePath, lineNumber, content } of addedLines) {
    for (let { pattern, label } of SECRET_PATTERNS)
      if (pattern.test(content)) {
        risks.push({
          pattern: label,
          severity: "critical",
          category: "secret",
          filePath,
          lineNumber,
          lineContext: content.trim().slice(0, 120)
        });
        break;
      }
    for (let { pattern, label } of SCHEMA_PATTERNS)
      if (pattern.test(content)) {
        risks.push({
          pattern: label,
          severity: "high",
          category: "schema_mutation",
          filePath,
          lineNumber,
          lineContext: content.trim().slice(0, 120)
        });
        break;
      }
    for (let { pattern, label } of AUTH_PATTERNS)
      if (pattern.test(content)) {
        risks.push({
          pattern: label,
          severity: "medium",
          category: "auth_code",
          filePath,
          lineNumber,
          lineContext: content.trim().slice(0, 120)
        });
        break;
      }
    for (let { pattern, label } of DEP_PATTERNS)
      if (pattern.test(content)) {
        risks.push({
          pattern: label,
          severity: "medium",
          category: "dependency_change",
          filePath,
          lineNumber,
          lineContext: content.trim().slice(0, 120)
        });
        break;
      }
  }
  return risks;
}
var SECRET_PATTERNS, SCHEMA_PATTERNS, AUTH_PATTERNS, DEP_PATTERNS, init_patchScanner = __esm({
  "../core/dist/governance/patchScanner.js"() {
    "use strict";
    SECRET_PATTERNS = [
      { pattern: /(?:api[_-]?key|apikey)\s*[=:]\s*['"]?[A-Za-z0-9_\-]{16,}/i, label: "api_key_assignment" },
      { pattern: /(?:secret|password|passwd|pwd)\s*[=:]\s*['"]?.{8,}/i, label: "secret_assignment" },
      { pattern: /['"]?sk-[A-Za-z0-9]{20,}['"]?/, label: "openai_key" },
      { pattern: /['"]?xox[baprs]-[A-Za-z0-9\-]{10,}['"]?/, label: "slack_token" },
      { pattern: /(?:aws_access_key_id|aws_secret_access_key)\s*[=:]\s*['"]?[A-Za-z0-9\/+]{16,}/i, label: "aws_credential" },
      { pattern: /['"]?ghp_[A-Za-z0-9]{36}['"]?/, label: "github_pat" },
      { pattern: /(?:bearer|token)\s*[=:]\s*['"]?[A-Za-z0-9_\-.]{20,}/i, label: "bearer_token" },
      { pattern: /ANTHROPIC_API_KEY\s*[=:]\s*['"]?sk-ant-[A-Za-z0-9_\-]{20,}/, label: "anthropic_key" }
    ], SCHEMA_PATTERNS = [
      { pattern: /\bALTER\s+TABLE\b/i, label: "sql_alter_table" },
      { pattern: /\bDROP\s+(?:TABLE|COLUMN|INDEX)\b/i, label: "sql_drop" },
      { pattern: /\bCREATE\s+TABLE\b/i, label: "sql_create_table" },
      { pattern: /\bCREATE\s+(?:UNIQUE\s+)?INDEX\b/i, label: "sql_create_index" },
      { pattern: /\bADD\s+COLUMN\b/i, label: "sql_add_column" },
      { pattern: /migrate\s*\(/i, label: "migration_call" },
      { pattern: /schema\.prisma/, label: "prisma_schema_edit" },
      { pattern: /\.migration\.(ts|js|sql)['"]?\s*$/, label: "migration_file" }
    ], AUTH_PATTERNS = [
      { pattern: /\bjwt\.(?:sign|verify|decode)\b/, label: "jwt_operation" },
      { pattern: /\bbcrypt\.(?:hash|compare)\b/, label: "bcrypt_operation" },
      { pattern: /\bpassport\.(?:use|authenticate)\b/, label: "passport_auth" },
      { pattern: /\bverifyToken\s*\(/, label: "verify_token_call" },
      { pattern: /\bcheckAuth\s*\(/, label: "check_auth_call" },
      { pattern: /\brequireAuth\b/, label: "require_auth_middleware" },
      { pattern: /\bauthorize\s*\(/, label: "authorize_call" },
      { pattern: /\brole\s*(?:===|!==|==)\s*['"][A-Za-z]+['"]/, label: "role_check" },
      { pattern: /\.hasPermission\s*\(/, label: "permission_check" },
      { pattern: /\bsession\.(?:user|userId|token)\b/, label: "session_access" }
    ], DEP_PATTERNS = [
      { pattern: /"dependencies"\s*:\s*\{/, label: "package_json_dependencies" },
      { pattern: /"devDependencies"\s*:\s*\{/, label: "package_json_dev_dependencies" },
      { pattern: /^[+-]\s*"[^"]+"\s*:\s*"[^"]+"/, label: "dependency_version_change" },
      { pattern: /\brequire\(['"][^'"]+['"]\)/, label: "new_require" },
      { pattern: /^import\s+.+from\s+['"][^.][^'"]+['"]/, label: "new_external_import" }
    ];
  }
});

// ../core/dist/governance/policyEngine.js
function meetsMinSeverity(risk, min) {
  return SEVERITY_ORDER2[risk.severity] >= SEVERITY_ORDER2[min];
}
function evaluatePolicy(_task, patchRisks, policy = DEFAULT_GOVERNANCE_POLICY) {
  if (patchRisks.length === 0)
    return { action: "allow", reasons: [], risks: [] };
  let reasons = [], matchedRisks = [], topAction = "allow", actionPriority = {
    allow: 0,
    warn: 1,
    require_approval: 2,
    deny: 3
  };
  for (let rule of policy.rules) {
    let matching = patchRisks.filter((r) => rule.categories.includes(r.category) && (rule.minSeverity === void 0 || meetsMinSeverity(r, rule.minSeverity)));
    if (matching.length === 0)
      continue;
    let action = rule.type === "deny_if" ? "deny" : rule.type === "require_approval_if" ? "require_approval" : "warn";
    actionPriority[action] > actionPriority[topAction] && (topAction = action);
    for (let risk of matching)
      reasons.push(`${action.toUpperCase()} \u2014 ${risk.category} (${risk.severity}) at ${risk.filePath}:${risk.lineNumber} [${risk.pattern}]`), matchedRisks.includes(risk) || matchedRisks.push(risk);
  }
  return { action: topAction, reasons, risks: matchedRisks };
}
async function loadGovernancePolicy(configDir) {
  let policyPath = (0, import_node_path.join)(configDir, "governance.json");
  try {
    let raw = await (0, import_promises12.readFile)(policyPath, "utf-8");
    return GovernancePolicySchema.parse(JSON.parse(raw));
  } catch {
    return DEFAULT_GOVERNANCE_POLICY;
  }
}
var import_promises12, import_node_path, SEVERITY_ORDER2, DEFAULT_GOVERNANCE_POLICY, init_policyEngine = __esm({
  "../core/dist/governance/policyEngine.js"() {
    "use strict";
    import_promises12 = require("node:fs/promises"), import_node_path = require("node:path");
    init_schemas();
    SEVERITY_ORDER2 = {
      critical: 3,
      high: 2,
      medium: 1
    };
    DEFAULT_GOVERNANCE_POLICY = {
      rules: [
        {
          type: "deny_if",
          categories: ["secret"],
          minSeverity: "critical"
        },
        {
          type: "require_approval_if",
          categories: ["schema_mutation"],
          minSeverity: "high"
        },
        {
          type: "require_approval_if",
          categories: ["auth_code"],
          minSeverity: "medium"
        },
        {
          type: "warn_if",
          categories: ["dependency_change"],
          minSeverity: "medium"
        }
      ]
    };
  }
});

// ../core/dist/governance/auditReport.js
function clamp(value, max) {
  return Math.min(value, max);
}
function computeRiskScore(tasks) {
  let secretExposure = 0, schemaChange = 0, authChange = 0, boundaryViolation = 0, toolDenial = 0;
  for (let task of tasks) {
    for (let risk of task.patchRisks)
      risk.category === "secret" ? secretExposure += WEIGHTS.secret_per_finding : risk.category === "schema_mutation" ? schemaChange += WEIGHTS.schema_per_finding : risk.category === "auth_code" && (authChange += WEIGHTS.auth_per_finding);
    boundaryViolation += task.boundaryViolations.length * WEIGHTS.boundary_per_violation, toolDenial += task.toolDenials.length * WEIGHTS.tool_denial_per;
  }
  let breakdown = {
    secret_exposure: clamp(secretExposure, 40),
    schema_change: clamp(schemaChange, 30),
    auth_change: clamp(authChange, 20),
    boundary_violation: clamp(boundaryViolation, 10),
    tool_denial: clamp(toolDenial, 10)
  };
  return { total: Math.min(breakdown.secret_exposure + breakdown.schema_change + breakdown.auth_change + breakdown.boundary_violation + breakdown.tool_denial, 100), breakdown };
}
function generateAuditReport(events, runId) {
  let runEvents = events.filter((e) => e.run_id === runId), runState = replayLedger(runEvents, runId), taskRecords = runState.tasks.map((task) => {
    let taskEvents = runEvents.filter((e) => e.task_id === task.taskId), patchRisks = [];
    for (let ev of taskEvents)
      if (ev.event_type === "PATCH_RISK_DETECTED") {
        let raw = ev.payload.risks;
        Array.isArray(raw) && patchRisks.push(...raw);
      }
    let toolDenials = [];
    for (let ev of taskEvents)
      if (ev.event_type === "TOOL_DENIED") {
        let d = ev.payload;
        d.toolName && d.path && d.reason && d.violationType && toolDenials.push(d);
      }
    let boundaryViolations = [];
    for (let ev of taskEvents)
      if (ev.event_type === "BOUNDARY_VIOLATION") {
        let bv = ev.payload;
        bv.violationType && bv.file && bv.message && boundaryViolations.push(bv);
      }
    let policyDecision;
    for (let ev of taskEvents)
      ev.event_type === "POLICY_EVALUATED" && (policyDecision = ev.payload.decision);
    let approvalRecord, approvalReq = taskEvents.find((e) => e.event_type === "HUMAN_APPROVAL_REQUESTED"), approvalGrant = taskEvents.find((e) => e.event_type === "HUMAN_APPROVAL_GRANTED"), approvalReject = taskEvents.find((e) => e.event_type === "HUMAN_APPROVAL_REJECTED");
    if (approvalReq) {
      let reasons = Array.isArray(approvalReq.payload.reasons) ? approvalReq.payload.reasons : [];
      approvalRecord = {
        taskId: task.taskId,
        requestedAt: approvalReq.timestamp,
        grantedAt: approvalGrant?.timestamp,
        rejectedAt: approvalReject?.timestamp,
        reasons
      };
    }
    let filesModified = [];
    for (let ev of taskEvents)
      if (ev.event_type === "PATCH_PROPOSED") {
        let raw = ev.payload.filesModified;
        Array.isArray(raw) && filesModified.push(...raw);
      }
    return {
      taskId: task.taskId,
      title: task.title,
      status: task.status,
      filesModified,
      patchRisks,
      toolDenials,
      boundaryViolations,
      policyDecision,
      approvalRecord
    };
  }), totalApprovals = taskRecords.filter((t) => t.approvalRecord !== void 0).length, granted = taskRecords.filter((t) => t.approvalRecord?.grantedAt !== void 0).length, rejected = taskRecords.filter((t) => t.approvalRecord?.rejectedAt !== void 0).length, pending = totalApprovals - granted - rejected, allFilesSet = /* @__PURE__ */ new Set();
  for (let t of taskRecords)
    t.filesModified.forEach((f) => allFilesSet.add(f));
  let policyDecisionCounts = { allow: 0, warn: 0, require_approval: 0, deny: 0 };
  for (let task of taskRecords)
    task.policyDecision && policyDecisionCounts[task.policyDecision.action]++;
  let thresholdBreachEvent = runEvents.find((e) => e.event_type === "RISK_THRESHOLD_BREACHED"), thresholdBreached = thresholdBreachEvent !== void 0, thresholdBreachAction = thresholdBreachEvent?.payload.action, governanceSummary = {
    policyDecisionCounts,
    thresholdBreached,
    thresholdBreachAction
  }, riskScore = computeRiskScore(taskRecords);
  return {
    runId,
    goal: runState.goal,
    runStatus: runState.status,
    generatedAt: (/* @__PURE__ */ new Date()).toISOString(),
    tasks: taskRecords,
    allFilesModified: [...allFilesSet],
    riskScore,
    approvalsSummary: { total: totalApprovals, granted, rejected, pending },
    governanceSummary
  };
}
var WEIGHTS, init_auditReport = __esm({
  "../core/dist/governance/auditReport.js"() {
    "use strict";
    init_replayLedger();
    WEIGHTS = {
      secret_per_finding: 20,
      // capped at 40
      schema_per_finding: 15,
      // capped at 30
      auth_per_finding: 10,
      // capped at 20
      boundary_per_violation: 5,
      // capped at 10
      tool_denial_per: 2
      // capped at 10
    };
  }
});

// ../core/dist/governance/riskThreshold.js
function checkRiskThreshold(cumulativeScore, policy) {
  return policy.riskThreshold === void 0 ? null : {
    breached: cumulativeScore > policy.riskThreshold,
    threshold: policy.riskThreshold,
    actualScore: cumulativeScore,
    action: policy.thresholdAction ?? "warn"
  };
}
var init_riskThreshold = __esm({
  "../core/dist/governance/riskThreshold.js"() {
    "use strict";
  }
});

// ../core/dist/governance/effectivePolicy.js
async function loadEffectivePolicy(configDir, task) {
  let runPolicy = await loadGovernancePolicy(configDir);
  if (!task?.governancePolicyFile)
    return runPolicy;
  let taskPolicy = null;
  try {
    let raw = await (0, import_promises13.readFile)((0, import_node_path2.join)(configDir, task.governancePolicyFile), "utf8");
    taskPolicy = GovernancePolicySchema.parse(JSON.parse(raw));
  } catch {
    return runPolicy;
  }
  return {
    rules: [...runPolicy.rules, ...taskPolicy.rules],
    riskThreshold: taskPolicy.riskThreshold ?? runPolicy.riskThreshold,
    thresholdAction: taskPolicy.thresholdAction ?? runPolicy.thresholdAction
  };
}
var import_promises13, import_node_path2, init_effectivePolicy = __esm({
  "../core/dist/governance/effectivePolicy.js"() {
    "use strict";
    import_promises13 = require("node:fs/promises"), import_node_path2 = require("node:path");
    init_schemas();
    init_policyEngine();
  }
});

// ../core/dist/governance/leaderboard.js
function buildLeaderboard(events) {
  let runIds = [
    ...new Set(events.filter((e) => e.event_type === "RUN_CREATED").map((e) => e.run_id))
  ], entries = [];
  for (let runId of runIds) {
    let report = generateAuditReport(events, runId);
    for (let task of report.tasks) {
      let taskScore = computeRiskScore([task]), denyCount = task.policyDecision?.action === "deny" ? 1 : 0, requireApprovalCount = task.policyDecision?.action === "require_approval" ? 1 : 0;
      entries.push({
        runId,
        taskId: task.taskId,
        title: task.title,
        riskScore: taskScore.total,
        denyCount,
        requireApprovalCount,
        boundaryViolationCount: task.boundaryViolations.length,
        toolDenialCount: task.toolDenials.length
      });
    }
  }
  return entries.sort((a, b) => b.riskScore !== a.riskScore ? b.riskScore - a.riskScore : a.taskId.localeCompare(b.taskId)), {
    generatedAt: (/* @__PURE__ */ new Date()).toISOString(),
    entries
  };
}
var init_leaderboard = __esm({
  "../core/dist/governance/leaderboard.js"() {
    "use strict";
    init_auditReport();
  }
});

// ../core/dist/governance/index.js
var init_governance = __esm({
  "../core/dist/governance/index.js"() {
    "use strict";
    init_patchScanner();
    init_policyEngine();
    init_auditReport();
    init_riskThreshold();
    init_effectivePolicy();
    init_leaderboard();
  }
});

// ../core/dist/scheduler/TaskScheduler.js
function buildDependentIndex(tasks) {
  let index = /* @__PURE__ */ new Map();
  for (let task of tasks)
    for (let depId of task.dependencies) {
      let set = index.get(depId);
      set || (set = /* @__PURE__ */ new Set(), index.set(depId, set)), set.add(task.taskId);
    }
  return index;
}
var TaskScheduler, init_TaskScheduler = __esm({
  "../core/dist/scheduler/TaskScheduler.js"() {
    "use strict";
    TaskScheduler = class {
      tasks;
      statuses;
      /** Map from taskId → set of taskIds that directly depend on it */
      dependents;
      constructor(tasks) {
        this.tasks = new Map(tasks.map((t) => [t.taskId, t])), this.statuses = new Map(tasks.map((t) => [t.taskId, "pending"])), this.dependents = buildDependentIndex(tasks);
      }
      getStatus(taskId) {
        let status = this.statuses.get(taskId);
        if (status === void 0)
          throw new Error(`Unknown taskId: "${taskId}"`);
        return status;
      }
      getReadyTasks() {
        let ready = [];
        for (let [taskId, status] of this.statuses) {
          if (status !== "pending")
            continue;
          let task = this.tasks.get(taskId);
          this.allDepsCompleted(task) && ready.push(task);
        }
        return ready;
      }
      isDone() {
        for (let status of this.statuses.values())
          if (status === "pending" || status === "running")
            return !1;
        return !0;
      }
      markRunning(taskId) {
        this.assertKnown(taskId), this.statuses.set(taskId, "running");
      }
      markCompleted(taskId) {
        this.assertKnown(taskId), this.statuses.set(taskId, "completed");
      }
      markFailed(taskId) {
        this.assertKnown(taskId), this.cascadeFail(taskId);
      }
      getStats() {
        let stats = { pending: 0, running: 0, completed: 0, failed: 0 };
        for (let status of this.statuses.values())
          stats[status]++;
        return stats;
      }
      // ─── private helpers ─────────────────────────────────────────────────────────
      allDepsCompleted(task) {
        return task.dependencies.every((depId) => this.statuses.get(depId) === "completed");
      }
      cascadeFail(taskId) {
        let queue = [taskId];
        for (; queue.length > 0; ) {
          let current = queue.shift();
          if (this.statuses.get(current) === "completed")
            continue;
          this.statuses.set(current, "failed");
          let deps = this.dependents.get(current);
          if (deps)
            for (let dep of deps)
              this.statuses.get(dep) !== "completed" && this.statuses.get(dep) !== "failed" && queue.push(dep);
        }
      }
      assertKnown(taskId) {
        if (!this.tasks.has(taskId))
          throw new Error(`Unknown taskId: "${taskId}"`);
      }
    };
  }
});

// ../core/dist/scheduler/index.js
var init_scheduler = __esm({
  "../core/dist/scheduler/index.js"() {
    "use strict";
    init_TaskScheduler();
  }
});

// ../core/dist/projects/registry.js
function homeDir() {
  return process.env.AGENTLEDGER_HOME ?? import_node_path3.default.join(import_node_os.default.homedir(), ".agentledger");
}
function registryPath() {
  return import_node_path3.default.join(homeDir(), "projects.json");
}
function canonicalize2(repoPath) {
  try {
    return import_node_fs.default.realpathSync(repoPath);
  } catch {
    return import_node_path3.default.resolve(repoPath);
  }
}
async function readRegistry(file = registryPath()) {
  let raw;
  try {
    raw = import_node_fs.default.readFileSync(file, "utf8");
  } catch {
    return [];
  }
  let parsed;
  try {
    parsed = JSON.parse(raw);
  } catch {
    return [];
  }
  let full = ProjectRegistrySchema.safeParse(parsed);
  if (full.success)
    return full.data.projects;
  let projects = parsed?.projects;
  return Array.isArray(projects) ? projects.flatMap((entry) => {
    let row = ProjectEntrySchema.safeParse(entry);
    return row.success ? [row.data] : [];
  }) : [];
}
async function registerProject(repoPath) {
  let canonical = canonicalize2(repoPath), name = import_node_path3.default.basename(canonical), now = (/* @__PURE__ */ new Date()).toISOString(), file = registryPath();
  import_node_fs.default.mkdirSync(import_node_path3.default.dirname(file), { recursive: !0 }), import_node_fs.default.existsSync(file) || import_node_fs.default.writeFileSync(file, JSON.stringify({ version: 1, projects: [] }, null, 2));
  let release = await import_proper_lockfile2.default.lock(file, { retries: { retries: 10, minTimeout: 50, maxTimeout: 500 } });
  try {
    let existing = await readRegistry(), prior = existing.find((p) => p.path === canonical), next = prior ? { ...prior, lastSeen: now } : { path: canonical, name, firstSeen: now, lastSeen: now }, projects = prior ? existing.map((p) => p.path === canonical ? next : p) : [...existing, next];
    import_node_fs.default.writeFileSync(file, JSON.stringify({ version: 1, projects }, null, 2));
  } finally {
    await release();
  }
}
var import_node_fs, import_node_path3, import_node_os, import_proper_lockfile2, init_registry = __esm({
  "../core/dist/projects/registry.js"() {
    "use strict";
    import_node_fs = __toESM(require("node:fs"), 1), import_node_path3 = __toESM(require("node:path"), 1), import_node_os = __toESM(require("node:os"), 1), import_proper_lockfile2 = __toESM(require_proper_lockfile(), 1);
    init_schemas();
  }
});

// ../core/dist/projects/index.js
var init_projects2 = __esm({
  "../core/dist/projects/index.js"() {
    "use strict";
    init_registry();
  }
});

// ../core/dist/index.js
var dist_exports = {};
__export(dist_exports, {
  AgentLedgerConfigSchema: () => AgentLedgerConfigSchema,
  AgentTaskSchema: () => AgentTaskSchema,
  ApprovalPolicySchema: () => ApprovalPolicySchema,
  ApprovalRecordSchema: () => ApprovalRecordSchema,
  ApprovalTriggerSchema: () => ApprovalTriggerSchema,
  AuditReportSchema: () => AuditReportSchema,
  AuditTaskRecordSchema: () => AuditTaskRecordSchema,
  AwaitingApprovalSummarySchema: () => AwaitingApprovalSummarySchema,
  BoundaryCheckResultSchema: () => BoundaryCheckResultSchema,
  BoundaryViolationSchema: () => BoundaryViolationSchema,
  CommandResultSchema: () => CommandResultSchema,
  CompletedTaskSummarySchema: () => CompletedTaskSummarySchema,
  DEFAULT_GOVERNANCE_POLICY: () => DEFAULT_GOVERNANCE_POLICY,
  DEFAULT_PLANNER_MODEL: () => DEFAULT_PLANNER_MODEL,
  DEFAULT_TOGETHER_PLANNER_MODEL: () => DEFAULT_TOGETHER_PLANNER_MODEL,
  DEFAULT_TOGETHER_WORKER_MODEL: () => DEFAULT_TOGETHER_WORKER_MODEL,
  DEFAULT_WORKER_MODEL: () => DEFAULT_WORKER_MODEL,
  FailedTaskDetailSchema: () => FailedTaskDetailSchema,
  FailedTaskSummarySchema: () => FailedTaskSummarySchema,
  FailureContextSchema: () => FailureContextSchema,
  FailureReasonSchema: () => FailureReasonSchema,
  FileInventorySchema: () => FileInventorySchema,
  GovernancePolicyRuleSchema: () => GovernancePolicyRuleSchema,
  GovernancePolicySchema: () => GovernancePolicySchema,
  GovernanceSummarySchema: () => GovernanceSummarySchema,
  HandoffBriefSchema: () => HandoffBriefSchema,
  HandoffDocumentSchema: () => HandoffDocumentSchema,
  IntentContractSchema: () => IntentContractSchema,
  LeaderboardEntrySchema: () => LeaderboardEntrySchema,
  LedgerEventSchema: () => LedgerEventSchema,
  LedgerEventTypeSchema: () => LedgerEventTypeSchema,
  LedgerReader: () => LedgerReader,
  LedgerWriter: () => LedgerWriter,
  PatchRiskCategorySchema: () => PatchRiskCategorySchema,
  PatchRiskSchema: () => PatchRiskSchema,
  PatchRiskSeveritySchema: () => PatchRiskSeveritySchema,
  PendingApprovalSchema: () => PendingApprovalSchema,
  PendingTaskSummarySchema: () => PendingTaskSummarySchema,
  PolicyDecisionSchema: () => PolicyDecisionSchema,
  PolicyLeaderboardSchema: () => PolicyLeaderboardSchema,
  PriorTaskContextSchema: () => PriorTaskContextSchema,
  ProjectEntrySchema: () => ProjectEntrySchema,
  ProjectRegistrySchema: () => ProjectRegistrySchema,
  ResumptionActionSchema: () => ResumptionActionSchema,
  ResumptionGuidanceSchema: () => ResumptionGuidanceSchema,
  RiskScoreBreakdownSchema: () => RiskScoreBreakdownSchema,
  RiskScoreSchema: () => RiskScoreSchema,
  RiskThresholdActionSchema: () => RiskThresholdActionSchema,
  RunCreatedPayloadSchema: () => RunCreatedPayloadSchema,
  RunModeSchema: () => RunModeSchema,
  RunReplayError: () => RunReplayError,
  RunStateSchema: () => RunStateSchema,
  RunStatusSchema: () => RunStatusSchema,
  SchemaValidationError: () => SchemaValidationError,
  TaskGraphSchema: () => TaskGraphSchema,
  TaskScheduler: () => TaskScheduler,
  TaskStatusSchema: () => TaskStatusSchema,
  ToolDenialSchema: () => ToolDenialSchema,
  UnresolvedRiskSchema: () => UnresolvedRiskSchema,
  VerificationCommandSchema: () => VerificationCommandSchema,
  VerificationResultSchema: () => VerificationResultSchema,
  WorkerContextSchema: () => WorkerContextSchema,
  WorkerResultSchema: () => WorkerResultSchema,
  WorktreeHandleSchema: () => WorktreeHandleSchema,
  _resetClient: () => _resetClient,
  _resetTogetherClient: () => _resetTogetherClient,
  buildLeaderboard: () => buildLeaderboard,
  buildPlannerSystemPrompt: () => buildPlannerSystemPrompt,
  buildPlannerUserMessage: () => buildPlannerUserMessage,
  buildPriorTaskContext: () => buildPriorTaskContext,
  buildWorkerSystemPrompt: () => buildWorkerSystemPrompt,
  buildWorkerUserMessage: () => buildWorkerUserMessage,
  checkFileBoundaries: () => checkFileBoundaries,
  checkRiskThreshold: () => checkRiskThreshold,
  cleanupWorktree: () => cleanupWorktree,
  computeHash: () => computeHash,
  computeRiskScore: () => computeRiskScore,
  createPlan: () => createPlan,
  createTaskWorktree: () => createTaskWorktree,
  evaluatePolicy: () => evaluatePolicy,
  extractJSON: () => extractJSON,
  formatPriorContextForPrompt: () => formatPriorContextForPrompt,
  gatherRepoContext: () => gatherRepoContext,
  generateAuditReport: () => generateAuditReport,
  generateHandoff: () => generateHandoff,
  generateHandoffBrief: () => generateHandoffBrief,
  generatePatch: () => generatePatch,
  getAnthropicClient: () => getAnthropicClient,
  getPendingApprovals: () => getPendingApprovals,
  getTogetherClient: () => getTogetherClient,
  getWorktreeDiff: () => getWorktreeDiff,
  isApproved: () => isApproved,
  isAwaitingApproval: () => isAwaitingApproval,
  isRejected: () => isRejected,
  isValidHash: () => isValidHash,
  listModifiedFiles: () => listModifiedFiles,
  loadEffectivePolicy: () => loadEffectivePolicy,
  loadGovernancePolicy: () => loadGovernancePolicy,
  planWithLLM: () => planWithLLM,
  planWithTogether: () => planWithTogether,
  readRegistry: () => readRegistry,
  reassignTask: () => reassignTask,
  registerProject: () => registerProject,
  registryPath: () => registryPath,
  replayLedger: () => replayLedger,
  retryWithSchema: () => retryWithSchema,
  runBoundaryViolatingWorker: () => runBoundaryViolatingWorker,
  runClaudeCodeWorker: () => runClaudeCodeWorker,
  runFalseSelfReportWorker: () => runFalseSelfReportWorker,
  runMockWorker: () => runMockWorker,
  runVerificationCommands: () => runVerificationCommands,
  runWorkerLLM: () => runWorkerLLM,
  runWorkerTogether: () => runWorkerTogether,
  scanPatch: () => scanPatch,
  shouldRequireApproval: () => shouldRequireApproval,
  topoSort: () => topoSort,
  validateReassignment: () => validateReassignment,
  validateTaskGraph: () => validateTaskGraph,
  validateWithSchema: () => validateWithSchema,
  verifyTask: () => verifyTask
});
var init_dist = __esm({
  "../core/dist/index.js"() {
    "use strict";
    init_schemas();
    init_ledger();
    init_planner();
    init_git();
    init_worker();
    init_verifier();
    init_replay();
    init_llm();
    init_approvals();
    init_handoff();
    init_context();
    init_reassignment();
    init_governance();
    init_scheduler();
    init_projects2();
  }
});

// scripts/hooks/stop.js
var import_fs9 = __toESM(require("fs"), 1), import_path97 = __toESM(require("path"), 1);

// scripts/state.js
var import_fs = __toESM(require("fs"), 1), import_path = __toESM(require("path"), 1), import_proper_lockfile = __toESM(require_proper_lockfile(), 1);
function sessionStatePath() {
  let projectDir3 = process.env.CLAUDE_PROJECT_DIR ?? process.cwd();
  return import_path.default.join(projectDir3, ".agentledger", "session.json");
}
var DEFAULT_STATE = () => ({
  runId: null,
  dirty: !1,
  sessionStart: (/* @__PURE__ */ new Date()).toISOString(),
  reads: 0,
  edits: 0,
  writes: 0,
  bashCalls: 0,
  blocks: 0,
  warnings: 0,
  claimsDetected: 0,
  claimsVerifiedTrue: 0,
  claimsVerifiedFalse: 0,
  claimsUnverifiable: 0,
  filesRead: [],
  filesEdited: [],
  editWithoutRead: [],
  falseClaims: [],
  // claim type → epoch ms of last verification. Persisted rather than held in
  // process memory because each hook invocation is a fresh process.
  recentVerifications: {}
});
async function readSessionState() {
  let statePath = sessionStatePath();
  if (!import_fs.default.existsSync(statePath))
    return DEFAULT_STATE();
  try {
    let raw = import_fs.default.readFileSync(statePath, "utf8");
    return JSON.parse(raw);
  } catch {
    return DEFAULT_STATE();
  }
}
async function writeSessionState(state) {
  let statePath = sessionStatePath(), dir = import_path.default.dirname(statePath);
  import_fs.default.mkdirSync(dir, { recursive: !0 }), import_fs.default.existsSync(statePath) || import_fs.default.writeFileSync(statePath, JSON.stringify(DEFAULT_STATE(), null, 2));
  let release;
  try {
    release = await import_proper_lockfile.default.lock(statePath, { retries: { retries: 5, minTimeout: 50 } }), import_fs.default.writeFileSync(statePath, JSON.stringify(state, null, 2));
  } finally {
    release && await release();
  }
}

// scripts/run-init.js
var import_fs8 = __toESM(require("fs"), 1), import_path95 = __toESM(require("path"), 1), import_crypto4 = require("crypto"), RUN_GOAL = "Observed Claude Code session";
function projectDir() {
  return process.env.CLAUDE_PROJECT_DIR ?? process.cwd();
}
function ledgerPath() {
  return import_path95.default.join(projectDir(), ".agentledger", "ledger.jsonl");
}
function loadOperator() {
  try {
    let raw = import_fs8.default.readFileSync(import_path95.default.join(projectDir(), ".agentledger", "config.json"), "utf8");
    return JSON.parse(raw).operator || process.env.USER || "unknown";
  } catch {
    return process.env.USER ?? "unknown";
  }
}
async function initRun(runId, actor) {
  let { LedgerWriter: LedgerWriter2 } = await Promise.resolve().then(() => (init_dist(), dist_exports)), target = ledgerPath();
  import_fs8.default.mkdirSync(import_path95.default.dirname(target), { recursive: !0 });
  let writer = new LedgerWriter2(target), timestamp = (/* @__PURE__ */ new Date()).toISOString();
  await writer.appendEvent({
    event_id: `evt_${Date.now()}_run_created`,
    run_id: runId,
    timestamp,
    actor,
    event_type: "RUN_CREATED",
    payload: { goal: RUN_GOAL, operator: loadOperator(), run_mode: "observed" }
  }), await writer.appendEvent({
    event_id: `evt_${Date.now()}_intent`,
    run_id: runId,
    timestamp: (/* @__PURE__ */ new Date()).toISOString(),
    actor,
    event_type: "INTENT_COMPILED",
    payload: { goal: RUN_GOAL, taskCount: 0, tasks: [] }
  });
}
async function ensureRun(state, actor) {
  if (state.runId) return state;
  let runId = (0, import_crypto4.randomUUID)();
  return await initRun(runId, actor), { ...state, runId, dirty: !0 };
}

// scripts/claim-detector.js
var CLAIM_PATTERNS = [
  // "tests pass" / "tests are passing" / "tests all pass" / "test suite passes"
  { pattern: /tests?(?:\s+suite)?\s+(?:\w+\s+){0,2}?(?:pass(?:es|ing|ed)?|succeed(?:s|ed)?|green)/i, type: "test_claim", label: "tests pass" },
  // "48/48 green", "48/48 passing", "24 of 24 pass"
  { pattern: /\b\d+\s*(?:\/|of)\s*\d+\s+(?:tests?\s+)?(?:pass(?:ed|ing)?|green)/i, type: "test_claim", label: "tests pass" },
  // "48 passed (48)" — vitest/jest summary line pasted or paraphrased
  { pattern: /\b\d+\s+passed\b/i, type: "test_claim", label: "tests pass" },
  // "all green", "suite is green", "baseline green"
  { pattern: /\b(?:all|suite|baseline|everything)\s+(?:is\s+)?green\b/i, type: "test_claim", label: "tests green" },
  { pattern: /(?:all\s+)?checks?\s+pass/i, type: "test_claim", label: "checks pass" },
  // "typecheck clean" / "typecheck is clean" / "typechecks cleanly" / "tsc clean"
  { pattern: /(?:typecheck|type-check|tsc|lint|build)s?\s+(?:is\s+|are\s+|runs\s+)?clean(?:ly)?/i, type: "build_claim", label: "typecheck clean" },
  { pattern: /build\s+(?:succeed|pass|green|success|succeeded)/i, type: "build_claim", label: "build succeeds" },
  { pattern: /(?:compiled?|built)\s+(?:successfully|without\s+errors?)/i, type: "build_claim", label: "compiled successfully" },
  { pattern: /successfully\s+(?:built|compiled|tested|deployed)/i, type: "build_claim", label: "successfully built" },
  { pattern: /no\s+(?:errors?|issues?|failures?|bugs?)\s/i, type: "quality_claim", label: "no errors" },
  { pattern: /(?:fixed|resolved)\s+(?:the\s+)?(?:bug|issue|error|problem)/i, type: "fix_claim", label: "fixed the bug" },
  { pattern: /(?:working|works)\s+(?:now|correctly|properly|as\s+expected)/i, type: "fix_claim", label: "working now" }
], QUOTED_SPANS = [
  /```[\s\S]*?```/g,
  // fenced code blocks
  /`[^`]+`/g,
  // inline code
  /^\s*>.*$/gm
  // blockquotes
], HEDGE_MARKERS = /\b(?:should|would|will|won't|can|could|may|might|if|whether|once|unless|try|trying|let me|let's|going to|about to|need to|needs to|expect|expects|expected to|hope|hopefully|see if|check if|verify that|make sure|ensure)\b/i;
function splitSentences(text) {
  return text.split(/(?<=[.!?;:])\s+|\n+/).map((s) => s.trim()).filter(Boolean);
}
function assertiveSentencesOnly(text) {
  let cleaned = text;
  for (let pattern of QUOTED_SPANS)
    cleaned = cleaned.replace(pattern, " ");
  return splitSentences(cleaned).filter((sentence) => !HEDGE_MARKERS.test(sentence)).join(" ");
}
function detectClaims(assistantMessage) {
  if (!assistantMessage || assistantMessage.length < 10)
    return [];
  let cleaned = assertiveSentencesOnly(assistantMessage), claims = [], seenTypes = /* @__PURE__ */ new Set();
  for (let { pattern, type, label } of CLAIM_PATTERNS)
    seenTypes.has(type) || pattern.test(cleaned) && (seenTypes.add(type), claims.push({
      text: label,
      type,
      matchedPattern: pattern.source
    }));
  return claims;
}
function classifyClaims(claims, hasTestCommand) {
  let verifiable = [], unverifiable = [];
  for (let claim of claims)
    claim.type === "test_claim" || claim.type === "build_claim" || claim.type === "fix_claim" || claim.type, hasTestCommand ? verifiable.push(claim) : unverifiable.push(claim);
  return { verifiable, unverifiable };
}

// scripts/verifier.js
var import_path96 = __toESM(require("path"), 1), import_child_process5 = require("child_process");
init_esm3();
var TIMEOUT_EXIT_CODE = 124;
function runTestCommand(testCommand, projectDir3, timeout = 12e4) {
  if (!testCommand)
    return { exitCode: 0, output: "(skipped \u2014 no testCommand configured)", timedOut: !1 };
  try {
    return { exitCode: 0, output: (0, import_child_process5.execSync)(testCommand, {
      cwd: projectDir3,
      encoding: "utf8",
      timeout,
      stdio: ["pipe", "pipe", "pipe"]
    }), timedOut: !1 };
  } catch (err) {
    let timedOut = err.killed === !0 || err.signal === "SIGTERM" || err.code === "ETIMEDOUT";
    return {
      exitCode: err.status ?? (timedOut ? TIMEOUT_EXIT_CODE : 1),
      output: (err.stdout ?? "") + (err.stderr ?? ""),
      timedOut
    };
  }
}
function getChangedFiles(projectDir3) {
  try {
    return (0, import_child_process5.execSync)("git diff --name-only HEAD", {
      cwd: projectDir3,
      encoding: "utf8",
      timeout: 5e3,
      stdio: ["pipe", "pipe", "pipe"]
    }).split(`
`).map((f) => f.trim()).filter(Boolean);
  } catch {
    return [];
  }
}
function detectBoundaryViolations(changedFiles, blockedPatterns) {
  let violations = [];
  for (let file of changedFiles)
    for (let pattern of blockedPatterns)
      if (minimatch(file, pattern, { dot: !0 }) || minimatch(import_path96.default.basename(file), pattern.replace(/\*\*\//, ""), { dot: !0 })) {
        violations.push({ file, pattern });
        break;
      }
  return violations;
}
function verify({ testCommand, blockedFiles, projectDir: projectDir3, testTimeout }) {
  let testResult = runTestCommand(testCommand, projectDir3, testTimeout), changedFiles = getChangedFiles(projectDir3), violations = detectBoundaryViolations(changedFiles, blockedFiles);
  return {
    testsPassed: testResult.exitCode === 0,
    testExitCode: testResult.exitCode,
    testOutput: testResult.output,
    testTimedOut: testResult.timedOut,
    boundaryClean: violations.length === 0,
    violations
  };
}

// scripts/hooks/stop.js
var projectDir2 = process.env.CLAUDE_PROJECT_DIR ?? process.cwd(), configPath = import_path97.default.join(projectDir2, ".agentledger", "config.json"), ledgerPath2 = import_path97.default.join(projectDir2, ".agentledger", "ledger.jsonl"), DEBOUNCE_MS = 6e4, TEST_TIMEOUT_MS = 3e4, DEBUG = !!process.env.AGENTLEDGER_DEBUG;
function trace(msg) {
  if (DEBUG) {
    process.stderr.write(`[agentledger:stop] ${msg}
`);
    try {
      import_fs9.default.appendFileSync(
        import_path97.default.join(projectDir2, ".agentledger", "stop-debug.log"),
        `${(/* @__PURE__ */ new Date()).toISOString()} ${msg}
`
      );
    } catch {
    }
  }
}
function loadConfig() {
  try {
    let raw = import_fs9.default.readFileSync(configPath, "utf8"), config = JSON.parse(raw);
    return {
      blockedFiles: Array.isArray(config.blockedFiles) ? config.blockedFiles : ["**/.env", "**/secrets.*", "**/*.pem", "**/*.key"],
      testCommand: typeof config.testCommand == "string" ? config.testCommand : "npm test",
      claimDetection: config.claimDetection !== !1
      // default on
    };
  } catch {
    return {
      blockedFiles: ["**/.env", "**/secrets.*", "**/*.pem", "**/*.key"],
      testCommand: "npm test",
      claimDetection: !0
    };
  }
}
function readLastAssistantMessage(transcriptPath) {
  try {
    let lines = import_fs9.default.readFileSync(transcriptPath, "utf8").trim().split(`
`).filter(Boolean);
    for (let i = lines.length - 1; i >= 0; i--)
      try {
        let entry = JSON.parse(lines[i]);
        if (entry.type === "assistant") {
          let content = entry.message?.content;
          if (typeof content == "string") return content;
          if (Array.isArray(content))
            return content.filter((b) => b.type === "text").map((b) => b.text).join(`
`);
        }
      } catch {
        continue;
      }
  } catch {
  }
  return "";
}
function extractAssistantMessage(input) {
  return typeof input?.last_assistant_message == "string" && input.last_assistant_message ? input.last_assistant_message : input?.transcript_path ? readLastAssistantMessage(String(input.transcript_path)) : "";
}
function isDebounced(recentVerifications, claimType) {
  let last = recentVerifications?.[claimType];
  return last ? Date.now() - last < DEBOUNCE_MS : !1;
}
async function appendClaimEvent(runId, eventType, payload) {
  if (!runId)
    return process.stderr.write(
      `[agentledger] cannot record ${eventType}: no run_id in session state
`
    ), !1;
  try {
    let { LedgerWriter: LedgerWriter2 } = await Promise.resolve().then(() => (init_dist(), dist_exports));
    return await new LedgerWriter2(ledgerPath2).appendEvent({
      event_id: `evt_${Date.now()}_${Math.random().toString(36).slice(2, 7)}`,
      run_id: runId,
      timestamp: (/* @__PURE__ */ new Date()).toISOString(),
      actor: "plugin:stop",
      event_type: eventType,
      payload
    }), !0;
  } catch (err) {
    return process.stderr.write(
      `[agentledger] FAILED to record ${eventType} to the ledger: ${err?.message ?? err}
`
    ), !1;
  }
}
async function main() {
  trace(`entry: projectDir=${projectDir2} cwd=${process.cwd()}`);
  let config = loadConfig();
  config.claimDetection || (trace("EXIT guard=claimDetection-disabled"), process.exit(0));
  let input;
  try {
    let raw = import_fs9.default.readFileSync(0, "utf8");
    trace(`stdin: ${raw.length} bytes, keys=${Object.keys(JSON.parse(raw)).join(",")}`), DEBUG && import_fs9.default.writeFileSync(import_path97.default.join(projectDir2, ".agentledger", "last-stop-payload.json"), raw), input = JSON.parse(raw);
  } catch (err) {
    trace(`EXIT guard=stdin-unreadable: ${err?.message}`), process.exit(0);
  }
  let message = extractAssistantMessage(input);
  message || (trace(`EXIT guard=no-message (transcript_path=${input?.transcript_path ?? "ABSENT"})`), process.exit(0)), trace(`message: ${message.length} chars: ${JSON.stringify(message.slice(0, 120))}`);
  let claims = detectClaims(message);
  claims.length === 0 && (trace("EXIT guard=no-claims-detected"), process.exit(0)), trace(`claims: ${claims.map((c) => `${c.type}:"${c.text}"`).join(", ")}`);
  let state = await readSessionState();
  trace(
    `state: runId=${state.runId} edits=${state.edits} writes=${state.writes} bash=${state.bashCalls} recentVerifications=${JSON.stringify(state.recentVerifications ?? {})}`
  );
  let freshClaims = claims.filter((c) => !isDebounced(state.recentVerifications, c.type));
  freshClaims.length === 0 && (trace("EXIT guard=debounced"), process.exit(0));
  let toolCalls = (state.reads ?? 0) + (state.edits ?? 0) + (state.writes ?? 0) + (state.bashCalls ?? 0);
  toolCalls === 0 && (trace("EXIT guard=no-tool-calls (nothing happened this session)"), process.exit(0)), trace(`all guards passed (${toolCalls} tool calls) \u2192 verifying`);
  let hadRun = !!state.runId;
  state = await ensureRun(state, "plugin:stop"), hadRun || await writeSessionState(state), trace(`run: ${state.runId}${hadRun ? " (existing)" : " (created by stop)"}`);
  let hasTestCommand = !!config.testCommand, { verifiable, unverifiable } = classifyClaims(freshClaims, hasTestCommand);
  for (let claim of freshClaims)
    await appendClaimEvent(state.runId, "CLAIM_DETECTED", {
      claim_text: claim.text,
      claim_type: claim.type,
      matched_pattern: claim.matchedPattern,
      verifiable: verifiable.includes(claim)
    });
  let stateUpdates = {
    claimsDetected: (state.claimsDetected ?? 0) + freshClaims.length,
    claimsUnverifiable: state.claimsUnverifiable ?? 0,
    claimsVerifiedTrue: state.claimsVerifiedTrue ?? 0,
    claimsVerifiedFalse: state.claimsVerifiedFalse ?? 0,
    falseClaims: [...state.falseClaims ?? []],
    recentVerifications: { ...state.recentVerifications ?? {} }
  };
  for (let claim of unverifiable)
    await appendClaimEvent(state.runId, "CLAIM_UNVERIFIABLE", {
      claim_text: claim.text,
      claim_type: claim.type,
      reason: "no test command configured"
    }), stateUpdates = {
      ...stateUpdates,
      claimsUnverifiable: stateUpdates.claimsUnverifiable + 1
    };
  if (verifiable.length > 0) {
    let result = verify({
      testCommand: config.testCommand,
      blockedFiles: config.blockedFiles,
      projectDir: projectDir2,
      testTimeout: TEST_TIMEOUT_MS
    });
    for (let claim of verifiable)
      if (stateUpdates = {
        ...stateUpdates,
        recentVerifications: { ...stateUpdates.recentVerifications, [claim.type]: Date.now() }
      }, result.testsPassed && result.boundaryClean)
        await appendClaimEvent(state.runId, "CLAIM_VERIFIED", {
          claim_text: claim.text,
          claim_type: claim.type,
          verification: {
            test_exit_code: result.testExitCode,
            boundary_clean: result.boundaryClean
          }
        }), process.stderr.write(
          `\u2713 CLAIM CHECK: Claude said "${claim.text}" \u2192 verified: tests pass, no violations
`
        ), stateUpdates = {
          ...stateUpdates,
          claimsVerifiedTrue: stateUpdates.claimsVerifiedTrue + 1
        };
      else {
        let actualParts = [];
        result.testsPassed || actualParts.push(`${config.testCommand} exit ${result.testExitCode}`), result.boundaryClean || actualParts.push(`${result.violations.length} boundary violation(s)`);
        let actual = actualParts.join(", ");
        await appendClaimEvent(state.runId, "CLAIM_FALSIFIED", {
          claim_text: claim.text,
          claim_type: claim.type,
          expected: claim.text,
          actual,
          verification: {
            test_exit_code: result.testExitCode,
            boundary_clean: result.boundaryClean,
            violations: result.violations
          }
        }), process.stderr.write(
          `\u26A0 CLAIM CHECK: Claude said "${claim.text}" \u2192 actual: ${actual}
`
        ), stateUpdates = {
          ...stateUpdates,
          claimsVerifiedFalse: stateUpdates.claimsVerifiedFalse + 1,
          falseClaims: [...stateUpdates.falseClaims, {
            claim: claim.text,
            actual,
            timestamp: (/* @__PURE__ */ new Date()).toISOString()
          }]
        };
      }
  }
  await writeSessionState({ ...state, ...stateUpdates }), process.exit(0);
}
main().catch((err) => {
  process.stderr.write(`[agentledger] stop hook error: ${err?.message}
`), process.exit(0);
});
